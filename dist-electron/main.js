import aE, { ipcMain as Re, dialog as Vt, app as Zt, safeStorage as Pn, net as mr, protocol as rw, shell as lE, screen as Gt, BrowserWindow as Oi } from "electron";
import Ee from "node:path";
import nw, { fileURLToPath as uE } from "node:url";
import * as iw from "fs";
import Lt from "fs";
import fE from "constants";
import tr from "stream";
import It from "util";
import cE from "assert";
import * as Ni from "path";
import Ue from "path";
import hc from "node:http";
import sw from "node:https";
import { createRequire as ow } from "node:module";
import aw from "node:os";
import dE from "node:dns";
import hE, { spawn as lw } from "child_process";
import Li from "os";
import Ke from "events";
import pE from "http";
import gE from "https";
import uw from "node:crypto";
import Fe from "buffer";
import yE, { createWriteStream as bE } from "node:fs";
import _E from "node:fs/promises";
import wE from "node:events";
import mE from "node:stream";
import SE from "node:string_decoder";
import Pi from "zlib";
import EE from "crypto";
import { WebSocketServer as vE, WebSocket as RE } from "ws";
var Tt = typeof globalThis < "u" ? globalThis : typeof window < "u" ? window : typeof global < "u" ? global : typeof self < "u" ? self : {};
function Ci(t) {
  return t && t.__esModule && Object.prototype.hasOwnProperty.call(t, "default") ? t.default : t;
}
function fw(t) {
  if (Object.prototype.hasOwnProperty.call(t, "__esModule")) return t;
  var e = t.default;
  if (typeof e == "function") {
    var r = function o() {
      var a = !1;
      try {
        a = this instanceof o;
      } catch {
      }
      return a ? Reflect.construct(e, arguments, this.constructor) : e.apply(this, arguments);
    };
    r.prototype = e.prototype;
  } else r = {};
  return Object.defineProperty(r, "__esModule", { value: !0 }), Object.keys(t).forEach(function(o) {
    var a = Object.getOwnPropertyDescriptor(t, o);
    Object.defineProperty(r, o, a.get ? a : {
      enumerable: !0,
      get: function() {
        return t[o];
      }
    });
  }), r;
}
var gs = {}, Cn = {}, rd;
function at() {
  return rd || (rd = 1, Cn.fromCallback = function(t) {
    return Object.defineProperty(function(...e) {
      if (typeof e[e.length - 1] == "function") t.apply(this, e);
      else
        return new Promise((r, o) => {
          e.push((a, n) => a != null ? o(a) : r(n)), t.apply(this, e);
        });
    }, "name", { value: t.name });
  }, Cn.fromPromise = function(t) {
    return Object.defineProperty(function(...e) {
      const r = e[e.length - 1];
      if (typeof r != "function") return t.apply(this, e);
      e.pop(), t.apply(this, e).then((o) => r(null, o), r);
    }, "name", { value: t.name });
  }), Cn;
}
var ys, nd;
function AE() {
  if (nd) return ys;
  nd = 1;
  var t = fE, e = process.cwd, r = null, o = process.env.GRACEFUL_FS_PLATFORM || process.platform;
  process.cwd = function() {
    return r || (r = e.call(process)), r;
  };
  try {
    process.cwd();
  } catch {
  }
  if (typeof process.chdir == "function") {
    var a = process.chdir;
    process.chdir = function(i) {
      r = null, a.call(process, i);
    }, Object.setPrototypeOf && Object.setPrototypeOf(process.chdir, a);
  }
  ys = n;
  function n(i) {
    t.hasOwnProperty("O_SYMLINK") && process.version.match(/^v0\.6\.[0-2]|^v0\.5\./) && l(i), i.lutimes || s(i), i.chown = y(i.chown), i.fchown = y(i.fchown), i.lchown = y(i.lchown), i.chmod = f(i.chmod), i.fchmod = f(i.fchmod), i.lchmod = f(i.lchmod), i.chownSync = R(i.chownSync), i.fchownSync = R(i.fchownSync), i.lchownSync = R(i.lchownSync), i.chmodSync = d(i.chmodSync), i.fchmodSync = d(i.fchmodSync), i.lchmodSync = d(i.lchmodSync), i.stat = w(i.stat), i.fstat = w(i.fstat), i.lstat = w(i.lstat), i.statSync = I(i.statSync), i.fstatSync = I(i.fstatSync), i.lstatSync = I(i.lstatSync), i.chmod && !i.lchmod && (i.lchmod = function(u, g, p) {
      p && process.nextTick(p);
    }, i.lchmodSync = function() {
    }), i.chown && !i.lchown && (i.lchown = function(u, g, p, b) {
      b && process.nextTick(b);
    }, i.lchownSync = function() {
    }), o === "win32" && (i.rename = typeof i.rename != "function" ? i.rename : (function(u) {
      function g(p, b, C) {
        var N = Date.now(), W = 0;
        u(p, b, function k(L) {
          if (L && (L.code === "EACCES" || L.code === "EPERM" || L.code === "EBUSY") && Date.now() - N < 6e4) {
            setTimeout(function() {
              i.stat(b, function(U, x) {
                U && U.code === "ENOENT" ? u(p, b, k) : C(L);
              });
            }, W), W < 100 && (W += 10);
            return;
          }
          C && C(L);
        });
      }
      return Object.setPrototypeOf && Object.setPrototypeOf(g, u), g;
    })(i.rename)), i.read = typeof i.read != "function" ? i.read : (function(u) {
      function g(p, b, C, N, W, k) {
        var L;
        if (k && typeof k == "function") {
          var U = 0;
          L = function(x, D, F) {
            if (x && x.code === "EAGAIN" && U < 10)
              return U++, u.call(i, p, b, C, N, W, L);
            k.apply(this, arguments);
          };
        }
        return u.call(i, p, b, C, N, W, L);
      }
      return Object.setPrototypeOf && Object.setPrototypeOf(g, u), g;
    })(i.read), i.readSync = typeof i.readSync != "function" ? i.readSync : /* @__PURE__ */ (function(u) {
      return function(g, p, b, C, N) {
        for (var W = 0; ; )
          try {
            return u.call(i, g, p, b, C, N);
          } catch (k) {
            if (k.code === "EAGAIN" && W < 10) {
              W++;
              continue;
            }
            throw k;
          }
      };
    })(i.readSync);
    function l(u) {
      u.lchmod = function(g, p, b) {
        u.open(
          g,
          t.O_WRONLY | t.O_SYMLINK,
          p,
          function(C, N) {
            if (C) {
              b && b(C);
              return;
            }
            u.fchmod(N, p, function(W) {
              u.close(N, function(k) {
                b && b(W || k);
              });
            });
          }
        );
      }, u.lchmodSync = function(g, p) {
        var b = u.openSync(g, t.O_WRONLY | t.O_SYMLINK, p), C = !0, N;
        try {
          N = u.fchmodSync(b, p), C = !1;
        } finally {
          if (C)
            try {
              u.closeSync(b);
            } catch {
            }
          else
            u.closeSync(b);
        }
        return N;
      };
    }
    function s(u) {
      t.hasOwnProperty("O_SYMLINK") && u.futimes ? (u.lutimes = function(g, p, b, C) {
        u.open(g, t.O_SYMLINK, function(N, W) {
          if (N) {
            C && C(N);
            return;
          }
          u.futimes(W, p, b, function(k) {
            u.close(W, function(L) {
              C && C(k || L);
            });
          });
        });
      }, u.lutimesSync = function(g, p, b) {
        var C = u.openSync(g, t.O_SYMLINK), N, W = !0;
        try {
          N = u.futimesSync(C, p, b), W = !1;
        } finally {
          if (W)
            try {
              u.closeSync(C);
            } catch {
            }
          else
            u.closeSync(C);
        }
        return N;
      }) : u.futimes && (u.lutimes = function(g, p, b, C) {
        C && process.nextTick(C);
      }, u.lutimesSync = function() {
      });
    }
    function f(u) {
      return u && function(g, p, b) {
        return u.call(i, g, p, function(C) {
          A(C) && (C = null), b && b.apply(this, arguments);
        });
      };
    }
    function d(u) {
      return u && function(g, p) {
        try {
          return u.call(i, g, p);
        } catch (b) {
          if (!A(b)) throw b;
        }
      };
    }
    function y(u) {
      return u && function(g, p, b, C) {
        return u.call(i, g, p, b, function(N) {
          A(N) && (N = null), C && C.apply(this, arguments);
        });
      };
    }
    function R(u) {
      return u && function(g, p, b) {
        try {
          return u.call(i, g, p, b);
        } catch (C) {
          if (!A(C)) throw C;
        }
      };
    }
    function w(u) {
      return u && function(g, p, b) {
        typeof p == "function" && (b = p, p = null);
        function C(N, W) {
          W && (W.uid < 0 && (W.uid += 4294967296), W.gid < 0 && (W.gid += 4294967296)), b && b.apply(this, arguments);
        }
        return p ? u.call(i, g, p, C) : u.call(i, g, C);
      };
    }
    function I(u) {
      return u && function(g, p) {
        var b = p ? u.call(i, g, p) : u.call(i, g);
        return b && (b.uid < 0 && (b.uid += 4294967296), b.gid < 0 && (b.gid += 4294967296)), b;
      };
    }
    function A(u) {
      if (!u || u.code === "ENOSYS")
        return !0;
      var g = !process.getuid || process.getuid() !== 0;
      return !!(g && (u.code === "EINVAL" || u.code === "EPERM"));
    }
  }
  return ys;
}
var bs, id;
function TE() {
  if (id) return bs;
  id = 1;
  var t = tr.Stream;
  bs = e;
  function e(r) {
    return {
      ReadStream: o,
      WriteStream: a
    };
    function o(n, i) {
      if (!(this instanceof o)) return new o(n, i);
      t.call(this);
      var l = this;
      this.path = n, this.fd = null, this.readable = !0, this.paused = !1, this.flags = "r", this.mode = 438, this.bufferSize = 64 * 1024, i = i || {};
      for (var s = Object.keys(i), f = 0, d = s.length; f < d; f++) {
        var y = s[f];
        this[y] = i[y];
      }
      if (this.encoding && this.setEncoding(this.encoding), this.start !== void 0) {
        if (typeof this.start != "number")
          throw TypeError("start must be a Number");
        if (this.end === void 0)
          this.end = 1 / 0;
        else if (typeof this.end != "number")
          throw TypeError("end must be a Number");
        if (this.start > this.end)
          throw new Error("start must be <= end");
        this.pos = this.start;
      }
      if (this.fd !== null) {
        process.nextTick(function() {
          l._read();
        });
        return;
      }
      r.open(this.path, this.flags, this.mode, function(R, w) {
        if (R) {
          l.emit("error", R), l.readable = !1;
          return;
        }
        l.fd = w, l.emit("open", w), l._read();
      });
    }
    function a(n, i) {
      if (!(this instanceof a)) return new a(n, i);
      t.call(this), this.path = n, this.fd = null, this.writable = !0, this.flags = "w", this.encoding = "binary", this.mode = 438, this.bytesWritten = 0, i = i || {};
      for (var l = Object.keys(i), s = 0, f = l.length; s < f; s++) {
        var d = l[s];
        this[d] = i[d];
      }
      if (this.start !== void 0) {
        if (typeof this.start != "number")
          throw TypeError("start must be a Number");
        if (this.start < 0)
          throw new Error("start must be >= zero");
        this.pos = this.start;
      }
      this.busy = !1, this._queue = [], this.fd === null && (this._open = r.open, this._queue.push([this._open, this.path, this.flags, this.mode, void 0]), this.flush());
    }
  }
  return bs;
}
var _s, sd;
function xE() {
  if (sd) return _s;
  sd = 1, _s = e;
  var t = Object.getPrototypeOf || function(r) {
    return r.__proto__;
  };
  function e(r) {
    if (r === null || typeof r != "object")
      return r;
    if (r instanceof Object)
      var o = { __proto__: t(r) };
    else
      var o = /* @__PURE__ */ Object.create(null);
    return Object.getOwnPropertyNames(r).forEach(function(a) {
      Object.defineProperty(o, a, Object.getOwnPropertyDescriptor(r, a));
    }), o;
  }
  return _s;
}
var Mn, od;
function Rr() {
  if (od) return Mn;
  od = 1;
  var t = Lt, e = AE(), r = TE(), o = xE(), a = It, n, i;
  typeof Symbol == "function" && typeof Symbol.for == "function" ? (n = /* @__PURE__ */ Symbol.for("graceful-fs.queue"), i = /* @__PURE__ */ Symbol.for("graceful-fs.previous")) : (n = "___graceful-fs.queue", i = "___graceful-fs.previous");
  function l() {
  }
  function s(u, g) {
    Object.defineProperty(u, n, {
      get: function() {
        return g;
      }
    });
  }
  var f = l;
  if (a.debuglog ? f = a.debuglog("gfs4") : /\bgfs4\b/i.test(process.env.NODE_DEBUG || "") && (f = function() {
    var u = a.format.apply(a, arguments);
    u = "GFS4: " + u.split(/\n/).join(`
GFS4: `), console.error(u);
  }), !t[n]) {
    var d = Tt[n] || [];
    s(t, d), t.close = (function(u) {
      function g(p, b) {
        return u.call(t, p, function(C) {
          C || I(), typeof b == "function" && b.apply(this, arguments);
        });
      }
      return Object.defineProperty(g, i, {
        value: u
      }), g;
    })(t.close), t.closeSync = (function(u) {
      function g(p) {
        u.apply(t, arguments), I();
      }
      return Object.defineProperty(g, i, {
        value: u
      }), g;
    })(t.closeSync), /\bgfs4\b/i.test(process.env.NODE_DEBUG || "") && process.on("exit", function() {
      f(t[n]), cE.equal(t[n].length, 0);
    });
  }
  Tt[n] || s(Tt, t[n]), Mn = y(o(t)), process.env.TEST_GRACEFUL_FS_GLOBAL_PATCH && !t.__patched && (Mn = y(t), t.__patched = !0);
  function y(u) {
    e(u), u.gracefulify = y, u.createReadStream = $, u.createWriteStream = ee;
    var g = u.readFile;
    u.readFile = p;
    function p(ae, ce, Y) {
      return typeof ce == "function" && (Y = ce, ce = null), P(ae, ce, Y);
      function P(q, V, Z, X) {
        return g(q, V, function(m) {
          m && (m.code === "EMFILE" || m.code === "ENFILE") ? R([P, [q, V, Z], m, X || Date.now(), Date.now()]) : typeof Z == "function" && Z.apply(this, arguments);
        });
      }
    }
    var b = u.writeFile;
    u.writeFile = C;
    function C(ae, ce, Y, P) {
      return typeof Y == "function" && (P = Y, Y = null), q(ae, ce, Y, P);
      function q(V, Z, X, m, c) {
        return b(V, Z, X, function(E) {
          E && (E.code === "EMFILE" || E.code === "ENFILE") ? R([q, [V, Z, X, m], E, c || Date.now(), Date.now()]) : typeof m == "function" && m.apply(this, arguments);
        });
      }
    }
    var N = u.appendFile;
    N && (u.appendFile = W);
    function W(ae, ce, Y, P) {
      return typeof Y == "function" && (P = Y, Y = null), q(ae, ce, Y, P);
      function q(V, Z, X, m, c) {
        return N(V, Z, X, function(E) {
          E && (E.code === "EMFILE" || E.code === "ENFILE") ? R([q, [V, Z, X, m], E, c || Date.now(), Date.now()]) : typeof m == "function" && m.apply(this, arguments);
        });
      }
    }
    var k = u.copyFile;
    k && (u.copyFile = L);
    function L(ae, ce, Y, P) {
      return typeof Y == "function" && (P = Y, Y = 0), q(ae, ce, Y, P);
      function q(V, Z, X, m, c) {
        return k(V, Z, X, function(E) {
          E && (E.code === "EMFILE" || E.code === "ENFILE") ? R([q, [V, Z, X, m], E, c || Date.now(), Date.now()]) : typeof m == "function" && m.apply(this, arguments);
        });
      }
    }
    var U = u.readdir;
    u.readdir = D;
    var x = /^v[0-5]\./;
    function D(ae, ce, Y) {
      typeof ce == "function" && (Y = ce, ce = null);
      var P = x.test(process.version) ? function(Z, X, m, c) {
        return U(Z, q(
          Z,
          X,
          m,
          c
        ));
      } : function(Z, X, m, c) {
        return U(Z, X, q(
          Z,
          X,
          m,
          c
        ));
      };
      return P(ae, ce, Y);
      function q(V, Z, X, m) {
        return function(c, E) {
          c && (c.code === "EMFILE" || c.code === "ENFILE") ? R([
            P,
            [V, Z, X],
            c,
            m || Date.now(),
            Date.now()
          ]) : (E && E.sort && E.sort(), typeof X == "function" && X.call(this, c, E));
        };
      }
    }
    if (process.version.substr(0, 4) === "v0.8") {
      var F = r(u);
      ne = F.ReadStream, Q = F.WriteStream;
    }
    var T = u.ReadStream;
    T && (ne.prototype = Object.create(T.prototype), ne.prototype.open = M);
    var J = u.WriteStream;
    J && (Q.prototype = Object.create(J.prototype), Q.prototype.open = v), Object.defineProperty(u, "ReadStream", {
      get: function() {
        return ne;
      },
      set: function(ae) {
        ne = ae;
      },
      enumerable: !0,
      configurable: !0
    }), Object.defineProperty(u, "WriteStream", {
      get: function() {
        return Q;
      },
      set: function(ae) {
        Q = ae;
      },
      enumerable: !0,
      configurable: !0
    });
    var re = ne;
    Object.defineProperty(u, "FileReadStream", {
      get: function() {
        return re;
      },
      set: function(ae) {
        re = ae;
      },
      enumerable: !0,
      configurable: !0
    });
    var se = Q;
    Object.defineProperty(u, "FileWriteStream", {
      get: function() {
        return se;
      },
      set: function(ae) {
        se = ae;
      },
      enumerable: !0,
      configurable: !0
    });
    function ne(ae, ce) {
      return this instanceof ne ? (T.apply(this, arguments), this) : ne.apply(Object.create(ne.prototype), arguments);
    }
    function M() {
      var ae = this;
      le(ae.path, ae.flags, ae.mode, function(ce, Y) {
        ce ? (ae.autoClose && ae.destroy(), ae.emit("error", ce)) : (ae.fd = Y, ae.emit("open", Y), ae.read());
      });
    }
    function Q(ae, ce) {
      return this instanceof Q ? (J.apply(this, arguments), this) : Q.apply(Object.create(Q.prototype), arguments);
    }
    function v() {
      var ae = this;
      le(ae.path, ae.flags, ae.mode, function(ce, Y) {
        ce ? (ae.destroy(), ae.emit("error", ce)) : (ae.fd = Y, ae.emit("open", Y));
      });
    }
    function $(ae, ce) {
      return new u.ReadStream(ae, ce);
    }
    function ee(ae, ce) {
      return new u.WriteStream(ae, ce);
    }
    var B = u.open;
    u.open = le;
    function le(ae, ce, Y, P) {
      return typeof Y == "function" && (P = Y, Y = null), q(ae, ce, Y, P);
      function q(V, Z, X, m, c) {
        return B(V, Z, X, function(E, j) {
          E && (E.code === "EMFILE" || E.code === "ENFILE") ? R([q, [V, Z, X, m], E, c || Date.now(), Date.now()]) : typeof m == "function" && m.apply(this, arguments);
        });
      }
    }
    return u;
  }
  function R(u) {
    f("ENQUEUE", u[0].name, u[1]), t[n].push(u), A();
  }
  var w;
  function I() {
    for (var u = Date.now(), g = 0; g < t[n].length; ++g)
      t[n][g].length > 2 && (t[n][g][3] = u, t[n][g][4] = u);
    A();
  }
  function A() {
    if (clearTimeout(w), w = void 0, t[n].length !== 0) {
      var u = t[n].shift(), g = u[0], p = u[1], b = u[2], C = u[3], N = u[4];
      if (C === void 0)
        f("RETRY", g.name, p), g.apply(null, p);
      else if (Date.now() - C >= 6e4) {
        f("TIMEOUT", g.name, p);
        var W = p.pop();
        typeof W == "function" && W.call(null, b);
      } else {
        var k = Date.now() - N, L = Math.max(N - C, 1), U = Math.min(L * 1.2, 100);
        k >= U ? (f("RETRY", g.name, p), g.apply(null, p.concat([C]))) : t[n].push(u);
      }
      w === void 0 && (w = setTimeout(A, 0));
    }
  }
  return Mn;
}
var ad;
function _t() {
  return ad || (ad = 1, (function(t) {
    const e = at().fromCallback, r = Rr(), o = [
      "access",
      "appendFile",
      "chmod",
      "chown",
      "close",
      "copyFile",
      "cp",
      "fchmod",
      "fchown",
      "fdatasync",
      "fstat",
      "fsync",
      "ftruncate",
      "futimes",
      "glob",
      "lchmod",
      "lchown",
      "lutimes",
      "link",
      "lstat",
      "mkdir",
      "mkdtemp",
      "open",
      "opendir",
      "readdir",
      "readFile",
      "readlink",
      "realpath",
      "rename",
      "rm",
      "rmdir",
      "stat",
      "statfs",
      "symlink",
      "truncate",
      "unlink",
      "utimes",
      "writeFile"
    ].filter((a) => typeof r[a] == "function");
    Object.assign(t, r), o.forEach((a) => {
      t[a] = e(r[a]);
    }), t.exists = function(a, n) {
      return typeof n == "function" ? r.exists(a, n) : new Promise((i) => r.exists(a, i));
    }, t.read = function(a, n, i, l, s, f) {
      return typeof f == "function" ? r.read(a, n, i, l, s, f) : new Promise((d, y) => {
        r.read(a, n, i, l, s, (R, w, I) => {
          if (R) return y(R);
          d({ bytesRead: w, buffer: I });
        });
      });
    }, t.write = function(a, n, ...i) {
      return typeof i[i.length - 1] == "function" ? r.write(a, n, ...i) : new Promise((l, s) => {
        r.write(a, n, ...i, (f, d, y) => {
          if (f) return s(f);
          l({ bytesWritten: d, buffer: y });
        });
      });
    }, t.readv = function(a, n, ...i) {
      return typeof i[i.length - 1] == "function" ? r.readv(a, n, ...i) : new Promise((l, s) => {
        r.readv(a, n, ...i, (f, d, y) => {
          if (f) return s(f);
          l({ bytesRead: d, buffers: y });
        });
      });
    }, t.writev = function(a, n, ...i) {
      return typeof i[i.length - 1] == "function" ? r.writev(a, n, ...i) : new Promise((l, s) => {
        r.writev(a, n, ...i, (f, d, y) => {
          if (f) return s(f);
          l({ bytesWritten: d, buffers: y });
        });
      });
    }, typeof r.realpath.native == "function" ? t.realpath.native = e(r.realpath.native) : process.emitWarning(
      "fs.realpath.native is not a function. Is fs being monkey-patched?",
      "Warning",
      "fs-extra-WARN0003"
    );
  })(gs)), gs;
}
var kn = {}, ws = {}, ld;
function IE() {
  if (ld) return ws;
  ld = 1;
  const t = Ue;
  return ws.checkPath = function(r) {
    if (process.platform === "win32" && /[<>:"|?*]/.test(r.replace(t.parse(r).root, ""))) {
      const a = new Error(`Path contains invalid characters: ${r}`);
      throw a.code = "EINVAL", a;
    }
  }, ws;
}
var ud;
function DE() {
  if (ud) return kn;
  ud = 1;
  const t = /* @__PURE__ */ _t(), { checkPath: e } = /* @__PURE__ */ IE(), r = (o) => {
    const a = { mode: 511 };
    return typeof o == "number" ? o : { ...a, ...o }.mode;
  };
  return kn.makeDir = async (o, a) => (e(o), t.mkdir(o, {
    mode: r(a),
    recursive: !0
  })), kn.makeDirSync = (o, a) => (e(o), t.mkdirSync(o, {
    mode: r(a),
    recursive: !0
  })), kn;
}
var ms, fd;
function Ft() {
  if (fd) return ms;
  fd = 1;
  const t = at().fromPromise, { makeDir: e, makeDirSync: r } = /* @__PURE__ */ DE(), o = t(e);
  return ms = {
    mkdirs: o,
    mkdirsSync: r,
    // alias
    mkdirp: o,
    mkdirpSync: r,
    ensureDir: o,
    ensureDirSync: r
  }, ms;
}
var Ss, cd;
function Ar() {
  if (cd) return Ss;
  cd = 1;
  const t = at().fromPromise, e = /* @__PURE__ */ _t();
  function r(o) {
    return e.access(o).then(() => !0).catch(() => !1);
  }
  return Ss = {
    pathExists: t(r),
    pathExistsSync: e.existsSync
  }, Ss;
}
var Es, dd;
function cw() {
  if (dd) return Es;
  dd = 1;
  const t = /* @__PURE__ */ _t(), e = at().fromPromise;
  async function r(a, n, i) {
    const l = await t.open(a, "r+");
    let s = null;
    try {
      await t.futimes(l, n, i);
    } finally {
      try {
        await t.close(l);
      } catch (f) {
        s = f;
      }
    }
    if (s)
      throw s;
  }
  function o(a, n, i) {
    const l = t.openSync(a, "r+");
    return t.futimesSync(l, n, i), t.closeSync(l);
  }
  return Es = {
    utimesMillis: e(r),
    utimesMillisSync: o
  }, Es;
}
var vs, hd;
function Fr() {
  if (hd) return vs;
  hd = 1;
  const t = /* @__PURE__ */ _t(), e = Ue, r = at().fromPromise;
  function o(R, w, I) {
    const A = I.dereference ? (u) => t.stat(u, { bigint: !0 }) : (u) => t.lstat(u, { bigint: !0 });
    return Promise.all([
      A(R),
      A(w).catch((u) => {
        if (u.code === "ENOENT") return null;
        throw u;
      })
    ]).then(([u, g]) => ({ srcStat: u, destStat: g }));
  }
  function a(R, w, I) {
    let A;
    const u = I.dereference ? (p) => t.statSync(p, { bigint: !0 }) : (p) => t.lstatSync(p, { bigint: !0 }), g = u(R);
    try {
      A = u(w);
    } catch (p) {
      if (p.code === "ENOENT") return { srcStat: g, destStat: null };
      throw p;
    }
    return { srcStat: g, destStat: A };
  }
  async function n(R, w, I, A) {
    const { srcStat: u, destStat: g } = await o(R, w, A);
    if (g) {
      if (f(u, g)) {
        const p = e.basename(R), b = e.basename(w);
        if (I === "move" && p !== b && p.toLowerCase() === b.toLowerCase())
          return { srcStat: u, destStat: g, isChangingCase: !0 };
        throw new Error("Source and destination must not be the same.");
      }
      if (u.isDirectory() && !g.isDirectory())
        throw new Error(`Cannot overwrite non-directory '${w}' with directory '${R}'.`);
      if (!u.isDirectory() && g.isDirectory())
        throw new Error(`Cannot overwrite directory '${w}' with non-directory '${R}'.`);
    }
    if (u.isDirectory() && d(R, w))
      throw new Error(y(R, w, I));
    return { srcStat: u, destStat: g };
  }
  function i(R, w, I, A) {
    const { srcStat: u, destStat: g } = a(R, w, A);
    if (g) {
      if (f(u, g)) {
        const p = e.basename(R), b = e.basename(w);
        if (I === "move" && p !== b && p.toLowerCase() === b.toLowerCase())
          return { srcStat: u, destStat: g, isChangingCase: !0 };
        throw new Error("Source and destination must not be the same.");
      }
      if (u.isDirectory() && !g.isDirectory())
        throw new Error(`Cannot overwrite non-directory '${w}' with directory '${R}'.`);
      if (!u.isDirectory() && g.isDirectory())
        throw new Error(`Cannot overwrite directory '${w}' with non-directory '${R}'.`);
    }
    if (u.isDirectory() && d(R, w))
      throw new Error(y(R, w, I));
    return { srcStat: u, destStat: g };
  }
  async function l(R, w, I, A) {
    const u = e.resolve(e.dirname(R)), g = e.resolve(e.dirname(I));
    if (g === u || g === e.parse(g).root) return;
    let p;
    try {
      p = await t.stat(g, { bigint: !0 });
    } catch (b) {
      if (b.code === "ENOENT") return;
      throw b;
    }
    if (f(w, p))
      throw new Error(y(R, I, A));
    return l(R, w, g, A);
  }
  function s(R, w, I, A) {
    const u = e.resolve(e.dirname(R)), g = e.resolve(e.dirname(I));
    if (g === u || g === e.parse(g).root) return;
    let p;
    try {
      p = t.statSync(g, { bigint: !0 });
    } catch (b) {
      if (b.code === "ENOENT") return;
      throw b;
    }
    if (f(w, p))
      throw new Error(y(R, I, A));
    return s(R, w, g, A);
  }
  function f(R, w) {
    return w.ino !== void 0 && w.dev !== void 0 && w.ino === R.ino && w.dev === R.dev;
  }
  function d(R, w) {
    const I = e.resolve(R).split(e.sep).filter((u) => u), A = e.resolve(w).split(e.sep).filter((u) => u);
    return I.every((u, g) => A[g] === u);
  }
  function y(R, w, I) {
    return `Cannot ${I} '${R}' to a subdirectory of itself, '${w}'.`;
  }
  return vs = {
    // checkPaths
    checkPaths: r(n),
    checkPathsSync: i,
    // checkParent
    checkParentPaths: r(l),
    checkParentPathsSync: s,
    // Misc
    isSrcSubdir: d,
    areIdentical: f
  }, vs;
}
var Rs, pd;
function OE() {
  if (pd) return Rs;
  pd = 1;
  async function t(e, r) {
    const o = [];
    for await (const a of e)
      o.push(
        r(a).then(
          () => null,
          (n) => n ?? new Error("unknown error")
        )
      );
    await Promise.all(
      o.map(
        (a) => a.then((n) => {
          if (n !== null) throw n;
        })
      )
    );
  }
  return Rs = {
    asyncIteratorConcurrentProcess: t
  }, Rs;
}
var As, gd;
function NE() {
  if (gd) return As;
  gd = 1;
  const t = /* @__PURE__ */ _t(), e = Ue, { mkdirs: r } = /* @__PURE__ */ Ft(), { pathExists: o } = /* @__PURE__ */ Ar(), { utimesMillis: a } = /* @__PURE__ */ cw(), n = /* @__PURE__ */ Fr(), { asyncIteratorConcurrentProcess: i } = /* @__PURE__ */ OE();
  async function l(u, g, p = {}) {
    typeof p == "function" && (p = { filter: p }), p.clobber = "clobber" in p ? !!p.clobber : !0, p.overwrite = "overwrite" in p ? !!p.overwrite : p.clobber, p.preserveTimestamps && process.arch === "ia32" && process.emitWarning(
      `Using the preserveTimestamps option in 32-bit node is not recommended;

	see https://github.com/jprichardson/node-fs-extra/issues/269`,
      "Warning",
      "fs-extra-WARN0001"
    );
    const { srcStat: b, destStat: C } = await n.checkPaths(u, g, "copy", p);
    if (await n.checkParentPaths(u, b, g, "copy"), !await s(u, g, p)) return;
    const W = e.dirname(g);
    await o(W) || await r(W), await f(C, u, g, p);
  }
  async function s(u, g, p) {
    return p.filter ? p.filter(u, g) : !0;
  }
  async function f(u, g, p, b) {
    const N = await (b.dereference ? t.stat : t.lstat)(g);
    if (N.isDirectory()) return I(N, u, g, p, b);
    if (N.isFile() || N.isCharacterDevice() || N.isBlockDevice()) return d(N, u, g, p, b);
    if (N.isSymbolicLink()) return A(u, g, p, b);
    throw N.isSocket() ? new Error(`Cannot copy a socket file: ${g}`) : N.isFIFO() ? new Error(`Cannot copy a FIFO pipe: ${g}`) : new Error(`Unknown file: ${g}`);
  }
  async function d(u, g, p, b, C) {
    if (!g) return y(u, p, b, C);
    if (C.overwrite)
      return await t.unlink(b), y(u, p, b, C);
    if (C.errorOnExist)
      throw new Error(`'${b}' already exists`);
  }
  async function y(u, g, p, b) {
    if (await t.copyFile(g, p), b.preserveTimestamps) {
      R(u.mode) && await w(p, u.mode);
      const C = await t.stat(g);
      await a(p, C.atime, C.mtime);
    }
    return t.chmod(p, u.mode);
  }
  function R(u) {
    return (u & 128) === 0;
  }
  function w(u, g) {
    return t.chmod(u, g | 128);
  }
  async function I(u, g, p, b, C) {
    g || await t.mkdir(b), await i(await t.opendir(p), async (N) => {
      const W = e.join(p, N.name), k = e.join(b, N.name);
      if (await s(W, k, C)) {
        const { destStat: U } = await n.checkPaths(W, k, "copy", C);
        await f(U, W, k, C);
      }
    }), g || await t.chmod(b, u.mode);
  }
  async function A(u, g, p, b) {
    let C = await t.readlink(g);
    if (b.dereference && (C = e.resolve(process.cwd(), C)), !u)
      return t.symlink(C, p);
    let N = null;
    try {
      N = await t.readlink(p);
    } catch (W) {
      if (W.code === "EINVAL" || W.code === "UNKNOWN") return t.symlink(C, p);
      throw W;
    }
    if (b.dereference && (N = e.resolve(process.cwd(), N)), C !== N) {
      if (n.isSrcSubdir(C, N))
        throw new Error(`Cannot copy '${C}' to a subdirectory of itself, '${N}'.`);
      if (n.isSrcSubdir(N, C))
        throw new Error(`Cannot overwrite '${N}' with '${C}'.`);
    }
    return await t.unlink(p), t.symlink(C, p);
  }
  return As = l, As;
}
var Ts, yd;
function LE() {
  if (yd) return Ts;
  yd = 1;
  const t = Rr(), e = Ue, r = Ft().mkdirsSync, o = cw().utimesMillisSync, a = /* @__PURE__ */ Fr();
  function n(N, W, k) {
    typeof k == "function" && (k = { filter: k }), k = k || {}, k.clobber = "clobber" in k ? !!k.clobber : !0, k.overwrite = "overwrite" in k ? !!k.overwrite : k.clobber, k.preserveTimestamps && process.arch === "ia32" && process.emitWarning(
      `Using the preserveTimestamps option in 32-bit node is not recommended;

	see https://github.com/jprichardson/node-fs-extra/issues/269`,
      "Warning",
      "fs-extra-WARN0002"
    );
    const { srcStat: L, destStat: U } = a.checkPathsSync(N, W, "copy", k);
    if (a.checkParentPathsSync(N, L, W, "copy"), k.filter && !k.filter(N, W)) return;
    const x = e.dirname(W);
    return t.existsSync(x) || r(x), i(U, N, W, k);
  }
  function i(N, W, k, L) {
    const x = (L.dereference ? t.statSync : t.lstatSync)(W);
    if (x.isDirectory()) return A(x, N, W, k, L);
    if (x.isFile() || x.isCharacterDevice() || x.isBlockDevice()) return l(x, N, W, k, L);
    if (x.isSymbolicLink()) return b(N, W, k, L);
    throw x.isSocket() ? new Error(`Cannot copy a socket file: ${W}`) : x.isFIFO() ? new Error(`Cannot copy a FIFO pipe: ${W}`) : new Error(`Unknown file: ${W}`);
  }
  function l(N, W, k, L, U) {
    return W ? s(N, k, L, U) : f(N, k, L, U);
  }
  function s(N, W, k, L) {
    if (L.overwrite)
      return t.unlinkSync(k), f(N, W, k, L);
    if (L.errorOnExist)
      throw new Error(`'${k}' already exists`);
  }
  function f(N, W, k, L) {
    return t.copyFileSync(W, k), L.preserveTimestamps && d(N.mode, W, k), w(k, N.mode);
  }
  function d(N, W, k) {
    return y(N) && R(k, N), I(W, k);
  }
  function y(N) {
    return (N & 128) === 0;
  }
  function R(N, W) {
    return w(N, W | 128);
  }
  function w(N, W) {
    return t.chmodSync(N, W);
  }
  function I(N, W) {
    const k = t.statSync(N);
    return o(W, k.atime, k.mtime);
  }
  function A(N, W, k, L, U) {
    return W ? g(k, L, U) : u(N.mode, k, L, U);
  }
  function u(N, W, k, L) {
    return t.mkdirSync(k), g(W, k, L), w(k, N);
  }
  function g(N, W, k) {
    const L = t.opendirSync(N);
    try {
      let U;
      for (; (U = L.readSync()) !== null; )
        p(U.name, N, W, k);
    } finally {
      L.closeSync();
    }
  }
  function p(N, W, k, L) {
    const U = e.join(W, N), x = e.join(k, N);
    if (L.filter && !L.filter(U, x)) return;
    const { destStat: D } = a.checkPathsSync(U, x, "copy", L);
    return i(D, U, x, L);
  }
  function b(N, W, k, L) {
    let U = t.readlinkSync(W);
    if (L.dereference && (U = e.resolve(process.cwd(), U)), N) {
      let x;
      try {
        x = t.readlinkSync(k);
      } catch (D) {
        if (D.code === "EINVAL" || D.code === "UNKNOWN") return t.symlinkSync(U, k);
        throw D;
      }
      if (L.dereference && (x = e.resolve(process.cwd(), x)), U !== x) {
        if (a.isSrcSubdir(U, x))
          throw new Error(`Cannot copy '${U}' to a subdirectory of itself, '${x}'.`);
        if (a.isSrcSubdir(x, U))
          throw new Error(`Cannot overwrite '${x}' with '${U}'.`);
      }
      return C(U, k);
    } else
      return t.symlinkSync(U, k);
  }
  function C(N, W) {
    return t.unlinkSync(W), t.symlinkSync(N, W);
  }
  return Ts = n, Ts;
}
var xs, bd;
function pc() {
  if (bd) return xs;
  bd = 1;
  const t = at().fromPromise;
  return xs = {
    copy: t(/* @__PURE__ */ NE()),
    copySync: /* @__PURE__ */ LE()
  }, xs;
}
var Is, _d;
function Mi() {
  if (_d) return Is;
  _d = 1;
  const t = Rr(), e = at().fromCallback;
  function r(a, n) {
    t.rm(a, { recursive: !0, force: !0 }, n);
  }
  function o(a) {
    t.rmSync(a, { recursive: !0, force: !0 });
  }
  return Is = {
    remove: e(r),
    removeSync: o
  }, Is;
}
var Ds, wd;
function PE() {
  if (wd) return Ds;
  wd = 1;
  const t = at().fromPromise, e = /* @__PURE__ */ _t(), r = Ue, o = /* @__PURE__ */ Ft(), a = /* @__PURE__ */ Mi(), n = t(async function(s) {
    let f;
    try {
      f = await e.readdir(s);
    } catch {
      return o.mkdirs(s);
    }
    return Promise.all(f.map((d) => a.remove(r.join(s, d))));
  });
  function i(l) {
    let s;
    try {
      s = e.readdirSync(l);
    } catch {
      return o.mkdirsSync(l);
    }
    s.forEach((f) => {
      f = r.join(l, f), a.removeSync(f);
    });
  }
  return Ds = {
    emptyDirSync: i,
    emptydirSync: i,
    emptyDir: n,
    emptydir: n
  }, Ds;
}
var Os, md;
function CE() {
  if (md) return Os;
  md = 1;
  const t = at().fromPromise, e = Ue, r = /* @__PURE__ */ _t(), o = /* @__PURE__ */ Ft();
  async function a(i) {
    let l;
    try {
      l = await r.stat(i);
    } catch {
    }
    if (l && l.isFile()) return;
    const s = e.dirname(i);
    let f = null;
    try {
      f = await r.stat(s);
    } catch (d) {
      if (d.code === "ENOENT") {
        await o.mkdirs(s), await r.writeFile(i, "");
        return;
      } else
        throw d;
    }
    f.isDirectory() ? await r.writeFile(i, "") : await r.readdir(s);
  }
  function n(i) {
    let l;
    try {
      l = r.statSync(i);
    } catch {
    }
    if (l && l.isFile()) return;
    const s = e.dirname(i);
    try {
      r.statSync(s).isDirectory() || r.readdirSync(s);
    } catch (f) {
      if (f && f.code === "ENOENT") o.mkdirsSync(s);
      else throw f;
    }
    r.writeFileSync(i, "");
  }
  return Os = {
    createFile: t(a),
    createFileSync: n
  }, Os;
}
var Ns, Sd;
function ME() {
  if (Sd) return Ns;
  Sd = 1;
  const t = at().fromPromise, e = Ue, r = /* @__PURE__ */ _t(), o = /* @__PURE__ */ Ft(), { pathExists: a } = /* @__PURE__ */ Ar(), { areIdentical: n } = /* @__PURE__ */ Fr();
  async function i(s, f) {
    let d;
    try {
      d = await r.lstat(f);
    } catch {
    }
    let y;
    try {
      y = await r.lstat(s);
    } catch (I) {
      throw I.message = I.message.replace("lstat", "ensureLink"), I;
    }
    if (d && n(y, d)) return;
    const R = e.dirname(f);
    await a(R) || await o.mkdirs(R), await r.link(s, f);
  }
  function l(s, f) {
    let d;
    try {
      d = r.lstatSync(f);
    } catch {
    }
    try {
      const w = r.lstatSync(s);
      if (d && n(w, d)) return;
    } catch (w) {
      throw w.message = w.message.replace("lstat", "ensureLink"), w;
    }
    const y = e.dirname(f);
    return r.existsSync(y) || o.mkdirsSync(y), r.linkSync(s, f);
  }
  return Ns = {
    createLink: t(i),
    createLinkSync: l
  }, Ns;
}
var Ls, Ed;
function kE() {
  if (Ed) return Ls;
  Ed = 1;
  const t = Ue, e = /* @__PURE__ */ _t(), { pathExists: r } = /* @__PURE__ */ Ar(), o = at().fromPromise;
  async function a(i, l) {
    if (t.isAbsolute(i)) {
      try {
        await e.lstat(i);
      } catch (y) {
        throw y.message = y.message.replace("lstat", "ensureSymlink"), y;
      }
      return {
        toCwd: i,
        toDst: i
      };
    }
    const s = t.dirname(l), f = t.join(s, i);
    if (await r(f))
      return {
        toCwd: f,
        toDst: i
      };
    try {
      await e.lstat(i);
    } catch (y) {
      throw y.message = y.message.replace("lstat", "ensureSymlink"), y;
    }
    return {
      toCwd: i,
      toDst: t.relative(s, i)
    };
  }
  function n(i, l) {
    if (t.isAbsolute(i)) {
      if (!e.existsSync(i)) throw new Error("absolute srcpath does not exist");
      return {
        toCwd: i,
        toDst: i
      };
    }
    const s = t.dirname(l), f = t.join(s, i);
    if (e.existsSync(f))
      return {
        toCwd: f,
        toDst: i
      };
    if (!e.existsSync(i)) throw new Error("relative srcpath does not exist");
    return {
      toCwd: i,
      toDst: t.relative(s, i)
    };
  }
  return Ls = {
    symlinkPaths: o(a),
    symlinkPathsSync: n
  }, Ls;
}
var Ps, vd;
function FE() {
  if (vd) return Ps;
  vd = 1;
  const t = /* @__PURE__ */ _t(), e = at().fromPromise;
  async function r(a, n) {
    if (n) return n;
    let i;
    try {
      i = await t.lstat(a);
    } catch {
      return "file";
    }
    return i && i.isDirectory() ? "dir" : "file";
  }
  function o(a, n) {
    if (n) return n;
    let i;
    try {
      i = t.lstatSync(a);
    } catch {
      return "file";
    }
    return i && i.isDirectory() ? "dir" : "file";
  }
  return Ps = {
    symlinkType: e(r),
    symlinkTypeSync: o
  }, Ps;
}
var Cs, Rd;
function jE() {
  if (Rd) return Cs;
  Rd = 1;
  const t = at().fromPromise, e = Ue, r = /* @__PURE__ */ _t(), { mkdirs: o, mkdirsSync: a } = /* @__PURE__ */ Ft(), { symlinkPaths: n, symlinkPathsSync: i } = /* @__PURE__ */ kE(), { symlinkType: l, symlinkTypeSync: s } = /* @__PURE__ */ FE(), { pathExists: f } = /* @__PURE__ */ Ar(), { areIdentical: d } = /* @__PURE__ */ Fr();
  async function y(w, I, A) {
    let u;
    try {
      u = await r.lstat(I);
    } catch {
    }
    if (u && u.isSymbolicLink()) {
      let C;
      if (e.isAbsolute(w))
        C = await r.stat(w);
      else {
        const W = e.dirname(I), k = e.join(W, w);
        try {
          C = await r.stat(k);
        } catch {
          C = await r.stat(w);
        }
      }
      const N = await r.stat(I);
      if (d(C, N)) return;
    }
    const g = await n(w, I);
    w = g.toDst;
    const p = await l(g.toCwd, A), b = e.dirname(I);
    return await f(b) || await o(b), r.symlink(w, I, p);
  }
  function R(w, I, A) {
    let u;
    try {
      u = r.lstatSync(I);
    } catch {
    }
    if (u && u.isSymbolicLink()) {
      let C;
      if (e.isAbsolute(w))
        C = r.statSync(w);
      else {
        const W = e.dirname(I), k = e.join(W, w);
        try {
          C = r.statSync(k);
        } catch {
          C = r.statSync(w);
        }
      }
      const N = r.statSync(I);
      if (d(C, N)) return;
    }
    const g = i(w, I);
    w = g.toDst, A = s(g.toCwd, A);
    const p = e.dirname(I);
    return r.existsSync(p) || a(p), r.symlinkSync(w, I, A);
  }
  return Cs = {
    createSymlink: t(y),
    createSymlinkSync: R
  }, Cs;
}
var Ms, Ad;
function $E() {
  if (Ad) return Ms;
  Ad = 1;
  const { createFile: t, createFileSync: e } = /* @__PURE__ */ CE(), { createLink: r, createLinkSync: o } = /* @__PURE__ */ ME(), { createSymlink: a, createSymlinkSync: n } = /* @__PURE__ */ jE();
  return Ms = {
    // file
    createFile: t,
    createFileSync: e,
    ensureFile: t,
    ensureFileSync: e,
    // link
    createLink: r,
    createLinkSync: o,
    ensureLink: r,
    ensureLinkSync: o,
    // symlink
    createSymlink: a,
    createSymlinkSync: n,
    ensureSymlink: a,
    ensureSymlinkSync: n
  }, Ms;
}
var ks, Td;
function gc() {
  if (Td) return ks;
  Td = 1;
  function t(r, { EOL: o = `
`, finalEOL: a = !0, replacer: n = null, spaces: i } = {}) {
    const l = a ? o : "";
    return JSON.stringify(r, n, i).replace(/\n/g, o) + l;
  }
  function e(r) {
    return Buffer.isBuffer(r) && (r = r.toString("utf8")), r.replace(/^\uFEFF/, "");
  }
  return ks = { stringify: t, stripBom: e }, ks;
}
var Fs, xd;
function WE() {
  if (xd) return Fs;
  xd = 1;
  let t;
  try {
    t = Rr();
  } catch {
    t = Lt;
  }
  const e = at(), { stringify: r, stripBom: o } = gc();
  async function a(d, y = {}) {
    typeof y == "string" && (y = { encoding: y });
    const R = y.fs || t, w = "throws" in y ? y.throws : !0;
    let I = await e.fromCallback(R.readFile)(d, y);
    I = o(I);
    let A;
    try {
      A = JSON.parse(I, y ? y.reviver : null);
    } catch (u) {
      if (w)
        throw u.message = `${d}: ${u.message}`, u;
      return null;
    }
    return A;
  }
  const n = e.fromPromise(a);
  function i(d, y = {}) {
    typeof y == "string" && (y = { encoding: y });
    const R = y.fs || t, w = "throws" in y ? y.throws : !0;
    try {
      let I = R.readFileSync(d, y);
      return I = o(I), JSON.parse(I, y.reviver);
    } catch (I) {
      if (w)
        throw I.message = `${d}: ${I.message}`, I;
      return null;
    }
  }
  async function l(d, y, R = {}) {
    const w = R.fs || t, I = r(y, R);
    await e.fromCallback(w.writeFile)(d, I, R);
  }
  const s = e.fromPromise(l);
  function f(d, y, R = {}) {
    const w = R.fs || t, I = r(y, R);
    return w.writeFileSync(d, I, R);
  }
  return Fs = {
    readFile: n,
    readFileSync: i,
    writeFile: s,
    writeFileSync: f
  }, Fs;
}
var js, Id;
function qE() {
  if (Id) return js;
  Id = 1;
  const t = WE();
  return js = {
    // jsonfile exports
    readJson: t.readFile,
    readJsonSync: t.readFileSync,
    writeJson: t.writeFile,
    writeJsonSync: t.writeFileSync
  }, js;
}
var $s, Dd;
function yc() {
  if (Dd) return $s;
  Dd = 1;
  const t = at().fromPromise, e = /* @__PURE__ */ _t(), r = Ue, o = /* @__PURE__ */ Ft(), a = Ar().pathExists;
  async function n(l, s, f = "utf-8") {
    const d = r.dirname(l);
    return await a(d) || await o.mkdirs(d), e.writeFile(l, s, f);
  }
  function i(l, ...s) {
    const f = r.dirname(l);
    e.existsSync(f) || o.mkdirsSync(f), e.writeFileSync(l, ...s);
  }
  return $s = {
    outputFile: t(n),
    outputFileSync: i
  }, $s;
}
var Ws, Od;
function BE() {
  if (Od) return Ws;
  Od = 1;
  const { stringify: t } = gc(), { outputFile: e } = /* @__PURE__ */ yc();
  async function r(o, a, n = {}) {
    const i = t(a, n);
    await e(o, i, n);
  }
  return Ws = r, Ws;
}
var qs, Nd;
function UE() {
  if (Nd) return qs;
  Nd = 1;
  const { stringify: t } = gc(), { outputFileSync: e } = /* @__PURE__ */ yc();
  function r(o, a, n) {
    const i = t(a, n);
    e(o, i, n);
  }
  return qs = r, qs;
}
var Bs, Ld;
function GE() {
  if (Ld) return Bs;
  Ld = 1;
  const t = at().fromPromise, e = /* @__PURE__ */ qE();
  return e.outputJson = t(/* @__PURE__ */ BE()), e.outputJsonSync = /* @__PURE__ */ UE(), e.outputJSON = e.outputJson, e.outputJSONSync = e.outputJsonSync, e.writeJSON = e.writeJson, e.writeJSONSync = e.writeJsonSync, e.readJSON = e.readJson, e.readJSONSync = e.readJsonSync, Bs = e, Bs;
}
var Us, Pd;
function HE() {
  if (Pd) return Us;
  Pd = 1;
  const t = /* @__PURE__ */ _t(), e = Ue, { copy: r } = /* @__PURE__ */ pc(), { remove: o } = /* @__PURE__ */ Mi(), { mkdirp: a } = /* @__PURE__ */ Ft(), { pathExists: n } = /* @__PURE__ */ Ar(), i = /* @__PURE__ */ Fr();
  async function l(d, y, R = {}) {
    const w = R.overwrite || R.clobber || !1, { srcStat: I, isChangingCase: A = !1 } = await i.checkPaths(d, y, "move", R);
    await i.checkParentPaths(d, I, y, "move");
    const u = e.dirname(y);
    return e.parse(u).root !== u && await a(u), s(d, y, w, A);
  }
  async function s(d, y, R, w) {
    if (!w) {
      if (R)
        await o(y);
      else if (await n(y))
        throw new Error("dest already exists.");
    }
    try {
      await t.rename(d, y);
    } catch (I) {
      if (I.code !== "EXDEV")
        throw I;
      await f(d, y, R);
    }
  }
  async function f(d, y, R) {
    return await r(d, y, {
      overwrite: R,
      errorOnExist: !0,
      preserveTimestamps: !0
    }), o(d);
  }
  return Us = l, Us;
}
var Gs, Cd;
function zE() {
  if (Cd) return Gs;
  Cd = 1;
  const t = Rr(), e = Ue, r = pc().copySync, o = Mi().removeSync, a = Ft().mkdirpSync, n = /* @__PURE__ */ Fr();
  function i(y, R, w) {
    w = w || {};
    const I = w.overwrite || w.clobber || !1, { srcStat: A, isChangingCase: u = !1 } = n.checkPathsSync(y, R, "move", w);
    return n.checkParentPathsSync(y, A, R, "move"), l(R) || a(e.dirname(R)), s(y, R, I, u);
  }
  function l(y) {
    const R = e.dirname(y);
    return e.parse(R).root === R;
  }
  function s(y, R, w, I) {
    if (I) return f(y, R, w);
    if (w)
      return o(R), f(y, R, w);
    if (t.existsSync(R)) throw new Error("dest already exists.");
    return f(y, R, w);
  }
  function f(y, R, w) {
    try {
      t.renameSync(y, R);
    } catch (I) {
      if (I.code !== "EXDEV") throw I;
      return d(y, R, w);
    }
  }
  function d(y, R, w) {
    return r(y, R, {
      overwrite: w,
      errorOnExist: !0,
      preserveTimestamps: !0
    }), o(y);
  }
  return Gs = i, Gs;
}
var Hs, Md;
function VE() {
  if (Md) return Hs;
  Md = 1;
  const t = at().fromPromise;
  return Hs = {
    move: t(/* @__PURE__ */ HE()),
    moveSync: /* @__PURE__ */ zE()
  }, Hs;
}
var zs, kd;
function ZE() {
  return kd || (kd = 1, zs = {
    // Export promiseified graceful-fs:
    .../* @__PURE__ */ _t(),
    // Export extra methods:
    .../* @__PURE__ */ pc(),
    .../* @__PURE__ */ PE(),
    .../* @__PURE__ */ $E(),
    .../* @__PURE__ */ GE(),
    .../* @__PURE__ */ Ft(),
    .../* @__PURE__ */ VE(),
    .../* @__PURE__ */ yc(),
    .../* @__PURE__ */ Ar(),
    .../* @__PURE__ */ Mi()
  }), zs;
}
var YE = /* @__PURE__ */ ZE();
const Se = /* @__PURE__ */ Ci(YE);
var zr = { exports: {} }, Vs = { exports: {} }, Fd;
function dw() {
  return Fd || (Fd = 1, (function(t) {
    let e = {};
    try {
      e = require("electron");
    } catch {
    }
    e.ipcRenderer && r(e), t.exports = r;
    function r({ contextBridge: o, ipcRenderer: a }) {
      if (!a)
        return;
      a.on("__ELECTRON_LOG_IPC__", (i, l) => {
        window.postMessage({ cmd: "message", ...l });
      }), a.invoke("__ELECTRON_LOG__", { cmd: "getOptions" }).catch((i) => console.error(new Error(
        `electron-log isn't initialized in the main process. Please call log.initialize() before. ${i.message}`
      )));
      const n = {
        sendToMain(i) {
          try {
            a.send("__ELECTRON_LOG__", i);
          } catch (l) {
            console.error("electronLog.sendToMain ", l, "data:", i), a.send("__ELECTRON_LOG__", {
              cmd: "errorHandler",
              error: { message: l?.message, stack: l?.stack },
              errorName: "sendToMain"
            });
          }
        },
        log(...i) {
          n.sendToMain({ data: i, level: "info" });
        }
      };
      for (const i of ["error", "warn", "info", "verbose", "debug", "silly"])
        n[i] = (...l) => n.sendToMain({
          data: l,
          level: i
        });
      if (o && process.contextIsolated)
        try {
          o.exposeInMainWorld("__electronLog", n);
        } catch {
        }
      typeof window == "object" ? window.__electronLog = n : __electronLog = n;
    }
  })(Vs)), Vs.exports;
}
var Zs = { exports: {} }, Ys, jd;
function KE() {
  if (jd) return Ys;
  jd = 1, Ys = t;
  function t(e) {
    return Object.defineProperties(r, {
      defaultLabel: { value: "", writable: !0 },
      labelPadding: { value: !0, writable: !0 },
      maxLabelLength: { value: 0, writable: !0 },
      labelLength: {
        get() {
          switch (typeof r.labelPadding) {
            case "boolean":
              return r.labelPadding ? r.maxLabelLength : 0;
            case "number":
              return r.labelPadding;
            default:
              return 0;
          }
        }
      }
    });
    function r(o) {
      r.maxLabelLength = Math.max(r.maxLabelLength, o.length);
      const a = {};
      for (const n of e.levels)
        a[n] = (...i) => e.logData(i, { level: n, scope: o });
      return a.log = a.info, a;
    }
  }
  return Ys;
}
var Ks, $d;
function JE() {
  if ($d) return Ks;
  $d = 1;
  class t {
    constructor({ processMessage: r }) {
      this.processMessage = r, this.buffer = [], this.enabled = !1, this.begin = this.begin.bind(this), this.commit = this.commit.bind(this), this.reject = this.reject.bind(this);
    }
    addMessage(r) {
      this.buffer.push(r);
    }
    begin() {
      this.enabled = [];
    }
    commit() {
      this.enabled = !1, this.buffer.forEach((r) => this.processMessage(r)), this.buffer = [];
    }
    reject() {
      this.enabled = !1, this.buffer = [];
    }
  }
  return Ks = t, Ks;
}
var Js, Wd;
function hw() {
  if (Wd) return Js;
  Wd = 1;
  const t = KE(), e = JE();
  class r {
    static instances = {};
    dependencies = {};
    errorHandler = null;
    eventLogger = null;
    functions = {};
    hooks = [];
    isDev = !1;
    levels = null;
    logId = null;
    scope = null;
    transports = {};
    variables = {};
    constructor({
      allowUnknownLevel: a = !1,
      dependencies: n = {},
      errorHandler: i,
      eventLogger: l,
      initializeFn: s,
      isDev: f = !1,
      levels: d = ["error", "warn", "info", "verbose", "debug", "silly"],
      logId: y,
      transportFactories: R = {},
      variables: w
    } = {}) {
      this.addLevel = this.addLevel.bind(this), this.create = this.create.bind(this), this.initialize = this.initialize.bind(this), this.logData = this.logData.bind(this), this.processMessage = this.processMessage.bind(this), this.allowUnknownLevel = a, this.buffering = new e(this), this.dependencies = n, this.initializeFn = s, this.isDev = f, this.levels = d, this.logId = y, this.scope = t(this), this.transportFactories = R, this.variables = w || {};
      for (const I of this.levels)
        this.addLevel(I, !1);
      this.log = this.info, this.functions.log = this.log, this.errorHandler = i, i?.setOptions({ ...n, logFn: this.error }), this.eventLogger = l, l?.setOptions({ ...n, logger: this });
      for (const [I, A] of Object.entries(R))
        this.transports[I] = A(this, n);
      r.instances[y] = this;
    }
    static getInstance({ logId: a }) {
      return this.instances[a] || this.instances.default;
    }
    addLevel(a, n = this.levels.length) {
      n !== !1 && this.levels.splice(n, 0, a), this[a] = (...i) => this.logData(i, { level: a }), this.functions[a] = this[a];
    }
    catchErrors(a) {
      return this.processMessage(
        {
          data: ["log.catchErrors is deprecated. Use log.errorHandler instead"],
          level: "warn"
        },
        { transports: ["console"] }
      ), this.errorHandler.startCatching(a);
    }
    create(a) {
      return typeof a == "string" && (a = { logId: a }), new r({
        dependencies: this.dependencies,
        errorHandler: this.errorHandler,
        initializeFn: this.initializeFn,
        isDev: this.isDev,
        transportFactories: this.transportFactories,
        variables: { ...this.variables },
        ...a
      });
    }
    compareLevels(a, n, i = this.levels) {
      const l = i.indexOf(a), s = i.indexOf(n);
      return s === -1 || l === -1 ? !0 : s <= l;
    }
    initialize(a = {}) {
      this.initializeFn({ logger: this, ...this.dependencies, ...a });
    }
    logData(a, n = {}) {
      this.buffering.enabled ? this.buffering.addMessage({ data: a, date: /* @__PURE__ */ new Date(), ...n }) : this.processMessage({ data: a, ...n });
    }
    processMessage(a, { transports: n = this.transports } = {}) {
      if (a.cmd === "errorHandler") {
        this.errorHandler.handle(a.error, {
          errorName: a.errorName,
          processType: "renderer",
          showDialog: !!a.showDialog
        });
        return;
      }
      let i = a.level;
      this.allowUnknownLevel || (i = this.levels.includes(a.level) ? a.level : "info");
      const l = {
        date: /* @__PURE__ */ new Date(),
        logId: this.logId,
        ...a,
        level: i,
        variables: {
          ...this.variables,
          ...a.variables
        }
      };
      for (const [s, f] of this.transportEntries(n))
        if (!(typeof f != "function" || f.level === !1) && this.compareLevels(f.level, a.level))
          try {
            const d = this.hooks.reduce((y, R) => y && R(y, f, s), l);
            d && f({ ...d, data: [...d.data] });
          } catch (d) {
            this.processInternalErrorFn(d);
          }
    }
    processInternalErrorFn(a) {
    }
    transportEntries(a = this.transports) {
      return (Array.isArray(a) ? a : Object.entries(a)).map((i) => {
        switch (typeof i) {
          case "string":
            return this.transports[i] ? [i, this.transports[i]] : null;
          case "function":
            return [i.name, i];
          default:
            return Array.isArray(i) ? i : null;
        }
      }).filter(Boolean);
    }
  }
  return Js = r, Js;
}
var Xs, qd;
function XE() {
  if (qd) return Xs;
  qd = 1;
  const t = console.error;
  class e {
    logFn = null;
    onError = null;
    showDialog = !1;
    preventDefault = !0;
    constructor({ logFn: o = null } = {}) {
      this.handleError = this.handleError.bind(this), this.handleRejection = this.handleRejection.bind(this), this.startCatching = this.startCatching.bind(this), this.logFn = o;
    }
    handle(o, {
      logFn: a = this.logFn,
      errorName: n = "",
      onError: i = this.onError,
      showDialog: l = this.showDialog
    } = {}) {
      try {
        i?.({ error: o, errorName: n, processType: "renderer" }) !== !1 && a({ error: o, errorName: n, showDialog: l });
      } catch {
        t(o);
      }
    }
    setOptions({ logFn: o, onError: a, preventDefault: n, showDialog: i }) {
      typeof o == "function" && (this.logFn = o), typeof a == "function" && (this.onError = a), typeof n == "boolean" && (this.preventDefault = n), typeof i == "boolean" && (this.showDialog = i);
    }
    startCatching({ onError: o, showDialog: a } = {}) {
      this.isActive || (this.isActive = !0, this.setOptions({ onError: o, showDialog: a }), window.addEventListener("error", (n) => {
        this.preventDefault && n.preventDefault?.(), this.handleError(n.error || n);
      }), window.addEventListener("unhandledrejection", (n) => {
        this.preventDefault && n.preventDefault?.(), this.handleRejection(n.reason || n);
      }));
    }
    handleError(o) {
      this.handle(o, { errorName: "Unhandled" });
    }
    handleRejection(o) {
      const a = o instanceof Error ? o : new Error(JSON.stringify(o));
      this.handle(a, { errorName: "Unhandled rejection" });
    }
  }
  return Xs = e, Xs;
}
var Qs, Bd;
function Tr() {
  if (Bd) return Qs;
  Bd = 1, Qs = { transform: t };
  function t({
    logger: e,
    message: r,
    transport: o,
    initialData: a = r?.data || [],
    transforms: n = o?.transforms
  }) {
    return n.reduce((i, l) => typeof l == "function" ? l({ data: i, logger: e, message: r, transport: o }) : i, a);
  }
  return Qs;
}
var eo, Ud;
function QE() {
  if (Ud) return eo;
  Ud = 1;
  const { transform: t } = Tr();
  eo = r;
  const e = {
    error: console.error,
    warn: console.warn,
    info: console.info,
    verbose: console.info,
    debug: console.debug,
    silly: console.debug,
    log: console.log
  };
  function r(a) {
    return Object.assign(n, {
      format: "{h}:{i}:{s}.{ms}{scope} › {text}",
      transforms: [o],
      writeFn({ message: { level: i, data: l } }) {
        const s = e[i] || e.info;
        setTimeout(() => s(...l));
      }
    });
    function n(i) {
      n.writeFn({
        message: { ...i, data: t({ logger: a, message: i, transport: n }) }
      });
    }
  }
  function o({
    data: a = [],
    logger: n = {},
    message: i = {},
    transport: l = {}
  }) {
    if (typeof l.format == "function")
      return l.format({
        data: a,
        level: i?.level || "info",
        logger: n,
        message: i,
        transport: l
      });
    if (typeof l.format != "string")
      return a;
    a.unshift(l.format), typeof a[1] == "string" && a[1].match(/%[1cdfiOos]/) && (a = [`${a[0]}${a[1]}`, ...a.slice(2)]);
    const s = i.date || /* @__PURE__ */ new Date();
    return a[0] = a[0].replace(/\{(\w+)}/g, (f, d) => {
      switch (d) {
        case "level":
          return i.level;
        case "logId":
          return i.logId;
        case "scope": {
          const y = i.scope || n.scope?.defaultLabel;
          return y ? ` (${y})` : "";
        }
        case "text":
          return "";
        case "y":
          return s.getFullYear().toString(10);
        case "m":
          return (s.getMonth() + 1).toString(10).padStart(2, "0");
        case "d":
          return s.getDate().toString(10).padStart(2, "0");
        case "h":
          return s.getHours().toString(10).padStart(2, "0");
        case "i":
          return s.getMinutes().toString(10).padStart(2, "0");
        case "s":
          return s.getSeconds().toString(10).padStart(2, "0");
        case "ms":
          return s.getMilliseconds().toString(10).padStart(3, "0");
        case "iso":
          return s.toISOString();
        default:
          return i.variables?.[d] || f;
      }
    }).trim(), a;
  }
  return eo;
}
var to, Gd;
function ev() {
  if (Gd) return to;
  Gd = 1;
  const { transform: t } = Tr();
  to = r;
  const e = /* @__PURE__ */ new Set([Promise, WeakMap, WeakSet]);
  function r(n) {
    return Object.assign(i, {
      depth: 5,
      transforms: [a]
    });
    function i(l) {
      if (!window.__electronLog) {
        n.processMessage(
          {
            data: ["electron-log: logger isn't initialized in the main process"],
            level: "error"
          },
          { transports: ["console"] }
        );
        return;
      }
      try {
        const s = t({
          initialData: l,
          logger: n,
          message: l,
          transport: i
        });
        __electronLog.sendToMain(s);
      } catch (s) {
        n.transports.console({
          data: ["electronLog.transports.ipc", s, "data:", l.data],
          level: "error"
        });
      }
    }
  }
  function o(n) {
    return Object(n) !== n;
  }
  function a({
    data: n,
    depth: i,
    seen: l = /* @__PURE__ */ new WeakSet(),
    transport: s = {}
  } = {}) {
    const f = i || s.depth || 5;
    return l.has(n) ? "[Circular]" : f < 1 ? o(n) ? n : Array.isArray(n) ? "[Array]" : `[${typeof n}]` : ["function", "symbol"].includes(typeof n) ? n.toString() : o(n) ? n : e.has(n.constructor) ? `[${n.constructor.name}]` : Array.isArray(n) ? n.map((d) => a({
      data: d,
      depth: f - 1,
      seen: l
    })) : n instanceof Date ? n.toISOString() : n instanceof Error ? n.stack : n instanceof Map ? new Map(
      Array.from(n).map(([d, y]) => [
        a({ data: d, depth: f - 1, seen: l }),
        a({ data: y, depth: f - 1, seen: l })
      ])
    ) : n instanceof Set ? new Set(
      Array.from(n).map(
        (d) => a({ data: d, depth: f - 1, seen: l })
      )
    ) : (l.add(n), Object.fromEntries(
      Object.entries(n).map(
        ([d, y]) => [
          d,
          a({ data: y, depth: f - 1, seen: l })
        ]
      )
    ));
  }
  return to;
}
var Hd;
function tv() {
  return Hd || (Hd = 1, (function(t) {
    const e = hw(), r = XE(), o = QE(), a = ev();
    typeof process == "object" && process.type === "browser" && console.warn(
      "electron-log/renderer is loaded in the main process. It could cause unexpected behaviour."
    ), t.exports = n(), t.exports.Logger = e, t.exports.default = t.exports;
    function n() {
      const i = new e({
        allowUnknownLevel: !0,
        errorHandler: new r(),
        initializeFn: () => {
        },
        logId: "default",
        transportFactories: {
          console: o,
          ipc: a
        },
        variables: {
          processType: "renderer"
        }
      });
      return i.errorHandler.setOptions({
        logFn({ error: l, errorName: s, showDialog: f }) {
          i.transports.console({
            data: [s, l].filter(Boolean),
            level: "error"
          }), i.transports.ipc({
            cmd: "errorHandler",
            error: {
              cause: l?.cause,
              code: l?.code,
              name: l?.name,
              message: l?.message,
              stack: l?.stack
            },
            errorName: s,
            logId: i.logId,
            showDialog: f
          });
        }
      }), typeof window == "object" && window.addEventListener("message", (l) => {
        const { cmd: s, logId: f, ...d } = l.data || {}, y = e.getInstance({ logId: f });
        s === "message" && y.processMessage(d, { transports: ["console"] });
      }), new Proxy(i, {
        get(l, s) {
          return typeof l[s] < "u" ? l[s] : (...f) => i.logData(f, { level: s });
        }
      });
    }
  })(Zs)), Zs.exports;
}
var ro, zd;
function rv() {
  if (zd) return ro;
  zd = 1;
  const t = Lt, e = Ue;
  ro = {
    findAndReadPackageJson: r,
    tryReadJsonAt: o
  };
  function r() {
    return o(i()) || o(n()) || o(process.resourcesPath, "app.asar") || o(process.resourcesPath, "app") || o(process.cwd()) || { name: void 0, version: void 0 };
  }
  function o(...l) {
    if (l[0])
      try {
        const s = e.join(...l), f = a("package.json", s);
        if (!f)
          return;
        const d = JSON.parse(t.readFileSync(f, "utf8")), y = d?.productName || d?.name;
        return !y || y.toLowerCase() === "electron" ? void 0 : y ? { name: y, version: d?.version } : void 0;
      } catch {
        return;
      }
  }
  function a(l, s) {
    let f = s;
    for (; ; ) {
      const d = e.parse(f), y = d.root, R = d.dir;
      if (t.existsSync(e.join(f, l)))
        return e.resolve(e.join(f, l));
      if (f === y)
        return null;
      f = R;
    }
  }
  function n() {
    const l = process.argv.filter((f) => f.indexOf("--user-data-dir=") === 0);
    return l.length === 0 || typeof l[0] != "string" ? null : l[0].replace("--user-data-dir=", "");
  }
  function i() {
    try {
      return require.main?.filename;
    } catch {
      return;
    }
  }
  return ro;
}
var no, Vd;
function pw() {
  if (Vd) return no;
  Vd = 1;
  const t = hE, e = Li, r = Ue, o = rv();
  class a {
    appName = void 0;
    appPackageJson = void 0;
    platform = process.platform;
    getAppLogPath(i = this.getAppName()) {
      return this.platform === "darwin" ? r.join(this.getSystemPathHome(), "Library/Logs", i) : r.join(this.getAppUserDataPath(i), "logs");
    }
    getAppName() {
      const i = this.appName || this.getAppPackageJson()?.name;
      if (!i)
        throw new Error(
          "electron-log can't determine the app name. It tried these methods:\n1. Use `electron.app.name`\n2. Use productName or name from the nearest package.json`\nYou can also set it through log.transports.file.setAppName()"
        );
      return i;
    }
    /**
     * @private
     * @returns {undefined}
     */
    getAppPackageJson() {
      return typeof this.appPackageJson != "object" && (this.appPackageJson = o.findAndReadPackageJson()), this.appPackageJson;
    }
    getAppUserDataPath(i = this.getAppName()) {
      return i ? r.join(this.getSystemPathAppData(), i) : void 0;
    }
    getAppVersion() {
      return this.getAppPackageJson()?.version;
    }
    getElectronLogPath() {
      return this.getAppLogPath();
    }
    getMacOsVersion() {
      const i = Number(e.release().split(".")[0]);
      return i <= 19 ? `10.${i - 4}` : i - 9;
    }
    /**
     * @protected
     * @returns {string}
     */
    getOsVersion() {
      let i = e.type().replace("_", " "), l = e.release();
      return i === "Darwin" && (i = "macOS", l = this.getMacOsVersion()), `${i} ${l}`;
    }
    /**
     * @return {PathVariables}
     */
    getPathVariables() {
      const i = this.getAppName(), l = this.getAppVersion(), s = this;
      return {
        appData: this.getSystemPathAppData(),
        appName: i,
        appVersion: l,
        get electronDefaultDir() {
          return s.getElectronLogPath();
        },
        home: this.getSystemPathHome(),
        libraryDefaultDir: this.getAppLogPath(i),
        libraryTemplate: this.getAppLogPath("{appName}"),
        temp: this.getSystemPathTemp(),
        userData: this.getAppUserDataPath(i)
      };
    }
    getSystemPathAppData() {
      const i = this.getSystemPathHome();
      switch (this.platform) {
        case "darwin":
          return r.join(i, "Library/Application Support");
        case "win32":
          return process.env.APPDATA || r.join(i, "AppData/Roaming");
        default:
          return process.env.XDG_CONFIG_HOME || r.join(i, ".config");
      }
    }
    getSystemPathHome() {
      return e.homedir?.() || process.env.HOME;
    }
    getSystemPathTemp() {
      return e.tmpdir();
    }
    getVersions() {
      return {
        app: `${this.getAppName()} ${this.getAppVersion()}`,
        electron: void 0,
        os: this.getOsVersion()
      };
    }
    isDev() {
      return process.env.NODE_ENV === "development" || process.env.ELECTRON_IS_DEV === "1";
    }
    isElectron() {
      return !!process.versions.electron;
    }
    onAppEvent(i, l) {
    }
    onAppReady(i) {
      i();
    }
    onEveryWebContentsEvent(i, l) {
    }
    /**
     * Listen to async messages sent from opposite process
     * @param {string} channel
     * @param {function} listener
     */
    onIpc(i, l) {
    }
    onIpcInvoke(i, l) {
    }
    /**
     * @param {string} url
     * @param {Function} [logFunction]
     */
    openUrl(i, l = console.error) {
      const f = { darwin: "open", win32: "start", linux: "xdg-open" }[process.platform] || "xdg-open";
      t.exec(`${f} ${i}`, {}, (d) => {
        d && l(d);
      });
    }
    setAppName(i) {
      this.appName = i;
    }
    setPlatform(i) {
      this.platform = i;
    }
    setPreloadFileForSessions({
      filePath: i,
      // eslint-disable-line no-unused-vars
      includeFutureSession: l = !0,
      // eslint-disable-line no-unused-vars
      getSessions: s = () => []
      // eslint-disable-line no-unused-vars
    }) {
    }
    /**
     * Sent a message to opposite process
     * @param {string} channel
     * @param {any} message
     */
    sendIpc(i, l) {
    }
    showErrorBox(i, l) {
    }
  }
  return no = a, no;
}
var io, Zd;
function nv() {
  if (Zd) return io;
  Zd = 1;
  const t = Ue, e = pw();
  class r extends e {
    /**
     * @type {typeof Electron}
     */
    electron = void 0;
    /**
     * @param {object} options
     * @param {typeof Electron} [options.electron]
     */
    constructor({ electron: a } = {}) {
      super(), this.electron = a;
    }
    getAppName() {
      let a;
      try {
        a = this.appName || this.electron.app?.name || this.electron.app?.getName();
      } catch {
      }
      return a || super.getAppName();
    }
    getAppUserDataPath(a) {
      return this.getPath("userData") || super.getAppUserDataPath(a);
    }
    getAppVersion() {
      let a;
      try {
        a = this.electron.app?.getVersion();
      } catch {
      }
      return a || super.getAppVersion();
    }
    getElectronLogPath() {
      return this.getPath("logs") || super.getElectronLogPath();
    }
    /**
     * @private
     * @param {any} name
     * @returns {string|undefined}
     */
    getPath(a) {
      try {
        return this.electron.app?.getPath(a);
      } catch {
        return;
      }
    }
    getVersions() {
      return {
        app: `${this.getAppName()} ${this.getAppVersion()}`,
        electron: `Electron ${process.versions.electron}`,
        os: this.getOsVersion()
      };
    }
    getSystemPathAppData() {
      return this.getPath("appData") || super.getSystemPathAppData();
    }
    isDev() {
      return this.electron.app?.isPackaged !== void 0 ? !this.electron.app.isPackaged : typeof process.execPath == "string" ? t.basename(process.execPath).toLowerCase().startsWith("electron") : super.isDev();
    }
    onAppEvent(a, n) {
      return this.electron.app?.on(a, n), () => {
        this.electron.app?.off(a, n);
      };
    }
    onAppReady(a) {
      this.electron.app?.isReady() ? a() : this.electron.app?.once ? this.electron.app?.once("ready", a) : a();
    }
    onEveryWebContentsEvent(a, n) {
      return this.electron.webContents?.getAllWebContents()?.forEach((l) => {
        l.on(a, n);
      }), this.electron.app?.on("web-contents-created", i), () => {
        this.electron.webContents?.getAllWebContents().forEach((l) => {
          l.off(a, n);
        }), this.electron.app?.off("web-contents-created", i);
      };
      function i(l, s) {
        s.on(a, n);
      }
    }
    /**
     * Listen to async messages sent from opposite process
     * @param {string} channel
     * @param {function} listener
     */
    onIpc(a, n) {
      this.electron.ipcMain?.on(a, n);
    }
    onIpcInvoke(a, n) {
      this.electron.ipcMain?.handle?.(a, n);
    }
    /**
     * @param {string} url
     * @param {Function} [logFunction]
     */
    openUrl(a, n = console.error) {
      this.electron.shell?.openExternal(a).catch(n);
    }
    setPreloadFileForSessions({
      filePath: a,
      includeFutureSession: n = !0,
      getSessions: i = () => [this.electron.session?.defaultSession]
    }) {
      for (const s of i().filter(Boolean))
        l(s);
      n && this.onAppEvent("session-created", (s) => {
        l(s);
      });
      function l(s) {
        typeof s.registerPreloadScript == "function" ? s.registerPreloadScript({
          filePath: a,
          id: "electron-log-preload",
          type: "frame"
        }) : s.setPreloads([...s.getPreloads(), a]);
      }
    }
    /**
     * Sent a message to opposite process
     * @param {string} channel
     * @param {any} message
     */
    sendIpc(a, n) {
      this.electron.BrowserWindow?.getAllWindows()?.forEach((i) => {
        i.webContents?.isDestroyed() === !1 && i.webContents?.isCrashed() === !1 && i.webContents.send(a, n);
      });
    }
    showErrorBox(a, n) {
      this.electron.dialog?.showErrorBox(a, n);
    }
  }
  return io = r, io;
}
var so, Yd;
function iv() {
  if (Yd) return so;
  Yd = 1;
  const t = Lt, e = Li, r = Ue, o = dw();
  let a = !1, n = !1;
  so = {
    initialize({
      externalApi: s,
      getSessions: f,
      includeFutureSession: d,
      logger: y,
      preload: R = !0,
      spyRendererConsole: w = !1
    }) {
      s.onAppReady(() => {
        try {
          R && i({
            externalApi: s,
            getSessions: f,
            includeFutureSession: d,
            logger: y,
            preloadOption: R
          }), w && l({ externalApi: s, logger: y });
        } catch (I) {
          y.warn(I);
        }
      });
    }
  };
  function i({
    externalApi: s,
    getSessions: f,
    includeFutureSession: d,
    logger: y,
    preloadOption: R
  }) {
    let w = typeof R == "string" ? R : void 0;
    if (a) {
      y.warn(new Error("log.initialize({ preload }) already called").stack);
      return;
    }
    a = !0;
    try {
      w = r.resolve(
        __dirname,
        "../renderer/electron-log-preload.js"
      );
    } catch {
    }
    if (!w || !t.existsSync(w)) {
      w = r.join(
        s.getAppUserDataPath() || e.tmpdir(),
        "electron-log-preload.js"
      );
      const I = `
      try {
        (${o.toString()})(require('electron'));
      } catch(e) {
        console.error(e);
      }
    `;
      t.writeFileSync(w, I, "utf8");
    }
    s.setPreloadFileForSessions({
      filePath: w,
      includeFutureSession: d,
      getSessions: f
    });
  }
  function l({ externalApi: s, logger: f }) {
    if (n) {
      f.warn(
        new Error("log.initialize({ spyRendererConsole }) already called").stack
      );
      return;
    }
    n = !0;
    const d = ["debug", "info", "warn", "error"];
    s.onEveryWebContentsEvent(
      "console-message",
      (y, R, w) => {
        f.processMessage({
          data: [w],
          level: d[R],
          variables: { processType: "renderer" }
        });
      }
    );
  }
  return so;
}
var oo, Kd;
function sv() {
  if (Kd) return oo;
  Kd = 1;
  class t {
    externalApi = void 0;
    isActive = !1;
    logFn = void 0;
    onError = void 0;
    showDialog = !0;
    constructor({
      externalApi: o,
      logFn: a = void 0,
      onError: n = void 0,
      showDialog: i = void 0
    } = {}) {
      this.createIssue = this.createIssue.bind(this), this.handleError = this.handleError.bind(this), this.handleRejection = this.handleRejection.bind(this), this.setOptions({ externalApi: o, logFn: a, onError: n, showDialog: i }), this.startCatching = this.startCatching.bind(this), this.stopCatching = this.stopCatching.bind(this);
    }
    handle(o, {
      logFn: a = this.logFn,
      onError: n = this.onError,
      processType: i = "browser",
      showDialog: l = this.showDialog,
      errorName: s = ""
    } = {}) {
      o = e(o);
      try {
        if (typeof n == "function") {
          const f = this.externalApi?.getVersions() || {}, d = this.createIssue;
          if (n({
            createIssue: d,
            error: o,
            errorName: s,
            processType: i,
            versions: f
          }) === !1)
            return;
        }
        s ? a(s, o) : a(o), l && !s.includes("rejection") && this.externalApi && this.externalApi.showErrorBox(
          `A JavaScript error occurred in the ${i} process`,
          o.stack
        );
      } catch {
        console.error(o);
      }
    }
    setOptions({ externalApi: o, logFn: a, onError: n, showDialog: i }) {
      typeof o == "object" && (this.externalApi = o), typeof a == "function" && (this.logFn = a), typeof n == "function" && (this.onError = n), typeof i == "boolean" && (this.showDialog = i);
    }
    startCatching({ onError: o, showDialog: a } = {}) {
      this.isActive || (this.isActive = !0, this.setOptions({ onError: o, showDialog: a }), process.on("uncaughtException", this.handleError), process.on("unhandledRejection", this.handleRejection));
    }
    stopCatching() {
      this.isActive = !1, process.removeListener("uncaughtException", this.handleError), process.removeListener("unhandledRejection", this.handleRejection);
    }
    createIssue(o, a) {
      this.externalApi?.openUrl(
        `${o}?${new URLSearchParams(a).toString()}`
      );
    }
    handleError(o) {
      this.handle(o, { errorName: "Unhandled" });
    }
    handleRejection(o) {
      const a = o instanceof Error ? o : new Error(JSON.stringify(o));
      this.handle(a, { errorName: "Unhandled rejection" });
    }
  }
  function e(r) {
    if (r instanceof Error)
      return r;
    if (r && typeof r == "object") {
      if (r.message)
        return Object.assign(new Error(r.message), r);
      try {
        return new Error(JSON.stringify(r));
      } catch (o) {
        return new Error(`Couldn't normalize error ${String(r)}: ${o}`);
      }
    }
    return new Error(`Can't normalize error ${String(r)}`);
  }
  return oo = t, oo;
}
var ao, Jd;
function ov() {
  if (Jd) return ao;
  Jd = 1;
  class t {
    disposers = [];
    format = "{eventSource}#{eventName}:";
    formatters = {
      app: {
        "certificate-error": ({ args: r }) => this.arrayToObject(r.slice(1, 4), [
          "url",
          "error",
          "certificate"
        ]),
        "child-process-gone": ({ args: r }) => r.length === 1 ? r[0] : r,
        "render-process-gone": ({ args: [r, o] }) => o && typeof o == "object" ? { ...o, ...this.getWebContentsDetails(r) } : []
      },
      webContents: {
        "console-message": ({ args: [r, o, a, n] }) => {
          if (!(r < 3))
            return { message: o, source: `${n}:${a}` };
        },
        "did-fail-load": ({ args: r }) => this.arrayToObject(r, [
          "errorCode",
          "errorDescription",
          "validatedURL",
          "isMainFrame",
          "frameProcessId",
          "frameRoutingId"
        ]),
        "did-fail-provisional-load": ({ args: r }) => this.arrayToObject(r, [
          "errorCode",
          "errorDescription",
          "validatedURL",
          "isMainFrame",
          "frameProcessId",
          "frameRoutingId"
        ]),
        "plugin-crashed": ({ args: r }) => this.arrayToObject(r, ["name", "version"]),
        "preload-error": ({ args: r }) => this.arrayToObject(r, ["preloadPath", "error"])
      }
    };
    events = {
      app: {
        "certificate-error": !0,
        "child-process-gone": !0,
        "render-process-gone": !0
      },
      webContents: {
        // 'console-message': true,
        "did-fail-load": !0,
        "did-fail-provisional-load": !0,
        "plugin-crashed": !0,
        "preload-error": !0,
        unresponsive: !0
      }
    };
    externalApi = void 0;
    level = "error";
    scope = "";
    constructor(r = {}) {
      this.setOptions(r);
    }
    setOptions({
      events: r,
      externalApi: o,
      level: a,
      logger: n,
      format: i,
      formatters: l,
      scope: s
    }) {
      typeof r == "object" && (this.events = r), typeof o == "object" && (this.externalApi = o), typeof a == "string" && (this.level = a), typeof n == "object" && (this.logger = n), (typeof i == "string" || typeof i == "function") && (this.format = i), typeof l == "object" && (this.formatters = l), typeof s == "string" && (this.scope = s);
    }
    startLogging(r = {}) {
      this.setOptions(r), this.disposeListeners();
      for (const o of this.getEventNames(this.events.app))
        this.disposers.push(
          this.externalApi.onAppEvent(o, (...a) => {
            this.handleEvent({ eventSource: "app", eventName: o, handlerArgs: a });
          })
        );
      for (const o of this.getEventNames(this.events.webContents))
        this.disposers.push(
          this.externalApi.onEveryWebContentsEvent(
            o,
            (...a) => {
              this.handleEvent(
                { eventSource: "webContents", eventName: o, handlerArgs: a }
              );
            }
          )
        );
    }
    stopLogging() {
      this.disposeListeners();
    }
    arrayToObject(r, o) {
      const a = {};
      return o.forEach((n, i) => {
        a[n] = r[i];
      }), r.length > o.length && (a.unknownArgs = r.slice(o.length)), a;
    }
    disposeListeners() {
      this.disposers.forEach((r) => r()), this.disposers = [];
    }
    formatEventLog({ eventName: r, eventSource: o, handlerArgs: a }) {
      const [n, ...i] = a;
      if (typeof this.format == "function")
        return this.format({ args: i, event: n, eventName: r, eventSource: o });
      const l = this.formatters[o]?.[r];
      let s = i;
      if (typeof l == "function" && (s = l({ args: i, event: n, eventName: r, eventSource: o })), !s)
        return;
      const f = {};
      return Array.isArray(s) ? f.args = s : typeof s == "object" && Object.assign(f, s), o === "webContents" && Object.assign(f, this.getWebContentsDetails(n?.sender)), [this.format.replace("{eventSource}", o === "app" ? "App" : "WebContents").replace("{eventName}", r), f];
    }
    getEventNames(r) {
      return !r || typeof r != "object" ? [] : Object.entries(r).filter(([o, a]) => a).map(([o]) => o);
    }
    getWebContentsDetails(r) {
      if (!r?.loadURL)
        return {};
      try {
        return {
          webContents: {
            id: r.id,
            url: r.getURL()
          }
        };
      } catch {
        return {};
      }
    }
    handleEvent({ eventName: r, eventSource: o, handlerArgs: a }) {
      const n = this.formatEventLog({ eventName: r, eventSource: o, handlerArgs: a });
      n && (this.scope ? this.logger.scope(this.scope) : this.logger)?.[this.level]?.(...n);
    }
  }
  return ao = t, ao;
}
var lo, Xd;
function gw() {
  if (Xd) return lo;
  Xd = 1;
  const { transform: t } = Tr();
  lo = {
    concatFirstStringElements: e,
    formatScope: o,
    formatText: n,
    formatVariables: a,
    timeZoneFromOffset: r,
    format({ message: i, logger: l, transport: s, data: f = i?.data }) {
      switch (typeof s.format) {
        case "string":
          return t({
            message: i,
            logger: l,
            transforms: [a, o, n],
            transport: s,
            initialData: [s.format, ...f]
          });
        case "function":
          return s.format({
            data: f,
            level: i?.level || "info",
            logger: l,
            message: i,
            transport: s
          });
        default:
          return f;
      }
    }
  };
  function e({ data: i }) {
    return typeof i[0] != "string" || typeof i[1] != "string" || i[0].match(/%[1cdfiOos]/) ? i : [`${i[0]} ${i[1]}`, ...i.slice(2)];
  }
  function r(i) {
    const l = Math.abs(i), s = i > 0 ? "-" : "+", f = Math.floor(l / 60).toString().padStart(2, "0"), d = (l % 60).toString().padStart(2, "0");
    return `${s}${f}:${d}`;
  }
  function o({ data: i, logger: l, message: s }) {
    const { defaultLabel: f, labelLength: d } = l?.scope || {}, y = i[0];
    let R = s.scope;
    R || (R = f);
    let w;
    return R === "" ? w = d > 0 ? "".padEnd(d + 3) : "" : typeof R == "string" ? w = ` (${R})`.padEnd(d + 3) : w = "", i[0] = y.replace("{scope}", w), i;
  }
  function a({ data: i, message: l }) {
    let s = i[0];
    if (typeof s != "string")
      return i;
    s = s.replace("{level}]", `${l.level}]`.padEnd(6, " "));
    const f = l.date || /* @__PURE__ */ new Date();
    return i[0] = s.replace(/\{(\w+)}/g, (d, y) => {
      switch (y) {
        case "level":
          return l.level || "info";
        case "logId":
          return l.logId;
        case "y":
          return f.getFullYear().toString(10);
        case "m":
          return (f.getMonth() + 1).toString(10).padStart(2, "0");
        case "d":
          return f.getDate().toString(10).padStart(2, "0");
        case "h":
          return f.getHours().toString(10).padStart(2, "0");
        case "i":
          return f.getMinutes().toString(10).padStart(2, "0");
        case "s":
          return f.getSeconds().toString(10).padStart(2, "0");
        case "ms":
          return f.getMilliseconds().toString(10).padStart(3, "0");
        case "z":
          return r(f.getTimezoneOffset());
        case "iso":
          return f.toISOString();
        default:
          return l.variables?.[y] || d;
      }
    }).trim(), i;
  }
  function n({ data: i }) {
    const l = i[0];
    if (typeof l != "string")
      return i;
    if (l.lastIndexOf("{text}") === l.length - 6)
      return i[0] = l.replace(/\s?{text}/, ""), i[0] === "" && i.shift(), i;
    const f = l.split("{text}");
    let d = [];
    return f[0] !== "" && d.push(f[0]), d = d.concat(i.slice(1)), f[1] !== "" && d.push(f[1]), d;
  }
  return lo;
}
var uo = { exports: {} }, Qd;
function ki() {
  return Qd || (Qd = 1, (function(t) {
    const e = It;
    t.exports = {
      serialize: o,
      maxDepth({ data: a, transport: n, depth: i = n?.depth ?? 6 }) {
        if (!a)
          return a;
        if (i < 1)
          return Array.isArray(a) ? "[array]" : typeof a == "object" && a ? "[object]" : a;
        if (Array.isArray(a))
          return a.map((s) => t.exports.maxDepth({
            data: s,
            depth: i - 1
          }));
        if (typeof a != "object" || a && typeof a.toISOString == "function")
          return a;
        if (a === null)
          return null;
        if (a instanceof Error)
          return a;
        const l = {};
        for (const s in a)
          Object.prototype.hasOwnProperty.call(a, s) && (l[s] = t.exports.maxDepth({
            data: a[s],
            depth: i - 1
          }));
        return l;
      },
      toJSON({ data: a }) {
        return JSON.parse(JSON.stringify(a, r()));
      },
      toString({ data: a, transport: n }) {
        const i = n?.inspectOptions || {}, l = a.map((s) => {
          if (s !== void 0)
            try {
              const f = JSON.stringify(s, r(), "  ");
              return f === void 0 ? void 0 : JSON.parse(f);
            } catch {
              return s;
            }
        });
        return e.formatWithOptions(i, ...l);
      }
    };
    function r(a = {}) {
      const n = /* @__PURE__ */ new WeakSet();
      return function(i, l) {
        if (typeof l == "object" && l !== null) {
          if (n.has(l))
            return;
          n.add(l);
        }
        return o(i, l, a);
      };
    }
    function o(a, n, i = {}) {
      const l = i?.serializeMapAndSet !== !1;
      return n instanceof Error ? n.stack : n && (typeof n == "function" ? `[function] ${n.toString()}` : n instanceof Date ? n.toISOString() : l && n instanceof Map && Object.fromEntries ? Object.fromEntries(n) : l && n instanceof Set && Array.from ? Array.from(n) : n);
    }
  })(uo)), uo.exports;
}
var fo, eh;
function bc() {
  if (eh) return fo;
  eh = 1, fo = {
    transformStyles: o,
    applyAnsiStyles({ data: a }) {
      return o(a, e, r);
    },
    removeStyles({ data: a }) {
      return o(a, () => "");
    }
  };
  const t = {
    unset: "\x1B[0m",
    black: "\x1B[30m",
    red: "\x1B[31m",
    green: "\x1B[32m",
    yellow: "\x1B[33m",
    blue: "\x1B[34m",
    magenta: "\x1B[35m",
    cyan: "\x1B[36m",
    white: "\x1B[37m",
    gray: "\x1B[90m"
  };
  function e(a) {
    const n = a.replace(/color:\s*(\w+).*/, "$1").toLowerCase();
    return t[n] || "";
  }
  function r(a) {
    return a + t.unset;
  }
  function o(a, n, i) {
    const l = {};
    return a.reduce((s, f, d, y) => {
      if (l[d])
        return s;
      if (typeof f == "string") {
        let R = d, w = !1;
        f = f.replace(/%[1cdfiOos]/g, (I) => {
          if (R += 1, I !== "%c")
            return I;
          const A = y[R];
          return typeof A == "string" ? (l[R] = !0, w = !0, n(A, f)) : I;
        }), w && i && (f = i(f));
      }
      return s.push(f), s;
    }, []);
  }
  return fo;
}
var co, th;
function av() {
  if (th) return co;
  th = 1;
  const {
    concatFirstStringElements: t,
    format: e
  } = gw(), { maxDepth: r, toJSON: o } = ki(), {
    applyAnsiStyles: a,
    removeStyles: n
  } = bc(), { transform: i } = Tr(), l = {
    error: console.error,
    warn: console.warn,
    info: console.info,
    verbose: console.info,
    debug: console.debug,
    silly: console.debug,
    log: console.log
  };
  co = d;
  const f = `%c{h}:{i}:{s}.{ms}{scope}%c ${process.platform === "win32" ? ">" : "›"} {text}`;
  Object.assign(d, {
    DEFAULT_FORMAT: f
  });
  function d(A) {
    return Object.assign(u, {
      colorMap: {
        error: "red",
        warn: "yellow",
        info: "cyan",
        verbose: "unset",
        debug: "gray",
        silly: "gray",
        default: "unset"
      },
      format: f,
      level: "silly",
      transforms: [
        y,
        e,
        w,
        t,
        r,
        o
      ],
      useStyles: process.env.FORCE_STYLES,
      writeFn({ message: g }) {
        (l[g.level] || l.info)(...g.data);
      }
    });
    function u(g) {
      const p = i({ logger: A, message: g, transport: u });
      u.writeFn({
        message: { ...g, data: p }
      });
    }
  }
  function y({ data: A, message: u, transport: g }) {
    return typeof g.format != "string" || !g.format.includes("%c") ? A : [
      `color:${I(u.level, g)}`,
      "color:unset",
      ...A
    ];
  }
  function R(A, u) {
    if (typeof A == "boolean")
      return A;
    const p = u === "error" || u === "warn" ? process.stderr : process.stdout;
    return p && p.isTTY;
  }
  function w(A) {
    const { message: u, transport: g } = A;
    return (R(g.useStyles, u.level) ? a : n)(A);
  }
  function I(A, u) {
    return u.colorMap[A] || u.colorMap.default;
  }
  return co;
}
var ho, rh;
function yw() {
  if (rh) return ho;
  rh = 1;
  const t = Ke, e = Lt, r = Li;
  class o extends t {
    asyncWriteQueue = [];
    bytesWritten = 0;
    hasActiveAsyncWriting = !1;
    path = null;
    initialSize = void 0;
    writeOptions = null;
    writeAsync = !1;
    constructor({
      path: i,
      writeOptions: l = { encoding: "utf8", flag: "a", mode: 438 },
      writeAsync: s = !1
    }) {
      super(), this.path = i, this.writeOptions = l, this.writeAsync = s;
    }
    get size() {
      return this.getSize();
    }
    clear() {
      try {
        return e.writeFileSync(this.path, "", {
          mode: this.writeOptions.mode,
          flag: "w"
        }), this.reset(), !0;
      } catch (i) {
        return i.code === "ENOENT" ? !0 : (this.emit("error", i, this), !1);
      }
    }
    crop(i) {
      try {
        const l = a(this.path, i || 4096);
        this.clear(), this.writeLine(`[log cropped]${r.EOL}${l}`);
      } catch (l) {
        this.emit(
          "error",
          new Error(`Couldn't crop file ${this.path}. ${l.message}`),
          this
        );
      }
    }
    getSize() {
      if (this.initialSize === void 0)
        try {
          const i = e.statSync(this.path);
          this.initialSize = i.size;
        } catch {
          this.initialSize = 0;
        }
      return this.initialSize + this.bytesWritten;
    }
    increaseBytesWrittenCounter(i) {
      this.bytesWritten += Buffer.byteLength(i, this.writeOptions.encoding);
    }
    isNull() {
      return !1;
    }
    nextAsyncWrite() {
      const i = this;
      if (this.hasActiveAsyncWriting || this.asyncWriteQueue.length === 0)
        return;
      const l = this.asyncWriteQueue.join("");
      this.asyncWriteQueue = [], this.hasActiveAsyncWriting = !0, e.writeFile(this.path, l, this.writeOptions, (s) => {
        i.hasActiveAsyncWriting = !1, s ? i.emit(
          "error",
          new Error(`Couldn't write to ${i.path}. ${s.message}`),
          this
        ) : i.increaseBytesWrittenCounter(l), i.nextAsyncWrite();
      });
    }
    reset() {
      this.initialSize = void 0, this.bytesWritten = 0;
    }
    toString() {
      return this.path;
    }
    writeLine(i) {
      if (i += r.EOL, this.writeAsync) {
        this.asyncWriteQueue.push(i), this.nextAsyncWrite();
        return;
      }
      try {
        e.writeFileSync(this.path, i, this.writeOptions), this.increaseBytesWrittenCounter(i);
      } catch (l) {
        this.emit(
          "error",
          new Error(`Couldn't write to ${this.path}. ${l.message}`),
          this
        );
      }
    }
  }
  ho = o;
  function a(n, i) {
    const l = Buffer.alloc(i), s = e.statSync(n), f = Math.min(s.size, i), d = Math.max(0, s.size - i), y = e.openSync(n, "r"), R = e.readSync(y, l, 0, f, d);
    return e.closeSync(y), l.toString("utf8", 0, R);
  }
  return ho;
}
var po, nh;
function lv() {
  if (nh) return po;
  nh = 1;
  const t = yw();
  class e extends t {
    clear() {
    }
    crop() {
    }
    getSize() {
      return 0;
    }
    isNull() {
      return !0;
    }
    writeLine() {
    }
  }
  return po = e, po;
}
var go, ih;
function uv() {
  if (ih) return go;
  ih = 1;
  const t = Ke, e = Lt, r = Ue, o = yw(), a = lv();
  class n extends t {
    store = {};
    constructor() {
      super(), this.emitError = this.emitError.bind(this);
    }
    /**
     * Provide a File object corresponding to the filePath
     * @param {string} filePath
     * @param {WriteOptions} [writeOptions]
     * @param {boolean} [writeAsync]
     * @return {File}
     */
    provide({ filePath: l, writeOptions: s = {}, writeAsync: f = !1 }) {
      let d;
      try {
        if (l = r.resolve(l), this.store[l])
          return this.store[l];
        d = this.createFile({ filePath: l, writeOptions: s, writeAsync: f });
      } catch (y) {
        d = new a({ path: l }), this.emitError(y, d);
      }
      return d.on("error", this.emitError), this.store[l] = d, d;
    }
    /**
     * @param {string} filePath
     * @param {WriteOptions} writeOptions
     * @param {boolean} async
     * @return {File}
     * @private
     */
    createFile({ filePath: l, writeOptions: s, writeAsync: f }) {
      return this.testFileWriting({ filePath: l, writeOptions: s }), new o({ path: l, writeOptions: s, writeAsync: f });
    }
    /**
     * @param {Error} error
     * @param {File} file
     * @private
     */
    emitError(l, s) {
      this.emit("error", l, s);
    }
    /**
     * @param {string} filePath
     * @param {WriteOptions} writeOptions
     * @private
     */
    testFileWriting({ filePath: l, writeOptions: s }) {
      e.mkdirSync(r.dirname(l), { recursive: !0 }), e.writeFileSync(l, "", { flag: "a", mode: s.mode });
    }
  }
  return go = n, go;
}
var yo, sh;
function fv() {
  if (sh) return yo;
  sh = 1;
  const t = Lt, e = Li, r = Ue, o = uv(), { transform: a } = Tr(), { removeStyles: n } = bc(), {
    format: i,
    concatFirstStringElements: l
  } = gw(), { toString: s } = ki();
  yo = d;
  const f = new o();
  function d(R, { registry: w = f, externalApi: I } = {}) {
    let A;
    return w.listenerCount("error") < 1 && w.on("error", (N, W) => {
      p(`Can't write to ${W}`, N);
    }), Object.assign(u, {
      fileName: y(R.variables.processType),
      format: "[{y}-{m}-{d} {h}:{i}:{s}.{ms}] [{level}]{scope} {text}",
      getFile: b,
      inspectOptions: { depth: 5 },
      level: "silly",
      maxSize: 1024 ** 2,
      readAllLogs: C,
      sync: !0,
      transforms: [n, i, l, s],
      writeOptions: { flag: "a", mode: 438, encoding: "utf8" },
      archiveLogFn(N) {
        const W = N.toString(), k = r.parse(W);
        try {
          t.renameSync(W, r.join(k.dir, `${k.name}.old${k.ext}`));
        } catch (L) {
          p("Could not rotate log", L);
          const U = Math.round(u.maxSize / 4);
          N.crop(Math.min(U, 256 * 1024));
        }
      },
      resolvePathFn(N) {
        return r.join(N.libraryDefaultDir, N.fileName);
      },
      setAppName(N) {
        R.dependencies.externalApi.setAppName(N);
      }
    });
    function u(N) {
      const W = b(N);
      u.maxSize > 0 && W.size > u.maxSize && (u.archiveLogFn(W), W.reset());
      const L = a({ logger: R, message: N, transport: u });
      W.writeLine(L);
    }
    function g() {
      A || (A = Object.create(
        Object.prototype,
        {
          ...Object.getOwnPropertyDescriptors(
            I.getPathVariables()
          ),
          fileName: {
            get() {
              return u.fileName;
            },
            enumerable: !0
          }
        }
      ), typeof u.archiveLog == "function" && (u.archiveLogFn = u.archiveLog, p("archiveLog is deprecated. Use archiveLogFn instead")), typeof u.resolvePath == "function" && (u.resolvePathFn = u.resolvePath, p("resolvePath is deprecated. Use resolvePathFn instead")));
    }
    function p(N, W = null, k = "error") {
      const L = [`electron-log.transports.file: ${N}`];
      W && L.push(W), R.transports.console({ data: L, date: /* @__PURE__ */ new Date(), level: k });
    }
    function b(N) {
      g();
      const W = u.resolvePathFn(A, N);
      return w.provide({
        filePath: W,
        writeAsync: !u.sync,
        writeOptions: u.writeOptions
      });
    }
    function C({ fileFilter: N = (W) => W.endsWith(".log") } = {}) {
      g();
      const W = r.dirname(u.resolvePathFn(A));
      return t.existsSync(W) ? t.readdirSync(W).map((k) => r.join(W, k)).filter(N).map((k) => {
        try {
          return {
            path: k,
            lines: t.readFileSync(k, "utf8").split(e.EOL)
          };
        } catch {
          return null;
        }
      }).filter(Boolean) : [];
    }
  }
  function y(R = process.type) {
    switch (R) {
      case "renderer":
        return "renderer.log";
      case "worker":
        return "worker.log";
      default:
        return "main.log";
    }
  }
  return yo;
}
var bo, oh;
function cv() {
  if (oh) return bo;
  oh = 1;
  const { maxDepth: t, toJSON: e } = ki(), { transform: r } = Tr();
  bo = o;
  function o(a, { externalApi: n }) {
    return Object.assign(i, {
      depth: 3,
      eventId: "__ELECTRON_LOG_IPC__",
      level: a.isDev ? "silly" : !1,
      transforms: [e, t]
    }), n?.isElectron() ? i : void 0;
    function i(l) {
      l?.variables?.processType !== "renderer" && n?.sendIpc(i.eventId, {
        ...l,
        data: r({ logger: a, message: l, transport: i })
      });
    }
  }
  return bo;
}
var _o, ah;
function dv() {
  if (ah) return _o;
  ah = 1;
  const t = pE, e = gE, { transform: r } = Tr(), { removeStyles: o } = bc(), { toJSON: a, maxDepth: n } = ki();
  _o = i;
  function i(l) {
    return Object.assign(s, {
      client: { name: "electron-application" },
      depth: 6,
      level: !1,
      requestOptions: {},
      transforms: [o, a, n],
      makeBodyFn({ message: f }) {
        return JSON.stringify({
          client: s.client,
          data: f.data,
          date: f.date.getTime(),
          level: f.level,
          scope: f.scope,
          variables: f.variables
        });
      },
      processErrorFn({ error: f }) {
        l.processMessage(
          {
            data: [`electron-log: can't POST ${s.url}`, f],
            level: "warn"
          },
          { transports: ["console", "file"] }
        );
      },
      sendRequestFn({ serverUrl: f, requestOptions: d, body: y }) {
        const w = (f.startsWith("https:") ? e : t).request(f, {
          method: "POST",
          ...d,
          headers: {
            "Content-Type": "application/json",
            "Content-Length": y.length,
            ...d.headers
          }
        });
        return w.write(y), w.end(), w;
      }
    });
    function s(f) {
      if (!s.url)
        return;
      const d = s.makeBodyFn({
        logger: l,
        message: { ...f, data: r({ logger: l, message: f, transport: s }) },
        transport: s
      }), y = s.sendRequestFn({
        serverUrl: s.url,
        requestOptions: s.requestOptions,
        body: Buffer.from(d, "utf8")
      });
      y.on("error", (R) => s.processErrorFn({
        error: R,
        logger: l,
        message: f,
        request: y,
        transport: s
      }));
    }
  }
  return _o;
}
var wo, lh;
function bw() {
  if (lh) return wo;
  lh = 1;
  const t = hw(), e = sv(), r = ov(), o = av(), a = fv(), n = cv(), i = dv();
  wo = l;
  function l({ dependencies: s, initializeFn: f }) {
    const d = new t({
      dependencies: s,
      errorHandler: new e(),
      eventLogger: new r(),
      initializeFn: f,
      isDev: s.externalApi?.isDev(),
      logId: "default",
      transportFactories: {
        console: o,
        file: a,
        ipc: n,
        remote: i
      },
      variables: {
        processType: "main"
      }
    });
    return d.default = d, d.Logger = t, d.processInternalErrorFn = (y) => {
      d.transports.console.writeFn({
        message: {
          data: ["Unhandled electron-log error", y],
          level: "error"
        }
      });
    }, d;
  }
  return wo;
}
var mo, uh;
function hv() {
  if (uh) return mo;
  uh = 1;
  const t = aE, e = nv(), { initialize: r } = iv(), o = bw(), a = new e({ electron: t }), n = o({
    dependencies: { externalApi: a },
    initializeFn: r
  });
  mo = n, a.onIpc("__ELECTRON_LOG__", (l, s) => {
    s.scope && n.Logger.getInstance(s).scope(s.scope);
    const f = new Date(s.date);
    i({
      ...s,
      date: f.getTime() ? f : /* @__PURE__ */ new Date()
    });
  }), a.onIpcInvoke("__ELECTRON_LOG__", (l, { cmd: s = "", logId: f }) => s === "getOptions" ? {
    levels: n.Logger.getInstance({ logId: f }).levels,
    logId: f
  } : (i({ data: [`Unknown cmd '${s}'`], level: "error" }), {}));
  function i(l) {
    n.Logger.getInstance(l)?.processMessage(l);
  }
  return mo;
}
var So, fh;
function pv() {
  if (fh) return So;
  fh = 1;
  const t = pw(), e = bw(), r = new t();
  return So = e({
    dependencies: { externalApi: r }
  }), So;
}
var ch;
function gv() {
  if (ch) return zr.exports;
  ch = 1;
  const t = typeof process > "u" || process.type === "renderer" || process.type === "worker", e = typeof process == "object" && process.type === "browser";
  return t ? (dw(), zr.exports = tv()) : e ? zr.exports = hv() : zr.exports = pv(), zr.exports;
}
var yv = gv();
const Ht = /* @__PURE__ */ Ci(yv), bv = ow(import.meta.url);
let cn;
try {
  cn = bv("pdf-parse");
} catch (t) {
  console.error("[RAG Engine] Failed to load pdf-parse:", t);
}
class Ot {
  constructor() {
    this.index = /* @__PURE__ */ new Map(), this.isIndexing = !1, this.docsPath = Ee.join(process.env.APP_ROOT || "", "docs");
  }
  setDocsPath(e) {
    e && e !== this.docsPath && (console.log(`[RAG Engine] Updating docs path to: ${e}`), this.docsPath = e, this.index.clear(), this.updateIndex());
  }
  static getInstance() {
    return Ot.instance || (Ot.instance = new Ot()), Ot.instance;
  }
  /**
   * Scan the docs folder and update the index
   */
  async updateIndex() {
    if (!this.isIndexing) {
      this.isIndexing = !0, console.log("[RAG Engine] Starting background indexing...");
      try {
        if (!await Se.pathExists(this.docsPath)) {
          await Se.ensureDir(this.docsPath), this.isIndexing = !1;
          return;
        }
        const e = await this.getAllFiles(this.docsPath);
        let r = 0;
        for (const o of e) {
          const n = (await Se.stat(o)).mtimeMs, i = Ee.relative(this.docsPath, o), l = this.index.get(i);
          if (!l || l.mtime !== n) {
            const s = await this.readFileContent(o);
            s && (this.index.set(i, {
              mtime: n,
              content: s,
              path: i
            }), r++);
          }
        }
        r > 0 && console.log(`[RAG Engine] Index updated: ${r} files reloaded. Total: ${this.index.size}`);
      } catch (e) {
        console.error("[RAG Engine] Indexing error:", e);
      } finally {
        this.isIndexing = !1;
      }
    }
  }
  /**
   * Search relevant content for a given system and campaign
   * Optimized for targeted indexing
   */
  async getRelevantContext(e, r) {
    this.index.size === 0 && await this.updateIndex();
    const o = e.toLowerCase(), a = r.toLowerCase(), n = [];
    for (const [i, l] of this.index.entries()) {
      const s = i.toLowerCase(), f = s.split(/[/\\]/), d = f.some((R) => R.includes(o)) || s.includes("systems"), y = f.some((R) => R.includes(a)) || s.includes("campaigns");
      if ((d || y) && ((s.includes(o) ? 2 : 0) + (s.includes(a) ? 2 : 0) > 0 || d && s.includes("systems") || y && s.includes("campaigns"))) {
        const w = `[Source: ${i}]
`;
        n.push(w + l.content);
      }
    }
    return n.slice(0, 15).join(`

---

`);
  }
  async getAllFiles(e) {
    const r = [], o = await Se.readdir(e);
    for (const a of o) {
      const n = Ee.join(e, a), i = await Se.stat(n);
      if (i && i.isDirectory())
        r.push(...await this.getAllFiles(n));
      else {
        const l = Ee.extname(a).toLowerCase();
        [".md", ".txt", ".pdf"].includes(l) && r.push(n);
      }
    }
    return r;
  }
  async readFileContent(e) {
    const r = Ee.extname(e).toLowerCase(), o = 5e4;
    try {
      if (r === ".md" || r === ".txt") {
        const a = await Se.readFile(e, "utf-8");
        return a.length > o ? a.substring(0, o) + "... [Tronqué]" : a;
      } else if (r === ".pdf") {
        const a = await Se.readFile(e);
        if (typeof cn == "function")
          return (await cn(a)).text || "";
      }
    } catch (a) {
      console.error(`[RAG Engine] Error reading ${e}:`, a);
    }
    return null;
  }
}
function _v() {
  const t = Ot.getInstance();
  Re.handle("ai:search-context", async (e, r, o) => await t.getRelevantContext(r, o)), Re.handle("ai:reindex", async (e, r) => (r && t.setDocsPath(r), await t.updateIndex(), !0)), Re.handle("ai:list-docs", async () => {
    const e = Ot.getInstance().docsPath;
    if (console.log(`[RAG Engine] Listing docs in root: ${e}`), !await Se.pathExists(e))
      return console.warn(`[RAG Engine] Root path does not exist: ${e}`), [];
    async function r(o) {
      const a = await Se.readdir(o, { withFileTypes: !0 });
      return (await Promise.all(a.map(async (i) => {
        const l = Ee.join(o, i.name), s = Ee.relative(e, l);
        return i.name.startsWith(".") ? null : i.isDirectory() ? {
          name: i.name,
          path: s.replace(/\\/g, "/"),
          type: "directory",
          children: await r(l)
        } : {
          name: i.name,
          path: s.replace(/\\/g, "/"),
          type: "file",
          extension: Ee.extname(i.name).toLowerCase()
        };
      }))).filter((i) => i !== null);
    }
    return r(e);
  }), Re.handle("ai:read-doc", async (e, r) => {
    const o = Ot.getInstance().docsPath, a = Ee.join(o, r);
    return !await Se.pathExists(a) || !a.startsWith(o) ? null : Se.readFile(a, "utf-8");
  }), Re.handle("ai:extract-pdf", async (e, r) => {
    const o = Ot.getInstance().docsPath, a = Ee.join(o, r);
    if (!await Se.pathExists(a) || !a.startsWith(o)) return "Fichier introuvable.";
    try {
      const n = await Se.readFile(a);
      return cn ? (await cn(n)).text || "" : "PDF Parser non disponible.";
    } catch (n) {
      return console.error("[RAGEngine] PDF Extraction Error:", n), "Erreur lors de l'extraction.";
    }
  }), Re.handle("ai:write-doc", async (e, r, o) => {
    const a = Ot.getInstance().docsPath, n = Ee.join(a, r);
    if (console.log(`[RAG Engine] Writing doc: ${r} -> ${n}`), !n.startsWith(a))
      return console.error(`[RAG Engine] Security Violation: Attempted to write outside docs: ${n}`), !1;
    try {
      return await Se.ensureDir(Ee.dirname(n)), await Se.writeFile(n, o, "utf-8"), Ot.getInstance().updateIndex(), !0;
    } catch (i) {
      return console.error("[RAG Engine] Error writing doc:", i), !1;
    }
  }), setInterval(() => t.updateIndex(), 1e3 * 60 * 15), setTimeout(() => t.updateIndex(), 5e3);
}
const _w = process.env.USERPROFILE || process.env.HOME || "", ww = Ni.join(_w, ".antigravity", "notebooklm-mcp"), mw = Ni.join(_w, "mcp_bridge_debug.log"), Sw = "python", oc = Ni.join(ww, "run_mcp.py"), wv = Ni.join(ww, "notebooklm-config.json");
function We(t) {
  try {
    const e = (/* @__PURE__ */ new Date()).toISOString();
    iw.appendFileSync(mw, `[${e}] ${t}
`);
  } catch (e) {
    console.error("Failed to write to debug log:", e);
  }
}
try {
  process.type === "browser" && iw.writeFileSync(mw, `--- MCP Bridge Started ---
`);
} catch (t) {
  console.error("Failed to initialize debug log:", t);
}
let lr = null, mv = 1;
const Or = /* @__PURE__ */ new Map();
let Ir = "", Dr = null, Sr = null, Nr = !1;
async function Sv() {
  if (!Nr)
    return Sr ? (We("Waiting for existing initialization to complete..."), Sr) : (Sr = (async () => {
      try {
        We("Performing initialize handshake..."), await ac("initialize", {
          protocolVersion: "2024-11-05",
          capabilities: {
            sampling: {},
            roots: { listChanged: !1 }
          },
          clientInfo: { name: "gm-os", version: "1.0.0" }
        }), We("Sending notifications/initialized...");
        const t = await Ew(), e = JSON.stringify({
          jsonrpc: "2.0",
          method: "notifications/initialized"
        }) + `
`;
        t.stdin?.write(e), We("Handshake: Waiting 200ms for server state to stabilize..."), await new Promise((r) => setTimeout(r, 200)), Nr = !0, We("Handshake SUCCESS");
      } catch (t) {
        throw We(`Handshake FAILED: ${t}`), console.error("[MCP Bridge] Handshake failed:", t), Sr = null, t;
      }
    })(), Sr);
}
async function Ew() {
  return lr && lr.connected ? lr : Dr || (Dr = (async () => {
    Nr = !1, Sr = null, We(`Spawning NotebookLM MCP Server with wrapper: ${oc}`);
    const t = lw(Sw, [oc, "server", "--debug"], {
      stdio: ["pipe", "pipe", "pipe"],
      env: {
        ...process.env,
        PYTHONUNBUFFERED: "1",
        NOTEBOOKLM_CONFIG: wv
      }
    });
    return t.stdout?.on("data", (e) => {
      Ir += e.toString();
      let r = Ir.indexOf(`
`);
      for (; r !== -1; ) {
        const o = Ir.substring(0, r).trim();
        if (Ir = Ir.substring(r + 1), o) {
          We(`<<< RECV: ${o}`);
          try {
            const a = JSON.parse(o);
            if (We(`[Bridge] Parsed ID: ${a.id}, Method: ${a.method || "N/A"}`), a.id !== void 0) {
              const n = Or.get(a.id);
              if (n) {
                if (We(`[Bridge] Matching pending request found for ID ${a.id} (${n.method})`), clearTimeout(n.timeout), a.error) {
                  const i = JSON.stringify(a.error);
                  We(`!!! ERROR for ID ${a.id}: ${i}`), n.reject(new Error(`${a.error.message || "Unknown error"} (Data: ${i})`));
                } else
                  We(`[Bridge] Resolving ID ${a.id} with ${JSON.stringify(a.result).substring(0, 100)}...`), n.resolve(a.result);
                Or.delete(a.id);
              } else
                We(`[Bridge] WARNING: Received response for unknown ID ${a.id}`);
            } else a.method === "notifications/message" && We(`[Server Notification] ${a.params?.message}`);
          } catch (a) {
            We(`[Bridge] JSON Parse Error or Handling Error: ${a}`), We(`[Raw Output] ${o}`);
          }
        }
        r = Ir.indexOf(`
`);
      }
    }), t.stderr?.on("data", (e) => {
      const r = e.toString().trim();
      r && (We(`stderr: ${r}`), r.toLowerCase().includes("error") && console.error(`[MCP Server] ${r}`));
    }), t.on("exit", (e, r) => {
      We(`Server exited (code: ${e}, signal: ${r})`), Nr = !1, lr = null, Dr = null, Or.forEach((o) => o.reject(new Error(`MCP Server exited with code ${e}`))), Or.clear();
    }), lr = t, t;
  })(), Dr);
}
async function ac(t, e) {
  const r = await Ew();
  t !== "initialize" && !Nr && await Sv();
  const o = mv++;
  return We(`[Bridge] Preparing request ${o} for method: ${t}`), new Promise((a, n) => {
    const l = setTimeout(() => {
      const f = `MCP Request ${o} (${t}) timed out after 45m`;
      We(`!!! TIMEOUT: ${f}`), console.error(`[MCP Bridge] ${f}`), Or.delete(o), n(new Error(f));
    }, 27e5);
    Or.set(o, { resolve: a, reject: n, method: t, timeout: l });
    const s = JSON.stringify({
      jsonrpc: "2.0",
      id: o,
      method: t,
      params: e
    }) + `
`;
    We(`>>> SEND [ID:${o}]: ${s.trim().substring(0, 200)}...`), r.stdin?.write(s);
  });
}
function Ev() {
  console.log("[MCP Bridge] Registering IPC Handlers"), Re.handle("mcp:list-tools", async () => {
    try {
      return console.log("[MCP Bridge] Requesting tool list..."), (await ac("tools/list", {})).tools || [];
    } catch (t) {
      throw console.error("[MCP Bridge] tools/list failed:", t), t;
    }
  }), Re.handle("mcp:call-tool", async (t, e, r, o) => {
    try {
      console.log(`[MCP Bridge] Calling tool: ${r}`);
      const a = JSON.parse(JSON.stringify(o)), n = await ac("tools/call", {
        name: r,
        arguments: a
      });
      if (n && n.content && Array.isArray(n.content)) {
        const i = n.content.filter((l) => l.type === "text").map((l) => l.text).join(`
`);
        if (i.trim().startsWith("{")) {
          let l = null;
          try {
            if (l = JSON.parse(i), l.status === "success")
              return We("[Bridge] Success detected, unwrapping content..."), typeof l.answer == "string" ? { content: l.answer || "L'Oracle n'a pas trouvé de réponse précise pour ce notebook." } : l;
            if (l.status === "error") {
              const s = l.message || l.error || "Erreur inconnue provenant de l'Oracle.";
              throw new Error(s);
            }
          } catch (s) {
            const f = s;
            if (f.message && f.message.includes("provenant de l'Oracle") || l?.status === "error")
              throw console.error("[MCP Bridge] Oracle Error intercepted:", f), f;
          }
        }
        return { content: i };
      }
      return n;
    } catch (a) {
      throw console.error(`[MCP Bridge] tools/call ${r} failed:`, a), a;
    }
  }), Re.handle("mcp:reauthenticate", async () => {
    We("[Auth] Triggering re-authentication CLI...");
    try {
      return lw(Sw, [oc, "auth_cli"], {
        shell: !1,
        detached: !0,
        stdio: "ignore"
      }).unref(), We("[Auth] Auth CLI process spawned via wrapper."), { success: !0, message: "Authentification lancée." };
    } catch (t) {
      throw We(`[Auth] Error: ${t}`), t;
    }
  }), Re.handle("mcp:restart", async () => (We("[System] Restarting MCP Server..."), lr && (lr.kill(), lr = null), Dr = null, Nr = !1, Sr = null, { success: !0, message: "Serveur MCP redémarré avec succès." }));
}
const Fn = "C:\\Users\\david\\OneDrive\\Obsidian Vault";
function vv() {
  console.log("[Obsidian Bridge] Registering IPC Handlers"), Re.handle("obsidian:list-notes", async (t, e) => {
    const r = e || Fn;
    if (!await Se.pathExists(r))
      return console.error(`[Obsidian Bridge] Vault path not found: ${r}`), [];
    async function o(a) {
      const n = await Se.readdir(a, { withFileTypes: !0 });
      return (await Promise.all(n.map(async (l) => {
        const s = Ee.join(a, l.name), f = Ee.relative(r, s);
        if (l.name.startsWith(".")) return null;
        if (l.isDirectory()) {
          const d = await o(s);
          return d.length === 0 ? null : {
            name: l.name,
            path: f,
            type: "directory",
            children: d
          };
        }
        return l.name.toLowerCase().endsWith(".md") ? {
          name: l.name,
          path: f,
          type: "file"
        } : null;
      }))).filter((l) => l !== null);
    }
    try {
      return await o(r);
    } catch (a) {
      return console.error("[Obsidian Bridge] Error listing notes:", a), [];
    }
  }), Re.handle("obsidian:read-note", async (t, e, r) => {
    const o = r || Fn, a = Ee.join(o, e);
    if (!a.startsWith(o))
      return console.error(`[Obsidian Bridge] Security Violation: Attempted to read outside vault: ${a}`), null;
    if (!await Se.pathExists(a))
      return console.error(`[Obsidian Bridge] Note not found: ${a}`), null;
    try {
      return await Se.readFile(a, "utf-8");
    } catch (n) {
      return console.error("[Obsidian Bridge] Error reading note:", n), null;
    }
  }), Re.handle("obsidian:write-note", async (t, e, r, o) => {
    const a = o || Fn, n = Ee.join(a, e);
    if (!n.startsWith(a))
      return console.error(`[Obsidian Bridge] Security Violation: Attempted to write outside vault: ${n}`), !1;
    try {
      return await Se.ensureDir(Ee.dirname(n)), await Se.writeFile(n, r, "utf-8"), !0;
    } catch (i) {
      return console.error("[Obsidian Bridge] Error writing note:", i), !1;
    }
  }), Re.handle("obsidian:ensure-directory", async (t, e, r) => {
    const o = r || Fn, a = Ee.join(o, e);
    if (!a.startsWith(o))
      return console.error(`[Obsidian Bridge] Security Violation: Attempted to create directory outside vault: ${a}`), !1;
    try {
      return await Se.ensureDir(a), !0;
    } catch (n) {
      return console.error("[Obsidian Bridge] Error creating directory:", n), !1;
    }
  }), Re.handle("obsidian:select-vault", async (t) => {
    const { filePaths: e } = await Vt.showOpenDialog({
      title: "Sélectionner le coffre Obsidian",
      properties: ["openDirectory", "createDirectory"]
    });
    return e && e.length > 0 ? e[0] : null;
  });
}
var Eo, dh;
function Rv() {
  return dh || (dh = 1, Eo = typeof process == "object" && process && process.platform === "win32" ? { sep: "\\" } : { sep: "/" }), Eo;
}
var vo, hh;
function vw() {
  if (hh) return vo;
  hh = 1, vo = t;
  function t(o, a, n) {
    o instanceof RegExp && (o = e(o, n)), a instanceof RegExp && (a = e(a, n));
    var i = r(o, a, n);
    return i && {
      start: i[0],
      end: i[1],
      pre: n.slice(0, i[0]),
      body: n.slice(i[0] + o.length, i[1]),
      post: n.slice(i[1] + a.length)
    };
  }
  function e(o, a) {
    var n = a.match(o);
    return n ? n[0] : null;
  }
  t.range = r;
  function r(o, a, n) {
    var i, l, s, f, d, y = n.indexOf(o), R = n.indexOf(a, y + 1), w = y;
    if (y >= 0 && R > 0) {
      if (o === a)
        return [y, R];
      for (i = [], s = n.length; w >= 0 && !d; )
        w == y ? (i.push(w), y = n.indexOf(o, w + 1)) : i.length == 1 ? d = [i.pop(), R] : (l = i.pop(), l < s && (s = l, f = R), R = n.indexOf(a, w + 1)), w = y < R && y >= 0 ? y : R;
      i.length && (d = [s, f]);
    }
    return d;
  }
  return vo;
}
var Ro, ph;
function Av() {
  if (ph) return Ro;
  ph = 1;
  var t = vw();
  Ro = d;
  var e = "\0SLASH" + Math.random() + "\0", r = "\0OPEN" + Math.random() + "\0", o = "\0CLOSE" + Math.random() + "\0", a = "\0COMMA" + Math.random() + "\0", n = "\0PERIOD" + Math.random() + "\0";
  function i(u) {
    return parseInt(u, 10) == u ? parseInt(u, 10) : u.charCodeAt(0);
  }
  function l(u) {
    return u.split("\\\\").join(e).split("\\{").join(r).split("\\}").join(o).split("\\,").join(a).split("\\.").join(n);
  }
  function s(u) {
    return u.split(e).join("\\").split(r).join("{").split(o).join("}").split(a).join(",").split(n).join(".");
  }
  function f(u) {
    if (!u)
      return [""];
    var g = [], p = t("{", "}", u);
    if (!p)
      return u.split(",");
    var b = p.pre, C = p.body, N = p.post, W = b.split(",");
    W[W.length - 1] += "{" + C + "}";
    var k = f(N);
    return N.length && (W[W.length - 1] += k.shift(), W.push.apply(W, k)), g.push.apply(g, W), g;
  }
  function d(u) {
    return u ? (u.substr(0, 2) === "{}" && (u = "\\{\\}" + u.substr(2)), A(l(u), !0).map(s)) : [];
  }
  function y(u) {
    return "{" + u + "}";
  }
  function R(u) {
    return /^-?0\d/.test(u);
  }
  function w(u, g) {
    return u <= g;
  }
  function I(u, g) {
    return u >= g;
  }
  function A(u, g) {
    var p = [], b = t("{", "}", u);
    if (!b) return [u];
    var C = b.pre, N = b.post.length ? A(b.post, !1) : [""];
    if (/\$$/.test(b.pre))
      for (var W = 0; W < N.length; W++) {
        var k = C + "{" + b.body + "}" + N[W];
        p.push(k);
      }
    else {
      var L = /^-?\d+\.\.-?\d+(?:\.\.-?\d+)?$/.test(b.body), U = /^[a-zA-Z]\.\.[a-zA-Z](?:\.\.-?\d+)?$/.test(b.body), x = L || U, D = b.body.indexOf(",") >= 0;
      if (!x && !D)
        return b.post.match(/,(?!,).*\}/) ? (u = b.pre + "{" + b.body + o + b.post, A(u)) : [u];
      var F;
      if (x)
        F = b.body.split(/\.\./);
      else if (F = f(b.body), F.length === 1 && (F = A(F[0], !1).map(y), F.length === 1))
        return N.map(function(ce) {
          return b.pre + F[0] + ce;
        });
      var T;
      if (x) {
        var J = i(F[0]), re = i(F[1]), se = Math.max(F[0].length, F[1].length), ne = F.length == 3 ? Math.max(Math.abs(i(F[2])), 1) : 1, M = w, Q = re < J;
        Q && (ne *= -1, M = I);
        var v = F.some(R);
        T = [];
        for (var $ = J; M($, re); $ += ne) {
          var ee;
          if (U)
            ee = String.fromCharCode($), ee === "\\" && (ee = "");
          else if (ee = String($), v) {
            var B = se - ee.length;
            if (B > 0) {
              var le = new Array(B + 1).join("0");
              $ < 0 ? ee = "-" + le + ee.slice(1) : ee = le + ee;
            }
          }
          T.push(ee);
        }
      } else {
        T = [];
        for (var ae = 0; ae < F.length; ae++)
          T.push.apply(T, A(F[ae], !1));
      }
      for (var ae = 0; ae < T.length; ae++)
        for (var W = 0; W < N.length; W++) {
          var k = C + T[ae] + N[W];
          (!g || x || k) && p.push(k);
        }
    }
    return p;
  }
  return Ro;
}
var jn, gh;
function Tv() {
  if (gh) return jn;
  gh = 1;
  const t = jn = (k, L, U = {}) => (u(L), !U.nocomment && L.charAt(0) === "#" ? !1 : new W(L, U).match(k));
  jn = t;
  const e = Rv();
  t.sep = e.sep;
  const r = /* @__PURE__ */ Symbol("globstar **");
  t.GLOBSTAR = r;
  const o = Av(), a = {
    "!": { open: "(?:(?!(?:", close: "))[^/]*?)" },
    "?": { open: "(?:", close: ")?" },
    "+": { open: "(?:", close: ")+" },
    "*": { open: "(?:", close: ")*" },
    "@": { open: "(?:", close: ")" }
  }, n = "[^/]", i = n + "*?", l = "(?:(?!(?:\\/|^)(?:\\.{1,2})($|\\/)).)*?", s = "(?:(?!(?:\\/|^)\\.).)*?", f = (k) => k.split("").reduce((L, U) => (L[U] = !0, L), {}), d = f("().*{}+?[]^$\\!"), y = f("[.("), R = /\/+/;
  t.filter = (k, L = {}) => (U, x, D) => t(U, k, L);
  const w = (k, L = {}) => {
    const U = {};
    return Object.keys(k).forEach((x) => U[x] = k[x]), Object.keys(L).forEach((x) => U[x] = L[x]), U;
  };
  t.defaults = (k) => {
    if (!k || typeof k != "object" || !Object.keys(k).length)
      return t;
    const L = t, U = (x, D, F) => L(x, D, w(k, F));
    return U.Minimatch = class extends L.Minimatch {
      constructor(D, F) {
        super(D, w(k, F));
      }
    }, U.Minimatch.defaults = (x) => L.defaults(w(k, x)).Minimatch, U.filter = (x, D) => L.filter(x, w(k, D)), U.defaults = (x) => L.defaults(w(k, x)), U.makeRe = (x, D) => L.makeRe(x, w(k, D)), U.braceExpand = (x, D) => L.braceExpand(x, w(k, D)), U.match = (x, D, F) => L.match(x, D, w(k, F)), U;
  }, t.braceExpand = (k, L) => I(k, L);
  const I = (k, L = {}) => (u(k), L.nobrace || !/\{(?:(?!\{).)*\}/.test(k) ? [k] : o(k)), A = 1024 * 64, u = (k) => {
    if (typeof k != "string")
      throw new TypeError("invalid pattern");
    if (k.length > A)
      throw new TypeError("pattern is too long");
  }, g = /* @__PURE__ */ Symbol("subparse");
  t.makeRe = (k, L) => new W(k, L || {}).makeRe(), t.match = (k, L, U = {}) => {
    const x = new W(L, U);
    return k = k.filter((D) => x.match(D)), x.options.nonull && !k.length && k.push(L), k;
  };
  const p = (k) => k.replace(/\\(.)/g, "$1"), b = (k) => k.replace(/\\([^-\]])/g, "$1"), C = (k) => k.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, "\\$&"), N = (k) => k.replace(/[[\]\\]/g, "\\$&");
  class W {
    constructor(L, U) {
      u(L), U || (U = {}), this.options = U, this.maxGlobstarRecursion = U.maxGlobstarRecursion !== void 0 ? U.maxGlobstarRecursion : 200, this.set = [], this.pattern = L, this.windowsPathsNoEscape = !!U.windowsPathsNoEscape || U.allowWindowsEscape === !1, this.windowsPathsNoEscape && (this.pattern = this.pattern.replace(/\\/g, "/")), this.regexp = null, this.negate = !1, this.comment = !1, this.empty = !1, this.partial = !!U.partial, this.make();
    }
    debug() {
    }
    make() {
      const L = this.pattern, U = this.options;
      if (!U.nocomment && L.charAt(0) === "#") {
        this.comment = !0;
        return;
      }
      if (!L) {
        this.empty = !0;
        return;
      }
      this.parseNegate();
      let x = this.globSet = this.braceExpand();
      U.debug && (this.debug = (...D) => console.error(...D)), this.debug(this.pattern, x), x = this.globParts = x.map((D) => D.split(R)), this.debug(this.pattern, x), x = x.map((D, F, T) => D.map(this.parse, this)), this.debug(this.pattern, x), x = x.filter((D) => D.indexOf(!1) === -1), this.debug(this.pattern, x), this.set = x;
    }
    parseNegate() {
      if (this.options.nonegate) return;
      const L = this.pattern;
      let U = !1, x = 0;
      for (let D = 0; D < L.length && L.charAt(D) === "!"; D++)
        U = !U, x++;
      x && (this.pattern = L.slice(x)), this.negate = U;
    }
    // set partial to true to test if, for example,
    // "/a/b" matches the start of "/*/b/*/d"
    // Partial means, if you run out of file before you run
    // out of pattern, then that's fine, as long as all
    // the parts match.
    matchOne(L, U, x) {
      return U.indexOf(r) !== -1 ? this._matchGlobstar(L, U, x, 0, 0) : this._matchOne(L, U, x, 0, 0);
    }
    _matchGlobstar(L, U, x, D, F) {
      let T = -1;
      for (let ae = F; ae < U.length; ae++)
        if (U[ae] === r) {
          T = ae;
          break;
        }
      let J = -1;
      for (let ae = U.length - 1; ae >= 0; ae--)
        if (U[ae] === r) {
          J = ae;
          break;
        }
      const re = U.slice(F, T), se = x ? U.slice(T + 1) : U.slice(T + 1, J), ne = x ? [] : U.slice(J + 1);
      if (re.length) {
        const ae = L.slice(D, D + re.length);
        if (!this._matchOne(ae, re, x, 0, 0))
          return !1;
        D += re.length;
      }
      let M = 0;
      if (ne.length) {
        if (ne.length + D > L.length) return !1;
        const ae = L.length - ne.length;
        if (this._matchOne(L, ne, x, ae, 0))
          M = ne.length;
        else {
          if (L[L.length - 1] !== "" || D + ne.length === L.length || !this._matchOne(L, ne, x, ae - 1, 0))
            return !1;
          M = ne.length + 1;
        }
      }
      if (!se.length) {
        let ae = !!M;
        for (let ce = D; ce < L.length - M; ce++) {
          const Y = String(L[ce]);
          if (ae = !0, Y === "." || Y === ".." || !this.options.dot && Y.charAt(0) === ".")
            return !1;
        }
        return x || ae;
      }
      const Q = [[[], 0]];
      let v = Q[0], $ = 0;
      const ee = [0];
      for (const ae of se)
        ae === r ? (ee.push($), v = [[], 0], Q.push(v)) : (v[0].push(ae), $++);
      let B = Q.length - 1;
      const le = L.length - M;
      for (const ae of Q)
        ae[1] = le - (ee[B--] + ae[0].length);
      return !!this._matchGlobStarBodySections(
        L,
        Q,
        D,
        0,
        x,
        0,
        !!M
      );
    }
    // return false for "nope, not matching"
    // return null for "not matching, cannot keep trying"
    _matchGlobStarBodySections(L, U, x, D, F, T, J) {
      const re = U[D];
      if (!re) {
        for (let M = x; M < L.length; M++) {
          J = !0;
          const Q = L[M];
          if (Q === "." || Q === ".." || !this.options.dot && Q.charAt(0) === ".")
            return !1;
        }
        return J;
      }
      const [se, ne] = re;
      for (; x <= ne; ) {
        if (this._matchOne(
          L.slice(0, x + se.length),
          se,
          F,
          x,
          0
        ) && T < this.maxGlobstarRecursion) {
          const v = this._matchGlobStarBodySections(
            L,
            U,
            x + se.length,
            D + 1,
            F,
            T + 1,
            J
          );
          if (v !== !1)
            return v;
        }
        const Q = L[x];
        if (Q === "." || Q === ".." || !this.options.dot && Q.charAt(0) === ".")
          return !1;
        x++;
      }
      return F || null;
    }
    _matchOne(L, U, x, D, F) {
      let T, J, re, se;
      for (T = D, J = F, re = L.length, se = U.length; T < re && J < se; T++, J++) {
        this.debug("matchOne loop");
        const ne = U[J], M = L[T];
        if (this.debug(U, ne, M), ne === !1 || ne === r) return !1;
        let Q;
        if (typeof ne == "string" ? (Q = M === ne, this.debug("string match", ne, M, Q)) : (Q = M.match(ne), this.debug("pattern match", ne, M, Q)), !Q) return !1;
      }
      if (T === re && J === se)
        return !0;
      if (T === re)
        return x;
      if (J === se)
        return T === re - 1 && L[T] === "";
      throw new Error("wtf?");
    }
    braceExpand() {
      return I(this.pattern, this.options);
    }
    parse(L, U) {
      u(L);
      const x = this.options;
      if (L === "**")
        if (x.noglobstar)
          L = "*";
        else
          return r;
      if (L === "") return "";
      let D = "", F = !1, T = !1;
      const J = [], re = [];
      let se, ne = !1, M = -1, Q = -1, v, $, ee, B = L.charAt(0) === ".", le = x.dot || B;
      const ae = () => B ? "" : le ? "(?!(?:^|\\/)\\.{1,2}(?:$|\\/))" : "(?!\\.)", ce = (V) => V.charAt(0) === "." ? "" : x.dot ? "(?!(?:^|\\/)\\.{1,2}(?:$|\\/))" : "(?!\\.)", Y = () => {
        if (se) {
          switch (se) {
            case "*":
              D += i, F = !0;
              break;
            case "?":
              D += n, F = !0;
              break;
            default:
              D += "\\" + se;
              break;
          }
          this.debug("clearStateChar %j %j", se, D), se = !1;
        }
      };
      for (let V = 0, Z; V < L.length && (Z = L.charAt(V)); V++) {
        if (this.debug("%s	%s %s %j", L, V, D, Z), T) {
          if (Z === "/")
            return !1;
          d[Z] && (D += "\\"), D += Z, T = !1;
          continue;
        }
        switch (Z) {
          /* istanbul ignore next */
          case "/":
            return !1;
          case "\\":
            if (ne && L.charAt(V + 1) === "-") {
              D += Z;
              continue;
            }
            Y(), T = !0;
            continue;
          // the various stateChar values
          // for the "extglob" stuff.
          case "?":
          case "*":
          case "+":
          case "@":
          case "!":
            if (this.debug("%s	%s %s %j <-- stateChar", L, V, D, Z), ne) {
              this.debug("  in class"), Z === "!" && V === Q + 1 && (Z = "^"), D += Z;
              continue;
            }
            if (Z === "*" && se === "*") continue;
            this.debug("call clearStateChar %j", se), Y(), se = Z, x.noext && Y();
            continue;
          case "(": {
            if (ne) {
              D += "(";
              continue;
            }
            if (!se) {
              D += "\\(";
              continue;
            }
            const X = {
              type: se,
              start: V - 1,
              reStart: D.length,
              open: a[se].open,
              close: a[se].close
            };
            this.debug(this.pattern, "	", X), J.push(X), D += X.open, X.start === 0 && X.type !== "!" && (B = !0, D += ce(L.slice(V + 1))), this.debug("plType %j %j", se, D), se = !1;
            continue;
          }
          case ")": {
            const X = J[J.length - 1];
            if (ne || !X) {
              D += "\\)";
              continue;
            }
            J.pop(), Y(), F = !0, $ = X, D += $.close, $.type === "!" && re.push(Object.assign($, { reEnd: D.length }));
            continue;
          }
          case "|": {
            const X = J[J.length - 1];
            if (ne || !X) {
              D += "\\|";
              continue;
            }
            Y(), D += "|", X.start === 0 && X.type !== "!" && (B = !0, D += ce(L.slice(V + 1)));
            continue;
          }
          // these are mostly the same in regexp and glob
          case "[":
            if (Y(), ne) {
              D += "\\" + Z;
              continue;
            }
            ne = !0, Q = V, M = D.length, D += Z;
            continue;
          case "]":
            if (V === Q + 1 || !ne) {
              D += "\\" + Z;
              continue;
            }
            v = L.substring(Q + 1, V);
            try {
              RegExp("[" + N(b(v)) + "]"), D += Z;
            } catch {
              D = D.substring(0, M) + "(?:$.)";
            }
            F = !0, ne = !1;
            continue;
          default:
            Y(), d[Z] && !(Z === "^" && ne) && (D += "\\"), D += Z;
            break;
        }
      }
      for (ne && (v = L.slice(Q + 1), ee = this.parse(v, g), D = D.substring(0, M) + "\\[" + ee[0], F = F || ee[1]), $ = J.pop(); $; $ = J.pop()) {
        let V;
        V = D.slice($.reStart + $.open.length), this.debug("setting tail", D, $), V = V.replace(/((?:\\{2}){0,64})(\\?)\|/g, (X, m, c) => (c || (c = "\\"), m + m + c + "|")), this.debug(`tail=%j
   %s`, V, V, $, D);
        const Z = $.type === "*" ? i : $.type === "?" ? n : "\\" + $.type;
        F = !0, D = D.slice(0, $.reStart) + Z + "\\(" + V;
      }
      Y(), T && (D += "\\\\");
      const P = y[D.charAt(0)];
      for (let V = re.length - 1; V > -1; V--) {
        const Z = re[V], X = D.slice(0, Z.reStart), m = D.slice(Z.reStart, Z.reEnd - 8);
        let c = D.slice(Z.reEnd);
        const E = D.slice(Z.reEnd - 8, Z.reEnd) + c, j = X.split(")").length, G = X.split("(").length - j;
        let K = c;
        for (let H = 0; H < G; H++)
          K = K.replace(/\)[+*?]?/, "");
        c = K;
        const O = c === "" && U !== g ? "(?:$|\\/)" : "";
        D = X + m + c + O + E;
      }
      if (D !== "" && F && (D = "(?=.)" + D), P && (D = ae() + D), U === g)
        return [D, F];
      if (x.nocase && !F && (F = L.toUpperCase() !== L.toLowerCase()), !F)
        return p(L);
      const q = x.nocase ? "i" : "";
      try {
        return Object.assign(new RegExp("^" + D + "$", q), {
          _glob: L,
          _src: D
        });
      } catch {
        return new RegExp("$.");
      }
    }
    makeRe() {
      if (this.regexp || this.regexp === !1) return this.regexp;
      const L = this.set;
      if (!L.length)
        return this.regexp = !1, this.regexp;
      const U = this.options, x = U.noglobstar ? i : U.dot ? l : s, D = U.nocase ? "i" : "";
      let F = L.map((T) => (T = T.map(
        (J) => typeof J == "string" ? C(J) : J === r ? r : J._src
      ).reduce((J, re) => (J[J.length - 1] === r && re === r || J.push(re), J), []), T.forEach((J, re) => {
        J !== r || T[re - 1] === r || (re === 0 ? T.length > 1 ? T[re + 1] = "(?:\\/|" + x + "\\/)?" + T[re + 1] : T[re] = x : re === T.length - 1 ? T[re - 1] += "(?:\\/|" + x + ")?" : (T[re - 1] += "(?:\\/|\\/" + x + "\\/)" + T[re + 1], T[re + 1] = r));
      }), T.filter((J) => J !== r).join("/"))).join("|");
      F = "^(?:" + F + ")$", this.negate && (F = "^(?!" + F + ").*$");
      try {
        this.regexp = new RegExp(F, D);
      } catch {
        this.regexp = !1;
      }
      return this.regexp;
    }
    match(L, U = this.partial) {
      if (this.debug("match", L, this.pattern), this.comment) return !1;
      if (this.empty) return L === "";
      if (L === "/" && U) return !0;
      const x = this.options;
      e.sep !== "/" && (L = L.split(e.sep).join("/")), L = L.split(R), this.debug(this.pattern, "split", L);
      const D = this.set;
      this.debug(this.pattern, "set", D);
      let F;
      for (let T = L.length - 1; T >= 0 && (F = L[T], !F); T--)
        ;
      for (let T = 0; T < D.length; T++) {
        const J = D[T];
        let re = L;
        if (x.matchBase && J.length === 1 && (re = [F]), this.matchOne(re, J, U))
          return x.flipNegate ? !0 : !this.negate;
      }
      return x.flipNegate ? !1 : this.negate;
    }
    static defaults(L) {
      return t.defaults(L).Minimatch;
    }
  }
  return t.Minimatch = W, jn;
}
var Ao, yh;
function xv() {
  if (yh) return Ao;
  yh = 1, Ao = d;
  const t = Lt, { EventEmitter: e } = Ke, { Minimatch: r } = Tv(), { resolve: o } = Ue;
  function a(y, R) {
    return new Promise((w, I) => {
      t.readdir(y, { withFileTypes: !0 }, (A, u) => {
        if (A)
          switch (A.code) {
            case "ENOTDIR":
              R ? I(A) : w([]);
              break;
            case "ENOTSUP":
            // Operation not supported
            case "ENOENT":
            // No such file or directory
            case "ENAMETOOLONG":
            // Filename too long
            case "UNKNOWN":
              w([]);
              break;
            // Too many levels of symbolic links
            default:
              I(A);
              break;
          }
        else
          w(u);
      });
    });
  }
  function n(y, R) {
    return new Promise((w, I) => {
      (R ? t.stat : t.lstat)(y, (u, g) => {
        u ? u.code === "ENOENT" ? w(R ? n(y, !1) : null) : w(null) : w(g);
      });
    });
  }
  async function* i(y, R, w, I, A, u) {
    let g = await a(R + y, u);
    for (const p of g) {
      let b = p.name;
      b === void 0 && (b = p, I = !0);
      const C = y + "/" + b, N = C.slice(1), W = R + "/" + N;
      let k = null;
      (I || w) && (k = await n(W, w)), !k && p.name !== void 0 && (k = p), k === null && (k = { isDirectory: () => !1 }), k.isDirectory() ? A(N) || (yield { relative: N, absolute: W, stats: k }, yield* i(C, R, w, I, A, !1)) : yield { relative: N, absolute: W, stats: k };
    }
  }
  async function* l(y, R, w, I) {
    yield* i("", y, R, w, I, !0);
  }
  function s(y) {
    return {
      pattern: y.pattern,
      dot: !!y.dot,
      noglobstar: !!y.noglobstar,
      matchBase: !!y.matchBase,
      nocase: !!y.nocase,
      ignore: y.ignore,
      skip: y.skip,
      follow: !!y.follow,
      stat: !!y.stat,
      nodir: !!y.nodir,
      mark: !!y.mark,
      silent: !!y.silent,
      absolute: !!y.absolute
    };
  }
  class f extends e {
    constructor(R, w, I) {
      if (super(), typeof w == "function" && (I = w, w = null), this.options = s(w || {}), this.matchers = [], this.options.pattern) {
        const A = Array.isArray(this.options.pattern) ? this.options.pattern : [this.options.pattern];
        this.matchers = A.map(
          (u) => new r(u, {
            dot: this.options.dot,
            noglobstar: this.options.noglobstar,
            matchBase: this.options.matchBase,
            nocase: this.options.nocase
          })
        );
      }
      if (this.ignoreMatchers = [], this.options.ignore) {
        const A = Array.isArray(this.options.ignore) ? this.options.ignore : [this.options.ignore];
        this.ignoreMatchers = A.map(
          (u) => new r(u, { dot: !0 })
        );
      }
      if (this.skipMatchers = [], this.options.skip) {
        const A = Array.isArray(this.options.skip) ? this.options.skip : [this.options.skip];
        this.skipMatchers = A.map(
          (u) => new r(u, { dot: !0 })
        );
      }
      this.iterator = l(o(R || "."), this.options.follow, this.options.stat, this._shouldSkipDirectory.bind(this)), this.paused = !1, this.inactive = !1, this.aborted = !1, I && (this._matches = [], this.on("match", (A) => this._matches.push(this.options.absolute ? A.absolute : A.relative)), this.on("error", (A) => I(A)), this.on("end", () => I(null, this._matches))), setTimeout(() => this._next(), 0);
    }
    _shouldSkipDirectory(R) {
      return this.skipMatchers.some((w) => w.match(R));
    }
    _fileMatches(R, w) {
      const I = R + (w ? "/" : "");
      return (this.matchers.length === 0 || this.matchers.some((A) => A.match(I))) && !this.ignoreMatchers.some((A) => A.match(I)) && (!this.options.nodir || !w);
    }
    _next() {
      !this.paused && !this.aborted ? this.iterator.next().then((R) => {
        if (R.done)
          this.emit("end");
        else {
          const w = R.value.stats.isDirectory();
          if (this._fileMatches(R.value.relative, w)) {
            let I = R.value.relative, A = R.value.absolute;
            this.options.mark && w && (I += "/", A += "/"), this.options.stat ? this.emit("match", { relative: I, absolute: A, stat: R.value.stats }) : this.emit("match", { relative: I, absolute: A });
          }
          this._next(this.iterator);
        }
      }).catch((R) => {
        this.abort(), this.emit("error", R), !R.code && !this.options.silent && console.error(R);
      }) : this.inactive = !0;
    }
    abort() {
      this.aborted = !0;
    }
    pause() {
      this.paused = !0;
    }
    resume() {
      this.paused = !1, this.inactive && (this.inactive = !1, this._next());
    }
  }
  function d(y, R, w) {
    return new f(y, R, w);
  }
  return d.ReaddirGlob = f, Ao;
}
function Rw(t, ...e) {
  return (...r) => t(...e, ...r);
}
function bn(t) {
  return function(...e) {
    var r = e.pop();
    return t.call(this, e, r);
  };
}
var Iv = typeof queueMicrotask == "function" && queueMicrotask, Aw = typeof setImmediate == "function" && setImmediate, Tw = typeof process == "object" && typeof process.nextTick == "function";
function xw(t) {
  setTimeout(t, 0);
}
function Iw(t) {
  return (e, ...r) => t(() => e(...r));
}
var an;
Iv ? an = queueMicrotask : Aw ? an = setImmediate : Tw ? an = process.nextTick : an = xw;
var fr = Iw(an);
function dn(t) {
  return _n(t) ? function(...e) {
    const r = e.pop(), o = t.apply(this, e);
    return bh(o, r);
  } : bn(function(e, r) {
    var o;
    try {
      o = t.apply(this, e);
    } catch (a) {
      return r(a);
    }
    if (o && typeof o.then == "function")
      return bh(o, r);
    r(null, o);
  });
}
function bh(t, e) {
  return t.then((r) => {
    _h(e, null, r);
  }, (r) => {
    _h(e, r && (r instanceof Error || r.message) ? r : new Error(r));
  });
}
function _h(t, e, r) {
  try {
    t(e, r);
  } catch (o) {
    fr((a) => {
      throw a;
    }, o);
  }
}
function _n(t) {
  return t[Symbol.toStringTag] === "AsyncFunction";
}
function Dv(t) {
  return t[Symbol.toStringTag] === "AsyncGenerator";
}
function Ov(t) {
  return typeof t[Symbol.asyncIterator] == "function";
}
function Ne(t) {
  if (typeof t != "function") throw new Error("expected a function");
  return _n(t) ? dn(t) : t;
}
function Oe(t, e) {
  if (e || (e = t.length), !e) throw new Error("arity is undefined");
  function r(...o) {
    return typeof o[e - 1] == "function" ? t.apply(this, o) : new Promise((a, n) => {
      o[e - 1] = (i, ...l) => {
        if (i) return n(i);
        a(l.length > 1 ? l : l[0]);
      }, t.apply(this, o);
    });
  }
  return r;
}
function Dw(t) {
  return function(r, ...o) {
    return Oe(function(n) {
      var i = this;
      return t(r, (l, s) => {
        Ne(l).apply(i, o.concat(s));
      }, n);
    });
  };
}
function _c(t, e, r, o) {
  e = e || [];
  var a = [], n = 0, i = Ne(r);
  return t(e, (l, s, f) => {
    var d = n++;
    i(l, (y, R) => {
      a[d] = R, f(y);
    });
  }, (l) => {
    o(l, a);
  });
}
function Fi(t) {
  return t && typeof t.length == "number" && t.length >= 0 && t.length % 1 === 0;
}
const ji = {};
function dr(t) {
  function e(...r) {
    if (t !== null) {
      var o = t;
      t = null, o.apply(this, r);
    }
  }
  return Object.assign(e, t), e;
}
function Nv(t) {
  return t[Symbol.iterator] && t[Symbol.iterator]();
}
function Lv(t) {
  var e = -1, r = t.length;
  return function() {
    return ++e < r ? { value: t[e], key: e } : null;
  };
}
function Pv(t) {
  var e = -1;
  return function() {
    var o = t.next();
    return o.done ? null : (e++, { value: o.value, key: e });
  };
}
function Cv(t) {
  var e = t ? Object.keys(t) : [], r = -1, o = e.length;
  return function a() {
    var n = e[++r];
    return n === "__proto__" ? a() : r < o ? { value: t[n], key: n } : null;
  };
}
function Mv(t) {
  if (Fi(t))
    return Lv(t);
  var e = Nv(t);
  return e ? Pv(e) : Cv(t);
}
function hr(t) {
  return function(...e) {
    if (t === null) throw new Error("Callback was already called.");
    var r = t;
    t = null, r.apply(this, e);
  };
}
function wh(t, e, r, o) {
  let a = !1, n = !1, i = !1, l = 0, s = 0;
  function f() {
    l >= e || i || a || (i = !0, t.next().then(({ value: R, done: w }) => {
      if (!(n || a)) {
        if (i = !1, w) {
          a = !0, l <= 0 && o(null);
          return;
        }
        l++, r(R, s, d), s++, f();
      }
    }).catch(y));
  }
  function d(R, w) {
    if (l -= 1, !n) {
      if (R) return y(R);
      if (R === !1) {
        a = !0, n = !0;
        return;
      }
      if (w === ji || a && l <= 0)
        return a = !0, o(null);
      f();
    }
  }
  function y(R) {
    n || (i = !1, a = !0, o(R));
  }
  f();
}
var Pt = (t) => (e, r, o) => {
  if (o = dr(o), t <= 0)
    throw new RangeError("concurrency limit cannot be less than 1");
  if (!e)
    return o(null);
  if (Dv(e))
    return wh(e, t, r, o);
  if (Ov(e))
    return wh(e[Symbol.asyncIterator](), t, r, o);
  var a = Mv(e), n = !1, i = !1, l = 0, s = !1;
  function f(y, R) {
    if (!i)
      if (l -= 1, y)
        n = !0, o(y);
      else if (y === !1)
        n = !0, i = !0;
      else {
        if (R === ji || n && l <= 0)
          return n = !0, o(null);
        s || d();
      }
  }
  function d() {
    for (s = !0; l < t && !n; ) {
      var y = a();
      if (y === null) {
        n = !0, l <= 0 && o(null);
        return;
      }
      l += 1, r(y.value, y.key, hr(f));
    }
    s = !1;
  }
  d();
};
function kv(t, e, r, o) {
  return Pt(e)(t, Ne(r), o);
}
var Lr = Oe(kv, 4);
function Fv(t, e, r) {
  r = dr(r);
  var o = 0, a = 0, { length: n } = t, i = !1;
  n === 0 && r(null);
  function l(s, f) {
    s === !1 && (i = !0), i !== !0 && (s ? r(s) : (++a === n || f === ji) && r(null));
  }
  for (; o < n; o++)
    e(t[o], o, hr(l));
}
function jv(t, e, r) {
  return Lr(t, 1 / 0, e, r);
}
function $v(t, e, r) {
  var o = Fi(t) ? Fv : jv;
  return o(t, Ne(e), r);
}
var xt = Oe($v, 3);
function Wv(t, e, r) {
  return _c(xt, t, e, r);
}
var $i = Oe(Wv, 3), Ow = Dw($i);
function qv(t, e, r) {
  return Lr(t, 1, e, r);
}
var Nt = Oe(qv, 3);
function Bv(t, e, r) {
  return _c(Nt, t, e, r);
}
var wc = Oe(Bv, 3), Nw = Dw(wc);
const jr = /* @__PURE__ */ Symbol("promiseCallback");
function Pr() {
  let t, e;
  function r(o, ...a) {
    if (o) return e(o);
    t(a.length > 1 ? a : a[0]);
  }
  return r[jr] = new Promise((o, a) => {
    t = o, e = a;
  }), r;
}
function mc(t, e, r) {
  typeof e != "number" && (r = e, e = null), r = dr(r || Pr());
  var o = Object.keys(t).length;
  if (!o)
    return r(null);
  e || (e = o);
  var a = {}, n = 0, i = !1, l = !1, s = /* @__PURE__ */ Object.create(null), f = [], d = [], y = {};
  Object.keys(t).forEach((b) => {
    var C = t[b];
    if (!Array.isArray(C)) {
      R(b, [C]), d.push(b);
      return;
    }
    var N = C.slice(0, C.length - 1), W = N.length;
    if (W === 0) {
      R(b, C), d.push(b);
      return;
    }
    y[b] = W, N.forEach((k) => {
      if (!t[k])
        throw new Error("async.auto task `" + b + "` has a non-existent dependency `" + k + "` in " + N.join(", "));
      I(k, () => {
        W--, W === 0 && R(b, C);
      });
    });
  }), g(), w();
  function R(b, C) {
    f.push(() => u(b, C));
  }
  function w() {
    if (!i) {
      if (f.length === 0 && n === 0)
        return r(null, a);
      for (; f.length && n < e; ) {
        var b = f.shift();
        b();
      }
    }
  }
  function I(b, C) {
    var N = s[b];
    N || (N = s[b] = []), N.push(C);
  }
  function A(b) {
    var C = s[b] || [];
    C.forEach((N) => N()), w();
  }
  function u(b, C) {
    if (!l) {
      var N = hr((k, ...L) => {
        if (n--, k === !1) {
          i = !0;
          return;
        }
        if (L.length < 2 && ([L] = L), k) {
          var U = {};
          if (Object.keys(a).forEach((x) => {
            U[x] = a[x];
          }), U[b] = L, l = !0, s = /* @__PURE__ */ Object.create(null), i) return;
          r(k, U);
        } else
          a[b] = L, A(b);
      });
      n++;
      var W = Ne(C[C.length - 1]);
      C.length > 1 ? W(a, N) : W(N);
    }
  }
  function g() {
    for (var b, C = 0; d.length; )
      b = d.pop(), C++, p(b).forEach((N) => {
        --y[N] === 0 && d.push(N);
      });
    if (C !== o)
      throw new Error(
        "async.auto cannot execute tasks due to a recursive dependency"
      );
  }
  function p(b) {
    var C = [];
    return Object.keys(t).forEach((N) => {
      const W = t[N];
      Array.isArray(W) && W.indexOf(b) >= 0 && C.push(N);
    }), C;
  }
  return r[jr];
}
var Uv = /^(?:async\s)?(?:function)?\s*(?:\w+\s*)?\(([^)]+)\)(?:\s*{)/, Gv = /^(?:async\s)?\s*(?:\(\s*)?((?:[^)=\s]\s*)*)(?:\)\s*)?=>/, Hv = /,/, zv = /(=.+)?(\s*)$/;
function Vv(t) {
  let e = "", r = 0, o = t.indexOf("*/");
  for (; r < t.length; )
    if (t[r] === "/" && t[r + 1] === "/") {
      let a = t.indexOf(`
`, r);
      r = a === -1 ? t.length : a;
    } else if (o !== -1 && t[r] === "/" && t[r + 1] === "*") {
      let a = t.indexOf("*/", r);
      a !== -1 ? (r = a + 2, o = t.indexOf("*/", r)) : (e += t[r], r++);
    } else
      e += t[r], r++;
  return e;
}
function Zv(t) {
  const e = Vv(t.toString());
  let r = e.match(Uv);
  if (r || (r = e.match(Gv)), !r) throw new Error(`could not parse args in autoInject
Source:
` + e);
  let [, o] = r;
  return o.replace(/\s/g, "").split(Hv).map((a) => a.replace(zv, "").trim());
}
function Lw(t, e) {
  var r = {};
  return Object.keys(t).forEach((o) => {
    var a = t[o], n, i = _n(a), l = !i && a.length === 1 || i && a.length === 0;
    if (Array.isArray(a))
      n = [...a], a = n.pop(), r[o] = n.concat(n.length > 0 ? s : a);
    else if (l)
      r[o] = a;
    else {
      if (n = Zv(a), a.length === 0 && !i && n.length === 0)
        throw new Error("autoInject task functions require explicit parameters.");
      i || n.pop(), r[o] = n.concat(s);
    }
    function s(f, d) {
      var y = n.map((R) => f[R]);
      y.push(d), Ne(a)(...y);
    }
  }), mc(r, e);
}
class Yv {
  constructor() {
    this.head = this.tail = null, this.length = 0;
  }
  removeLink(e) {
    return e.prev ? e.prev.next = e.next : this.head = e.next, e.next ? e.next.prev = e.prev : this.tail = e.prev, e.prev = e.next = null, this.length -= 1, e;
  }
  empty() {
    for (; this.head; ) this.shift();
    return this;
  }
  insertAfter(e, r) {
    r.prev = e, r.next = e.next, e.next ? e.next.prev = r : this.tail = r, e.next = r, this.length += 1;
  }
  insertBefore(e, r) {
    r.prev = e.prev, r.next = e, e.prev ? e.prev.next = r : this.head = r, e.prev = r, this.length += 1;
  }
  unshift(e) {
    this.head ? this.insertBefore(this.head, e) : mh(this, e);
  }
  push(e) {
    this.tail ? this.insertAfter(this.tail, e) : mh(this, e);
  }
  shift() {
    return this.head && this.removeLink(this.head);
  }
  pop() {
    return this.tail && this.removeLink(this.tail);
  }
  toArray() {
    return [...this];
  }
  *[Symbol.iterator]() {
    for (var e = this.head; e; )
      yield e.data, e = e.next;
  }
  remove(e) {
    for (var r = this.head; r; ) {
      var { next: o } = r;
      e(r) && this.removeLink(r), r = o;
    }
    return this;
  }
}
function mh(t, e) {
  t.length = 1, t.head = t.tail = e;
}
function Sc(t, e, r) {
  if (e == null)
    e = 1;
  else if (e === 0)
    throw new RangeError("Concurrency must not be zero");
  var o = Ne(t), a = 0, n = [];
  const i = {
    error: [],
    drain: [],
    saturated: [],
    unsaturated: [],
    empty: []
  };
  function l(p, b) {
    i[p].push(b);
  }
  function s(p, b) {
    const C = (...N) => {
      f(p, C), b(...N);
    };
    i[p].push(C);
  }
  function f(p, b) {
    if (!p) return Object.keys(i).forEach((C) => i[C] = []);
    if (!b) return i[p] = [];
    i[p] = i[p].filter((C) => C !== b);
  }
  function d(p, ...b) {
    i[p].forEach((C) => C(...b));
  }
  var y = !1;
  function R(p, b, C, N) {
    if (N != null && typeof N != "function")
      throw new Error("task callback must be a function");
    g.started = !0;
    var W, k;
    function L(x, ...D) {
      if (x) return C ? k(x) : W();
      if (D.length <= 1) return W(D[0]);
      W(D);
    }
    var U = g._createTaskItem(
      p,
      C ? L : N || L
    );
    if (b ? g._tasks.unshift(U) : g._tasks.push(U), y || (y = !0, fr(() => {
      y = !1, g.process();
    })), C || !N)
      return new Promise((x, D) => {
        W = x, k = D;
      });
  }
  function w(p) {
    return function(b, ...C) {
      a -= 1;
      for (var N = 0, W = p.length; N < W; N++) {
        var k = p[N], L = n.indexOf(k);
        L === 0 ? n.shift() : L > 0 && n.splice(L, 1), k.callback(b, ...C), b != null && d("error", b, k.data);
      }
      a <= g.concurrency - g.buffer && d("unsaturated"), g.idle() && d("drain"), g.process();
    };
  }
  function I(p) {
    return p.length === 0 && g.idle() ? (fr(() => d("drain")), !0) : !1;
  }
  const A = (p) => (b) => {
    if (!b)
      return new Promise((C, N) => {
        s(p, (W, k) => {
          if (W) return N(W);
          C(k);
        });
      });
    f(p), l(p, b);
  };
  var u = !1, g = {
    _tasks: new Yv(),
    _createTaskItem(p, b) {
      return {
        data: p,
        callback: b
      };
    },
    *[Symbol.iterator]() {
      yield* g._tasks[Symbol.iterator]();
    },
    concurrency: e,
    payload: r,
    buffer: e / 4,
    started: !1,
    paused: !1,
    push(p, b) {
      return Array.isArray(p) ? I(p) ? void 0 : p.map((C) => R(C, !1, !1, b)) : R(p, !1, !1, b);
    },
    pushAsync(p, b) {
      return Array.isArray(p) ? I(p) ? void 0 : p.map((C) => R(C, !1, !0, b)) : R(p, !1, !0, b);
    },
    kill() {
      f(), g._tasks.empty();
    },
    unshift(p, b) {
      return Array.isArray(p) ? I(p) ? void 0 : p.map((C) => R(C, !0, !1, b)) : R(p, !0, !1, b);
    },
    unshiftAsync(p, b) {
      return Array.isArray(p) ? I(p) ? void 0 : p.map((C) => R(C, !0, !0, b)) : R(p, !0, !0, b);
    },
    remove(p) {
      g._tasks.remove(p);
    },
    process() {
      if (!u) {
        for (u = !0; !g.paused && a < g.concurrency && g._tasks.length; ) {
          var p = [], b = [], C = g._tasks.length;
          g.payload && (C = Math.min(C, g.payload));
          for (var N = 0; N < C; N++) {
            var W = g._tasks.shift();
            p.push(W), n.push(W), b.push(W.data);
          }
          a += 1, g._tasks.length === 0 && d("empty"), a === g.concurrency && d("saturated");
          var k = hr(w(p));
          o(b, k);
        }
        u = !1;
      }
    },
    length() {
      return g._tasks.length;
    },
    running() {
      return a;
    },
    workersList() {
      return n;
    },
    idle() {
      return g._tasks.length + a === 0;
    },
    pause() {
      g.paused = !0;
    },
    resume() {
      g.paused !== !1 && (g.paused = !1, fr(g.process));
    }
  };
  return Object.defineProperties(g, {
    saturated: {
      writable: !1,
      value: A("saturated")
    },
    unsaturated: {
      writable: !1,
      value: A("unsaturated")
    },
    empty: {
      writable: !1,
      value: A("empty")
    },
    drain: {
      writable: !1,
      value: A("drain")
    },
    error: {
      writable: !1,
      value: A("error")
    }
  }), g;
}
function Pw(t, e) {
  return Sc(t, 1, e);
}
function Cw(t, e, r) {
  return Sc(t, e, r);
}
function Kv(t, e, r, o) {
  o = dr(o);
  var a = Ne(r);
  return Nt(t, (n, i, l) => {
    a(e, n, (s, f) => {
      e = f, l(s);
    });
  }, (n) => o(n, e));
}
var cr = Oe(Kv, 4);
function Ec(...t) {
  var e = t.map(Ne);
  return function(...r) {
    var o = this, a = r[r.length - 1];
    return typeof a == "function" ? r.pop() : a = Pr(), cr(
      e,
      r,
      (n, i, l) => {
        i.apply(o, n.concat((s, ...f) => {
          l(s, f);
        }));
      },
      (n, i) => a(n, ...i)
    ), a[jr];
  };
}
function Mw(...t) {
  return Ec(...t.reverse());
}
function Jv(t, e, r, o) {
  return _c(Pt(e), t, r, o);
}
var wn = Oe(Jv, 4);
function Xv(t, e, r, o) {
  var a = Ne(r);
  return wn(t, e, (n, i) => {
    a(n, (l, ...s) => l ? i(l) : i(l, s));
  }, (n, i) => {
    for (var l = [], s = 0; s < i.length; s++)
      i[s] && (l = l.concat(...i[s]));
    return o(n, l);
  });
}
var Cr = Oe(Xv, 4);
function Qv(t, e, r) {
  return Cr(t, 1 / 0, e, r);
}
var ui = Oe(Qv, 3);
function e0(t, e, r) {
  return Cr(t, 1, e, r);
}
var fi = Oe(e0, 3);
function kw(...t) {
  return function(...e) {
    var r = e.pop();
    return r(null, ...t);
  };
}
function rr(t, e) {
  return (r, o, a, n) => {
    var i = !1, l;
    const s = Ne(a);
    r(o, (f, d, y) => {
      s(f, (R, w) => {
        if (R || R === !1) return y(R);
        if (t(w) && !l)
          return i = !0, l = e(!0, f), y(null, ji);
        y();
      });
    }, (f) => {
      if (f) return n(f);
      n(null, i ? l : e(!1));
    });
  };
}
function t0(t, e, r) {
  return rr((o) => o, (o, a) => a)(xt, t, e, r);
}
var ci = Oe(t0, 3);
function r0(t, e, r, o) {
  return rr((a) => a, (a, n) => n)(Pt(e), t, r, o);
}
var di = Oe(r0, 4);
function n0(t, e, r) {
  return rr((o) => o, (o, a) => a)(Pt(1), t, e, r);
}
var hi = Oe(n0, 3);
function Fw(t) {
  return (e, ...r) => Ne(e)(...r, (o, ...a) => {
    typeof console == "object" && (o ? console.error && console.error(o) : console[t] && a.forEach((n) => console[t](n)));
  });
}
var jw = Fw("dir");
function i0(t, e, r) {
  r = hr(r);
  var o = Ne(t), a = Ne(e), n;
  function i(s, ...f) {
    if (s) return r(s);
    s !== !1 && (n = f, a(...f, l));
  }
  function l(s, f) {
    if (s) return r(s);
    if (s !== !1) {
      if (!f) return r(null, ...n);
      o(i);
    }
  }
  return l(null, !0);
}
var hn = Oe(i0, 3);
function $w(t, e, r) {
  const o = Ne(e);
  return hn(t, (...a) => {
    const n = a.pop();
    o(...a, (i, l) => n(i, !l));
  }, r);
}
function Ww(t) {
  return (e, r, o) => t(e, o);
}
function s0(t, e, r) {
  return xt(t, Ww(Ne(e)), r);
}
var pi = Oe(s0, 3);
function o0(t, e, r, o) {
  return Pt(e)(t, Ww(Ne(r)), o);
}
var pn = Oe(o0, 4);
function a0(t, e, r) {
  return pn(t, 1, e, r);
}
var gn = Oe(a0, 3);
function vc(t) {
  return _n(t) ? t : function(...e) {
    var r = e.pop(), o = !0;
    e.push((...a) => {
      o ? fr(() => r(...a)) : r(...a);
    }), t.apply(this, e), o = !1;
  };
}
function l0(t, e, r) {
  return rr((o) => !o, (o) => !o)(xt, t, e, r);
}
var gi = Oe(l0, 3);
function u0(t, e, r, o) {
  return rr((a) => !a, (a) => !a)(Pt(e), t, r, o);
}
var yi = Oe(u0, 4);
function f0(t, e, r) {
  return rr((o) => !o, (o) => !o)(Nt, t, e, r);
}
var bi = Oe(f0, 3);
function c0(t, e, r, o) {
  var a = new Array(e.length);
  t(e, (n, i, l) => {
    r(n, (s, f) => {
      a[i] = !!f, l(s);
    });
  }, (n) => {
    if (n) return o(n);
    for (var i = [], l = 0; l < e.length; l++)
      a[l] && i.push(e[l]);
    o(null, i);
  });
}
function d0(t, e, r, o) {
  var a = [];
  t(e, (n, i, l) => {
    r(n, (s, f) => {
      if (s) return l(s);
      f && a.push({ index: i, value: n }), l(s);
    });
  }, (n) => {
    if (n) return o(n);
    o(null, a.sort((i, l) => i.index - l.index).map((i) => i.value));
  });
}
function Wi(t, e, r, o) {
  var a = Fi(e) ? c0 : d0;
  return a(t, e, Ne(r), o);
}
function h0(t, e, r) {
  return Wi(xt, t, e, r);
}
var _i = Oe(h0, 3);
function p0(t, e, r, o) {
  return Wi(Pt(e), t, r, o);
}
var wi = Oe(p0, 4);
function g0(t, e, r) {
  return Wi(Nt, t, e, r);
}
var mi = Oe(g0, 3);
function y0(t, e) {
  var r = hr(e), o = Ne(vc(t));
  function a(n) {
    if (n) return r(n);
    n !== !1 && o(a);
  }
  return a();
}
var qw = Oe(y0, 2);
function b0(t, e, r, o) {
  var a = Ne(r);
  return wn(t, e, (n, i) => {
    a(n, (l, s) => l ? i(l) : i(l, { key: s, val: n }));
  }, (n, i) => {
    for (var l = {}, { hasOwnProperty: s } = Object.prototype, f = 0; f < i.length; f++)
      if (i[f]) {
        var { key: d } = i[f], { val: y } = i[f];
        s.call(l, d) ? l[d].push(y) : l[d] = [y];
      }
    return o(n, l);
  });
}
var qi = Oe(b0, 4);
function Bw(t, e, r) {
  return qi(t, 1 / 0, e, r);
}
function Uw(t, e, r) {
  return qi(t, 1, e, r);
}
var Gw = Fw("log");
function _0(t, e, r, o) {
  o = dr(o);
  var a = {}, n = Ne(r);
  return Pt(e)(t, (i, l, s) => {
    n(i, l, (f, d) => {
      if (f) return s(f);
      a[l] = d, s(f);
    });
  }, (i) => o(i, a));
}
var Bi = Oe(_0, 4);
function Hw(t, e, r) {
  return Bi(t, 1 / 0, e, r);
}
function zw(t, e, r) {
  return Bi(t, 1, e, r);
}
function Vw(t, e = (r) => r) {
  var r = /* @__PURE__ */ Object.create(null), o = /* @__PURE__ */ Object.create(null), a = Ne(t), n = bn((i, l) => {
    var s = e(...i);
    s in r ? fr(() => l(null, ...r[s])) : s in o ? o[s].push(l) : (o[s] = [l], a(...i, (f, ...d) => {
      f || (r[s] = d);
      var y = o[s];
      delete o[s];
      for (var R = 0, w = y.length; R < w; R++)
        y[R](f, ...d);
    }));
  });
  return n.memo = r, n.unmemoized = t, n;
}
var ri;
Tw ? ri = process.nextTick : Aw ? ri = setImmediate : ri = xw;
var Zw = Iw(ri), Rc = Oe((t, e, r) => {
  var o = Fi(e) ? [] : {};
  t(e, (a, n, i) => {
    Ne(a)((l, ...s) => {
      s.length < 2 && ([s] = s), o[n] = s, i(l);
    });
  }, (a) => r(a, o));
}, 3);
function Yw(t, e) {
  return Rc(xt, t, e);
}
function Kw(t, e, r) {
  return Rc(Pt(e), t, r);
}
function Ac(t, e) {
  var r = Ne(t);
  return Sc((o, a) => {
    r(o[0], a);
  }, e, 1);
}
class w0 {
  constructor() {
    this.heap = [], this.pushCount = Number.MIN_SAFE_INTEGER;
  }
  get length() {
    return this.heap.length;
  }
  empty() {
    return this.heap = [], this;
  }
  percUp(e) {
    let r;
    for (; e > 0 && To(this.heap[e], this.heap[r = Sh(e)]); ) {
      let o = this.heap[e];
      this.heap[e] = this.heap[r], this.heap[r] = o, e = r;
    }
  }
  percDown(e) {
    let r;
    for (; (r = m0(e)) < this.heap.length && (r + 1 < this.heap.length && To(this.heap[r + 1], this.heap[r]) && (r = r + 1), !To(this.heap[e], this.heap[r])); ) {
      let o = this.heap[e];
      this.heap[e] = this.heap[r], this.heap[r] = o, e = r;
    }
  }
  push(e) {
    e.pushCount = ++this.pushCount, this.heap.push(e), this.percUp(this.heap.length - 1);
  }
  unshift(e) {
    return this.heap.push(e);
  }
  shift() {
    let [e] = this.heap;
    return this.heap[0] = this.heap[this.heap.length - 1], this.heap.pop(), this.percDown(0), e;
  }
  toArray() {
    return [...this];
  }
  *[Symbol.iterator]() {
    for (let e = 0; e < this.heap.length; e++)
      yield this.heap[e].data;
  }
  remove(e) {
    let r = 0;
    for (let o = 0; o < this.heap.length; o++)
      e(this.heap[o]) || (this.heap[r] = this.heap[o], r++);
    this.heap.splice(r);
    for (let o = Sh(this.heap.length - 1); o >= 0; o--)
      this.percDown(o);
    return this;
  }
}
function m0(t) {
  return (t << 1) + 1;
}
function Sh(t) {
  return (t + 1 >> 1) - 1;
}
function To(t, e) {
  return t.priority !== e.priority ? t.priority < e.priority : t.pushCount < e.pushCount;
}
function Jw(t, e) {
  var r = Ac(t, e), {
    push: o,
    pushAsync: a
  } = r;
  r._tasks = new w0(), r._createTaskItem = ({ data: i, priority: l }, s) => ({
    data: i,
    priority: l,
    callback: s
  });
  function n(i, l) {
    return Array.isArray(i) ? i.map((s) => ({ data: s, priority: l })) : { data: i, priority: l };
  }
  return r.push = function(i, l = 0, s) {
    return o(n(i, l), s);
  }, r.pushAsync = function(i, l = 0, s) {
    return a(n(i, l), s);
  }, delete r.unshift, delete r.unshiftAsync, r;
}
function S0(t, e) {
  if (e = dr(e), !Array.isArray(t)) return e(new TypeError("First argument to race must be an array of functions"));
  if (!t.length) return e();
  for (var r = 0, o = t.length; r < o; r++)
    Ne(t[r])(e);
}
var Xw = Oe(S0, 2);
function Si(t, e, r, o) {
  var a = [...t].reverse();
  return cr(a, e, r, o);
}
function Ei(t) {
  var e = Ne(t);
  return bn(function(o, a) {
    return o.push((n, ...i) => {
      let l = {};
      if (n && (l.error = n), i.length > 0) {
        var s = i;
        i.length <= 1 && ([s] = i), l.value = s;
      }
      a(null, l);
    }), e.apply(this, o);
  });
}
function Qw(t) {
  var e;
  return Array.isArray(t) ? e = t.map(Ei) : (e = {}, Object.keys(t).forEach((r) => {
    e[r] = Ei.call(this, t[r]);
  })), e;
}
function Tc(t, e, r, o) {
  const a = Ne(r);
  return Wi(t, e, (n, i) => {
    a(n, (l, s) => {
      i(l, !s);
    });
  }, o);
}
function E0(t, e, r) {
  return Tc(xt, t, e, r);
}
var em = Oe(E0, 3);
function v0(t, e, r, o) {
  return Tc(Pt(e), t, r, o);
}
var tm = Oe(v0, 4);
function R0(t, e, r) {
  return Tc(Nt, t, e, r);
}
var rm = Oe(R0, 3);
function nm(t) {
  return function() {
    return t;
  };
}
const lc = 5, im = 0;
function vi(t, e, r) {
  var o = {
    times: lc,
    intervalFunc: nm(im)
  };
  if (arguments.length < 3 && typeof t == "function" ? (r = e || Pr(), e = t) : (A0(o, t), r = r || Pr()), typeof e != "function")
    throw new Error("Invalid arguments for async.retry");
  var a = Ne(e), n = 1;
  function i() {
    a((l, ...s) => {
      l !== !1 && (l && n++ < o.times && (typeof o.errorFilter != "function" || o.errorFilter(l)) ? setTimeout(i, o.intervalFunc(n - 1)) : r(l, ...s));
    });
  }
  return i(), r[jr];
}
function A0(t, e) {
  if (typeof e == "object")
    t.times = +e.times || lc, t.intervalFunc = typeof e.interval == "function" ? e.interval : nm(+e.interval || im), t.errorFilter = e.errorFilter;
  else if (typeof e == "number" || typeof e == "string")
    t.times = +e || lc;
  else
    throw new Error("Invalid arguments for async.retry");
}
function sm(t, e) {
  e || (e = t, t = null);
  let r = t && t.arity || e.length;
  _n(e) && (r += 1);
  var o = Ne(e);
  return bn((a, n) => {
    (a.length < r - 1 || n == null) && (a.push(n), n = Pr());
    function i(l) {
      o(...a, l);
    }
    return t ? vi(t, i, n) : vi(i, n), n[jr];
  });
}
function om(t, e) {
  return Rc(Nt, t, e);
}
function T0(t, e, r) {
  return rr(Boolean, (o) => o)(xt, t, e, r);
}
var Ri = Oe(T0, 3);
function x0(t, e, r, o) {
  return rr(Boolean, (a) => a)(Pt(e), t, r, o);
}
var Ai = Oe(x0, 4);
function I0(t, e, r) {
  return rr(Boolean, (o) => o)(Nt, t, e, r);
}
var Ti = Oe(I0, 3);
function D0(t, e, r) {
  var o = Ne(e);
  return $i(t, (n, i) => {
    o(n, (l, s) => {
      if (l) return i(l);
      i(l, { value: n, criteria: s });
    });
  }, (n, i) => {
    if (n) return r(n);
    r(null, i.sort(a).map((l) => l.value));
  });
  function a(n, i) {
    var l = n.criteria, s = i.criteria;
    return l < s ? -1 : l > s ? 1 : 0;
  }
}
var am = Oe(D0, 3);
function lm(t, e, r) {
  var o = Ne(t);
  return bn((a, n) => {
    var i = !1, l;
    function s() {
      var f = t.name || "anonymous", d = new Error('Callback function "' + f + '" timed out.');
      d.code = "ETIMEDOUT", r && (d.info = r), i = !0, n(d);
    }
    a.push((...f) => {
      i || (n(...f), clearTimeout(l));
    }), l = setTimeout(s, e), o(...a);
  });
}
function O0(t) {
  for (var e = Array(t); t--; )
    e[t] = t;
  return e;
}
function Ui(t, e, r, o) {
  var a = Ne(r);
  return wn(O0(t), e, a, o);
}
function um(t, e, r) {
  return Ui(t, 1 / 0, e, r);
}
function fm(t, e, r) {
  return Ui(t, 1, e, r);
}
function cm(t, e, r, o) {
  arguments.length <= 3 && typeof e == "function" && (o = r, r = e, e = Array.isArray(t) ? [] : {}), o = dr(o || Pr());
  var a = Ne(r);
  return xt(t, (n, i, l) => {
    a(e, n, i, l);
  }, (n) => o(n, e)), o[jr];
}
function N0(t, e) {
  var r = null, o;
  return gn(t, (a, n) => {
    Ne(a)((i, ...l) => {
      if (i === !1) return n(i);
      l.length < 2 ? [o] = l : o = l, r = i, n(i ? null : {});
    });
  }, () => e(r, o));
}
var dm = Oe(N0);
function hm(t) {
  return (...e) => (t.unmemoized || t)(...e);
}
function L0(t, e, r) {
  r = hr(r);
  var o = Ne(e), a = Ne(t), n = [];
  function i(s, ...f) {
    if (s) return r(s);
    n = f, s !== !1 && a(l);
  }
  function l(s, f) {
    if (s) return r(s);
    if (s !== !1) {
      if (!f) return r(null, ...n);
      o(i);
    }
  }
  return a(l);
}
var yn = Oe(L0, 3);
function pm(t, e, r) {
  const o = Ne(t);
  return yn((a) => o((n, i) => a(n, !i)), e, r);
}
function P0(t, e) {
  if (e = dr(e), !Array.isArray(t)) return e(new Error("First argument to waterfall must be an array of functions"));
  if (!t.length) return e();
  var r = 0;
  function o(n) {
    var i = Ne(t[r++]);
    i(...n, hr(a));
  }
  function a(n, ...i) {
    if (n !== !1) {
      if (n || r === t.length)
        return e(n, ...i);
      o(i);
    }
  }
  o([]);
}
var gm = Oe(P0), C0 = {
  apply: Rw,
  applyEach: Ow,
  applyEachSeries: Nw,
  asyncify: dn,
  auto: mc,
  autoInject: Lw,
  cargo: Pw,
  cargoQueue: Cw,
  compose: Mw,
  concat: ui,
  concatLimit: Cr,
  concatSeries: fi,
  constant: kw,
  detect: ci,
  detectLimit: di,
  detectSeries: hi,
  dir: jw,
  doUntil: $w,
  doWhilst: hn,
  each: pi,
  eachLimit: pn,
  eachOf: xt,
  eachOfLimit: Lr,
  eachOfSeries: Nt,
  eachSeries: gn,
  ensureAsync: vc,
  every: gi,
  everyLimit: yi,
  everySeries: bi,
  filter: _i,
  filterLimit: wi,
  filterSeries: mi,
  forever: qw,
  groupBy: Bw,
  groupByLimit: qi,
  groupBySeries: Uw,
  log: Gw,
  map: $i,
  mapLimit: wn,
  mapSeries: wc,
  mapValues: Hw,
  mapValuesLimit: Bi,
  mapValuesSeries: zw,
  memoize: Vw,
  nextTick: Zw,
  parallel: Yw,
  parallelLimit: Kw,
  priorityQueue: Jw,
  queue: Ac,
  race: Xw,
  reduce: cr,
  reduceRight: Si,
  reflect: Ei,
  reflectAll: Qw,
  reject: em,
  rejectLimit: tm,
  rejectSeries: rm,
  retry: vi,
  retryable: sm,
  seq: Ec,
  series: om,
  setImmediate: fr,
  some: Ri,
  someLimit: Ai,
  someSeries: Ti,
  sortBy: am,
  timeout: lm,
  times: um,
  timesLimit: Ui,
  timesSeries: fm,
  transform: cm,
  tryEach: dm,
  unmemoize: hm,
  until: pm,
  waterfall: gm,
  whilst: yn,
  // aliases
  all: gi,
  allLimit: yi,
  allSeries: bi,
  any: Ri,
  anyLimit: Ai,
  anySeries: Ti,
  find: ci,
  findLimit: di,
  findSeries: hi,
  flatMap: ui,
  flatMapLimit: Cr,
  flatMapSeries: fi,
  forEach: pi,
  forEachSeries: gn,
  forEachLimit: pn,
  forEachOf: xt,
  forEachOfSeries: Nt,
  forEachOfLimit: Lr,
  inject: cr,
  foldl: cr,
  foldr: Si,
  select: _i,
  selectLimit: wi,
  selectSeries: mi,
  wrapSync: dn,
  during: yn,
  doDuring: hn
};
const M0 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  all: gi,
  allLimit: yi,
  allSeries: bi,
  any: Ri,
  anyLimit: Ai,
  anySeries: Ti,
  apply: Rw,
  applyEach: Ow,
  applyEachSeries: Nw,
  asyncify: dn,
  auto: mc,
  autoInject: Lw,
  cargo: Pw,
  cargoQueue: Cw,
  compose: Mw,
  concat: ui,
  concatLimit: Cr,
  concatSeries: fi,
  constant: kw,
  default: C0,
  detect: ci,
  detectLimit: di,
  detectSeries: hi,
  dir: jw,
  doDuring: hn,
  doUntil: $w,
  doWhilst: hn,
  during: yn,
  each: pi,
  eachLimit: pn,
  eachOf: xt,
  eachOfLimit: Lr,
  eachOfSeries: Nt,
  eachSeries: gn,
  ensureAsync: vc,
  every: gi,
  everyLimit: yi,
  everySeries: bi,
  filter: _i,
  filterLimit: wi,
  filterSeries: mi,
  find: ci,
  findLimit: di,
  findSeries: hi,
  flatMap: ui,
  flatMapLimit: Cr,
  flatMapSeries: fi,
  foldl: cr,
  foldr: Si,
  forEach: pi,
  forEachLimit: pn,
  forEachOf: xt,
  forEachOfLimit: Lr,
  forEachOfSeries: Nt,
  forEachSeries: gn,
  forever: qw,
  groupBy: Bw,
  groupByLimit: qi,
  groupBySeries: Uw,
  inject: cr,
  log: Gw,
  map: $i,
  mapLimit: wn,
  mapSeries: wc,
  mapValues: Hw,
  mapValuesLimit: Bi,
  mapValuesSeries: zw,
  memoize: Vw,
  nextTick: Zw,
  parallel: Yw,
  parallelLimit: Kw,
  priorityQueue: Jw,
  queue: Ac,
  race: Xw,
  reduce: cr,
  reduceRight: Si,
  reflect: Ei,
  reflectAll: Qw,
  reject: em,
  rejectLimit: tm,
  rejectSeries: rm,
  retry: vi,
  retryable: sm,
  select: _i,
  selectLimit: wi,
  selectSeries: mi,
  seq: Ec,
  series: om,
  setImmediate: fr,
  some: Ri,
  someLimit: Ai,
  someSeries: Ti,
  sortBy: am,
  timeout: lm,
  times: um,
  timesLimit: Ui,
  timesSeries: fm,
  transform: cm,
  tryEach: dm,
  unmemoize: hm,
  until: pm,
  waterfall: gm,
  whilst: yn,
  wrapSync: dn
}, Symbol.toStringTag, { value: "Module" })), k0 = /* @__PURE__ */ fw(M0);
var xo = { exports: {} }, Io, Eh;
function F0() {
  if (Eh) return Io;
  Eh = 1;
  const t = (e) => e !== null && typeof e == "object" && typeof e.pipe == "function";
  return t.writable = (e) => t(e) && e.writable !== !1 && typeof e._write == "function" && typeof e._writableState == "object", t.readable = (e) => t(e) && e.readable !== !1 && typeof e._read == "function" && typeof e._readableState == "object", t.duplex = (e) => t.writable(e) && t.readable(e), t.transform = (e) => t.duplex(e) && typeof e._transform == "function", Io = t, Io;
}
var $n = { exports: {} }, Wn = { exports: {} }, vh;
function Gi() {
  if (vh) return Wn.exports;
  vh = 1, typeof process > "u" || !process.version || process.version.indexOf("v0.") === 0 || process.version.indexOf("v1.") === 0 && process.version.indexOf("v1.8.") !== 0 ? Wn.exports = { nextTick: t } : Wn.exports = process;
  function t(e, r, o, a) {
    if (typeof e != "function")
      throw new TypeError('"callback" argument must be a function');
    var n = arguments.length, i, l;
    switch (n) {
      case 0:
      case 1:
        return process.nextTick(e);
      case 2:
        return process.nextTick(function() {
          e.call(null, r);
        });
      case 3:
        return process.nextTick(function() {
          e.call(null, r, o);
        });
      case 4:
        return process.nextTick(function() {
          e.call(null, r, o, a);
        });
      default:
        for (i = new Array(n - 1), l = 0; l < i.length; )
          i[l++] = arguments[l];
        return process.nextTick(function() {
          e.apply(null, i);
        });
    }
  }
  return Wn.exports;
}
var Do, Rh;
function j0() {
  if (Rh) return Do;
  Rh = 1;
  var t = {}.toString;
  return Do = Array.isArray || function(e) {
    return t.call(e) == "[object Array]";
  }, Do;
}
var Oo, Ah;
function ym() {
  return Ah || (Ah = 1, Oo = tr), Oo;
}
var qn = { exports: {} }, Th;
function Hi() {
  return Th || (Th = 1, (function(t, e) {
    var r = Fe, o = r.Buffer;
    function a(i, l) {
      for (var s in i)
        l[s] = i[s];
    }
    o.from && o.alloc && o.allocUnsafe && o.allocUnsafeSlow ? t.exports = r : (a(r, e), e.Buffer = n);
    function n(i, l, s) {
      return o(i, l, s);
    }
    a(o, n), n.from = function(i, l, s) {
      if (typeof i == "number")
        throw new TypeError("Argument must not be a number");
      return o(i, l, s);
    }, n.alloc = function(i, l, s) {
      if (typeof i != "number")
        throw new TypeError("Argument must be a number");
      var f = o(i);
      return l !== void 0 ? typeof s == "string" ? f.fill(l, s) : f.fill(l) : f.fill(0), f;
    }, n.allocUnsafe = function(i) {
      if (typeof i != "number")
        throw new TypeError("Argument must be a number");
      return o(i);
    }, n.allocUnsafeSlow = function(i) {
      if (typeof i != "number")
        throw new TypeError("Argument must be a number");
      return r.SlowBuffer(i);
    };
  })(qn, qn.exports)), qn.exports;
}
var ot = {}, xh;
function mn() {
  if (xh) return ot;
  xh = 1;
  function t(A) {
    return Array.isArray ? Array.isArray(A) : I(A) === "[object Array]";
  }
  ot.isArray = t;
  function e(A) {
    return typeof A == "boolean";
  }
  ot.isBoolean = e;
  function r(A) {
    return A === null;
  }
  ot.isNull = r;
  function o(A) {
    return A == null;
  }
  ot.isNullOrUndefined = o;
  function a(A) {
    return typeof A == "number";
  }
  ot.isNumber = a;
  function n(A) {
    return typeof A == "string";
  }
  ot.isString = n;
  function i(A) {
    return typeof A == "symbol";
  }
  ot.isSymbol = i;
  function l(A) {
    return A === void 0;
  }
  ot.isUndefined = l;
  function s(A) {
    return I(A) === "[object RegExp]";
  }
  ot.isRegExp = s;
  function f(A) {
    return typeof A == "object" && A !== null;
  }
  ot.isObject = f;
  function d(A) {
    return I(A) === "[object Date]";
  }
  ot.isDate = d;
  function y(A) {
    return I(A) === "[object Error]" || A instanceof Error;
  }
  ot.isError = y;
  function R(A) {
    return typeof A == "function";
  }
  ot.isFunction = R;
  function w(A) {
    return A === null || typeof A == "boolean" || typeof A == "number" || typeof A == "string" || typeof A == "symbol" || // ES6 symbol
    typeof A > "u";
  }
  ot.isPrimitive = w, ot.isBuffer = Fe.Buffer.isBuffer;
  function I(A) {
    return Object.prototype.toString.call(A);
  }
  return ot;
}
var Bn = { exports: {} }, Un = { exports: {} }, Ih;
function $0() {
  return Ih || (Ih = 1, typeof Object.create == "function" ? Un.exports = function(e, r) {
    r && (e.super_ = r, e.prototype = Object.create(r.prototype, {
      constructor: {
        value: e,
        enumerable: !1,
        writable: !0,
        configurable: !0
      }
    }));
  } : Un.exports = function(e, r) {
    if (r) {
      e.super_ = r;
      var o = function() {
      };
      o.prototype = r.prototype, e.prototype = new o(), e.prototype.constructor = e;
    }
  }), Un.exports;
}
var Dh;
function Sn() {
  if (Dh) return Bn.exports;
  Dh = 1;
  try {
    var t = require("util");
    if (typeof t.inherits != "function") throw "";
    Bn.exports = t.inherits;
  } catch {
    Bn.exports = $0();
  }
  return Bn.exports;
}
var No = { exports: {} }, Oh;
function W0() {
  return Oh || (Oh = 1, (function(t) {
    function e(n, i) {
      if (!(n instanceof i))
        throw new TypeError("Cannot call a class as a function");
    }
    var r = Hi().Buffer, o = It;
    function a(n, i, l) {
      n.copy(i, l);
    }
    t.exports = (function() {
      function n() {
        e(this, n), this.head = null, this.tail = null, this.length = 0;
      }
      return n.prototype.push = function(l) {
        var s = { data: l, next: null };
        this.length > 0 ? this.tail.next = s : this.head = s, this.tail = s, ++this.length;
      }, n.prototype.unshift = function(l) {
        var s = { data: l, next: this.head };
        this.length === 0 && (this.tail = s), this.head = s, ++this.length;
      }, n.prototype.shift = function() {
        if (this.length !== 0) {
          var l = this.head.data;
          return this.length === 1 ? this.head = this.tail = null : this.head = this.head.next, --this.length, l;
        }
      }, n.prototype.clear = function() {
        this.head = this.tail = null, this.length = 0;
      }, n.prototype.join = function(l) {
        if (this.length === 0) return "";
        for (var s = this.head, f = "" + s.data; s = s.next; )
          f += l + s.data;
        return f;
      }, n.prototype.concat = function(l) {
        if (this.length === 0) return r.alloc(0);
        for (var s = r.allocUnsafe(l >>> 0), f = this.head, d = 0; f; )
          a(f.data, s, d), d += f.data.length, f = f.next;
        return s;
      }, n;
    })(), o && o.inspect && o.inspect.custom && (t.exports.prototype[o.inspect.custom] = function() {
      var n = o.inspect({ length: this.length });
      return this.constructor.name + " " + n;
    });
  })(No)), No.exports;
}
var Lo, Nh;
function bm() {
  if (Nh) return Lo;
  Nh = 1;
  var t = Gi();
  function e(a, n) {
    var i = this, l = this._readableState && this._readableState.destroyed, s = this._writableState && this._writableState.destroyed;
    return l || s ? (n ? n(a) : a && (this._writableState ? this._writableState.errorEmitted || (this._writableState.errorEmitted = !0, t.nextTick(o, this, a)) : t.nextTick(o, this, a)), this) : (this._readableState && (this._readableState.destroyed = !0), this._writableState && (this._writableState.destroyed = !0), this._destroy(a || null, function(f) {
      !n && f ? i._writableState ? i._writableState.errorEmitted || (i._writableState.errorEmitted = !0, t.nextTick(o, i, f)) : t.nextTick(o, i, f) : n && n(f);
    }), this);
  }
  function r() {
    this._readableState && (this._readableState.destroyed = !1, this._readableState.reading = !1, this._readableState.ended = !1, this._readableState.endEmitted = !1), this._writableState && (this._writableState.destroyed = !1, this._writableState.ended = !1, this._writableState.ending = !1, this._writableState.finalCalled = !1, this._writableState.prefinished = !1, this._writableState.finished = !1, this._writableState.errorEmitted = !1);
  }
  function o(a, n) {
    a.emit("error", n);
  }
  return Lo = {
    destroy: e,
    undestroy: r
  }, Lo;
}
var Po, Lh;
function q0() {
  return Lh || (Lh = 1, Po = It.deprecate), Po;
}
var Co, Ph;
function _m() {
  if (Ph) return Co;
  Ph = 1;
  var t = Gi();
  Co = A;
  function e(ne) {
    var M = this;
    this.next = null, this.entry = null, this.finish = function() {
      se(M, ne);
    };
  }
  var r = !process.browser && ["v0.10", "v0.9."].indexOf(process.version.slice(0, 5)) > -1 ? setImmediate : t.nextTick, o;
  A.WritableState = w;
  var a = Object.create(mn());
  a.inherits = Sn();
  var n = {
    deprecate: q0()
  }, i = ym(), l = Hi().Buffer, s = (typeof Tt < "u" ? Tt : typeof window < "u" ? window : typeof self < "u" ? self : {}).Uint8Array || function() {
  };
  function f(ne) {
    return l.from(ne);
  }
  function d(ne) {
    return l.isBuffer(ne) || ne instanceof s;
  }
  var y = bm();
  a.inherits(A, i);
  function R() {
  }
  function w(ne, M) {
    o = o || Mr(), ne = ne || {};
    var Q = M instanceof o;
    this.objectMode = !!ne.objectMode, Q && (this.objectMode = this.objectMode || !!ne.writableObjectMode);
    var v = ne.highWaterMark, $ = ne.writableHighWaterMark, ee = this.objectMode ? 16 : 16 * 1024;
    v || v === 0 ? this.highWaterMark = v : Q && ($ || $ === 0) ? this.highWaterMark = $ : this.highWaterMark = ee, this.highWaterMark = Math.floor(this.highWaterMark), this.finalCalled = !1, this.needDrain = !1, this.ending = !1, this.ended = !1, this.finished = !1, this.destroyed = !1;
    var B = ne.decodeStrings === !1;
    this.decodeStrings = !B, this.defaultEncoding = ne.defaultEncoding || "utf8", this.length = 0, this.writing = !1, this.corked = 0, this.sync = !0, this.bufferProcessing = !1, this.onwrite = function(le) {
      k(M, le);
    }, this.writecb = null, this.writelen = 0, this.bufferedRequest = null, this.lastBufferedRequest = null, this.pendingcb = 0, this.prefinished = !1, this.errorEmitted = !1, this.bufferedRequestCount = 0, this.corkedRequestsFree = new e(this);
  }
  w.prototype.getBuffer = function() {
    for (var M = this.bufferedRequest, Q = []; M; )
      Q.push(M), M = M.next;
    return Q;
  }, (function() {
    try {
      Object.defineProperty(w.prototype, "buffer", {
        get: n.deprecate(function() {
          return this.getBuffer();
        }, "_writableState.buffer is deprecated. Use _writableState.getBuffer instead.", "DEP0003")
      });
    } catch {
    }
  })();
  var I;
  typeof Symbol == "function" && Symbol.hasInstance && typeof Function.prototype[Symbol.hasInstance] == "function" ? (I = Function.prototype[Symbol.hasInstance], Object.defineProperty(A, Symbol.hasInstance, {
    value: function(ne) {
      return I.call(this, ne) ? !0 : this !== A ? !1 : ne && ne._writableState instanceof w;
    }
  })) : I = function(ne) {
    return ne instanceof this;
  };
  function A(ne) {
    if (o = o || Mr(), !I.call(A, this) && !(this instanceof o))
      return new A(ne);
    this._writableState = new w(ne, this), this.writable = !0, ne && (typeof ne.write == "function" && (this._write = ne.write), typeof ne.writev == "function" && (this._writev = ne.writev), typeof ne.destroy == "function" && (this._destroy = ne.destroy), typeof ne.final == "function" && (this._final = ne.final)), i.call(this);
  }
  A.prototype.pipe = function() {
    this.emit("error", new Error("Cannot pipe, not readable"));
  };
  function u(ne, M) {
    var Q = new Error("write after end");
    ne.emit("error", Q), t.nextTick(M, Q);
  }
  function g(ne, M, Q, v) {
    var $ = !0, ee = !1;
    return Q === null ? ee = new TypeError("May not write null values to stream") : typeof Q != "string" && Q !== void 0 && !M.objectMode && (ee = new TypeError("Invalid non-string/buffer chunk")), ee && (ne.emit("error", ee), t.nextTick(v, ee), $ = !1), $;
  }
  A.prototype.write = function(ne, M, Q) {
    var v = this._writableState, $ = !1, ee = !v.objectMode && d(ne);
    return ee && !l.isBuffer(ne) && (ne = f(ne)), typeof M == "function" && (Q = M, M = null), ee ? M = "buffer" : M || (M = v.defaultEncoding), typeof Q != "function" && (Q = R), v.ended ? u(this, Q) : (ee || g(this, v, ne, Q)) && (v.pendingcb++, $ = b(this, v, ee, ne, M, Q)), $;
  }, A.prototype.cork = function() {
    var ne = this._writableState;
    ne.corked++;
  }, A.prototype.uncork = function() {
    var ne = this._writableState;
    ne.corked && (ne.corked--, !ne.writing && !ne.corked && !ne.bufferProcessing && ne.bufferedRequest && x(this, ne));
  }, A.prototype.setDefaultEncoding = function(M) {
    if (typeof M == "string" && (M = M.toLowerCase()), !(["hex", "utf8", "utf-8", "ascii", "binary", "base64", "ucs2", "ucs-2", "utf16le", "utf-16le", "raw"].indexOf((M + "").toLowerCase()) > -1)) throw new TypeError("Unknown encoding: " + M);
    return this._writableState.defaultEncoding = M, this;
  };
  function p(ne, M, Q) {
    return !ne.objectMode && ne.decodeStrings !== !1 && typeof M == "string" && (M = l.from(M, Q)), M;
  }
  Object.defineProperty(A.prototype, "writableHighWaterMark", {
    // making it explicit this property is not enumerable
    // because otherwise some prototype manipulation in
    // userland will fail
    enumerable: !1,
    get: function() {
      return this._writableState.highWaterMark;
    }
  });
  function b(ne, M, Q, v, $, ee) {
    if (!Q) {
      var B = p(M, v, $);
      v !== B && (Q = !0, $ = "buffer", v = B);
    }
    var le = M.objectMode ? 1 : v.length;
    M.length += le;
    var ae = M.length < M.highWaterMark;
    if (ae || (M.needDrain = !0), M.writing || M.corked) {
      var ce = M.lastBufferedRequest;
      M.lastBufferedRequest = {
        chunk: v,
        encoding: $,
        isBuf: Q,
        callback: ee,
        next: null
      }, ce ? ce.next = M.lastBufferedRequest : M.bufferedRequest = M.lastBufferedRequest, M.bufferedRequestCount += 1;
    } else
      C(ne, M, !1, le, v, $, ee);
    return ae;
  }
  function C(ne, M, Q, v, $, ee, B) {
    M.writelen = v, M.writecb = B, M.writing = !0, M.sync = !0, Q ? ne._writev($, M.onwrite) : ne._write($, ee, M.onwrite), M.sync = !1;
  }
  function N(ne, M, Q, v, $) {
    --M.pendingcb, Q ? (t.nextTick($, v), t.nextTick(J, ne, M), ne._writableState.errorEmitted = !0, ne.emit("error", v)) : ($(v), ne._writableState.errorEmitted = !0, ne.emit("error", v), J(ne, M));
  }
  function W(ne) {
    ne.writing = !1, ne.writecb = null, ne.length -= ne.writelen, ne.writelen = 0;
  }
  function k(ne, M) {
    var Q = ne._writableState, v = Q.sync, $ = Q.writecb;
    if (W(Q), M) N(ne, Q, v, M, $);
    else {
      var ee = D(Q);
      !ee && !Q.corked && !Q.bufferProcessing && Q.bufferedRequest && x(ne, Q), v ? r(L, ne, Q, ee, $) : L(ne, Q, ee, $);
    }
  }
  function L(ne, M, Q, v) {
    Q || U(ne, M), M.pendingcb--, v(), J(ne, M);
  }
  function U(ne, M) {
    M.length === 0 && M.needDrain && (M.needDrain = !1, ne.emit("drain"));
  }
  function x(ne, M) {
    M.bufferProcessing = !0;
    var Q = M.bufferedRequest;
    if (ne._writev && Q && Q.next) {
      var v = M.bufferedRequestCount, $ = new Array(v), ee = M.corkedRequestsFree;
      ee.entry = Q;
      for (var B = 0, le = !0; Q; )
        $[B] = Q, Q.isBuf || (le = !1), Q = Q.next, B += 1;
      $.allBuffers = le, C(ne, M, !0, M.length, $, "", ee.finish), M.pendingcb++, M.lastBufferedRequest = null, ee.next ? (M.corkedRequestsFree = ee.next, ee.next = null) : M.corkedRequestsFree = new e(M), M.bufferedRequestCount = 0;
    } else {
      for (; Q; ) {
        var ae = Q.chunk, ce = Q.encoding, Y = Q.callback, P = M.objectMode ? 1 : ae.length;
        if (C(ne, M, !1, P, ae, ce, Y), Q = Q.next, M.bufferedRequestCount--, M.writing)
          break;
      }
      Q === null && (M.lastBufferedRequest = null);
    }
    M.bufferedRequest = Q, M.bufferProcessing = !1;
  }
  A.prototype._write = function(ne, M, Q) {
    Q(new Error("_write() is not implemented"));
  }, A.prototype._writev = null, A.prototype.end = function(ne, M, Q) {
    var v = this._writableState;
    typeof ne == "function" ? (Q = ne, ne = null, M = null) : typeof M == "function" && (Q = M, M = null), ne != null && this.write(ne, M), v.corked && (v.corked = 1, this.uncork()), v.ending || re(this, v, Q);
  };
  function D(ne) {
    return ne.ending && ne.length === 0 && ne.bufferedRequest === null && !ne.finished && !ne.writing;
  }
  function F(ne, M) {
    ne._final(function(Q) {
      M.pendingcb--, Q && ne.emit("error", Q), M.prefinished = !0, ne.emit("prefinish"), J(ne, M);
    });
  }
  function T(ne, M) {
    !M.prefinished && !M.finalCalled && (typeof ne._final == "function" ? (M.pendingcb++, M.finalCalled = !0, t.nextTick(F, ne, M)) : (M.prefinished = !0, ne.emit("prefinish")));
  }
  function J(ne, M) {
    var Q = D(M);
    return Q && (T(ne, M), M.pendingcb === 0 && (M.finished = !0, ne.emit("finish"))), Q;
  }
  function re(ne, M, Q) {
    M.ending = !0, J(ne, M), Q && (M.finished ? t.nextTick(Q) : ne.once("finish", Q)), M.ended = !0, ne.writable = !1;
  }
  function se(ne, M, Q) {
    var v = ne.entry;
    for (ne.entry = null; v; ) {
      var $ = v.callback;
      M.pendingcb--, $(Q), v = v.next;
    }
    M.corkedRequestsFree.next = ne;
  }
  return Object.defineProperty(A.prototype, "destroyed", {
    get: function() {
      return this._writableState === void 0 ? !1 : this._writableState.destroyed;
    },
    set: function(ne) {
      this._writableState && (this._writableState.destroyed = ne);
    }
  }), A.prototype.destroy = y.destroy, A.prototype._undestroy = y.undestroy, A.prototype._destroy = function(ne, M) {
    this.end(), M(ne);
  }, Co;
}
var Mo, Ch;
function Mr() {
  if (Ch) return Mo;
  Ch = 1;
  var t = Gi(), e = Object.keys || function(y) {
    var R = [];
    for (var w in y)
      R.push(w);
    return R;
  };
  Mo = s;
  var r = Object.create(mn());
  r.inherits = Sn();
  var o = wm(), a = _m();
  r.inherits(s, o);
  for (var n = e(a.prototype), i = 0; i < n.length; i++) {
    var l = n[i];
    s.prototype[l] || (s.prototype[l] = a.prototype[l]);
  }
  function s(y) {
    if (!(this instanceof s)) return new s(y);
    o.call(this, y), a.call(this, y), y && y.readable === !1 && (this.readable = !1), y && y.writable === !1 && (this.writable = !1), this.allowHalfOpen = !0, y && y.allowHalfOpen === !1 && (this.allowHalfOpen = !1), this.once("end", f);
  }
  Object.defineProperty(s.prototype, "writableHighWaterMark", {
    // making it explicit this property is not enumerable
    // because otherwise some prototype manipulation in
    // userland will fail
    enumerable: !1,
    get: function() {
      return this._writableState.highWaterMark;
    }
  });
  function f() {
    this.allowHalfOpen || this._writableState.ended || t.nextTick(d, this);
  }
  function d(y) {
    y.end();
  }
  return Object.defineProperty(s.prototype, "destroyed", {
    get: function() {
      return this._readableState === void 0 || this._writableState === void 0 ? !1 : this._readableState.destroyed && this._writableState.destroyed;
    },
    set: function(y) {
      this._readableState === void 0 || this._writableState === void 0 || (this._readableState.destroyed = y, this._writableState.destroyed = y);
    }
  }), s.prototype._destroy = function(y, R) {
    this.push(null), this.end(), t.nextTick(R, y);
  }, Mo;
}
var ko = {}, Mh;
function kh() {
  if (Mh) return ko;
  Mh = 1;
  var t = Hi().Buffer, e = t.isEncoding || function(g) {
    switch (g = "" + g, g && g.toLowerCase()) {
      case "hex":
      case "utf8":
      case "utf-8":
      case "ascii":
      case "binary":
      case "base64":
      case "ucs2":
      case "ucs-2":
      case "utf16le":
      case "utf-16le":
      case "raw":
        return !0;
      default:
        return !1;
    }
  };
  function r(g) {
    if (!g) return "utf8";
    for (var p; ; )
      switch (g) {
        case "utf8":
        case "utf-8":
          return "utf8";
        case "ucs2":
        case "ucs-2":
        case "utf16le":
        case "utf-16le":
          return "utf16le";
        case "latin1":
        case "binary":
          return "latin1";
        case "base64":
        case "ascii":
        case "hex":
          return g;
        default:
          if (p) return;
          g = ("" + g).toLowerCase(), p = !0;
      }
  }
  function o(g) {
    var p = r(g);
    if (typeof p != "string" && (t.isEncoding === e || !e(g))) throw new Error("Unknown encoding: " + g);
    return p || g;
  }
  ko.StringDecoder = a;
  function a(g) {
    this.encoding = o(g);
    var p;
    switch (this.encoding) {
      case "utf16le":
        this.text = y, this.end = R, p = 4;
        break;
      case "utf8":
        this.fillLast = s, p = 4;
        break;
      case "base64":
        this.text = w, this.end = I, p = 3;
        break;
      default:
        this.write = A, this.end = u;
        return;
    }
    this.lastNeed = 0, this.lastTotal = 0, this.lastChar = t.allocUnsafe(p);
  }
  a.prototype.write = function(g) {
    if (g.length === 0) return "";
    var p, b;
    if (this.lastNeed) {
      if (p = this.fillLast(g), p === void 0) return "";
      b = this.lastNeed, this.lastNeed = 0;
    } else
      b = 0;
    return b < g.length ? p ? p + this.text(g, b) : this.text(g, b) : p || "";
  }, a.prototype.end = d, a.prototype.text = f, a.prototype.fillLast = function(g) {
    if (this.lastNeed <= g.length)
      return g.copy(this.lastChar, this.lastTotal - this.lastNeed, 0, this.lastNeed), this.lastChar.toString(this.encoding, 0, this.lastTotal);
    g.copy(this.lastChar, this.lastTotal - this.lastNeed, 0, g.length), this.lastNeed -= g.length;
  };
  function n(g) {
    return g <= 127 ? 0 : g >> 5 === 6 ? 2 : g >> 4 === 14 ? 3 : g >> 3 === 30 ? 4 : g >> 6 === 2 ? -1 : -2;
  }
  function i(g, p, b) {
    var C = p.length - 1;
    if (C < b) return 0;
    var N = n(p[C]);
    return N >= 0 ? (N > 0 && (g.lastNeed = N - 1), N) : --C < b || N === -2 ? 0 : (N = n(p[C]), N >= 0 ? (N > 0 && (g.lastNeed = N - 2), N) : --C < b || N === -2 ? 0 : (N = n(p[C]), N >= 0 ? (N > 0 && (N === 2 ? N = 0 : g.lastNeed = N - 3), N) : 0));
  }
  function l(g, p, b) {
    if ((p[0] & 192) !== 128)
      return g.lastNeed = 0, "�";
    if (g.lastNeed > 1 && p.length > 1) {
      if ((p[1] & 192) !== 128)
        return g.lastNeed = 1, "�";
      if (g.lastNeed > 2 && p.length > 2 && (p[2] & 192) !== 128)
        return g.lastNeed = 2, "�";
    }
  }
  function s(g) {
    var p = this.lastTotal - this.lastNeed, b = l(this, g);
    if (b !== void 0) return b;
    if (this.lastNeed <= g.length)
      return g.copy(this.lastChar, p, 0, this.lastNeed), this.lastChar.toString(this.encoding, 0, this.lastTotal);
    g.copy(this.lastChar, p, 0, g.length), this.lastNeed -= g.length;
  }
  function f(g, p) {
    var b = i(this, g, p);
    if (!this.lastNeed) return g.toString("utf8", p);
    this.lastTotal = b;
    var C = g.length - (b - this.lastNeed);
    return g.copy(this.lastChar, 0, C), g.toString("utf8", p, C);
  }
  function d(g) {
    var p = g && g.length ? this.write(g) : "";
    return this.lastNeed ? p + "�" : p;
  }
  function y(g, p) {
    if ((g.length - p) % 2 === 0) {
      var b = g.toString("utf16le", p);
      if (b) {
        var C = b.charCodeAt(b.length - 1);
        if (C >= 55296 && C <= 56319)
          return this.lastNeed = 2, this.lastTotal = 4, this.lastChar[0] = g[g.length - 2], this.lastChar[1] = g[g.length - 1], b.slice(0, -1);
      }
      return b;
    }
    return this.lastNeed = 1, this.lastTotal = 2, this.lastChar[0] = g[g.length - 1], g.toString("utf16le", p, g.length - 1);
  }
  function R(g) {
    var p = g && g.length ? this.write(g) : "";
    if (this.lastNeed) {
      var b = this.lastTotal - this.lastNeed;
      return p + this.lastChar.toString("utf16le", 0, b);
    }
    return p;
  }
  function w(g, p) {
    var b = (g.length - p) % 3;
    return b === 0 ? g.toString("base64", p) : (this.lastNeed = 3 - b, this.lastTotal = 3, b === 1 ? this.lastChar[0] = g[g.length - 1] : (this.lastChar[0] = g[g.length - 2], this.lastChar[1] = g[g.length - 1]), g.toString("base64", p, g.length - b));
  }
  function I(g) {
    var p = g && g.length ? this.write(g) : "";
    return this.lastNeed ? p + this.lastChar.toString("base64", 0, 3 - this.lastNeed) : p;
  }
  function A(g) {
    return g.toString(this.encoding);
  }
  function u(g) {
    return g && g.length ? this.write(g) : "";
  }
  return ko;
}
var Fo, Fh;
function wm() {
  if (Fh) return Fo;
  Fh = 1;
  var t = Gi();
  Fo = p;
  var e = j0(), r;
  p.ReadableState = g, Ke.EventEmitter;
  var o = function(Y, P) {
    return Y.listeners(P).length;
  }, a = ym(), n = Hi().Buffer, i = (typeof Tt < "u" ? Tt : typeof window < "u" ? window : typeof self < "u" ? self : {}).Uint8Array || function() {
  };
  function l(Y) {
    return n.from(Y);
  }
  function s(Y) {
    return n.isBuffer(Y) || Y instanceof i;
  }
  var f = Object.create(mn());
  f.inherits = Sn();
  var d = It, y = void 0;
  d && d.debuglog ? y = d.debuglog("stream") : y = function() {
  };
  var R = W0(), w = bm(), I;
  f.inherits(p, a);
  var A = ["error", "close", "destroy", "pause", "resume"];
  function u(Y, P, q) {
    if (typeof Y.prependListener == "function") return Y.prependListener(P, q);
    !Y._events || !Y._events[P] ? Y.on(P, q) : e(Y._events[P]) ? Y._events[P].unshift(q) : Y._events[P] = [q, Y._events[P]];
  }
  function g(Y, P) {
    r = r || Mr(), Y = Y || {};
    var q = P instanceof r;
    this.objectMode = !!Y.objectMode, q && (this.objectMode = this.objectMode || !!Y.readableObjectMode);
    var V = Y.highWaterMark, Z = Y.readableHighWaterMark, X = this.objectMode ? 16 : 16 * 1024;
    V || V === 0 ? this.highWaterMark = V : q && (Z || Z === 0) ? this.highWaterMark = Z : this.highWaterMark = X, this.highWaterMark = Math.floor(this.highWaterMark), this.buffer = new R(), this.length = 0, this.pipes = null, this.pipesCount = 0, this.flowing = null, this.ended = !1, this.endEmitted = !1, this.reading = !1, this.sync = !0, this.needReadable = !1, this.emittedReadable = !1, this.readableListening = !1, this.resumeScheduled = !1, this.destroyed = !1, this.defaultEncoding = Y.defaultEncoding || "utf8", this.awaitDrain = 0, this.readingMore = !1, this.decoder = null, this.encoding = null, Y.encoding && (I || (I = kh().StringDecoder), this.decoder = new I(Y.encoding), this.encoding = Y.encoding);
  }
  function p(Y) {
    if (r = r || Mr(), !(this instanceof p)) return new p(Y);
    this._readableState = new g(Y, this), this.readable = !0, Y && (typeof Y.read == "function" && (this._read = Y.read), typeof Y.destroy == "function" && (this._destroy = Y.destroy)), a.call(this);
  }
  Object.defineProperty(p.prototype, "destroyed", {
    get: function() {
      return this._readableState === void 0 ? !1 : this._readableState.destroyed;
    },
    set: function(Y) {
      this._readableState && (this._readableState.destroyed = Y);
    }
  }), p.prototype.destroy = w.destroy, p.prototype._undestroy = w.undestroy, p.prototype._destroy = function(Y, P) {
    this.push(null), P(Y);
  }, p.prototype.push = function(Y, P) {
    var q = this._readableState, V;
    return q.objectMode ? V = !0 : typeof Y == "string" && (P = P || q.defaultEncoding, P !== q.encoding && (Y = n.from(Y, P), P = ""), V = !0), b(this, Y, P, !1, V);
  }, p.prototype.unshift = function(Y) {
    return b(this, Y, null, !0, !1);
  };
  function b(Y, P, q, V, Z) {
    var X = Y._readableState;
    if (P === null)
      X.reading = !1, x(Y, X);
    else {
      var m;
      Z || (m = N(X, P)), m ? Y.emit("error", m) : X.objectMode || P && P.length > 0 ? (typeof P != "string" && !X.objectMode && Object.getPrototypeOf(P) !== n.prototype && (P = l(P)), V ? X.endEmitted ? Y.emit("error", new Error("stream.unshift() after end event")) : C(Y, X, P, !0) : X.ended ? Y.emit("error", new Error("stream.push() after EOF")) : (X.reading = !1, X.decoder && !q ? (P = X.decoder.write(P), X.objectMode || P.length !== 0 ? C(Y, X, P, !1) : T(Y, X)) : C(Y, X, P, !1))) : V || (X.reading = !1);
    }
    return W(X);
  }
  function C(Y, P, q, V) {
    P.flowing && P.length === 0 && !P.sync ? (Y.emit("data", q), Y.read(0)) : (P.length += P.objectMode ? 1 : q.length, V ? P.buffer.unshift(q) : P.buffer.push(q), P.needReadable && D(Y)), T(Y, P);
  }
  function N(Y, P) {
    var q;
    return !s(P) && typeof P != "string" && P !== void 0 && !Y.objectMode && (q = new TypeError("Invalid non-string/buffer chunk")), q;
  }
  function W(Y) {
    return !Y.ended && (Y.needReadable || Y.length < Y.highWaterMark || Y.length === 0);
  }
  p.prototype.isPaused = function() {
    return this._readableState.flowing === !1;
  }, p.prototype.setEncoding = function(Y) {
    return I || (I = kh().StringDecoder), this._readableState.decoder = new I(Y), this._readableState.encoding = Y, this;
  };
  var k = 8388608;
  function L(Y) {
    return Y >= k ? Y = k : (Y--, Y |= Y >>> 1, Y |= Y >>> 2, Y |= Y >>> 4, Y |= Y >>> 8, Y |= Y >>> 16, Y++), Y;
  }
  function U(Y, P) {
    return Y <= 0 || P.length === 0 && P.ended ? 0 : P.objectMode ? 1 : Y !== Y ? P.flowing && P.length ? P.buffer.head.data.length : P.length : (Y > P.highWaterMark && (P.highWaterMark = L(Y)), Y <= P.length ? Y : P.ended ? P.length : (P.needReadable = !0, 0));
  }
  p.prototype.read = function(Y) {
    y("read", Y), Y = parseInt(Y, 10);
    var P = this._readableState, q = Y;
    if (Y !== 0 && (P.emittedReadable = !1), Y === 0 && P.needReadable && (P.length >= P.highWaterMark || P.ended))
      return y("read: emitReadable", P.length, P.ended), P.length === 0 && P.ended ? le(this) : D(this), null;
    if (Y = U(Y, P), Y === 0 && P.ended)
      return P.length === 0 && le(this), null;
    var V = P.needReadable;
    y("need readable", V), (P.length === 0 || P.length - Y < P.highWaterMark) && (V = !0, y("length less than watermark", V)), P.ended || P.reading ? (V = !1, y("reading or ended", V)) : V && (y("do read"), P.reading = !0, P.sync = !0, P.length === 0 && (P.needReadable = !0), this._read(P.highWaterMark), P.sync = !1, P.reading || (Y = U(q, P)));
    var Z;
    return Y > 0 ? Z = v(Y, P) : Z = null, Z === null ? (P.needReadable = !0, Y = 0) : P.length -= Y, P.length === 0 && (P.ended || (P.needReadable = !0), q !== Y && P.ended && le(this)), Z !== null && this.emit("data", Z), Z;
  };
  function x(Y, P) {
    if (!P.ended) {
      if (P.decoder) {
        var q = P.decoder.end();
        q && q.length && (P.buffer.push(q), P.length += P.objectMode ? 1 : q.length);
      }
      P.ended = !0, D(Y);
    }
  }
  function D(Y) {
    var P = Y._readableState;
    P.needReadable = !1, P.emittedReadable || (y("emitReadable", P.flowing), P.emittedReadable = !0, P.sync ? t.nextTick(F, Y) : F(Y));
  }
  function F(Y) {
    y("emit readable"), Y.emit("readable"), Q(Y);
  }
  function T(Y, P) {
    P.readingMore || (P.readingMore = !0, t.nextTick(J, Y, P));
  }
  function J(Y, P) {
    for (var q = P.length; !P.reading && !P.flowing && !P.ended && P.length < P.highWaterMark && (y("maybeReadMore read 0"), Y.read(0), q !== P.length); )
      q = P.length;
    P.readingMore = !1;
  }
  p.prototype._read = function(Y) {
    this.emit("error", new Error("_read() is not implemented"));
  }, p.prototype.pipe = function(Y, P) {
    var q = this, V = this._readableState;
    switch (V.pipesCount) {
      case 0:
        V.pipes = Y;
        break;
      case 1:
        V.pipes = [V.pipes, Y];
        break;
      default:
        V.pipes.push(Y);
        break;
    }
    V.pipesCount += 1, y("pipe count=%d opts=%j", V.pipesCount, P);
    var Z = (!P || P.end !== !1) && Y !== process.stdout && Y !== process.stderr, X = Z ? c : de;
    V.endEmitted ? t.nextTick(X) : q.once("end", X), Y.on("unpipe", m);
    function m(pe, he) {
      y("onunpipe"), pe === q && he && he.hasUnpiped === !1 && (he.hasUnpiped = !0, G());
    }
    function c() {
      y("onend"), Y.end();
    }
    var E = re(q);
    Y.on("drain", E);
    var j = !1;
    function G() {
      y("cleanup"), Y.removeListener("close", te), Y.removeListener("finish", z), Y.removeListener("drain", E), Y.removeListener("error", H), Y.removeListener("unpipe", m), q.removeListener("end", c), q.removeListener("end", de), q.removeListener("data", O), j = !0, V.awaitDrain && (!Y._writableState || Y._writableState.needDrain) && E();
    }
    var K = !1;
    q.on("data", O);
    function O(pe) {
      y("ondata"), K = !1;
      var he = Y.write(pe);
      he === !1 && !K && ((V.pipesCount === 1 && V.pipes === Y || V.pipesCount > 1 && ce(V.pipes, Y) !== -1) && !j && (y("false write response, pause", V.awaitDrain), V.awaitDrain++, K = !0), q.pause());
    }
    function H(pe) {
      y("onerror", pe), de(), Y.removeListener("error", H), o(Y, "error") === 0 && Y.emit("error", pe);
    }
    u(Y, "error", H);
    function te() {
      Y.removeListener("finish", z), de();
    }
    Y.once("close", te);
    function z() {
      y("onfinish"), Y.removeListener("close", te), de();
    }
    Y.once("finish", z);
    function de() {
      y("unpipe"), q.unpipe(Y);
    }
    return Y.emit("pipe", q), V.flowing || (y("pipe resume"), q.resume()), Y;
  };
  function re(Y) {
    return function() {
      var P = Y._readableState;
      y("pipeOnDrain", P.awaitDrain), P.awaitDrain && P.awaitDrain--, P.awaitDrain === 0 && o(Y, "data") && (P.flowing = !0, Q(Y));
    };
  }
  p.prototype.unpipe = function(Y) {
    var P = this._readableState, q = { hasUnpiped: !1 };
    if (P.pipesCount === 0) return this;
    if (P.pipesCount === 1)
      return Y && Y !== P.pipes ? this : (Y || (Y = P.pipes), P.pipes = null, P.pipesCount = 0, P.flowing = !1, Y && Y.emit("unpipe", this, q), this);
    if (!Y) {
      var V = P.pipes, Z = P.pipesCount;
      P.pipes = null, P.pipesCount = 0, P.flowing = !1;
      for (var X = 0; X < Z; X++)
        V[X].emit("unpipe", this, { hasUnpiped: !1 });
      return this;
    }
    var m = ce(P.pipes, Y);
    return m === -1 ? this : (P.pipes.splice(m, 1), P.pipesCount -= 1, P.pipesCount === 1 && (P.pipes = P.pipes[0]), Y.emit("unpipe", this, q), this);
  }, p.prototype.on = function(Y, P) {
    var q = a.prototype.on.call(this, Y, P);
    if (Y === "data")
      this._readableState.flowing !== !1 && this.resume();
    else if (Y === "readable") {
      var V = this._readableState;
      !V.endEmitted && !V.readableListening && (V.readableListening = V.needReadable = !0, V.emittedReadable = !1, V.reading ? V.length && D(this) : t.nextTick(se, this));
    }
    return q;
  }, p.prototype.addListener = p.prototype.on;
  function se(Y) {
    y("readable nexttick read 0"), Y.read(0);
  }
  p.prototype.resume = function() {
    var Y = this._readableState;
    return Y.flowing || (y("resume"), Y.flowing = !0, ne(this, Y)), this;
  };
  function ne(Y, P) {
    P.resumeScheduled || (P.resumeScheduled = !0, t.nextTick(M, Y, P));
  }
  function M(Y, P) {
    P.reading || (y("resume read 0"), Y.read(0)), P.resumeScheduled = !1, P.awaitDrain = 0, Y.emit("resume"), Q(Y), P.flowing && !P.reading && Y.read(0);
  }
  p.prototype.pause = function() {
    return y("call pause flowing=%j", this._readableState.flowing), this._readableState.flowing !== !1 && (y("pause"), this._readableState.flowing = !1, this.emit("pause")), this;
  };
  function Q(Y) {
    var P = Y._readableState;
    for (y("flow", P.flowing); P.flowing && Y.read() !== null; )
      ;
  }
  p.prototype.wrap = function(Y) {
    var P = this, q = this._readableState, V = !1;
    Y.on("end", function() {
      if (y("wrapped end"), q.decoder && !q.ended) {
        var m = q.decoder.end();
        m && m.length && P.push(m);
      }
      P.push(null);
    }), Y.on("data", function(m) {
      if (y("wrapped data"), q.decoder && (m = q.decoder.write(m)), !(q.objectMode && m == null) && !(!q.objectMode && (!m || !m.length))) {
        var c = P.push(m);
        c || (V = !0, Y.pause());
      }
    });
    for (var Z in Y)
      this[Z] === void 0 && typeof Y[Z] == "function" && (this[Z] = /* @__PURE__ */ (function(m) {
        return function() {
          return Y[m].apply(Y, arguments);
        };
      })(Z));
    for (var X = 0; X < A.length; X++)
      Y.on(A[X], this.emit.bind(this, A[X]));
    return this._read = function(m) {
      y("wrapped _read", m), V && (V = !1, Y.resume());
    }, this;
  }, Object.defineProperty(p.prototype, "readableHighWaterMark", {
    // making it explicit this property is not enumerable
    // because otherwise some prototype manipulation in
    // userland will fail
    enumerable: !1,
    get: function() {
      return this._readableState.highWaterMark;
    }
  }), p._fromList = v;
  function v(Y, P) {
    if (P.length === 0) return null;
    var q;
    return P.objectMode ? q = P.buffer.shift() : !Y || Y >= P.length ? (P.decoder ? q = P.buffer.join("") : P.buffer.length === 1 ? q = P.buffer.head.data : q = P.buffer.concat(P.length), P.buffer.clear()) : q = $(Y, P.buffer, P.decoder), q;
  }
  function $(Y, P, q) {
    var V;
    return Y < P.head.data.length ? (V = P.head.data.slice(0, Y), P.head.data = P.head.data.slice(Y)) : Y === P.head.data.length ? V = P.shift() : V = q ? ee(Y, P) : B(Y, P), V;
  }
  function ee(Y, P) {
    var q = P.head, V = 1, Z = q.data;
    for (Y -= Z.length; q = q.next; ) {
      var X = q.data, m = Y > X.length ? X.length : Y;
      if (m === X.length ? Z += X : Z += X.slice(0, Y), Y -= m, Y === 0) {
        m === X.length ? (++V, q.next ? P.head = q.next : P.head = P.tail = null) : (P.head = q, q.data = X.slice(m));
        break;
      }
      ++V;
    }
    return P.length -= V, Z;
  }
  function B(Y, P) {
    var q = n.allocUnsafe(Y), V = P.head, Z = 1;
    for (V.data.copy(q), Y -= V.data.length; V = V.next; ) {
      var X = V.data, m = Y > X.length ? X.length : Y;
      if (X.copy(q, q.length - Y, 0, m), Y -= m, Y === 0) {
        m === X.length ? (++Z, V.next ? P.head = V.next : P.head = P.tail = null) : (P.head = V, V.data = X.slice(m));
        break;
      }
      ++Z;
    }
    return P.length -= Z, q;
  }
  function le(Y) {
    var P = Y._readableState;
    if (P.length > 0) throw new Error('"endReadable()" called on non-empty stream');
    P.endEmitted || (P.ended = !0, t.nextTick(ae, P, Y));
  }
  function ae(Y, P) {
    !Y.endEmitted && Y.length === 0 && (Y.endEmitted = !0, P.readable = !1, P.emit("end"));
  }
  function ce(Y, P) {
    for (var q = 0, V = Y.length; q < V; q++)
      if (Y[q] === P) return q;
    return -1;
  }
  return Fo;
}
var jo, jh;
function mm() {
  if (jh) return jo;
  jh = 1, jo = o;
  var t = Mr(), e = Object.create(mn());
  e.inherits = Sn(), e.inherits(o, t);
  function r(i, l) {
    var s = this._transformState;
    s.transforming = !1;
    var f = s.writecb;
    if (!f)
      return this.emit("error", new Error("write callback called multiple times"));
    s.writechunk = null, s.writecb = null, l != null && this.push(l), f(i);
    var d = this._readableState;
    d.reading = !1, (d.needReadable || d.length < d.highWaterMark) && this._read(d.highWaterMark);
  }
  function o(i) {
    if (!(this instanceof o)) return new o(i);
    t.call(this, i), this._transformState = {
      afterTransform: r.bind(this),
      needTransform: !1,
      transforming: !1,
      writecb: null,
      writechunk: null,
      writeencoding: null
    }, this._readableState.needReadable = !0, this._readableState.sync = !1, i && (typeof i.transform == "function" && (this._transform = i.transform), typeof i.flush == "function" && (this._flush = i.flush)), this.on("prefinish", a);
  }
  function a() {
    var i = this;
    typeof this._flush == "function" ? this._flush(function(l, s) {
      n(i, l, s);
    }) : n(this, null, null);
  }
  o.prototype.push = function(i, l) {
    return this._transformState.needTransform = !1, t.prototype.push.call(this, i, l);
  }, o.prototype._transform = function(i, l, s) {
    throw new Error("_transform() is not implemented");
  }, o.prototype._write = function(i, l, s) {
    var f = this._transformState;
    if (f.writecb = s, f.writechunk = i, f.writeencoding = l, !f.transforming) {
      var d = this._readableState;
      (f.needTransform || d.needReadable || d.length < d.highWaterMark) && this._read(d.highWaterMark);
    }
  }, o.prototype._read = function(i) {
    var l = this._transformState;
    l.writechunk !== null && l.writecb && !l.transforming ? (l.transforming = !0, this._transform(l.writechunk, l.writeencoding, l.afterTransform)) : l.needTransform = !0;
  }, o.prototype._destroy = function(i, l) {
    var s = this;
    t.prototype._destroy.call(this, i, function(f) {
      l(f), s.emit("close");
    });
  };
  function n(i, l, s) {
    if (l) return i.emit("error", l);
    if (s != null && i.push(s), i._writableState.length) throw new Error("Calling transform done when ws.length != 0");
    if (i._transformState.transforming) throw new Error("Calling transform done when still transforming");
    return i.push(null);
  }
  return jo;
}
var $o, $h;
function B0() {
  if ($h) return $o;
  $h = 1, $o = r;
  var t = mm(), e = Object.create(mn());
  e.inherits = Sn(), e.inherits(r, t);
  function r(o) {
    if (!(this instanceof r)) return new r(o);
    t.call(this, o);
  }
  return r.prototype._transform = function(o, a, n) {
    n(null, o);
  }, $o;
}
var Wh;
function U0() {
  return Wh || (Wh = 1, (function(t, e) {
    var r = tr;
    process.env.READABLE_STREAM === "disable" && r ? (t.exports = r, e = t.exports = r.Readable, e.Readable = r.Readable, e.Writable = r.Writable, e.Duplex = r.Duplex, e.Transform = r.Transform, e.PassThrough = r.PassThrough, e.Stream = r) : (e = t.exports = wm(), e.Stream = r || e, e.Readable = e, e.Writable = _m(), e.Duplex = Mr(), e.Transform = mm(), e.PassThrough = B0());
  })($n, $n.exports)), $n.exports;
}
var Wo, qh;
function G0() {
  return qh || (qh = 1, Wo = U0().PassThrough), Wo;
}
var qo, Bh;
function H0() {
  if (Bh) return qo;
  Bh = 1;
  var t = It, e = G0();
  qo = {
    Readable: o,
    Writable: a
  }, t.inherits(o, e), t.inherits(a, e);
  function r(n, i, l) {
    n[i] = function() {
      return delete n[i], l.apply(this, arguments), this[i].apply(this, arguments);
    };
  }
  function o(n, i) {
    if (!(this instanceof o))
      return new o(n, i);
    e.call(this, i), r(this, "_read", function() {
      var l = n.call(this, i), s = this.emit.bind(this, "error");
      l.on("error", s), l.pipe(this);
    }), this.emit("readable");
  }
  function a(n, i) {
    if (!(this instanceof a))
      return new a(n, i);
    e.call(this, i), r(this, "_write", function() {
      var l = n.call(this, i), s = this.emit.bind(this, "error");
      l.on("error", s), this.pipe(l);
    }), this.emit("writable");
  }
  return qo;
}
var Bo, Uh;
function Sm() {
  return Uh || (Uh = 1, Bo = function(t, e) {
    if (typeof t != "string")
      throw new TypeError("expected path to be a string");
    if (t === "\\" || t === "/") return "/";
    var r = t.length;
    if (r <= 1) return t;
    var o = "";
    if (r > 4 && t[3] === "\\") {
      var a = t[2];
      (a === "?" || a === ".") && t.slice(0, 2) === "\\\\" && (t = t.slice(2), o = "//");
    }
    var n = t.split(/[/\\]+/);
    return e !== !1 && n[n.length - 1] === "" && n.pop(), o + n.join("/");
  }), Bo;
}
var Uo, Gh;
function Em() {
  if (Gh) return Uo;
  Gh = 1;
  function t(e) {
    return e;
  }
  return Uo = t, Uo;
}
var Go, Hh;
function z0() {
  if (Hh) return Go;
  Hh = 1;
  function t(e, r, o) {
    switch (o.length) {
      case 0:
        return e.call(r);
      case 1:
        return e.call(r, o[0]);
      case 2:
        return e.call(r, o[0], o[1]);
      case 3:
        return e.call(r, o[0], o[1], o[2]);
    }
    return e.apply(r, o);
  }
  return Go = t, Go;
}
var Ho, zh;
function V0() {
  if (zh) return Ho;
  zh = 1;
  var t = z0(), e = Math.max;
  function r(o, a, n) {
    return a = e(a === void 0 ? o.length - 1 : a, 0), function() {
      for (var i = arguments, l = -1, s = e(i.length - a, 0), f = Array(s); ++l < s; )
        f[l] = i[a + l];
      l = -1;
      for (var d = Array(a + 1); ++l < a; )
        d[l] = i[l];
      return d[a] = n(f), t(o, this, d);
    };
  }
  return Ho = r, Ho;
}
var zo, Vh;
function Z0() {
  if (Vh) return zo;
  Vh = 1;
  function t(e) {
    return function() {
      return e;
    };
  }
  return zo = t, zo;
}
var Vo, Zh;
function vm() {
  if (Zh) return Vo;
  Zh = 1;
  var t = typeof Tt == "object" && Tt && Tt.Object === Object && Tt;
  return Vo = t, Vo;
}
var Zo, Yh;
function En() {
  if (Yh) return Zo;
  Yh = 1;
  var t = vm(), e = typeof self == "object" && self && self.Object === Object && self, r = t || e || Function("return this")();
  return Zo = r, Zo;
}
var Yo, Kh;
function xc() {
  if (Kh) return Yo;
  Kh = 1;
  var t = En(), e = t.Symbol;
  return Yo = e, Yo;
}
var Ko, Jh;
function Y0() {
  if (Jh) return Ko;
  Jh = 1;
  var t = xc(), e = Object.prototype, r = e.hasOwnProperty, o = e.toString, a = t ? t.toStringTag : void 0;
  function n(i) {
    var l = r.call(i, a), s = i[a];
    try {
      i[a] = void 0;
      var f = !0;
    } catch {
    }
    var d = o.call(i);
    return f && (l ? i[a] = s : delete i[a]), d;
  }
  return Ko = n, Ko;
}
var Jo, Xh;
function K0() {
  if (Xh) return Jo;
  Xh = 1;
  var t = Object.prototype, e = t.toString;
  function r(o) {
    return e.call(o);
  }
  return Jo = r, Jo;
}
var Xo, Qh;
function zi() {
  if (Qh) return Xo;
  Qh = 1;
  var t = xc(), e = Y0(), r = K0(), o = "[object Null]", a = "[object Undefined]", n = t ? t.toStringTag : void 0;
  function i(l) {
    return l == null ? l === void 0 ? a : o : n && n in Object(l) ? e(l) : r(l);
  }
  return Xo = i, Xo;
}
var Qo, ep;
function Vi() {
  if (ep) return Qo;
  ep = 1;
  function t(e) {
    var r = typeof e;
    return e != null && (r == "object" || r == "function");
  }
  return Qo = t, Qo;
}
var ea, tp;
function Rm() {
  if (tp) return ea;
  tp = 1;
  var t = zi(), e = Vi(), r = "[object AsyncFunction]", o = "[object Function]", a = "[object GeneratorFunction]", n = "[object Proxy]";
  function i(l) {
    if (!e(l))
      return !1;
    var s = t(l);
    return s == o || s == a || s == r || s == n;
  }
  return ea = i, ea;
}
var ta, rp;
function J0() {
  if (rp) return ta;
  rp = 1;
  var t = En(), e = t["__core-js_shared__"];
  return ta = e, ta;
}
var ra, np;
function X0() {
  if (np) return ra;
  np = 1;
  var t = J0(), e = (function() {
    var o = /[^.]+$/.exec(t && t.keys && t.keys.IE_PROTO || "");
    return o ? "Symbol(src)_1." + o : "";
  })();
  function r(o) {
    return !!e && e in o;
  }
  return ra = r, ra;
}
var na, ip;
function Q0() {
  if (ip) return na;
  ip = 1;
  var t = Function.prototype, e = t.toString;
  function r(o) {
    if (o != null) {
      try {
        return e.call(o);
      } catch {
      }
      try {
        return o + "";
      } catch {
      }
    }
    return "";
  }
  return na = r, na;
}
var ia, sp;
function eR() {
  if (sp) return ia;
  sp = 1;
  var t = Rm(), e = X0(), r = Vi(), o = Q0(), a = /[\\^$.*+?()[\]{}|]/g, n = /^\[object .+?Constructor\]$/, i = Function.prototype, l = Object.prototype, s = i.toString, f = l.hasOwnProperty, d = RegExp(
    "^" + s.call(f).replace(a, "\\$&").replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, "$1.*?") + "$"
  );
  function y(R) {
    if (!r(R) || e(R))
      return !1;
    var w = t(R) ? d : n;
    return w.test(o(R));
  }
  return ia = y, ia;
}
var sa, op;
function tR() {
  if (op) return sa;
  op = 1;
  function t(e, r) {
    return e?.[r];
  }
  return sa = t, sa;
}
var oa, ap;
function Zi() {
  if (ap) return oa;
  ap = 1;
  var t = eR(), e = tR();
  function r(o, a) {
    var n = e(o, a);
    return t(n) ? n : void 0;
  }
  return oa = r, oa;
}
var aa, lp;
function rR() {
  if (lp) return aa;
  lp = 1;
  var t = Zi(), e = (function() {
    try {
      var r = t(Object, "defineProperty");
      return r({}, "", {}), r;
    } catch {
    }
  })();
  return aa = e, aa;
}
var la, up;
function nR() {
  if (up) return la;
  up = 1;
  var t = Z0(), e = rR(), r = Em(), o = e ? function(a, n) {
    return e(a, "toString", {
      configurable: !0,
      enumerable: !1,
      value: t(n),
      writable: !0
    });
  } : r;
  return la = o, la;
}
var ua, fp;
function iR() {
  if (fp) return ua;
  fp = 1;
  var t = 800, e = 16, r = Date.now;
  function o(a) {
    var n = 0, i = 0;
    return function() {
      var l = r(), s = e - (l - i);
      if (i = l, s > 0) {
        if (++n >= t)
          return arguments[0];
      } else
        n = 0;
      return a.apply(void 0, arguments);
    };
  }
  return ua = o, ua;
}
var fa, cp;
function sR() {
  if (cp) return fa;
  cp = 1;
  var t = nR(), e = iR(), r = e(t);
  return fa = r, fa;
}
var ca, dp;
function Ic() {
  if (dp) return ca;
  dp = 1;
  var t = Em(), e = V0(), r = sR();
  function o(a, n) {
    return r(e(a, n, t), a + "");
  }
  return ca = o, ca;
}
var da, hp;
function Dc() {
  if (hp) return da;
  hp = 1;
  function t(e, r) {
    return e === r || e !== e && r !== r;
  }
  return da = t, da;
}
var ha, pp;
function Am() {
  if (pp) return ha;
  pp = 1;
  var t = 9007199254740991;
  function e(r) {
    return typeof r == "number" && r > -1 && r % 1 == 0 && r <= t;
  }
  return ha = e, ha;
}
var pa, gp;
function Oc() {
  if (gp) return pa;
  gp = 1;
  var t = Rm(), e = Am();
  function r(o) {
    return o != null && e(o.length) && !t(o);
  }
  return pa = r, pa;
}
var ga, yp;
function Tm() {
  if (yp) return ga;
  yp = 1;
  var t = 9007199254740991, e = /^(?:0|[1-9]\d*)$/;
  function r(o, a) {
    var n = typeof o;
    return a = a ?? t, !!a && (n == "number" || n != "symbol" && e.test(o)) && o > -1 && o % 1 == 0 && o < a;
  }
  return ga = r, ga;
}
var ya, bp;
function oR() {
  if (bp) return ya;
  bp = 1;
  var t = Dc(), e = Oc(), r = Tm(), o = Vi();
  function a(n, i, l) {
    if (!o(l))
      return !1;
    var s = typeof i;
    return (s == "number" ? e(l) && r(i, l.length) : s == "string" && i in l) ? t(l[i], n) : !1;
  }
  return ya = a, ya;
}
var ba, _p;
function aR() {
  if (_p) return ba;
  _p = 1;
  function t(e, r) {
    for (var o = -1, a = Array(e); ++o < e; )
      a[o] = r(o);
    return a;
  }
  return ba = t, ba;
}
var _a, wp;
function vn() {
  if (wp) return _a;
  wp = 1;
  function t(e) {
    return e != null && typeof e == "object";
  }
  return _a = t, _a;
}
var wa, mp;
function lR() {
  if (mp) return wa;
  mp = 1;
  var t = zi(), e = vn(), r = "[object Arguments]";
  function o(a) {
    return e(a) && t(a) == r;
  }
  return wa = o, wa;
}
var ma, Sp;
function xm() {
  if (Sp) return ma;
  Sp = 1;
  var t = lR(), e = vn(), r = Object.prototype, o = r.hasOwnProperty, a = r.propertyIsEnumerable, n = t(/* @__PURE__ */ (function() {
    return arguments;
  })()) ? t : function(i) {
    return e(i) && o.call(i, "callee") && !a.call(i, "callee");
  };
  return ma = n, ma;
}
var Sa, Ep;
function Im() {
  if (Ep) return Sa;
  Ep = 1;
  var t = Array.isArray;
  return Sa = t, Sa;
}
var ln = { exports: {} }, Ea, vp;
function uR() {
  if (vp) return Ea;
  vp = 1;
  function t() {
    return !1;
  }
  return Ea = t, Ea;
}
ln.exports;
var Rp;
function fR() {
  return Rp || (Rp = 1, (function(t, e) {
    var r = En(), o = uR(), a = e && !e.nodeType && e, n = a && !0 && t && !t.nodeType && t, i = n && n.exports === a, l = i ? r.Buffer : void 0, s = l ? l.isBuffer : void 0, f = s || o;
    t.exports = f;
  })(ln, ln.exports)), ln.exports;
}
var va, Ap;
function cR() {
  if (Ap) return va;
  Ap = 1;
  var t = zi(), e = Am(), r = vn(), o = "[object Arguments]", a = "[object Array]", n = "[object Boolean]", i = "[object Date]", l = "[object Error]", s = "[object Function]", f = "[object Map]", d = "[object Number]", y = "[object Object]", R = "[object RegExp]", w = "[object Set]", I = "[object String]", A = "[object WeakMap]", u = "[object ArrayBuffer]", g = "[object DataView]", p = "[object Float32Array]", b = "[object Float64Array]", C = "[object Int8Array]", N = "[object Int16Array]", W = "[object Int32Array]", k = "[object Uint8Array]", L = "[object Uint8ClampedArray]", U = "[object Uint16Array]", x = "[object Uint32Array]", D = {};
  D[p] = D[b] = D[C] = D[N] = D[W] = D[k] = D[L] = D[U] = D[x] = !0, D[o] = D[a] = D[u] = D[n] = D[g] = D[i] = D[l] = D[s] = D[f] = D[d] = D[y] = D[R] = D[w] = D[I] = D[A] = !1;
  function F(T) {
    return r(T) && e(T.length) && !!D[t(T)];
  }
  return va = F, va;
}
var Ra, Tp;
function Dm() {
  if (Tp) return Ra;
  Tp = 1;
  function t(e) {
    return function(r) {
      return e(r);
    };
  }
  return Ra = t, Ra;
}
var un = { exports: {} };
un.exports;
var xp;
function dR() {
  return xp || (xp = 1, (function(t, e) {
    var r = vm(), o = e && !e.nodeType && e, a = o && !0 && t && !t.nodeType && t, n = a && a.exports === o, i = n && r.process, l = (function() {
      try {
        var s = a && a.require && a.require("util").types;
        return s || i && i.binding && i.binding("util");
      } catch {
      }
    })();
    t.exports = l;
  })(un, un.exports)), un.exports;
}
var Aa, Ip;
function hR() {
  if (Ip) return Aa;
  Ip = 1;
  var t = cR(), e = Dm(), r = dR(), o = r && r.isTypedArray, a = o ? e(o) : t;
  return Aa = a, Aa;
}
var Ta, Dp;
function pR() {
  if (Dp) return Ta;
  Dp = 1;
  var t = aR(), e = xm(), r = Im(), o = fR(), a = Tm(), n = hR(), i = Object.prototype, l = i.hasOwnProperty;
  function s(f, d) {
    var y = r(f), R = !y && e(f), w = !y && !R && o(f), I = !y && !R && !w && n(f), A = y || R || w || I, u = A ? t(f.length, String) : [], g = u.length;
    for (var p in f)
      (d || l.call(f, p)) && !(A && // Safari 9 has enumerable `arguments.length` in strict mode.
      (p == "length" || // Node.js 0.10 has enumerable non-index properties on buffers.
      w && (p == "offset" || p == "parent") || // PhantomJS 2 has enumerable non-index properties on typed arrays.
      I && (p == "buffer" || p == "byteLength" || p == "byteOffset") || // Skip index properties.
      a(p, g))) && u.push(p);
    return u;
  }
  return Ta = s, Ta;
}
var xa, Op;
function gR() {
  if (Op) return xa;
  Op = 1;
  var t = Object.prototype;
  function e(r) {
    var o = r && r.constructor, a = typeof o == "function" && o.prototype || t;
    return r === a;
  }
  return xa = e, xa;
}
var Ia, Np;
function yR() {
  if (Np) return Ia;
  Np = 1;
  function t(e) {
    var r = [];
    if (e != null)
      for (var o in Object(e))
        r.push(o);
    return r;
  }
  return Ia = t, Ia;
}
var Da, Lp;
function bR() {
  if (Lp) return Da;
  Lp = 1;
  var t = Vi(), e = gR(), r = yR(), o = Object.prototype, a = o.hasOwnProperty;
  function n(i) {
    if (!t(i))
      return r(i);
    var l = e(i), s = [];
    for (var f in i)
      f == "constructor" && (l || !a.call(i, f)) || s.push(f);
    return s;
  }
  return Da = n, Da;
}
var Oa, Pp;
function _R() {
  if (Pp) return Oa;
  Pp = 1;
  var t = pR(), e = bR(), r = Oc();
  function o(a) {
    return r(a) ? t(a, !0) : e(a);
  }
  return Oa = o, Oa;
}
var Na, Cp;
function wR() {
  if (Cp) return Na;
  Cp = 1;
  var t = Ic(), e = Dc(), r = oR(), o = _R(), a = Object.prototype, n = a.hasOwnProperty, i = t(function(l, s) {
    l = Object(l);
    var f = -1, d = s.length, y = d > 2 ? s[2] : void 0;
    for (y && r(s[0], s[1], y) && (d = 1); ++f < d; )
      for (var R = s[f], w = o(R), I = -1, A = w.length; ++I < A; ) {
        var u = w[I], g = l[u];
        (g === void 0 || e(g, a[u]) && !n.call(l, u)) && (l[u] = R[u]);
      }
    return l;
  });
  return Na = i, Na;
}
var ni = { exports: {} }, La = { exports: {} }, Pa, Mp;
function ze() {
  if (Mp) return Pa;
  Mp = 1;
  class t extends Error {
    constructor(r) {
      if (!Array.isArray(r))
        throw new TypeError(`Expected input to be an Array, got ${typeof r}`);
      let o = "";
      for (let a = 0; a < r.length; a++)
        o += `    ${r[a].stack}
`;
      super(o), this.name = "AggregateError", this.errors = r;
    }
  }
  return Pa = {
    AggregateError: t,
    ArrayIsArray(e) {
      return Array.isArray(e);
    },
    ArrayPrototypeIncludes(e, r) {
      return e.includes(r);
    },
    ArrayPrototypeIndexOf(e, r) {
      return e.indexOf(r);
    },
    ArrayPrototypeJoin(e, r) {
      return e.join(r);
    },
    ArrayPrototypeMap(e, r) {
      return e.map(r);
    },
    ArrayPrototypePop(e, r) {
      return e.pop(r);
    },
    ArrayPrototypePush(e, r) {
      return e.push(r);
    },
    ArrayPrototypeSlice(e, r, o) {
      return e.slice(r, o);
    },
    Error,
    FunctionPrototypeCall(e, r, ...o) {
      return e.call(r, ...o);
    },
    FunctionPrototypeSymbolHasInstance(e, r) {
      return Function.prototype[Symbol.hasInstance].call(e, r);
    },
    MathFloor: Math.floor,
    Number,
    NumberIsInteger: Number.isInteger,
    NumberIsNaN: Number.isNaN,
    NumberMAX_SAFE_INTEGER: Number.MAX_SAFE_INTEGER,
    NumberMIN_SAFE_INTEGER: Number.MIN_SAFE_INTEGER,
    NumberParseInt: Number.parseInt,
    ObjectDefineProperties(e, r) {
      return Object.defineProperties(e, r);
    },
    ObjectDefineProperty(e, r, o) {
      return Object.defineProperty(e, r, o);
    },
    ObjectGetOwnPropertyDescriptor(e, r) {
      return Object.getOwnPropertyDescriptor(e, r);
    },
    ObjectKeys(e) {
      return Object.keys(e);
    },
    ObjectSetPrototypeOf(e, r) {
      return Object.setPrototypeOf(e, r);
    },
    Promise,
    PromisePrototypeCatch(e, r) {
      return e.catch(r);
    },
    PromisePrototypeThen(e, r, o) {
      return e.then(r, o);
    },
    PromiseReject(e) {
      return Promise.reject(e);
    },
    PromiseResolve(e) {
      return Promise.resolve(e);
    },
    ReflectApply: Reflect.apply,
    RegExpPrototypeTest(e, r) {
      return e.test(r);
    },
    SafeSet: Set,
    String,
    StringPrototypeSlice(e, r, o) {
      return e.slice(r, o);
    },
    StringPrototypeToLowerCase(e) {
      return e.toLowerCase();
    },
    StringPrototypeToUpperCase(e) {
      return e.toUpperCase();
    },
    StringPrototypeTrim(e) {
      return e.trim();
    },
    Symbol,
    SymbolFor: Symbol.for,
    SymbolAsyncIterator: Symbol.asyncIterator,
    SymbolHasInstance: Symbol.hasInstance,
    SymbolIterator: Symbol.iterator,
    SymbolDispose: Symbol.dispose || /* @__PURE__ */ Symbol("Symbol.dispose"),
    SymbolAsyncDispose: Symbol.asyncDispose || /* @__PURE__ */ Symbol("Symbol.asyncDispose"),
    TypedArrayPrototypeSet(e, r, o) {
      return e.set(r, o);
    },
    Boolean,
    Uint8Array
  }, Pa;
}
var Ca = { exports: {} }, Ma, kp;
function Om() {
  return kp || (kp = 1, Ma = {
    format(t, ...e) {
      return t.replace(/%([sdifj])/g, function(...[r, o]) {
        const a = e.shift();
        return o === "f" ? a.toFixed(6) : o === "j" ? JSON.stringify(a) : o === "s" && typeof a == "object" ? `${a.constructor !== Object ? a.constructor.name : ""} {}`.trim() : a.toString();
      });
    },
    inspect(t) {
      switch (typeof t) {
        case "string":
          if (t.includes("'"))
            if (t.includes('"')) {
              if (!t.includes("`") && !t.includes("${"))
                return `\`${t}\``;
            } else return `"${t}"`;
          return `'${t}'`;
        case "number":
          return isNaN(t) ? "NaN" : Object.is(t, -0) ? String(t) : t;
        case "bigint":
          return `${String(t)}n`;
        case "boolean":
        case "undefined":
          return String(t);
        case "object":
          return "{}";
      }
    }
  }), Ma;
}
var ka, Fp;
function ut() {
  if (Fp) return ka;
  Fp = 1;
  const { format: t, inspect: e } = Om(), { AggregateError: r } = ze(), o = globalThis.AggregateError || r, a = /* @__PURE__ */ Symbol("kIsNodeError"), n = [
    "string",
    "function",
    "number",
    "object",
    // Accept 'Function' and 'Object' as alternative to the lower cased version.
    "Function",
    "Object",
    "boolean",
    "bigint",
    "symbol"
  ], i = /^([A-Z][a-z0-9]*)+$/, l = "__node_internal_", s = {};
  function f(u, g) {
    if (!u)
      throw new s.ERR_INTERNAL_ASSERTION(g);
  }
  function d(u) {
    let g = "", p = u.length;
    const b = u[0] === "-" ? 1 : 0;
    for (; p >= b + 4; p -= 3)
      g = `_${u.slice(p - 3, p)}${g}`;
    return `${u.slice(0, p)}${g}`;
  }
  function y(u, g, p) {
    if (typeof g == "function")
      return f(
        g.length <= p.length,
        // Default options do not count.
        `Code: ${u}; The provided arguments length (${p.length}) does not match the required ones (${g.length}).`
      ), g(...p);
    const b = (g.match(/%[dfijoOs]/g) || []).length;
    return f(
      b === p.length,
      `Code: ${u}; The provided arguments length (${p.length}) does not match the required ones (${b}).`
    ), p.length === 0 ? g : t(g, ...p);
  }
  function R(u, g, p) {
    p || (p = Error);
    class b extends p {
      constructor(...N) {
        super(y(u, g, N));
      }
      toString() {
        return `${this.name} [${u}]: ${this.message}`;
      }
    }
    Object.defineProperties(b.prototype, {
      name: {
        value: p.name,
        writable: !0,
        enumerable: !1,
        configurable: !0
      },
      toString: {
        value() {
          return `${this.name} [${u}]: ${this.message}`;
        },
        writable: !0,
        enumerable: !1,
        configurable: !0
      }
    }), b.prototype.code = u, b.prototype[a] = !0, s[u] = b;
  }
  function w(u) {
    const g = l + u.name;
    return Object.defineProperty(u, "name", {
      value: g
    }), u;
  }
  function I(u, g) {
    if (u && g && u !== g) {
      if (Array.isArray(g.errors))
        return g.errors.push(u), g;
      const p = new o([g, u], g.message);
      return p.code = g.code, p;
    }
    return u || g;
  }
  class A extends Error {
    constructor(g = "The operation was aborted", p = void 0) {
      if (p !== void 0 && typeof p != "object")
        throw new s.ERR_INVALID_ARG_TYPE("options", "Object", p);
      super(g, p), this.code = "ABORT_ERR", this.name = "AbortError";
    }
  }
  return R("ERR_ASSERTION", "%s", Error), R(
    "ERR_INVALID_ARG_TYPE",
    (u, g, p) => {
      f(typeof u == "string", "'name' must be a string"), Array.isArray(g) || (g = [g]);
      let b = "The ";
      u.endsWith(" argument") ? b += `${u} ` : b += `"${u}" ${u.includes(".") ? "property" : "argument"} `, b += "must be ";
      const C = [], N = [], W = [];
      for (const L of g)
        f(typeof L == "string", "All expected entries have to be of type string"), n.includes(L) ? C.push(L.toLowerCase()) : i.test(L) ? N.push(L) : (f(L !== "object", 'The value "object" should be written as "Object"'), W.push(L));
      if (N.length > 0) {
        const L = C.indexOf("object");
        L !== -1 && (C.splice(C, L, 1), N.push("Object"));
      }
      if (C.length > 0) {
        switch (C.length) {
          case 1:
            b += `of type ${C[0]}`;
            break;
          case 2:
            b += `one of type ${C[0]} or ${C[1]}`;
            break;
          default: {
            const L = C.pop();
            b += `one of type ${C.join(", ")}, or ${L}`;
          }
        }
        (N.length > 0 || W.length > 0) && (b += " or ");
      }
      if (N.length > 0) {
        switch (N.length) {
          case 1:
            b += `an instance of ${N[0]}`;
            break;
          case 2:
            b += `an instance of ${N[0]} or ${N[1]}`;
            break;
          default: {
            const L = N.pop();
            b += `an instance of ${N.join(", ")}, or ${L}`;
          }
        }
        W.length > 0 && (b += " or ");
      }
      switch (W.length) {
        case 0:
          break;
        case 1:
          W[0].toLowerCase() !== W[0] && (b += "an "), b += `${W[0]}`;
          break;
        case 2:
          b += `one of ${W[0]} or ${W[1]}`;
          break;
        default: {
          const L = W.pop();
          b += `one of ${W.join(", ")}, or ${L}`;
        }
      }
      if (p == null)
        b += `. Received ${p}`;
      else if (typeof p == "function" && p.name)
        b += `. Received function ${p.name}`;
      else if (typeof p == "object") {
        var k;
        if ((k = p.constructor) !== null && k !== void 0 && k.name)
          b += `. Received an instance of ${p.constructor.name}`;
        else {
          const L = e(p, {
            depth: -1
          });
          b += `. Received ${L}`;
        }
      } else {
        let L = e(p, {
          colors: !1
        });
        L.length > 25 && (L = `${L.slice(0, 25)}...`), b += `. Received type ${typeof p} (${L})`;
      }
      return b;
    },
    TypeError
  ), R(
    "ERR_INVALID_ARG_VALUE",
    (u, g, p = "is invalid") => {
      let b = e(g);
      return b.length > 128 && (b = b.slice(0, 128) + "..."), `The ${u.includes(".") ? "property" : "argument"} '${u}' ${p}. Received ${b}`;
    },
    TypeError
  ), R(
    "ERR_INVALID_RETURN_VALUE",
    (u, g, p) => {
      var b;
      const C = p != null && (b = p.constructor) !== null && b !== void 0 && b.name ? `instance of ${p.constructor.name}` : `type ${typeof p}`;
      return `Expected ${u} to be returned from the "${g}" function but got ${C}.`;
    },
    TypeError
  ), R(
    "ERR_MISSING_ARGS",
    (...u) => {
      f(u.length > 0, "At least one arg needs to be specified");
      let g;
      const p = u.length;
      switch (u = (Array.isArray(u) ? u : [u]).map((b) => `"${b}"`).join(" or "), p) {
        case 1:
          g += `The ${u[0]} argument`;
          break;
        case 2:
          g += `The ${u[0]} and ${u[1]} arguments`;
          break;
        default:
          {
            const b = u.pop();
            g += `The ${u.join(", ")}, and ${b} arguments`;
          }
          break;
      }
      return `${g} must be specified`;
    },
    TypeError
  ), R(
    "ERR_OUT_OF_RANGE",
    (u, g, p) => {
      f(g, 'Missing "range" argument');
      let b;
      if (Number.isInteger(p) && Math.abs(p) > 2 ** 32)
        b = d(String(p));
      else if (typeof p == "bigint") {
        b = String(p);
        const C = BigInt(2) ** BigInt(32);
        (p > C || p < -C) && (b = d(b)), b += "n";
      } else
        b = e(p);
      return `The value of "${u}" is out of range. It must be ${g}. Received ${b}`;
    },
    RangeError
  ), R("ERR_MULTIPLE_CALLBACK", "Callback called multiple times", Error), R("ERR_METHOD_NOT_IMPLEMENTED", "The %s method is not implemented", Error), R("ERR_STREAM_ALREADY_FINISHED", "Cannot call %s after a stream was finished", Error), R("ERR_STREAM_CANNOT_PIPE", "Cannot pipe, not readable", Error), R("ERR_STREAM_DESTROYED", "Cannot call %s after a stream was destroyed", Error), R("ERR_STREAM_NULL_VALUES", "May not write null values to stream", TypeError), R("ERR_STREAM_PREMATURE_CLOSE", "Premature close", Error), R("ERR_STREAM_PUSH_AFTER_EOF", "stream.push() after EOF", Error), R("ERR_STREAM_UNSHIFT_AFTER_END_EVENT", "stream.unshift() after end event", Error), R("ERR_STREAM_WRITE_AFTER_END", "write after end", Error), R("ERR_UNKNOWN_ENCODING", "Unknown encoding: %s", TypeError), ka = {
    AbortError: A,
    aggregateTwoErrors: w(I),
    hideStackFrames: w,
    codes: s
  }, ka;
}
const Nm = /* @__PURE__ */ new WeakMap(), uc = /* @__PURE__ */ new WeakMap();
function Be(t) {
  const e = Nm.get(t);
  return console.assert(
    e != null,
    "'this' is expected an Event object, but got",
    t
  ), e;
}
function jp(t) {
  if (t.passiveListener != null) {
    typeof console < "u" && typeof console.error == "function" && console.error(
      "Unable to preventDefault inside passive event listener invocation.",
      t.passiveListener
    );
    return;
  }
  t.event.cancelable && (t.canceled = !0, typeof t.event.preventDefault == "function" && t.event.preventDefault());
}
function kr(t, e) {
  Nm.set(this, {
    eventTarget: t,
    event: e,
    eventPhase: 2,
    currentTarget: t,
    canceled: !1,
    stopped: !1,
    immediateStopped: !1,
    passiveListener: null,
    timeStamp: e.timeStamp || Date.now()
  }), Object.defineProperty(this, "isTrusted", { value: !1, enumerable: !0 });
  const r = Object.keys(e);
  for (let o = 0; o < r.length; ++o) {
    const a = r[o];
    a in this || Object.defineProperty(this, a, Lm(a));
  }
}
kr.prototype = {
  /**
   * The type of this event.
   * @type {string}
   */
  get type() {
    return Be(this).event.type;
  },
  /**
   * The target of this event.
   * @type {EventTarget}
   */
  get target() {
    return Be(this).eventTarget;
  },
  /**
   * The target of this event.
   * @type {EventTarget}
   */
  get currentTarget() {
    return Be(this).currentTarget;
  },
  /**
   * @returns {EventTarget[]} The composed path of this event.
   */
  composedPath() {
    const t = Be(this).currentTarget;
    return t == null ? [] : [t];
  },
  /**
   * Constant of NONE.
   * @type {number}
   */
  get NONE() {
    return 0;
  },
  /**
   * Constant of CAPTURING_PHASE.
   * @type {number}
   */
  get CAPTURING_PHASE() {
    return 1;
  },
  /**
   * Constant of AT_TARGET.
   * @type {number}
   */
  get AT_TARGET() {
    return 2;
  },
  /**
   * Constant of BUBBLING_PHASE.
   * @type {number}
   */
  get BUBBLING_PHASE() {
    return 3;
  },
  /**
   * The target of this event.
   * @type {number}
   */
  get eventPhase() {
    return Be(this).eventPhase;
  },
  /**
   * Stop event bubbling.
   * @returns {void}
   */
  stopPropagation() {
    const t = Be(this);
    t.stopped = !0, typeof t.event.stopPropagation == "function" && t.event.stopPropagation();
  },
  /**
   * Stop event bubbling.
   * @returns {void}
   */
  stopImmediatePropagation() {
    const t = Be(this);
    t.stopped = !0, t.immediateStopped = !0, typeof t.event.stopImmediatePropagation == "function" && t.event.stopImmediatePropagation();
  },
  /**
   * The flag to be bubbling.
   * @type {boolean}
   */
  get bubbles() {
    return !!Be(this).event.bubbles;
  },
  /**
   * The flag to be cancelable.
   * @type {boolean}
   */
  get cancelable() {
    return !!Be(this).event.cancelable;
  },
  /**
   * Cancel this event.
   * @returns {void}
   */
  preventDefault() {
    jp(Be(this));
  },
  /**
   * The flag to indicate cancellation state.
   * @type {boolean}
   */
  get defaultPrevented() {
    return Be(this).canceled;
  },
  /**
   * The flag to be composed.
   * @type {boolean}
   */
  get composed() {
    return !!Be(this).event.composed;
  },
  /**
   * The unix time of this event.
   * @type {number}
   */
  get timeStamp() {
    return Be(this).timeStamp;
  },
  /**
   * The target of this event.
   * @type {EventTarget}
   * @deprecated
   */
  get srcElement() {
    return Be(this).eventTarget;
  },
  /**
   * The flag to stop event bubbling.
   * @type {boolean}
   * @deprecated
   */
  get cancelBubble() {
    return Be(this).stopped;
  },
  set cancelBubble(t) {
    if (!t)
      return;
    const e = Be(this);
    e.stopped = !0, typeof e.event.cancelBubble == "boolean" && (e.event.cancelBubble = !0);
  },
  /**
   * The flag to indicate cancellation state.
   * @type {boolean}
   * @deprecated
   */
  get returnValue() {
    return !Be(this).canceled;
  },
  set returnValue(t) {
    t || jp(Be(this));
  },
  /**
   * Initialize this event object. But do nothing under event dispatching.
   * @param {string} type The event type.
   * @param {boolean} [bubbles=false] The flag to be possible to bubble up.
   * @param {boolean} [cancelable=false] The flag to be possible to cancel.
   * @deprecated
   */
  initEvent() {
  }
};
Object.defineProperty(kr.prototype, "constructor", {
  value: kr,
  configurable: !0,
  writable: !0
});
typeof window < "u" && typeof window.Event < "u" && (Object.setPrototypeOf(kr.prototype, window.Event.prototype), uc.set(window.Event.prototype, kr));
function Lm(t) {
  return {
    get() {
      return Be(this).event[t];
    },
    set(e) {
      Be(this).event[t] = e;
    },
    configurable: !0,
    enumerable: !0
  };
}
function mR(t) {
  return {
    value() {
      const e = Be(this).event;
      return e[t].apply(e, arguments);
    },
    configurable: !0,
    enumerable: !0
  };
}
function SR(t, e) {
  const r = Object.keys(e);
  if (r.length === 0)
    return t;
  function o(a, n) {
    t.call(this, a, n);
  }
  o.prototype = Object.create(t.prototype, {
    constructor: { value: o, configurable: !0, writable: !0 }
  });
  for (let a = 0; a < r.length; ++a) {
    const n = r[a];
    if (!(n in t.prototype)) {
      const l = typeof Object.getOwnPropertyDescriptor(e, n).value == "function";
      Object.defineProperty(
        o.prototype,
        n,
        l ? mR(n) : Lm(n)
      );
    }
  }
  return o;
}
function Pm(t) {
  if (t == null || t === Object.prototype)
    return kr;
  let e = uc.get(t);
  return e == null && (e = SR(Pm(Object.getPrototypeOf(t)), t), uc.set(t, e)), e;
}
function ER(t, e) {
  const r = Pm(Object.getPrototypeOf(e));
  return new r(t, e);
}
function vR(t) {
  return Be(t).immediateStopped;
}
function RR(t, e) {
  Be(t).eventPhase = e;
}
function AR(t, e) {
  Be(t).currentTarget = e;
}
function $p(t, e) {
  Be(t).passiveListener = e;
}
const Cm = /* @__PURE__ */ new WeakMap(), Wp = 1, qp = 2, ii = 3;
function si(t) {
  return t !== null && typeof t == "object";
}
function fn(t) {
  const e = Cm.get(t);
  if (e == null)
    throw new TypeError(
      "'this' is expected an EventTarget object, but got another value."
    );
  return e;
}
function TR(t) {
  return {
    get() {
      let r = fn(this).get(t);
      for (; r != null; ) {
        if (r.listenerType === ii)
          return r.listener;
        r = r.next;
      }
      return null;
    },
    set(e) {
      typeof e != "function" && !si(e) && (e = null);
      const r = fn(this);
      let o = null, a = r.get(t);
      for (; a != null; )
        a.listenerType === ii ? o !== null ? o.next = a.next : a.next !== null ? r.set(t, a.next) : r.delete(t) : o = a, a = a.next;
      if (e !== null) {
        const n = {
          listener: e,
          listenerType: ii,
          passive: !1,
          once: !1,
          next: null
        };
        o === null ? r.set(t, n) : o.next = n;
      }
    },
    configurable: !0,
    enumerable: !0
  };
}
function Mm(t, e) {
  Object.defineProperty(
    t,
    `on${e}`,
    TR(e)
  );
}
function Bp(t) {
  function e() {
    Yt.call(this);
  }
  e.prototype = Object.create(Yt.prototype, {
    constructor: {
      value: e,
      configurable: !0,
      writable: !0
    }
  });
  for (let r = 0; r < t.length; ++r)
    Mm(e.prototype, t[r]);
  return e;
}
function Yt() {
  if (this instanceof Yt) {
    Cm.set(this, /* @__PURE__ */ new Map());
    return;
  }
  if (arguments.length === 1 && Array.isArray(arguments[0]))
    return Bp(arguments[0]);
  if (arguments.length > 0) {
    const t = new Array(arguments.length);
    for (let e = 0; e < arguments.length; ++e)
      t[e] = arguments[e];
    return Bp(t);
  }
  throw new TypeError("Cannot call a class as a function");
}
Yt.prototype = {
  /**
   * Add a given listener to this event target.
   * @param {string} eventName The event name to add.
   * @param {Function} listener The listener to add.
   * @param {boolean|{capture?:boolean,passive?:boolean,once?:boolean}} [options] The options for this listener.
   * @returns {void}
   */
  addEventListener(t, e, r) {
    if (e == null)
      return;
    if (typeof e != "function" && !si(e))
      throw new TypeError("'listener' should be a function or an object.");
    const o = fn(this), a = si(r), i = (a ? !!r.capture : !!r) ? Wp : qp, l = {
      listener: e,
      listenerType: i,
      passive: a && !!r.passive,
      once: a && !!r.once,
      next: null
    };
    let s = o.get(t);
    if (s === void 0) {
      o.set(t, l);
      return;
    }
    let f = null;
    for (; s != null; ) {
      if (s.listener === e && s.listenerType === i)
        return;
      f = s, s = s.next;
    }
    f.next = l;
  },
  /**
   * Remove a given listener from this event target.
   * @param {string} eventName The event name to remove.
   * @param {Function} listener The listener to remove.
   * @param {boolean|{capture?:boolean,passive?:boolean,once?:boolean}} [options] The options for this listener.
   * @returns {void}
   */
  removeEventListener(t, e, r) {
    if (e == null)
      return;
    const o = fn(this), n = (si(r) ? !!r.capture : !!r) ? Wp : qp;
    let i = null, l = o.get(t);
    for (; l != null; ) {
      if (l.listener === e && l.listenerType === n) {
        i !== null ? i.next = l.next : l.next !== null ? o.set(t, l.next) : o.delete(t);
        return;
      }
      i = l, l = l.next;
    }
  },
  /**
   * Dispatch a given event.
   * @param {Event|{type:string}} event The event to dispatch.
   * @returns {boolean} `false` if canceled.
   */
  dispatchEvent(t) {
    if (t == null || typeof t.type != "string")
      throw new TypeError('"event.type" should be a string.');
    const e = fn(this), r = t.type;
    let o = e.get(r);
    if (o == null)
      return !0;
    const a = ER(this, t);
    let n = null;
    for (; o != null; ) {
      if (o.once ? n !== null ? n.next = o.next : o.next !== null ? e.set(r, o.next) : e.delete(r) : n = o, $p(
        a,
        o.passive ? o.listener : null
      ), typeof o.listener == "function")
        try {
          o.listener.call(this, a);
        } catch (i) {
          typeof console < "u" && typeof console.error == "function" && console.error(i);
        }
      else o.listenerType !== ii && typeof o.listener.handleEvent == "function" && o.listener.handleEvent(a);
      if (vR(a))
        break;
      o = o.next;
    }
    return $p(a, null), RR(a, 0), AR(a, null), !a.defaultPrevented;
  }
};
Object.defineProperty(Yt.prototype, "constructor", {
  value: Yt,
  configurable: !0,
  writable: !0
});
typeof window < "u" && typeof window.EventTarget < "u" && Object.setPrototypeOf(Yt.prototype, window.EventTarget.prototype);
class Rn extends Yt {
  /**
   * AbortSignal cannot be constructed directly.
   */
  constructor() {
    throw super(), new TypeError("AbortSignal cannot be constructed directly");
  }
  /**
   * Returns `true` if this `AbortSignal`'s `AbortController` has signaled to abort, and `false` otherwise.
   */
  get aborted() {
    const e = xi.get(this);
    if (typeof e != "boolean")
      throw new TypeError(`Expected 'this' to be an 'AbortSignal' object, but got ${this === null ? "null" : typeof this}`);
    return e;
  }
}
Mm(Rn.prototype, "abort");
function xR() {
  const t = Object.create(Rn.prototype);
  return Yt.call(t), xi.set(t, !1), t;
}
function IR(t) {
  xi.get(t) === !1 && (xi.set(t, !0), t.dispatchEvent({ type: "abort" }));
}
const xi = /* @__PURE__ */ new WeakMap();
Object.defineProperties(Rn.prototype, {
  aborted: { enumerable: !0 }
});
typeof Symbol == "function" && typeof Symbol.toStringTag == "symbol" && Object.defineProperty(Rn.prototype, Symbol.toStringTag, {
  configurable: !0,
  value: "AbortSignal"
});
class Ii {
  /**
   * Initialize this controller.
   */
  constructor() {
    km.set(this, xR());
  }
  /**
   * Returns the `AbortSignal` object associated with this object.
   */
  get signal() {
    return Up(this);
  }
  /**
   * Abort and signal to any observers that the associated activity is to be aborted.
   */
  abort() {
    IR(Up(this));
  }
}
const km = /* @__PURE__ */ new WeakMap();
function Up(t) {
  const e = km.get(t);
  if (e == null)
    throw new TypeError(`Expected 'this' to be an 'AbortController' object, but got ${t === null ? "null" : typeof t}`);
  return e;
}
Object.defineProperties(Ii.prototype, {
  signal: { enumerable: !0 },
  abort: { enumerable: !0 }
});
typeof Symbol == "function" && typeof Symbol.toStringTag == "symbol" && Object.defineProperty(Ii.prototype, Symbol.toStringTag, {
  configurable: !0,
  value: "AbortController"
});
const DR = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  AbortController: Ii,
  AbortSignal: Rn,
  default: Ii
}, Symbol.toStringTag, { value: "Module" })), et = /* @__PURE__ */ fw(DR);
var Gp;
function pt() {
  return Gp || (Gp = 1, (function(t) {
    const e = Fe, { format: r, inspect: o } = Om(), {
      codes: { ERR_INVALID_ARG_TYPE: a }
    } = ut(), { kResistStopPropagation: n, AggregateError: i, SymbolDispose: l } = ze(), s = globalThis.AbortSignal || et.AbortSignal, f = globalThis.AbortController || et.AbortController, d = Object.getPrototypeOf(async function() {
    }).constructor, y = globalThis.Blob || e.Blob, R = typeof y < "u" ? function(u) {
      return u instanceof y;
    } : function(u) {
      return !1;
    }, w = (A, u) => {
      if (A !== void 0 && (A === null || typeof A != "object" || !("aborted" in A)))
        throw new a(u, "AbortSignal", A);
    }, I = (A, u) => {
      if (typeof A != "function")
        throw new a(u, "Function", A);
    };
    t.exports = {
      AggregateError: i,
      kEmptyObject: Object.freeze({}),
      once(A) {
        let u = !1;
        return function(...g) {
          u || (u = !0, A.apply(this, g));
        };
      },
      createDeferredPromise: function() {
        let A, u;
        return {
          promise: new Promise((p, b) => {
            A = p, u = b;
          }),
          resolve: A,
          reject: u
        };
      },
      promisify(A) {
        return new Promise((u, g) => {
          A((p, ...b) => p ? g(p) : u(...b));
        });
      },
      debuglog() {
        return function() {
        };
      },
      format: r,
      inspect: o,
      types: {
        isAsyncFunction(A) {
          return A instanceof d;
        },
        isArrayBufferView(A) {
          return ArrayBuffer.isView(A);
        }
      },
      isBlob: R,
      deprecate(A, u) {
        return A;
      },
      addAbortListener: Ke.addAbortListener || function(u, g) {
        if (u === void 0)
          throw new a("signal", "AbortSignal", u);
        w(u, "signal"), I(g, "listener");
        let p;
        return u.aborted ? queueMicrotask(() => g()) : (u.addEventListener("abort", g, {
          __proto__: null,
          once: !0,
          [n]: !0
        }), p = () => {
          u.removeEventListener("abort", g);
        }), {
          __proto__: null,
          [l]() {
            var b;
            (b = p) === null || b === void 0 || b();
          }
        };
      },
      AbortSignalAny: s.any || function(u) {
        if (u.length === 1)
          return u[0];
        const g = new f(), p = () => g.abort();
        return u.forEach((b) => {
          w(b, "signals"), b.addEventListener("abort", p, {
            once: !0
          });
        }), g.signal.addEventListener(
          "abort",
          () => {
            u.forEach((b) => b.removeEventListener("abort", p));
          },
          {
            once: !0
          }
        ), g.signal;
      }
    }, t.exports.promisify.custom = /* @__PURE__ */ Symbol.for("nodejs.util.promisify.custom");
  })(Ca)), Ca.exports;
}
var Gn = {}, Fa, Hp;
function An() {
  if (Hp) return Fa;
  Hp = 1;
  const {
    ArrayIsArray: t,
    ArrayPrototypeIncludes: e,
    ArrayPrototypeJoin: r,
    ArrayPrototypeMap: o,
    NumberIsInteger: a,
    NumberIsNaN: n,
    NumberMAX_SAFE_INTEGER: i,
    NumberMIN_SAFE_INTEGER: l,
    NumberParseInt: s,
    ObjectPrototypeHasOwnProperty: f,
    RegExpPrototypeExec: d,
    String: y,
    StringPrototypeToUpperCase: R,
    StringPrototypeTrim: w
  } = ze(), {
    hideStackFrames: I,
    codes: { ERR_SOCKET_BAD_PORT: A, ERR_INVALID_ARG_TYPE: u, ERR_INVALID_ARG_VALUE: g, ERR_OUT_OF_RANGE: p, ERR_UNKNOWN_SIGNAL: b }
  } = ut(), { normalizeEncoding: C } = pt(), { isAsyncFunction: N, isArrayBufferView: W } = pt().types, k = {};
  function L(O) {
    return O === (O | 0);
  }
  function U(O) {
    return O === O >>> 0;
  }
  const x = /^[0-7]+$/, D = "must be a 32-bit unsigned integer or an octal string";
  function F(O, H, te) {
    if (typeof O > "u" && (O = te), typeof O == "string") {
      if (d(x, O) === null)
        throw new g(H, O, D);
      O = s(O, 8);
    }
    return re(O, H), O;
  }
  const T = I((O, H, te = l, z = i) => {
    if (typeof O != "number") throw new u(H, "number", O);
    if (!a(O)) throw new p(H, "an integer", O);
    if (O < te || O > z) throw new p(H, `>= ${te} && <= ${z}`, O);
  }), J = I((O, H, te = -2147483648, z = 2147483647) => {
    if (typeof O != "number")
      throw new u(H, "number", O);
    if (!a(O))
      throw new p(H, "an integer", O);
    if (O < te || O > z)
      throw new p(H, `>= ${te} && <= ${z}`, O);
  }), re = I((O, H, te = !1) => {
    if (typeof O != "number")
      throw new u(H, "number", O);
    if (!a(O))
      throw new p(H, "an integer", O);
    const z = te ? 1 : 0, de = 4294967295;
    if (O < z || O > de)
      throw new p(H, `>= ${z} && <= ${de}`, O);
  });
  function se(O, H) {
    if (typeof O != "string") throw new u(H, "string", O);
  }
  function ne(O, H, te = void 0, z) {
    if (typeof O != "number") throw new u(H, "number", O);
    if (te != null && O < te || z != null && O > z || (te != null || z != null) && n(O))
      throw new p(
        H,
        `${te != null ? `>= ${te}` : ""}${te != null && z != null ? " && " : ""}${z != null ? `<= ${z}` : ""}`,
        O
      );
  }
  const M = I((O, H, te) => {
    if (!e(te, O)) {
      const de = "must be one of: " + r(
        o(te, (pe) => typeof pe == "string" ? `'${pe}'` : y(pe)),
        ", "
      );
      throw new g(H, O, de);
    }
  });
  function Q(O, H) {
    if (typeof O != "boolean") throw new u(H, "boolean", O);
  }
  function v(O, H, te) {
    return O == null || !f(O, H) ? te : O[H];
  }
  const $ = I((O, H, te = null) => {
    const z = v(te, "allowArray", !1), de = v(te, "allowFunction", !1);
    if (!v(te, "nullable", !1) && O === null || !z && t(O) || typeof O != "object" && (!de || typeof O != "function"))
      throw new u(H, "Object", O);
  }), ee = I((O, H) => {
    if (O != null && typeof O != "object" && typeof O != "function")
      throw new u(H, "a dictionary", O);
  }), B = I((O, H, te = 0) => {
    if (!t(O))
      throw new u(H, "Array", O);
    if (O.length < te) {
      const z = `must be longer than ${te}`;
      throw new g(H, O, z);
    }
  });
  function le(O, H) {
    B(O, H);
    for (let te = 0; te < O.length; te++)
      se(O[te], `${H}[${te}]`);
  }
  function ae(O, H) {
    B(O, H);
    for (let te = 0; te < O.length; te++)
      Q(O[te], `${H}[${te}]`);
  }
  function ce(O, H) {
    B(O, H);
    for (let te = 0; te < O.length; te++) {
      const z = O[te], de = `${H}[${te}]`;
      if (z == null)
        throw new u(de, "AbortSignal", z);
      Z(z, de);
    }
  }
  function Y(O, H = "signal") {
    if (se(O, H), k[O] === void 0)
      throw k[R(O)] !== void 0 ? new b(O + " (signals must use all capital letters)") : new b(O);
  }
  const P = I((O, H = "buffer") => {
    if (!W(O))
      throw new u(H, ["Buffer", "TypedArray", "DataView"], O);
  });
  function q(O, H) {
    const te = C(H), z = O.length;
    if (te === "hex" && z % 2 !== 0)
      throw new g("encoding", H, `is invalid for data of length ${z}`);
  }
  function V(O, H = "Port", te = !0) {
    if (typeof O != "number" && typeof O != "string" || typeof O == "string" && w(O).length === 0 || +O !== +O >>> 0 || O > 65535 || O === 0 && !te)
      throw new A(H, O, te);
    return O | 0;
  }
  const Z = I((O, H) => {
    if (O !== void 0 && (O === null || typeof O != "object" || !("aborted" in O)))
      throw new u(H, "AbortSignal", O);
  }), X = I((O, H) => {
    if (typeof O != "function") throw new u(H, "Function", O);
  }), m = I((O, H) => {
    if (typeof O != "function" || N(O)) throw new u(H, "Function", O);
  }), c = I((O, H) => {
    if (O !== void 0) throw new u(H, "undefined", O);
  });
  function E(O, H, te) {
    if (!e(te, O))
      throw new u(H, `('${r(te, "|")}')`, O);
  }
  const j = /^(?:<[^>]*>)(?:\s*;\s*[^;"\s]+(?:=(")?[^;"\s]*\1)?)*$/;
  function G(O, H) {
    if (typeof O > "u" || !d(j, O))
      throw new g(
        H,
        O,
        'must be an array or string of format "</styles.css>; rel=preload; as=style"'
      );
  }
  function K(O) {
    if (typeof O == "string")
      return G(O, "hints"), O;
    if (t(O)) {
      const H = O.length;
      let te = "";
      if (H === 0)
        return te;
      for (let z = 0; z < H; z++) {
        const de = O[z];
        G(de, "hints"), te += de, z !== H - 1 && (te += ", ");
      }
      return te;
    }
    throw new g(
      "hints",
      O,
      'must be an array or string of format "</styles.css>; rel=preload; as=style"'
    );
  }
  return Fa = {
    isInt32: L,
    isUint32: U,
    parseFileMode: F,
    validateArray: B,
    validateStringArray: le,
    validateBooleanArray: ae,
    validateAbortSignalArray: ce,
    validateBoolean: Q,
    validateBuffer: P,
    validateDictionary: ee,
    validateEncoding: q,
    validateFunction: X,
    validateInt32: J,
    validateInteger: T,
    validateNumber: ne,
    validateObject: $,
    validateOneOf: M,
    validatePlainFunction: m,
    validatePort: V,
    validateSignalName: Y,
    validateString: se,
    validateUint32: re,
    validateUndefined: c,
    validateUnion: E,
    validateAbortSignal: Z,
    validateLinkHeaderValue: K
  }, Fa;
}
var Hn = { exports: {} }, ja, zp;
function je() {
  return zp || (zp = 1, ja = Tt.process), ja;
}
var $a, Vp;
function nr() {
  if (Vp) return $a;
  Vp = 1;
  const { SymbolAsyncIterator: t, SymbolIterator: e, SymbolFor: r } = ze(), o = r("nodejs.stream.destroyed"), a = r("nodejs.stream.errored"), n = r("nodejs.stream.readable"), i = r("nodejs.stream.writable"), l = r("nodejs.stream.disturbed"), s = r("nodejs.webstream.isClosedPromise"), f = r("nodejs.webstream.controllerErrorFunction");
  function d(v, $ = !1) {
    var ee;
    return !!(v && typeof v.pipe == "function" && typeof v.on == "function" && (!$ || typeof v.pause == "function" && typeof v.resume == "function") && (!v._writableState || ((ee = v._readableState) === null || ee === void 0 ? void 0 : ee.readable) !== !1) && // Duplex
    (!v._writableState || v._readableState));
  }
  function y(v) {
    var $;
    return !!(v && typeof v.write == "function" && typeof v.on == "function" && (!v._readableState || (($ = v._writableState) === null || $ === void 0 ? void 0 : $.writable) !== !1));
  }
  function R(v) {
    return !!(v && typeof v.pipe == "function" && v._readableState && typeof v.on == "function" && typeof v.write == "function");
  }
  function w(v) {
    return v && (v._readableState || v._writableState || typeof v.write == "function" && typeof v.on == "function" || typeof v.pipe == "function" && typeof v.on == "function");
  }
  function I(v) {
    return !!(v && !w(v) && typeof v.pipeThrough == "function" && typeof v.getReader == "function" && typeof v.cancel == "function");
  }
  function A(v) {
    return !!(v && !w(v) && typeof v.getWriter == "function" && typeof v.abort == "function");
  }
  function u(v) {
    return !!(v && !w(v) && typeof v.readable == "object" && typeof v.writable == "object");
  }
  function g(v) {
    return I(v) || A(v) || u(v);
  }
  function p(v, $) {
    return v == null ? !1 : $ === !0 ? typeof v[t] == "function" : $ === !1 ? typeof v[e] == "function" : typeof v[t] == "function" || typeof v[e] == "function";
  }
  function b(v) {
    if (!w(v)) return null;
    const $ = v._writableState, ee = v._readableState, B = $ || ee;
    return !!(v.destroyed || v[o] || B != null && B.destroyed);
  }
  function C(v) {
    if (!y(v)) return null;
    if (v.writableEnded === !0) return !0;
    const $ = v._writableState;
    return $ != null && $.errored ? !1 : typeof $?.ended != "boolean" ? null : $.ended;
  }
  function N(v, $) {
    if (!y(v)) return null;
    if (v.writableFinished === !0) return !0;
    const ee = v._writableState;
    return ee != null && ee.errored ? !1 : typeof ee?.finished != "boolean" ? null : !!(ee.finished || $ === !1 && ee.ended === !0 && ee.length === 0);
  }
  function W(v) {
    if (!d(v)) return null;
    if (v.readableEnded === !0) return !0;
    const $ = v._readableState;
    return !$ || $.errored ? !1 : typeof $?.ended != "boolean" ? null : $.ended;
  }
  function k(v, $) {
    if (!d(v)) return null;
    const ee = v._readableState;
    return ee != null && ee.errored ? !1 : typeof ee?.endEmitted != "boolean" ? null : !!(ee.endEmitted || $ === !1 && ee.ended === !0 && ee.length === 0);
  }
  function L(v) {
    return v && v[n] != null ? v[n] : typeof v?.readable != "boolean" ? null : b(v) ? !1 : d(v) && v.readable && !k(v);
  }
  function U(v) {
    return v && v[i] != null ? v[i] : typeof v?.writable != "boolean" ? null : b(v) ? !1 : y(v) && v.writable && !C(v);
  }
  function x(v, $) {
    return w(v) ? b(v) ? !0 : !($?.readable !== !1 && L(v) || $?.writable !== !1 && U(v)) : null;
  }
  function D(v) {
    var $, ee;
    return w(v) ? v.writableErrored ? v.writableErrored : ($ = (ee = v._writableState) === null || ee === void 0 ? void 0 : ee.errored) !== null && $ !== void 0 ? $ : null : null;
  }
  function F(v) {
    var $, ee;
    return w(v) ? v.readableErrored ? v.readableErrored : ($ = (ee = v._readableState) === null || ee === void 0 ? void 0 : ee.errored) !== null && $ !== void 0 ? $ : null : null;
  }
  function T(v) {
    if (!w(v))
      return null;
    if (typeof v.closed == "boolean")
      return v.closed;
    const $ = v._writableState, ee = v._readableState;
    return typeof $?.closed == "boolean" || typeof ee?.closed == "boolean" ? $?.closed || ee?.closed : typeof v._closed == "boolean" && J(v) ? v._closed : null;
  }
  function J(v) {
    return typeof v._closed == "boolean" && typeof v._defaultKeepAlive == "boolean" && typeof v._removedConnection == "boolean" && typeof v._removedContLen == "boolean";
  }
  function re(v) {
    return typeof v._sent100 == "boolean" && J(v);
  }
  function se(v) {
    var $;
    return typeof v._consuming == "boolean" && typeof v._dumped == "boolean" && (($ = v.req) === null || $ === void 0 ? void 0 : $.upgradeOrConnect) === void 0;
  }
  function ne(v) {
    if (!w(v)) return null;
    const $ = v._writableState, ee = v._readableState, B = $ || ee;
    return !B && re(v) || !!(B && B.autoDestroy && B.emitClose && B.closed === !1);
  }
  function M(v) {
    var $;
    return !!(v && (($ = v[l]) !== null && $ !== void 0 ? $ : v.readableDidRead || v.readableAborted));
  }
  function Q(v) {
    var $, ee, B, le, ae, ce, Y, P, q, V;
    return !!(v && (($ = (ee = (B = (le = (ae = (ce = v[a]) !== null && ce !== void 0 ? ce : v.readableErrored) !== null && ae !== void 0 ? ae : v.writableErrored) !== null && le !== void 0 ? le : (Y = v._readableState) === null || Y === void 0 ? void 0 : Y.errorEmitted) !== null && B !== void 0 ? B : (P = v._writableState) === null || P === void 0 ? void 0 : P.errorEmitted) !== null && ee !== void 0 ? ee : (q = v._readableState) === null || q === void 0 ? void 0 : q.errored) !== null && $ !== void 0 ? $ : !((V = v._writableState) === null || V === void 0) && V.errored));
  }
  return $a = {
    isDestroyed: b,
    kIsDestroyed: o,
    isDisturbed: M,
    kIsDisturbed: l,
    isErrored: Q,
    kIsErrored: a,
    isReadable: L,
    kIsReadable: n,
    kIsClosedPromise: s,
    kControllerErrorFunction: f,
    kIsWritable: i,
    isClosed: T,
    isDuplexNodeStream: R,
    isFinished: x,
    isIterable: p,
    isReadableNodeStream: d,
    isReadableStream: I,
    isReadableEnded: W,
    isReadableFinished: k,
    isReadableErrored: F,
    isNodeStream: w,
    isWebStream: g,
    isWritable: U,
    isWritableNodeStream: y,
    isWritableStream: A,
    isWritableEnded: C,
    isWritableFinished: N,
    isWritableErrored: D,
    isServerRequest: se,
    isServerResponse: re,
    willEmitClose: ne,
    isTransformStream: u
  }, $a;
}
var Zp;
function pr() {
  if (Zp) return Hn.exports;
  Zp = 1;
  const t = je(), { AbortError: e, codes: r } = ut(), { ERR_INVALID_ARG_TYPE: o, ERR_STREAM_PREMATURE_CLOSE: a } = r, { kEmptyObject: n, once: i } = pt(), { validateAbortSignal: l, validateFunction: s, validateObject: f, validateBoolean: d } = An(), { Promise: y, PromisePrototypeThen: R, SymbolDispose: w } = ze(), {
    isClosed: I,
    isReadable: A,
    isReadableNodeStream: u,
    isReadableStream: g,
    isReadableFinished: p,
    isReadableErrored: b,
    isWritable: C,
    isWritableNodeStream: N,
    isWritableStream: W,
    isWritableFinished: k,
    isWritableErrored: L,
    isNodeStream: U,
    willEmitClose: x,
    kIsClosedPromise: D
  } = nr();
  let F;
  function T(M) {
    return M.setHeader && typeof M.abort == "function";
  }
  const J = () => {
  };
  function re(M, Q, v) {
    var $, ee;
    if (arguments.length === 2 ? (v = Q, Q = n) : Q == null ? Q = n : f(Q, "options"), s(v, "callback"), l(Q.signal, "options.signal"), v = i(v), g(M) || W(M))
      return se(M, Q, v);
    if (!U(M))
      throw new o("stream", ["ReadableStream", "WritableStream", "Stream"], M);
    const B = ($ = Q.readable) !== null && $ !== void 0 ? $ : u(M), le = (ee = Q.writable) !== null && ee !== void 0 ? ee : N(M), ae = M._writableState, ce = M._readableState, Y = () => {
      M.writable || V();
    };
    let P = x(M) && u(M) === B && N(M) === le, q = k(M, !1);
    const V = () => {
      q = !0, M.destroyed && (P = !1), !(P && (!M.readable || B)) && (!B || Z) && v.call(M);
    };
    let Z = p(M, !1);
    const X = () => {
      Z = !0, M.destroyed && (P = !1), !(P && (!M.writable || le)) && (!le || q) && v.call(M);
    }, m = (O) => {
      v.call(M, O);
    };
    let c = I(M);
    const E = () => {
      c = !0;
      const O = L(M) || b(M);
      if (O && typeof O != "boolean")
        return v.call(M, O);
      if (B && !Z && u(M, !0) && !p(M, !1))
        return v.call(M, new a());
      if (le && !q && !k(M, !1))
        return v.call(M, new a());
      v.call(M);
    }, j = () => {
      c = !0;
      const O = L(M) || b(M);
      if (O && typeof O != "boolean")
        return v.call(M, O);
      v.call(M);
    }, G = () => {
      M.req.on("finish", V);
    };
    T(M) ? (M.on("complete", V), P || M.on("abort", E), M.req ? G() : M.on("request", G)) : le && !ae && (M.on("end", Y), M.on("close", Y)), !P && typeof M.aborted == "boolean" && M.on("aborted", E), M.on("end", X), M.on("finish", V), Q.error !== !1 && M.on("error", m), M.on("close", E), c ? t.nextTick(E) : ae != null && ae.errorEmitted || ce != null && ce.errorEmitted ? P || t.nextTick(j) : (!B && (!P || A(M)) && (q || C(M) === !1) || !le && (!P || C(M)) && (Z || A(M) === !1) || ce && M.req && M.aborted) && t.nextTick(j);
    const K = () => {
      v = J, M.removeListener("aborted", E), M.removeListener("complete", V), M.removeListener("abort", E), M.removeListener("request", G), M.req && M.req.removeListener("finish", V), M.removeListener("end", Y), M.removeListener("close", Y), M.removeListener("finish", V), M.removeListener("end", X), M.removeListener("error", m), M.removeListener("close", E);
    };
    if (Q.signal && !c) {
      const O = () => {
        const H = v;
        K(), H.call(
          M,
          new e(void 0, {
            cause: Q.signal.reason
          })
        );
      };
      if (Q.signal.aborted)
        t.nextTick(O);
      else {
        F = F || pt().addAbortListener;
        const H = F(Q.signal, O), te = v;
        v = i((...z) => {
          H[w](), te.apply(M, z);
        });
      }
    }
    return K;
  }
  function se(M, Q, v) {
    let $ = !1, ee = J;
    if (Q.signal)
      if (ee = () => {
        $ = !0, v.call(
          M,
          new e(void 0, {
            cause: Q.signal.reason
          })
        );
      }, Q.signal.aborted)
        t.nextTick(ee);
      else {
        F = F || pt().addAbortListener;
        const le = F(Q.signal, ee), ae = v;
        v = i((...ce) => {
          le[w](), ae.apply(M, ce);
        });
      }
    const B = (...le) => {
      $ || t.nextTick(() => v.apply(M, le));
    };
    return R(M[D].promise, B, B), J;
  }
  function ne(M, Q) {
    var v;
    let $ = !1;
    return Q === null && (Q = n), (v = Q) !== null && v !== void 0 && v.cleanup && (d(Q.cleanup, "cleanup"), $ = Q.cleanup), new y((ee, B) => {
      const le = re(M, Q, (ae) => {
        $ && le(), ae ? B(ae) : ee();
      });
    });
  }
  return Hn.exports = re, Hn.exports.finished = ne, Hn.exports;
}
var Wa, Yp;
function $r() {
  if (Yp) return Wa;
  Yp = 1;
  const t = je(), {
    aggregateTwoErrors: e,
    codes: { ERR_MULTIPLE_CALLBACK: r },
    AbortError: o
  } = ut(), { Symbol: a } = ze(), { kIsDestroyed: n, isDestroyed: i, isFinished: l, isServerRequest: s } = nr(), f = a("kDestroy"), d = a("kConstruct");
  function y(x, D, F) {
    x && (x.stack, D && !D.errored && (D.errored = x), F && !F.errored && (F.errored = x));
  }
  function R(x, D) {
    const F = this._readableState, T = this._writableState, J = T || F;
    return T != null && T.destroyed || F != null && F.destroyed ? (typeof D == "function" && D(), this) : (y(x, T, F), T && (T.destroyed = !0), F && (F.destroyed = !0), J.constructed ? w(this, x, D) : this.once(f, function(re) {
      w(this, e(re, x), D);
    }), this);
  }
  function w(x, D, F) {
    let T = !1;
    function J(re) {
      if (T)
        return;
      T = !0;
      const se = x._readableState, ne = x._writableState;
      y(re, ne, se), ne && (ne.closed = !0), se && (se.closed = !0), typeof F == "function" && F(re), re ? t.nextTick(I, x, re) : t.nextTick(A, x);
    }
    try {
      x._destroy(D || null, J);
    } catch (re) {
      J(re);
    }
  }
  function I(x, D) {
    u(x, D), A(x);
  }
  function A(x) {
    const D = x._readableState, F = x._writableState;
    F && (F.closeEmitted = !0), D && (D.closeEmitted = !0), (F != null && F.emitClose || D != null && D.emitClose) && x.emit("close");
  }
  function u(x, D) {
    const F = x._readableState, T = x._writableState;
    T != null && T.errorEmitted || F != null && F.errorEmitted || (T && (T.errorEmitted = !0), F && (F.errorEmitted = !0), x.emit("error", D));
  }
  function g() {
    const x = this._readableState, D = this._writableState;
    x && (x.constructed = !0, x.closed = !1, x.closeEmitted = !1, x.destroyed = !1, x.errored = null, x.errorEmitted = !1, x.reading = !1, x.ended = x.readable === !1, x.endEmitted = x.readable === !1), D && (D.constructed = !0, D.destroyed = !1, D.closed = !1, D.closeEmitted = !1, D.errored = null, D.errorEmitted = !1, D.finalCalled = !1, D.prefinished = !1, D.ended = D.writable === !1, D.ending = D.writable === !1, D.finished = D.writable === !1);
  }
  function p(x, D, F) {
    const T = x._readableState, J = x._writableState;
    if (J != null && J.destroyed || T != null && T.destroyed)
      return this;
    T != null && T.autoDestroy || J != null && J.autoDestroy ? x.destroy(D) : D && (D.stack, J && !J.errored && (J.errored = D), T && !T.errored && (T.errored = D), F ? t.nextTick(u, x, D) : u(x, D));
  }
  function b(x, D) {
    if (typeof x._construct != "function")
      return;
    const F = x._readableState, T = x._writableState;
    F && (F.constructed = !1), T && (T.constructed = !1), x.once(d, D), !(x.listenerCount(d) > 1) && t.nextTick(C, x);
  }
  function C(x) {
    let D = !1;
    function F(T) {
      if (D) {
        p(x, T ?? new r());
        return;
      }
      D = !0;
      const J = x._readableState, re = x._writableState, se = re || J;
      J && (J.constructed = !0), re && (re.constructed = !0), se.destroyed ? x.emit(f, T) : T ? p(x, T, !0) : t.nextTick(N, x);
    }
    try {
      x._construct((T) => {
        t.nextTick(F, T);
      });
    } catch (T) {
      t.nextTick(F, T);
    }
  }
  function N(x) {
    x.emit(d);
  }
  function W(x) {
    return x?.setHeader && typeof x.abort == "function";
  }
  function k(x) {
    x.emit("close");
  }
  function L(x, D) {
    x.emit("error", D), t.nextTick(k, x);
  }
  function U(x, D) {
    !x || i(x) || (!D && !l(x) && (D = new o()), s(x) ? (x.socket = null, x.destroy(D)) : W(x) ? x.abort() : W(x.req) ? x.req.abort() : typeof x.destroy == "function" ? x.destroy(D) : typeof x.close == "function" ? x.close() : D ? t.nextTick(L, x, D) : t.nextTick(k, x), x.destroyed || (x[n] = !0));
  }
  return Wa = {
    construct: b,
    destroyer: U,
    destroy: R,
    undestroy: g,
    errorOrDestroy: p
  }, Wa;
}
var qa, Kp;
function Nc() {
  if (Kp) return qa;
  Kp = 1;
  const { ArrayIsArray: t, ObjectSetPrototypeOf: e } = ze(), { EventEmitter: r } = Ke;
  function o(n) {
    r.call(this, n);
  }
  e(o.prototype, r.prototype), e(o, r), o.prototype.pipe = function(n, i) {
    const l = this;
    function s(A) {
      n.writable && n.write(A) === !1 && l.pause && l.pause();
    }
    l.on("data", s);
    function f() {
      l.readable && l.resume && l.resume();
    }
    n.on("drain", f), !n._isStdio && (!i || i.end !== !1) && (l.on("end", y), l.on("close", R));
    let d = !1;
    function y() {
      d || (d = !0, n.end());
    }
    function R() {
      d || (d = !0, typeof n.destroy == "function" && n.destroy());
    }
    function w(A) {
      I(), r.listenerCount(this, "error") === 0 && this.emit("error", A);
    }
    a(l, "error", w), a(n, "error", w);
    function I() {
      l.removeListener("data", s), n.removeListener("drain", f), l.removeListener("end", y), l.removeListener("close", R), l.removeListener("error", w), n.removeListener("error", w), l.removeListener("end", I), l.removeListener("close", I), n.removeListener("close", I);
    }
    return l.on("end", I), l.on("close", I), n.on("close", I), n.emit("pipe", l), n;
  };
  function a(n, i, l) {
    if (typeof n.prependListener == "function") return n.prependListener(i, l);
    !n._events || !n._events[i] ? n.on(i, l) : t(n._events[i]) ? n._events[i].unshift(l) : n._events[i] = [l, n._events[i]];
  }
  return qa = {
    Stream: o,
    prependListener: a
  }, qa;
}
var Ba = { exports: {} }, Jp;
function Yi() {
  return Jp || (Jp = 1, (function(t) {
    const { SymbolDispose: e } = ze(), { AbortError: r, codes: o } = ut(), { isNodeStream: a, isWebStream: n, kControllerErrorFunction: i } = nr(), l = pr(), { ERR_INVALID_ARG_TYPE: s } = o;
    let f;
    const d = (y, R) => {
      if (typeof y != "object" || !("aborted" in y))
        throw new s(R, "AbortSignal", y);
    };
    t.exports.addAbortSignal = function(R, w) {
      if (d(R, "signal"), !a(w) && !n(w))
        throw new s("stream", ["ReadableStream", "WritableStream", "Stream"], w);
      return t.exports.addAbortSignalNoValidate(R, w);
    }, t.exports.addAbortSignalNoValidate = function(y, R) {
      if (typeof y != "object" || !("aborted" in y))
        return R;
      const w = a(R) ? () => {
        R.destroy(
          new r(void 0, {
            cause: y.reason
          })
        );
      } : () => {
        R[i](
          new r(void 0, {
            cause: y.reason
          })
        );
      };
      if (y.aborted)
        w();
      else {
        f = f || pt().addAbortListener;
        const I = f(y, w);
        l(R, I[e]);
      }
      return R;
    };
  })(Ba)), Ba.exports;
}
var Ua, Xp;
function OR() {
  if (Xp) return Ua;
  Xp = 1;
  const { StringPrototypeSlice: t, SymbolIterator: e, TypedArrayPrototypeSet: r, Uint8Array: o } = ze(), { Buffer: a } = Fe, { inspect: n } = pt();
  return Ua = class {
    constructor() {
      this.head = null, this.tail = null, this.length = 0;
    }
    push(l) {
      const s = {
        data: l,
        next: null
      };
      this.length > 0 ? this.tail.next = s : this.head = s, this.tail = s, ++this.length;
    }
    unshift(l) {
      const s = {
        data: l,
        next: this.head
      };
      this.length === 0 && (this.tail = s), this.head = s, ++this.length;
    }
    shift() {
      if (this.length === 0) return;
      const l = this.head.data;
      return this.length === 1 ? this.head = this.tail = null : this.head = this.head.next, --this.length, l;
    }
    clear() {
      this.head = this.tail = null, this.length = 0;
    }
    join(l) {
      if (this.length === 0) return "";
      let s = this.head, f = "" + s.data;
      for (; (s = s.next) !== null; ) f += l + s.data;
      return f;
    }
    concat(l) {
      if (this.length === 0) return a.alloc(0);
      const s = a.allocUnsafe(l >>> 0);
      let f = this.head, d = 0;
      for (; f; )
        r(s, f.data, d), d += f.data.length, f = f.next;
      return s;
    }
    // Consumes a specified amount of bytes or characters from the buffered data.
    consume(l, s) {
      const f = this.head.data;
      if (l < f.length) {
        const d = f.slice(0, l);
        return this.head.data = f.slice(l), d;
      }
      return l === f.length ? this.shift() : s ? this._getString(l) : this._getBuffer(l);
    }
    first() {
      return this.head.data;
    }
    *[e]() {
      for (let l = this.head; l; l = l.next)
        yield l.data;
    }
    // Consumes a specified amount of characters from the buffered data.
    _getString(l) {
      let s = "", f = this.head, d = 0;
      do {
        const y = f.data;
        if (l > y.length)
          s += y, l -= y.length;
        else {
          l === y.length ? (s += y, ++d, f.next ? this.head = f.next : this.head = this.tail = null) : (s += t(y, 0, l), this.head = f, f.data = t(y, l));
          break;
        }
        ++d;
      } while ((f = f.next) !== null);
      return this.length -= d, s;
    }
    // Consumes a specified amount of bytes from the buffered data.
    _getBuffer(l) {
      const s = a.allocUnsafe(l), f = l;
      let d = this.head, y = 0;
      do {
        const R = d.data;
        if (l > R.length)
          r(s, R, f - l), l -= R.length;
        else {
          l === R.length ? (r(s, R, f - l), ++y, d.next ? this.head = d.next : this.head = this.tail = null) : (r(s, new o(R.buffer, R.byteOffset, l), f - l), this.head = d, d.data = R.slice(l));
          break;
        }
        ++y;
      } while ((d = d.next) !== null);
      return this.length -= y, s;
    }
    // Make sure the linked list only shows the minimal necessary information.
    [/* @__PURE__ */ Symbol.for("nodejs.util.inspect.custom")](l, s) {
      return n(this, {
        ...s,
        // Only inspect one level.
        depth: 0,
        // It should not recurse.
        customInspect: !1
      });
    }
  }, Ua;
}
var Ga, Qp;
function Ki() {
  if (Qp) return Ga;
  Qp = 1;
  const { MathFloor: t, NumberIsInteger: e } = ze(), { validateInteger: r } = An(), { ERR_INVALID_ARG_VALUE: o } = ut().codes;
  let a = 16 * 1024, n = 16;
  function i(d, y, R) {
    return d.highWaterMark != null ? d.highWaterMark : y ? d[R] : null;
  }
  function l(d) {
    return d ? n : a;
  }
  function s(d, y) {
    r(y, "value", 0), d ? n = y : a = y;
  }
  function f(d, y, R, w) {
    const I = i(y, w, R);
    if (I != null) {
      if (!e(I) || I < 0) {
        const A = w ? `options.${R}` : "options.highWaterMark";
        throw new o(A, I);
      }
      return t(I);
    }
    return l(d.objectMode);
  }
  return Ga = {
    getHighWaterMark: f,
    getDefaultHighWaterMark: l,
    setDefaultHighWaterMark: s
  }, Ga;
}
var Ha = {}, zn = { exports: {} };
var eg;
function NR() {
  return eg || (eg = 1, (function(t, e) {
    var r = Fe, o = r.Buffer;
    function a(i, l) {
      for (var s in i)
        l[s] = i[s];
    }
    o.from && o.alloc && o.allocUnsafe && o.allocUnsafeSlow ? t.exports = r : (a(r, e), e.Buffer = n);
    function n(i, l, s) {
      return o(i, l, s);
    }
    n.prototype = Object.create(o.prototype), a(o, n), n.from = function(i, l, s) {
      if (typeof i == "number")
        throw new TypeError("Argument must not be a number");
      return o(i, l, s);
    }, n.alloc = function(i, l, s) {
      if (typeof i != "number")
        throw new TypeError("Argument must be a number");
      var f = o(i);
      return l !== void 0 ? typeof s == "string" ? f.fill(l, s) : f.fill(l) : f.fill(0), f;
    }, n.allocUnsafe = function(i) {
      if (typeof i != "number")
        throw new TypeError("Argument must be a number");
      return o(i);
    }, n.allocUnsafeSlow = function(i) {
      if (typeof i != "number")
        throw new TypeError("Argument must be a number");
      return r.SlowBuffer(i);
    };
  })(zn, zn.exports)), zn.exports;
}
var tg;
function Ji() {
  if (tg) return Ha;
  tg = 1;
  var t = NR().Buffer, e = t.isEncoding || function(g) {
    switch (g = "" + g, g && g.toLowerCase()) {
      case "hex":
      case "utf8":
      case "utf-8":
      case "ascii":
      case "binary":
      case "base64":
      case "ucs2":
      case "ucs-2":
      case "utf16le":
      case "utf-16le":
      case "raw":
        return !0;
      default:
        return !1;
    }
  };
  function r(g) {
    if (!g) return "utf8";
    for (var p; ; )
      switch (g) {
        case "utf8":
        case "utf-8":
          return "utf8";
        case "ucs2":
        case "ucs-2":
        case "utf16le":
        case "utf-16le":
          return "utf16le";
        case "latin1":
        case "binary":
          return "latin1";
        case "base64":
        case "ascii":
        case "hex":
          return g;
        default:
          if (p) return;
          g = ("" + g).toLowerCase(), p = !0;
      }
  }
  function o(g) {
    var p = r(g);
    if (typeof p != "string" && (t.isEncoding === e || !e(g))) throw new Error("Unknown encoding: " + g);
    return p || g;
  }
  Ha.StringDecoder = a;
  function a(g) {
    this.encoding = o(g);
    var p;
    switch (this.encoding) {
      case "utf16le":
        this.text = y, this.end = R, p = 4;
        break;
      case "utf8":
        this.fillLast = s, p = 4;
        break;
      case "base64":
        this.text = w, this.end = I, p = 3;
        break;
      default:
        this.write = A, this.end = u;
        return;
    }
    this.lastNeed = 0, this.lastTotal = 0, this.lastChar = t.allocUnsafe(p);
  }
  a.prototype.write = function(g) {
    if (g.length === 0) return "";
    var p, b;
    if (this.lastNeed) {
      if (p = this.fillLast(g), p === void 0) return "";
      b = this.lastNeed, this.lastNeed = 0;
    } else
      b = 0;
    return b < g.length ? p ? p + this.text(g, b) : this.text(g, b) : p || "";
  }, a.prototype.end = d, a.prototype.text = f, a.prototype.fillLast = function(g) {
    if (this.lastNeed <= g.length)
      return g.copy(this.lastChar, this.lastTotal - this.lastNeed, 0, this.lastNeed), this.lastChar.toString(this.encoding, 0, this.lastTotal);
    g.copy(this.lastChar, this.lastTotal - this.lastNeed, 0, g.length), this.lastNeed -= g.length;
  };
  function n(g) {
    return g <= 127 ? 0 : g >> 5 === 6 ? 2 : g >> 4 === 14 ? 3 : g >> 3 === 30 ? 4 : g >> 6 === 2 ? -1 : -2;
  }
  function i(g, p, b) {
    var C = p.length - 1;
    if (C < b) return 0;
    var N = n(p[C]);
    return N >= 0 ? (N > 0 && (g.lastNeed = N - 1), N) : --C < b || N === -2 ? 0 : (N = n(p[C]), N >= 0 ? (N > 0 && (g.lastNeed = N - 2), N) : --C < b || N === -2 ? 0 : (N = n(p[C]), N >= 0 ? (N > 0 && (N === 2 ? N = 0 : g.lastNeed = N - 3), N) : 0));
  }
  function l(g, p, b) {
    if ((p[0] & 192) !== 128)
      return g.lastNeed = 0, "�";
    if (g.lastNeed > 1 && p.length > 1) {
      if ((p[1] & 192) !== 128)
        return g.lastNeed = 1, "�";
      if (g.lastNeed > 2 && p.length > 2 && (p[2] & 192) !== 128)
        return g.lastNeed = 2, "�";
    }
  }
  function s(g) {
    var p = this.lastTotal - this.lastNeed, b = l(this, g);
    if (b !== void 0) return b;
    if (this.lastNeed <= g.length)
      return g.copy(this.lastChar, p, 0, this.lastNeed), this.lastChar.toString(this.encoding, 0, this.lastTotal);
    g.copy(this.lastChar, p, 0, g.length), this.lastNeed -= g.length;
  }
  function f(g, p) {
    var b = i(this, g, p);
    if (!this.lastNeed) return g.toString("utf8", p);
    this.lastTotal = b;
    var C = g.length - (b - this.lastNeed);
    return g.copy(this.lastChar, 0, C), g.toString("utf8", p, C);
  }
  function d(g) {
    var p = g && g.length ? this.write(g) : "";
    return this.lastNeed ? p + "�" : p;
  }
  function y(g, p) {
    if ((g.length - p) % 2 === 0) {
      var b = g.toString("utf16le", p);
      if (b) {
        var C = b.charCodeAt(b.length - 1);
        if (C >= 55296 && C <= 56319)
          return this.lastNeed = 2, this.lastTotal = 4, this.lastChar[0] = g[g.length - 2], this.lastChar[1] = g[g.length - 1], b.slice(0, -1);
      }
      return b;
    }
    return this.lastNeed = 1, this.lastTotal = 2, this.lastChar[0] = g[g.length - 1], g.toString("utf16le", p, g.length - 1);
  }
  function R(g) {
    var p = g && g.length ? this.write(g) : "";
    if (this.lastNeed) {
      var b = this.lastTotal - this.lastNeed;
      return p + this.lastChar.toString("utf16le", 0, b);
    }
    return p;
  }
  function w(g, p) {
    var b = (g.length - p) % 3;
    return b === 0 ? g.toString("base64", p) : (this.lastNeed = 3 - b, this.lastTotal = 3, b === 1 ? this.lastChar[0] = g[g.length - 1] : (this.lastChar[0] = g[g.length - 2], this.lastChar[1] = g[g.length - 1]), g.toString("base64", p, g.length - b));
  }
  function I(g) {
    var p = g && g.length ? this.write(g) : "";
    return this.lastNeed ? p + this.lastChar.toString("base64", 0, 3 - this.lastNeed) : p;
  }
  function A(g) {
    return g.toString(this.encoding);
  }
  function u(g) {
    return g && g.length ? this.write(g) : "";
  }
  return Ha;
}
var za, rg;
function Fm() {
  if (rg) return za;
  rg = 1;
  const t = je(), { PromisePrototypeThen: e, SymbolAsyncIterator: r, SymbolIterator: o } = ze(), { Buffer: a } = Fe, { ERR_INVALID_ARG_TYPE: n, ERR_STREAM_NULL_VALUES: i } = ut().codes;
  function l(s, f, d) {
    let y;
    if (typeof f == "string" || f instanceof a)
      return new s({
        objectMode: !0,
        ...d,
        read() {
          this.push(f), this.push(null);
        }
      });
    let R;
    if (f && f[r])
      R = !0, y = f[r]();
    else if (f && f[o])
      R = !1, y = f[o]();
    else
      throw new n("iterable", ["Iterable"], f);
    const w = new s({
      objectMode: !0,
      highWaterMark: 1,
      // TODO(ronag): What options should be allowed?
      ...d
    });
    let I = !1;
    w._read = function() {
      I || (I = !0, u());
    }, w._destroy = function(g, p) {
      e(
        A(g),
        () => t.nextTick(p, g),
        // nextTick is here in case cb throws
        (b) => t.nextTick(p, b || g)
      );
    };
    async function A(g) {
      const p = g != null, b = typeof y.throw == "function";
      if (p && b) {
        const { value: C, done: N } = await y.throw(g);
        if (await C, N)
          return;
      }
      if (typeof y.return == "function") {
        const { value: C } = await y.return();
        await C;
      }
    }
    async function u() {
      for (; ; ) {
        try {
          const { value: g, done: p } = R ? await y.next() : y.next();
          if (p)
            w.push(null);
          else {
            const b = g && typeof g.then == "function" ? await g : g;
            if (b === null)
              throw I = !1, new i();
            if (w.push(b))
              continue;
            I = !1;
          }
        } catch (g) {
          w.destroy(g);
        }
        break;
      }
    }
    return w;
  }
  return za = l, za;
}
var Va, ng;
function Xi() {
  if (ng) return Va;
  ng = 1;
  const t = je(), {
    ArrayPrototypeIndexOf: e,
    NumberIsInteger: r,
    NumberIsNaN: o,
    NumberParseInt: a,
    ObjectDefineProperties: n,
    ObjectKeys: i,
    ObjectSetPrototypeOf: l,
    Promise: s,
    SafeSet: f,
    SymbolAsyncDispose: d,
    SymbolAsyncIterator: y,
    Symbol: R
  } = ze();
  Va = z, z.ReadableState = te;
  const { EventEmitter: w } = Ke, { Stream: I, prependListener: A } = Nc(), { Buffer: u } = Fe, { addAbortSignal: g } = Yi(), p = pr();
  let b = pt().debuglog("stream", (h) => {
    b = h;
  });
  const C = OR(), N = $r(), { getHighWaterMark: W, getDefaultHighWaterMark: k } = Ki(), {
    aggregateTwoErrors: L,
    codes: {
      ERR_INVALID_ARG_TYPE: U,
      ERR_METHOD_NOT_IMPLEMENTED: x,
      ERR_OUT_OF_RANGE: D,
      ERR_STREAM_PUSH_AFTER_EOF: F,
      ERR_STREAM_UNSHIFT_AFTER_END_EVENT: T
    },
    AbortError: J
  } = ut(), { validateObject: re } = An(), se = R("kPaused"), { StringDecoder: ne } = Ji(), M = Fm();
  l(z.prototype, I.prototype), l(z, I);
  const Q = () => {
  }, { errorOrDestroy: v } = N, $ = 1, ee = 2, B = 4, le = 8, ae = 16, ce = 32, Y = 64, P = 128, q = 256, V = 512, Z = 1024, X = 2048, m = 4096, c = 8192, E = 16384, j = 32768, G = 65536, K = 1 << 17, O = 1 << 18;
  function H(h) {
    return {
      enumerable: !1,
      get() {
        return (this.state & h) !== 0;
      },
      set(S) {
        S ? this.state |= h : this.state &= ~h;
      }
    };
  }
  n(te.prototype, {
    objectMode: H($),
    ended: H(ee),
    endEmitted: H(B),
    reading: H(le),
    // Stream is still being constructed and cannot be
    // destroyed until construction finished or failed.
    // Async construction is opt in, therefore we start as
    // constructed.
    constructed: H(ae),
    // A flag to be able to tell if the event 'readable'/'data' is emitted
    // immediately, or on a later tick.  We set this to true at first, because
    // any actions that shouldn't happen until "later" should generally also
    // not happen before the first read call.
    sync: H(ce),
    // Whenever we return null, then we set a flag to say
    // that we're awaiting a 'readable' event emission.
    needReadable: H(Y),
    emittedReadable: H(P),
    readableListening: H(q),
    resumeScheduled: H(V),
    // True if the error was already emitted and should not be thrown again.
    errorEmitted: H(Z),
    emitClose: H(X),
    autoDestroy: H(m),
    // Has it been destroyed.
    destroyed: H(c),
    // Indicates whether the stream has finished destroying.
    closed: H(E),
    // True if close has been emitted or would have been emitted
    // depending on emitClose.
    closeEmitted: H(j),
    multiAwaitDrain: H(G),
    // If true, a maybeReadMore has been scheduled.
    readingMore: H(K),
    dataEmitted: H(O)
  });
  function te(h, S, ie) {
    typeof ie != "boolean" && (ie = S instanceof Kt()), this.state = X | m | ae | ce, h && h.objectMode && (this.state |= $), ie && h && h.readableObjectMode && (this.state |= $), this.highWaterMark = h ? W(this, h, "readableHighWaterMark", ie) : k(!1), this.buffer = new C(), this.length = 0, this.pipes = [], this.flowing = null, this[se] = null, h && h.emitClose === !1 && (this.state &= ~X), h && h.autoDestroy === !1 && (this.state &= ~m), this.errored = null, this.defaultEncoding = h && h.defaultEncoding || "utf8", this.awaitDrainWriters = null, this.decoder = null, this.encoding = null, h && h.encoding && (this.decoder = new ne(h.encoding), this.encoding = h.encoding);
  }
  function z(h) {
    if (!(this instanceof z)) return new z(h);
    const S = this instanceof Kt();
    this._readableState = new te(h, this, S), h && (typeof h.read == "function" && (this._read = h.read), typeof h.destroy == "function" && (this._destroy = h.destroy), typeof h.construct == "function" && (this._construct = h.construct), h.signal && !S && g(h.signal, this)), I.call(this, h), N.construct(this, () => {
      this._readableState.needReadable && me(this, this._readableState);
    });
  }
  z.prototype.destroy = N.destroy, z.prototype._undestroy = N.undestroy, z.prototype._destroy = function(h, S) {
    S(h);
  }, z.prototype[w.captureRejectionSymbol] = function(h) {
    this.destroy(h);
  }, z.prototype[d] = function() {
    let h;
    return this.destroyed || (h = this.readableEnded ? null : new J(), this.destroy(h)), new s((S, ie) => p(this, (oe) => oe && oe !== h ? ie(oe) : S(null)));
  }, z.prototype.push = function(h, S) {
    return de(this, h, S, !1);
  }, z.prototype.unshift = function(h, S) {
    return de(this, h, S, !0);
  };
  function de(h, S, ie, oe) {
    b("readableAddChunk", S);
    const fe = h._readableState;
    let ve;
    if ((fe.state & $) === 0 && (typeof S == "string" ? (ie = ie || fe.defaultEncoding, fe.encoding !== ie && (oe && fe.encoding ? S = u.from(S, ie).toString(fe.encoding) : (S = u.from(S, ie), ie = ""))) : S instanceof u ? ie = "" : I._isUint8Array(S) ? (S = I._uint8ArrayToBuffer(S), ie = "") : S != null && (ve = new U("chunk", ["string", "Buffer", "Uint8Array"], S))), ve)
      v(h, ve);
    else if (S === null)
      fe.state &= ~le, _e(h, fe);
    else if ((fe.state & $) !== 0 || S && S.length > 0)
      if (oe)
        if ((fe.state & B) !== 0) v(h, new T());
        else {
          if (fe.destroyed || fe.errored) return !1;
          pe(h, fe, S, !0);
        }
      else if (fe.ended)
        v(h, new F());
      else {
        if (fe.destroyed || fe.errored)
          return !1;
        fe.state &= ~le, fe.decoder && !ie ? (S = fe.decoder.write(S), fe.objectMode || S.length !== 0 ? pe(h, fe, S, !1) : me(h, fe)) : pe(h, fe, S, !1);
      }
    else oe || (fe.state &= ~le, me(h, fe));
    return !fe.ended && (fe.length < fe.highWaterMark || fe.length === 0);
  }
  function pe(h, S, ie, oe) {
    S.flowing && S.length === 0 && !S.sync && h.listenerCount("data") > 0 ? ((S.state & G) !== 0 ? S.awaitDrainWriters.clear() : S.awaitDrainWriters = null, S.dataEmitted = !0, h.emit("data", ie)) : (S.length += S.objectMode ? 1 : ie.length, oe ? S.buffer.unshift(ie) : S.buffer.push(ie), (S.state & Y) !== 0 && Ae(h)), me(h, S);
  }
  z.prototype.isPaused = function() {
    const h = this._readableState;
    return h[se] === !0 || h.flowing === !1;
  }, z.prototype.setEncoding = function(h) {
    const S = new ne(h);
    this._readableState.decoder = S, this._readableState.encoding = this._readableState.decoder.encoding;
    const ie = this._readableState.buffer;
    let oe = "";
    for (const fe of ie)
      oe += S.write(fe);
    return ie.clear(), oe !== "" && ie.push(oe), this._readableState.length = oe.length, this;
  };
  const he = 1073741824;
  function we(h) {
    if (h > he)
      throw new D("size", "<= 1GiB", h);
    return h--, h |= h >>> 1, h |= h >>> 2, h |= h >>> 4, h |= h >>> 8, h |= h >>> 16, h++, h;
  }
  function be(h, S) {
    return h <= 0 || S.length === 0 && S.ended ? 0 : (S.state & $) !== 0 ? 1 : o(h) ? S.flowing && S.length ? S.buffer.first().length : S.length : h <= S.length ? h : S.ended ? S.length : 0;
  }
  z.prototype.read = function(h) {
    b("read", h), h === void 0 ? h = NaN : r(h) || (h = a(h, 10));
    const S = this._readableState, ie = h;
    if (h > S.highWaterMark && (S.highWaterMark = we(h)), h !== 0 && (S.state &= ~P), h === 0 && S.needReadable && ((S.highWaterMark !== 0 ? S.length >= S.highWaterMark : S.length > 0) || S.ended))
      return b("read: emitReadable", S.length, S.ended), S.length === 0 && S.ended ? tt(this) : Ae(this), null;
    if (h = be(h, S), h === 0 && S.ended)
      return S.length === 0 && tt(this), null;
    let oe = (S.state & Y) !== 0;
    if (b("need readable", oe), (S.length === 0 || S.length - h < S.highWaterMark) && (oe = !0, b("length less than watermark", oe)), S.ended || S.reading || S.destroyed || S.errored || !S.constructed)
      oe = !1, b("reading, ended or constructing", oe);
    else if (oe) {
      b("do read"), S.state |= le | ce, S.length === 0 && (S.state |= Y);
      try {
        this._read(S.highWaterMark);
      } catch (ve) {
        v(this, ve);
      }
      S.state &= ~ce, S.reading || (h = be(ie, S));
    }
    let fe;
    return h > 0 ? fe = mt(h, S) : fe = null, fe === null ? (S.needReadable = S.length <= S.highWaterMark, h = 0) : (S.length -= h, S.multiAwaitDrain ? S.awaitDrainWriters.clear() : S.awaitDrainWriters = null), S.length === 0 && (S.ended || (S.needReadable = !0), ie !== h && S.ended && tt(this)), fe !== null && !S.errorEmitted && !S.closeEmitted && (S.dataEmitted = !0, this.emit("data", fe)), fe;
  };
  function _e(h, S) {
    if (b("onEofChunk"), !S.ended) {
      if (S.decoder) {
        const ie = S.decoder.end();
        ie && ie.length && (S.buffer.push(ie), S.length += S.objectMode ? 1 : ie.length);
      }
      S.ended = !0, S.sync ? Ae(h) : (S.needReadable = !1, S.emittedReadable = !0, xe(h));
    }
  }
  function Ae(h) {
    const S = h._readableState;
    b("emitReadable", S.needReadable, S.emittedReadable), S.needReadable = !1, S.emittedReadable || (b("emitReadable", S.flowing), S.emittedReadable = !0, t.nextTick(xe, h));
  }
  function xe(h) {
    const S = h._readableState;
    b("emitReadable_", S.destroyed, S.length, S.ended), !S.destroyed && !S.errored && (S.length || S.ended) && (h.emit("readable"), S.emittedReadable = !1), S.needReadable = !S.flowing && !S.ended && S.length <= S.highWaterMark, ht(h);
  }
  function me(h, S) {
    !S.readingMore && S.constructed && (S.readingMore = !0, t.nextTick(Ge, h, S));
  }
  function Ge(h, S) {
    for (; !S.reading && !S.ended && (S.length < S.highWaterMark || S.flowing && S.length === 0); ) {
      const ie = S.length;
      if (b("maybeReadMore read 0"), h.read(0), ie === S.length)
        break;
    }
    S.readingMore = !1;
  }
  z.prototype._read = function(h) {
    throw new x("_read()");
  }, z.prototype.pipe = function(h, S) {
    const ie = this, oe = this._readableState;
    oe.pipes.length === 1 && (oe.multiAwaitDrain || (oe.multiAwaitDrain = !0, oe.awaitDrainWriters = new f(oe.awaitDrainWriters ? [oe.awaitDrainWriters] : []))), oe.pipes.push(h), b("pipe count=%d opts=%j", oe.pipes.length, S);
    const ve = (!S || S.end !== !1) && h !== t.stdout && h !== t.stderr ? Et : $e;
    oe.endEmitted ? t.nextTick(ve) : ie.once("end", ve), h.on("unpipe", Te);
    function Te(Ce, De) {
      b("onunpipe"), Ce === ie && De && De.hasUnpiped === !1 && (De.hasUnpiped = !0, Mt());
    }
    function Et() {
      b("onend"), h.end();
    }
    let Pe, lt = !1;
    function Mt() {
      b("cleanup"), h.removeListener("close", it), h.removeListener("finish", st), Pe && h.removeListener("drain", Pe), h.removeListener("error", nt), h.removeListener("unpipe", Te), ie.removeListener("end", Et), ie.removeListener("end", $e), ie.removeListener("data", Rt), lt = !0, Pe && oe.awaitDrainWriters && (!h._writableState || h._writableState.needDrain) && Pe();
    }
    function vt() {
      lt || (oe.pipes.length === 1 && oe.pipes[0] === h ? (b("false write response, pause", 0), oe.awaitDrainWriters = h, oe.multiAwaitDrain = !1) : oe.pipes.length > 1 && oe.pipes.includes(h) && (b("false write response, pause", oe.awaitDrainWriters.size), oe.awaitDrainWriters.add(h)), ie.pause()), Pe || (Pe = Je(ie, h), h.on("drain", Pe));
    }
    ie.on("data", Rt);
    function Rt(Ce) {
      b("ondata");
      const De = h.write(Ce);
      b("dest.write", De), De === !1 && vt();
    }
    function nt(Ce) {
      if (b("onerror", Ce), $e(), h.removeListener("error", nt), h.listenerCount("error") === 0) {
        const De = h._writableState || h._readableState;
        De && !De.errorEmitted ? v(h, Ce) : h.emit("error", Ce);
      }
    }
    A(h, "error", nt);
    function it() {
      h.removeListener("finish", st), $e();
    }
    h.once("close", it);
    function st() {
      b("onfinish"), h.removeListener("close", it), $e();
    }
    h.once("finish", st);
    function $e() {
      b("unpipe"), ie.unpipe(h);
    }
    return h.emit("pipe", ie), h.writableNeedDrain === !0 ? vt() : oe.flowing || (b("pipe resume"), ie.resume()), h;
  };
  function Je(h, S) {
    return function() {
      const oe = h._readableState;
      oe.awaitDrainWriters === S ? (b("pipeOnDrain", 1), oe.awaitDrainWriters = null) : oe.multiAwaitDrain && (b("pipeOnDrain", oe.awaitDrainWriters.size), oe.awaitDrainWriters.delete(S)), (!oe.awaitDrainWriters || oe.awaitDrainWriters.size === 0) && h.listenerCount("data") && h.resume();
    };
  }
  z.prototype.unpipe = function(h) {
    const S = this._readableState, ie = {
      hasUnpiped: !1
    };
    if (S.pipes.length === 0) return this;
    if (!h) {
      const fe = S.pipes;
      S.pipes = [], this.pause();
      for (let ve = 0; ve < fe.length; ve++)
        fe[ve].emit("unpipe", this, {
          hasUnpiped: !1
        });
      return this;
    }
    const oe = e(S.pipes, h);
    return oe === -1 ? this : (S.pipes.splice(oe, 1), S.pipes.length === 0 && this.pause(), h.emit("unpipe", this, ie), this);
  }, z.prototype.on = function(h, S) {
    const ie = I.prototype.on.call(this, h, S), oe = this._readableState;
    return h === "data" ? (oe.readableListening = this.listenerCount("readable") > 0, oe.flowing !== !1 && this.resume()) : h === "readable" && !oe.endEmitted && !oe.readableListening && (oe.readableListening = oe.needReadable = !0, oe.flowing = !1, oe.emittedReadable = !1, b("on readable", oe.length, oe.reading), oe.length ? Ae(this) : oe.reading || t.nextTick(jt, this)), ie;
  }, z.prototype.addListener = z.prototype.on, z.prototype.removeListener = function(h, S) {
    const ie = I.prototype.removeListener.call(this, h, S);
    return h === "readable" && t.nextTick(Xe, this), ie;
  }, z.prototype.off = z.prototype.removeListener, z.prototype.removeAllListeners = function(h) {
    const S = I.prototype.removeAllListeners.apply(this, arguments);
    return (h === "readable" || h === void 0) && t.nextTick(Xe, this), S;
  };
  function Xe(h) {
    const S = h._readableState;
    S.readableListening = h.listenerCount("readable") > 0, S.resumeScheduled && S[se] === !1 ? S.flowing = !0 : h.listenerCount("data") > 0 ? h.resume() : S.readableListening || (S.flowing = null);
  }
  function jt(h) {
    b("readable nexttick read 0"), h.read(0);
  }
  z.prototype.resume = function() {
    const h = this._readableState;
    return h.flowing || (b("resume"), h.flowing = !h.readableListening, Ct(this, h)), h[se] = !1, this;
  };
  function Ct(h, S) {
    S.resumeScheduled || (S.resumeScheduled = !0, t.nextTick($t, h, S));
  }
  function $t(h, S) {
    b("resume", S.reading), S.reading || h.read(0), S.resumeScheduled = !1, h.emit("resume"), ht(h), S.flowing && !S.reading && h.read(0);
  }
  z.prototype.pause = function() {
    return b("call pause flowing=%j", this._readableState.flowing), this._readableState.flowing !== !1 && (b("pause"), this._readableState.flowing = !1, this.emit("pause")), this._readableState[se] = !0, this;
  };
  function ht(h) {
    const S = h._readableState;
    for (b("flow", S.flowing); S.flowing && h.read() !== null; ) ;
  }
  z.prototype.wrap = function(h) {
    let S = !1;
    h.on("data", (oe) => {
      !this.push(oe) && h.pause && (S = !0, h.pause());
    }), h.on("end", () => {
      this.push(null);
    }), h.on("error", (oe) => {
      v(this, oe);
    }), h.on("close", () => {
      this.destroy();
    }), h.on("destroy", () => {
      this.destroy();
    }), this._read = () => {
      S && h.resume && (S = !1, h.resume());
    };
    const ie = i(h);
    for (let oe = 1; oe < ie.length; oe++) {
      const fe = ie[oe];
      this[fe] === void 0 && typeof h[fe] == "function" && (this[fe] = h[fe].bind(h));
    }
    return this;
  }, z.prototype[y] = function() {
    return wt(this);
  }, z.prototype.iterator = function(h) {
    return h !== void 0 && re(h, "options"), wt(this, h);
  };
  function wt(h, S) {
    typeof h.read != "function" && (h = z.wrap(h, {
      objectMode: !0
    }));
    const ie = Wt(h, S);
    return ie.stream = h, ie;
  }
  async function* Wt(h, S) {
    let ie = Q;
    function oe(Te) {
      this === h ? (ie(), ie = Q) : ie = Te;
    }
    h.on("readable", oe);
    let fe;
    const ve = p(
      h,
      {
        writable: !1
      },
      (Te) => {
        fe = Te ? L(fe, Te) : null, ie(), ie = Q;
      }
    );
    try {
      for (; ; ) {
        const Te = h.destroyed ? null : h.read();
        if (Te !== null)
          yield Te;
        else {
          if (fe)
            throw fe;
          if (fe === null)
            return;
          await new s(oe);
        }
      }
    } catch (Te) {
      throw fe = L(fe, Te), fe;
    } finally {
      (fe || S?.destroyOnReturn !== !1) && (fe === void 0 || h._readableState.autoDestroy) ? N.destroyer(h, null) : (h.off("readable", oe), ve());
    }
  }
  n(z.prototype, {
    readable: {
      __proto__: null,
      get() {
        const h = this._readableState;
        return !!h && h.readable !== !1 && !h.destroyed && !h.errorEmitted && !h.endEmitted;
      },
      set(h) {
        this._readableState && (this._readableState.readable = !!h);
      }
    },
    readableDidRead: {
      __proto__: null,
      enumerable: !1,
      get: function() {
        return this._readableState.dataEmitted;
      }
    },
    readableAborted: {
      __proto__: null,
      enumerable: !1,
      get: function() {
        return !!(this._readableState.readable !== !1 && (this._readableState.destroyed || this._readableState.errored) && !this._readableState.endEmitted);
      }
    },
    readableHighWaterMark: {
      __proto__: null,
      enumerable: !1,
      get: function() {
        return this._readableState.highWaterMark;
      }
    },
    readableBuffer: {
      __proto__: null,
      enumerable: !1,
      get: function() {
        return this._readableState && this._readableState.buffer;
      }
    },
    readableFlowing: {
      __proto__: null,
      enumerable: !1,
      get: function() {
        return this._readableState.flowing;
      },
      set: function(h) {
        this._readableState && (this._readableState.flowing = h);
      }
    },
    readableLength: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._readableState.length;
      }
    },
    readableObjectMode: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._readableState ? this._readableState.objectMode : !1;
      }
    },
    readableEncoding: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._readableState ? this._readableState.encoding : null;
      }
    },
    errored: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._readableState ? this._readableState.errored : null;
      }
    },
    closed: {
      __proto__: null,
      get() {
        return this._readableState ? this._readableState.closed : !1;
      }
    },
    destroyed: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._readableState ? this._readableState.destroyed : !1;
      },
      set(h) {
        this._readableState && (this._readableState.destroyed = h);
      }
    },
    readableEnded: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._readableState ? this._readableState.endEmitted : !1;
      }
    }
  }), n(te.prototype, {
    // Legacy getter for `pipesCount`.
    pipesCount: {
      __proto__: null,
      get() {
        return this.pipes.length;
      }
    },
    // Legacy property for `paused`.
    paused: {
      __proto__: null,
      get() {
        return this[se] !== !1;
      },
      set(h) {
        this[se] = !!h;
      }
    }
  }), z._fromList = mt;
  function mt(h, S) {
    if (S.length === 0) return null;
    let ie;
    return S.objectMode ? ie = S.buffer.shift() : !h || h >= S.length ? (S.decoder ? ie = S.buffer.join("") : S.buffer.length === 1 ? ie = S.buffer.first() : ie = S.buffer.concat(S.length), S.buffer.clear()) : ie = S.buffer.consume(h, S.decoder), ie;
  }
  function tt(h) {
    const S = h._readableState;
    b("endReadable", S.endEmitted), S.endEmitted || (S.ended = !0, t.nextTick(qt, S, h));
  }
  function qt(h, S) {
    if (b("endReadableNT", h.endEmitted, h.length), !h.errored && !h.closeEmitted && !h.endEmitted && h.length === 0) {
      if (h.endEmitted = !0, S.emit("end"), S.writable && S.allowHalfOpen === !1)
        t.nextTick(Bt, S);
      else if (h.autoDestroy) {
        const ie = S._writableState;
        (!ie || ie.autoDestroy && // We don't expect the writable to ever 'finish'
        // if writable is explicitly set to false.
        (ie.finished || ie.writable === !1)) && S.destroy();
      }
    }
  }
  function Bt(h) {
    h.writable && !h.writableEnded && !h.destroyed && h.end();
  }
  z.from = function(h, S) {
    return M(z, h, S);
  };
  let rt;
  function St() {
    return rt === void 0 && (rt = {}), rt;
  }
  return z.fromWeb = function(h, S) {
    return St().newStreamReadableFromReadableStream(h, S);
  }, z.toWeb = function(h, S) {
    return St().newReadableStreamFromStreamReadable(h, S);
  }, z.wrap = function(h, S) {
    var ie, oe;
    return new z({
      objectMode: (ie = (oe = h.readableObjectMode) !== null && oe !== void 0 ? oe : h.objectMode) !== null && ie !== void 0 ? ie : !0,
      ...S,
      destroy(fe, ve) {
        N.destroyer(h, fe), ve(fe);
      }
    }).wrap(h);
  }, Va;
}
var Za, ig;
function Lc() {
  if (ig) return Za;
  ig = 1;
  const t = je(), {
    ArrayPrototypeSlice: e,
    Error: r,
    FunctionPrototypeSymbolHasInstance: o,
    ObjectDefineProperty: a,
    ObjectDefineProperties: n,
    ObjectSetPrototypeOf: i,
    StringPrototypeToLowerCase: l,
    Symbol: s,
    SymbolHasInstance: f
  } = ze();
  Za = re, re.WritableState = T;
  const { EventEmitter: d } = Ke, y = Nc().Stream, { Buffer: R } = Fe, w = $r(), { addAbortSignal: I } = Yi(), { getHighWaterMark: A, getDefaultHighWaterMark: u } = Ki(), {
    ERR_INVALID_ARG_TYPE: g,
    ERR_METHOD_NOT_IMPLEMENTED: p,
    ERR_MULTIPLE_CALLBACK: b,
    ERR_STREAM_CANNOT_PIPE: C,
    ERR_STREAM_DESTROYED: N,
    ERR_STREAM_ALREADY_FINISHED: W,
    ERR_STREAM_NULL_VALUES: k,
    ERR_STREAM_WRITE_AFTER_END: L,
    ERR_UNKNOWN_ENCODING: U
  } = ut().codes, { errorOrDestroy: x } = w;
  i(re.prototype, y.prototype), i(re, y);
  function D() {
  }
  const F = s("kOnFinished");
  function T(m, c, E) {
    typeof E != "boolean" && (E = c instanceof Kt()), this.objectMode = !!(m && m.objectMode), E && (this.objectMode = this.objectMode || !!(m && m.writableObjectMode)), this.highWaterMark = m ? A(this, m, "writableHighWaterMark", E) : u(!1), this.finalCalled = !1, this.needDrain = !1, this.ending = !1, this.ended = !1, this.finished = !1, this.destroyed = !1;
    const j = !!(m && m.decodeStrings === !1);
    this.decodeStrings = !j, this.defaultEncoding = m && m.defaultEncoding || "utf8", this.length = 0, this.writing = !1, this.corked = 0, this.sync = !0, this.bufferProcessing = !1, this.onwrite = v.bind(void 0, c), this.writecb = null, this.writelen = 0, this.afterWriteTickInfo = null, J(this), this.pendingcb = 0, this.constructed = !0, this.prefinished = !1, this.errorEmitted = !1, this.emitClose = !m || m.emitClose !== !1, this.autoDestroy = !m || m.autoDestroy !== !1, this.errored = null, this.closed = !1, this.closeEmitted = !1, this[F] = [];
  }
  function J(m) {
    m.buffered = [], m.bufferedIndex = 0, m.allBuffers = !0, m.allNoop = !0;
  }
  T.prototype.getBuffer = function() {
    return e(this.buffered, this.bufferedIndex);
  }, a(T.prototype, "bufferedRequestCount", {
    __proto__: null,
    get() {
      return this.buffered.length - this.bufferedIndex;
    }
  });
  function re(m) {
    const c = this instanceof Kt();
    if (!c && !o(re, this)) return new re(m);
    this._writableState = new T(m, this, c), m && (typeof m.write == "function" && (this._write = m.write), typeof m.writev == "function" && (this._writev = m.writev), typeof m.destroy == "function" && (this._destroy = m.destroy), typeof m.final == "function" && (this._final = m.final), typeof m.construct == "function" && (this._construct = m.construct), m.signal && I(m.signal, this)), y.call(this, m), w.construct(this, () => {
      const E = this._writableState;
      E.writing || le(this, E), P(this, E);
    });
  }
  a(re, f, {
    __proto__: null,
    value: function(m) {
      return o(this, m) ? !0 : this !== re ? !1 : m && m._writableState instanceof T;
    }
  }), re.prototype.pipe = function() {
    x(this, new C());
  };
  function se(m, c, E, j) {
    const G = m._writableState;
    if (typeof E == "function")
      j = E, E = G.defaultEncoding;
    else {
      if (!E) E = G.defaultEncoding;
      else if (E !== "buffer" && !R.isEncoding(E)) throw new U(E);
      typeof j != "function" && (j = D);
    }
    if (c === null)
      throw new k();
    if (!G.objectMode)
      if (typeof c == "string")
        G.decodeStrings !== !1 && (c = R.from(c, E), E = "buffer");
      else if (c instanceof R)
        E = "buffer";
      else if (y._isUint8Array(c))
        c = y._uint8ArrayToBuffer(c), E = "buffer";
      else
        throw new g("chunk", ["string", "Buffer", "Uint8Array"], c);
    let K;
    return G.ending ? K = new L() : G.destroyed && (K = new N("write")), K ? (t.nextTick(j, K), x(m, K, !0), K) : (G.pendingcb++, ne(m, G, c, E, j));
  }
  re.prototype.write = function(m, c, E) {
    return se(this, m, c, E) === !0;
  }, re.prototype.cork = function() {
    this._writableState.corked++;
  }, re.prototype.uncork = function() {
    const m = this._writableState;
    m.corked && (m.corked--, m.writing || le(this, m));
  }, re.prototype.setDefaultEncoding = function(c) {
    if (typeof c == "string" && (c = l(c)), !R.isEncoding(c)) throw new U(c);
    return this._writableState.defaultEncoding = c, this;
  };
  function ne(m, c, E, j, G) {
    const K = c.objectMode ? 1 : E.length;
    c.length += K;
    const O = c.length < c.highWaterMark;
    return O || (c.needDrain = !0), c.writing || c.corked || c.errored || !c.constructed ? (c.buffered.push({
      chunk: E,
      encoding: j,
      callback: G
    }), c.allBuffers && j !== "buffer" && (c.allBuffers = !1), c.allNoop && G !== D && (c.allNoop = !1)) : (c.writelen = K, c.writecb = G, c.writing = !0, c.sync = !0, m._write(E, j, c.onwrite), c.sync = !1), O && !c.errored && !c.destroyed;
  }
  function M(m, c, E, j, G, K, O) {
    c.writelen = j, c.writecb = O, c.writing = !0, c.sync = !0, c.destroyed ? c.onwrite(new N("write")) : E ? m._writev(G, c.onwrite) : m._write(G, K, c.onwrite), c.sync = !1;
  }
  function Q(m, c, E, j) {
    --c.pendingcb, j(E), B(c), x(m, E);
  }
  function v(m, c) {
    const E = m._writableState, j = E.sync, G = E.writecb;
    if (typeof G != "function") {
      x(m, new b());
      return;
    }
    E.writing = !1, E.writecb = null, E.length -= E.writelen, E.writelen = 0, c ? (c.stack, E.errored || (E.errored = c), m._readableState && !m._readableState.errored && (m._readableState.errored = c), j ? t.nextTick(Q, m, E, c, G) : Q(m, E, c, G)) : (E.buffered.length > E.bufferedIndex && le(m, E), j ? E.afterWriteTickInfo !== null && E.afterWriteTickInfo.cb === G ? E.afterWriteTickInfo.count++ : (E.afterWriteTickInfo = {
      count: 1,
      cb: G,
      stream: m,
      state: E
    }, t.nextTick($, E.afterWriteTickInfo)) : ee(m, E, 1, G));
  }
  function $({ stream: m, state: c, count: E, cb: j }) {
    return c.afterWriteTickInfo = null, ee(m, c, E, j);
  }
  function ee(m, c, E, j) {
    for (!c.ending && !m.destroyed && c.length === 0 && c.needDrain && (c.needDrain = !1, m.emit("drain")); E-- > 0; )
      c.pendingcb--, j();
    c.destroyed && B(c), P(m, c);
  }
  function B(m) {
    if (m.writing)
      return;
    for (let G = m.bufferedIndex; G < m.buffered.length; ++G) {
      var c;
      const { chunk: K, callback: O } = m.buffered[G], H = m.objectMode ? 1 : K.length;
      m.length -= H, O(
        (c = m.errored) !== null && c !== void 0 ? c : new N("write")
      );
    }
    const E = m[F].splice(0);
    for (let G = 0; G < E.length; G++) {
      var j;
      E[G](
        (j = m.errored) !== null && j !== void 0 ? j : new N("end")
      );
    }
    J(m);
  }
  function le(m, c) {
    if (c.corked || c.bufferProcessing || c.destroyed || !c.constructed)
      return;
    const { buffered: E, bufferedIndex: j, objectMode: G } = c, K = E.length - j;
    if (!K)
      return;
    let O = j;
    if (c.bufferProcessing = !0, K > 1 && m._writev) {
      c.pendingcb -= K - 1;
      const H = c.allNoop ? D : (z) => {
        for (let de = O; de < E.length; ++de)
          E[de].callback(z);
      }, te = c.allNoop && O === 0 ? E : e(E, O);
      te.allBuffers = c.allBuffers, M(m, c, !0, c.length, te, "", H), J(c);
    } else {
      do {
        const { chunk: H, encoding: te, callback: z } = E[O];
        E[O++] = null;
        const de = G ? 1 : H.length;
        M(m, c, !1, de, H, te, z);
      } while (O < E.length && !c.writing);
      O === E.length ? J(c) : O > 256 ? (E.splice(0, O), c.bufferedIndex = 0) : c.bufferedIndex = O;
    }
    c.bufferProcessing = !1;
  }
  re.prototype._write = function(m, c, E) {
    if (this._writev)
      this._writev(
        [
          {
            chunk: m,
            encoding: c
          }
        ],
        E
      );
    else
      throw new p("_write()");
  }, re.prototype._writev = null, re.prototype.end = function(m, c, E) {
    const j = this._writableState;
    typeof m == "function" ? (E = m, m = null, c = null) : typeof c == "function" && (E = c, c = null);
    let G;
    if (m != null) {
      const K = se(this, m, c);
      K instanceof r && (G = K);
    }
    return j.corked && (j.corked = 1, this.uncork()), G || (!j.errored && !j.ending ? (j.ending = !0, P(this, j, !0), j.ended = !0) : j.finished ? G = new W("end") : j.destroyed && (G = new N("end"))), typeof E == "function" && (G || j.finished ? t.nextTick(E, G) : j[F].push(E)), this;
  };
  function ae(m) {
    return m.ending && !m.destroyed && m.constructed && m.length === 0 && !m.errored && m.buffered.length === 0 && !m.finished && !m.writing && !m.errorEmitted && !m.closeEmitted;
  }
  function ce(m, c) {
    let E = !1;
    function j(G) {
      if (E) {
        x(m, G ?? b());
        return;
      }
      if (E = !0, c.pendingcb--, G) {
        const K = c[F].splice(0);
        for (let O = 0; O < K.length; O++)
          K[O](G);
        x(m, G, c.sync);
      } else ae(c) && (c.prefinished = !0, m.emit("prefinish"), c.pendingcb++, t.nextTick(q, m, c));
    }
    c.sync = !0, c.pendingcb++;
    try {
      m._final(j);
    } catch (G) {
      j(G);
    }
    c.sync = !1;
  }
  function Y(m, c) {
    !c.prefinished && !c.finalCalled && (typeof m._final == "function" && !c.destroyed ? (c.finalCalled = !0, ce(m, c)) : (c.prefinished = !0, m.emit("prefinish")));
  }
  function P(m, c, E) {
    ae(c) && (Y(m, c), c.pendingcb === 0 && (E ? (c.pendingcb++, t.nextTick(
      (j, G) => {
        ae(G) ? q(j, G) : G.pendingcb--;
      },
      m,
      c
    )) : ae(c) && (c.pendingcb++, q(m, c))));
  }
  function q(m, c) {
    c.pendingcb--, c.finished = !0;
    const E = c[F].splice(0);
    for (let j = 0; j < E.length; j++)
      E[j]();
    if (m.emit("finish"), c.autoDestroy) {
      const j = m._readableState;
      (!j || j.autoDestroy && // We don't expect the readable to ever 'end'
      // if readable is explicitly set to false.
      (j.endEmitted || j.readable === !1)) && m.destroy();
    }
  }
  n(re.prototype, {
    closed: {
      __proto__: null,
      get() {
        return this._writableState ? this._writableState.closed : !1;
      }
    },
    destroyed: {
      __proto__: null,
      get() {
        return this._writableState ? this._writableState.destroyed : !1;
      },
      set(m) {
        this._writableState && (this._writableState.destroyed = m);
      }
    },
    writable: {
      __proto__: null,
      get() {
        const m = this._writableState;
        return !!m && m.writable !== !1 && !m.destroyed && !m.errored && !m.ending && !m.ended;
      },
      set(m) {
        this._writableState && (this._writableState.writable = !!m);
      }
    },
    writableFinished: {
      __proto__: null,
      get() {
        return this._writableState ? this._writableState.finished : !1;
      }
    },
    writableObjectMode: {
      __proto__: null,
      get() {
        return this._writableState ? this._writableState.objectMode : !1;
      }
    },
    writableBuffer: {
      __proto__: null,
      get() {
        return this._writableState && this._writableState.getBuffer();
      }
    },
    writableEnded: {
      __proto__: null,
      get() {
        return this._writableState ? this._writableState.ending : !1;
      }
    },
    writableNeedDrain: {
      __proto__: null,
      get() {
        const m = this._writableState;
        return m ? !m.destroyed && !m.ending && m.needDrain : !1;
      }
    },
    writableHighWaterMark: {
      __proto__: null,
      get() {
        return this._writableState && this._writableState.highWaterMark;
      }
    },
    writableCorked: {
      __proto__: null,
      get() {
        return this._writableState ? this._writableState.corked : 0;
      }
    },
    writableLength: {
      __proto__: null,
      get() {
        return this._writableState && this._writableState.length;
      }
    },
    errored: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._writableState ? this._writableState.errored : null;
      }
    },
    writableAborted: {
      __proto__: null,
      enumerable: !1,
      get: function() {
        return !!(this._writableState.writable !== !1 && (this._writableState.destroyed || this._writableState.errored) && !this._writableState.finished);
      }
    }
  });
  const V = w.destroy;
  re.prototype.destroy = function(m, c) {
    const E = this._writableState;
    return !E.destroyed && (E.bufferedIndex < E.buffered.length || E[F].length) && t.nextTick(B, E), V.call(this, m, c), this;
  }, re.prototype._undestroy = w.undestroy, re.prototype._destroy = function(m, c) {
    c(m);
  }, re.prototype[d.captureRejectionSymbol] = function(m) {
    this.destroy(m);
  };
  let Z;
  function X() {
    return Z === void 0 && (Z = {}), Z;
  }
  return re.fromWeb = function(m, c) {
    return X().newStreamWritableFromWritableStream(m, c);
  }, re.toWeb = function(m) {
    return X().newWritableStreamFromStreamWritable(m);
  }, Za;
}
var Ya, sg;
function LR() {
  if (sg) return Ya;
  sg = 1;
  const t = je(), e = Fe, {
    isReadable: r,
    isWritable: o,
    isIterable: a,
    isNodeStream: n,
    isReadableNodeStream: i,
    isWritableNodeStream: l,
    isDuplexNodeStream: s,
    isReadableStream: f,
    isWritableStream: d
  } = nr(), y = pr(), {
    AbortError: R,
    codes: { ERR_INVALID_ARG_TYPE: w, ERR_INVALID_RETURN_VALUE: I }
  } = ut(), { destroyer: A } = $r(), u = Kt(), g = Xi(), p = Lc(), { createDeferredPromise: b } = pt(), C = Fm(), N = globalThis.Blob || e.Blob, W = typeof N < "u" ? function(T) {
    return T instanceof N;
  } : function(T) {
    return !1;
  }, k = globalThis.AbortController || et.AbortController, { FunctionPrototypeCall: L } = ze();
  class U extends u {
    constructor(T) {
      super(T), T?.readable === !1 && (this._readableState.readable = !1, this._readableState.ended = !0, this._readableState.endEmitted = !0), T?.writable === !1 && (this._writableState.writable = !1, this._writableState.ending = !0, this._writableState.ended = !0, this._writableState.finished = !0);
    }
  }
  Ya = function F(T, J) {
    if (s(T))
      return T;
    if (i(T))
      return D({
        readable: T
      });
    if (l(T))
      return D({
        writable: T
      });
    if (n(T))
      return D({
        writable: !1,
        readable: !1
      });
    if (f(T))
      return D({
        readable: g.fromWeb(T)
      });
    if (d(T))
      return D({
        writable: p.fromWeb(T)
      });
    if (typeof T == "function") {
      const { value: se, write: ne, final: M, destroy: Q } = x(T);
      if (a(se))
        return C(U, se, {
          // TODO (ronag): highWaterMark?
          objectMode: !0,
          write: ne,
          final: M,
          destroy: Q
        });
      const v = se?.then;
      if (typeof v == "function") {
        let $;
        const ee = L(
          v,
          se,
          (B) => {
            if (B != null)
              throw new I("nully", "body", B);
          },
          (B) => {
            A($, B);
          }
        );
        return $ = new U({
          // TODO (ronag): highWaterMark?
          objectMode: !0,
          readable: !1,
          write: ne,
          final(B) {
            M(async () => {
              try {
                await ee, t.nextTick(B, null);
              } catch (le) {
                t.nextTick(B, le);
              }
            });
          },
          destroy: Q
        });
      }
      throw new I("Iterable, AsyncIterable or AsyncFunction", J, se);
    }
    if (W(T))
      return F(T.arrayBuffer());
    if (a(T))
      return C(U, T, {
        // TODO (ronag): highWaterMark?
        objectMode: !0,
        writable: !1
      });
    if (f(T?.readable) && d(T?.writable))
      return U.fromWeb(T);
    if (typeof T?.writable == "object" || typeof T?.readable == "object") {
      const se = T != null && T.readable ? i(T?.readable) ? T?.readable : F(T.readable) : void 0, ne = T != null && T.writable ? l(T?.writable) ? T?.writable : F(T.writable) : void 0;
      return D({
        readable: se,
        writable: ne
      });
    }
    const re = T?.then;
    if (typeof re == "function") {
      let se;
      return L(
        re,
        T,
        (ne) => {
          ne != null && se.push(ne), se.push(null);
        },
        (ne) => {
          A(se, ne);
        }
      ), se = new U({
        objectMode: !0,
        writable: !1,
        read() {
        }
      });
    }
    throw new w(
      J,
      [
        "Blob",
        "ReadableStream",
        "WritableStream",
        "Stream",
        "Iterable",
        "AsyncIterable",
        "Function",
        "{ readable, writable } pair",
        "Promise"
      ],
      T
    );
  };
  function x(F) {
    let { promise: T, resolve: J } = b();
    const re = new k(), se = re.signal;
    return {
      value: F(
        (async function* () {
          for (; ; ) {
            const M = T;
            T = null;
            const { chunk: Q, done: v, cb: $ } = await M;
            if (t.nextTick($), v) return;
            if (se.aborted)
              throw new R(void 0, {
                cause: se.reason
              });
            ({ promise: T, resolve: J } = b()), yield Q;
          }
        })(),
        {
          signal: se
        }
      ),
      write(M, Q, v) {
        const $ = J;
        J = null, $({
          chunk: M,
          done: !1,
          cb: v
        });
      },
      final(M) {
        const Q = J;
        J = null, Q({
          done: !0,
          cb: M
        });
      },
      destroy(M, Q) {
        re.abort(), Q(M);
      }
    };
  }
  function D(F) {
    const T = F.readable && typeof F.readable.read != "function" ? g.wrap(F.readable) : F.readable, J = F.writable;
    let re = !!r(T), se = !!o(J), ne, M, Q, v, $;
    function ee(B) {
      const le = v;
      v = null, le ? le(B) : B && $.destroy(B);
    }
    return $ = new U({
      // TODO (ronag): highWaterMark?
      readableObjectMode: !!(T != null && T.readableObjectMode),
      writableObjectMode: !!(J != null && J.writableObjectMode),
      readable: re,
      writable: se
    }), se && (y(J, (B) => {
      se = !1, B && A(T, B), ee(B);
    }), $._write = function(B, le, ae) {
      J.write(B, le) ? ae() : ne = ae;
    }, $._final = function(B) {
      J.end(), M = B;
    }, J.on("drain", function() {
      if (ne) {
        const B = ne;
        ne = null, B();
      }
    }), J.on("finish", function() {
      if (M) {
        const B = M;
        M = null, B();
      }
    })), re && (y(T, (B) => {
      re = !1, B && A(T, B), ee(B);
    }), T.on("readable", function() {
      if (Q) {
        const B = Q;
        Q = null, B();
      }
    }), T.on("end", function() {
      $.push(null);
    }), $._read = function() {
      for (; ; ) {
        const B = T.read();
        if (B === null) {
          Q = $._read;
          return;
        }
        if (!$.push(B))
          return;
      }
    }), $._destroy = function(B, le) {
      !B && v !== null && (B = new R()), Q = null, ne = null, M = null, v === null ? le(B) : (v = le, A(J, B), A(T, B));
    }, $;
  }
  return Ya;
}
var Ka, og;
function Kt() {
  if (og) return Ka;
  og = 1;
  const {
    ObjectDefineProperties: t,
    ObjectGetOwnPropertyDescriptor: e,
    ObjectKeys: r,
    ObjectSetPrototypeOf: o
  } = ze();
  Ka = i;
  const a = Xi(), n = Lc();
  o(i.prototype, a.prototype), o(i, a);
  {
    const d = r(n.prototype);
    for (let y = 0; y < d.length; y++) {
      const R = d[y];
      i.prototype[R] || (i.prototype[R] = n.prototype[R]);
    }
  }
  function i(d) {
    if (!(this instanceof i)) return new i(d);
    a.call(this, d), n.call(this, d), d ? (this.allowHalfOpen = d.allowHalfOpen !== !1, d.readable === !1 && (this._readableState.readable = !1, this._readableState.ended = !0, this._readableState.endEmitted = !0), d.writable === !1 && (this._writableState.writable = !1, this._writableState.ending = !0, this._writableState.ended = !0, this._writableState.finished = !0)) : this.allowHalfOpen = !0;
  }
  t(i.prototype, {
    writable: {
      __proto__: null,
      ...e(n.prototype, "writable")
    },
    writableHighWaterMark: {
      __proto__: null,
      ...e(n.prototype, "writableHighWaterMark")
    },
    writableObjectMode: {
      __proto__: null,
      ...e(n.prototype, "writableObjectMode")
    },
    writableBuffer: {
      __proto__: null,
      ...e(n.prototype, "writableBuffer")
    },
    writableLength: {
      __proto__: null,
      ...e(n.prototype, "writableLength")
    },
    writableFinished: {
      __proto__: null,
      ...e(n.prototype, "writableFinished")
    },
    writableCorked: {
      __proto__: null,
      ...e(n.prototype, "writableCorked")
    },
    writableEnded: {
      __proto__: null,
      ...e(n.prototype, "writableEnded")
    },
    writableNeedDrain: {
      __proto__: null,
      ...e(n.prototype, "writableNeedDrain")
    },
    destroyed: {
      __proto__: null,
      get() {
        return this._readableState === void 0 || this._writableState === void 0 ? !1 : this._readableState.destroyed && this._writableState.destroyed;
      },
      set(d) {
        this._readableState && this._writableState && (this._readableState.destroyed = d, this._writableState.destroyed = d);
      }
    }
  });
  let l;
  function s() {
    return l === void 0 && (l = {}), l;
  }
  i.fromWeb = function(d, y) {
    return s().newStreamDuplexFromReadableWritablePair(d, y);
  }, i.toWeb = function(d) {
    return s().newReadableWritablePairFromDuplex(d);
  };
  let f;
  return i.from = function(d) {
    return f || (f = LR()), f(d, "body");
  }, Ka;
}
var Ja, ag;
function jm() {
  if (ag) return Ja;
  ag = 1;
  const { ObjectSetPrototypeOf: t, Symbol: e } = ze();
  Ja = i;
  const { ERR_METHOD_NOT_IMPLEMENTED: r } = ut().codes, o = Kt(), { getHighWaterMark: a } = Ki();
  t(i.prototype, o.prototype), t(i, o);
  const n = e("kCallback");
  function i(f) {
    if (!(this instanceof i)) return new i(f);
    const d = f ? a(this, f, "readableHighWaterMark", !0) : null;
    d === 0 && (f = {
      ...f,
      highWaterMark: null,
      readableHighWaterMark: d,
      // TODO (ronag): 0 is not optimal since we have
      // a "bug" where we check needDrain before calling _write and not after.
      // Refs: https://github.com/nodejs/node/pull/32887
      // Refs: https://github.com/nodejs/node/pull/35941
      writableHighWaterMark: f.writableHighWaterMark || 0
    }), o.call(this, f), this._readableState.sync = !1, this[n] = null, f && (typeof f.transform == "function" && (this._transform = f.transform), typeof f.flush == "function" && (this._flush = f.flush)), this.on("prefinish", s);
  }
  function l(f) {
    typeof this._flush == "function" && !this.destroyed ? this._flush((d, y) => {
      if (d) {
        f ? f(d) : this.destroy(d);
        return;
      }
      y != null && this.push(y), this.push(null), f && f();
    }) : (this.push(null), f && f());
  }
  function s() {
    this._final !== l && l.call(this);
  }
  return i.prototype._final = l, i.prototype._transform = function(f, d, y) {
    throw new r("_transform()");
  }, i.prototype._write = function(f, d, y) {
    const R = this._readableState, w = this._writableState, I = R.length;
    this._transform(f, d, (A, u) => {
      if (A) {
        y(A);
        return;
      }
      u != null && this.push(u), w.ended || // Backwards compat.
      I === R.length || // Backwards compat.
      R.length < R.highWaterMark ? y() : this[n] = y;
    });
  }, i.prototype._read = function() {
    if (this[n]) {
      const f = this[n];
      this[n] = null, f();
    }
  }, Ja;
}
var Xa, lg;
function $m() {
  if (lg) return Xa;
  lg = 1;
  const { ObjectSetPrototypeOf: t } = ze();
  Xa = r;
  const e = jm();
  t(r.prototype, e.prototype), t(r, e);
  function r(o) {
    if (!(this instanceof r)) return new r(o);
    e.call(this, o);
  }
  return r.prototype._transform = function(o, a, n) {
    n(null, o);
  }, Xa;
}
var Qa, ug;
function Pc() {
  if (ug) return Qa;
  ug = 1;
  const t = je(), { ArrayIsArray: e, Promise: r, SymbolAsyncIterator: o, SymbolDispose: a } = ze(), n = pr(), { once: i } = pt(), l = $r(), s = Kt(), {
    aggregateTwoErrors: f,
    codes: {
      ERR_INVALID_ARG_TYPE: d,
      ERR_INVALID_RETURN_VALUE: y,
      ERR_MISSING_ARGS: R,
      ERR_STREAM_DESTROYED: w,
      ERR_STREAM_PREMATURE_CLOSE: I
    },
    AbortError: A
  } = ut(), { validateFunction: u, validateAbortSignal: g } = An(), {
    isIterable: p,
    isReadable: b,
    isReadableNodeStream: C,
    isNodeStream: N,
    isTransformStream: W,
    isWebStream: k,
    isReadableStream: L,
    isReadableFinished: U
  } = nr(), x = globalThis.AbortController || et.AbortController;
  let D, F, T;
  function J(B, le, ae) {
    let ce = !1;
    B.on("close", () => {
      ce = !0;
    });
    const Y = n(
      B,
      {
        readable: le,
        writable: ae
      },
      (P) => {
        ce = !P;
      }
    );
    return {
      destroy: (P) => {
        ce || (ce = !0, l.destroyer(B, P || new w("pipe")));
      },
      cleanup: Y
    };
  }
  function re(B) {
    return u(B[B.length - 1], "streams[stream.length - 1]"), B.pop();
  }
  function se(B) {
    if (p(B))
      return B;
    if (C(B))
      return ne(B);
    throw new d("val", ["Readable", "Iterable", "AsyncIterable"], B);
  }
  async function* ne(B) {
    F || (F = Xi()), yield* F.prototype[o].call(B);
  }
  async function M(B, le, ae, { end: ce }) {
    let Y, P = null;
    const q = (X) => {
      if (X && (Y = X), P) {
        const m = P;
        P = null, m();
      }
    }, V = () => new r((X, m) => {
      Y ? m(Y) : P = () => {
        Y ? m(Y) : X();
      };
    });
    le.on("drain", q);
    const Z = n(
      le,
      {
        readable: !1
      },
      q
    );
    try {
      le.writableNeedDrain && await V();
      for await (const X of B)
        le.write(X) || await V();
      ce && (le.end(), await V()), ae();
    } catch (X) {
      ae(Y !== X ? f(Y, X) : X);
    } finally {
      Z(), le.off("drain", q);
    }
  }
  async function Q(B, le, ae, { end: ce }) {
    W(le) && (le = le.writable);
    const Y = le.getWriter();
    try {
      for await (const P of B)
        await Y.ready, Y.write(P).catch(() => {
        });
      await Y.ready, ce && await Y.close(), ae();
    } catch (P) {
      try {
        await Y.abort(P), ae(P);
      } catch (q) {
        ae(q);
      }
    }
  }
  function v(...B) {
    return $(B, i(re(B)));
  }
  function $(B, le, ae) {
    if (B.length === 1 && e(B[0]) && (B = B[0]), B.length < 2)
      throw new R("streams");
    const ce = new x(), Y = ce.signal, P = ae?.signal, q = [];
    g(P, "options.signal");
    function V() {
      G(new A());
    }
    T = T || pt().addAbortListener;
    let Z;
    P && (Z = T(P, V));
    let X, m;
    const c = [];
    let E = 0;
    function j(te) {
      G(te, --E === 0);
    }
    function G(te, z) {
      var de;
      if (te && (!X || X.code === "ERR_STREAM_PREMATURE_CLOSE") && (X = te), !(!X && !z)) {
        for (; c.length; )
          c.shift()(X);
        (de = Z) === null || de === void 0 || de[a](), ce.abort(), z && (X || q.forEach((pe) => pe()), t.nextTick(le, X, m));
      }
    }
    let K;
    for (let te = 0; te < B.length; te++) {
      const z = B[te], de = te < B.length - 1, pe = te > 0, he = de || ae?.end !== !1, we = te === B.length - 1;
      if (N(z)) {
        let be = function(_e) {
          _e && _e.name !== "AbortError" && _e.code !== "ERR_STREAM_PREMATURE_CLOSE" && j(_e);
        };
        if (he) {
          const { destroy: _e, cleanup: Ae } = J(z, de, pe);
          c.push(_e), b(z) && we && q.push(Ae);
        }
        z.on("error", be), b(z) && we && q.push(() => {
          z.removeListener("error", be);
        });
      }
      if (te === 0)
        if (typeof z == "function") {
          if (K = z({
            signal: Y
          }), !p(K))
            throw new y("Iterable, AsyncIterable or Stream", "source", K);
        } else p(z) || C(z) || W(z) ? K = z : K = s.from(z);
      else if (typeof z == "function") {
        if (W(K)) {
          var O;
          K = se((O = K) === null || O === void 0 ? void 0 : O.readable);
        } else
          K = se(K);
        if (K = z(K, {
          signal: Y
        }), de) {
          if (!p(K, !0))
            throw new y("AsyncIterable", `transform[${te - 1}]`, K);
        } else {
          var H;
          D || (D = $m());
          const be = new D({
            objectMode: !0
          }), _e = (H = K) === null || H === void 0 ? void 0 : H.then;
          if (typeof _e == "function")
            E++, _e.call(
              K,
              (me) => {
                m = me, me != null && be.write(me), he && be.end(), t.nextTick(j);
              },
              (me) => {
                be.destroy(me), t.nextTick(j, me);
              }
            );
          else if (p(K, !0))
            E++, M(K, be, j, {
              end: he
            });
          else if (L(K) || W(K)) {
            const me = K.readable || K;
            E++, M(me, be, j, {
              end: he
            });
          } else
            throw new y("AsyncIterable or Promise", "destination", K);
          K = be;
          const { destroy: Ae, cleanup: xe } = J(K, !1, !0);
          c.push(Ae), we && q.push(xe);
        }
      } else if (N(z)) {
        if (C(K)) {
          E += 2;
          const be = ee(K, z, j, {
            end: he
          });
          b(z) && we && q.push(be);
        } else if (W(K) || L(K)) {
          const be = K.readable || K;
          E++, M(be, z, j, {
            end: he
          });
        } else if (p(K))
          E++, M(K, z, j, {
            end: he
          });
        else
          throw new d(
            "val",
            ["Readable", "Iterable", "AsyncIterable", "ReadableStream", "TransformStream"],
            K
          );
        K = z;
      } else if (k(z)) {
        if (C(K))
          E++, Q(se(K), z, j, {
            end: he
          });
        else if (L(K) || p(K))
          E++, Q(K, z, j, {
            end: he
          });
        else if (W(K))
          E++, Q(K.readable, z, j, {
            end: he
          });
        else
          throw new d(
            "val",
            ["Readable", "Iterable", "AsyncIterable", "ReadableStream", "TransformStream"],
            K
          );
        K = z;
      } else
        K = s.from(z);
    }
    return (Y != null && Y.aborted || P != null && P.aborted) && t.nextTick(V), K;
  }
  function ee(B, le, ae, { end: ce }) {
    let Y = !1;
    if (le.on("close", () => {
      Y || ae(new I());
    }), B.pipe(le, {
      end: !1
    }), ce) {
      let P = function() {
        Y = !0, le.end();
      };
      U(B) ? t.nextTick(P) : B.once("end", P);
    } else
      ae();
    return n(
      B,
      {
        readable: !0,
        writable: !1
      },
      (P) => {
        const q = B._readableState;
        P && P.code === "ERR_STREAM_PREMATURE_CLOSE" && q && q.ended && !q.errored && !q.errorEmitted ? B.once("end", ae).once("error", ae) : ae(P);
      }
    ), n(
      le,
      {
        readable: !1,
        writable: !0
      },
      ae
    );
  }
  return Qa = {
    pipelineImpl: $,
    pipeline: v
  }, Qa;
}
var el, fg;
function Wm() {
  if (fg) return el;
  fg = 1;
  const { pipeline: t } = Pc(), e = Kt(), { destroyer: r } = $r(), {
    isNodeStream: o,
    isReadable: a,
    isWritable: n,
    isWebStream: i,
    isTransformStream: l,
    isWritableStream: s,
    isReadableStream: f
  } = nr(), {
    AbortError: d,
    codes: { ERR_INVALID_ARG_VALUE: y, ERR_MISSING_ARGS: R }
  } = ut(), w = pr();
  return el = function(...A) {
    if (A.length === 0)
      throw new R("streams");
    if (A.length === 1)
      return e.from(A[0]);
    const u = [...A];
    if (typeof A[0] == "function" && (A[0] = e.from(A[0])), typeof A[A.length - 1] == "function") {
      const D = A.length - 1;
      A[D] = e.from(A[D]);
    }
    for (let D = 0; D < A.length; ++D)
      if (!(!o(A[D]) && !i(A[D]))) {
        if (D < A.length - 1 && !(a(A[D]) || f(A[D]) || l(A[D])))
          throw new y(`streams[${D}]`, u[D], "must be readable");
        if (D > 0 && !(n(A[D]) || s(A[D]) || l(A[D])))
          throw new y(`streams[${D}]`, u[D], "must be writable");
      }
    let g, p, b, C, N;
    function W(D) {
      const F = C;
      C = null, F ? F(D) : D ? N.destroy(D) : !x && !U && N.destroy();
    }
    const k = A[0], L = t(A, W), U = !!(n(k) || s(k) || l(k)), x = !!(a(L) || f(L) || l(L));
    if (N = new e({
      // TODO (ronag): highWaterMark?
      writableObjectMode: !!(k != null && k.writableObjectMode),
      readableObjectMode: !!(L != null && L.readableObjectMode),
      writable: U,
      readable: x
    }), U) {
      if (o(k))
        N._write = function(F, T, J) {
          k.write(F, T) ? J() : g = J;
        }, N._final = function(F) {
          k.end(), p = F;
        }, k.on("drain", function() {
          if (g) {
            const F = g;
            g = null, F();
          }
        });
      else if (i(k)) {
        const T = (l(k) ? k.writable : k).getWriter();
        N._write = async function(J, re, se) {
          try {
            await T.ready, T.write(J).catch(() => {
            }), se();
          } catch (ne) {
            se(ne);
          }
        }, N._final = async function(J) {
          try {
            await T.ready, T.close().catch(() => {
            }), p = J;
          } catch (re) {
            J(re);
          }
        };
      }
      const D = l(L) ? L.readable : L;
      w(D, () => {
        if (p) {
          const F = p;
          p = null, F();
        }
      });
    }
    if (x) {
      if (o(L))
        L.on("readable", function() {
          if (b) {
            const D = b;
            b = null, D();
          }
        }), L.on("end", function() {
          N.push(null);
        }), N._read = function() {
          for (; ; ) {
            const D = L.read();
            if (D === null) {
              b = N._read;
              return;
            }
            if (!N.push(D))
              return;
          }
        };
      else if (i(L)) {
        const F = (l(L) ? L.readable : L).getReader();
        N._read = async function() {
          for (; ; )
            try {
              const { value: T, done: J } = await F.read();
              if (!N.push(T))
                return;
              if (J) {
                N.push(null);
                return;
              }
            } catch {
              return;
            }
        };
      }
    }
    return N._destroy = function(D, F) {
      !D && C !== null && (D = new d()), b = null, g = null, p = null, C === null ? F(D) : (C = F, o(L) && r(L, D));
    }, N;
  }, el;
}
var cg;
function PR() {
  if (cg) return Gn;
  cg = 1;
  const t = globalThis.AbortController || et.AbortController, {
    codes: { ERR_INVALID_ARG_VALUE: e, ERR_INVALID_ARG_TYPE: r, ERR_MISSING_ARGS: o, ERR_OUT_OF_RANGE: a },
    AbortError: n
  } = ut(), { validateAbortSignal: i, validateInteger: l, validateObject: s } = An(), f = ze().Symbol("kWeak"), d = ze().Symbol("kResistStopPropagation"), { finished: y } = pr(), R = Wm(), { addAbortSignalNoValidate: w } = Yi(), { isWritable: I, isNodeStream: A } = nr(), { deprecate: u } = pt(), {
    ArrayPrototypePush: g,
    Boolean: p,
    MathFloor: b,
    Number: C,
    NumberIsNaN: N,
    Promise: W,
    PromiseReject: k,
    PromiseResolve: L,
    PromisePrototypeThen: U,
    Symbol: x
  } = ze(), D = x("kEmpty"), F = x("kEof");
  function T(P, q) {
    if (q != null && s(q, "options"), q?.signal != null && i(q.signal, "options.signal"), A(P) && !I(P))
      throw new e("stream", P, "must be writable");
    const V = R(this, P);
    return q != null && q.signal && w(q.signal, V), V;
  }
  function J(P, q) {
    if (typeof P != "function")
      throw new r("fn", ["Function", "AsyncFunction"], P);
    q != null && s(q, "options"), q?.signal != null && i(q.signal, "options.signal");
    let V = 1;
    q?.concurrency != null && (V = b(q.concurrency));
    let Z = V - 1;
    return q?.highWaterMark != null && (Z = b(q.highWaterMark)), l(V, "options.concurrency", 1), l(Z, "options.highWaterMark", 0), Z += V, (async function* () {
      const m = pt().AbortSignalAny(
        [q?.signal].filter(p)
      ), c = this, E = [], j = {
        signal: m
      };
      let G, K, O = !1, H = 0;
      function te() {
        O = !0, z();
      }
      function z() {
        H -= 1, de();
      }
      function de() {
        K && !O && H < V && E.length < Z && (K(), K = null);
      }
      async function pe() {
        try {
          for await (let he of c) {
            if (O)
              return;
            if (m.aborted)
              throw new n();
            try {
              if (he = P(he, j), he === D)
                continue;
              he = L(he);
            } catch (we) {
              he = k(we);
            }
            H += 1, U(he, z, te), E.push(he), G && (G(), G = null), !O && (E.length >= Z || H >= V) && await new W((we) => {
              K = we;
            });
          }
          E.push(F);
        } catch (he) {
          const we = k(he);
          U(we, z, te), E.push(we);
        } finally {
          O = !0, G && (G(), G = null);
        }
      }
      pe();
      try {
        for (; ; ) {
          for (; E.length > 0; ) {
            const he = await E[0];
            if (he === F)
              return;
            if (m.aborted)
              throw new n();
            he !== D && (yield he), E.shift(), de();
          }
          await new W((he) => {
            G = he;
          });
        }
      } finally {
        O = !0, K && (K(), K = null);
      }
    }).call(this);
  }
  function re(P = void 0) {
    return P != null && s(P, "options"), P?.signal != null && i(P.signal, "options.signal"), (async function* () {
      let V = 0;
      for await (const X of this) {
        var Z;
        if (P != null && (Z = P.signal) !== null && Z !== void 0 && Z.aborted)
          throw new n({
            cause: P.signal.reason
          });
        yield [V++, X];
      }
    }).call(this);
  }
  async function se(P, q = void 0) {
    for await (const V of v.call(this, P, q))
      return !0;
    return !1;
  }
  async function ne(P, q = void 0) {
    if (typeof P != "function")
      throw new r("fn", ["Function", "AsyncFunction"], P);
    return !await se.call(
      this,
      async (...V) => !await P(...V),
      q
    );
  }
  async function M(P, q) {
    for await (const V of v.call(this, P, q))
      return V;
  }
  async function Q(P, q) {
    if (typeof P != "function")
      throw new r("fn", ["Function", "AsyncFunction"], P);
    async function V(Z, X) {
      return await P(Z, X), D;
    }
    for await (const Z of J.call(this, V, q)) ;
  }
  function v(P, q) {
    if (typeof P != "function")
      throw new r("fn", ["Function", "AsyncFunction"], P);
    async function V(Z, X) {
      return await P(Z, X) ? Z : D;
    }
    return J.call(this, V, q);
  }
  class $ extends o {
    constructor() {
      super("reduce"), this.message = "Reduce of an empty stream requires an initial value";
    }
  }
  async function ee(P, q, V) {
    var Z;
    if (typeof P != "function")
      throw new r("reducer", ["Function", "AsyncFunction"], P);
    V != null && s(V, "options"), V?.signal != null && i(V.signal, "options.signal");
    let X = arguments.length > 1;
    if (V != null && (Z = V.signal) !== null && Z !== void 0 && Z.aborted) {
      const G = new n(void 0, {
        cause: V.signal.reason
      });
      throw this.once("error", () => {
      }), await y(this.destroy(G)), G;
    }
    const m = new t(), c = m.signal;
    if (V != null && V.signal) {
      const G = {
        once: !0,
        [f]: this,
        [d]: !0
      };
      V.signal.addEventListener("abort", () => m.abort(), G);
    }
    let E = !1;
    try {
      for await (const G of this) {
        var j;
        if (E = !0, V != null && (j = V.signal) !== null && j !== void 0 && j.aborted)
          throw new n();
        X ? q = await P(q, G, {
          signal: c
        }) : (q = G, X = !0);
      }
      if (!E && !X)
        throw new $();
    } finally {
      m.abort();
    }
    return q;
  }
  async function B(P) {
    P != null && s(P, "options"), P?.signal != null && i(P.signal, "options.signal");
    const q = [];
    for await (const Z of this) {
      var V;
      if (P != null && (V = P.signal) !== null && V !== void 0 && V.aborted)
        throw new n(void 0, {
          cause: P.signal.reason
        });
      g(q, Z);
    }
    return q;
  }
  function le(P, q) {
    const V = J.call(this, P, q);
    return (async function* () {
      for await (const X of V)
        yield* X;
    }).call(this);
  }
  function ae(P) {
    if (P = C(P), N(P))
      return 0;
    if (P < 0)
      throw new a("number", ">= 0", P);
    return P;
  }
  function ce(P, q = void 0) {
    return q != null && s(q, "options"), q?.signal != null && i(q.signal, "options.signal"), P = ae(P), (async function* () {
      var Z;
      if (q != null && (Z = q.signal) !== null && Z !== void 0 && Z.aborted)
        throw new n();
      for await (const m of this) {
        var X;
        if (q != null && (X = q.signal) !== null && X !== void 0 && X.aborted)
          throw new n();
        P-- <= 0 && (yield m);
      }
    }).call(this);
  }
  function Y(P, q = void 0) {
    return q != null && s(q, "options"), q?.signal != null && i(q.signal, "options.signal"), P = ae(P), (async function* () {
      var Z;
      if (q != null && (Z = q.signal) !== null && Z !== void 0 && Z.aborted)
        throw new n();
      for await (const m of this) {
        var X;
        if (q != null && (X = q.signal) !== null && X !== void 0 && X.aborted)
          throw new n();
        if (P-- > 0 && (yield m), P <= 0)
          return;
      }
    }).call(this);
  }
  return Gn.streamReturningOperators = {
    asIndexedPairs: u(re, "readable.asIndexedPairs will be removed in a future version."),
    drop: ce,
    filter: v,
    flatMap: le,
    map: J,
    take: Y,
    compose: T
  }, Gn.promiseReturningOperators = {
    every: ne,
    forEach: Q,
    reduce: ee,
    toArray: B,
    some: se,
    find: M
  }, Gn;
}
var tl, dg;
function qm() {
  if (dg) return tl;
  dg = 1;
  const { ArrayPrototypePop: t, Promise: e } = ze(), { isIterable: r, isNodeStream: o, isWebStream: a } = nr(), { pipelineImpl: n } = Pc(), { finished: i } = pr();
  Bm();
  function l(...s) {
    return new e((f, d) => {
      let y, R;
      const w = s[s.length - 1];
      if (w && typeof w == "object" && !o(w) && !r(w) && !a(w)) {
        const I = t(s);
        y = I.signal, R = I.end;
      }
      n(
        s,
        (I, A) => {
          I ? d(I) : f(A);
        },
        {
          signal: y,
          end: R
        }
      );
    });
  }
  return tl = {
    finished: i,
    pipeline: l
  }, tl;
}
var hg;
function Bm() {
  if (hg) return La.exports;
  hg = 1;
  const { Buffer: t } = Fe, { ObjectDefineProperty: e, ObjectKeys: r, ReflectApply: o } = ze(), {
    promisify: { custom: a }
  } = pt(), { streamReturningOperators: n, promiseReturningOperators: i } = PR(), {
    codes: { ERR_ILLEGAL_CONSTRUCTOR: l }
  } = ut(), s = Wm(), { setDefaultHighWaterMark: f, getDefaultHighWaterMark: d } = Ki(), { pipeline: y } = Pc(), { destroyer: R } = $r(), w = pr(), I = qm(), A = nr(), u = La.exports = Nc().Stream;
  u.isDestroyed = A.isDestroyed, u.isDisturbed = A.isDisturbed, u.isErrored = A.isErrored, u.isReadable = A.isReadable, u.isWritable = A.isWritable, u.Readable = Xi();
  for (const p of r(n)) {
    let C = function(...N) {
      if (new.target)
        throw l();
      return u.Readable.from(o(b, this, N));
    };
    const b = n[p];
    e(C, "name", {
      __proto__: null,
      value: b.name
    }), e(C, "length", {
      __proto__: null,
      value: b.length
    }), e(u.Readable.prototype, p, {
      __proto__: null,
      value: C,
      enumerable: !1,
      configurable: !0,
      writable: !0
    });
  }
  for (const p of r(i)) {
    let C = function(...N) {
      if (new.target)
        throw l();
      return o(b, this, N);
    };
    const b = i[p];
    e(C, "name", {
      __proto__: null,
      value: b.name
    }), e(C, "length", {
      __proto__: null,
      value: b.length
    }), e(u.Readable.prototype, p, {
      __proto__: null,
      value: C,
      enumerable: !1,
      configurable: !0,
      writable: !0
    });
  }
  u.Writable = Lc(), u.Duplex = Kt(), u.Transform = jm(), u.PassThrough = $m(), u.pipeline = y;
  const { addAbortSignal: g } = Yi();
  return u.addAbortSignal = g, u.finished = w, u.destroy = R, u.compose = s, u.setDefaultHighWaterMark = f, u.getDefaultHighWaterMark = d, e(u, "promises", {
    __proto__: null,
    configurable: !0,
    enumerable: !0,
    get() {
      return I;
    }
  }), e(y, a, {
    __proto__: null,
    enumerable: !0,
    get() {
      return I.pipeline;
    }
  }), e(w, a, {
    __proto__: null,
    enumerable: !0,
    get() {
      return I.finished;
    }
  }), u.Stream = u, u._isUint8Array = function(b) {
    return b instanceof Uint8Array;
  }, u._uint8ArrayToBuffer = function(b) {
    return t.from(b.buffer, b.byteOffset, b.byteLength);
  }, La.exports;
}
ni.exports;
var pg;
function CR() {
  return pg || (pg = 1, (function(t) {
    const e = tr;
    if (e && process.env.READABLE_STREAM === "disable") {
      const r = e.promises;
      t.exports._uint8ArrayToBuffer = e._uint8ArrayToBuffer, t.exports._isUint8Array = e._isUint8Array, t.exports.isDisturbed = e.isDisturbed, t.exports.isErrored = e.isErrored, t.exports.isReadable = e.isReadable, t.exports.Readable = e.Readable, t.exports.Writable = e.Writable, t.exports.Duplex = e.Duplex, t.exports.Transform = e.Transform, t.exports.PassThrough = e.PassThrough, t.exports.addAbortSignal = e.addAbortSignal, t.exports.finished = e.finished, t.exports.destroy = e.destroy, t.exports.pipeline = e.pipeline, t.exports.compose = e.compose, Object.defineProperty(e, "promises", {
        configurable: !0,
        enumerable: !0,
        get() {
          return r;
        }
      }), t.exports.Stream = e.Stream;
    } else {
      const r = Bm(), o = qm(), a = r.Readable.destroy;
      t.exports = r.Readable, t.exports._uint8ArrayToBuffer = r._uint8ArrayToBuffer, t.exports._isUint8Array = r._isUint8Array, t.exports.isDisturbed = r.isDisturbed, t.exports.isErrored = r.isErrored, t.exports.isReadable = r.isReadable, t.exports.Readable = r.Readable, t.exports.Writable = r.Writable, t.exports.Duplex = r.Duplex, t.exports.Transform = r.Transform, t.exports.PassThrough = r.PassThrough, t.exports.addAbortSignal = r.addAbortSignal, t.exports.finished = r.finished, t.exports.destroy = r.destroy, t.exports.destroy = a, t.exports.pipeline = r.pipeline, t.exports.compose = r.compose, Object.defineProperty(r, "promises", {
        configurable: !0,
        enumerable: !0,
        get() {
          return o;
        }
      }), t.exports.Stream = r.Stream;
    }
    t.exports.default = t.exports;
  })(ni)), ni.exports;
}
var rl = { exports: {} }, nl, gg;
function MR() {
  if (gg) return nl;
  gg = 1;
  function t(e, r) {
    for (var o = -1, a = r.length, n = e.length; ++o < a; )
      e[n + o] = r[o];
    return e;
  }
  return nl = t, nl;
}
var il, yg;
function kR() {
  if (yg) return il;
  yg = 1;
  var t = xc(), e = xm(), r = Im(), o = t ? t.isConcatSpreadable : void 0;
  function a(n) {
    return r(n) || e(n) || !!(o && n && n[o]);
  }
  return il = a, il;
}
var sl, bg;
function Cc() {
  if (bg) return sl;
  bg = 1;
  var t = MR(), e = kR();
  function r(o, a, n, i, l) {
    var s = -1, f = o.length;
    for (n || (n = e), l || (l = []); ++s < f; ) {
      var d = o[s];
      a > 0 && n(d) ? a > 1 ? r(d, a - 1, n, i, l) : t(l, d) : i || (l[l.length] = d);
    }
    return l;
  }
  return sl = r, sl;
}
var ol, _g;
function FR() {
  if (_g) return ol;
  _g = 1;
  var t = Cc();
  function e(r) {
    var o = r == null ? 0 : r.length;
    return o ? t(r, 1) : [];
  }
  return ol = e, ol;
}
var al, wg;
function Qi() {
  if (wg) return al;
  wg = 1;
  var t = Zi(), e = t(Object, "create");
  return al = e, al;
}
var ll, mg;
function jR() {
  if (mg) return ll;
  mg = 1;
  var t = Qi();
  function e() {
    this.__data__ = t ? t(null) : {}, this.size = 0;
  }
  return ll = e, ll;
}
var ul, Sg;
function $R() {
  if (Sg) return ul;
  Sg = 1;
  function t(e) {
    var r = this.has(e) && delete this.__data__[e];
    return this.size -= r ? 1 : 0, r;
  }
  return ul = t, ul;
}
var fl, Eg;
function WR() {
  if (Eg) return fl;
  Eg = 1;
  var t = Qi(), e = "__lodash_hash_undefined__", r = Object.prototype, o = r.hasOwnProperty;
  function a(n) {
    var i = this.__data__;
    if (t) {
      var l = i[n];
      return l === e ? void 0 : l;
    }
    return o.call(i, n) ? i[n] : void 0;
  }
  return fl = a, fl;
}
var cl, vg;
function qR() {
  if (vg) return cl;
  vg = 1;
  var t = Qi(), e = Object.prototype, r = e.hasOwnProperty;
  function o(a) {
    var n = this.__data__;
    return t ? n[a] !== void 0 : r.call(n, a);
  }
  return cl = o, cl;
}
var dl, Rg;
function BR() {
  if (Rg) return dl;
  Rg = 1;
  var t = Qi(), e = "__lodash_hash_undefined__";
  function r(o, a) {
    var n = this.__data__;
    return this.size += this.has(o) ? 0 : 1, n[o] = t && a === void 0 ? e : a, this;
  }
  return dl = r, dl;
}
var hl, Ag;
function UR() {
  if (Ag) return hl;
  Ag = 1;
  var t = jR(), e = $R(), r = WR(), o = qR(), a = BR();
  function n(i) {
    var l = -1, s = i == null ? 0 : i.length;
    for (this.clear(); ++l < s; ) {
      var f = i[l];
      this.set(f[0], f[1]);
    }
  }
  return n.prototype.clear = t, n.prototype.delete = e, n.prototype.get = r, n.prototype.has = o, n.prototype.set = a, hl = n, hl;
}
var pl, Tg;
function GR() {
  if (Tg) return pl;
  Tg = 1;
  function t() {
    this.__data__ = [], this.size = 0;
  }
  return pl = t, pl;
}
var gl, xg;
function es() {
  if (xg) return gl;
  xg = 1;
  var t = Dc();
  function e(r, o) {
    for (var a = r.length; a--; )
      if (t(r[a][0], o))
        return a;
    return -1;
  }
  return gl = e, gl;
}
var yl, Ig;
function HR() {
  if (Ig) return yl;
  Ig = 1;
  var t = es(), e = Array.prototype, r = e.splice;
  function o(a) {
    var n = this.__data__, i = t(n, a);
    if (i < 0)
      return !1;
    var l = n.length - 1;
    return i == l ? n.pop() : r.call(n, i, 1), --this.size, !0;
  }
  return yl = o, yl;
}
var bl, Dg;
function zR() {
  if (Dg) return bl;
  Dg = 1;
  var t = es();
  function e(r) {
    var o = this.__data__, a = t(o, r);
    return a < 0 ? void 0 : o[a][1];
  }
  return bl = e, bl;
}
var _l, Og;
function VR() {
  if (Og) return _l;
  Og = 1;
  var t = es();
  function e(r) {
    return t(this.__data__, r) > -1;
  }
  return _l = e, _l;
}
var wl, Ng;
function ZR() {
  if (Ng) return wl;
  Ng = 1;
  var t = es();
  function e(r, o) {
    var a = this.__data__, n = t(a, r);
    return n < 0 ? (++this.size, a.push([r, o])) : a[n][1] = o, this;
  }
  return wl = e, wl;
}
var ml, Lg;
function YR() {
  if (Lg) return ml;
  Lg = 1;
  var t = GR(), e = HR(), r = zR(), o = VR(), a = ZR();
  function n(i) {
    var l = -1, s = i == null ? 0 : i.length;
    for (this.clear(); ++l < s; ) {
      var f = i[l];
      this.set(f[0], f[1]);
    }
  }
  return n.prototype.clear = t, n.prototype.delete = e, n.prototype.get = r, n.prototype.has = o, n.prototype.set = a, ml = n, ml;
}
var Sl, Pg;
function KR() {
  if (Pg) return Sl;
  Pg = 1;
  var t = Zi(), e = En(), r = t(e, "Map");
  return Sl = r, Sl;
}
var El, Cg;
function JR() {
  if (Cg) return El;
  Cg = 1;
  var t = UR(), e = YR(), r = KR();
  function o() {
    this.size = 0, this.__data__ = {
      hash: new t(),
      map: new (r || e)(),
      string: new t()
    };
  }
  return El = o, El;
}
var vl, Mg;
function XR() {
  if (Mg) return vl;
  Mg = 1;
  function t(e) {
    var r = typeof e;
    return r == "string" || r == "number" || r == "symbol" || r == "boolean" ? e !== "__proto__" : e === null;
  }
  return vl = t, vl;
}
var Rl, kg;
function ts() {
  if (kg) return Rl;
  kg = 1;
  var t = XR();
  function e(r, o) {
    var a = r.__data__;
    return t(o) ? a[typeof o == "string" ? "string" : "hash"] : a.map;
  }
  return Rl = e, Rl;
}
var Al, Fg;
function QR() {
  if (Fg) return Al;
  Fg = 1;
  var t = ts();
  function e(r) {
    var o = t(this, r).delete(r);
    return this.size -= o ? 1 : 0, o;
  }
  return Al = e, Al;
}
var Tl, jg;
function eA() {
  if (jg) return Tl;
  jg = 1;
  var t = ts();
  function e(r) {
    return t(this, r).get(r);
  }
  return Tl = e, Tl;
}
var xl, $g;
function tA() {
  if ($g) return xl;
  $g = 1;
  var t = ts();
  function e(r) {
    return t(this, r).has(r);
  }
  return xl = e, xl;
}
var Il, Wg;
function rA() {
  if (Wg) return Il;
  Wg = 1;
  var t = ts();
  function e(r, o) {
    var a = t(this, r), n = a.size;
    return a.set(r, o), this.size += a.size == n ? 0 : 1, this;
  }
  return Il = e, Il;
}
var Dl, qg;
function nA() {
  if (qg) return Dl;
  qg = 1;
  var t = JR(), e = QR(), r = eA(), o = tA(), a = rA();
  function n(i) {
    var l = -1, s = i == null ? 0 : i.length;
    for (this.clear(); ++l < s; ) {
      var f = i[l];
      this.set(f[0], f[1]);
    }
  }
  return n.prototype.clear = t, n.prototype.delete = e, n.prototype.get = r, n.prototype.has = o, n.prototype.set = a, Dl = n, Dl;
}
var Ol, Bg;
function iA() {
  if (Bg) return Ol;
  Bg = 1;
  var t = "__lodash_hash_undefined__";
  function e(r) {
    return this.__data__.set(r, t), this;
  }
  return Ol = e, Ol;
}
var Nl, Ug;
function sA() {
  if (Ug) return Nl;
  Ug = 1;
  function t(e) {
    return this.__data__.has(e);
  }
  return Nl = t, Nl;
}
var Ll, Gg;
function Um() {
  if (Gg) return Ll;
  Gg = 1;
  var t = nA(), e = iA(), r = sA();
  function o(a) {
    var n = -1, i = a == null ? 0 : a.length;
    for (this.__data__ = new t(); ++n < i; )
      this.add(a[n]);
  }
  return o.prototype.add = o.prototype.push = e, o.prototype.has = r, Ll = o, Ll;
}
var Pl, Hg;
function oA() {
  if (Hg) return Pl;
  Hg = 1;
  function t(e, r, o, a) {
    for (var n = e.length, i = o + (a ? 1 : -1); a ? i-- : ++i < n; )
      if (r(e[i], i, e))
        return i;
    return -1;
  }
  return Pl = t, Pl;
}
var Cl, zg;
function aA() {
  if (zg) return Cl;
  zg = 1;
  function t(e) {
    return e !== e;
  }
  return Cl = t, Cl;
}
var Ml, Vg;
function lA() {
  if (Vg) return Ml;
  Vg = 1;
  function t(e, r, o) {
    for (var a = o - 1, n = e.length; ++a < n; )
      if (e[a] === r)
        return a;
    return -1;
  }
  return Ml = t, Ml;
}
var kl, Zg;
function uA() {
  if (Zg) return kl;
  Zg = 1;
  var t = oA(), e = aA(), r = lA();
  function o(a, n, i) {
    return n === n ? r(a, n, i) : t(a, e, i);
  }
  return kl = o, kl;
}
var Fl, Yg;
function Gm() {
  if (Yg) return Fl;
  Yg = 1;
  var t = uA();
  function e(r, o) {
    var a = r == null ? 0 : r.length;
    return !!a && t(r, o, 0) > -1;
  }
  return Fl = e, Fl;
}
var jl, Kg;
function Hm() {
  if (Kg) return jl;
  Kg = 1;
  function t(e, r, o) {
    for (var a = -1, n = e == null ? 0 : e.length; ++a < n; )
      if (o(r, e[a]))
        return !0;
    return !1;
  }
  return jl = t, jl;
}
var $l, Jg;
function fA() {
  if (Jg) return $l;
  Jg = 1;
  function t(e, r) {
    for (var o = -1, a = e == null ? 0 : e.length, n = Array(a); ++o < a; )
      n[o] = r(e[o], o, e);
    return n;
  }
  return $l = t, $l;
}
var Wl, Xg;
function zm() {
  if (Xg) return Wl;
  Xg = 1;
  function t(e, r) {
    return e.has(r);
  }
  return Wl = t, Wl;
}
var ql, Qg;
function cA() {
  if (Qg) return ql;
  Qg = 1;
  var t = Um(), e = Gm(), r = Hm(), o = fA(), a = Dm(), n = zm(), i = 200;
  function l(s, f, d, y) {
    var R = -1, w = e, I = !0, A = s.length, u = [], g = f.length;
    if (!A)
      return u;
    d && (f = o(f, a(d))), y ? (w = r, I = !1) : f.length >= i && (w = n, I = !1, f = new t(f));
    e:
      for (; ++R < A; ) {
        var p = s[R], b = d == null ? p : d(p);
        if (p = y || p !== 0 ? p : 0, I && b === b) {
          for (var C = g; C--; )
            if (f[C] === b)
              continue e;
          u.push(p);
        } else w(f, b, y) || u.push(p);
      }
    return u;
  }
  return ql = l, ql;
}
var Bl, ey;
function Vm() {
  if (ey) return Bl;
  ey = 1;
  var t = Oc(), e = vn();
  function r(o) {
    return e(o) && t(o);
  }
  return Bl = r, Bl;
}
var Ul, ty;
function dA() {
  if (ty) return Ul;
  ty = 1;
  var t = cA(), e = Cc(), r = Ic(), o = Vm(), a = r(function(n, i) {
    return o(n) ? t(n, e(i, 1, o, !0)) : [];
  });
  return Ul = a, Ul;
}
var Gl, ry;
function hA() {
  if (ry) return Gl;
  ry = 1;
  var t = Zi(), e = En(), r = t(e, "Set");
  return Gl = r, Gl;
}
var Hl, ny;
function pA() {
  if (ny) return Hl;
  ny = 1;
  function t() {
  }
  return Hl = t, Hl;
}
var zl, iy;
function Zm() {
  if (iy) return zl;
  iy = 1;
  function t(e) {
    var r = -1, o = Array(e.size);
    return e.forEach(function(a) {
      o[++r] = a;
    }), o;
  }
  return zl = t, zl;
}
var Vl, sy;
function gA() {
  if (sy) return Vl;
  sy = 1;
  var t = hA(), e = pA(), r = Zm(), o = 1 / 0, a = t && 1 / r(new t([, -0]))[1] == o ? function(n) {
    return new t(n);
  } : e;
  return Vl = a, Vl;
}
var Zl, oy;
function yA() {
  if (oy) return Zl;
  oy = 1;
  var t = Um(), e = Gm(), r = Hm(), o = zm(), a = gA(), n = Zm(), i = 200;
  function l(s, f, d) {
    var y = -1, R = e, w = s.length, I = !0, A = [], u = A;
    if (d)
      I = !1, R = r;
    else if (w >= i) {
      var g = f ? null : a(s);
      if (g)
        return n(g);
      I = !1, R = o, u = new t();
    } else
      u = f ? [] : A;
    e:
      for (; ++y < w; ) {
        var p = s[y], b = f ? f(p) : p;
        if (p = d || p !== 0 ? p : 0, I && b === b) {
          for (var C = u.length; C--; )
            if (u[C] === b)
              continue e;
          f && u.push(b), A.push(p);
        } else R(u, b, d) || (u !== A && u.push(b), A.push(p));
      }
    return A;
  }
  return Zl = l, Zl;
}
var Yl, ay;
function bA() {
  if (ay) return Yl;
  ay = 1;
  var t = Cc(), e = Ic(), r = yA(), o = Vm(), a = e(function(n) {
    return r(t(n, 1, o, !0));
  });
  return Yl = a, Yl;
}
var Kl, ly;
function _A() {
  if (ly) return Kl;
  ly = 1;
  function t(e, r) {
    return function(o) {
      return e(r(o));
    };
  }
  return Kl = t, Kl;
}
var Jl, uy;
function wA() {
  if (uy) return Jl;
  uy = 1;
  var t = _A(), e = t(Object.getPrototypeOf, Object);
  return Jl = e, Jl;
}
var Xl, fy;
function mA() {
  if (fy) return Xl;
  fy = 1;
  var t = zi(), e = wA(), r = vn(), o = "[object Object]", a = Function.prototype, n = Object.prototype, i = a.toString, l = n.hasOwnProperty, s = i.call(Object);
  function f(d) {
    if (!r(d) || t(d) != o)
      return !1;
    var y = e(d);
    if (y === null)
      return !0;
    var R = l.call(y, "constructor") && y.constructor;
    return typeof R == "function" && R instanceof R && i.call(R) == s;
  }
  return Xl = f, Xl;
}
var Ql = {}, Vr = {}, eu, cy;
function SA() {
  if (cy) return eu;
  cy = 1;
  var t = vw();
  eu = d;
  var e = "\0SLASH" + Math.random() + "\0", r = "\0OPEN" + Math.random() + "\0", o = "\0CLOSE" + Math.random() + "\0", a = "\0COMMA" + Math.random() + "\0", n = "\0PERIOD" + Math.random() + "\0";
  function i(u) {
    return parseInt(u, 10) == u ? parseInt(u, 10) : u.charCodeAt(0);
  }
  function l(u) {
    return u.split("\\\\").join(e).split("\\{").join(r).split("\\}").join(o).split("\\,").join(a).split("\\.").join(n);
  }
  function s(u) {
    return u.split(e).join("\\").split(r).join("{").split(o).join("}").split(a).join(",").split(n).join(".");
  }
  function f(u) {
    if (!u)
      return [""];
    var g = [], p = t("{", "}", u);
    if (!p)
      return u.split(",");
    var b = p.pre, C = p.body, N = p.post, W = b.split(",");
    W[W.length - 1] += "{" + C + "}";
    var k = f(N);
    return N.length && (W[W.length - 1] += k.shift(), W.push.apply(W, k)), g.push.apply(g, W), g;
  }
  function d(u) {
    return u ? (u.substr(0, 2) === "{}" && (u = "\\{\\}" + u.substr(2)), A(l(u), !0).map(s)) : [];
  }
  function y(u) {
    return "{" + u + "}";
  }
  function R(u) {
    return /^-?0\d/.test(u);
  }
  function w(u, g) {
    return u <= g;
  }
  function I(u, g) {
    return u >= g;
  }
  function A(u, g) {
    var p = [], b = t("{", "}", u);
    if (!b) return [u];
    var C = b.pre, N = b.post.length ? A(b.post, !1) : [""];
    if (/\$$/.test(b.pre))
      for (var W = 0; W < N.length; W++) {
        var k = C + "{" + b.body + "}" + N[W];
        p.push(k);
      }
    else {
      var L = /^-?\d+\.\.-?\d+(?:\.\.-?\d+)?$/.test(b.body), U = /^[a-zA-Z]\.\.[a-zA-Z](?:\.\.-?\d+)?$/.test(b.body), x = L || U, D = b.body.indexOf(",") >= 0;
      if (!x && !D)
        return b.post.match(/,(?!,).*\}/) ? (u = b.pre + "{" + b.body + o + b.post, A(u)) : [u];
      var F;
      if (x)
        F = b.body.split(/\.\./);
      else if (F = f(b.body), F.length === 1 && (F = A(F[0], !1).map(y), F.length === 1))
        return N.map(function(ce) {
          return b.pre + F[0] + ce;
        });
      var T;
      if (x) {
        var J = i(F[0]), re = i(F[1]), se = Math.max(F[0].length, F[1].length), ne = F.length == 3 ? Math.max(Math.abs(i(F[2])), 1) : 1, M = w, Q = re < J;
        Q && (ne *= -1, M = I);
        var v = F.some(R);
        T = [];
        for (var $ = J; M($, re); $ += ne) {
          var ee;
          if (U)
            ee = String.fromCharCode($), ee === "\\" && (ee = "");
          else if (ee = String($), v) {
            var B = se - ee.length;
            if (B > 0) {
              var le = new Array(B + 1).join("0");
              $ < 0 ? ee = "-" + le + ee.slice(1) : ee = le + ee;
            }
          }
          T.push(ee);
        }
      } else {
        T = [];
        for (var ae = 0; ae < F.length; ae++)
          T.push.apply(T, A(F[ae], !1));
      }
      for (var ae = 0; ae < T.length; ae++)
        for (var W = 0; W < N.length; W++) {
          var k = C + T[ae] + N[W];
          (!g || x || k) && p.push(k);
        }
    }
    return p;
  }
  return eu;
}
var Zr = {}, dy;
function EA() {
  if (dy) return Zr;
  dy = 1, Object.defineProperty(Zr, "__esModule", { value: !0 }), Zr.assertValidPattern = void 0;
  const t = 1024 * 64, e = (r) => {
    if (typeof r != "string")
      throw new TypeError("invalid pattern");
    if (r.length > t)
      throw new TypeError("pattern is too long");
  };
  return Zr.assertValidPattern = e, Zr;
}
var Yr = {}, Kr = {}, hy;
function vA() {
  if (hy) return Kr;
  hy = 1, Object.defineProperty(Kr, "__esModule", { value: !0 }), Kr.parseClass = void 0;
  const t = {
    "[:alnum:]": ["\\p{L}\\p{Nl}\\p{Nd}", !0],
    "[:alpha:]": ["\\p{L}\\p{Nl}", !0],
    "[:ascii:]": ["\\x00-\\x7f", !1],
    "[:blank:]": ["\\p{Zs}\\t", !0],
    "[:cntrl:]": ["\\p{Cc}", !0],
    "[:digit:]": ["\\p{Nd}", !0],
    "[:graph:]": ["\\p{Z}\\p{C}", !0, !0],
    "[:lower:]": ["\\p{Ll}", !0],
    "[:print:]": ["\\p{C}", !0],
    "[:punct:]": ["\\p{P}", !0],
    "[:space:]": ["\\p{Z}\\t\\r\\n\\v\\f", !0],
    "[:upper:]": ["\\p{Lu}", !0],
    "[:word:]": ["\\p{L}\\p{Nl}\\p{Nd}\\p{Pc}", !0],
    "[:xdigit:]": ["A-Fa-f0-9", !1]
  }, e = (n) => n.replace(/[[\]\\-]/g, "\\$&"), r = (n) => n.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, "\\$&"), o = (n) => n.join(""), a = (n, i) => {
    const l = i;
    if (n.charAt(l) !== "[")
      throw new Error("not in a brace expression");
    const s = [], f = [];
    let d = l + 1, y = !1, R = !1, w = !1, I = !1, A = l, u = "";
    e: for (; d < n.length; ) {
      const C = n.charAt(d);
      if ((C === "!" || C === "^") && d === l + 1) {
        I = !0, d++;
        continue;
      }
      if (C === "]" && y && !w) {
        A = d + 1;
        break;
      }
      if (y = !0, C === "\\" && !w) {
        w = !0, d++;
        continue;
      }
      if (C === "[" && !w) {
        for (const [N, [W, k, L]] of Object.entries(t))
          if (n.startsWith(N, d)) {
            if (u)
              return ["$.", !1, n.length - l, !0];
            d += N.length, L ? f.push(W) : s.push(W), R = R || k;
            continue e;
          }
      }
      if (w = !1, u) {
        C > u ? s.push(e(u) + "-" + e(C)) : C === u && s.push(e(C)), u = "", d++;
        continue;
      }
      if (n.startsWith("-]", d + 1)) {
        s.push(e(C + "-")), d += 2;
        continue;
      }
      if (n.startsWith("-", d + 1)) {
        u = C, d += 2;
        continue;
      }
      s.push(e(C)), d++;
    }
    if (A < d)
      return ["", !1, 0, !1];
    if (!s.length && !f.length)
      return ["$.", !1, n.length - l, !0];
    if (f.length === 0 && s.length === 1 && /^\\?.$/.test(s[0]) && !I) {
      const C = s[0].length === 2 ? s[0].slice(-1) : s[0];
      return [r(C), !1, A - l, !1];
    }
    const g = "[" + (I ? "^" : "") + o(s) + "]", p = "[" + (I ? "" : "^") + o(f) + "]";
    return [s.length && f.length ? "(" + g + "|" + p + ")" : s.length ? g : p, R, A - l, !0];
  };
  return Kr.parseClass = a, Kr;
}
var Jr = {}, py;
function fc() {
  if (py) return Jr;
  py = 1, Object.defineProperty(Jr, "__esModule", { value: !0 }), Jr.unescape = void 0;
  const t = (e, { windowsPathsNoEscape: r = !1 } = {}) => r ? e.replace(/\[([^\/\\])\]/g, "$1") : e.replace(/((?!\\).|^)\[([^\/\\])\]/g, "$1$2").replace(/\\([^\/])/g, "$1");
  return Jr.unescape = t, Jr;
}
var gy;
function yy() {
  if (gy) return Yr;
  gy = 1;
  var t;
  Object.defineProperty(Yr, "__esModule", { value: !0 }), Yr.AST = void 0;
  const e = vA(), r = fc(), o = /* @__PURE__ */ new Set(["!", "?", "+", "*", "@"]), a = (C) => o.has(C), n = (C) => a(C.type), i = /* @__PURE__ */ new Map([
    ["!", ["@"]],
    ["?", ["?", "@"]],
    ["@", ["@"]],
    ["*", ["*", "+", "?", "@"]],
    ["+", ["+", "@"]]
  ]), l = /* @__PURE__ */ new Map([
    ["!", ["?"]],
    ["@", ["?"]],
    ["+", ["?", "*"]]
  ]), s = /* @__PURE__ */ new Map([
    ["!", ["?", "@"]],
    ["?", ["?", "@"]],
    ["@", ["?", "@"]],
    ["*", ["*", "+", "?", "@"]],
    ["+", ["+", "@", "?", "*"]]
  ]), f = /* @__PURE__ */ new Map([
    ["!", /* @__PURE__ */ new Map([["!", "@"]])],
    ["?", /* @__PURE__ */ new Map([["*", "*"], ["+", "*"]])],
    ["@", /* @__PURE__ */ new Map([["!", "!"], ["?", "?"], ["@", "@"], ["*", "*"], ["+", "+"]])],
    ["+", /* @__PURE__ */ new Map([["?", "*"], ["*", "*"]])]
  ]), d = "(?!(?:^|/)\\.\\.?(?:$|/))", y = "(?!\\.)", R = /* @__PURE__ */ new Set(["[", "."]), w = /* @__PURE__ */ new Set(["..", "."]), I = new Set("().*{}+?[]^$\\!"), A = (C) => C.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, "\\$&"), u = "[^/]", g = u + "*?", p = u + "+?";
  class b {
    type;
    #e;
    #r;
    #i = !1;
    #t = [];
    #o;
    #S;
    #u;
    #c = !1;
    #l;
    #a;
    // set to true if it's an extglob with no children
    // (which really means one child of '')
    #s = !1;
    constructor(N, W, k = {}) {
      this.type = N, N && (this.#r = !0), this.#o = W, this.#e = this.#o ? this.#o.#e : this, this.#l = this.#e === this ? k : this.#e.#l, this.#u = this.#e === this ? [] : this.#e.#u, N === "!" && !this.#e.#c && this.#u.push(this), this.#S = this.#o ? this.#o.#t.length : 0;
    }
    get hasMagic() {
      if (this.#r !== void 0)
        return this.#r;
      for (const N of this.#t)
        if (typeof N != "string" && (N.type || N.hasMagic))
          return this.#r = !0;
      return this.#r;
    }
    // reconstructs the pattern
    toString() {
      return this.#a !== void 0 ? this.#a : this.type ? this.#a = this.type + "(" + this.#t.map((N) => String(N)).join("|") + ")" : this.#a = this.#t.map((N) => String(N)).join("");
    }
    #b() {
      if (this !== this.#e)
        throw new Error("should only call on root");
      if (this.#c)
        return this;
      this.toString(), this.#c = !0;
      let N;
      for (; N = this.#u.pop(); ) {
        if (N.type !== "!")
          continue;
        let W = N, k = W.#o;
        for (; k; ) {
          for (let L = W.#S + 1; !k.type && L < k.#t.length; L++)
            for (const U of N.#t) {
              if (typeof U == "string")
                throw new Error("string part in extglob AST??");
              U.copyIn(k.#t[L]);
            }
          W = k, k = W.#o;
        }
      }
      return this;
    }
    push(...N) {
      for (const W of N)
        if (W !== "") {
          if (typeof W != "string" && !(W instanceof t && W.#o === this))
            throw new Error("invalid part: " + W);
          this.#t.push(W);
        }
    }
    toJSON() {
      const N = this.type === null ? this.#t.slice().map((W) => typeof W == "string" ? W : W.toJSON()) : [this.type, ...this.#t.map((W) => W.toJSON())];
      return this.isStart() && !this.type && N.unshift([]), this.isEnd() && (this === this.#e || this.#e.#c && this.#o?.type === "!") && N.push({}), N;
    }
    isStart() {
      if (this.#e === this)
        return !0;
      if (!this.#o?.isStart())
        return !1;
      if (this.#S === 0)
        return !0;
      const N = this.#o;
      for (let W = 0; W < this.#S; W++) {
        const k = N.#t[W];
        if (!(k instanceof t && k.type === "!"))
          return !1;
      }
      return !0;
    }
    isEnd() {
      if (this.#e === this || this.#o?.type === "!")
        return !0;
      if (!this.#o?.isEnd())
        return !1;
      if (!this.type)
        return this.#o?.isEnd();
      const N = this.#o ? this.#o.#t.length : 0;
      return this.#S === N - 1;
    }
    copyIn(N) {
      typeof N == "string" ? this.push(N) : this.push(N.clone(this));
    }
    clone(N) {
      const W = new t(this.type, N);
      for (const k of this.#t)
        W.copyIn(k);
      return W;
    }
    static #_(N, W, k, L, U) {
      const x = L.maxExtglobRecursion ?? 2;
      let D = !1, F = !1, T = -1, J = !1;
      if (W.type === null) {
        let Q = k, v = "";
        for (; Q < N.length; ) {
          const $ = N.charAt(Q++);
          if (D || $ === "\\") {
            D = !D, v += $;
            continue;
          }
          if (F) {
            Q === T + 1 ? ($ === "^" || $ === "!") && (J = !0) : $ === "]" && !(Q === T + 2 && J) && (F = !1), v += $;
            continue;
          } else if ($ === "[") {
            F = !0, T = Q, J = !1, v += $;
            continue;
          }
          if (!L.noext && a($) && N.charAt(Q) === "(" && U <= x) {
            W.push(v), v = "";
            const B = new t($, W);
            Q = t.#_(N, B, Q, L, U + 1), W.push(B);
            continue;
          }
          v += $;
        }
        return W.push(v), Q;
      }
      let re = k + 1, se = new t(null, W);
      const ne = [];
      let M = "";
      for (; re < N.length; ) {
        const Q = N.charAt(re++);
        if (D || Q === "\\") {
          D = !D, M += Q;
          continue;
        }
        if (F) {
          re === T + 1 ? (Q === "^" || Q === "!") && (J = !0) : Q === "]" && !(re === T + 2 && J) && (F = !1), M += Q;
          continue;
        } else if (Q === "[") {
          F = !0, T = re, J = !1, M += Q;
          continue;
        }
        if (a(Q) && N.charAt(re) === "(" && /* c8 ignore start - the maxDepth is sufficient here */
        (U <= x || W && W.#E(Q))) {
          const $ = W && W.#E(Q) ? 0 : 1;
          se.push(M), M = "";
          const ee = new t(Q, se);
          se.push(ee), re = t.#_(N, ee, re, L, U + $);
          continue;
        }
        if (Q === "|") {
          se.push(M), M = "", ne.push(se), se = new t(null, W);
          continue;
        }
        if (Q === ")")
          return M === "" && W.#t.length === 0 && (W.#s = !0), se.push(M), M = "", W.push(...ne, se), re;
        M += Q;
      }
      return W.type = null, W.#r = void 0, W.#t = [N.substring(k - 1)], re;
    }
    #g(N) {
      return this.#h(N, l);
    }
    #h(N, W = i) {
      if (!N || typeof N != "object" || N.type !== null || N.#t.length !== 1 || this.type === null)
        return !1;
      const k = N.#t[0];
      return !k || typeof k != "object" || k.type === null ? !1 : this.#E(k.type, W);
    }
    #E(N, W = s) {
      return !!W.get(this.type)?.includes(N);
    }
    #p(N, W) {
      const k = N.#t[0], L = new t(null, k, this.options);
      L.#t.push(""), k.push(L), this.#R(N, W);
    }
    #R(N, W) {
      const k = N.#t[0];
      this.#t.splice(W, 1, ...k.#t);
      for (const L of k.#t)
        typeof L == "object" && (L.#o = this);
      this.#a = void 0;
    }
    #A(N) {
      return !!f.get(this.type)?.has(N);
    }
    #y(N) {
      if (!N || typeof N != "object" || N.type !== null || N.#t.length !== 1 || this.type === null || this.#t.length !== 1)
        return !1;
      const W = N.#t[0];
      return !W || typeof W != "object" || W.type === null ? !1 : this.#A(W.type);
    }
    #v(N) {
      const W = f.get(this.type), k = N.#t[0], L = W?.get(k.type);
      if (!L)
        return !1;
      this.#t = k.#t;
      for (const U of this.#t)
        typeof U == "object" && (U.#o = this);
      this.type = L, this.#a = void 0, this.#s = !1;
    }
    #w() {
      if (n(this)) {
        let N = 0, W = !1;
        do {
          W = !0;
          for (let k = 0; k < this.#t.length; k++) {
            const L = this.#t[k];
            typeof L == "object" && (L.#w(), this.#h(L) ? (W = !1, this.#R(L, k)) : this.#g(L) ? (W = !1, this.#p(L, k)) : this.#y(L) && (W = !1, this.#v(L)));
          }
        } while (!W && ++N < 10);
      } else
        for (const N of this.#t)
          typeof N == "object" && N.#w();
      this.#a = void 0;
    }
    static fromGlob(N, W = {}) {
      const k = new t(null, void 0, W);
      return t.#_(N, k, 0, W, 0), k;
    }
    // returns the regular expression if there's magic, or the unescaped
    // string if not.
    toMMPattern() {
      if (this !== this.#e)
        return this.#e.toMMPattern();
      const N = this.toString(), [W, k, L, U] = this.toRegExpSource();
      if (!(L || this.#r || this.#l.nocase && !this.#l.nocaseMagicOnly && N.toUpperCase() !== N.toLowerCase()))
        return k;
      const D = (this.#l.nocase ? "i" : "") + (U ? "u" : "");
      return Object.assign(new RegExp(`^${W}$`, D), {
        _src: W,
        _glob: N
      });
    }
    get options() {
      return this.#l;
    }
    // returns the string match, the regexp source, whether there's magic
    // in the regexp (so a regular expression is required) and whether or
    // not the uflag is needed for the regular expression (for posix classes)
    // TODO: instead of injecting the start/end at this point, just return
    // the BODY of the regexp, along with the start/end portions suitable
    // for binding the start/end in either a joined full-path makeRe context
    // (where we bind to (^|/), or a standalone matchPart context (where
    // we bind to ^, and not /).  Otherwise slashes get duped!
    //
    // In part-matching mode, the start is:
    // - if not isStart: nothing
    // - if traversal possible, but not allowed: ^(?!\.\.?$)
    // - if dots allowed or not possible: ^
    // - if dots possible and not allowed: ^(?!\.)
    // end is:
    // - if not isEnd(): nothing
    // - else: $
    //
    // In full-path matching mode, we put the slash at the START of the
    // pattern, so start is:
    // - if first pattern: same as part-matching mode
    // - if not isStart(): nothing
    // - if traversal possible, but not allowed: /(?!\.\.?(?:$|/))
    // - if dots allowed or not possible: /
    // - if dots possible and not allowed: /(?!\.)
    // end is:
    // - if last pattern, same as part-matching mode
    // - else nothing
    //
    // Always put the (?:$|/) on negated tails, though, because that has to be
    // there to bind the end of the negated pattern portion, and it's easier to
    // just stick it in now rather than try to inject it later in the middle of
    // the pattern.
    //
    // We can just always return the same end, and leave it up to the caller
    // to know whether it's going to be used joined or in parts.
    // And, if the start is adjusted slightly, can do the same there:
    // - if not isStart: nothing
    // - if traversal possible, but not allowed: (?:/|^)(?!\.\.?$)
    // - if dots allowed or not possible: (?:/|^)
    // - if dots possible and not allowed: (?:/|^)(?!\.)
    //
    // But it's better to have a simpler binding without a conditional, for
    // performance, so probably better to return both start options.
    //
    // Then the caller just ignores the end if it's not the first pattern,
    // and the start always gets applied.
    //
    // But that's always going to be $ if it's the ending pattern, or nothing,
    // so the caller can just attach $ at the end of the pattern when building.
    //
    // So the todo is:
    // - better detect what kind of start is needed
    // - return both flavors of starting pattern
    // - attach $ at the end of the pattern when creating the actual RegExp
    //
    // Ah, but wait, no, that all only applies to the root when the first pattern
    // is not an extglob. If the first pattern IS an extglob, then we need all
    // that dot prevention biz to live in the extglob portions, because eg
    // +(*|.x*) can match .xy but not .yx.
    //
    // So, return the two flavors if it's #root and the first child is not an
    // AST, otherwise leave it to the child AST to handle it, and there,
    // use the (?:^|/) style of start binding.
    //
    // Even simplified further:
    // - Since the start for a join is eg /(?!\.) and the start for a part
    // is ^(?!\.), we can just prepend (?!\.) to the pattern (either root
    // or start or whatever) and prepend ^ or / at the Regexp construction.
    toRegExpSource(N) {
      const W = N ?? !!this.#l.dot;
      if (this.#e === this && (this.#w(), this.#b()), !n(this)) {
        const F = this.isStart() && this.isEnd(), T = this.#t.map((ne) => {
          const [M, Q, v, $] = typeof ne == "string" ? t.#T(ne, this.#r, F) : ne.toRegExpSource(N);
          return this.#r = this.#r || v, this.#i = this.#i || $, M;
        }).join("");
        let J = "";
        if (this.isStart() && typeof this.#t[0] == "string" && !(this.#t.length === 1 && w.has(this.#t[0]))) {
          const M = R, Q = (
            // dots are allowed, and the pattern starts with [ or .
            W && M.has(T.charAt(0)) || // the pattern starts with \., and then [ or .
            T.startsWith("\\.") && M.has(T.charAt(2)) || // the pattern starts with \.\., and then [ or .
            T.startsWith("\\.\\.") && M.has(T.charAt(4))
          ), v = !W && !N && M.has(T.charAt(0));
          J = Q ? d : v ? y : "";
        }
        let re = "";
        return this.isEnd() && this.#e.#c && this.#o?.type === "!" && (re = "(?:$|\\/)"), [
          J + T + re,
          (0, r.unescape)(T),
          this.#r = !!this.#r,
          this.#i
        ];
      }
      const k = this.type === "*" || this.type === "+", L = this.type === "!" ? "(?:(?!(?:" : "(?:";
      let U = this.#d(W);
      if (this.isStart() && this.isEnd() && !U && this.type !== "!") {
        const F = this.toString(), T = this;
        return T.#t = [F], T.type = null, T.#r = void 0, [F, (0, r.unescape)(this.toString()), !1, !1];
      }
      let x = !k || N || W || !y ? "" : this.#d(!0);
      x === U && (x = ""), x && (U = `(?:${U})(?:${x})*?`);
      let D = "";
      if (this.type === "!" && this.#s)
        D = (this.isStart() && !W ? y : "") + p;
      else {
        const F = this.type === "!" ? (
          // !() must match something,but !(x) can match ''
          "))" + (this.isStart() && !W && !N ? y : "") + g + ")"
        ) : this.type === "@" ? ")" : this.type === "?" ? ")?" : this.type === "+" && x ? ")" : this.type === "*" && x ? ")?" : `)${this.type}`;
        D = L + U + F;
      }
      return [
        D,
        (0, r.unescape)(U),
        this.#r = !!this.#r,
        this.#i
      ];
    }
    #d(N) {
      return this.#t.map((W) => {
        if (typeof W == "string")
          throw new Error("string type in extglob ast??");
        const [k, L, U, x] = W.toRegExpSource(N);
        return this.#i = this.#i || x, k;
      }).filter((W) => !(this.isStart() && this.isEnd()) || !!W).join("|");
    }
    static #T(N, W, k = !1) {
      let L = !1, U = "", x = !1, D = !1;
      for (let F = 0; F < N.length; F++) {
        const T = N.charAt(F);
        if (L) {
          L = !1, U += (I.has(T) ? "\\" : "") + T, D = !1;
          continue;
        }
        if (T === "\\") {
          F === N.length - 1 ? U += "\\\\" : L = !0;
          continue;
        }
        if (T === "[") {
          const [J, re, se, ne] = (0, e.parseClass)(N, F);
          if (se) {
            U += J, x = x || re, F += se - 1, W = W || ne, D = !1;
            continue;
          }
        }
        if (T === "*") {
          if (D)
            continue;
          D = !0, U += k && /^[*]+$/.test(N) ? p : g, W = !0;
          continue;
        } else
          D = !1;
        if (T === "?") {
          U += u, W = !0;
          continue;
        }
        U += A(T);
      }
      return [U, (0, r.unescape)(N), !!W, x];
    }
  }
  return Yr.AST = b, t = b, Yr;
}
var Xr = {}, by;
function _y() {
  if (by) return Xr;
  by = 1, Object.defineProperty(Xr, "__esModule", { value: !0 }), Xr.escape = void 0;
  const t = (e, { windowsPathsNoEscape: r = !1 } = {}) => r ? e.replace(/[?*()[\]]/g, "[$&]") : e.replace(/[?*()[\]\\]/g, "\\$&");
  return Xr.escape = t, Xr;
}
var wy;
function vr() {
  return wy || (wy = 1, (function(t) {
    var e = Vr && Vr.__importDefault || function(V) {
      return V && V.__esModule ? V : { default: V };
    };
    Object.defineProperty(t, "__esModule", { value: !0 }), t.unescape = t.escape = t.AST = t.Minimatch = t.match = t.makeRe = t.braceExpand = t.defaults = t.filter = t.GLOBSTAR = t.sep = t.minimatch = void 0;
    const r = e(SA()), o = EA(), a = yy(), n = _y(), i = fc(), l = (V, Z, X = {}) => ((0, o.assertValidPattern)(Z), !X.nocomment && Z.charAt(0) === "#" ? !1 : new ce(Z, X).match(V));
    t.minimatch = l;
    const s = /^\*+([^+@!?\*\[\(]*)$/, f = (V) => (Z) => !Z.startsWith(".") && Z.endsWith(V), d = (V) => (Z) => Z.endsWith(V), y = (V) => (V = V.toLowerCase(), (Z) => !Z.startsWith(".") && Z.toLowerCase().endsWith(V)), R = (V) => (V = V.toLowerCase(), (Z) => Z.toLowerCase().endsWith(V)), w = /^\*+\.\*+$/, I = (V) => !V.startsWith(".") && V.includes("."), A = (V) => V !== "." && V !== ".." && V.includes("."), u = /^\.\*+$/, g = (V) => V !== "." && V !== ".." && V.startsWith("."), p = /^\*+$/, b = (V) => V.length !== 0 && !V.startsWith("."), C = (V) => V.length !== 0 && V !== "." && V !== "..", N = /^\?+([^+@!?\*\[\(]*)?$/, W = ([V, Z = ""]) => {
      const X = x([V]);
      return Z ? (Z = Z.toLowerCase(), (m) => X(m) && m.toLowerCase().endsWith(Z)) : X;
    }, k = ([V, Z = ""]) => {
      const X = D([V]);
      return Z ? (Z = Z.toLowerCase(), (m) => X(m) && m.toLowerCase().endsWith(Z)) : X;
    }, L = ([V, Z = ""]) => {
      const X = D([V]);
      return Z ? (m) => X(m) && m.endsWith(Z) : X;
    }, U = ([V, Z = ""]) => {
      const X = x([V]);
      return Z ? (m) => X(m) && m.endsWith(Z) : X;
    }, x = ([V]) => {
      const Z = V.length;
      return (X) => X.length === Z && !X.startsWith(".");
    }, D = ([V]) => {
      const Z = V.length;
      return (X) => X.length === Z && X !== "." && X !== "..";
    }, F = typeof process == "object" && process ? typeof process.env == "object" && process.env && process.env.__MINIMATCH_TESTING_PLATFORM__ || process.platform : "posix", T = {
      win32: { sep: "\\" },
      posix: { sep: "/" }
    };
    t.sep = F === "win32" ? T.win32.sep : T.posix.sep, t.minimatch.sep = t.sep, t.GLOBSTAR = /* @__PURE__ */ Symbol("globstar **"), t.minimatch.GLOBSTAR = t.GLOBSTAR;
    const re = "[^/]" + "*?", se = "(?:(?!(?:\\/|^)(?:\\.{1,2})($|\\/)).)*?", ne = "(?:(?!(?:\\/|^)\\.).)*?", M = (V, Z = {}) => (X) => (0, t.minimatch)(X, V, Z);
    t.filter = M, t.minimatch.filter = t.filter;
    const Q = (V, Z = {}) => Object.assign({}, V, Z), v = (V) => {
      if (!V || typeof V != "object" || !Object.keys(V).length)
        return t.minimatch;
      const Z = t.minimatch;
      return Object.assign((m, c, E = {}) => Z(m, c, Q(V, E)), {
        Minimatch: class extends Z.Minimatch {
          constructor(c, E = {}) {
            super(c, Q(V, E));
          }
          static defaults(c) {
            return Z.defaults(Q(V, c)).Minimatch;
          }
        },
        AST: class extends Z.AST {
          /* c8 ignore start */
          constructor(c, E, j = {}) {
            super(c, E, Q(V, j));
          }
          /* c8 ignore stop */
          static fromGlob(c, E = {}) {
            return Z.AST.fromGlob(c, Q(V, E));
          }
        },
        unescape: (m, c = {}) => Z.unescape(m, Q(V, c)),
        escape: (m, c = {}) => Z.escape(m, Q(V, c)),
        filter: (m, c = {}) => Z.filter(m, Q(V, c)),
        defaults: (m) => Z.defaults(Q(V, m)),
        makeRe: (m, c = {}) => Z.makeRe(m, Q(V, c)),
        braceExpand: (m, c = {}) => Z.braceExpand(m, Q(V, c)),
        match: (m, c, E = {}) => Z.match(m, c, Q(V, E)),
        sep: Z.sep,
        GLOBSTAR: t.GLOBSTAR
      });
    };
    t.defaults = v, t.minimatch.defaults = t.defaults;
    const $ = (V, Z = {}) => ((0, o.assertValidPattern)(V), Z.nobrace || !/\{(?:(?!\{).)*\}/.test(V) ? [V] : (0, r.default)(V));
    t.braceExpand = $, t.minimatch.braceExpand = t.braceExpand;
    const ee = (V, Z = {}) => new ce(V, Z).makeRe();
    t.makeRe = ee, t.minimatch.makeRe = t.makeRe;
    const B = (V, Z, X = {}) => {
      const m = new ce(Z, X);
      return V = V.filter((c) => m.match(c)), m.options.nonull && !V.length && V.push(Z), V;
    };
    t.match = B, t.minimatch.match = t.match;
    const le = /[?*]|[+@!]\(.*?\)|\[|\]/, ae = (V) => V.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, "\\$&");
    class ce {
      options;
      set;
      pattern;
      windowsPathsNoEscape;
      nonegate;
      negate;
      comment;
      empty;
      preserveMultipleSlashes;
      partial;
      globSet;
      globParts;
      nocase;
      isWindows;
      platform;
      windowsNoMagicRoot;
      maxGlobstarRecursion;
      regexp;
      constructor(Z, X = {}) {
        (0, o.assertValidPattern)(Z), X = X || {}, this.options = X, this.maxGlobstarRecursion = X.maxGlobstarRecursion ?? 200, this.pattern = Z, this.platform = X.platform || F, this.isWindows = this.platform === "win32", this.windowsPathsNoEscape = !!X.windowsPathsNoEscape || X.allowWindowsEscape === !1, this.windowsPathsNoEscape && (this.pattern = this.pattern.replace(/\\/g, "/")), this.preserveMultipleSlashes = !!X.preserveMultipleSlashes, this.regexp = null, this.negate = !1, this.nonegate = !!X.nonegate, this.comment = !1, this.empty = !1, this.partial = !!X.partial, this.nocase = !!this.options.nocase, this.windowsNoMagicRoot = X.windowsNoMagicRoot !== void 0 ? X.windowsNoMagicRoot : !!(this.isWindows && this.nocase), this.globSet = [], this.globParts = [], this.set = [], this.make();
      }
      hasMagic() {
        if (this.options.magicalBraces && this.set.length > 1)
          return !0;
        for (const Z of this.set)
          for (const X of Z)
            if (typeof X != "string")
              return !0;
        return !1;
      }
      debug(...Z) {
      }
      make() {
        const Z = this.pattern, X = this.options;
        if (!X.nocomment && Z.charAt(0) === "#") {
          this.comment = !0;
          return;
        }
        if (!Z) {
          this.empty = !0;
          return;
        }
        this.parseNegate(), this.globSet = [...new Set(this.braceExpand())], X.debug && (this.debug = (...E) => console.error(...E)), this.debug(this.pattern, this.globSet);
        const m = this.globSet.map((E) => this.slashSplit(E));
        this.globParts = this.preprocess(m), this.debug(this.pattern, this.globParts);
        let c = this.globParts.map((E, j, G) => {
          if (this.isWindows && this.windowsNoMagicRoot) {
            const K = E[0] === "" && E[1] === "" && (E[2] === "?" || !le.test(E[2])) && !le.test(E[3]), O = /^[a-z]:/i.test(E[0]);
            if (K)
              return [...E.slice(0, 4), ...E.slice(4).map((H) => this.parse(H))];
            if (O)
              return [E[0], ...E.slice(1).map((H) => this.parse(H))];
          }
          return E.map((K) => this.parse(K));
        });
        if (this.debug(this.pattern, c), this.set = c.filter((E) => E.indexOf(!1) === -1), this.isWindows)
          for (let E = 0; E < this.set.length; E++) {
            const j = this.set[E];
            j[0] === "" && j[1] === "" && this.globParts[E][2] === "?" && typeof j[3] == "string" && /^[a-z]:$/i.test(j[3]) && (j[2] = "?");
          }
        this.debug(this.pattern, this.set);
      }
      // various transforms to equivalent pattern sets that are
      // faster to process in a filesystem walk.  The goal is to
      // eliminate what we can, and push all ** patterns as far
      // to the right as possible, even if it increases the number
      // of patterns that we have to process.
      preprocess(Z) {
        if (this.options.noglobstar)
          for (let m = 0; m < Z.length; m++)
            for (let c = 0; c < Z[m].length; c++)
              Z[m][c] === "**" && (Z[m][c] = "*");
        const { optimizationLevel: X = 1 } = this.options;
        return X >= 2 ? (Z = this.firstPhasePreProcess(Z), Z = this.secondPhasePreProcess(Z)) : X >= 1 ? Z = this.levelOneOptimize(Z) : Z = this.adjascentGlobstarOptimize(Z), Z;
      }
      // just get rid of adjascent ** portions
      adjascentGlobstarOptimize(Z) {
        return Z.map((X) => {
          let m = -1;
          for (; (m = X.indexOf("**", m + 1)) !== -1; ) {
            let c = m;
            for (; X[c + 1] === "**"; )
              c++;
            c !== m && X.splice(m, c - m);
          }
          return X;
        });
      }
      // get rid of adjascent ** and resolve .. portions
      levelOneOptimize(Z) {
        return Z.map((X) => (X = X.reduce((m, c) => {
          const E = m[m.length - 1];
          return c === "**" && E === "**" ? m : c === ".." && E && E !== ".." && E !== "." && E !== "**" ? (m.pop(), m) : (m.push(c), m);
        }, []), X.length === 0 ? [""] : X));
      }
      levelTwoFileOptimize(Z) {
        Array.isArray(Z) || (Z = this.slashSplit(Z));
        let X = !1;
        do {
          if (X = !1, !this.preserveMultipleSlashes) {
            for (let c = 1; c < Z.length - 1; c++) {
              const E = Z[c];
              c === 1 && E === "" && Z[0] === "" || (E === "." || E === "") && (X = !0, Z.splice(c, 1), c--);
            }
            Z[0] === "." && Z.length === 2 && (Z[1] === "." || Z[1] === "") && (X = !0, Z.pop());
          }
          let m = 0;
          for (; (m = Z.indexOf("..", m + 1)) !== -1; ) {
            const c = Z[m - 1];
            c && c !== "." && c !== ".." && c !== "**" && (X = !0, Z.splice(m - 1, 2), m -= 2);
          }
        } while (X);
        return Z.length === 0 ? [""] : Z;
      }
      // First phase: single-pattern processing
      // <pre> is 1 or more portions
      // <rest> is 1 or more portions
      // <p> is any portion other than ., .., '', or **
      // <e> is . or ''
      //
      // **/.. is *brutal* for filesystem walking performance, because
      // it effectively resets the recursive walk each time it occurs,
      // and ** cannot be reduced out by a .. pattern part like a regexp
      // or most strings (other than .., ., and '') can be.
      //
      // <pre>/**/../<p>/<p>/<rest> -> {<pre>/../<p>/<p>/<rest>,<pre>/**/<p>/<p>/<rest>}
      // <pre>/<e>/<rest> -> <pre>/<rest>
      // <pre>/<p>/../<rest> -> <pre>/<rest>
      // **/**/<rest> -> **/<rest>
      //
      // **/*/<rest> -> */**/<rest> <== not valid because ** doesn't follow
      // this WOULD be allowed if ** did follow symlinks, or * didn't
      firstPhasePreProcess(Z) {
        let X = !1;
        do {
          X = !1;
          for (let m of Z) {
            let c = -1;
            for (; (c = m.indexOf("**", c + 1)) !== -1; ) {
              let j = c;
              for (; m[j + 1] === "**"; )
                j++;
              j > c && m.splice(c + 1, j - c);
              let G = m[c + 1];
              const K = m[c + 2], O = m[c + 3];
              if (G !== ".." || !K || K === "." || K === ".." || !O || O === "." || O === "..")
                continue;
              X = !0, m.splice(c, 1);
              const H = m.slice(0);
              H[c] = "**", Z.push(H), c--;
            }
            if (!this.preserveMultipleSlashes) {
              for (let j = 1; j < m.length - 1; j++) {
                const G = m[j];
                j === 1 && G === "" && m[0] === "" || (G === "." || G === "") && (X = !0, m.splice(j, 1), j--);
              }
              m[0] === "." && m.length === 2 && (m[1] === "." || m[1] === "") && (X = !0, m.pop());
            }
            let E = 0;
            for (; (E = m.indexOf("..", E + 1)) !== -1; ) {
              const j = m[E - 1];
              if (j && j !== "." && j !== ".." && j !== "**") {
                X = !0;
                const K = E === 1 && m[E + 1] === "**" ? ["."] : [];
                m.splice(E - 1, 2, ...K), m.length === 0 && m.push(""), E -= 2;
              }
            }
          }
        } while (X);
        return Z;
      }
      // second phase: multi-pattern dedupes
      // {<pre>/*/<rest>,<pre>/<p>/<rest>} -> <pre>/*/<rest>
      // {<pre>/<rest>,<pre>/<rest>} -> <pre>/<rest>
      // {<pre>/**/<rest>,<pre>/<rest>} -> <pre>/**/<rest>
      //
      // {<pre>/**/<rest>,<pre>/**/<p>/<rest>} -> <pre>/**/<rest>
      // ^-- not valid because ** doens't follow symlinks
      secondPhasePreProcess(Z) {
        for (let X = 0; X < Z.length - 1; X++)
          for (let m = X + 1; m < Z.length; m++) {
            const c = this.partsMatch(Z[X], Z[m], !this.preserveMultipleSlashes);
            if (c) {
              Z[X] = [], Z[m] = c;
              break;
            }
          }
        return Z.filter((X) => X.length);
      }
      partsMatch(Z, X, m = !1) {
        let c = 0, E = 0, j = [], G = "";
        for (; c < Z.length && E < X.length; )
          if (Z[c] === X[E])
            j.push(G === "b" ? X[E] : Z[c]), c++, E++;
          else if (m && Z[c] === "**" && X[E] === Z[c + 1])
            j.push(Z[c]), c++;
          else if (m && X[E] === "**" && Z[c] === X[E + 1])
            j.push(X[E]), E++;
          else if (Z[c] === "*" && X[E] && (this.options.dot || !X[E].startsWith(".")) && X[E] !== "**") {
            if (G === "b")
              return !1;
            G = "a", j.push(Z[c]), c++, E++;
          } else if (X[E] === "*" && Z[c] && (this.options.dot || !Z[c].startsWith(".")) && Z[c] !== "**") {
            if (G === "a")
              return !1;
            G = "b", j.push(X[E]), c++, E++;
          } else
            return !1;
        return Z.length === X.length && j;
      }
      parseNegate() {
        if (this.nonegate)
          return;
        const Z = this.pattern;
        let X = !1, m = 0;
        for (let c = 0; c < Z.length && Z.charAt(c) === "!"; c++)
          X = !X, m++;
        m && (this.pattern = Z.slice(m)), this.negate = X;
      }
      // set partial to true to test if, for example,
      // "/a/b" matches the start of "/*/b/*/d"
      // Partial means, if you run out of file before you run
      // out of pattern, then that's fine, as long as all
      // the parts match.
      matchOne(Z, X, m = !1) {
        let c = 0, E = 0;
        if (this.isWindows) {
          const G = typeof Z[0] == "string" && /^[a-z]:$/i.test(Z[0]), K = !G && Z[0] === "" && Z[1] === "" && Z[2] === "?" && /^[a-z]:$/i.test(Z[3]), O = typeof X[0] == "string" && /^[a-z]:$/i.test(X[0]), H = !O && X[0] === "" && X[1] === "" && X[2] === "?" && typeof X[3] == "string" && /^[a-z]:$/i.test(X[3]), te = K ? 3 : G ? 0 : void 0, z = H ? 3 : O ? 0 : void 0;
          if (typeof te == "number" && typeof z == "number") {
            const [de, pe] = [
              Z[te],
              X[z]
            ];
            de.toLowerCase() === pe.toLowerCase() && (X[z] = de, E = z, c = te);
          }
        }
        const { optimizationLevel: j = 1 } = this.options;
        return j >= 2 && (Z = this.levelTwoFileOptimize(Z)), X.includes(t.GLOBSTAR) ? this.#e(Z, X, m, c, E) : this.#i(Z, X, m, c, E);
      }
      #e(Z, X, m, c, E) {
        const j = X.indexOf(t.GLOBSTAR, E), G = X.lastIndexOf(t.GLOBSTAR), [K, O, H] = m ? [
          X.slice(E, j),
          X.slice(j + 1),
          []
        ] : [
          X.slice(E, j),
          X.slice(j + 1, G),
          X.slice(G + 1)
        ];
        if (K.length) {
          const _e = Z.slice(c, c + K.length);
          if (!this.#i(_e, K, m, 0, 0))
            return !1;
          c += K.length;
        }
        let te = 0;
        if (H.length) {
          if (H.length + c > Z.length)
            return !1;
          let _e = Z.length - H.length;
          if (this.#i(Z, H, m, _e, 0))
            te = H.length;
          else {
            if (Z[Z.length - 1] !== "" || c + H.length === Z.length || (_e--, !this.#i(Z, H, m, _e, 0)))
              return !1;
            te = H.length + 1;
          }
        }
        if (!O.length) {
          let _e = !!te;
          for (let Ae = c; Ae < Z.length - te; Ae++) {
            const xe = String(Z[Ae]);
            if (_e = !0, xe === "." || xe === ".." || !this.options.dot && xe.startsWith("."))
              return !1;
          }
          return m || _e;
        }
        const z = [[[], 0]];
        let de = z[0], pe = 0;
        const he = [0];
        for (const _e of O)
          _e === t.GLOBSTAR ? (he.push(pe), de = [[], 0], z.push(de)) : (de[0].push(_e), pe++);
        let we = z.length - 1;
        const be = Z.length - te;
        for (const _e of z)
          _e[1] = be - (he[we--] + _e[0].length);
        return !!this.#r(Z, z, c, 0, m, 0, !!te);
      }
      #r(Z, X, m, c, E, j, G) {
        const K = X[c];
        if (!K) {
          for (let te = m; te < Z.length; te++) {
            G = !0;
            const z = Z[te];
            if (z === "." || z === ".." || !this.options.dot && z.startsWith("."))
              return !1;
          }
          return G;
        }
        const [O, H] = K;
        for (; m <= H; ) {
          if (this.#i(Z.slice(0, m + O.length), O, E, m, 0) && j < this.maxGlobstarRecursion) {
            const de = this.#r(Z, X, m + O.length, c + 1, E, j + 1, G);
            if (de !== !1)
              return de;
          }
          const z = Z[m];
          if (z === "." || z === ".." || !this.options.dot && z.startsWith("."))
            return !1;
          m++;
        }
        return E || null;
      }
      #i(Z, X, m, c, E) {
        let j, G, K, O;
        for (j = c, G = E, O = Z.length, K = X.length; j < O && G < K; j++, G++) {
          this.debug("matchOne loop");
          let H = X[G], te = Z[j];
          if (this.debug(X, H, te), H === !1 || H === t.GLOBSTAR)
            return !1;
          let z;
          if (typeof H == "string" ? (z = te === H, this.debug("string match", H, te, z)) : (z = H.test(te), this.debug("pattern match", H, te, z)), !z)
            return !1;
        }
        if (j === O && G === K)
          return !0;
        if (j === O)
          return m;
        if (G === K)
          return j === O - 1 && Z[j] === "";
        throw new Error("wtf?");
      }
      braceExpand() {
        return (0, t.braceExpand)(this.pattern, this.options);
      }
      parse(Z) {
        (0, o.assertValidPattern)(Z);
        const X = this.options;
        if (Z === "**")
          return t.GLOBSTAR;
        if (Z === "")
          return "";
        let m, c = null;
        (m = Z.match(p)) ? c = X.dot ? C : b : (m = Z.match(s)) ? c = (X.nocase ? X.dot ? R : y : X.dot ? d : f)(m[1]) : (m = Z.match(N)) ? c = (X.nocase ? X.dot ? k : W : X.dot ? L : U)(m) : (m = Z.match(w)) ? c = X.dot ? A : I : (m = Z.match(u)) && (c = g);
        const E = a.AST.fromGlob(Z, this.options).toMMPattern();
        return c && typeof E == "object" && Reflect.defineProperty(E, "test", { value: c }), E;
      }
      makeRe() {
        if (this.regexp || this.regexp === !1)
          return this.regexp;
        const Z = this.set;
        if (!Z.length)
          return this.regexp = !1, this.regexp;
        const X = this.options, m = X.noglobstar ? re : X.dot ? se : ne, c = new Set(X.nocase ? ["i"] : []);
        let E = Z.map((K) => {
          const O = K.map((H) => {
            if (H instanceof RegExp)
              for (const te of H.flags.split(""))
                c.add(te);
            return typeof H == "string" ? ae(H) : H === t.GLOBSTAR ? t.GLOBSTAR : H._src;
          });
          return O.forEach((H, te) => {
            const z = O[te + 1], de = O[te - 1];
            H !== t.GLOBSTAR || de === t.GLOBSTAR || (de === void 0 ? z !== void 0 && z !== t.GLOBSTAR ? O[te + 1] = "(?:\\/|" + m + "\\/)?" + z : O[te] = m : z === void 0 ? O[te - 1] = de + "(?:\\/|" + m + ")?" : z !== t.GLOBSTAR && (O[te - 1] = de + "(?:\\/|\\/" + m + "\\/)" + z, O[te + 1] = t.GLOBSTAR));
          }), O.filter((H) => H !== t.GLOBSTAR).join("/");
        }).join("|");
        const [j, G] = Z.length > 1 ? ["(?:", ")"] : ["", ""];
        E = "^" + j + E + G + "$", this.negate && (E = "^(?!" + E + ").+$");
        try {
          this.regexp = new RegExp(E, [...c].join(""));
        } catch {
          this.regexp = !1;
        }
        return this.regexp;
      }
      slashSplit(Z) {
        return this.preserveMultipleSlashes ? Z.split("/") : this.isWindows && /^\/\/[^\/]+/.test(Z) ? ["", ...Z.split(/\/+/)] : Z.split(/\/+/);
      }
      match(Z, X = this.partial) {
        if (this.debug("match", Z, this.pattern), this.comment)
          return !1;
        if (this.empty)
          return Z === "";
        if (Z === "/" && X)
          return !0;
        const m = this.options;
        this.isWindows && (Z = Z.split("\\").join("/"));
        const c = this.slashSplit(Z);
        this.debug(this.pattern, "split", c);
        const E = this.set;
        this.debug(this.pattern, "set", E);
        let j = c[c.length - 1];
        if (!j)
          for (let G = c.length - 2; !j && G >= 0; G--)
            j = c[G];
        for (let G = 0; G < E.length; G++) {
          const K = E[G];
          let O = c;
          if (m.matchBase && K.length === 1 && (O = [j]), this.matchOne(O, K, X))
            return m.flipNegate ? !0 : !this.negate;
        }
        return m.flipNegate ? !1 : this.negate;
      }
      static defaults(Z) {
        return t.minimatch.defaults(Z).Minimatch;
      }
    }
    t.Minimatch = ce;
    var Y = yy();
    Object.defineProperty(t, "AST", { enumerable: !0, get: function() {
      return Y.AST;
    } });
    var P = _y();
    Object.defineProperty(t, "escape", { enumerable: !0, get: function() {
      return P.escape;
    } });
    var q = fc();
    Object.defineProperty(t, "unescape", { enumerable: !0, get: function() {
      return q.unescape;
    } }), t.minimatch.AST = a.AST, t.minimatch.Minimatch = ce, t.minimatch.escape = n.escape, t.minimatch.unescape = i.unescape;
  })(Vr)), Vr;
}
var Qr = {}, ke = {}, en = {}, my;
function RA() {
  if (my) return en;
  my = 1, Object.defineProperty(en, "__esModule", { value: !0 }), en.LRUCache = void 0;
  const t = typeof performance == "object" && performance && typeof performance.now == "function" ? performance : Date, e = /* @__PURE__ */ new Set(), r = typeof process == "object" && process ? process : {}, o = (R, w, I, A) => {
    typeof r.emitWarning == "function" ? r.emitWarning(R, w, I, A) : console.error(`[${I}] ${w}: ${R}`);
  };
  let a = globalThis.AbortController, n = globalThis.AbortSignal;
  if (typeof a > "u") {
    n = class {
      onabort;
      _onabort = [];
      reason;
      aborted = !1;
      addEventListener(A, u) {
        this._onabort.push(u);
      }
    }, a = class {
      constructor() {
        w();
      }
      signal = new n();
      abort(A) {
        if (!this.signal.aborted) {
          this.signal.reason = A, this.signal.aborted = !0;
          for (const u of this.signal._onabort)
            u(A);
          this.signal.onabort?.(A);
        }
      }
    };
    let R = r.env?.LRU_CACHE_IGNORE_AC_WARNING !== "1";
    const w = () => {
      R && (R = !1, o("AbortController is not defined. If using lru-cache in node 14, load an AbortController polyfill from the `node-abort-controller` package. A minimal polyfill is provided for use by LRUCache.fetch(), but it should not be relied upon in other contexts (eg, passing it to other APIs that use AbortController/AbortSignal might have undesirable effects). You may disable this with LRU_CACHE_IGNORE_AC_WARNING=1 in the env.", "NO_ABORT_CONTROLLER", "ENOTSUP", w));
    };
  }
  const i = (R) => !e.has(R), l = (R) => R && R === Math.floor(R) && R > 0 && isFinite(R), s = (R) => l(R) ? R <= Math.pow(2, 8) ? Uint8Array : R <= Math.pow(2, 16) ? Uint16Array : R <= Math.pow(2, 32) ? Uint32Array : R <= Number.MAX_SAFE_INTEGER ? f : null : null;
  class f extends Array {
    constructor(w) {
      super(w), this.fill(0);
    }
  }
  class d {
    heap;
    length;
    // private constructor
    static #e = !1;
    static create(w) {
      const I = s(w);
      if (!I)
        return [];
      d.#e = !0;
      const A = new d(w, I);
      return d.#e = !1, A;
    }
    constructor(w, I) {
      if (!d.#e)
        throw new TypeError("instantiate Stack using Stack.create(n)");
      this.heap = new I(w), this.length = 0;
    }
    push(w) {
      this.heap[this.length++] = w;
    }
    pop() {
      return this.heap[--this.length];
    }
  }
  class y {
    // options that cannot be changed without disaster
    #e;
    #r;
    #i;
    #t;
    #o;
    #S;
    /**
     * {@link LRUCache.OptionsBase.ttl}
     */
    ttl;
    /**
     * {@link LRUCache.OptionsBase.ttlResolution}
     */
    ttlResolution;
    /**
     * {@link LRUCache.OptionsBase.ttlAutopurge}
     */
    ttlAutopurge;
    /**
     * {@link LRUCache.OptionsBase.updateAgeOnGet}
     */
    updateAgeOnGet;
    /**
     * {@link LRUCache.OptionsBase.updateAgeOnHas}
     */
    updateAgeOnHas;
    /**
     * {@link LRUCache.OptionsBase.allowStale}
     */
    allowStale;
    /**
     * {@link LRUCache.OptionsBase.noDisposeOnSet}
     */
    noDisposeOnSet;
    /**
     * {@link LRUCache.OptionsBase.noUpdateTTL}
     */
    noUpdateTTL;
    /**
     * {@link LRUCache.OptionsBase.maxEntrySize}
     */
    maxEntrySize;
    /**
     * {@link LRUCache.OptionsBase.sizeCalculation}
     */
    sizeCalculation;
    /**
     * {@link LRUCache.OptionsBase.noDeleteOnFetchRejection}
     */
    noDeleteOnFetchRejection;
    /**
     * {@link LRUCache.OptionsBase.noDeleteOnStaleGet}
     */
    noDeleteOnStaleGet;
    /**
     * {@link LRUCache.OptionsBase.allowStaleOnFetchAbort}
     */
    allowStaleOnFetchAbort;
    /**
     * {@link LRUCache.OptionsBase.allowStaleOnFetchRejection}
     */
    allowStaleOnFetchRejection;
    /**
     * {@link LRUCache.OptionsBase.ignoreFetchAbort}
     */
    ignoreFetchAbort;
    // computed properties
    #u;
    #c;
    #l;
    #a;
    #s;
    #b;
    #_;
    #g;
    #h;
    #E;
    #p;
    #R;
    #A;
    #y;
    #v;
    #w;
    #d;
    /**
     * Do not call this method unless you need to inspect the
     * inner workings of the cache.  If anything returned by this
     * object is modified in any way, strange breakage may occur.
     *
     * These fields are private for a reason!
     *
     * @internal
     */
    static unsafeExposeInternals(w) {
      return {
        // properties
        starts: w.#A,
        ttls: w.#y,
        sizes: w.#R,
        keyMap: w.#l,
        keyList: w.#a,
        valList: w.#s,
        next: w.#b,
        prev: w.#_,
        get head() {
          return w.#g;
        },
        get tail() {
          return w.#h;
        },
        free: w.#E,
        // methods
        isBackgroundFetch: (I) => w.#f(I),
        backgroundFetch: (I, A, u, g) => w.#j(I, A, u, g),
        moveToTail: (I) => w.#W(I),
        indexes: (I) => w.#I(I),
        rindexes: (I) => w.#D(I),
        isStale: (I) => w.#m(I)
      };
    }
    // Protected read-only members
    /**
     * {@link LRUCache.OptionsBase.max} (read-only)
     */
    get max() {
      return this.#e;
    }
    /**
     * {@link LRUCache.OptionsBase.maxSize} (read-only)
     */
    get maxSize() {
      return this.#r;
    }
    /**
     * The total computed size of items in the cache (read-only)
     */
    get calculatedSize() {
      return this.#c;
    }
    /**
     * The number of items stored in the cache (read-only)
     */
    get size() {
      return this.#u;
    }
    /**
     * {@link LRUCache.OptionsBase.fetchMethod} (read-only)
     */
    get fetchMethod() {
      return this.#o;
    }
    get memoMethod() {
      return this.#S;
    }
    /**
     * {@link LRUCache.OptionsBase.dispose} (read-only)
     */
    get dispose() {
      return this.#i;
    }
    /**
     * {@link LRUCache.OptionsBase.disposeAfter} (read-only)
     */
    get disposeAfter() {
      return this.#t;
    }
    constructor(w) {
      const { max: I = 0, ttl: A, ttlResolution: u = 1, ttlAutopurge: g, updateAgeOnGet: p, updateAgeOnHas: b, allowStale: C, dispose: N, disposeAfter: W, noDisposeOnSet: k, noUpdateTTL: L, maxSize: U = 0, maxEntrySize: x = 0, sizeCalculation: D, fetchMethod: F, memoMethod: T, noDeleteOnFetchRejection: J, noDeleteOnStaleGet: re, allowStaleOnFetchRejection: se, allowStaleOnFetchAbort: ne, ignoreFetchAbort: M } = w;
      if (I !== 0 && !l(I))
        throw new TypeError("max option must be a nonnegative integer");
      const Q = I ? s(I) : Array;
      if (!Q)
        throw new Error("invalid max value: " + I);
      if (this.#e = I, this.#r = U, this.maxEntrySize = x || this.#r, this.sizeCalculation = D, this.sizeCalculation) {
        if (!this.#r && !this.maxEntrySize)
          throw new TypeError("cannot set sizeCalculation without setting maxSize or maxEntrySize");
        if (typeof this.sizeCalculation != "function")
          throw new TypeError("sizeCalculation set to non-function");
      }
      if (T !== void 0 && typeof T != "function")
        throw new TypeError("memoMethod must be a function if defined");
      if (this.#S = T, F !== void 0 && typeof F != "function")
        throw new TypeError("fetchMethod must be a function if specified");
      if (this.#o = F, this.#w = !!F, this.#l = /* @__PURE__ */ new Map(), this.#a = new Array(I).fill(void 0), this.#s = new Array(I).fill(void 0), this.#b = new Q(I), this.#_ = new Q(I), this.#g = 0, this.#h = 0, this.#E = d.create(I), this.#u = 0, this.#c = 0, typeof N == "function" && (this.#i = N), typeof W == "function" ? (this.#t = W, this.#p = []) : (this.#t = void 0, this.#p = void 0), this.#v = !!this.#i, this.#d = !!this.#t, this.noDisposeOnSet = !!k, this.noUpdateTTL = !!L, this.noDeleteOnFetchRejection = !!J, this.allowStaleOnFetchRejection = !!se, this.allowStaleOnFetchAbort = !!ne, this.ignoreFetchAbort = !!M, this.maxEntrySize !== 0) {
        if (this.#r !== 0 && !l(this.#r))
          throw new TypeError("maxSize must be a positive integer if specified");
        if (!l(this.maxEntrySize))
          throw new TypeError("maxEntrySize must be a positive integer if specified");
        this.#O();
      }
      if (this.allowStale = !!C, this.noDeleteOnStaleGet = !!re, this.updateAgeOnGet = !!p, this.updateAgeOnHas = !!b, this.ttlResolution = l(u) || u === 0 ? u : 1, this.ttlAutopurge = !!g, this.ttl = A || 0, this.ttl) {
        if (!l(this.ttl))
          throw new TypeError("ttl must be a positive integer if specified");
        this.#T();
      }
      if (this.#e === 0 && this.ttl === 0 && this.#r === 0)
        throw new TypeError("At least one of max, maxSize, or ttl is required");
      if (!this.ttlAutopurge && !this.#e && !this.#r) {
        const v = "LRU_CACHE_UNBOUNDED";
        i(v) && (e.add(v), o("TTL caching without ttlAutopurge, max, or maxSize can result in unbounded memory consumption.", "UnboundedCacheWarning", v, y));
      }
    }
    /**
     * Return the number of ms left in the item's TTL. If item is not in cache,
     * returns `0`. Returns `Infinity` if item is in cache without a defined TTL.
     */
    getRemainingTTL(w) {
      return this.#l.has(w) ? 1 / 0 : 0;
    }
    #T() {
      const w = new f(this.#e), I = new f(this.#e);
      this.#y = w, this.#A = I, this.#L = (g, p, b = t.now()) => {
        if (I[g] = p !== 0 ? b : 0, w[g] = p, p !== 0 && this.ttlAutopurge) {
          const C = setTimeout(() => {
            this.#m(g) && this.#N(this.#a[g], "expire");
          }, p + 1);
          C.unref && C.unref();
        }
      }, this.#x = (g) => {
        I[g] = w[g] !== 0 ? t.now() : 0;
      }, this.#n = (g, p) => {
        if (w[p]) {
          const b = w[p], C = I[p];
          if (!b || !C)
            return;
          g.ttl = b, g.start = C, g.now = A || u();
          const N = g.now - C;
          g.remainingTTL = b - N;
        }
      };
      let A = 0;
      const u = () => {
        const g = t.now();
        if (this.ttlResolution > 0) {
          A = g;
          const p = setTimeout(() => A = 0, this.ttlResolution);
          p.unref && p.unref();
        }
        return g;
      };
      this.getRemainingTTL = (g) => {
        const p = this.#l.get(g);
        if (p === void 0)
          return 0;
        const b = w[p], C = I[p];
        if (!b || !C)
          return 1 / 0;
        const N = (A || u()) - C;
        return b - N;
      }, this.#m = (g) => {
        const p = I[g], b = w[g];
        return !!b && !!p && (A || u()) - p > b;
      };
    }
    // conditionally set private methods related to TTL
    #x = () => {
    };
    #n = () => {
    };
    #L = () => {
    };
    /* c8 ignore stop */
    #m = () => !1;
    #O() {
      const w = new f(this.#e);
      this.#c = 0, this.#R = w, this.#P = (I) => {
        this.#c -= w[I], w[I] = 0;
      }, this.#M = (I, A, u, g) => {
        if (this.#f(A))
          return 0;
        if (!l(u))
          if (g) {
            if (typeof g != "function")
              throw new TypeError("sizeCalculation must be a function");
            if (u = g(A, I), !l(u))
              throw new TypeError("sizeCalculation return invalid (expect positive integer)");
          } else
            throw new TypeError("invalid size value (must be positive integer). When maxSize or maxEntrySize is used, sizeCalculation or size must be set.");
        return u;
      }, this.#C = (I, A, u) => {
        if (w[I] = A, this.#r) {
          const g = this.#r - w[I];
          for (; this.#c > g; )
            this.#F(!0);
        }
        this.#c += w[I], u && (u.entrySize = A, u.totalCalculatedSize = this.#c);
      };
    }
    #P = (w) => {
    };
    #C = (w, I, A) => {
    };
    #M = (w, I, A, u) => {
      if (A || u)
        throw new TypeError("cannot set size without setting maxSize or maxEntrySize on cache");
      return 0;
    };
    *#I({ allowStale: w = this.allowStale } = {}) {
      if (this.#u)
        for (let I = this.#h; !(!this.#k(I) || ((w || !this.#m(I)) && (yield I), I === this.#g)); )
          I = this.#_[I];
    }
    *#D({ allowStale: w = this.allowStale } = {}) {
      if (this.#u)
        for (let I = this.#g; !(!this.#k(I) || ((w || !this.#m(I)) && (yield I), I === this.#h)); )
          I = this.#b[I];
    }
    #k(w) {
      return w !== void 0 && this.#l.get(this.#a[w]) === w;
    }
    /**
     * Return a generator yielding `[key, value]` pairs,
     * in order from most recently used to least recently used.
     */
    *entries() {
      for (const w of this.#I())
        this.#s[w] !== void 0 && this.#a[w] !== void 0 && !this.#f(this.#s[w]) && (yield [this.#a[w], this.#s[w]]);
    }
    /**
     * Inverse order version of {@link LRUCache.entries}
     *
     * Return a generator yielding `[key, value]` pairs,
     * in order from least recently used to most recently used.
     */
    *rentries() {
      for (const w of this.#D())
        this.#s[w] !== void 0 && this.#a[w] !== void 0 && !this.#f(this.#s[w]) && (yield [this.#a[w], this.#s[w]]);
    }
    /**
     * Return a generator yielding the keys in the cache,
     * in order from most recently used to least recently used.
     */
    *keys() {
      for (const w of this.#I()) {
        const I = this.#a[w];
        I !== void 0 && !this.#f(this.#s[w]) && (yield I);
      }
    }
    /**
     * Inverse order version of {@link LRUCache.keys}
     *
     * Return a generator yielding the keys in the cache,
     * in order from least recently used to most recently used.
     */
    *rkeys() {
      for (const w of this.#D()) {
        const I = this.#a[w];
        I !== void 0 && !this.#f(this.#s[w]) && (yield I);
      }
    }
    /**
     * Return a generator yielding the values in the cache,
     * in order from most recently used to least recently used.
     */
    *values() {
      for (const w of this.#I())
        this.#s[w] !== void 0 && !this.#f(this.#s[w]) && (yield this.#s[w]);
    }
    /**
     * Inverse order version of {@link LRUCache.values}
     *
     * Return a generator yielding the values in the cache,
     * in order from least recently used to most recently used.
     */
    *rvalues() {
      for (const w of this.#D())
        this.#s[w] !== void 0 && !this.#f(this.#s[w]) && (yield this.#s[w]);
    }
    /**
     * Iterating over the cache itself yields the same results as
     * {@link LRUCache.entries}
     */
    [Symbol.iterator]() {
      return this.entries();
    }
    /**
     * A String value that is used in the creation of the default string
     * description of an object. Called by the built-in method
     * `Object.prototype.toString`.
     */
    [Symbol.toStringTag] = "LRUCache";
    /**
     * Find a value for which the supplied fn method returns a truthy value,
     * similar to `Array.find()`. fn is called as `fn(value, key, cache)`.
     */
    find(w, I = {}) {
      for (const A of this.#I()) {
        const u = this.#s[A], g = this.#f(u) ? u.__staleWhileFetching : u;
        if (g !== void 0 && w(g, this.#a[A], this))
          return this.get(this.#a[A], I);
      }
    }
    /**
     * Call the supplied function on each item in the cache, in order from most
     * recently used to least recently used.
     *
     * `fn` is called as `fn(value, key, cache)`.
     *
     * If `thisp` is provided, function will be called in the `this`-context of
     * the provided object, or the cache if no `thisp` object is provided.
     *
     * Does not update age or recenty of use, or iterate over stale values.
     */
    forEach(w, I = this) {
      for (const A of this.#I()) {
        const u = this.#s[A], g = this.#f(u) ? u.__staleWhileFetching : u;
        g !== void 0 && w.call(I, g, this.#a[A], this);
      }
    }
    /**
     * The same as {@link LRUCache.forEach} but items are iterated over in
     * reverse order.  (ie, less recently used items are iterated over first.)
     */
    rforEach(w, I = this) {
      for (const A of this.#D()) {
        const u = this.#s[A], g = this.#f(u) ? u.__staleWhileFetching : u;
        g !== void 0 && w.call(I, g, this.#a[A], this);
      }
    }
    /**
     * Delete any stale entries. Returns true if anything was removed,
     * false otherwise.
     */
    purgeStale() {
      let w = !1;
      for (const I of this.#D({ allowStale: !0 }))
        this.#m(I) && (this.#N(this.#a[I], "expire"), w = !0);
      return w;
    }
    /**
     * Get the extended info about a given entry, to get its value, size, and
     * TTL info simultaneously. Returns `undefined` if the key is not present.
     *
     * Unlike {@link LRUCache#dump}, which is designed to be portable and survive
     * serialization, the `start` value is always the current timestamp, and the
     * `ttl` is a calculated remaining time to live (negative if expired).
     *
     * Always returns stale values, if their info is found in the cache, so be
     * sure to check for expirations (ie, a negative {@link LRUCache.Entry#ttl})
     * if relevant.
     */
    info(w) {
      const I = this.#l.get(w);
      if (I === void 0)
        return;
      const A = this.#s[I], u = this.#f(A) ? A.__staleWhileFetching : A;
      if (u === void 0)
        return;
      const g = { value: u };
      if (this.#y && this.#A) {
        const p = this.#y[I], b = this.#A[I];
        if (p && b) {
          const C = p - (t.now() - b);
          g.ttl = C, g.start = Date.now();
        }
      }
      return this.#R && (g.size = this.#R[I]), g;
    }
    /**
     * Return an array of [key, {@link LRUCache.Entry}] tuples which can be
     * passed to {@link LRLUCache#load}.
     *
     * The `start` fields are calculated relative to a portable `Date.now()`
     * timestamp, even if `performance.now()` is available.
     *
     * Stale entries are always included in the `dump`, even if
     * {@link LRUCache.OptionsBase.allowStale} is false.
     *
     * Note: this returns an actual array, not a generator, so it can be more
     * easily passed around.
     */
    dump() {
      const w = [];
      for (const I of this.#I({ allowStale: !0 })) {
        const A = this.#a[I], u = this.#s[I], g = this.#f(u) ? u.__staleWhileFetching : u;
        if (g === void 0 || A === void 0)
          continue;
        const p = { value: g };
        if (this.#y && this.#A) {
          p.ttl = this.#y[I];
          const b = t.now() - this.#A[I];
          p.start = Math.floor(Date.now() - b);
        }
        this.#R && (p.size = this.#R[I]), w.unshift([A, p]);
      }
      return w;
    }
    /**
     * Reset the cache and load in the items in entries in the order listed.
     *
     * The shape of the resulting cache may be different if the same options are
     * not used in both caches.
     *
     * The `start` fields are assumed to be calculated relative to a portable
     * `Date.now()` timestamp, even if `performance.now()` is available.
     */
    load(w) {
      this.clear();
      for (const [I, A] of w) {
        if (A.start) {
          const u = Date.now() - A.start;
          A.start = t.now() - u;
        }
        this.set(I, A.value, A);
      }
    }
    /**
     * Add a value to the cache.
     *
     * Note: if `undefined` is specified as a value, this is an alias for
     * {@link LRUCache#delete}
     *
     * Fields on the {@link LRUCache.SetOptions} options param will override
     * their corresponding values in the constructor options for the scope
     * of this single `set()` operation.
     *
     * If `start` is provided, then that will set the effective start
     * time for the TTL calculation. Note that this must be a previous
     * value of `performance.now()` if supported, or a previous value of
     * `Date.now()` if not.
     *
     * Options object may also include `size`, which will prevent
     * calling the `sizeCalculation` function and just use the specified
     * number if it is a positive integer, and `noDisposeOnSet` which
     * will prevent calling a `dispose` function in the case of
     * overwrites.
     *
     * If the `size` (or return value of `sizeCalculation`) for a given
     * entry is greater than `maxEntrySize`, then the item will not be
     * added to the cache.
     *
     * Will update the recency of the entry.
     *
     * If the value is `undefined`, then this is an alias for
     * `cache.delete(key)`. `undefined` is never stored in the cache.
     */
    set(w, I, A = {}) {
      if (I === void 0)
        return this.delete(w), this;
      const { ttl: u = this.ttl, start: g, noDisposeOnSet: p = this.noDisposeOnSet, sizeCalculation: b = this.sizeCalculation, status: C } = A;
      let { noUpdateTTL: N = this.noUpdateTTL } = A;
      const W = this.#M(w, I, A.size || 0, b);
      if (this.maxEntrySize && W > this.maxEntrySize)
        return C && (C.set = "miss", C.maxEntrySizeExceeded = !0), this.#N(w, "set"), this;
      let k = this.#u === 0 ? void 0 : this.#l.get(w);
      if (k === void 0)
        k = this.#u === 0 ? this.#h : this.#E.length !== 0 ? this.#E.pop() : this.#u === this.#e ? this.#F(!1) : this.#u, this.#a[k] = w, this.#s[k] = I, this.#l.set(w, k), this.#b[this.#h] = k, this.#_[k] = this.#h, this.#h = k, this.#u++, this.#C(k, W, C), C && (C.set = "add"), N = !1;
      else {
        this.#W(k);
        const L = this.#s[k];
        if (I !== L) {
          if (this.#w && this.#f(L)) {
            L.__abortController.abort(new Error("replaced"));
            const { __staleWhileFetching: U } = L;
            U !== void 0 && !p && (this.#v && this.#i?.(U, w, "set"), this.#d && this.#p?.push([U, w, "set"]));
          } else p || (this.#v && this.#i?.(L, w, "set"), this.#d && this.#p?.push([L, w, "set"]));
          if (this.#P(k), this.#C(k, W, C), this.#s[k] = I, C) {
            C.set = "replace";
            const U = L && this.#f(L) ? L.__staleWhileFetching : L;
            U !== void 0 && (C.oldValue = U);
          }
        } else C && (C.set = "update");
      }
      if (u !== 0 && !this.#y && this.#T(), this.#y && (N || this.#L(k, u, g), C && this.#n(C, k)), !p && this.#d && this.#p) {
        const L = this.#p;
        let U;
        for (; U = L?.shift(); )
          this.#t?.(...U);
      }
      return this;
    }
    /**
     * Evict the least recently used item, returning its value or
     * `undefined` if cache is empty.
     */
    pop() {
      try {
        for (; this.#u; ) {
          const w = this.#s[this.#g];
          if (this.#F(!0), this.#f(w)) {
            if (w.__staleWhileFetching)
              return w.__staleWhileFetching;
          } else if (w !== void 0)
            return w;
        }
      } finally {
        if (this.#d && this.#p) {
          const w = this.#p;
          let I;
          for (; I = w?.shift(); )
            this.#t?.(...I);
        }
      }
    }
    #F(w) {
      const I = this.#g, A = this.#a[I], u = this.#s[I];
      return this.#w && this.#f(u) ? u.__abortController.abort(new Error("evicted")) : (this.#v || this.#d) && (this.#v && this.#i?.(u, A, "evict"), this.#d && this.#p?.push([u, A, "evict"])), this.#P(I), w && (this.#a[I] = void 0, this.#s[I] = void 0, this.#E.push(I)), this.#u === 1 ? (this.#g = this.#h = 0, this.#E.length = 0) : this.#g = this.#b[I], this.#l.delete(A), this.#u--, I;
    }
    /**
     * Check if a key is in the cache, without updating the recency of use.
     * Will return false if the item is stale, even though it is technically
     * in the cache.
     *
     * Check if a key is in the cache, without updating the recency of
     * use. Age is updated if {@link LRUCache.OptionsBase.updateAgeOnHas} is set
     * to `true` in either the options or the constructor.
     *
     * Will return `false` if the item is stale, even though it is technically in
     * the cache. The difference can be determined (if it matters) by using a
     * `status` argument, and inspecting the `has` field.
     *
     * Will not update item age unless
     * {@link LRUCache.OptionsBase.updateAgeOnHas} is set.
     */
    has(w, I = {}) {
      const { updateAgeOnHas: A = this.updateAgeOnHas, status: u } = I, g = this.#l.get(w);
      if (g !== void 0) {
        const p = this.#s[g];
        if (this.#f(p) && p.__staleWhileFetching === void 0)
          return !1;
        if (this.#m(g))
          u && (u.has = "stale", this.#n(u, g));
        else return A && this.#x(g), u && (u.has = "hit", this.#n(u, g)), !0;
      } else u && (u.has = "miss");
      return !1;
    }
    /**
     * Like {@link LRUCache#get} but doesn't update recency or delete stale
     * items.
     *
     * Returns `undefined` if the item is stale, unless
     * {@link LRUCache.OptionsBase.allowStale} is set.
     */
    peek(w, I = {}) {
      const { allowStale: A = this.allowStale } = I, u = this.#l.get(w);
      if (u === void 0 || !A && this.#m(u))
        return;
      const g = this.#s[u];
      return this.#f(g) ? g.__staleWhileFetching : g;
    }
    #j(w, I, A, u) {
      const g = I === void 0 ? void 0 : this.#s[I];
      if (this.#f(g))
        return g;
      const p = new a(), { signal: b } = A;
      b?.addEventListener("abort", () => p.abort(b.reason), {
        signal: p.signal
      });
      const C = {
        signal: p.signal,
        options: A,
        context: u
      }, N = (D, F = !1) => {
        const { aborted: T } = p.signal, J = A.ignoreFetchAbort && D !== void 0;
        if (A.status && (T && !F ? (A.status.fetchAborted = !0, A.status.fetchError = p.signal.reason, J && (A.status.fetchAbortIgnored = !0)) : A.status.fetchResolved = !0), T && !J && !F)
          return k(p.signal.reason);
        const re = U;
        return this.#s[I] === U && (D === void 0 ? re.__staleWhileFetching ? this.#s[I] = re.__staleWhileFetching : this.#N(w, "fetch") : (A.status && (A.status.fetchUpdated = !0), this.set(w, D, C.options))), D;
      }, W = (D) => (A.status && (A.status.fetchRejected = !0, A.status.fetchError = D), k(D)), k = (D) => {
        const { aborted: F } = p.signal, T = F && A.allowStaleOnFetchAbort, J = T || A.allowStaleOnFetchRejection, re = J || A.noDeleteOnFetchRejection, se = U;
        if (this.#s[I] === U && (!re || se.__staleWhileFetching === void 0 ? this.#N(w, "fetch") : T || (this.#s[I] = se.__staleWhileFetching)), J)
          return A.status && se.__staleWhileFetching !== void 0 && (A.status.returnedStale = !0), se.__staleWhileFetching;
        if (se.__returned === se)
          throw D;
      }, L = (D, F) => {
        const T = this.#o?.(w, g, C);
        T && T instanceof Promise && T.then((J) => D(J === void 0 ? void 0 : J), F), p.signal.addEventListener("abort", () => {
          (!A.ignoreFetchAbort || A.allowStaleOnFetchAbort) && (D(void 0), A.allowStaleOnFetchAbort && (D = (J) => N(J, !0)));
        });
      };
      A.status && (A.status.fetchDispatched = !0);
      const U = new Promise(L).then(N, W), x = Object.assign(U, {
        __abortController: p,
        __staleWhileFetching: g,
        __returned: void 0
      });
      return I === void 0 ? (this.set(w, x, { ...C.options, status: void 0 }), I = this.#l.get(w)) : this.#s[I] = x, x;
    }
    #f(w) {
      if (!this.#w)
        return !1;
      const I = w;
      return !!I && I instanceof Promise && I.hasOwnProperty("__staleWhileFetching") && I.__abortController instanceof a;
    }
    async fetch(w, I = {}) {
      const {
        // get options
        allowStale: A = this.allowStale,
        updateAgeOnGet: u = this.updateAgeOnGet,
        noDeleteOnStaleGet: g = this.noDeleteOnStaleGet,
        // set options
        ttl: p = this.ttl,
        noDisposeOnSet: b = this.noDisposeOnSet,
        size: C = 0,
        sizeCalculation: N = this.sizeCalculation,
        noUpdateTTL: W = this.noUpdateTTL,
        // fetch exclusive options
        noDeleteOnFetchRejection: k = this.noDeleteOnFetchRejection,
        allowStaleOnFetchRejection: L = this.allowStaleOnFetchRejection,
        ignoreFetchAbort: U = this.ignoreFetchAbort,
        allowStaleOnFetchAbort: x = this.allowStaleOnFetchAbort,
        context: D,
        forceRefresh: F = !1,
        status: T,
        signal: J
      } = I;
      if (!this.#w)
        return T && (T.fetch = "get"), this.get(w, {
          allowStale: A,
          updateAgeOnGet: u,
          noDeleteOnStaleGet: g,
          status: T
        });
      const re = {
        allowStale: A,
        updateAgeOnGet: u,
        noDeleteOnStaleGet: g,
        ttl: p,
        noDisposeOnSet: b,
        size: C,
        sizeCalculation: N,
        noUpdateTTL: W,
        noDeleteOnFetchRejection: k,
        allowStaleOnFetchRejection: L,
        allowStaleOnFetchAbort: x,
        ignoreFetchAbort: U,
        status: T,
        signal: J
      };
      let se = this.#l.get(w);
      if (se === void 0) {
        T && (T.fetch = "miss");
        const ne = this.#j(w, se, re, D);
        return ne.__returned = ne;
      } else {
        const ne = this.#s[se];
        if (this.#f(ne)) {
          const ee = A && ne.__staleWhileFetching !== void 0;
          return T && (T.fetch = "inflight", ee && (T.returnedStale = !0)), ee ? ne.__staleWhileFetching : ne.__returned = ne;
        }
        const M = this.#m(se);
        if (!F && !M)
          return T && (T.fetch = "hit"), this.#W(se), u && this.#x(se), T && this.#n(T, se), ne;
        const Q = this.#j(w, se, re, D), $ = Q.__staleWhileFetching !== void 0 && A;
        return T && (T.fetch = M ? "stale" : "refresh", $ && M && (T.returnedStale = !0)), $ ? Q.__staleWhileFetching : Q.__returned = Q;
      }
    }
    async forceFetch(w, I = {}) {
      const A = await this.fetch(w, I);
      if (A === void 0)
        throw new Error("fetch() returned undefined");
      return A;
    }
    memo(w, I = {}) {
      const A = this.#S;
      if (!A)
        throw new Error("no memoMethod provided to constructor");
      const { context: u, forceRefresh: g, ...p } = I, b = this.get(w, p);
      if (!g && b !== void 0)
        return b;
      const C = A(w, b, {
        options: p,
        context: u
      });
      return this.set(w, C, p), C;
    }
    /**
     * Return a value from the cache. Will update the recency of the cache
     * entry found.
     *
     * If the key is not found, get() will return `undefined`.
     */
    get(w, I = {}) {
      const { allowStale: A = this.allowStale, updateAgeOnGet: u = this.updateAgeOnGet, noDeleteOnStaleGet: g = this.noDeleteOnStaleGet, status: p } = I, b = this.#l.get(w);
      if (b !== void 0) {
        const C = this.#s[b], N = this.#f(C);
        return p && this.#n(p, b), this.#m(b) ? (p && (p.get = "stale"), N ? (p && A && C.__staleWhileFetching !== void 0 && (p.returnedStale = !0), A ? C.__staleWhileFetching : void 0) : (g || this.#N(w, "expire"), p && A && (p.returnedStale = !0), A ? C : void 0)) : (p && (p.get = "hit"), N ? C.__staleWhileFetching : (this.#W(b), u && this.#x(b), C));
      } else p && (p.get = "miss");
    }
    #$(w, I) {
      this.#_[I] = w, this.#b[w] = I;
    }
    #W(w) {
      w !== this.#h && (w === this.#g ? this.#g = this.#b[w] : this.#$(this.#_[w], this.#b[w]), this.#$(this.#h, w), this.#h = w);
    }
    /**
     * Deletes a key out of the cache.
     *
     * Returns true if the key was deleted, false otherwise.
     */
    delete(w) {
      return this.#N(w, "delete");
    }
    #N(w, I) {
      let A = !1;
      if (this.#u !== 0) {
        const u = this.#l.get(w);
        if (u !== void 0)
          if (A = !0, this.#u === 1)
            this.#q(I);
          else {
            this.#P(u);
            const g = this.#s[u];
            if (this.#f(g) ? g.__abortController.abort(new Error("deleted")) : (this.#v || this.#d) && (this.#v && this.#i?.(g, w, I), this.#d && this.#p?.push([g, w, I])), this.#l.delete(w), this.#a[u] = void 0, this.#s[u] = void 0, u === this.#h)
              this.#h = this.#_[u];
            else if (u === this.#g)
              this.#g = this.#b[u];
            else {
              const p = this.#_[u];
              this.#b[p] = this.#b[u];
              const b = this.#b[u];
              this.#_[b] = this.#_[u];
            }
            this.#u--, this.#E.push(u);
          }
      }
      if (this.#d && this.#p?.length) {
        const u = this.#p;
        let g;
        for (; g = u?.shift(); )
          this.#t?.(...g);
      }
      return A;
    }
    /**
     * Clear the cache entirely, throwing away all values.
     */
    clear() {
      return this.#q("delete");
    }
    #q(w) {
      for (const I of this.#D({ allowStale: !0 })) {
        const A = this.#s[I];
        if (this.#f(A))
          A.__abortController.abort(new Error("deleted"));
        else {
          const u = this.#a[I];
          this.#v && this.#i?.(A, u, w), this.#d && this.#p?.push([A, u, w]);
        }
      }
      if (this.#l.clear(), this.#s.fill(void 0), this.#a.fill(void 0), this.#y && this.#A && (this.#y.fill(0), this.#A.fill(0)), this.#R && this.#R.fill(0), this.#g = 0, this.#h = 0, this.#E.length = 0, this.#c = 0, this.#u = 0, this.#d && this.#p) {
        const I = this.#p;
        let A;
        for (; A = I?.shift(); )
          this.#t?.(...A);
      }
    }
  }
  return en.LRUCache = y, en;
}
var tn = {}, Sy;
function Ym() {
  return Sy || (Sy = 1, (function(t) {
    var e = tn && tn.__importDefault || function(c) {
      return c && c.__esModule ? c : { default: c };
    };
    Object.defineProperty(t, "__esModule", { value: !0 }), t.Minipass = t.isWritable = t.isReadable = t.isStream = void 0;
    const r = typeof process == "object" && process ? process : {
      stdout: null,
      stderr: null
    }, o = wE, a = e(mE), n = SE, i = (c) => !!c && typeof c == "object" && (c instanceof m || c instanceof a.default || (0, t.isReadable)(c) || (0, t.isWritable)(c));
    t.isStream = i;
    const l = (c) => !!c && typeof c == "object" && c instanceof o.EventEmitter && typeof c.pipe == "function" && // node core Writable streams have a pipe() method, but it throws
    c.pipe !== a.default.Writable.prototype.pipe;
    t.isReadable = l;
    const s = (c) => !!c && typeof c == "object" && c instanceof o.EventEmitter && typeof c.write == "function" && typeof c.end == "function";
    t.isWritable = s;
    const f = /* @__PURE__ */ Symbol("EOF"), d = /* @__PURE__ */ Symbol("maybeEmitEnd"), y = /* @__PURE__ */ Symbol("emittedEnd"), R = /* @__PURE__ */ Symbol("emittingEnd"), w = /* @__PURE__ */ Symbol("emittedError"), I = /* @__PURE__ */ Symbol("closed"), A = /* @__PURE__ */ Symbol("read"), u = /* @__PURE__ */ Symbol("flush"), g = /* @__PURE__ */ Symbol("flushChunk"), p = /* @__PURE__ */ Symbol("encoding"), b = /* @__PURE__ */ Symbol("decoder"), C = /* @__PURE__ */ Symbol("flowing"), N = /* @__PURE__ */ Symbol("paused"), W = /* @__PURE__ */ Symbol("resume"), k = /* @__PURE__ */ Symbol("buffer"), L = /* @__PURE__ */ Symbol("pipes"), U = /* @__PURE__ */ Symbol("bufferLength"), x = /* @__PURE__ */ Symbol("bufferPush"), D = /* @__PURE__ */ Symbol("bufferShift"), F = /* @__PURE__ */ Symbol("objectMode"), T = /* @__PURE__ */ Symbol("destroyed"), J = /* @__PURE__ */ Symbol("error"), re = /* @__PURE__ */ Symbol("emitData"), se = /* @__PURE__ */ Symbol("emitEnd"), ne = /* @__PURE__ */ Symbol("emitEnd2"), M = /* @__PURE__ */ Symbol("async"), Q = /* @__PURE__ */ Symbol("abort"), v = /* @__PURE__ */ Symbol("aborted"), $ = /* @__PURE__ */ Symbol("signal"), ee = /* @__PURE__ */ Symbol("dataListeners"), B = /* @__PURE__ */ Symbol("discarded"), le = (c) => Promise.resolve().then(c), ae = (c) => c(), ce = (c) => c === "end" || c === "finish" || c === "prefinish", Y = (c) => c instanceof ArrayBuffer || !!c && typeof c == "object" && c.constructor && c.constructor.name === "ArrayBuffer" && c.byteLength >= 0, P = (c) => !Buffer.isBuffer(c) && ArrayBuffer.isView(c);
    class q {
      src;
      dest;
      opts;
      ondrain;
      constructor(E, j, G) {
        this.src = E, this.dest = j, this.opts = G, this.ondrain = () => E[W](), this.dest.on("drain", this.ondrain);
      }
      unpipe() {
        this.dest.removeListener("drain", this.ondrain);
      }
      // only here for the prototype
      /* c8 ignore start */
      proxyErrors(E) {
      }
      /* c8 ignore stop */
      end() {
        this.unpipe(), this.opts.end && this.dest.end();
      }
    }
    class V extends q {
      unpipe() {
        this.src.removeListener("error", this.proxyErrors), super.unpipe();
      }
      constructor(E, j, G) {
        super(E, j, G), this.proxyErrors = (K) => this.dest.emit("error", K), E.on("error", this.proxyErrors);
      }
    }
    const Z = (c) => !!c.objectMode, X = (c) => !c.objectMode && !!c.encoding && c.encoding !== "buffer";
    class m extends o.EventEmitter {
      [C] = !1;
      [N] = !1;
      [L] = [];
      [k] = [];
      [F];
      [p];
      [M];
      [b];
      [f] = !1;
      [y] = !1;
      [R] = !1;
      [I] = !1;
      [w] = null;
      [U] = 0;
      [T] = !1;
      [$];
      [v] = !1;
      [ee] = 0;
      [B] = !1;
      /**
       * true if the stream can be written
       */
      writable = !0;
      /**
       * true if the stream can be read
       */
      readable = !0;
      /**
       * If `RType` is Buffer, then options do not need to be provided.
       * Otherwise, an options object must be provided to specify either
       * {@link Minipass.SharedOptions.objectMode} or
       * {@link Minipass.SharedOptions.encoding}, as appropriate.
       */
      constructor(...E) {
        const j = E[0] || {};
        if (super(), j.objectMode && typeof j.encoding == "string")
          throw new TypeError("Encoding and objectMode may not be used together");
        Z(j) ? (this[F] = !0, this[p] = null) : X(j) ? (this[p] = j.encoding, this[F] = !1) : (this[F] = !1, this[p] = null), this[M] = !!j.async, this[b] = this[p] ? new n.StringDecoder(this[p]) : null, j && j.debugExposeBuffer === !0 && Object.defineProperty(this, "buffer", { get: () => this[k] }), j && j.debugExposePipes === !0 && Object.defineProperty(this, "pipes", { get: () => this[L] });
        const { signal: G } = j;
        G && (this[$] = G, G.aborted ? this[Q]() : G.addEventListener("abort", () => this[Q]()));
      }
      /**
       * The amount of data stored in the buffer waiting to be read.
       *
       * For Buffer strings, this will be the total byte length.
       * For string encoding streams, this will be the string character length,
       * according to JavaScript's `string.length` logic.
       * For objectMode streams, this is a count of the items waiting to be
       * emitted.
       */
      get bufferLength() {
        return this[U];
      }
      /**
       * The `BufferEncoding` currently in use, or `null`
       */
      get encoding() {
        return this[p];
      }
      /**
       * @deprecated - This is a read only property
       */
      set encoding(E) {
        throw new Error("Encoding must be set at instantiation time");
      }
      /**
       * @deprecated - Encoding may only be set at instantiation time
       */
      setEncoding(E) {
        throw new Error("Encoding must be set at instantiation time");
      }
      /**
       * True if this is an objectMode stream
       */
      get objectMode() {
        return this[F];
      }
      /**
       * @deprecated - This is a read-only property
       */
      set objectMode(E) {
        throw new Error("objectMode must be set at instantiation time");
      }
      /**
       * true if this is an async stream
       */
      get async() {
        return this[M];
      }
      /**
       * Set to true to make this stream async.
       *
       * Once set, it cannot be unset, as this would potentially cause incorrect
       * behavior.  Ie, a sync stream can be made async, but an async stream
       * cannot be safely made sync.
       */
      set async(E) {
        this[M] = this[M] || !!E;
      }
      // drop everything and get out of the flow completely
      [Q]() {
        this[v] = !0, this.emit("abort", this[$]?.reason), this.destroy(this[$]?.reason);
      }
      /**
       * True if the stream has been aborted.
       */
      get aborted() {
        return this[v];
      }
      /**
       * No-op setter. Stream aborted status is set via the AbortSignal provided
       * in the constructor options.
       */
      set aborted(E) {
      }
      write(E, j, G) {
        if (this[v])
          return !1;
        if (this[f])
          throw new Error("write after end");
        if (this[T])
          return this.emit("error", Object.assign(new Error("Cannot call write after a stream was destroyed"), { code: "ERR_STREAM_DESTROYED" })), !0;
        typeof j == "function" && (G = j, j = "utf8"), j || (j = "utf8");
        const K = this[M] ? le : ae;
        if (!this[F] && !Buffer.isBuffer(E)) {
          if (P(E))
            E = Buffer.from(E.buffer, E.byteOffset, E.byteLength);
          else if (Y(E))
            E = Buffer.from(E);
          else if (typeof E != "string")
            throw new Error("Non-contiguous data written to non-objectMode stream");
        }
        return this[F] ? (this[C] && this[U] !== 0 && this[u](!0), this[C] ? this.emit("data", E) : this[x](E), this[U] !== 0 && this.emit("readable"), G && K(G), this[C]) : E.length ? (typeof E == "string" && // unless it is a string already ready for us to use
        !(j === this[p] && !this[b]?.lastNeed) && (E = Buffer.from(E, j)), Buffer.isBuffer(E) && this[p] && (E = this[b].write(E)), this[C] && this[U] !== 0 && this[u](!0), this[C] ? this.emit("data", E) : this[x](E), this[U] !== 0 && this.emit("readable"), G && K(G), this[C]) : (this[U] !== 0 && this.emit("readable"), G && K(G), this[C]);
      }
      /**
       * Low-level explicit read method.
       *
       * In objectMode, the argument is ignored, and one item is returned if
       * available.
       *
       * `n` is the number of bytes (or in the case of encoding streams,
       * characters) to consume. If `n` is not provided, then the entire buffer
       * is returned, or `null` is returned if no data is available.
       *
       * If `n` is greater that the amount of data in the internal buffer,
       * then `null` is returned.
       */
      read(E) {
        if (this[T])
          return null;
        if (this[B] = !1, this[U] === 0 || E === 0 || E && E > this[U])
          return this[d](), null;
        this[F] && (E = null), this[k].length > 1 && !this[F] && (this[k] = [
          this[p] ? this[k].join("") : Buffer.concat(this[k], this[U])
        ]);
        const j = this[A](E || null, this[k][0]);
        return this[d](), j;
      }
      [A](E, j) {
        if (this[F])
          this[D]();
        else {
          const G = j;
          E === G.length || E === null ? this[D]() : typeof G == "string" ? (this[k][0] = G.slice(E), j = G.slice(0, E), this[U] -= E) : (this[k][0] = G.subarray(E), j = G.subarray(0, E), this[U] -= E);
        }
        return this.emit("data", j), !this[k].length && !this[f] && this.emit("drain"), j;
      }
      end(E, j, G) {
        return typeof E == "function" && (G = E, E = void 0), typeof j == "function" && (G = j, j = "utf8"), E !== void 0 && this.write(E, j), G && this.once("end", G), this[f] = !0, this.writable = !1, (this[C] || !this[N]) && this[d](), this;
      }
      // don't let the internal resume be overwritten
      [W]() {
        this[T] || (!this[ee] && !this[L].length && (this[B] = !0), this[N] = !1, this[C] = !0, this.emit("resume"), this[k].length ? this[u]() : this[f] ? this[d]() : this.emit("drain"));
      }
      /**
       * Resume the stream if it is currently in a paused state
       *
       * If called when there are no pipe destinations or `data` event listeners,
       * this will place the stream in a "discarded" state, where all data will
       * be thrown away. The discarded state is removed if a pipe destination or
       * data handler is added, if pause() is called, or if any synchronous or
       * asynchronous iteration is started.
       */
      resume() {
        return this[W]();
      }
      /**
       * Pause the stream
       */
      pause() {
        this[C] = !1, this[N] = !0, this[B] = !1;
      }
      /**
       * true if the stream has been forcibly destroyed
       */
      get destroyed() {
        return this[T];
      }
      /**
       * true if the stream is currently in a flowing state, meaning that
       * any writes will be immediately emitted.
       */
      get flowing() {
        return this[C];
      }
      /**
       * true if the stream is currently in a paused state
       */
      get paused() {
        return this[N];
      }
      [x](E) {
        this[F] ? this[U] += 1 : this[U] += E.length, this[k].push(E);
      }
      [D]() {
        return this[F] ? this[U] -= 1 : this[U] -= this[k][0].length, this[k].shift();
      }
      [u](E = !1) {
        do
          ;
        while (this[g](this[D]()) && this[k].length);
        !E && !this[k].length && !this[f] && this.emit("drain");
      }
      [g](E) {
        return this.emit("data", E), this[C];
      }
      /**
       * Pipe all data emitted by this stream into the destination provided.
       *
       * Triggers the flow of data.
       */
      pipe(E, j) {
        if (this[T])
          return E;
        this[B] = !1;
        const G = this[y];
        return j = j || {}, E === r.stdout || E === r.stderr ? j.end = !1 : j.end = j.end !== !1, j.proxyErrors = !!j.proxyErrors, G ? j.end && E.end() : (this[L].push(j.proxyErrors ? new V(this, E, j) : new q(this, E, j)), this[M] ? le(() => this[W]()) : this[W]()), E;
      }
      /**
       * Fully unhook a piped destination stream.
       *
       * If the destination stream was the only consumer of this stream (ie,
       * there are no other piped destinations or `'data'` event listeners)
       * then the flow of data will stop until there is another consumer or
       * {@link Minipass#resume} is explicitly called.
       */
      unpipe(E) {
        const j = this[L].find((G) => G.dest === E);
        j && (this[L].length === 1 ? (this[C] && this[ee] === 0 && (this[C] = !1), this[L] = []) : this[L].splice(this[L].indexOf(j), 1), j.unpipe());
      }
      /**
       * Alias for {@link Minipass#on}
       */
      addListener(E, j) {
        return this.on(E, j);
      }
      /**
       * Mostly identical to `EventEmitter.on`, with the following
       * behavior differences to prevent data loss and unnecessary hangs:
       *
       * - Adding a 'data' event handler will trigger the flow of data
       *
       * - Adding a 'readable' event handler when there is data waiting to be read
       *   will cause 'readable' to be emitted immediately.
       *
       * - Adding an 'endish' event handler ('end', 'finish', etc.) which has
       *   already passed will cause the event to be emitted immediately and all
       *   handlers removed.
       *
       * - Adding an 'error' event handler after an error has been emitted will
       *   cause the event to be re-emitted immediately with the error previously
       *   raised.
       */
      on(E, j) {
        const G = super.on(E, j);
        if (E === "data")
          this[B] = !1, this[ee]++, !this[L].length && !this[C] && this[W]();
        else if (E === "readable" && this[U] !== 0)
          super.emit("readable");
        else if (ce(E) && this[y])
          super.emit(E), this.removeAllListeners(E);
        else if (E === "error" && this[w]) {
          const K = j;
          this[M] ? le(() => K.call(this, this[w])) : K.call(this, this[w]);
        }
        return G;
      }
      /**
       * Alias for {@link Minipass#off}
       */
      removeListener(E, j) {
        return this.off(E, j);
      }
      /**
       * Mostly identical to `EventEmitter.off`
       *
       * If a 'data' event handler is removed, and it was the last consumer
       * (ie, there are no pipe destinations or other 'data' event listeners),
       * then the flow of data will stop until there is another consumer or
       * {@link Minipass#resume} is explicitly called.
       */
      off(E, j) {
        const G = super.off(E, j);
        return E === "data" && (this[ee] = this.listeners("data").length, this[ee] === 0 && !this[B] && !this[L].length && (this[C] = !1)), G;
      }
      /**
       * Mostly identical to `EventEmitter.removeAllListeners`
       *
       * If all 'data' event handlers are removed, and they were the last consumer
       * (ie, there are no pipe destinations), then the flow of data will stop
       * until there is another consumer or {@link Minipass#resume} is explicitly
       * called.
       */
      removeAllListeners(E) {
        const j = super.removeAllListeners(E);
        return (E === "data" || E === void 0) && (this[ee] = 0, !this[B] && !this[L].length && (this[C] = !1)), j;
      }
      /**
       * true if the 'end' event has been emitted
       */
      get emittedEnd() {
        return this[y];
      }
      [d]() {
        !this[R] && !this[y] && !this[T] && this[k].length === 0 && this[f] && (this[R] = !0, this.emit("end"), this.emit("prefinish"), this.emit("finish"), this[I] && this.emit("close"), this[R] = !1);
      }
      /**
       * Mostly identical to `EventEmitter.emit`, with the following
       * behavior differences to prevent data loss and unnecessary hangs:
       *
       * If the stream has been destroyed, and the event is something other
       * than 'close' or 'error', then `false` is returned and no handlers
       * are called.
       *
       * If the event is 'end', and has already been emitted, then the event
       * is ignored. If the stream is in a paused or non-flowing state, then
       * the event will be deferred until data flow resumes. If the stream is
       * async, then handlers will be called on the next tick rather than
       * immediately.
       *
       * If the event is 'close', and 'end' has not yet been emitted, then
       * the event will be deferred until after 'end' is emitted.
       *
       * If the event is 'error', and an AbortSignal was provided for the stream,
       * and there are no listeners, then the event is ignored, matching the
       * behavior of node core streams in the presense of an AbortSignal.
       *
       * If the event is 'finish' or 'prefinish', then all listeners will be
       * removed after emitting the event, to prevent double-firing.
       */
      emit(E, ...j) {
        const G = j[0];
        if (E !== "error" && E !== "close" && E !== T && this[T])
          return !1;
        if (E === "data")
          return !this[F] && !G ? !1 : this[M] ? (le(() => this[re](G)), !0) : this[re](G);
        if (E === "end")
          return this[se]();
        if (E === "close") {
          if (this[I] = !0, !this[y] && !this[T])
            return !1;
          const O = super.emit("close");
          return this.removeAllListeners("close"), O;
        } else if (E === "error") {
          this[w] = G, super.emit(J, G);
          const O = !this[$] || this.listeners("error").length ? super.emit("error", G) : !1;
          return this[d](), O;
        } else if (E === "resume") {
          const O = super.emit("resume");
          return this[d](), O;
        } else if (E === "finish" || E === "prefinish") {
          const O = super.emit(E);
          return this.removeAllListeners(E), O;
        }
        const K = super.emit(E, ...j);
        return this[d](), K;
      }
      [re](E) {
        for (const G of this[L])
          G.dest.write(E) === !1 && this.pause();
        const j = this[B] ? !1 : super.emit("data", E);
        return this[d](), j;
      }
      [se]() {
        return this[y] ? !1 : (this[y] = !0, this.readable = !1, this[M] ? (le(() => this[ne]()), !0) : this[ne]());
      }
      [ne]() {
        if (this[b]) {
          const j = this[b].end();
          if (j) {
            for (const G of this[L])
              G.dest.write(j);
            this[B] || super.emit("data", j);
          }
        }
        for (const j of this[L])
          j.end();
        const E = super.emit("end");
        return this.removeAllListeners("end"), E;
      }
      /**
       * Return a Promise that resolves to an array of all emitted data once
       * the stream ends.
       */
      async collect() {
        const E = Object.assign([], {
          dataLength: 0
        });
        this[F] || (E.dataLength = 0);
        const j = this.promise();
        return this.on("data", (G) => {
          E.push(G), this[F] || (E.dataLength += G.length);
        }), await j, E;
      }
      /**
       * Return a Promise that resolves to the concatenation of all emitted data
       * once the stream ends.
       *
       * Not allowed on objectMode streams.
       */
      async concat() {
        if (this[F])
          throw new Error("cannot concat in objectMode");
        const E = await this.collect();
        return this[p] ? E.join("") : Buffer.concat(E, E.dataLength);
      }
      /**
       * Return a void Promise that resolves once the stream ends.
       */
      async promise() {
        return new Promise((E, j) => {
          this.on(T, () => j(new Error("stream destroyed"))), this.on("error", (G) => j(G)), this.on("end", () => E());
        });
      }
      /**
       * Asynchronous `for await of` iteration.
       *
       * This will continue emitting all chunks until the stream terminates.
       */
      [Symbol.asyncIterator]() {
        this[B] = !1;
        let E = !1;
        const j = async () => (this.pause(), E = !0, { value: void 0, done: !0 });
        return {
          next: () => {
            if (E)
              return j();
            const K = this.read();
            if (K !== null)
              return Promise.resolve({ done: !1, value: K });
            if (this[f])
              return j();
            let O, H;
            const te = (he) => {
              this.off("data", z), this.off("end", de), this.off(T, pe), j(), H(he);
            }, z = (he) => {
              this.off("error", te), this.off("end", de), this.off(T, pe), this.pause(), O({ value: he, done: !!this[f] });
            }, de = () => {
              this.off("error", te), this.off("data", z), this.off(T, pe), j(), O({ done: !0, value: void 0 });
            }, pe = () => te(new Error("stream destroyed"));
            return new Promise((he, we) => {
              H = we, O = he, this.once(T, pe), this.once("error", te), this.once("end", de), this.once("data", z);
            });
          },
          throw: j,
          return: j,
          [Symbol.asyncIterator]() {
            return this;
          },
          [Symbol.asyncDispose]: async () => {
          }
        };
      }
      /**
       * Synchronous `for of` iteration.
       *
       * The iteration will terminate when the internal buffer runs out, even
       * if the stream has not yet terminated.
       */
      [Symbol.iterator]() {
        this[B] = !1;
        let E = !1;
        const j = () => (this.pause(), this.off(J, j), this.off(T, j), this.off("end", j), E = !0, { done: !0, value: void 0 }), G = () => {
          if (E)
            return j();
          const K = this.read();
          return K === null ? j() : { done: !1, value: K };
        };
        return this.once("end", j), this.once(J, j), this.once(T, j), {
          next: G,
          throw: j,
          return: j,
          [Symbol.iterator]() {
            return this;
          },
          [Symbol.dispose]: () => {
          }
        };
      }
      /**
       * Destroy a stream, preventing it from being used for any further purpose.
       *
       * If the stream has a `close()` method, then it will be called on
       * destruction.
       *
       * After destruction, any attempt to write data, read data, or emit most
       * events will be ignored.
       *
       * If an error argument is provided, then it will be emitted in an
       * 'error' event.
       */
      destroy(E) {
        if (this[T])
          return E ? this.emit("error", E) : this.emit(T), this;
        this[T] = !0, this[B] = !0, this[k].length = 0, this[U] = 0;
        const j = this;
        return typeof j.close == "function" && !this[I] && j.close(), E ? this.emit("error", E) : this.emit(T), this;
      }
      /**
       * Alias for {@link isStream}
       *
       * Former export location, maintained for backwards compatibility.
       *
       * @deprecated
       */
      static get isStream() {
        return t.isStream;
      }
    }
    t.Minipass = m;
  })(tn)), tn;
}
var Ey;
function AA() {
  if (Ey) return ke;
  Ey = 1;
  var t = ke && ke.__createBinding || (Object.create ? (function(m, c, E, j) {
    j === void 0 && (j = E);
    var G = Object.getOwnPropertyDescriptor(c, E);
    (!G || ("get" in G ? !c.__esModule : G.writable || G.configurable)) && (G = { enumerable: !0, get: function() {
      return c[E];
    } }), Object.defineProperty(m, j, G);
  }) : (function(m, c, E, j) {
    j === void 0 && (j = E), m[j] = c[E];
  })), e = ke && ke.__setModuleDefault || (Object.create ? (function(m, c) {
    Object.defineProperty(m, "default", { enumerable: !0, value: c });
  }) : function(m, c) {
    m.default = c;
  }), r = ke && ke.__importStar || function(m) {
    if (m && m.__esModule) return m;
    var c = {};
    if (m != null) for (var E in m) E !== "default" && Object.prototype.hasOwnProperty.call(m, E) && t(c, m, E);
    return e(c, m), c;
  };
  Object.defineProperty(ke, "__esModule", { value: !0 }), ke.PathScurry = ke.Path = ke.PathScurryDarwin = ke.PathScurryPosix = ke.PathScurryWin32 = ke.PathScurryBase = ke.PathPosix = ke.PathWin32 = ke.PathBase = ke.ChildrenCache = ke.ResolveCache = void 0;
  const o = /* @__PURE__ */ RA(), a = Ee, n = nw, i = Lt, l = r(yE), s = i.realpathSync.native, f = _E, d = Ym(), y = {
    lstatSync: i.lstatSync,
    readdir: i.readdir,
    readdirSync: i.readdirSync,
    readlinkSync: i.readlinkSync,
    realpathSync: s,
    promises: {
      lstat: f.lstat,
      readdir: f.readdir,
      readlink: f.readlink,
      realpath: f.realpath
    }
  }, R = (m) => !m || m === y || m === l ? y : {
    ...y,
    ...m,
    promises: {
      ...y.promises,
      ...m.promises || {}
    }
  }, w = /^\\\\\?\\([a-z]:)\\?$/i, I = (m) => m.replace(/\//g, "\\").replace(w, "$1\\"), A = /[\\\/]/, u = 0, g = 1, p = 2, b = 4, C = 6, N = 8, W = 10, k = 12, L = 15, U = ~L, x = 16, D = 32, F = 64, T = 128, J = 256, re = 512, se = F | T | re, ne = 1023, M = (m) => m.isFile() ? N : m.isDirectory() ? b : m.isSymbolicLink() ? W : m.isCharacterDevice() ? p : m.isBlockDevice() ? C : m.isSocket() ? k : m.isFIFO() ? g : u, Q = /* @__PURE__ */ new Map(), v = (m) => {
    const c = Q.get(m);
    if (c)
      return c;
    const E = m.normalize("NFKD");
    return Q.set(m, E), E;
  }, $ = /* @__PURE__ */ new Map(), ee = (m) => {
    const c = $.get(m);
    if (c)
      return c;
    const E = v(m.toLowerCase());
    return $.set(m, E), E;
  };
  class B extends o.LRUCache {
    constructor() {
      super({ max: 256 });
    }
  }
  ke.ResolveCache = B;
  class le extends o.LRUCache {
    constructor(c = 16 * 1024) {
      super({
        maxSize: c,
        // parent + children
        sizeCalculation: (E) => E.length + 1
      });
    }
  }
  ke.ChildrenCache = le;
  const ae = /* @__PURE__ */ Symbol("PathScurry setAsCwd");
  class ce {
    /**
     * the basename of this path
     *
     * **Important**: *always* test the path name against any test string
     * usingthe {@link isNamed} method, and not by directly comparing this
     * string. Otherwise, unicode path strings that the system sees as identical
     * will not be properly treated as the same path, leading to incorrect
     * behavior and possible security issues.
     */
    name;
    /**
     * the Path entry corresponding to the path root.
     *
     * @internal
     */
    root;
    /**
     * All roots found within the current PathScurry family
     *
     * @internal
     */
    roots;
    /**
     * a reference to the parent path, or undefined in the case of root entries
     *
     * @internal
     */
    parent;
    /**
     * boolean indicating whether paths are compared case-insensitively
     * @internal
     */
    nocase;
    /**
     * boolean indicating that this path is the current working directory
     * of the PathScurry collection that contains it.
     */
    isCWD = !1;
    // potential default fs override
    #e;
    // Stats fields
    #r;
    get dev() {
      return this.#r;
    }
    #i;
    get mode() {
      return this.#i;
    }
    #t;
    get nlink() {
      return this.#t;
    }
    #o;
    get uid() {
      return this.#o;
    }
    #S;
    get gid() {
      return this.#S;
    }
    #u;
    get rdev() {
      return this.#u;
    }
    #c;
    get blksize() {
      return this.#c;
    }
    #l;
    get ino() {
      return this.#l;
    }
    #a;
    get size() {
      return this.#a;
    }
    #s;
    get blocks() {
      return this.#s;
    }
    #b;
    get atimeMs() {
      return this.#b;
    }
    #_;
    get mtimeMs() {
      return this.#_;
    }
    #g;
    get ctimeMs() {
      return this.#g;
    }
    #h;
    get birthtimeMs() {
      return this.#h;
    }
    #E;
    get atime() {
      return this.#E;
    }
    #p;
    get mtime() {
      return this.#p;
    }
    #R;
    get ctime() {
      return this.#R;
    }
    #A;
    get birthtime() {
      return this.#A;
    }
    #y;
    #v;
    #w;
    #d;
    #T;
    #x;
    #n;
    #L;
    #m;
    #O;
    /**
     * This property is for compatibility with the Dirent class as of
     * Node v20, where Dirent['parentPath'] refers to the path of the
     * directory that was passed to readdir. For root entries, it's the path
     * to the entry itself.
     */
    get parentPath() {
      return (this.parent || this).fullpath();
    }
    /**
     * Deprecated alias for Dirent['parentPath'] Somewhat counterintuitively,
     * this property refers to the *parent* path, not the path object itself.
     */
    get path() {
      return this.parentPath;
    }
    /**
     * Do not create new Path objects directly.  They should always be accessed
     * via the PathScurry class or other methods on the Path class.
     *
     * @internal
     */
    constructor(c, E = u, j, G, K, O, H) {
      this.name = c, this.#y = K ? ee(c) : v(c), this.#n = E & ne, this.nocase = K, this.roots = G, this.root = j || this, this.#L = O, this.#w = H.fullpath, this.#T = H.relative, this.#x = H.relativePosix, this.parent = H.parent, this.parent ? this.#e = this.parent.#e : this.#e = R(H.fs);
    }
    /**
     * Returns the depth of the Path object from its root.
     *
     * For example, a path at `/foo/bar` would have a depth of 2.
     */
    depth() {
      return this.#v !== void 0 ? this.#v : this.parent ? this.#v = this.parent.depth() + 1 : this.#v = 0;
    }
    /**
     * @internal
     */
    childrenCache() {
      return this.#L;
    }
    /**
     * Get the Path object referenced by the string path, resolved from this Path
     */
    resolve(c) {
      if (!c)
        return this;
      const E = this.getRootString(c), G = c.substring(E.length).split(this.splitSep);
      return E ? this.getRoot(E).#P(G) : this.#P(G);
    }
    #P(c) {
      let E = this;
      for (const j of c)
        E = E.child(j);
      return E;
    }
    /**
     * Returns the cached children Path objects, if still available.  If they
     * have fallen out of the cache, then returns an empty array, and resets the
     * READDIR_CALLED bit, so that future calls to readdir() will require an fs
     * lookup.
     *
     * @internal
     */
    children() {
      const c = this.#L.get(this);
      if (c)
        return c;
      const E = Object.assign([], { provisional: 0 });
      return this.#L.set(this, E), this.#n &= ~x, E;
    }
    /**
     * Resolves a path portion and returns or creates the child Path.
     *
     * Returns `this` if pathPart is `''` or `'.'`, or `parent` if pathPart is
     * `'..'`.
     *
     * This should not be called directly.  If `pathPart` contains any path
     * separators, it will lead to unsafe undefined behavior.
     *
     * Use `Path.resolve()` instead.
     *
     * @internal
     */
    child(c, E) {
      if (c === "" || c === ".")
        return this;
      if (c === "..")
        return this.parent || this;
      const j = this.children(), G = this.nocase ? ee(c) : v(c);
      for (const te of j)
        if (te.#y === G)
          return te;
      const K = this.parent ? this.sep : "", O = this.#w ? this.#w + K + c : void 0, H = this.newChild(c, u, {
        ...E,
        parent: this,
        fullpath: O
      });
      return this.canReaddir() || (H.#n |= T), j.push(H), H;
    }
    /**
     * The relative path from the cwd. If it does not share an ancestor with
     * the cwd, then this ends up being equivalent to the fullpath()
     */
    relative() {
      if (this.isCWD)
        return "";
      if (this.#T !== void 0)
        return this.#T;
      const c = this.name, E = this.parent;
      if (!E)
        return this.#T = this.name;
      const j = E.relative();
      return j + (!j || !E.parent ? "" : this.sep) + c;
    }
    /**
     * The relative path from the cwd, using / as the path separator.
     * If it does not share an ancestor with
     * the cwd, then this ends up being equivalent to the fullpathPosix()
     * On posix systems, this is identical to relative().
     */
    relativePosix() {
      if (this.sep === "/")
        return this.relative();
      if (this.isCWD)
        return "";
      if (this.#x !== void 0)
        return this.#x;
      const c = this.name, E = this.parent;
      if (!E)
        return this.#x = this.fullpathPosix();
      const j = E.relativePosix();
      return j + (!j || !E.parent ? "" : "/") + c;
    }
    /**
     * The fully resolved path string for this Path entry
     */
    fullpath() {
      if (this.#w !== void 0)
        return this.#w;
      const c = this.name, E = this.parent;
      if (!E)
        return this.#w = this.name;
      const G = E.fullpath() + (E.parent ? this.sep : "") + c;
      return this.#w = G;
    }
    /**
     * On platforms other than windows, this is identical to fullpath.
     *
     * On windows, this is overridden to return the forward-slash form of the
     * full UNC path.
     */
    fullpathPosix() {
      if (this.#d !== void 0)
        return this.#d;
      if (this.sep === "/")
        return this.#d = this.fullpath();
      if (!this.parent) {
        const G = this.fullpath().replace(/\\/g, "/");
        return /^[a-z]:\//i.test(G) ? this.#d = `//?/${G}` : this.#d = G;
      }
      const c = this.parent, E = c.fullpathPosix(), j = E + (!E || !c.parent ? "" : "/") + this.name;
      return this.#d = j;
    }
    /**
     * Is the Path of an unknown type?
     *
     * Note that we might know *something* about it if there has been a previous
     * filesystem operation, for example that it does not exist, or is not a
     * link, or whether it has child entries.
     */
    isUnknown() {
      return (this.#n & L) === u;
    }
    isType(c) {
      return this[`is${c}`]();
    }
    getType() {
      return this.isUnknown() ? "Unknown" : this.isDirectory() ? "Directory" : this.isFile() ? "File" : this.isSymbolicLink() ? "SymbolicLink" : this.isFIFO() ? "FIFO" : this.isCharacterDevice() ? "CharacterDevice" : this.isBlockDevice() ? "BlockDevice" : (
        /* c8 ignore start */
        this.isSocket() ? "Socket" : "Unknown"
      );
    }
    /**
     * Is the Path a regular file?
     */
    isFile() {
      return (this.#n & L) === N;
    }
    /**
     * Is the Path a directory?
     */
    isDirectory() {
      return (this.#n & L) === b;
    }
    /**
     * Is the path a character device?
     */
    isCharacterDevice() {
      return (this.#n & L) === p;
    }
    /**
     * Is the path a block device?
     */
    isBlockDevice() {
      return (this.#n & L) === C;
    }
    /**
     * Is the path a FIFO pipe?
     */
    isFIFO() {
      return (this.#n & L) === g;
    }
    /**
     * Is the path a socket?
     */
    isSocket() {
      return (this.#n & L) === k;
    }
    /**
     * Is the path a symbolic link?
     */
    isSymbolicLink() {
      return (this.#n & W) === W;
    }
    /**
     * Return the entry if it has been subject of a successful lstat, or
     * undefined otherwise.
     *
     * Does not read the filesystem, so an undefined result *could* simply
     * mean that we haven't called lstat on it.
     */
    lstatCached() {
      return this.#n & D ? this : void 0;
    }
    /**
     * Return the cached link target if the entry has been the subject of a
     * successful readlink, or undefined otherwise.
     *
     * Does not read the filesystem, so an undefined result *could* just mean we
     * don't have any cached data. Only use it if you are very sure that a
     * readlink() has been called at some point.
     */
    readlinkCached() {
      return this.#m;
    }
    /**
     * Returns the cached realpath target if the entry has been the subject
     * of a successful realpath, or undefined otherwise.
     *
     * Does not read the filesystem, so an undefined result *could* just mean we
     * don't have any cached data. Only use it if you are very sure that a
     * realpath() has been called at some point.
     */
    realpathCached() {
      return this.#O;
    }
    /**
     * Returns the cached child Path entries array if the entry has been the
     * subject of a successful readdir(), or [] otherwise.
     *
     * Does not read the filesystem, so an empty array *could* just mean we
     * don't have any cached data. Only use it if you are very sure that a
     * readdir() has been called recently enough to still be valid.
     */
    readdirCached() {
      const c = this.children();
      return c.slice(0, c.provisional);
    }
    /**
     * Return true if it's worth trying to readlink.  Ie, we don't (yet) have
     * any indication that readlink will definitely fail.
     *
     * Returns false if the path is known to not be a symlink, if a previous
     * readlink failed, or if the entry does not exist.
     */
    canReadlink() {
      if (this.#m)
        return !0;
      if (!this.parent)
        return !1;
      const c = this.#n & L;
      return !(c !== u && c !== W || this.#n & J || this.#n & T);
    }
    /**
     * Return true if readdir has previously been successfully called on this
     * path, indicating that cachedReaddir() is likely valid.
     */
    calledReaddir() {
      return !!(this.#n & x);
    }
    /**
     * Returns true if the path is known to not exist. That is, a previous lstat
     * or readdir failed to verify its existence when that would have been
     * expected, or a parent entry was marked either enoent or enotdir.
     */
    isENOENT() {
      return !!(this.#n & T);
    }
    /**
     * Return true if the path is a match for the given path name.  This handles
     * case sensitivity and unicode normalization.
     *
     * Note: even on case-sensitive systems, it is **not** safe to test the
     * equality of the `.name` property to determine whether a given pathname
     * matches, due to unicode normalization mismatches.
     *
     * Always use this method instead of testing the `path.name` property
     * directly.
     */
    isNamed(c) {
      return this.nocase ? this.#y === ee(c) : this.#y === v(c);
    }
    /**
     * Return the Path object corresponding to the target of a symbolic link.
     *
     * If the Path is not a symbolic link, or if the readlink call fails for any
     * reason, `undefined` is returned.
     *
     * Result is cached, and thus may be outdated if the filesystem is mutated.
     */
    async readlink() {
      const c = this.#m;
      if (c)
        return c;
      if (this.canReadlink() && this.parent)
        try {
          const E = await this.#e.promises.readlink(this.fullpath()), j = (await this.parent.realpath())?.resolve(E);
          if (j)
            return this.#m = j;
        } catch (E) {
          this.#f(E.code);
          return;
        }
    }
    /**
     * Synchronous {@link PathBase.readlink}
     */
    readlinkSync() {
      const c = this.#m;
      if (c)
        return c;
      if (this.canReadlink() && this.parent)
        try {
          const E = this.#e.readlinkSync(this.fullpath()), j = this.parent.realpathSync()?.resolve(E);
          if (j)
            return this.#m = j;
        } catch (E) {
          this.#f(E.code);
          return;
        }
    }
    #C(c) {
      this.#n |= x;
      for (let E = c.provisional; E < c.length; E++) {
        const j = c[E];
        j && j.#M();
      }
    }
    #M() {
      this.#n & T || (this.#n = (this.#n | T) & U, this.#I());
    }
    #I() {
      const c = this.children();
      c.provisional = 0;
      for (const E of c)
        E.#M();
    }
    #D() {
      this.#n |= re, this.#k();
    }
    // save the information when we know the entry is not a dir
    #k() {
      if (this.#n & F)
        return;
      let c = this.#n;
      (c & L) === b && (c &= U), this.#n = c | F, this.#I();
    }
    #F(c = "") {
      c === "ENOTDIR" || c === "EPERM" ? this.#k() : c === "ENOENT" ? this.#M() : this.children().provisional = 0;
    }
    #j(c = "") {
      c === "ENOTDIR" ? this.parent.#k() : c === "ENOENT" && this.#M();
    }
    #f(c = "") {
      let E = this.#n;
      E |= J, c === "ENOENT" && (E |= T), (c === "EINVAL" || c === "UNKNOWN") && (E &= U), this.#n = E, c === "ENOTDIR" && this.parent && this.parent.#k();
    }
    #$(c, E) {
      return this.#N(c, E) || this.#W(c, E);
    }
    #W(c, E) {
      const j = M(c), G = this.newChild(c.name, j, { parent: this }), K = G.#n & L;
      return K !== b && K !== W && K !== u && (G.#n |= F), E.unshift(G), E.provisional++, G;
    }
    #N(c, E) {
      for (let j = E.provisional; j < E.length; j++) {
        const G = E[j];
        if ((this.nocase ? ee(c.name) : v(c.name)) === G.#y)
          return this.#q(c, G, j, E);
      }
    }
    #q(c, E, j, G) {
      const K = E.name;
      return E.#n = E.#n & U | M(c), K !== c.name && (E.name = c.name), j !== G.provisional && (j === G.length - 1 ? G.pop() : G.splice(j, 1), G.unshift(E)), G.provisional++, E;
    }
    /**
     * Call lstat() on this Path, and update all known information that can be
     * determined.
     *
     * Note that unlike `fs.lstat()`, the returned value does not contain some
     * information, such as `mode`, `dev`, `nlink`, and `ino`.  If that
     * information is required, you will need to call `fs.lstat` yourself.
     *
     * If the Path refers to a nonexistent file, or if the lstat call fails for
     * any reason, `undefined` is returned.  Otherwise the updated Path object is
     * returned.
     *
     * Results are cached, and thus may be out of date if the filesystem is
     * mutated.
     */
    async lstat() {
      if ((this.#n & T) === 0)
        try {
          return this.#H(await this.#e.promises.lstat(this.fullpath())), this;
        } catch (c) {
          this.#j(c.code);
        }
    }
    /**
     * synchronous {@link PathBase.lstat}
     */
    lstatSync() {
      if ((this.#n & T) === 0)
        try {
          return this.#H(this.#e.lstatSync(this.fullpath())), this;
        } catch (c) {
          this.#j(c.code);
        }
    }
    #H(c) {
      const { atime: E, atimeMs: j, birthtime: G, birthtimeMs: K, blksize: O, blocks: H, ctime: te, ctimeMs: z, dev: de, gid: pe, ino: he, mode: we, mtime: be, mtimeMs: _e, nlink: Ae, rdev: xe, size: me, uid: Ge } = c;
      this.#E = E, this.#b = j, this.#A = G, this.#h = K, this.#c = O, this.#s = H, this.#R = te, this.#g = z, this.#r = de, this.#S = pe, this.#l = he, this.#i = we, this.#p = be, this.#_ = _e, this.#t = Ae, this.#u = xe, this.#a = me, this.#o = Ge;
      const Je = M(c);
      this.#n = this.#n & U | Je | D, Je !== u && Je !== b && Je !== W && (this.#n |= F);
    }
    #U = [];
    #G = !1;
    #z(c) {
      this.#G = !1;
      const E = this.#U.slice();
      this.#U.length = 0, E.forEach((j) => j(null, c));
    }
    /**
     * Standard node-style callback interface to get list of directory entries.
     *
     * If the Path cannot or does not contain any children, then an empty array
     * is returned.
     *
     * Results are cached, and thus may be out of date if the filesystem is
     * mutated.
     *
     * @param cb The callback called with (er, entries).  Note that the `er`
     * param is somewhat extraneous, as all readdir() errors are handled and
     * simply result in an empty set of entries being returned.
     * @param allowZalgo Boolean indicating that immediately known results should
     * *not* be deferred with `queueMicrotask`. Defaults to `false`. Release
     * zalgo at your peril, the dark pony lord is devious and unforgiving.
     */
    readdirCB(c, E = !1) {
      if (!this.canReaddir()) {
        E ? c(null, []) : queueMicrotask(() => c(null, []));
        return;
      }
      const j = this.children();
      if (this.calledReaddir()) {
        const K = j.slice(0, j.provisional);
        E ? c(null, K) : queueMicrotask(() => c(null, K));
        return;
      }
      if (this.#U.push(c), this.#G)
        return;
      this.#G = !0;
      const G = this.fullpath();
      this.#e.readdir(G, { withFileTypes: !0 }, (K, O) => {
        if (K)
          this.#F(K.code), j.provisional = 0;
        else {
          for (const H of O)
            this.#$(H, j);
          this.#C(j);
        }
        this.#z(j.slice(0, j.provisional));
      });
    }
    #B;
    /**
     * Return an array of known child entries.
     *
     * If the Path cannot or does not contain any children, then an empty array
     * is returned.
     *
     * Results are cached, and thus may be out of date if the filesystem is
     * mutated.
     */
    async readdir() {
      if (!this.canReaddir())
        return [];
      const c = this.children();
      if (this.calledReaddir())
        return c.slice(0, c.provisional);
      const E = this.fullpath();
      if (this.#B)
        await this.#B;
      else {
        let j = () => {
        };
        this.#B = new Promise((G) => j = G);
        try {
          for (const G of await this.#e.promises.readdir(E, {
            withFileTypes: !0
          }))
            this.#$(G, c);
          this.#C(c);
        } catch (G) {
          this.#F(G.code), c.provisional = 0;
        }
        this.#B = void 0, j();
      }
      return c.slice(0, c.provisional);
    }
    /**
     * synchronous {@link PathBase.readdir}
     */
    readdirSync() {
      if (!this.canReaddir())
        return [];
      const c = this.children();
      if (this.calledReaddir())
        return c.slice(0, c.provisional);
      const E = this.fullpath();
      try {
        for (const j of this.#e.readdirSync(E, {
          withFileTypes: !0
        }))
          this.#$(j, c);
        this.#C(c);
      } catch (j) {
        this.#F(j.code), c.provisional = 0;
      }
      return c.slice(0, c.provisional);
    }
    canReaddir() {
      if (this.#n & se)
        return !1;
      const c = L & this.#n;
      return c === u || c === b || c === W;
    }
    shouldWalk(c, E) {
      return (this.#n & b) === b && !(this.#n & se) && !c.has(this) && (!E || E(this));
    }
    /**
     * Return the Path object corresponding to path as resolved
     * by realpath(3).
     *
     * If the realpath call fails for any reason, `undefined` is returned.
     *
     * Result is cached, and thus may be outdated if the filesystem is mutated.
     * On success, returns a Path object.
     */
    async realpath() {
      if (this.#O)
        return this.#O;
      if (!((re | J | T) & this.#n))
        try {
          const c = await this.#e.promises.realpath(this.fullpath());
          return this.#O = this.resolve(c);
        } catch {
          this.#D();
        }
    }
    /**
     * Synchronous {@link realpath}
     */
    realpathSync() {
      if (this.#O)
        return this.#O;
      if (!((re | J | T) & this.#n))
        try {
          const c = this.#e.realpathSync(this.fullpath());
          return this.#O = this.resolve(c);
        } catch {
          this.#D();
        }
    }
    /**
     * Internal method to mark this Path object as the scurry cwd,
     * called by {@link PathScurry#chdir}
     *
     * @internal
     */
    [ae](c) {
      if (c === this)
        return;
      c.isCWD = !1, this.isCWD = !0;
      const E = /* @__PURE__ */ new Set([]);
      let j = [], G = this;
      for (; G && G.parent; )
        E.add(G), G.#T = j.join(this.sep), G.#x = j.join("/"), G = G.parent, j.push("..");
      for (G = c; G && G.parent && !E.has(G); )
        G.#T = void 0, G.#x = void 0, G = G.parent;
    }
  }
  ke.PathBase = ce;
  class Y extends ce {
    /**
     * Separator for generating path strings.
     */
    sep = "\\";
    /**
     * Separator for parsing path strings.
     */
    splitSep = A;
    /**
     * Do not create new Path objects directly.  They should always be accessed
     * via the PathScurry class or other methods on the Path class.
     *
     * @internal
     */
    constructor(c, E = u, j, G, K, O, H) {
      super(c, E, j, G, K, O, H);
    }
    /**
     * @internal
     */
    newChild(c, E = u, j = {}) {
      return new Y(c, E, this.root, this.roots, this.nocase, this.childrenCache(), j);
    }
    /**
     * @internal
     */
    getRootString(c) {
      return a.win32.parse(c).root;
    }
    /**
     * @internal
     */
    getRoot(c) {
      if (c = I(c.toUpperCase()), c === this.root.name)
        return this.root;
      for (const [E, j] of Object.entries(this.roots))
        if (this.sameRoot(c, E))
          return this.roots[c] = j;
      return this.roots[c] = new V(c, this).root;
    }
    /**
     * @internal
     */
    sameRoot(c, E = this.root.name) {
      return c = c.toUpperCase().replace(/\//g, "\\").replace(w, "$1\\"), c === E;
    }
  }
  ke.PathWin32 = Y;
  class P extends ce {
    /**
     * separator for parsing path strings
     */
    splitSep = "/";
    /**
     * separator for generating path strings
     */
    sep = "/";
    /**
     * Do not create new Path objects directly.  They should always be accessed
     * via the PathScurry class or other methods on the Path class.
     *
     * @internal
     */
    constructor(c, E = u, j, G, K, O, H) {
      super(c, E, j, G, K, O, H);
    }
    /**
     * @internal
     */
    getRootString(c) {
      return c.startsWith("/") ? "/" : "";
    }
    /**
     * @internal
     */
    getRoot(c) {
      return this.root;
    }
    /**
     * @internal
     */
    newChild(c, E = u, j = {}) {
      return new P(c, E, this.root, this.roots, this.nocase, this.childrenCache(), j);
    }
  }
  ke.PathPosix = P;
  class q {
    /**
     * The root Path entry for the current working directory of this Scurry
     */
    root;
    /**
     * The string path for the root of this Scurry's current working directory
     */
    rootPath;
    /**
     * A collection of all roots encountered, referenced by rootPath
     */
    roots;
    /**
     * The Path entry corresponding to this PathScurry's current working directory.
     */
    cwd;
    #e;
    #r;
    #i;
    /**
     * Perform path comparisons case-insensitively.
     *
     * Defaults true on Darwin and Windows systems, false elsewhere.
     */
    nocase;
    #t;
    /**
     * This class should not be instantiated directly.
     *
     * Use PathScurryWin32, PathScurryDarwin, PathScurryPosix, or PathScurry
     *
     * @internal
     */
    constructor(c = process.cwd(), E, j, { nocase: G, childrenCacheSize: K = 16 * 1024, fs: O = y } = {}) {
      this.#t = R(O), (c instanceof URL || c.startsWith("file://")) && (c = (0, n.fileURLToPath)(c));
      const H = E.resolve(c);
      this.roots = /* @__PURE__ */ Object.create(null), this.rootPath = this.parseRootPath(H), this.#e = new B(), this.#r = new B(), this.#i = new le(K);
      const te = H.substring(this.rootPath.length).split(j);
      if (te.length === 1 && !te[0] && te.pop(), G === void 0)
        throw new TypeError("must provide nocase setting to PathScurryBase ctor");
      this.nocase = G, this.root = this.newRoot(this.#t), this.roots[this.rootPath] = this.root;
      let z = this.root, de = te.length - 1;
      const pe = E.sep;
      let he = this.rootPath, we = !1;
      for (const be of te) {
        const _e = de--;
        z = z.child(be, {
          relative: new Array(_e).fill("..").join(pe),
          relativePosix: new Array(_e).fill("..").join("/"),
          fullpath: he += (we ? "" : pe) + be
        }), we = !0;
      }
      this.cwd = z;
    }
    /**
     * Get the depth of a provided path, string, or the cwd
     */
    depth(c = this.cwd) {
      return typeof c == "string" && (c = this.cwd.resolve(c)), c.depth();
    }
    /**
     * Return the cache of child entries.  Exposed so subclasses can create
     * child Path objects in a platform-specific way.
     *
     * @internal
     */
    childrenCache() {
      return this.#i;
    }
    /**
     * Resolve one or more path strings to a resolved string
     *
     * Same interface as require('path').resolve.
     *
     * Much faster than path.resolve() when called multiple times for the same
     * path, because the resolved Path objects are cached.  Much slower
     * otherwise.
     */
    resolve(...c) {
      let E = "";
      for (let K = c.length - 1; K >= 0; K--) {
        const O = c[K];
        if (!(!O || O === ".") && (E = E ? `${O}/${E}` : O, this.isAbsolute(O)))
          break;
      }
      const j = this.#e.get(E);
      if (j !== void 0)
        return j;
      const G = this.cwd.resolve(E).fullpath();
      return this.#e.set(E, G), G;
    }
    /**
     * Resolve one or more path strings to a resolved string, returning
     * the posix path.  Identical to .resolve() on posix systems, but on
     * windows will return a forward-slash separated UNC path.
     *
     * Same interface as require('path').resolve.
     *
     * Much faster than path.resolve() when called multiple times for the same
     * path, because the resolved Path objects are cached.  Much slower
     * otherwise.
     */
    resolvePosix(...c) {
      let E = "";
      for (let K = c.length - 1; K >= 0; K--) {
        const O = c[K];
        if (!(!O || O === ".") && (E = E ? `${O}/${E}` : O, this.isAbsolute(O)))
          break;
      }
      const j = this.#r.get(E);
      if (j !== void 0)
        return j;
      const G = this.cwd.resolve(E).fullpathPosix();
      return this.#r.set(E, G), G;
    }
    /**
     * find the relative path from the cwd to the supplied path string or entry
     */
    relative(c = this.cwd) {
      return typeof c == "string" && (c = this.cwd.resolve(c)), c.relative();
    }
    /**
     * find the relative path from the cwd to the supplied path string or
     * entry, using / as the path delimiter, even on Windows.
     */
    relativePosix(c = this.cwd) {
      return typeof c == "string" && (c = this.cwd.resolve(c)), c.relativePosix();
    }
    /**
     * Return the basename for the provided string or Path object
     */
    basename(c = this.cwd) {
      return typeof c == "string" && (c = this.cwd.resolve(c)), c.name;
    }
    /**
     * Return the dirname for the provided string or Path object
     */
    dirname(c = this.cwd) {
      return typeof c == "string" && (c = this.cwd.resolve(c)), (c.parent || c).fullpath();
    }
    async readdir(c = this.cwd, E = {
      withFileTypes: !0
    }) {
      typeof c == "string" ? c = this.cwd.resolve(c) : c instanceof ce || (E = c, c = this.cwd);
      const { withFileTypes: j } = E;
      if (c.canReaddir()) {
        const G = await c.readdir();
        return j ? G : G.map((K) => K.name);
      } else
        return [];
    }
    readdirSync(c = this.cwd, E = {
      withFileTypes: !0
    }) {
      typeof c == "string" ? c = this.cwd.resolve(c) : c instanceof ce || (E = c, c = this.cwd);
      const { withFileTypes: j = !0 } = E;
      return c.canReaddir() ? j ? c.readdirSync() : c.readdirSync().map((G) => G.name) : [];
    }
    /**
     * Call lstat() on the string or Path object, and update all known
     * information that can be determined.
     *
     * Note that unlike `fs.lstat()`, the returned value does not contain some
     * information, such as `mode`, `dev`, `nlink`, and `ino`.  If that
     * information is required, you will need to call `fs.lstat` yourself.
     *
     * If the Path refers to a nonexistent file, or if the lstat call fails for
     * any reason, `undefined` is returned.  Otherwise the updated Path object is
     * returned.
     *
     * Results are cached, and thus may be out of date if the filesystem is
     * mutated.
     */
    async lstat(c = this.cwd) {
      return typeof c == "string" && (c = this.cwd.resolve(c)), c.lstat();
    }
    /**
     * synchronous {@link PathScurryBase.lstat}
     */
    lstatSync(c = this.cwd) {
      return typeof c == "string" && (c = this.cwd.resolve(c)), c.lstatSync();
    }
    async readlink(c = this.cwd, { withFileTypes: E } = {
      withFileTypes: !1
    }) {
      typeof c == "string" ? c = this.cwd.resolve(c) : c instanceof ce || (E = c.withFileTypes, c = this.cwd);
      const j = await c.readlink();
      return E ? j : j?.fullpath();
    }
    readlinkSync(c = this.cwd, { withFileTypes: E } = {
      withFileTypes: !1
    }) {
      typeof c == "string" ? c = this.cwd.resolve(c) : c instanceof ce || (E = c.withFileTypes, c = this.cwd);
      const j = c.readlinkSync();
      return E ? j : j?.fullpath();
    }
    async realpath(c = this.cwd, { withFileTypes: E } = {
      withFileTypes: !1
    }) {
      typeof c == "string" ? c = this.cwd.resolve(c) : c instanceof ce || (E = c.withFileTypes, c = this.cwd);
      const j = await c.realpath();
      return E ? j : j?.fullpath();
    }
    realpathSync(c = this.cwd, { withFileTypes: E } = {
      withFileTypes: !1
    }) {
      typeof c == "string" ? c = this.cwd.resolve(c) : c instanceof ce || (E = c.withFileTypes, c = this.cwd);
      const j = c.realpathSync();
      return E ? j : j?.fullpath();
    }
    async walk(c = this.cwd, E = {}) {
      typeof c == "string" ? c = this.cwd.resolve(c) : c instanceof ce || (E = c, c = this.cwd);
      const { withFileTypes: j = !0, follow: G = !1, filter: K, walkFilter: O } = E, H = [];
      (!K || K(c)) && H.push(j ? c : c.fullpath());
      const te = /* @__PURE__ */ new Set(), z = (pe, he) => {
        te.add(pe), pe.readdirCB((we, be) => {
          if (we)
            return he(we);
          let _e = be.length;
          if (!_e)
            return he();
          const Ae = () => {
            --_e === 0 && he();
          };
          for (const xe of be)
            (!K || K(xe)) && H.push(j ? xe : xe.fullpath()), G && xe.isSymbolicLink() ? xe.realpath().then((me) => me?.isUnknown() ? me.lstat() : me).then((me) => me?.shouldWalk(te, O) ? z(me, Ae) : Ae()) : xe.shouldWalk(te, O) ? z(xe, Ae) : Ae();
        }, !0);
      }, de = c;
      return new Promise((pe, he) => {
        z(de, (we) => {
          if (we)
            return he(we);
          pe(H);
        });
      });
    }
    walkSync(c = this.cwd, E = {}) {
      typeof c == "string" ? c = this.cwd.resolve(c) : c instanceof ce || (E = c, c = this.cwd);
      const { withFileTypes: j = !0, follow: G = !1, filter: K, walkFilter: O } = E, H = [];
      (!K || K(c)) && H.push(j ? c : c.fullpath());
      const te = /* @__PURE__ */ new Set([c]);
      for (const z of te) {
        const de = z.readdirSync();
        for (const pe of de) {
          (!K || K(pe)) && H.push(j ? pe : pe.fullpath());
          let he = pe;
          if (pe.isSymbolicLink()) {
            if (!(G && (he = pe.realpathSync())))
              continue;
            he.isUnknown() && he.lstatSync();
          }
          he.shouldWalk(te, O) && te.add(he);
        }
      }
      return H;
    }
    /**
     * Support for `for await`
     *
     * Alias for {@link PathScurryBase.iterate}
     *
     * Note: As of Node 19, this is very slow, compared to other methods of
     * walking.  Consider using {@link PathScurryBase.stream} if memory overhead
     * and backpressure are concerns, or {@link PathScurryBase.walk} if not.
     */
    [Symbol.asyncIterator]() {
      return this.iterate();
    }
    iterate(c = this.cwd, E = {}) {
      return typeof c == "string" ? c = this.cwd.resolve(c) : c instanceof ce || (E = c, c = this.cwd), this.stream(c, E)[Symbol.asyncIterator]();
    }
    /**
     * Iterating over a PathScurry performs a synchronous walk.
     *
     * Alias for {@link PathScurryBase.iterateSync}
     */
    [Symbol.iterator]() {
      return this.iterateSync();
    }
    *iterateSync(c = this.cwd, E = {}) {
      typeof c == "string" ? c = this.cwd.resolve(c) : c instanceof ce || (E = c, c = this.cwd);
      const { withFileTypes: j = !0, follow: G = !1, filter: K, walkFilter: O } = E;
      (!K || K(c)) && (yield j ? c : c.fullpath());
      const H = /* @__PURE__ */ new Set([c]);
      for (const te of H) {
        const z = te.readdirSync();
        for (const de of z) {
          (!K || K(de)) && (yield j ? de : de.fullpath());
          let pe = de;
          if (de.isSymbolicLink()) {
            if (!(G && (pe = de.realpathSync())))
              continue;
            pe.isUnknown() && pe.lstatSync();
          }
          pe.shouldWalk(H, O) && H.add(pe);
        }
      }
    }
    stream(c = this.cwd, E = {}) {
      typeof c == "string" ? c = this.cwd.resolve(c) : c instanceof ce || (E = c, c = this.cwd);
      const { withFileTypes: j = !0, follow: G = !1, filter: K, walkFilter: O } = E, H = new d.Minipass({ objectMode: !0 });
      (!K || K(c)) && H.write(j ? c : c.fullpath());
      const te = /* @__PURE__ */ new Set(), z = [c];
      let de = 0;
      const pe = () => {
        let he = !1;
        for (; !he; ) {
          const we = z.shift();
          if (!we) {
            de === 0 && H.end();
            return;
          }
          de++, te.add(we);
          const be = (Ae, xe, me = !1) => {
            if (Ae)
              return H.emit("error", Ae);
            if (G && !me) {
              const Ge = [];
              for (const Je of xe)
                Je.isSymbolicLink() && Ge.push(Je.realpath().then((Xe) => Xe?.isUnknown() ? Xe.lstat() : Xe));
              if (Ge.length) {
                Promise.all(Ge).then(() => be(null, xe, !0));
                return;
              }
            }
            for (const Ge of xe)
              Ge && (!K || K(Ge)) && (H.write(j ? Ge : Ge.fullpath()) || (he = !0));
            de--;
            for (const Ge of xe) {
              const Je = Ge.realpathCached() || Ge;
              Je.shouldWalk(te, O) && z.push(Je);
            }
            he && !H.flowing ? H.once("drain", pe) : _e || pe();
          };
          let _e = !0;
          we.readdirCB(be, !0), _e = !1;
        }
      };
      return pe(), H;
    }
    streamSync(c = this.cwd, E = {}) {
      typeof c == "string" ? c = this.cwd.resolve(c) : c instanceof ce || (E = c, c = this.cwd);
      const { withFileTypes: j = !0, follow: G = !1, filter: K, walkFilter: O } = E, H = new d.Minipass({ objectMode: !0 }), te = /* @__PURE__ */ new Set();
      (!K || K(c)) && H.write(j ? c : c.fullpath());
      const z = [c];
      let de = 0;
      const pe = () => {
        let he = !1;
        for (; !he; ) {
          const we = z.shift();
          if (!we) {
            de === 0 && H.end();
            return;
          }
          de++, te.add(we);
          const be = we.readdirSync();
          for (const _e of be)
            (!K || K(_e)) && (H.write(j ? _e : _e.fullpath()) || (he = !0));
          de--;
          for (const _e of be) {
            let Ae = _e;
            if (_e.isSymbolicLink()) {
              if (!(G && (Ae = _e.realpathSync())))
                continue;
              Ae.isUnknown() && Ae.lstatSync();
            }
            Ae.shouldWalk(te, O) && z.push(Ae);
          }
        }
        he && !H.flowing && H.once("drain", pe);
      };
      return pe(), H;
    }
    chdir(c = this.cwd) {
      const E = this.cwd;
      this.cwd = typeof c == "string" ? this.cwd.resolve(c) : c, this.cwd[ae](E);
    }
  }
  ke.PathScurryBase = q;
  class V extends q {
    /**
     * separator for generating path strings
     */
    sep = "\\";
    constructor(c = process.cwd(), E = {}) {
      const { nocase: j = !0 } = E;
      super(c, a.win32, "\\", { ...E, nocase: j }), this.nocase = j;
      for (let G = this.cwd; G; G = G.parent)
        G.nocase = this.nocase;
    }
    /**
     * @internal
     */
    parseRootPath(c) {
      return a.win32.parse(c).root.toUpperCase();
    }
    /**
     * @internal
     */
    newRoot(c) {
      return new Y(this.rootPath, b, void 0, this.roots, this.nocase, this.childrenCache(), { fs: c });
    }
    /**
     * Return true if the provided path string is an absolute path
     */
    isAbsolute(c) {
      return c.startsWith("/") || c.startsWith("\\") || /^[a-z]:(\/|\\)/i.test(c);
    }
  }
  ke.PathScurryWin32 = V;
  class Z extends q {
    /**
     * separator for generating path strings
     */
    sep = "/";
    constructor(c = process.cwd(), E = {}) {
      const { nocase: j = !1 } = E;
      super(c, a.posix, "/", { ...E, nocase: j }), this.nocase = j;
    }
    /**
     * @internal
     */
    parseRootPath(c) {
      return "/";
    }
    /**
     * @internal
     */
    newRoot(c) {
      return new P(this.rootPath, b, void 0, this.roots, this.nocase, this.childrenCache(), { fs: c });
    }
    /**
     * Return true if the provided path string is an absolute path
     */
    isAbsolute(c) {
      return c.startsWith("/");
    }
  }
  ke.PathScurryPosix = Z;
  class X extends Z {
    constructor(c = process.cwd(), E = {}) {
      const { nocase: j = !0 } = E;
      super(c, { ...E, nocase: j });
    }
  }
  return ke.PathScurryDarwin = X, ke.Path = process.platform === "win32" ? Y : P, ke.PathScurry = process.platform === "win32" ? V : process.platform === "darwin" ? X : Z, ke;
}
var rn = {}, vy;
function Km() {
  if (vy) return rn;
  vy = 1, Object.defineProperty(rn, "__esModule", { value: !0 }), rn.Pattern = void 0;
  const t = vr(), e = (a) => a.length >= 1, r = (a) => a.length >= 1;
  class o {
    #e;
    #r;
    #i;
    length;
    #t;
    #o;
    #S;
    #u;
    #c;
    #l;
    #a = !0;
    constructor(n, i, l, s) {
      if (!e(n))
        throw new TypeError("empty pattern list");
      if (!r(i))
        throw new TypeError("empty glob list");
      if (i.length !== n.length)
        throw new TypeError("mismatched pattern list and glob list lengths");
      if (this.length = n.length, l < 0 || l >= this.length)
        throw new TypeError("index out of range");
      if (this.#e = n, this.#r = i, this.#i = l, this.#t = s, this.#i === 0) {
        if (this.isUNC()) {
          const [f, d, y, R, ...w] = this.#e, [I, A, u, g, ...p] = this.#r;
          w[0] === "" && (w.shift(), p.shift());
          const b = [f, d, y, R, ""].join("/"), C = [I, A, u, g, ""].join("/");
          this.#e = [b, ...w], this.#r = [C, ...p], this.length = this.#e.length;
        } else if (this.isDrive() || this.isAbsolute()) {
          const [f, ...d] = this.#e, [y, ...R] = this.#r;
          d[0] === "" && (d.shift(), R.shift());
          const w = f + "/", I = y + "/";
          this.#e = [w, ...d], this.#r = [I, ...R], this.length = this.#e.length;
        }
      }
    }
    /**
     * The first entry in the parsed list of patterns
     */
    pattern() {
      return this.#e[this.#i];
    }
    /**
     * true of if pattern() returns a string
     */
    isString() {
      return typeof this.#e[this.#i] == "string";
    }
    /**
     * true of if pattern() returns GLOBSTAR
     */
    isGlobstar() {
      return this.#e[this.#i] === t.GLOBSTAR;
    }
    /**
     * true if pattern() returns a regexp
     */
    isRegExp() {
      return this.#e[this.#i] instanceof RegExp;
    }
    /**
     * The /-joined set of glob parts that make up this pattern
     */
    globString() {
      return this.#S = this.#S || (this.#i === 0 ? this.isAbsolute() ? this.#r[0] + this.#r.slice(1).join("/") : this.#r.join("/") : this.#r.slice(this.#i).join("/"));
    }
    /**
     * true if there are more pattern parts after this one
     */
    hasMore() {
      return this.length > this.#i + 1;
    }
    /**
     * The rest of the pattern after this part, or null if this is the end
     */
    rest() {
      return this.#o !== void 0 ? this.#o : this.hasMore() ? (this.#o = new o(this.#e, this.#r, this.#i + 1, this.#t), this.#o.#l = this.#l, this.#o.#c = this.#c, this.#o.#u = this.#u, this.#o) : this.#o = null;
    }
    /**
     * true if the pattern represents a //unc/path/ on windows
     */
    isUNC() {
      const n = this.#e;
      return this.#c !== void 0 ? this.#c : this.#c = this.#t === "win32" && this.#i === 0 && n[0] === "" && n[1] === "" && typeof n[2] == "string" && !!n[2] && typeof n[3] == "string" && !!n[3];
    }
    // pattern like C:/...
    // split = ['C:', ...]
    // XXX: would be nice to handle patterns like `c:*` to test the cwd
    // in c: for *, but I don't know of a way to even figure out what that
    // cwd is without actually chdir'ing into it?
    /**
     * True if the pattern starts with a drive letter on Windows
     */
    isDrive() {
      const n = this.#e;
      return this.#u !== void 0 ? this.#u : this.#u = this.#t === "win32" && this.#i === 0 && this.length > 1 && typeof n[0] == "string" && /^[a-z]:$/i.test(n[0]);
    }
    // pattern = '/' or '/...' or '/x/...'
    // split = ['', ''] or ['', ...] or ['', 'x', ...]
    // Drive and UNC both considered absolute on windows
    /**
     * True if the pattern is rooted on an absolute path
     */
    isAbsolute() {
      const n = this.#e;
      return this.#l !== void 0 ? this.#l : this.#l = n[0] === "" && n.length > 1 || this.isDrive() || this.isUNC();
    }
    /**
     * consume the root of the pattern, and return it
     */
    root() {
      const n = this.#e[0];
      return typeof n == "string" && this.isAbsolute() && this.#i === 0 ? n : "";
    }
    /**
     * Check to see if the current globstar pattern is allowed to follow
     * a symbolic link.
     */
    checkFollowGlobstar() {
      return !(this.#i === 0 || !this.isGlobstar() || !this.#a);
    }
    /**
     * Mark that the current globstar pattern is following a symbolic link
     */
    markFollowGlobstar() {
      return this.#i === 0 || !this.isGlobstar() || !this.#a ? !1 : (this.#a = !1, !0);
    }
  }
  return rn.Pattern = o, rn;
}
var Ut = {}, nn = {}, Ry;
function Jm() {
  if (Ry) return nn;
  Ry = 1, Object.defineProperty(nn, "__esModule", { value: !0 }), nn.Ignore = void 0;
  const t = vr(), e = Km(), r = typeof process == "object" && process && typeof process.platform == "string" ? process.platform : "linux";
  class o {
    relative;
    relativeChildren;
    absolute;
    absoluteChildren;
    platform;
    mmopts;
    constructor(n, { nobrace: i, nocase: l, noext: s, noglobstar: f, platform: d = r }) {
      this.relative = [], this.absolute = [], this.relativeChildren = [], this.absoluteChildren = [], this.platform = d, this.mmopts = {
        dot: !0,
        nobrace: i,
        nocase: l,
        noext: s,
        noglobstar: f,
        optimizationLevel: 2,
        platform: d,
        nocomment: !0,
        nonegate: !0
      };
      for (const y of n)
        this.add(y);
    }
    add(n) {
      const i = new t.Minimatch(n, this.mmopts);
      for (let l = 0; l < i.set.length; l++) {
        const s = i.set[l], f = i.globParts[l];
        if (!s || !f)
          throw new Error("invalid pattern object");
        for (; s[0] === "." && f[0] === "."; )
          s.shift(), f.shift();
        const d = new e.Pattern(s, f, 0, this.platform), y = new t.Minimatch(d.globString(), this.mmopts), R = f[f.length - 1] === "**", w = d.isAbsolute();
        w ? this.absolute.push(y) : this.relative.push(y), R && (w ? this.absoluteChildren.push(y) : this.relativeChildren.push(y));
      }
    }
    ignored(n) {
      const i = n.fullpath(), l = `${i}/`, s = n.relative() || ".", f = `${s}/`;
      for (const d of this.relative)
        if (d.match(s) || d.match(f))
          return !0;
      for (const d of this.absolute)
        if (d.match(i) || d.match(l))
          return !0;
      return !1;
    }
    childrenIgnored(n) {
      const i = n.fullpath() + "/", l = (n.relative() || ".") + "/";
      for (const s of this.relativeChildren)
        if (s.match(l))
          return !0;
      for (const s of this.absoluteChildren)
        if (s.match(i))
          return !0;
      return !1;
    }
  }
  return nn.Ignore = o, nn;
}
var Dt = {}, Ay;
function TA() {
  if (Ay) return Dt;
  Ay = 1, Object.defineProperty(Dt, "__esModule", { value: !0 }), Dt.Processor = Dt.SubWalks = Dt.MatchRecord = Dt.HasWalkedCache = void 0;
  const t = vr();
  class e {
    store;
    constructor(i = /* @__PURE__ */ new Map()) {
      this.store = i;
    }
    copy() {
      return new e(new Map(this.store));
    }
    hasWalked(i, l) {
      return this.store.get(i.fullpath())?.has(l.globString());
    }
    storeWalked(i, l) {
      const s = i.fullpath(), f = this.store.get(s);
      f ? f.add(l.globString()) : this.store.set(s, /* @__PURE__ */ new Set([l.globString()]));
    }
  }
  Dt.HasWalkedCache = e;
  class r {
    store = /* @__PURE__ */ new Map();
    add(i, l, s) {
      const f = (l ? 2 : 0) | (s ? 1 : 0), d = this.store.get(i);
      this.store.set(i, d === void 0 ? f : f & d);
    }
    // match, absolute, ifdir
    entries() {
      return [...this.store.entries()].map(([i, l]) => [
        i,
        !!(l & 2),
        !!(l & 1)
      ]);
    }
  }
  Dt.MatchRecord = r;
  class o {
    store = /* @__PURE__ */ new Map();
    add(i, l) {
      if (!i.canReaddir())
        return;
      const s = this.store.get(i);
      s ? s.find((f) => f.globString() === l.globString()) || s.push(l) : this.store.set(i, [l]);
    }
    get(i) {
      const l = this.store.get(i);
      if (!l)
        throw new Error("attempting to walk unknown path");
      return l;
    }
    entries() {
      return this.keys().map((i) => [i, this.store.get(i)]);
    }
    keys() {
      return [...this.store.keys()].filter((i) => i.canReaddir());
    }
  }
  Dt.SubWalks = o;
  class a {
    hasWalkedCache;
    matches = new r();
    subwalks = new o();
    patterns;
    follow;
    dot;
    opts;
    constructor(i, l) {
      this.opts = i, this.follow = !!i.follow, this.dot = !!i.dot, this.hasWalkedCache = l ? l.copy() : new e();
    }
    processPatterns(i, l) {
      this.patterns = l;
      const s = l.map((f) => [i, f]);
      for (let [f, d] of s) {
        this.hasWalkedCache.storeWalked(f, d);
        const y = d.root(), R = d.isAbsolute() && this.opts.absolute !== !1;
        if (y) {
          f = f.resolve(y === "/" && this.opts.root !== void 0 ? this.opts.root : y);
          const u = d.rest();
          if (u)
            d = u;
          else {
            this.matches.add(f, !0, !1);
            continue;
          }
        }
        if (f.isENOENT())
          continue;
        let w, I, A = !1;
        for (; typeof (w = d.pattern()) == "string" && (I = d.rest()); )
          f = f.resolve(w), d = I, A = !0;
        if (w = d.pattern(), I = d.rest(), A) {
          if (this.hasWalkedCache.hasWalked(f, d))
            continue;
          this.hasWalkedCache.storeWalked(f, d);
        }
        if (typeof w == "string") {
          const u = w === ".." || w === "" || w === ".";
          this.matches.add(f.resolve(w), R, u);
          continue;
        } else if (w === t.GLOBSTAR) {
          (!f.isSymbolicLink() || this.follow || d.checkFollowGlobstar()) && this.subwalks.add(f, d);
          const u = I?.pattern(), g = I?.rest();
          if (!I || (u === "" || u === ".") && !g)
            this.matches.add(f, R, u === "" || u === ".");
          else if (u === "..") {
            const p = f.parent || f;
            g ? this.hasWalkedCache.hasWalked(p, g) || this.subwalks.add(p, g) : this.matches.add(p, R, !0);
          }
        } else w instanceof RegExp && this.subwalks.add(f, d);
      }
      return this;
    }
    subwalkTargets() {
      return this.subwalks.keys();
    }
    child() {
      return new a(this.opts, this.hasWalkedCache);
    }
    // return a new Processor containing the subwalks for each
    // child entry, and a set of matches, and
    // a hasWalkedCache that's a copy of this one
    // then we're going to call
    filterEntries(i, l) {
      const s = this.subwalks.get(i), f = this.child();
      for (const d of l)
        for (const y of s) {
          const R = y.isAbsolute(), w = y.pattern(), I = y.rest();
          w === t.GLOBSTAR ? f.testGlobstar(d, y, I, R) : w instanceof RegExp ? f.testRegExp(d, w, I, R) : f.testString(d, w, I, R);
        }
      return f;
    }
    testGlobstar(i, l, s, f) {
      if ((this.dot || !i.name.startsWith(".")) && (l.hasMore() || this.matches.add(i, f, !1), i.canReaddir() && (this.follow || !i.isSymbolicLink() ? this.subwalks.add(i, l) : i.isSymbolicLink() && (s && l.checkFollowGlobstar() ? this.subwalks.add(i, s) : l.markFollowGlobstar() && this.subwalks.add(i, l)))), s) {
        const d = s.pattern();
        if (typeof d == "string" && // dots and empty were handled already
        d !== ".." && d !== "" && d !== ".")
          this.testString(i, d, s.rest(), f);
        else if (d === "..") {
          const y = i.parent || i;
          this.subwalks.add(y, s);
        } else d instanceof RegExp && this.testRegExp(i, d, s.rest(), f);
      }
    }
    testRegExp(i, l, s, f) {
      l.test(i.name) && (s ? this.subwalks.add(i, s) : this.matches.add(i, f, !1));
    }
    testString(i, l, s, f) {
      i.isNamed(l) && (s ? this.subwalks.add(i, s) : this.matches.add(i, f, !1));
    }
  }
  return Dt.Processor = a, Dt;
}
var Ty;
function xA() {
  if (Ty) return Ut;
  Ty = 1, Object.defineProperty(Ut, "__esModule", { value: !0 }), Ut.GlobStream = Ut.GlobWalker = Ut.GlobUtil = void 0;
  const t = Ym(), e = Jm(), r = TA(), o = (l, s) => typeof l == "string" ? new e.Ignore([l], s) : Array.isArray(l) ? new e.Ignore(l, s) : l;
  class a {
    path;
    patterns;
    opts;
    seen = /* @__PURE__ */ new Set();
    paused = !1;
    aborted = !1;
    #e = [];
    #r;
    #i;
    signal;
    maxDepth;
    includeChildMatches;
    constructor(s, f, d) {
      if (this.patterns = s, this.path = f, this.opts = d, this.#i = !d.posix && d.platform === "win32" ? "\\" : "/", this.includeChildMatches = d.includeChildMatches !== !1, (d.ignore || !this.includeChildMatches) && (this.#r = o(d.ignore ?? [], d), !this.includeChildMatches && typeof this.#r.add != "function")) {
        const y = "cannot ignore child matches, ignore lacks add() method.";
        throw new Error(y);
      }
      this.maxDepth = d.maxDepth || 1 / 0, d.signal && (this.signal = d.signal, this.signal.addEventListener("abort", () => {
        this.#e.length = 0;
      }));
    }
    #t(s) {
      return this.seen.has(s) || !!this.#r?.ignored?.(s);
    }
    #o(s) {
      return !!this.#r?.childrenIgnored?.(s);
    }
    // backpressure mechanism
    pause() {
      this.paused = !0;
    }
    resume() {
      if (this.signal?.aborted)
        return;
      this.paused = !1;
      let s;
      for (; !this.paused && (s = this.#e.shift()); )
        s();
    }
    onResume(s) {
      this.signal?.aborted || (this.paused ? this.#e.push(s) : s());
    }
    // do the requisite realpath/stat checking, and return the path
    // to add or undefined to filter it out.
    async matchCheck(s, f) {
      if (f && this.opts.nodir)
        return;
      let d;
      if (this.opts.realpath) {
        if (d = s.realpathCached() || await s.realpath(), !d)
          return;
        s = d;
      }
      const R = s.isUnknown() || this.opts.stat ? await s.lstat() : s;
      if (this.opts.follow && this.opts.nodir && R?.isSymbolicLink()) {
        const w = await R.realpath();
        w && (w.isUnknown() || this.opts.stat) && await w.lstat();
      }
      return this.matchCheckTest(R, f);
    }
    matchCheckTest(s, f) {
      return s && (this.maxDepth === 1 / 0 || s.depth() <= this.maxDepth) && (!f || s.canReaddir()) && (!this.opts.nodir || !s.isDirectory()) && (!this.opts.nodir || !this.opts.follow || !s.isSymbolicLink() || !s.realpathCached()?.isDirectory()) && !this.#t(s) ? s : void 0;
    }
    matchCheckSync(s, f) {
      if (f && this.opts.nodir)
        return;
      let d;
      if (this.opts.realpath) {
        if (d = s.realpathCached() || s.realpathSync(), !d)
          return;
        s = d;
      }
      const R = s.isUnknown() || this.opts.stat ? s.lstatSync() : s;
      if (this.opts.follow && this.opts.nodir && R?.isSymbolicLink()) {
        const w = R.realpathSync();
        w && (w?.isUnknown() || this.opts.stat) && w.lstatSync();
      }
      return this.matchCheckTest(R, f);
    }
    matchFinish(s, f) {
      if (this.#t(s))
        return;
      if (!this.includeChildMatches && this.#r?.add) {
        const R = `${s.relativePosix()}/**`;
        this.#r.add(R);
      }
      const d = this.opts.absolute === void 0 ? f : this.opts.absolute;
      this.seen.add(s);
      const y = this.opts.mark && s.isDirectory() ? this.#i : "";
      if (this.opts.withFileTypes)
        this.matchEmit(s);
      else if (d) {
        const R = this.opts.posix ? s.fullpathPosix() : s.fullpath();
        this.matchEmit(R + y);
      } else {
        const R = this.opts.posix ? s.relativePosix() : s.relative(), w = this.opts.dotRelative && !R.startsWith(".." + this.#i) ? "." + this.#i : "";
        this.matchEmit(R ? w + R + y : "." + y);
      }
    }
    async match(s, f, d) {
      const y = await this.matchCheck(s, d);
      y && this.matchFinish(y, f);
    }
    matchSync(s, f, d) {
      const y = this.matchCheckSync(s, d);
      y && this.matchFinish(y, f);
    }
    walkCB(s, f, d) {
      this.signal?.aborted && d(), this.walkCB2(s, f, new r.Processor(this.opts), d);
    }
    walkCB2(s, f, d, y) {
      if (this.#o(s))
        return y();
      if (this.signal?.aborted && y(), this.paused) {
        this.onResume(() => this.walkCB2(s, f, d, y));
        return;
      }
      d.processPatterns(s, f);
      let R = 1;
      const w = () => {
        --R === 0 && y();
      };
      for (const [I, A, u] of d.matches.entries())
        this.#t(I) || (R++, this.match(I, A, u).then(() => w()));
      for (const I of d.subwalkTargets()) {
        if (this.maxDepth !== 1 / 0 && I.depth() >= this.maxDepth)
          continue;
        R++;
        const A = I.readdirCached();
        I.calledReaddir() ? this.walkCB3(I, A, d, w) : I.readdirCB((u, g) => this.walkCB3(I, g, d, w), !0);
      }
      w();
    }
    walkCB3(s, f, d, y) {
      d = d.filterEntries(s, f);
      let R = 1;
      const w = () => {
        --R === 0 && y();
      };
      for (const [I, A, u] of d.matches.entries())
        this.#t(I) || (R++, this.match(I, A, u).then(() => w()));
      for (const [I, A] of d.subwalks.entries())
        R++, this.walkCB2(I, A, d.child(), w);
      w();
    }
    walkCBSync(s, f, d) {
      this.signal?.aborted && d(), this.walkCB2Sync(s, f, new r.Processor(this.opts), d);
    }
    walkCB2Sync(s, f, d, y) {
      if (this.#o(s))
        return y();
      if (this.signal?.aborted && y(), this.paused) {
        this.onResume(() => this.walkCB2Sync(s, f, d, y));
        return;
      }
      d.processPatterns(s, f);
      let R = 1;
      const w = () => {
        --R === 0 && y();
      };
      for (const [I, A, u] of d.matches.entries())
        this.#t(I) || this.matchSync(I, A, u);
      for (const I of d.subwalkTargets()) {
        if (this.maxDepth !== 1 / 0 && I.depth() >= this.maxDepth)
          continue;
        R++;
        const A = I.readdirSync();
        this.walkCB3Sync(I, A, d, w);
      }
      w();
    }
    walkCB3Sync(s, f, d, y) {
      d = d.filterEntries(s, f);
      let R = 1;
      const w = () => {
        --R === 0 && y();
      };
      for (const [I, A, u] of d.matches.entries())
        this.#t(I) || this.matchSync(I, A, u);
      for (const [I, A] of d.subwalks.entries())
        R++, this.walkCB2Sync(I, A, d.child(), w);
      w();
    }
  }
  Ut.GlobUtil = a;
  class n extends a {
    matches = /* @__PURE__ */ new Set();
    constructor(s, f, d) {
      super(s, f, d);
    }
    matchEmit(s) {
      this.matches.add(s);
    }
    async walk() {
      if (this.signal?.aborted)
        throw this.signal.reason;
      return this.path.isUnknown() && await this.path.lstat(), await new Promise((s, f) => {
        this.walkCB(this.path, this.patterns, () => {
          this.signal?.aborted ? f(this.signal.reason) : s(this.matches);
        });
      }), this.matches;
    }
    walkSync() {
      if (this.signal?.aborted)
        throw this.signal.reason;
      return this.path.isUnknown() && this.path.lstatSync(), this.walkCBSync(this.path, this.patterns, () => {
        if (this.signal?.aborted)
          throw this.signal.reason;
      }), this.matches;
    }
  }
  Ut.GlobWalker = n;
  class i extends a {
    results;
    constructor(s, f, d) {
      super(s, f, d), this.results = new t.Minipass({
        signal: this.signal,
        objectMode: !0
      }), this.results.on("drain", () => this.resume()), this.results.on("resume", () => this.resume());
    }
    matchEmit(s) {
      this.results.write(s), this.results.flowing || this.pause();
    }
    stream() {
      const s = this.path;
      return s.isUnknown() ? s.lstat().then(() => {
        this.walkCB(s, this.patterns, () => this.results.end());
      }) : this.walkCB(s, this.patterns, () => this.results.end()), this.results;
    }
    streamSync() {
      return this.path.isUnknown() && this.path.lstatSync(), this.walkCBSync(this.path, this.patterns, () => this.results.end()), this.results;
    }
  }
  return Ut.GlobStream = i, Ut;
}
var xy;
function Iy() {
  if (xy) return Qr;
  xy = 1, Object.defineProperty(Qr, "__esModule", { value: !0 }), Qr.Glob = void 0;
  const t = vr(), e = nw, r = AA(), o = Km(), a = xA(), n = typeof process == "object" && process && typeof process.platform == "string" ? process.platform : "linux";
  class i {
    absolute;
    cwd;
    root;
    dot;
    dotRelative;
    follow;
    ignore;
    magicalBraces;
    mark;
    matchBase;
    maxDepth;
    nobrace;
    nocase;
    nodir;
    noext;
    noglobstar;
    pattern;
    platform;
    realpath;
    scurry;
    stat;
    signal;
    windowsPathsNoEscape;
    withFileTypes;
    includeChildMatches;
    /**
     * The options provided to the constructor.
     */
    opts;
    /**
     * An array of parsed immutable {@link Pattern} objects.
     */
    patterns;
    /**
     * All options are stored as properties on the `Glob` object.
     *
     * See {@link GlobOptions} for full options descriptions.
     *
     * Note that a previous `Glob` object can be passed as the
     * `GlobOptions` to another `Glob` instantiation to re-use settings
     * and caches with a new pattern.
     *
     * Traversal functions can be called multiple times to run the walk
     * again.
     */
    constructor(s, f) {
      if (!f)
        throw new TypeError("glob options required");
      if (this.withFileTypes = !!f.withFileTypes, this.signal = f.signal, this.follow = !!f.follow, this.dot = !!f.dot, this.dotRelative = !!f.dotRelative, this.nodir = !!f.nodir, this.mark = !!f.mark, f.cwd ? (f.cwd instanceof URL || f.cwd.startsWith("file://")) && (f.cwd = (0, e.fileURLToPath)(f.cwd)) : this.cwd = "", this.cwd = f.cwd || "", this.root = f.root, this.magicalBraces = !!f.magicalBraces, this.nobrace = !!f.nobrace, this.noext = !!f.noext, this.realpath = !!f.realpath, this.absolute = f.absolute, this.includeChildMatches = f.includeChildMatches !== !1, this.noglobstar = !!f.noglobstar, this.matchBase = !!f.matchBase, this.maxDepth = typeof f.maxDepth == "number" ? f.maxDepth : 1 / 0, this.stat = !!f.stat, this.ignore = f.ignore, this.withFileTypes && this.absolute !== void 0)
        throw new Error("cannot set absolute and withFileTypes:true");
      if (typeof s == "string" && (s = [s]), this.windowsPathsNoEscape = !!f.windowsPathsNoEscape || f.allowWindowsEscape === !1, this.windowsPathsNoEscape && (s = s.map((A) => A.replace(/\\/g, "/"))), this.matchBase) {
        if (f.noglobstar)
          throw new TypeError("base matching requires globstar");
        s = s.map((A) => A.includes("/") ? A : `./**/${A}`);
      }
      if (this.pattern = s, this.platform = f.platform || n, this.opts = { ...f, platform: this.platform }, f.scurry) {
        if (this.scurry = f.scurry, f.nocase !== void 0 && f.nocase !== f.scurry.nocase)
          throw new Error("nocase option contradicts provided scurry option");
      } else {
        const A = f.platform === "win32" ? r.PathScurryWin32 : f.platform === "darwin" ? r.PathScurryDarwin : f.platform ? r.PathScurryPosix : r.PathScurry;
        this.scurry = new A(this.cwd, {
          nocase: f.nocase,
          fs: f.fs
        });
      }
      this.nocase = this.scurry.nocase;
      const d = this.platform === "darwin" || this.platform === "win32", y = {
        // default nocase based on platform
        ...f,
        dot: this.dot,
        matchBase: this.matchBase,
        nobrace: this.nobrace,
        nocase: this.nocase,
        nocaseMagicOnly: d,
        nocomment: !0,
        noext: this.noext,
        nonegate: !0,
        optimizationLevel: 2,
        platform: this.platform,
        windowsPathsNoEscape: this.windowsPathsNoEscape,
        debug: !!this.opts.debug
      }, R = this.pattern.map((A) => new t.Minimatch(A, y)), [w, I] = R.reduce((A, u) => (A[0].push(...u.set), A[1].push(...u.globParts), A), [[], []]);
      this.patterns = w.map((A, u) => {
        const g = I[u];
        if (!g)
          throw new Error("invalid pattern object");
        return new o.Pattern(A, g, 0, this.platform);
      });
    }
    async walk() {
      return [
        ...await new a.GlobWalker(this.patterns, this.scurry.cwd, {
          ...this.opts,
          maxDepth: this.maxDepth !== 1 / 0 ? this.maxDepth + this.scurry.cwd.depth() : 1 / 0,
          platform: this.platform,
          nocase: this.nocase,
          includeChildMatches: this.includeChildMatches
        }).walk()
      ];
    }
    walkSync() {
      return [
        ...new a.GlobWalker(this.patterns, this.scurry.cwd, {
          ...this.opts,
          maxDepth: this.maxDepth !== 1 / 0 ? this.maxDepth + this.scurry.cwd.depth() : 1 / 0,
          platform: this.platform,
          nocase: this.nocase,
          includeChildMatches: this.includeChildMatches
        }).walkSync()
      ];
    }
    stream() {
      return new a.GlobStream(this.patterns, this.scurry.cwd, {
        ...this.opts,
        maxDepth: this.maxDepth !== 1 / 0 ? this.maxDepth + this.scurry.cwd.depth() : 1 / 0,
        platform: this.platform,
        nocase: this.nocase,
        includeChildMatches: this.includeChildMatches
      }).stream();
    }
    streamSync() {
      return new a.GlobStream(this.patterns, this.scurry.cwd, {
        ...this.opts,
        maxDepth: this.maxDepth !== 1 / 0 ? this.maxDepth + this.scurry.cwd.depth() : 1 / 0,
        platform: this.platform,
        nocase: this.nocase,
        includeChildMatches: this.includeChildMatches
      }).streamSync();
    }
    /**
     * Default sync iteration function. Returns a Generator that
     * iterates over the results.
     */
    iterateSync() {
      return this.streamSync()[Symbol.iterator]();
    }
    [Symbol.iterator]() {
      return this.iterateSync();
    }
    /**
     * Default async iteration function. Returns an AsyncGenerator that
     * iterates over the results.
     */
    iterate() {
      return this.stream()[Symbol.asyncIterator]();
    }
    [Symbol.asyncIterator]() {
      return this.iterate();
    }
  }
  return Qr.Glob = i, Qr;
}
var sn = {}, Dy;
function Oy() {
  if (Dy) return sn;
  Dy = 1, Object.defineProperty(sn, "__esModule", { value: !0 }), sn.hasMagic = void 0;
  const t = vr(), e = (r, o = {}) => {
    Array.isArray(r) || (r = [r]);
    for (const a of r)
      if (new t.Minimatch(a, o).hasMagic())
        return !0;
    return !1;
  };
  return sn.hasMagic = e, sn;
}
var Ny;
function IA() {
  return Ny || (Ny = 1, (function(t) {
    Object.defineProperty(t, "__esModule", { value: !0 }), t.glob = t.sync = t.iterate = t.iterateSync = t.stream = t.streamSync = t.Ignore = t.hasMagic = t.Glob = t.unescape = t.escape = void 0, t.globStreamSync = s, t.globStream = f, t.globSync = d, t.globIterateSync = R, t.globIterate = w;
    const e = vr(), r = Iy(), o = Oy();
    var a = vr();
    Object.defineProperty(t, "escape", { enumerable: !0, get: function() {
      return a.escape;
    } }), Object.defineProperty(t, "unescape", { enumerable: !0, get: function() {
      return a.unescape;
    } });
    var n = Iy();
    Object.defineProperty(t, "Glob", { enumerable: !0, get: function() {
      return n.Glob;
    } });
    var i = Oy();
    Object.defineProperty(t, "hasMagic", { enumerable: !0, get: function() {
      return i.hasMagic;
    } });
    var l = Jm();
    Object.defineProperty(t, "Ignore", { enumerable: !0, get: function() {
      return l.Ignore;
    } });
    function s(I, A = {}) {
      return new r.Glob(I, A).streamSync();
    }
    function f(I, A = {}) {
      return new r.Glob(I, A).stream();
    }
    function d(I, A = {}) {
      return new r.Glob(I, A).walkSync();
    }
    async function y(I, A = {}) {
      return new r.Glob(I, A).walk();
    }
    function R(I, A = {}) {
      return new r.Glob(I, A).iterateSync();
    }
    function w(I, A = {}) {
      return new r.Glob(I, A).iterate();
    }
    t.streamSync = s, t.stream = Object.assign(f, { sync: s }), t.iterateSync = R, t.iterate = Object.assign(w, {
      sync: R
    }), t.sync = Object.assign(d, {
      stream: s,
      iterate: R
    }), t.glob = Object.assign(y, {
      glob: y,
      globSync: d,
      sync: t.sync,
      globStream: f,
      stream: t.stream,
      globStreamSync: s,
      streamSync: t.streamSync,
      globIterate: w,
      iterate: t.iterate,
      globIterateSync: R,
      iterateSync: t.iterateSync,
      Glob: r.Glob,
      hasMagic: o.hasMagic,
      escape: e.escape,
      unescape: e.unescape
    }), t.glob.glob = t.glob;
  })(Ql)), Ql;
}
var Ly;
function DA() {
  if (Ly) return rl.exports;
  Ly = 1;
  var t = Rr(), e = Ue, r = FR(), o = dA(), a = bA(), n = mA(), i = IA(), l = rl.exports = {}, s = /[\/\\]/g, f = function(d, y) {
    var R = [];
    return r(d).forEach(function(w) {
      var I = w.indexOf("!") === 0;
      I && (w = w.slice(1));
      var A = y(w);
      I ? R = o(R, A) : R = a(R, A);
    }), R;
  };
  return l.exists = function() {
    var d = e.join.apply(e, arguments);
    return t.existsSync(d);
  }, l.expand = function(...d) {
    var y = n(d[0]) ? d.shift() : {}, R = Array.isArray(d[0]) ? d[0] : d;
    if (R.length === 0)
      return [];
    var w = f(R, function(I) {
      return i.sync(I, y);
    });
    return y.filter && (w = w.filter(function(I) {
      I = e.join(y.cwd || "", I);
      try {
        return typeof y.filter == "function" ? y.filter(I) : t.statSync(I)[y.filter]();
      } catch {
        return !1;
      }
    })), w;
  }, l.expandMapping = function(d, y, R) {
    R = Object.assign({
      rename: function(A, u) {
        return e.join(A || "", u);
      }
    }, R);
    var w = [], I = {};
    return l.expand(R, d).forEach(function(A) {
      var u = A;
      R.flatten && (u = e.basename(u)), R.ext && (u = u.replace(/(\.[^\/]*)?$/, R.ext));
      var g = R.rename(y, u, R);
      R.cwd && (A = e.join(R.cwd, A)), g = g.replace(s, "/"), A = A.replace(s, "/"), I[g] ? I[g].src.push(A) : (w.push({
        src: [A],
        dest: g
      }), I[g] = w[w.length - 1]);
    }), w;
  }, l.normalizeFilesArray = function(d) {
    var y = [];
    return d.forEach(function(R) {
      ("src" in R || "dest" in R) && y.push(R);
    }), y.length === 0 ? [] : (y = _(y).chain().forEach(function(R) {
      !("src" in R) || !R.src || (Array.isArray(R.src) ? R.src = r(R.src) : R.src = [R.src]);
    }).map(function(R) {
      var w = Object.assign({}, R);
      if (delete w.src, delete w.dest, R.expand)
        return l.expandMapping(R.src, R.dest, w).map(function(A) {
          var u = Object.assign({}, R);
          return u.orig = Object.assign({}, R), u.src = A.src, u.dest = A.dest, ["expand", "cwd", "flatten", "rename", "ext"].forEach(function(g) {
            delete u[g];
          }), u;
        });
      var I = Object.assign({}, R);
      return I.orig = Object.assign({}, R), "src" in I && Object.defineProperty(I, "src", {
        enumerable: !0,
        get: function A() {
          var u;
          return "result" in A || (u = R.src, u = Array.isArray(u) ? r(u) : [u], A.result = l.expand(w, u)), A.result;
        }
      }), "dest" in I && (I.dest = R.dest), I;
    }).flatten().value(), y);
  }, rl.exports;
}
var Py;
function Tn() {
  if (Py) return xo.exports;
  Py = 1;
  var t = Rr(), e = Ue, r = F0(), o = H0(), a = Sm(), n = wR();
  tr.Stream;
  var i = CR().PassThrough, l = xo.exports = {};
  return l.file = DA(), l.collectStream = function(s, f) {
    var d = [], y = 0;
    s.on("error", f), s.on("data", function(R) {
      d.push(R), y += R.length;
    }), s.on("end", function() {
      var R = Buffer.alloc(y), w = 0;
      d.forEach(function(I) {
        I.copy(R, w), w += I.length;
      }), f(null, R);
    });
  }, l.dateify = function(s) {
    return s = s || /* @__PURE__ */ new Date(), s instanceof Date ? s = s : typeof s == "string" ? s = new Date(s) : s = /* @__PURE__ */ new Date(), s;
  }, l.defaults = function(s, f, d) {
    var y = arguments;
    return y[0] = y[0] || {}, n(...y);
  }, l.isStream = function(s) {
    return r(s);
  }, l.lazyReadStream = function(s) {
    return new o.Readable(function() {
      return t.createReadStream(s);
    });
  }, l.normalizeInputSource = function(s) {
    return s === null ? Buffer.alloc(0) : typeof s == "string" ? Buffer.from(s) : l.isStream(s) ? s.pipe(new i()) : s;
  }, l.sanitizePath = function(s) {
    return a(s, !1).replace(/^\w+:/, "").replace(/^(\.\.\/|\/)+/, "");
  }, l.trailingSlashIt = function(s) {
    return s.slice(-1) !== "/" ? s + "/" : s;
  }, l.unixifyPath = function(s) {
    return a(s, !1).replace(/^\w+:/, "");
  }, l.walkdir = function(s, f, d) {
    var y = [];
    typeof f == "function" && (d = f, f = s), t.readdir(s, function(R, w) {
      var I = 0, A, u;
      if (R)
        return d(R);
      (function g() {
        if (A = w[I++], !A)
          return d(null, y);
        u = e.join(s, A), t.stat(u, function(p, b) {
          y.push({
            path: u,
            relative: e.relative(f, u).replace(/\\/g, "/"),
            stats: b
          }), b && b.isDirectory() ? l.walkdir(u, f, function(C, N) {
            if (C)
              return d(C);
            N.forEach(function(W) {
              y.push(W);
            }), g();
          }) : g();
        });
      })();
    });
  }, xo.exports;
}
var tu = { exports: {} };
var Cy;
function OA() {
  return Cy || (Cy = 1, (function(t, e) {
    var r = It;
    const o = {
      ABORTED: "archive was aborted",
      DIRECTORYDIRPATHREQUIRED: "diretory dirpath argument must be a non-empty string value",
      DIRECTORYFUNCTIONINVALIDDATA: "invalid data returned by directory custom data function",
      ENTRYNAMEREQUIRED: "entry name must be a non-empty string value",
      FILEFILEPATHREQUIRED: "file filepath argument must be a non-empty string value",
      FINALIZING: "archive already finalizing",
      QUEUECLOSED: "queue closed",
      NOENDMETHOD: "no suitable finalize/end method defined by module",
      DIRECTORYNOTSUPPORTED: "support for directory entries not defined by module",
      FORMATSET: "archive format already set",
      INPUTSTEAMBUFFERREQUIRED: "input source must be valid Stream or Buffer instance",
      MODULESET: "module already set",
      SYMLINKNOTSUPPORTED: "support for symlink entries not defined by module",
      SYMLINKFILEPATHREQUIRED: "symlink filepath argument must be a non-empty string value",
      SYMLINKTARGETREQUIRED: "symlink target argument must be a non-empty string value",
      ENTRYNOTSUPPORTED: "entry not supported"
    };
    function a(n, i) {
      Error.captureStackTrace(this, this.constructor), this.message = o[n] || n, this.code = n, this.data = i;
    }
    r.inherits(a, Error), t.exports = a;
  })(tu)), tu.exports;
}
var oi = { exports: {} }, ru = { exports: {} }, nu, My;
function Ve() {
  if (My) return nu;
  My = 1;
  class t extends Error {
    constructor(r) {
      if (!Array.isArray(r))
        throw new TypeError(`Expected input to be an Array, got ${typeof r}`);
      let o = "";
      for (let a = 0; a < r.length; a++)
        o += `    ${r[a].stack}
`;
      super(o), this.name = "AggregateError", this.errors = r;
    }
  }
  return nu = {
    AggregateError: t,
    ArrayIsArray(e) {
      return Array.isArray(e);
    },
    ArrayPrototypeIncludes(e, r) {
      return e.includes(r);
    },
    ArrayPrototypeIndexOf(e, r) {
      return e.indexOf(r);
    },
    ArrayPrototypeJoin(e, r) {
      return e.join(r);
    },
    ArrayPrototypeMap(e, r) {
      return e.map(r);
    },
    ArrayPrototypePop(e, r) {
      return e.pop(r);
    },
    ArrayPrototypePush(e, r) {
      return e.push(r);
    },
    ArrayPrototypeSlice(e, r, o) {
      return e.slice(r, o);
    },
    Error,
    FunctionPrototypeCall(e, r, ...o) {
      return e.call(r, ...o);
    },
    FunctionPrototypeSymbolHasInstance(e, r) {
      return Function.prototype[Symbol.hasInstance].call(e, r);
    },
    MathFloor: Math.floor,
    Number,
    NumberIsInteger: Number.isInteger,
    NumberIsNaN: Number.isNaN,
    NumberMAX_SAFE_INTEGER: Number.MAX_SAFE_INTEGER,
    NumberMIN_SAFE_INTEGER: Number.MIN_SAFE_INTEGER,
    NumberParseInt: Number.parseInt,
    ObjectDefineProperties(e, r) {
      return Object.defineProperties(e, r);
    },
    ObjectDefineProperty(e, r, o) {
      return Object.defineProperty(e, r, o);
    },
    ObjectGetOwnPropertyDescriptor(e, r) {
      return Object.getOwnPropertyDescriptor(e, r);
    },
    ObjectKeys(e) {
      return Object.keys(e);
    },
    ObjectSetPrototypeOf(e, r) {
      return Object.setPrototypeOf(e, r);
    },
    Promise,
    PromisePrototypeCatch(e, r) {
      return e.catch(r);
    },
    PromisePrototypeThen(e, r, o) {
      return e.then(r, o);
    },
    PromiseReject(e) {
      return Promise.reject(e);
    },
    PromiseResolve(e) {
      return Promise.resolve(e);
    },
    ReflectApply: Reflect.apply,
    RegExpPrototypeTest(e, r) {
      return e.test(r);
    },
    SafeSet: Set,
    String,
    StringPrototypeSlice(e, r, o) {
      return e.slice(r, o);
    },
    StringPrototypeToLowerCase(e) {
      return e.toLowerCase();
    },
    StringPrototypeToUpperCase(e) {
      return e.toUpperCase();
    },
    StringPrototypeTrim(e) {
      return e.trim();
    },
    Symbol,
    SymbolFor: Symbol.for,
    SymbolAsyncIterator: Symbol.asyncIterator,
    SymbolHasInstance: Symbol.hasInstance,
    SymbolIterator: Symbol.iterator,
    SymbolDispose: Symbol.dispose || /* @__PURE__ */ Symbol("Symbol.dispose"),
    SymbolAsyncDispose: Symbol.asyncDispose || /* @__PURE__ */ Symbol("Symbol.asyncDispose"),
    TypedArrayPrototypeSet(e, r, o) {
      return e.set(r, o);
    },
    Boolean,
    Uint8Array
  }, nu;
}
var iu = { exports: {} }, su, ky;
function Xm() {
  return ky || (ky = 1, su = {
    format(t, ...e) {
      return t.replace(/%([sdifj])/g, function(...[r, o]) {
        const a = e.shift();
        return o === "f" ? a.toFixed(6) : o === "j" ? JSON.stringify(a) : o === "s" && typeof a == "object" ? `${a.constructor !== Object ? a.constructor.name : ""} {}`.trim() : a.toString();
      });
    },
    inspect(t) {
      switch (typeof t) {
        case "string":
          if (t.includes("'"))
            if (t.includes('"')) {
              if (!t.includes("`") && !t.includes("${"))
                return `\`${t}\``;
            } else return `"${t}"`;
          return `'${t}'`;
        case "number":
          return isNaN(t) ? "NaN" : Object.is(t, -0) ? String(t) : t;
        case "bigint":
          return `${String(t)}n`;
        case "boolean":
        case "undefined":
          return String(t);
        case "object":
          return "{}";
      }
    }
  }), su;
}
var ou, Fy;
function ft() {
  if (Fy) return ou;
  Fy = 1;
  const { format: t, inspect: e } = Xm(), { AggregateError: r } = Ve(), o = globalThis.AggregateError || r, a = /* @__PURE__ */ Symbol("kIsNodeError"), n = [
    "string",
    "function",
    "number",
    "object",
    // Accept 'Function' and 'Object' as alternative to the lower cased version.
    "Function",
    "Object",
    "boolean",
    "bigint",
    "symbol"
  ], i = /^([A-Z][a-z0-9]*)+$/, l = "__node_internal_", s = {};
  function f(u, g) {
    if (!u)
      throw new s.ERR_INTERNAL_ASSERTION(g);
  }
  function d(u) {
    let g = "", p = u.length;
    const b = u[0] === "-" ? 1 : 0;
    for (; p >= b + 4; p -= 3)
      g = `_${u.slice(p - 3, p)}${g}`;
    return `${u.slice(0, p)}${g}`;
  }
  function y(u, g, p) {
    if (typeof g == "function")
      return f(
        g.length <= p.length,
        // Default options do not count.
        `Code: ${u}; The provided arguments length (${p.length}) does not match the required ones (${g.length}).`
      ), g(...p);
    const b = (g.match(/%[dfijoOs]/g) || []).length;
    return f(
      b === p.length,
      `Code: ${u}; The provided arguments length (${p.length}) does not match the required ones (${b}).`
    ), p.length === 0 ? g : t(g, ...p);
  }
  function R(u, g, p) {
    p || (p = Error);
    class b extends p {
      constructor(...N) {
        super(y(u, g, N));
      }
      toString() {
        return `${this.name} [${u}]: ${this.message}`;
      }
    }
    Object.defineProperties(b.prototype, {
      name: {
        value: p.name,
        writable: !0,
        enumerable: !1,
        configurable: !0
      },
      toString: {
        value() {
          return `${this.name} [${u}]: ${this.message}`;
        },
        writable: !0,
        enumerable: !1,
        configurable: !0
      }
    }), b.prototype.code = u, b.prototype[a] = !0, s[u] = b;
  }
  function w(u) {
    const g = l + u.name;
    return Object.defineProperty(u, "name", {
      value: g
    }), u;
  }
  function I(u, g) {
    if (u && g && u !== g) {
      if (Array.isArray(g.errors))
        return g.errors.push(u), g;
      const p = new o([g, u], g.message);
      return p.code = g.code, p;
    }
    return u || g;
  }
  class A extends Error {
    constructor(g = "The operation was aborted", p = void 0) {
      if (p !== void 0 && typeof p != "object")
        throw new s.ERR_INVALID_ARG_TYPE("options", "Object", p);
      super(g, p), this.code = "ABORT_ERR", this.name = "AbortError";
    }
  }
  return R("ERR_ASSERTION", "%s", Error), R(
    "ERR_INVALID_ARG_TYPE",
    (u, g, p) => {
      f(typeof u == "string", "'name' must be a string"), Array.isArray(g) || (g = [g]);
      let b = "The ";
      u.endsWith(" argument") ? b += `${u} ` : b += `"${u}" ${u.includes(".") ? "property" : "argument"} `, b += "must be ";
      const C = [], N = [], W = [];
      for (const L of g)
        f(typeof L == "string", "All expected entries have to be of type string"), n.includes(L) ? C.push(L.toLowerCase()) : i.test(L) ? N.push(L) : (f(L !== "object", 'The value "object" should be written as "Object"'), W.push(L));
      if (N.length > 0) {
        const L = C.indexOf("object");
        L !== -1 && (C.splice(C, L, 1), N.push("Object"));
      }
      if (C.length > 0) {
        switch (C.length) {
          case 1:
            b += `of type ${C[0]}`;
            break;
          case 2:
            b += `one of type ${C[0]} or ${C[1]}`;
            break;
          default: {
            const L = C.pop();
            b += `one of type ${C.join(", ")}, or ${L}`;
          }
        }
        (N.length > 0 || W.length > 0) && (b += " or ");
      }
      if (N.length > 0) {
        switch (N.length) {
          case 1:
            b += `an instance of ${N[0]}`;
            break;
          case 2:
            b += `an instance of ${N[0]} or ${N[1]}`;
            break;
          default: {
            const L = N.pop();
            b += `an instance of ${N.join(", ")}, or ${L}`;
          }
        }
        W.length > 0 && (b += " or ");
      }
      switch (W.length) {
        case 0:
          break;
        case 1:
          W[0].toLowerCase() !== W[0] && (b += "an "), b += `${W[0]}`;
          break;
        case 2:
          b += `one of ${W[0]} or ${W[1]}`;
          break;
        default: {
          const L = W.pop();
          b += `one of ${W.join(", ")}, or ${L}`;
        }
      }
      if (p == null)
        b += `. Received ${p}`;
      else if (typeof p == "function" && p.name)
        b += `. Received function ${p.name}`;
      else if (typeof p == "object") {
        var k;
        if ((k = p.constructor) !== null && k !== void 0 && k.name)
          b += `. Received an instance of ${p.constructor.name}`;
        else {
          const L = e(p, {
            depth: -1
          });
          b += `. Received ${L}`;
        }
      } else {
        let L = e(p, {
          colors: !1
        });
        L.length > 25 && (L = `${L.slice(0, 25)}...`), b += `. Received type ${typeof p} (${L})`;
      }
      return b;
    },
    TypeError
  ), R(
    "ERR_INVALID_ARG_VALUE",
    (u, g, p = "is invalid") => {
      let b = e(g);
      return b.length > 128 && (b = b.slice(0, 128) + "..."), `The ${u.includes(".") ? "property" : "argument"} '${u}' ${p}. Received ${b}`;
    },
    TypeError
  ), R(
    "ERR_INVALID_RETURN_VALUE",
    (u, g, p) => {
      var b;
      const C = p != null && (b = p.constructor) !== null && b !== void 0 && b.name ? `instance of ${p.constructor.name}` : `type ${typeof p}`;
      return `Expected ${u} to be returned from the "${g}" function but got ${C}.`;
    },
    TypeError
  ), R(
    "ERR_MISSING_ARGS",
    (...u) => {
      f(u.length > 0, "At least one arg needs to be specified");
      let g;
      const p = u.length;
      switch (u = (Array.isArray(u) ? u : [u]).map((b) => `"${b}"`).join(" or "), p) {
        case 1:
          g += `The ${u[0]} argument`;
          break;
        case 2:
          g += `The ${u[0]} and ${u[1]} arguments`;
          break;
        default:
          {
            const b = u.pop();
            g += `The ${u.join(", ")}, and ${b} arguments`;
          }
          break;
      }
      return `${g} must be specified`;
    },
    TypeError
  ), R(
    "ERR_OUT_OF_RANGE",
    (u, g, p) => {
      f(g, 'Missing "range" argument');
      let b;
      if (Number.isInteger(p) && Math.abs(p) > 2 ** 32)
        b = d(String(p));
      else if (typeof p == "bigint") {
        b = String(p);
        const C = BigInt(2) ** BigInt(32);
        (p > C || p < -C) && (b = d(b)), b += "n";
      } else
        b = e(p);
      return `The value of "${u}" is out of range. It must be ${g}. Received ${b}`;
    },
    RangeError
  ), R("ERR_MULTIPLE_CALLBACK", "Callback called multiple times", Error), R("ERR_METHOD_NOT_IMPLEMENTED", "The %s method is not implemented", Error), R("ERR_STREAM_ALREADY_FINISHED", "Cannot call %s after a stream was finished", Error), R("ERR_STREAM_CANNOT_PIPE", "Cannot pipe, not readable", Error), R("ERR_STREAM_DESTROYED", "Cannot call %s after a stream was destroyed", Error), R("ERR_STREAM_NULL_VALUES", "May not write null values to stream", TypeError), R("ERR_STREAM_PREMATURE_CLOSE", "Premature close", Error), R("ERR_STREAM_PUSH_AFTER_EOF", "stream.push() after EOF", Error), R("ERR_STREAM_UNSHIFT_AFTER_END_EVENT", "stream.unshift() after end event", Error), R("ERR_STREAM_WRITE_AFTER_END", "write after end", Error), R("ERR_UNKNOWN_ENCODING", "Unknown encoding: %s", TypeError), ou = {
    AbortError: A,
    aggregateTwoErrors: w(I),
    hideStackFrames: w,
    codes: s
  }, ou;
}
var jy;
function gt() {
  return jy || (jy = 1, (function(t) {
    const e = Fe, { format: r, inspect: o } = Xm(), {
      codes: { ERR_INVALID_ARG_TYPE: a }
    } = ft(), { kResistStopPropagation: n, AggregateError: i, SymbolDispose: l } = Ve(), s = globalThis.AbortSignal || et.AbortSignal, f = globalThis.AbortController || et.AbortController, d = Object.getPrototypeOf(async function() {
    }).constructor, y = globalThis.Blob || e.Blob, R = typeof y < "u" ? function(u) {
      return u instanceof y;
    } : function(u) {
      return !1;
    }, w = (A, u) => {
      if (A !== void 0 && (A === null || typeof A != "object" || !("aborted" in A)))
        throw new a(u, "AbortSignal", A);
    }, I = (A, u) => {
      if (typeof A != "function")
        throw new a(u, "Function", A);
    };
    t.exports = {
      AggregateError: i,
      kEmptyObject: Object.freeze({}),
      once(A) {
        let u = !1;
        return function(...g) {
          u || (u = !0, A.apply(this, g));
        };
      },
      createDeferredPromise: function() {
        let A, u;
        return {
          promise: new Promise((p, b) => {
            A = p, u = b;
          }),
          resolve: A,
          reject: u
        };
      },
      promisify(A) {
        return new Promise((u, g) => {
          A((p, ...b) => p ? g(p) : u(...b));
        });
      },
      debuglog() {
        return function() {
        };
      },
      format: r,
      inspect: o,
      types: {
        isAsyncFunction(A) {
          return A instanceof d;
        },
        isArrayBufferView(A) {
          return ArrayBuffer.isView(A);
        }
      },
      isBlob: R,
      deprecate(A, u) {
        return A;
      },
      addAbortListener: Ke.addAbortListener || function(u, g) {
        if (u === void 0)
          throw new a("signal", "AbortSignal", u);
        w(u, "signal"), I(g, "listener");
        let p;
        return u.aborted ? queueMicrotask(() => g()) : (u.addEventListener("abort", g, {
          __proto__: null,
          once: !0,
          [n]: !0
        }), p = () => {
          u.removeEventListener("abort", g);
        }), {
          __proto__: null,
          [l]() {
            var b;
            (b = p) === null || b === void 0 || b();
          }
        };
      },
      AbortSignalAny: s.any || function(u) {
        if (u.length === 1)
          return u[0];
        const g = new f(), p = () => g.abort();
        return u.forEach((b) => {
          w(b, "signals"), b.addEventListener("abort", p, {
            once: !0
          });
        }), g.signal.addEventListener(
          "abort",
          () => {
            u.forEach((b) => b.removeEventListener("abort", p));
          },
          {
            once: !0
          }
        ), g.signal;
      }
    }, t.exports.promisify.custom = /* @__PURE__ */ Symbol.for("nodejs.util.promisify.custom");
  })(iu)), iu.exports;
}
var Vn = {}, au, $y;
function xn() {
  if ($y) return au;
  $y = 1;
  const {
    ArrayIsArray: t,
    ArrayPrototypeIncludes: e,
    ArrayPrototypeJoin: r,
    ArrayPrototypeMap: o,
    NumberIsInteger: a,
    NumberIsNaN: n,
    NumberMAX_SAFE_INTEGER: i,
    NumberMIN_SAFE_INTEGER: l,
    NumberParseInt: s,
    ObjectPrototypeHasOwnProperty: f,
    RegExpPrototypeExec: d,
    String: y,
    StringPrototypeToUpperCase: R,
    StringPrototypeTrim: w
  } = Ve(), {
    hideStackFrames: I,
    codes: { ERR_SOCKET_BAD_PORT: A, ERR_INVALID_ARG_TYPE: u, ERR_INVALID_ARG_VALUE: g, ERR_OUT_OF_RANGE: p, ERR_UNKNOWN_SIGNAL: b }
  } = ft(), { normalizeEncoding: C } = gt(), { isAsyncFunction: N, isArrayBufferView: W } = gt().types, k = {};
  function L(O) {
    return O === (O | 0);
  }
  function U(O) {
    return O === O >>> 0;
  }
  const x = /^[0-7]+$/, D = "must be a 32-bit unsigned integer or an octal string";
  function F(O, H, te) {
    if (typeof O > "u" && (O = te), typeof O == "string") {
      if (d(x, O) === null)
        throw new g(H, O, D);
      O = s(O, 8);
    }
    return re(O, H), O;
  }
  const T = I((O, H, te = l, z = i) => {
    if (typeof O != "number") throw new u(H, "number", O);
    if (!a(O)) throw new p(H, "an integer", O);
    if (O < te || O > z) throw new p(H, `>= ${te} && <= ${z}`, O);
  }), J = I((O, H, te = -2147483648, z = 2147483647) => {
    if (typeof O != "number")
      throw new u(H, "number", O);
    if (!a(O))
      throw new p(H, "an integer", O);
    if (O < te || O > z)
      throw new p(H, `>= ${te} && <= ${z}`, O);
  }), re = I((O, H, te = !1) => {
    if (typeof O != "number")
      throw new u(H, "number", O);
    if (!a(O))
      throw new p(H, "an integer", O);
    const z = te ? 1 : 0, de = 4294967295;
    if (O < z || O > de)
      throw new p(H, `>= ${z} && <= ${de}`, O);
  });
  function se(O, H) {
    if (typeof O != "string") throw new u(H, "string", O);
  }
  function ne(O, H, te = void 0, z) {
    if (typeof O != "number") throw new u(H, "number", O);
    if (te != null && O < te || z != null && O > z || (te != null || z != null) && n(O))
      throw new p(
        H,
        `${te != null ? `>= ${te}` : ""}${te != null && z != null ? " && " : ""}${z != null ? `<= ${z}` : ""}`,
        O
      );
  }
  const M = I((O, H, te) => {
    if (!e(te, O)) {
      const de = "must be one of: " + r(
        o(te, (pe) => typeof pe == "string" ? `'${pe}'` : y(pe)),
        ", "
      );
      throw new g(H, O, de);
    }
  });
  function Q(O, H) {
    if (typeof O != "boolean") throw new u(H, "boolean", O);
  }
  function v(O, H, te) {
    return O == null || !f(O, H) ? te : O[H];
  }
  const $ = I((O, H, te = null) => {
    const z = v(te, "allowArray", !1), de = v(te, "allowFunction", !1);
    if (!v(te, "nullable", !1) && O === null || !z && t(O) || typeof O != "object" && (!de || typeof O != "function"))
      throw new u(H, "Object", O);
  }), ee = I((O, H) => {
    if (O != null && typeof O != "object" && typeof O != "function")
      throw new u(H, "a dictionary", O);
  }), B = I((O, H, te = 0) => {
    if (!t(O))
      throw new u(H, "Array", O);
    if (O.length < te) {
      const z = `must be longer than ${te}`;
      throw new g(H, O, z);
    }
  });
  function le(O, H) {
    B(O, H);
    for (let te = 0; te < O.length; te++)
      se(O[te], `${H}[${te}]`);
  }
  function ae(O, H) {
    B(O, H);
    for (let te = 0; te < O.length; te++)
      Q(O[te], `${H}[${te}]`);
  }
  function ce(O, H) {
    B(O, H);
    for (let te = 0; te < O.length; te++) {
      const z = O[te], de = `${H}[${te}]`;
      if (z == null)
        throw new u(de, "AbortSignal", z);
      Z(z, de);
    }
  }
  function Y(O, H = "signal") {
    if (se(O, H), k[O] === void 0)
      throw k[R(O)] !== void 0 ? new b(O + " (signals must use all capital letters)") : new b(O);
  }
  const P = I((O, H = "buffer") => {
    if (!W(O))
      throw new u(H, ["Buffer", "TypedArray", "DataView"], O);
  });
  function q(O, H) {
    const te = C(H), z = O.length;
    if (te === "hex" && z % 2 !== 0)
      throw new g("encoding", H, `is invalid for data of length ${z}`);
  }
  function V(O, H = "Port", te = !0) {
    if (typeof O != "number" && typeof O != "string" || typeof O == "string" && w(O).length === 0 || +O !== +O >>> 0 || O > 65535 || O === 0 && !te)
      throw new A(H, O, te);
    return O | 0;
  }
  const Z = I((O, H) => {
    if (O !== void 0 && (O === null || typeof O != "object" || !("aborted" in O)))
      throw new u(H, "AbortSignal", O);
  }), X = I((O, H) => {
    if (typeof O != "function") throw new u(H, "Function", O);
  }), m = I((O, H) => {
    if (typeof O != "function" || N(O)) throw new u(H, "Function", O);
  }), c = I((O, H) => {
    if (O !== void 0) throw new u(H, "undefined", O);
  });
  function E(O, H, te) {
    if (!e(te, O))
      throw new u(H, `('${r(te, "|")}')`, O);
  }
  const j = /^(?:<[^>]*>)(?:\s*;\s*[^;"\s]+(?:=(")?[^;"\s]*\1)?)*$/;
  function G(O, H) {
    if (typeof O > "u" || !d(j, O))
      throw new g(
        H,
        O,
        'must be an array or string of format "</styles.css>; rel=preload; as=style"'
      );
  }
  function K(O) {
    if (typeof O == "string")
      return G(O, "hints"), O;
    if (t(O)) {
      const H = O.length;
      let te = "";
      if (H === 0)
        return te;
      for (let z = 0; z < H; z++) {
        const de = O[z];
        G(de, "hints"), te += de, z !== H - 1 && (te += ", ");
      }
      return te;
    }
    throw new g(
      "hints",
      O,
      'must be an array or string of format "</styles.css>; rel=preload; as=style"'
    );
  }
  return au = {
    isInt32: L,
    isUint32: U,
    parseFileMode: F,
    validateArray: B,
    validateStringArray: le,
    validateBooleanArray: ae,
    validateAbortSignalArray: ce,
    validateBoolean: Q,
    validateBuffer: P,
    validateDictionary: ee,
    validateEncoding: q,
    validateFunction: X,
    validateInt32: J,
    validateInteger: T,
    validateNumber: ne,
    validateObject: $,
    validateOneOf: M,
    validatePlainFunction: m,
    validatePort: V,
    validateSignalName: Y,
    validateString: se,
    validateUint32: re,
    validateUndefined: c,
    validateUnion: E,
    validateAbortSignal: Z,
    validateLinkHeaderValue: K
  }, au;
}
var Zn = { exports: {} }, lu, Wy;
function ir() {
  if (Wy) return lu;
  Wy = 1;
  const { SymbolAsyncIterator: t, SymbolIterator: e, SymbolFor: r } = Ve(), o = r("nodejs.stream.destroyed"), a = r("nodejs.stream.errored"), n = r("nodejs.stream.readable"), i = r("nodejs.stream.writable"), l = r("nodejs.stream.disturbed"), s = r("nodejs.webstream.isClosedPromise"), f = r("nodejs.webstream.controllerErrorFunction");
  function d(v, $ = !1) {
    var ee;
    return !!(v && typeof v.pipe == "function" && typeof v.on == "function" && (!$ || typeof v.pause == "function" && typeof v.resume == "function") && (!v._writableState || ((ee = v._readableState) === null || ee === void 0 ? void 0 : ee.readable) !== !1) && // Duplex
    (!v._writableState || v._readableState));
  }
  function y(v) {
    var $;
    return !!(v && typeof v.write == "function" && typeof v.on == "function" && (!v._readableState || (($ = v._writableState) === null || $ === void 0 ? void 0 : $.writable) !== !1));
  }
  function R(v) {
    return !!(v && typeof v.pipe == "function" && v._readableState && typeof v.on == "function" && typeof v.write == "function");
  }
  function w(v) {
    return v && (v._readableState || v._writableState || typeof v.write == "function" && typeof v.on == "function" || typeof v.pipe == "function" && typeof v.on == "function");
  }
  function I(v) {
    return !!(v && !w(v) && typeof v.pipeThrough == "function" && typeof v.getReader == "function" && typeof v.cancel == "function");
  }
  function A(v) {
    return !!(v && !w(v) && typeof v.getWriter == "function" && typeof v.abort == "function");
  }
  function u(v) {
    return !!(v && !w(v) && typeof v.readable == "object" && typeof v.writable == "object");
  }
  function g(v) {
    return I(v) || A(v) || u(v);
  }
  function p(v, $) {
    return v == null ? !1 : $ === !0 ? typeof v[t] == "function" : $ === !1 ? typeof v[e] == "function" : typeof v[t] == "function" || typeof v[e] == "function";
  }
  function b(v) {
    if (!w(v)) return null;
    const $ = v._writableState, ee = v._readableState, B = $ || ee;
    return !!(v.destroyed || v[o] || B != null && B.destroyed);
  }
  function C(v) {
    if (!y(v)) return null;
    if (v.writableEnded === !0) return !0;
    const $ = v._writableState;
    return $ != null && $.errored ? !1 : typeof $?.ended != "boolean" ? null : $.ended;
  }
  function N(v, $) {
    if (!y(v)) return null;
    if (v.writableFinished === !0) return !0;
    const ee = v._writableState;
    return ee != null && ee.errored ? !1 : typeof ee?.finished != "boolean" ? null : !!(ee.finished || $ === !1 && ee.ended === !0 && ee.length === 0);
  }
  function W(v) {
    if (!d(v)) return null;
    if (v.readableEnded === !0) return !0;
    const $ = v._readableState;
    return !$ || $.errored ? !1 : typeof $?.ended != "boolean" ? null : $.ended;
  }
  function k(v, $) {
    if (!d(v)) return null;
    const ee = v._readableState;
    return ee != null && ee.errored ? !1 : typeof ee?.endEmitted != "boolean" ? null : !!(ee.endEmitted || $ === !1 && ee.ended === !0 && ee.length === 0);
  }
  function L(v) {
    return v && v[n] != null ? v[n] : typeof v?.readable != "boolean" ? null : b(v) ? !1 : d(v) && v.readable && !k(v);
  }
  function U(v) {
    return v && v[i] != null ? v[i] : typeof v?.writable != "boolean" ? null : b(v) ? !1 : y(v) && v.writable && !C(v);
  }
  function x(v, $) {
    return w(v) ? b(v) ? !0 : !($?.readable !== !1 && L(v) || $?.writable !== !1 && U(v)) : null;
  }
  function D(v) {
    var $, ee;
    return w(v) ? v.writableErrored ? v.writableErrored : ($ = (ee = v._writableState) === null || ee === void 0 ? void 0 : ee.errored) !== null && $ !== void 0 ? $ : null : null;
  }
  function F(v) {
    var $, ee;
    return w(v) ? v.readableErrored ? v.readableErrored : ($ = (ee = v._readableState) === null || ee === void 0 ? void 0 : ee.errored) !== null && $ !== void 0 ? $ : null : null;
  }
  function T(v) {
    if (!w(v))
      return null;
    if (typeof v.closed == "boolean")
      return v.closed;
    const $ = v._writableState, ee = v._readableState;
    return typeof $?.closed == "boolean" || typeof ee?.closed == "boolean" ? $?.closed || ee?.closed : typeof v._closed == "boolean" && J(v) ? v._closed : null;
  }
  function J(v) {
    return typeof v._closed == "boolean" && typeof v._defaultKeepAlive == "boolean" && typeof v._removedConnection == "boolean" && typeof v._removedContLen == "boolean";
  }
  function re(v) {
    return typeof v._sent100 == "boolean" && J(v);
  }
  function se(v) {
    var $;
    return typeof v._consuming == "boolean" && typeof v._dumped == "boolean" && (($ = v.req) === null || $ === void 0 ? void 0 : $.upgradeOrConnect) === void 0;
  }
  function ne(v) {
    if (!w(v)) return null;
    const $ = v._writableState, ee = v._readableState, B = $ || ee;
    return !B && re(v) || !!(B && B.autoDestroy && B.emitClose && B.closed === !1);
  }
  function M(v) {
    var $;
    return !!(v && (($ = v[l]) !== null && $ !== void 0 ? $ : v.readableDidRead || v.readableAborted));
  }
  function Q(v) {
    var $, ee, B, le, ae, ce, Y, P, q, V;
    return !!(v && (($ = (ee = (B = (le = (ae = (ce = v[a]) !== null && ce !== void 0 ? ce : v.readableErrored) !== null && ae !== void 0 ? ae : v.writableErrored) !== null && le !== void 0 ? le : (Y = v._readableState) === null || Y === void 0 ? void 0 : Y.errorEmitted) !== null && B !== void 0 ? B : (P = v._writableState) === null || P === void 0 ? void 0 : P.errorEmitted) !== null && ee !== void 0 ? ee : (q = v._readableState) === null || q === void 0 ? void 0 : q.errored) !== null && $ !== void 0 ? $ : !((V = v._writableState) === null || V === void 0) && V.errored));
  }
  return lu = {
    isDestroyed: b,
    kIsDestroyed: o,
    isDisturbed: M,
    kIsDisturbed: l,
    isErrored: Q,
    kIsErrored: a,
    isReadable: L,
    kIsReadable: n,
    kIsClosedPromise: s,
    kControllerErrorFunction: f,
    kIsWritable: i,
    isClosed: T,
    isDuplexNodeStream: R,
    isFinished: x,
    isIterable: p,
    isReadableNodeStream: d,
    isReadableStream: I,
    isReadableEnded: W,
    isReadableFinished: k,
    isReadableErrored: F,
    isNodeStream: w,
    isWebStream: g,
    isWritable: U,
    isWritableNodeStream: y,
    isWritableStream: A,
    isWritableEnded: C,
    isWritableFinished: N,
    isWritableErrored: D,
    isServerRequest: se,
    isServerResponse: re,
    willEmitClose: ne,
    isTransformStream: u
  }, lu;
}
var qy;
function gr() {
  if (qy) return Zn.exports;
  qy = 1;
  const t = je(), { AbortError: e, codes: r } = ft(), { ERR_INVALID_ARG_TYPE: o, ERR_STREAM_PREMATURE_CLOSE: a } = r, { kEmptyObject: n, once: i } = gt(), { validateAbortSignal: l, validateFunction: s, validateObject: f, validateBoolean: d } = xn(), { Promise: y, PromisePrototypeThen: R, SymbolDispose: w } = Ve(), {
    isClosed: I,
    isReadable: A,
    isReadableNodeStream: u,
    isReadableStream: g,
    isReadableFinished: p,
    isReadableErrored: b,
    isWritable: C,
    isWritableNodeStream: N,
    isWritableStream: W,
    isWritableFinished: k,
    isWritableErrored: L,
    isNodeStream: U,
    willEmitClose: x,
    kIsClosedPromise: D
  } = ir();
  let F;
  function T(M) {
    return M.setHeader && typeof M.abort == "function";
  }
  const J = () => {
  };
  function re(M, Q, v) {
    var $, ee;
    if (arguments.length === 2 ? (v = Q, Q = n) : Q == null ? Q = n : f(Q, "options"), s(v, "callback"), l(Q.signal, "options.signal"), v = i(v), g(M) || W(M))
      return se(M, Q, v);
    if (!U(M))
      throw new o("stream", ["ReadableStream", "WritableStream", "Stream"], M);
    const B = ($ = Q.readable) !== null && $ !== void 0 ? $ : u(M), le = (ee = Q.writable) !== null && ee !== void 0 ? ee : N(M), ae = M._writableState, ce = M._readableState, Y = () => {
      M.writable || V();
    };
    let P = x(M) && u(M) === B && N(M) === le, q = k(M, !1);
    const V = () => {
      q = !0, M.destroyed && (P = !1), !(P && (!M.readable || B)) && (!B || Z) && v.call(M);
    };
    let Z = p(M, !1);
    const X = () => {
      Z = !0, M.destroyed && (P = !1), !(P && (!M.writable || le)) && (!le || q) && v.call(M);
    }, m = (O) => {
      v.call(M, O);
    };
    let c = I(M);
    const E = () => {
      c = !0;
      const O = L(M) || b(M);
      if (O && typeof O != "boolean")
        return v.call(M, O);
      if (B && !Z && u(M, !0) && !p(M, !1))
        return v.call(M, new a());
      if (le && !q && !k(M, !1))
        return v.call(M, new a());
      v.call(M);
    }, j = () => {
      c = !0;
      const O = L(M) || b(M);
      if (O && typeof O != "boolean")
        return v.call(M, O);
      v.call(M);
    }, G = () => {
      M.req.on("finish", V);
    };
    T(M) ? (M.on("complete", V), P || M.on("abort", E), M.req ? G() : M.on("request", G)) : le && !ae && (M.on("end", Y), M.on("close", Y)), !P && typeof M.aborted == "boolean" && M.on("aborted", E), M.on("end", X), M.on("finish", V), Q.error !== !1 && M.on("error", m), M.on("close", E), c ? t.nextTick(E) : ae != null && ae.errorEmitted || ce != null && ce.errorEmitted ? P || t.nextTick(j) : (!B && (!P || A(M)) && (q || C(M) === !1) || !le && (!P || C(M)) && (Z || A(M) === !1) || ce && M.req && M.aborted) && t.nextTick(j);
    const K = () => {
      v = J, M.removeListener("aborted", E), M.removeListener("complete", V), M.removeListener("abort", E), M.removeListener("request", G), M.req && M.req.removeListener("finish", V), M.removeListener("end", Y), M.removeListener("close", Y), M.removeListener("finish", V), M.removeListener("end", X), M.removeListener("error", m), M.removeListener("close", E);
    };
    if (Q.signal && !c) {
      const O = () => {
        const H = v;
        K(), H.call(
          M,
          new e(void 0, {
            cause: Q.signal.reason
          })
        );
      };
      if (Q.signal.aborted)
        t.nextTick(O);
      else {
        F = F || gt().addAbortListener;
        const H = F(Q.signal, O), te = v;
        v = i((...z) => {
          H[w](), te.apply(M, z);
        });
      }
    }
    return K;
  }
  function se(M, Q, v) {
    let $ = !1, ee = J;
    if (Q.signal)
      if (ee = () => {
        $ = !0, v.call(
          M,
          new e(void 0, {
            cause: Q.signal.reason
          })
        );
      }, Q.signal.aborted)
        t.nextTick(ee);
      else {
        F = F || gt().addAbortListener;
        const le = F(Q.signal, ee), ae = v;
        v = i((...ce) => {
          le[w](), ae.apply(M, ce);
        });
      }
    const B = (...le) => {
      $ || t.nextTick(() => v.apply(M, le));
    };
    return R(M[D].promise, B, B), J;
  }
  function ne(M, Q) {
    var v;
    let $ = !1;
    return Q === null && (Q = n), (v = Q) !== null && v !== void 0 && v.cleanup && (d(Q.cleanup, "cleanup"), $ = Q.cleanup), new y((ee, B) => {
      const le = re(M, Q, (ae) => {
        $ && le(), ae ? B(ae) : ee();
      });
    });
  }
  return Zn.exports = re, Zn.exports.finished = ne, Zn.exports;
}
var uu, By;
function Wr() {
  if (By) return uu;
  By = 1;
  const t = je(), {
    aggregateTwoErrors: e,
    codes: { ERR_MULTIPLE_CALLBACK: r },
    AbortError: o
  } = ft(), { Symbol: a } = Ve(), { kIsDestroyed: n, isDestroyed: i, isFinished: l, isServerRequest: s } = ir(), f = a("kDestroy"), d = a("kConstruct");
  function y(x, D, F) {
    x && (x.stack, D && !D.errored && (D.errored = x), F && !F.errored && (F.errored = x));
  }
  function R(x, D) {
    const F = this._readableState, T = this._writableState, J = T || F;
    return T != null && T.destroyed || F != null && F.destroyed ? (typeof D == "function" && D(), this) : (y(x, T, F), T && (T.destroyed = !0), F && (F.destroyed = !0), J.constructed ? w(this, x, D) : this.once(f, function(re) {
      w(this, e(re, x), D);
    }), this);
  }
  function w(x, D, F) {
    let T = !1;
    function J(re) {
      if (T)
        return;
      T = !0;
      const se = x._readableState, ne = x._writableState;
      y(re, ne, se), ne && (ne.closed = !0), se && (se.closed = !0), typeof F == "function" && F(re), re ? t.nextTick(I, x, re) : t.nextTick(A, x);
    }
    try {
      x._destroy(D || null, J);
    } catch (re) {
      J(re);
    }
  }
  function I(x, D) {
    u(x, D), A(x);
  }
  function A(x) {
    const D = x._readableState, F = x._writableState;
    F && (F.closeEmitted = !0), D && (D.closeEmitted = !0), (F != null && F.emitClose || D != null && D.emitClose) && x.emit("close");
  }
  function u(x, D) {
    const F = x._readableState, T = x._writableState;
    T != null && T.errorEmitted || F != null && F.errorEmitted || (T && (T.errorEmitted = !0), F && (F.errorEmitted = !0), x.emit("error", D));
  }
  function g() {
    const x = this._readableState, D = this._writableState;
    x && (x.constructed = !0, x.closed = !1, x.closeEmitted = !1, x.destroyed = !1, x.errored = null, x.errorEmitted = !1, x.reading = !1, x.ended = x.readable === !1, x.endEmitted = x.readable === !1), D && (D.constructed = !0, D.destroyed = !1, D.closed = !1, D.closeEmitted = !1, D.errored = null, D.errorEmitted = !1, D.finalCalled = !1, D.prefinished = !1, D.ended = D.writable === !1, D.ending = D.writable === !1, D.finished = D.writable === !1);
  }
  function p(x, D, F) {
    const T = x._readableState, J = x._writableState;
    if (J != null && J.destroyed || T != null && T.destroyed)
      return this;
    T != null && T.autoDestroy || J != null && J.autoDestroy ? x.destroy(D) : D && (D.stack, J && !J.errored && (J.errored = D), T && !T.errored && (T.errored = D), F ? t.nextTick(u, x, D) : u(x, D));
  }
  function b(x, D) {
    if (typeof x._construct != "function")
      return;
    const F = x._readableState, T = x._writableState;
    F && (F.constructed = !1), T && (T.constructed = !1), x.once(d, D), !(x.listenerCount(d) > 1) && t.nextTick(C, x);
  }
  function C(x) {
    let D = !1;
    function F(T) {
      if (D) {
        p(x, T ?? new r());
        return;
      }
      D = !0;
      const J = x._readableState, re = x._writableState, se = re || J;
      J && (J.constructed = !0), re && (re.constructed = !0), se.destroyed ? x.emit(f, T) : T ? p(x, T, !0) : t.nextTick(N, x);
    }
    try {
      x._construct((T) => {
        t.nextTick(F, T);
      });
    } catch (T) {
      t.nextTick(F, T);
    }
  }
  function N(x) {
    x.emit(d);
  }
  function W(x) {
    return x?.setHeader && typeof x.abort == "function";
  }
  function k(x) {
    x.emit("close");
  }
  function L(x, D) {
    x.emit("error", D), t.nextTick(k, x);
  }
  function U(x, D) {
    !x || i(x) || (!D && !l(x) && (D = new o()), s(x) ? (x.socket = null, x.destroy(D)) : W(x) ? x.abort() : W(x.req) ? x.req.abort() : typeof x.destroy == "function" ? x.destroy(D) : typeof x.close == "function" ? x.close() : D ? t.nextTick(L, x, D) : t.nextTick(k, x), x.destroyed || (x[n] = !0));
  }
  return uu = {
    construct: b,
    destroyer: U,
    destroy: R,
    undestroy: g,
    errorOrDestroy: p
  }, uu;
}
var fu, Uy;
function Mc() {
  if (Uy) return fu;
  Uy = 1;
  const { ArrayIsArray: t, ObjectSetPrototypeOf: e } = Ve(), { EventEmitter: r } = Ke;
  function o(n) {
    r.call(this, n);
  }
  e(o.prototype, r.prototype), e(o, r), o.prototype.pipe = function(n, i) {
    const l = this;
    function s(A) {
      n.writable && n.write(A) === !1 && l.pause && l.pause();
    }
    l.on("data", s);
    function f() {
      l.readable && l.resume && l.resume();
    }
    n.on("drain", f), !n._isStdio && (!i || i.end !== !1) && (l.on("end", y), l.on("close", R));
    let d = !1;
    function y() {
      d || (d = !0, n.end());
    }
    function R() {
      d || (d = !0, typeof n.destroy == "function" && n.destroy());
    }
    function w(A) {
      I(), r.listenerCount(this, "error") === 0 && this.emit("error", A);
    }
    a(l, "error", w), a(n, "error", w);
    function I() {
      l.removeListener("data", s), n.removeListener("drain", f), l.removeListener("end", y), l.removeListener("close", R), l.removeListener("error", w), n.removeListener("error", w), l.removeListener("end", I), l.removeListener("close", I), n.removeListener("close", I);
    }
    return l.on("end", I), l.on("close", I), n.on("close", I), n.emit("pipe", l), n;
  };
  function a(n, i, l) {
    if (typeof n.prependListener == "function") return n.prependListener(i, l);
    !n._events || !n._events[i] ? n.on(i, l) : t(n._events[i]) ? n._events[i].unshift(l) : n._events[i] = [l, n._events[i]];
  }
  return fu = {
    Stream: o,
    prependListener: a
  }, fu;
}
var cu = { exports: {} }, Gy;
function rs() {
  return Gy || (Gy = 1, (function(t) {
    const { SymbolDispose: e } = Ve(), { AbortError: r, codes: o } = ft(), { isNodeStream: a, isWebStream: n, kControllerErrorFunction: i } = ir(), l = gr(), { ERR_INVALID_ARG_TYPE: s } = o;
    let f;
    const d = (y, R) => {
      if (typeof y != "object" || !("aborted" in y))
        throw new s(R, "AbortSignal", y);
    };
    t.exports.addAbortSignal = function(R, w) {
      if (d(R, "signal"), !a(w) && !n(w))
        throw new s("stream", ["ReadableStream", "WritableStream", "Stream"], w);
      return t.exports.addAbortSignalNoValidate(R, w);
    }, t.exports.addAbortSignalNoValidate = function(y, R) {
      if (typeof y != "object" || !("aborted" in y))
        return R;
      const w = a(R) ? () => {
        R.destroy(
          new r(void 0, {
            cause: y.reason
          })
        );
      } : () => {
        R[i](
          new r(void 0, {
            cause: y.reason
          })
        );
      };
      if (y.aborted)
        w();
      else {
        f = f || gt().addAbortListener;
        const I = f(y, w);
        l(R, I[e]);
      }
      return R;
    };
  })(cu)), cu.exports;
}
var du, Hy;
function NA() {
  if (Hy) return du;
  Hy = 1;
  const { StringPrototypeSlice: t, SymbolIterator: e, TypedArrayPrototypeSet: r, Uint8Array: o } = Ve(), { Buffer: a } = Fe, { inspect: n } = gt();
  return du = class {
    constructor() {
      this.head = null, this.tail = null, this.length = 0;
    }
    push(l) {
      const s = {
        data: l,
        next: null
      };
      this.length > 0 ? this.tail.next = s : this.head = s, this.tail = s, ++this.length;
    }
    unshift(l) {
      const s = {
        data: l,
        next: this.head
      };
      this.length === 0 && (this.tail = s), this.head = s, ++this.length;
    }
    shift() {
      if (this.length === 0) return;
      const l = this.head.data;
      return this.length === 1 ? this.head = this.tail = null : this.head = this.head.next, --this.length, l;
    }
    clear() {
      this.head = this.tail = null, this.length = 0;
    }
    join(l) {
      if (this.length === 0) return "";
      let s = this.head, f = "" + s.data;
      for (; (s = s.next) !== null; ) f += l + s.data;
      return f;
    }
    concat(l) {
      if (this.length === 0) return a.alloc(0);
      const s = a.allocUnsafe(l >>> 0);
      let f = this.head, d = 0;
      for (; f; )
        r(s, f.data, d), d += f.data.length, f = f.next;
      return s;
    }
    // Consumes a specified amount of bytes or characters from the buffered data.
    consume(l, s) {
      const f = this.head.data;
      if (l < f.length) {
        const d = f.slice(0, l);
        return this.head.data = f.slice(l), d;
      }
      return l === f.length ? this.shift() : s ? this._getString(l) : this._getBuffer(l);
    }
    first() {
      return this.head.data;
    }
    *[e]() {
      for (let l = this.head; l; l = l.next)
        yield l.data;
    }
    // Consumes a specified amount of characters from the buffered data.
    _getString(l) {
      let s = "", f = this.head, d = 0;
      do {
        const y = f.data;
        if (l > y.length)
          s += y, l -= y.length;
        else {
          l === y.length ? (s += y, ++d, f.next ? this.head = f.next : this.head = this.tail = null) : (s += t(y, 0, l), this.head = f, f.data = t(y, l));
          break;
        }
        ++d;
      } while ((f = f.next) !== null);
      return this.length -= d, s;
    }
    // Consumes a specified amount of bytes from the buffered data.
    _getBuffer(l) {
      const s = a.allocUnsafe(l), f = l;
      let d = this.head, y = 0;
      do {
        const R = d.data;
        if (l > R.length)
          r(s, R, f - l), l -= R.length;
        else {
          l === R.length ? (r(s, R, f - l), ++y, d.next ? this.head = d.next : this.head = this.tail = null) : (r(s, new o(R.buffer, R.byteOffset, l), f - l), this.head = d, d.data = R.slice(l));
          break;
        }
        ++y;
      } while ((d = d.next) !== null);
      return this.length -= y, s;
    }
    // Make sure the linked list only shows the minimal necessary information.
    [/* @__PURE__ */ Symbol.for("nodejs.util.inspect.custom")](l, s) {
      return n(this, {
        ...s,
        // Only inspect one level.
        depth: 0,
        // It should not recurse.
        customInspect: !1
      });
    }
  }, du;
}
var hu, zy;
function ns() {
  if (zy) return hu;
  zy = 1;
  const { MathFloor: t, NumberIsInteger: e } = Ve(), { validateInteger: r } = xn(), { ERR_INVALID_ARG_VALUE: o } = ft().codes;
  let a = 16 * 1024, n = 16;
  function i(d, y, R) {
    return d.highWaterMark != null ? d.highWaterMark : y ? d[R] : null;
  }
  function l(d) {
    return d ? n : a;
  }
  function s(d, y) {
    r(y, "value", 0), d ? n = y : a = y;
  }
  function f(d, y, R, w) {
    const I = i(y, w, R);
    if (I != null) {
      if (!e(I) || I < 0) {
        const A = w ? `options.${R}` : "options.highWaterMark";
        throw new o(A, I);
      }
      return t(I);
    }
    return l(d.objectMode);
  }
  return hu = {
    getHighWaterMark: f,
    getDefaultHighWaterMark: l,
    setDefaultHighWaterMark: s
  }, hu;
}
var pu, Vy;
function Qm() {
  if (Vy) return pu;
  Vy = 1;
  const t = je(), { PromisePrototypeThen: e, SymbolAsyncIterator: r, SymbolIterator: o } = Ve(), { Buffer: a } = Fe, { ERR_INVALID_ARG_TYPE: n, ERR_STREAM_NULL_VALUES: i } = ft().codes;
  function l(s, f, d) {
    let y;
    if (typeof f == "string" || f instanceof a)
      return new s({
        objectMode: !0,
        ...d,
        read() {
          this.push(f), this.push(null);
        }
      });
    let R;
    if (f && f[r])
      R = !0, y = f[r]();
    else if (f && f[o])
      R = !1, y = f[o]();
    else
      throw new n("iterable", ["Iterable"], f);
    const w = new s({
      objectMode: !0,
      highWaterMark: 1,
      // TODO(ronag): What options should be allowed?
      ...d
    });
    let I = !1;
    w._read = function() {
      I || (I = !0, u());
    }, w._destroy = function(g, p) {
      e(
        A(g),
        () => t.nextTick(p, g),
        // nextTick is here in case cb throws
        (b) => t.nextTick(p, b || g)
      );
    };
    async function A(g) {
      const p = g != null, b = typeof y.throw == "function";
      if (p && b) {
        const { value: C, done: N } = await y.throw(g);
        if (await C, N)
          return;
      }
      if (typeof y.return == "function") {
        const { value: C } = await y.return();
        await C;
      }
    }
    async function u() {
      for (; ; ) {
        try {
          const { value: g, done: p } = R ? await y.next() : y.next();
          if (p)
            w.push(null);
          else {
            const b = g && typeof g.then == "function" ? await g : g;
            if (b === null)
              throw I = !1, new i();
            if (w.push(b))
              continue;
            I = !1;
          }
        } catch (g) {
          w.destroy(g);
        }
        break;
      }
    }
    return w;
  }
  return pu = l, pu;
}
var gu, Zy;
function is() {
  if (Zy) return gu;
  Zy = 1;
  const t = je(), {
    ArrayPrototypeIndexOf: e,
    NumberIsInteger: r,
    NumberIsNaN: o,
    NumberParseInt: a,
    ObjectDefineProperties: n,
    ObjectKeys: i,
    ObjectSetPrototypeOf: l,
    Promise: s,
    SafeSet: f,
    SymbolAsyncDispose: d,
    SymbolAsyncIterator: y,
    Symbol: R
  } = Ve();
  gu = z, z.ReadableState = te;
  const { EventEmitter: w } = Ke, { Stream: I, prependListener: A } = Mc(), { Buffer: u } = Fe, { addAbortSignal: g } = rs(), p = gr();
  let b = gt().debuglog("stream", (h) => {
    b = h;
  });
  const C = NA(), N = Wr(), { getHighWaterMark: W, getDefaultHighWaterMark: k } = ns(), {
    aggregateTwoErrors: L,
    codes: {
      ERR_INVALID_ARG_TYPE: U,
      ERR_METHOD_NOT_IMPLEMENTED: x,
      ERR_OUT_OF_RANGE: D,
      ERR_STREAM_PUSH_AFTER_EOF: F,
      ERR_STREAM_UNSHIFT_AFTER_END_EVENT: T
    },
    AbortError: J
  } = ft(), { validateObject: re } = xn(), se = R("kPaused"), { StringDecoder: ne } = Ji(), M = Qm();
  l(z.prototype, I.prototype), l(z, I);
  const Q = () => {
  }, { errorOrDestroy: v } = N, $ = 1, ee = 2, B = 4, le = 8, ae = 16, ce = 32, Y = 64, P = 128, q = 256, V = 512, Z = 1024, X = 2048, m = 4096, c = 8192, E = 16384, j = 32768, G = 65536, K = 1 << 17, O = 1 << 18;
  function H(h) {
    return {
      enumerable: !1,
      get() {
        return (this.state & h) !== 0;
      },
      set(S) {
        S ? this.state |= h : this.state &= ~h;
      }
    };
  }
  n(te.prototype, {
    objectMode: H($),
    ended: H(ee),
    endEmitted: H(B),
    reading: H(le),
    // Stream is still being constructed and cannot be
    // destroyed until construction finished or failed.
    // Async construction is opt in, therefore we start as
    // constructed.
    constructed: H(ae),
    // A flag to be able to tell if the event 'readable'/'data' is emitted
    // immediately, or on a later tick.  We set this to true at first, because
    // any actions that shouldn't happen until "later" should generally also
    // not happen before the first read call.
    sync: H(ce),
    // Whenever we return null, then we set a flag to say
    // that we're awaiting a 'readable' event emission.
    needReadable: H(Y),
    emittedReadable: H(P),
    readableListening: H(q),
    resumeScheduled: H(V),
    // True if the error was already emitted and should not be thrown again.
    errorEmitted: H(Z),
    emitClose: H(X),
    autoDestroy: H(m),
    // Has it been destroyed.
    destroyed: H(c),
    // Indicates whether the stream has finished destroying.
    closed: H(E),
    // True if close has been emitted or would have been emitted
    // depending on emitClose.
    closeEmitted: H(j),
    multiAwaitDrain: H(G),
    // If true, a maybeReadMore has been scheduled.
    readingMore: H(K),
    dataEmitted: H(O)
  });
  function te(h, S, ie) {
    typeof ie != "boolean" && (ie = S instanceof Jt()), this.state = X | m | ae | ce, h && h.objectMode && (this.state |= $), ie && h && h.readableObjectMode && (this.state |= $), this.highWaterMark = h ? W(this, h, "readableHighWaterMark", ie) : k(!1), this.buffer = new C(), this.length = 0, this.pipes = [], this.flowing = null, this[se] = null, h && h.emitClose === !1 && (this.state &= ~X), h && h.autoDestroy === !1 && (this.state &= ~m), this.errored = null, this.defaultEncoding = h && h.defaultEncoding || "utf8", this.awaitDrainWriters = null, this.decoder = null, this.encoding = null, h && h.encoding && (this.decoder = new ne(h.encoding), this.encoding = h.encoding);
  }
  function z(h) {
    if (!(this instanceof z)) return new z(h);
    const S = this instanceof Jt();
    this._readableState = new te(h, this, S), h && (typeof h.read == "function" && (this._read = h.read), typeof h.destroy == "function" && (this._destroy = h.destroy), typeof h.construct == "function" && (this._construct = h.construct), h.signal && !S && g(h.signal, this)), I.call(this, h), N.construct(this, () => {
      this._readableState.needReadable && me(this, this._readableState);
    });
  }
  z.prototype.destroy = N.destroy, z.prototype._undestroy = N.undestroy, z.prototype._destroy = function(h, S) {
    S(h);
  }, z.prototype[w.captureRejectionSymbol] = function(h) {
    this.destroy(h);
  }, z.prototype[d] = function() {
    let h;
    return this.destroyed || (h = this.readableEnded ? null : new J(), this.destroy(h)), new s((S, ie) => p(this, (oe) => oe && oe !== h ? ie(oe) : S(null)));
  }, z.prototype.push = function(h, S) {
    return de(this, h, S, !1);
  }, z.prototype.unshift = function(h, S) {
    return de(this, h, S, !0);
  };
  function de(h, S, ie, oe) {
    b("readableAddChunk", S);
    const fe = h._readableState;
    let ve;
    if ((fe.state & $) === 0 && (typeof S == "string" ? (ie = ie || fe.defaultEncoding, fe.encoding !== ie && (oe && fe.encoding ? S = u.from(S, ie).toString(fe.encoding) : (S = u.from(S, ie), ie = ""))) : S instanceof u ? ie = "" : I._isUint8Array(S) ? (S = I._uint8ArrayToBuffer(S), ie = "") : S != null && (ve = new U("chunk", ["string", "Buffer", "Uint8Array"], S))), ve)
      v(h, ve);
    else if (S === null)
      fe.state &= ~le, _e(h, fe);
    else if ((fe.state & $) !== 0 || S && S.length > 0)
      if (oe)
        if ((fe.state & B) !== 0) v(h, new T());
        else {
          if (fe.destroyed || fe.errored) return !1;
          pe(h, fe, S, !0);
        }
      else if (fe.ended)
        v(h, new F());
      else {
        if (fe.destroyed || fe.errored)
          return !1;
        fe.state &= ~le, fe.decoder && !ie ? (S = fe.decoder.write(S), fe.objectMode || S.length !== 0 ? pe(h, fe, S, !1) : me(h, fe)) : pe(h, fe, S, !1);
      }
    else oe || (fe.state &= ~le, me(h, fe));
    return !fe.ended && (fe.length < fe.highWaterMark || fe.length === 0);
  }
  function pe(h, S, ie, oe) {
    S.flowing && S.length === 0 && !S.sync && h.listenerCount("data") > 0 ? ((S.state & G) !== 0 ? S.awaitDrainWriters.clear() : S.awaitDrainWriters = null, S.dataEmitted = !0, h.emit("data", ie)) : (S.length += S.objectMode ? 1 : ie.length, oe ? S.buffer.unshift(ie) : S.buffer.push(ie), (S.state & Y) !== 0 && Ae(h)), me(h, S);
  }
  z.prototype.isPaused = function() {
    const h = this._readableState;
    return h[se] === !0 || h.flowing === !1;
  }, z.prototype.setEncoding = function(h) {
    const S = new ne(h);
    this._readableState.decoder = S, this._readableState.encoding = this._readableState.decoder.encoding;
    const ie = this._readableState.buffer;
    let oe = "";
    for (const fe of ie)
      oe += S.write(fe);
    return ie.clear(), oe !== "" && ie.push(oe), this._readableState.length = oe.length, this;
  };
  const he = 1073741824;
  function we(h) {
    if (h > he)
      throw new D("size", "<= 1GiB", h);
    return h--, h |= h >>> 1, h |= h >>> 2, h |= h >>> 4, h |= h >>> 8, h |= h >>> 16, h++, h;
  }
  function be(h, S) {
    return h <= 0 || S.length === 0 && S.ended ? 0 : (S.state & $) !== 0 ? 1 : o(h) ? S.flowing && S.length ? S.buffer.first().length : S.length : h <= S.length ? h : S.ended ? S.length : 0;
  }
  z.prototype.read = function(h) {
    b("read", h), h === void 0 ? h = NaN : r(h) || (h = a(h, 10));
    const S = this._readableState, ie = h;
    if (h > S.highWaterMark && (S.highWaterMark = we(h)), h !== 0 && (S.state &= ~P), h === 0 && S.needReadable && ((S.highWaterMark !== 0 ? S.length >= S.highWaterMark : S.length > 0) || S.ended))
      return b("read: emitReadable", S.length, S.ended), S.length === 0 && S.ended ? tt(this) : Ae(this), null;
    if (h = be(h, S), h === 0 && S.ended)
      return S.length === 0 && tt(this), null;
    let oe = (S.state & Y) !== 0;
    if (b("need readable", oe), (S.length === 0 || S.length - h < S.highWaterMark) && (oe = !0, b("length less than watermark", oe)), S.ended || S.reading || S.destroyed || S.errored || !S.constructed)
      oe = !1, b("reading, ended or constructing", oe);
    else if (oe) {
      b("do read"), S.state |= le | ce, S.length === 0 && (S.state |= Y);
      try {
        this._read(S.highWaterMark);
      } catch (ve) {
        v(this, ve);
      }
      S.state &= ~ce, S.reading || (h = be(ie, S));
    }
    let fe;
    return h > 0 ? fe = mt(h, S) : fe = null, fe === null ? (S.needReadable = S.length <= S.highWaterMark, h = 0) : (S.length -= h, S.multiAwaitDrain ? S.awaitDrainWriters.clear() : S.awaitDrainWriters = null), S.length === 0 && (S.ended || (S.needReadable = !0), ie !== h && S.ended && tt(this)), fe !== null && !S.errorEmitted && !S.closeEmitted && (S.dataEmitted = !0, this.emit("data", fe)), fe;
  };
  function _e(h, S) {
    if (b("onEofChunk"), !S.ended) {
      if (S.decoder) {
        const ie = S.decoder.end();
        ie && ie.length && (S.buffer.push(ie), S.length += S.objectMode ? 1 : ie.length);
      }
      S.ended = !0, S.sync ? Ae(h) : (S.needReadable = !1, S.emittedReadable = !0, xe(h));
    }
  }
  function Ae(h) {
    const S = h._readableState;
    b("emitReadable", S.needReadable, S.emittedReadable), S.needReadable = !1, S.emittedReadable || (b("emitReadable", S.flowing), S.emittedReadable = !0, t.nextTick(xe, h));
  }
  function xe(h) {
    const S = h._readableState;
    b("emitReadable_", S.destroyed, S.length, S.ended), !S.destroyed && !S.errored && (S.length || S.ended) && (h.emit("readable"), S.emittedReadable = !1), S.needReadable = !S.flowing && !S.ended && S.length <= S.highWaterMark, ht(h);
  }
  function me(h, S) {
    !S.readingMore && S.constructed && (S.readingMore = !0, t.nextTick(Ge, h, S));
  }
  function Ge(h, S) {
    for (; !S.reading && !S.ended && (S.length < S.highWaterMark || S.flowing && S.length === 0); ) {
      const ie = S.length;
      if (b("maybeReadMore read 0"), h.read(0), ie === S.length)
        break;
    }
    S.readingMore = !1;
  }
  z.prototype._read = function(h) {
    throw new x("_read()");
  }, z.prototype.pipe = function(h, S) {
    const ie = this, oe = this._readableState;
    oe.pipes.length === 1 && (oe.multiAwaitDrain || (oe.multiAwaitDrain = !0, oe.awaitDrainWriters = new f(oe.awaitDrainWriters ? [oe.awaitDrainWriters] : []))), oe.pipes.push(h), b("pipe count=%d opts=%j", oe.pipes.length, S);
    const ve = (!S || S.end !== !1) && h !== t.stdout && h !== t.stderr ? Et : $e;
    oe.endEmitted ? t.nextTick(ve) : ie.once("end", ve), h.on("unpipe", Te);
    function Te(Ce, De) {
      b("onunpipe"), Ce === ie && De && De.hasUnpiped === !1 && (De.hasUnpiped = !0, Mt());
    }
    function Et() {
      b("onend"), h.end();
    }
    let Pe, lt = !1;
    function Mt() {
      b("cleanup"), h.removeListener("close", it), h.removeListener("finish", st), Pe && h.removeListener("drain", Pe), h.removeListener("error", nt), h.removeListener("unpipe", Te), ie.removeListener("end", Et), ie.removeListener("end", $e), ie.removeListener("data", Rt), lt = !0, Pe && oe.awaitDrainWriters && (!h._writableState || h._writableState.needDrain) && Pe();
    }
    function vt() {
      lt || (oe.pipes.length === 1 && oe.pipes[0] === h ? (b("false write response, pause", 0), oe.awaitDrainWriters = h, oe.multiAwaitDrain = !1) : oe.pipes.length > 1 && oe.pipes.includes(h) && (b("false write response, pause", oe.awaitDrainWriters.size), oe.awaitDrainWriters.add(h)), ie.pause()), Pe || (Pe = Je(ie, h), h.on("drain", Pe));
    }
    ie.on("data", Rt);
    function Rt(Ce) {
      b("ondata");
      const De = h.write(Ce);
      b("dest.write", De), De === !1 && vt();
    }
    function nt(Ce) {
      if (b("onerror", Ce), $e(), h.removeListener("error", nt), h.listenerCount("error") === 0) {
        const De = h._writableState || h._readableState;
        De && !De.errorEmitted ? v(h, Ce) : h.emit("error", Ce);
      }
    }
    A(h, "error", nt);
    function it() {
      h.removeListener("finish", st), $e();
    }
    h.once("close", it);
    function st() {
      b("onfinish"), h.removeListener("close", it), $e();
    }
    h.once("finish", st);
    function $e() {
      b("unpipe"), ie.unpipe(h);
    }
    return h.emit("pipe", ie), h.writableNeedDrain === !0 ? vt() : oe.flowing || (b("pipe resume"), ie.resume()), h;
  };
  function Je(h, S) {
    return function() {
      const oe = h._readableState;
      oe.awaitDrainWriters === S ? (b("pipeOnDrain", 1), oe.awaitDrainWriters = null) : oe.multiAwaitDrain && (b("pipeOnDrain", oe.awaitDrainWriters.size), oe.awaitDrainWriters.delete(S)), (!oe.awaitDrainWriters || oe.awaitDrainWriters.size === 0) && h.listenerCount("data") && h.resume();
    };
  }
  z.prototype.unpipe = function(h) {
    const S = this._readableState, ie = {
      hasUnpiped: !1
    };
    if (S.pipes.length === 0) return this;
    if (!h) {
      const fe = S.pipes;
      S.pipes = [], this.pause();
      for (let ve = 0; ve < fe.length; ve++)
        fe[ve].emit("unpipe", this, {
          hasUnpiped: !1
        });
      return this;
    }
    const oe = e(S.pipes, h);
    return oe === -1 ? this : (S.pipes.splice(oe, 1), S.pipes.length === 0 && this.pause(), h.emit("unpipe", this, ie), this);
  }, z.prototype.on = function(h, S) {
    const ie = I.prototype.on.call(this, h, S), oe = this._readableState;
    return h === "data" ? (oe.readableListening = this.listenerCount("readable") > 0, oe.flowing !== !1 && this.resume()) : h === "readable" && !oe.endEmitted && !oe.readableListening && (oe.readableListening = oe.needReadable = !0, oe.flowing = !1, oe.emittedReadable = !1, b("on readable", oe.length, oe.reading), oe.length ? Ae(this) : oe.reading || t.nextTick(jt, this)), ie;
  }, z.prototype.addListener = z.prototype.on, z.prototype.removeListener = function(h, S) {
    const ie = I.prototype.removeListener.call(this, h, S);
    return h === "readable" && t.nextTick(Xe, this), ie;
  }, z.prototype.off = z.prototype.removeListener, z.prototype.removeAllListeners = function(h) {
    const S = I.prototype.removeAllListeners.apply(this, arguments);
    return (h === "readable" || h === void 0) && t.nextTick(Xe, this), S;
  };
  function Xe(h) {
    const S = h._readableState;
    S.readableListening = h.listenerCount("readable") > 0, S.resumeScheduled && S[se] === !1 ? S.flowing = !0 : h.listenerCount("data") > 0 ? h.resume() : S.readableListening || (S.flowing = null);
  }
  function jt(h) {
    b("readable nexttick read 0"), h.read(0);
  }
  z.prototype.resume = function() {
    const h = this._readableState;
    return h.flowing || (b("resume"), h.flowing = !h.readableListening, Ct(this, h)), h[se] = !1, this;
  };
  function Ct(h, S) {
    S.resumeScheduled || (S.resumeScheduled = !0, t.nextTick($t, h, S));
  }
  function $t(h, S) {
    b("resume", S.reading), S.reading || h.read(0), S.resumeScheduled = !1, h.emit("resume"), ht(h), S.flowing && !S.reading && h.read(0);
  }
  z.prototype.pause = function() {
    return b("call pause flowing=%j", this._readableState.flowing), this._readableState.flowing !== !1 && (b("pause"), this._readableState.flowing = !1, this.emit("pause")), this._readableState[se] = !0, this;
  };
  function ht(h) {
    const S = h._readableState;
    for (b("flow", S.flowing); S.flowing && h.read() !== null; ) ;
  }
  z.prototype.wrap = function(h) {
    let S = !1;
    h.on("data", (oe) => {
      !this.push(oe) && h.pause && (S = !0, h.pause());
    }), h.on("end", () => {
      this.push(null);
    }), h.on("error", (oe) => {
      v(this, oe);
    }), h.on("close", () => {
      this.destroy();
    }), h.on("destroy", () => {
      this.destroy();
    }), this._read = () => {
      S && h.resume && (S = !1, h.resume());
    };
    const ie = i(h);
    for (let oe = 1; oe < ie.length; oe++) {
      const fe = ie[oe];
      this[fe] === void 0 && typeof h[fe] == "function" && (this[fe] = h[fe].bind(h));
    }
    return this;
  }, z.prototype[y] = function() {
    return wt(this);
  }, z.prototype.iterator = function(h) {
    return h !== void 0 && re(h, "options"), wt(this, h);
  };
  function wt(h, S) {
    typeof h.read != "function" && (h = z.wrap(h, {
      objectMode: !0
    }));
    const ie = Wt(h, S);
    return ie.stream = h, ie;
  }
  async function* Wt(h, S) {
    let ie = Q;
    function oe(Te) {
      this === h ? (ie(), ie = Q) : ie = Te;
    }
    h.on("readable", oe);
    let fe;
    const ve = p(
      h,
      {
        writable: !1
      },
      (Te) => {
        fe = Te ? L(fe, Te) : null, ie(), ie = Q;
      }
    );
    try {
      for (; ; ) {
        const Te = h.destroyed ? null : h.read();
        if (Te !== null)
          yield Te;
        else {
          if (fe)
            throw fe;
          if (fe === null)
            return;
          await new s(oe);
        }
      }
    } catch (Te) {
      throw fe = L(fe, Te), fe;
    } finally {
      (fe || S?.destroyOnReturn !== !1) && (fe === void 0 || h._readableState.autoDestroy) ? N.destroyer(h, null) : (h.off("readable", oe), ve());
    }
  }
  n(z.prototype, {
    readable: {
      __proto__: null,
      get() {
        const h = this._readableState;
        return !!h && h.readable !== !1 && !h.destroyed && !h.errorEmitted && !h.endEmitted;
      },
      set(h) {
        this._readableState && (this._readableState.readable = !!h);
      }
    },
    readableDidRead: {
      __proto__: null,
      enumerable: !1,
      get: function() {
        return this._readableState.dataEmitted;
      }
    },
    readableAborted: {
      __proto__: null,
      enumerable: !1,
      get: function() {
        return !!(this._readableState.readable !== !1 && (this._readableState.destroyed || this._readableState.errored) && !this._readableState.endEmitted);
      }
    },
    readableHighWaterMark: {
      __proto__: null,
      enumerable: !1,
      get: function() {
        return this._readableState.highWaterMark;
      }
    },
    readableBuffer: {
      __proto__: null,
      enumerable: !1,
      get: function() {
        return this._readableState && this._readableState.buffer;
      }
    },
    readableFlowing: {
      __proto__: null,
      enumerable: !1,
      get: function() {
        return this._readableState.flowing;
      },
      set: function(h) {
        this._readableState && (this._readableState.flowing = h);
      }
    },
    readableLength: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._readableState.length;
      }
    },
    readableObjectMode: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._readableState ? this._readableState.objectMode : !1;
      }
    },
    readableEncoding: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._readableState ? this._readableState.encoding : null;
      }
    },
    errored: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._readableState ? this._readableState.errored : null;
      }
    },
    closed: {
      __proto__: null,
      get() {
        return this._readableState ? this._readableState.closed : !1;
      }
    },
    destroyed: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._readableState ? this._readableState.destroyed : !1;
      },
      set(h) {
        this._readableState && (this._readableState.destroyed = h);
      }
    },
    readableEnded: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._readableState ? this._readableState.endEmitted : !1;
      }
    }
  }), n(te.prototype, {
    // Legacy getter for `pipesCount`.
    pipesCount: {
      __proto__: null,
      get() {
        return this.pipes.length;
      }
    },
    // Legacy property for `paused`.
    paused: {
      __proto__: null,
      get() {
        return this[se] !== !1;
      },
      set(h) {
        this[se] = !!h;
      }
    }
  }), z._fromList = mt;
  function mt(h, S) {
    if (S.length === 0) return null;
    let ie;
    return S.objectMode ? ie = S.buffer.shift() : !h || h >= S.length ? (S.decoder ? ie = S.buffer.join("") : S.buffer.length === 1 ? ie = S.buffer.first() : ie = S.buffer.concat(S.length), S.buffer.clear()) : ie = S.buffer.consume(h, S.decoder), ie;
  }
  function tt(h) {
    const S = h._readableState;
    b("endReadable", S.endEmitted), S.endEmitted || (S.ended = !0, t.nextTick(qt, S, h));
  }
  function qt(h, S) {
    if (b("endReadableNT", h.endEmitted, h.length), !h.errored && !h.closeEmitted && !h.endEmitted && h.length === 0) {
      if (h.endEmitted = !0, S.emit("end"), S.writable && S.allowHalfOpen === !1)
        t.nextTick(Bt, S);
      else if (h.autoDestroy) {
        const ie = S._writableState;
        (!ie || ie.autoDestroy && // We don't expect the writable to ever 'finish'
        // if writable is explicitly set to false.
        (ie.finished || ie.writable === !1)) && S.destroy();
      }
    }
  }
  function Bt(h) {
    h.writable && !h.writableEnded && !h.destroyed && h.end();
  }
  z.from = function(h, S) {
    return M(z, h, S);
  };
  let rt;
  function St() {
    return rt === void 0 && (rt = {}), rt;
  }
  return z.fromWeb = function(h, S) {
    return St().newStreamReadableFromReadableStream(h, S);
  }, z.toWeb = function(h, S) {
    return St().newReadableStreamFromStreamReadable(h, S);
  }, z.wrap = function(h, S) {
    var ie, oe;
    return new z({
      objectMode: (ie = (oe = h.readableObjectMode) !== null && oe !== void 0 ? oe : h.objectMode) !== null && ie !== void 0 ? ie : !0,
      ...S,
      destroy(fe, ve) {
        N.destroyer(h, fe), ve(fe);
      }
    }).wrap(h);
  }, gu;
}
var yu, Yy;
function kc() {
  if (Yy) return yu;
  Yy = 1;
  const t = je(), {
    ArrayPrototypeSlice: e,
    Error: r,
    FunctionPrototypeSymbolHasInstance: o,
    ObjectDefineProperty: a,
    ObjectDefineProperties: n,
    ObjectSetPrototypeOf: i,
    StringPrototypeToLowerCase: l,
    Symbol: s,
    SymbolHasInstance: f
  } = Ve();
  yu = re, re.WritableState = T;
  const { EventEmitter: d } = Ke, y = Mc().Stream, { Buffer: R } = Fe, w = Wr(), { addAbortSignal: I } = rs(), { getHighWaterMark: A, getDefaultHighWaterMark: u } = ns(), {
    ERR_INVALID_ARG_TYPE: g,
    ERR_METHOD_NOT_IMPLEMENTED: p,
    ERR_MULTIPLE_CALLBACK: b,
    ERR_STREAM_CANNOT_PIPE: C,
    ERR_STREAM_DESTROYED: N,
    ERR_STREAM_ALREADY_FINISHED: W,
    ERR_STREAM_NULL_VALUES: k,
    ERR_STREAM_WRITE_AFTER_END: L,
    ERR_UNKNOWN_ENCODING: U
  } = ft().codes, { errorOrDestroy: x } = w;
  i(re.prototype, y.prototype), i(re, y);
  function D() {
  }
  const F = s("kOnFinished");
  function T(m, c, E) {
    typeof E != "boolean" && (E = c instanceof Jt()), this.objectMode = !!(m && m.objectMode), E && (this.objectMode = this.objectMode || !!(m && m.writableObjectMode)), this.highWaterMark = m ? A(this, m, "writableHighWaterMark", E) : u(!1), this.finalCalled = !1, this.needDrain = !1, this.ending = !1, this.ended = !1, this.finished = !1, this.destroyed = !1;
    const j = !!(m && m.decodeStrings === !1);
    this.decodeStrings = !j, this.defaultEncoding = m && m.defaultEncoding || "utf8", this.length = 0, this.writing = !1, this.corked = 0, this.sync = !0, this.bufferProcessing = !1, this.onwrite = v.bind(void 0, c), this.writecb = null, this.writelen = 0, this.afterWriteTickInfo = null, J(this), this.pendingcb = 0, this.constructed = !0, this.prefinished = !1, this.errorEmitted = !1, this.emitClose = !m || m.emitClose !== !1, this.autoDestroy = !m || m.autoDestroy !== !1, this.errored = null, this.closed = !1, this.closeEmitted = !1, this[F] = [];
  }
  function J(m) {
    m.buffered = [], m.bufferedIndex = 0, m.allBuffers = !0, m.allNoop = !0;
  }
  T.prototype.getBuffer = function() {
    return e(this.buffered, this.bufferedIndex);
  }, a(T.prototype, "bufferedRequestCount", {
    __proto__: null,
    get() {
      return this.buffered.length - this.bufferedIndex;
    }
  });
  function re(m) {
    const c = this instanceof Jt();
    if (!c && !o(re, this)) return new re(m);
    this._writableState = new T(m, this, c), m && (typeof m.write == "function" && (this._write = m.write), typeof m.writev == "function" && (this._writev = m.writev), typeof m.destroy == "function" && (this._destroy = m.destroy), typeof m.final == "function" && (this._final = m.final), typeof m.construct == "function" && (this._construct = m.construct), m.signal && I(m.signal, this)), y.call(this, m), w.construct(this, () => {
      const E = this._writableState;
      E.writing || le(this, E), P(this, E);
    });
  }
  a(re, f, {
    __proto__: null,
    value: function(m) {
      return o(this, m) ? !0 : this !== re ? !1 : m && m._writableState instanceof T;
    }
  }), re.prototype.pipe = function() {
    x(this, new C());
  };
  function se(m, c, E, j) {
    const G = m._writableState;
    if (typeof E == "function")
      j = E, E = G.defaultEncoding;
    else {
      if (!E) E = G.defaultEncoding;
      else if (E !== "buffer" && !R.isEncoding(E)) throw new U(E);
      typeof j != "function" && (j = D);
    }
    if (c === null)
      throw new k();
    if (!G.objectMode)
      if (typeof c == "string")
        G.decodeStrings !== !1 && (c = R.from(c, E), E = "buffer");
      else if (c instanceof R)
        E = "buffer";
      else if (y._isUint8Array(c))
        c = y._uint8ArrayToBuffer(c), E = "buffer";
      else
        throw new g("chunk", ["string", "Buffer", "Uint8Array"], c);
    let K;
    return G.ending ? K = new L() : G.destroyed && (K = new N("write")), K ? (t.nextTick(j, K), x(m, K, !0), K) : (G.pendingcb++, ne(m, G, c, E, j));
  }
  re.prototype.write = function(m, c, E) {
    return se(this, m, c, E) === !0;
  }, re.prototype.cork = function() {
    this._writableState.corked++;
  }, re.prototype.uncork = function() {
    const m = this._writableState;
    m.corked && (m.corked--, m.writing || le(this, m));
  }, re.prototype.setDefaultEncoding = function(c) {
    if (typeof c == "string" && (c = l(c)), !R.isEncoding(c)) throw new U(c);
    return this._writableState.defaultEncoding = c, this;
  };
  function ne(m, c, E, j, G) {
    const K = c.objectMode ? 1 : E.length;
    c.length += K;
    const O = c.length < c.highWaterMark;
    return O || (c.needDrain = !0), c.writing || c.corked || c.errored || !c.constructed ? (c.buffered.push({
      chunk: E,
      encoding: j,
      callback: G
    }), c.allBuffers && j !== "buffer" && (c.allBuffers = !1), c.allNoop && G !== D && (c.allNoop = !1)) : (c.writelen = K, c.writecb = G, c.writing = !0, c.sync = !0, m._write(E, j, c.onwrite), c.sync = !1), O && !c.errored && !c.destroyed;
  }
  function M(m, c, E, j, G, K, O) {
    c.writelen = j, c.writecb = O, c.writing = !0, c.sync = !0, c.destroyed ? c.onwrite(new N("write")) : E ? m._writev(G, c.onwrite) : m._write(G, K, c.onwrite), c.sync = !1;
  }
  function Q(m, c, E, j) {
    --c.pendingcb, j(E), B(c), x(m, E);
  }
  function v(m, c) {
    const E = m._writableState, j = E.sync, G = E.writecb;
    if (typeof G != "function") {
      x(m, new b());
      return;
    }
    E.writing = !1, E.writecb = null, E.length -= E.writelen, E.writelen = 0, c ? (c.stack, E.errored || (E.errored = c), m._readableState && !m._readableState.errored && (m._readableState.errored = c), j ? t.nextTick(Q, m, E, c, G) : Q(m, E, c, G)) : (E.buffered.length > E.bufferedIndex && le(m, E), j ? E.afterWriteTickInfo !== null && E.afterWriteTickInfo.cb === G ? E.afterWriteTickInfo.count++ : (E.afterWriteTickInfo = {
      count: 1,
      cb: G,
      stream: m,
      state: E
    }, t.nextTick($, E.afterWriteTickInfo)) : ee(m, E, 1, G));
  }
  function $({ stream: m, state: c, count: E, cb: j }) {
    return c.afterWriteTickInfo = null, ee(m, c, E, j);
  }
  function ee(m, c, E, j) {
    for (!c.ending && !m.destroyed && c.length === 0 && c.needDrain && (c.needDrain = !1, m.emit("drain")); E-- > 0; )
      c.pendingcb--, j();
    c.destroyed && B(c), P(m, c);
  }
  function B(m) {
    if (m.writing)
      return;
    for (let G = m.bufferedIndex; G < m.buffered.length; ++G) {
      var c;
      const { chunk: K, callback: O } = m.buffered[G], H = m.objectMode ? 1 : K.length;
      m.length -= H, O(
        (c = m.errored) !== null && c !== void 0 ? c : new N("write")
      );
    }
    const E = m[F].splice(0);
    for (let G = 0; G < E.length; G++) {
      var j;
      E[G](
        (j = m.errored) !== null && j !== void 0 ? j : new N("end")
      );
    }
    J(m);
  }
  function le(m, c) {
    if (c.corked || c.bufferProcessing || c.destroyed || !c.constructed)
      return;
    const { buffered: E, bufferedIndex: j, objectMode: G } = c, K = E.length - j;
    if (!K)
      return;
    let O = j;
    if (c.bufferProcessing = !0, K > 1 && m._writev) {
      c.pendingcb -= K - 1;
      const H = c.allNoop ? D : (z) => {
        for (let de = O; de < E.length; ++de)
          E[de].callback(z);
      }, te = c.allNoop && O === 0 ? E : e(E, O);
      te.allBuffers = c.allBuffers, M(m, c, !0, c.length, te, "", H), J(c);
    } else {
      do {
        const { chunk: H, encoding: te, callback: z } = E[O];
        E[O++] = null;
        const de = G ? 1 : H.length;
        M(m, c, !1, de, H, te, z);
      } while (O < E.length && !c.writing);
      O === E.length ? J(c) : O > 256 ? (E.splice(0, O), c.bufferedIndex = 0) : c.bufferedIndex = O;
    }
    c.bufferProcessing = !1;
  }
  re.prototype._write = function(m, c, E) {
    if (this._writev)
      this._writev(
        [
          {
            chunk: m,
            encoding: c
          }
        ],
        E
      );
    else
      throw new p("_write()");
  }, re.prototype._writev = null, re.prototype.end = function(m, c, E) {
    const j = this._writableState;
    typeof m == "function" ? (E = m, m = null, c = null) : typeof c == "function" && (E = c, c = null);
    let G;
    if (m != null) {
      const K = se(this, m, c);
      K instanceof r && (G = K);
    }
    return j.corked && (j.corked = 1, this.uncork()), G || (!j.errored && !j.ending ? (j.ending = !0, P(this, j, !0), j.ended = !0) : j.finished ? G = new W("end") : j.destroyed && (G = new N("end"))), typeof E == "function" && (G || j.finished ? t.nextTick(E, G) : j[F].push(E)), this;
  };
  function ae(m) {
    return m.ending && !m.destroyed && m.constructed && m.length === 0 && !m.errored && m.buffered.length === 0 && !m.finished && !m.writing && !m.errorEmitted && !m.closeEmitted;
  }
  function ce(m, c) {
    let E = !1;
    function j(G) {
      if (E) {
        x(m, G ?? b());
        return;
      }
      if (E = !0, c.pendingcb--, G) {
        const K = c[F].splice(0);
        for (let O = 0; O < K.length; O++)
          K[O](G);
        x(m, G, c.sync);
      } else ae(c) && (c.prefinished = !0, m.emit("prefinish"), c.pendingcb++, t.nextTick(q, m, c));
    }
    c.sync = !0, c.pendingcb++;
    try {
      m._final(j);
    } catch (G) {
      j(G);
    }
    c.sync = !1;
  }
  function Y(m, c) {
    !c.prefinished && !c.finalCalled && (typeof m._final == "function" && !c.destroyed ? (c.finalCalled = !0, ce(m, c)) : (c.prefinished = !0, m.emit("prefinish")));
  }
  function P(m, c, E) {
    ae(c) && (Y(m, c), c.pendingcb === 0 && (E ? (c.pendingcb++, t.nextTick(
      (j, G) => {
        ae(G) ? q(j, G) : G.pendingcb--;
      },
      m,
      c
    )) : ae(c) && (c.pendingcb++, q(m, c))));
  }
  function q(m, c) {
    c.pendingcb--, c.finished = !0;
    const E = c[F].splice(0);
    for (let j = 0; j < E.length; j++)
      E[j]();
    if (m.emit("finish"), c.autoDestroy) {
      const j = m._readableState;
      (!j || j.autoDestroy && // We don't expect the readable to ever 'end'
      // if readable is explicitly set to false.
      (j.endEmitted || j.readable === !1)) && m.destroy();
    }
  }
  n(re.prototype, {
    closed: {
      __proto__: null,
      get() {
        return this._writableState ? this._writableState.closed : !1;
      }
    },
    destroyed: {
      __proto__: null,
      get() {
        return this._writableState ? this._writableState.destroyed : !1;
      },
      set(m) {
        this._writableState && (this._writableState.destroyed = m);
      }
    },
    writable: {
      __proto__: null,
      get() {
        const m = this._writableState;
        return !!m && m.writable !== !1 && !m.destroyed && !m.errored && !m.ending && !m.ended;
      },
      set(m) {
        this._writableState && (this._writableState.writable = !!m);
      }
    },
    writableFinished: {
      __proto__: null,
      get() {
        return this._writableState ? this._writableState.finished : !1;
      }
    },
    writableObjectMode: {
      __proto__: null,
      get() {
        return this._writableState ? this._writableState.objectMode : !1;
      }
    },
    writableBuffer: {
      __proto__: null,
      get() {
        return this._writableState && this._writableState.getBuffer();
      }
    },
    writableEnded: {
      __proto__: null,
      get() {
        return this._writableState ? this._writableState.ending : !1;
      }
    },
    writableNeedDrain: {
      __proto__: null,
      get() {
        const m = this._writableState;
        return m ? !m.destroyed && !m.ending && m.needDrain : !1;
      }
    },
    writableHighWaterMark: {
      __proto__: null,
      get() {
        return this._writableState && this._writableState.highWaterMark;
      }
    },
    writableCorked: {
      __proto__: null,
      get() {
        return this._writableState ? this._writableState.corked : 0;
      }
    },
    writableLength: {
      __proto__: null,
      get() {
        return this._writableState && this._writableState.length;
      }
    },
    errored: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._writableState ? this._writableState.errored : null;
      }
    },
    writableAborted: {
      __proto__: null,
      enumerable: !1,
      get: function() {
        return !!(this._writableState.writable !== !1 && (this._writableState.destroyed || this._writableState.errored) && !this._writableState.finished);
      }
    }
  });
  const V = w.destroy;
  re.prototype.destroy = function(m, c) {
    const E = this._writableState;
    return !E.destroyed && (E.bufferedIndex < E.buffered.length || E[F].length) && t.nextTick(B, E), V.call(this, m, c), this;
  }, re.prototype._undestroy = w.undestroy, re.prototype._destroy = function(m, c) {
    c(m);
  }, re.prototype[d.captureRejectionSymbol] = function(m) {
    this.destroy(m);
  };
  let Z;
  function X() {
    return Z === void 0 && (Z = {}), Z;
  }
  return re.fromWeb = function(m, c) {
    return X().newStreamWritableFromWritableStream(m, c);
  }, re.toWeb = function(m) {
    return X().newWritableStreamFromStreamWritable(m);
  }, yu;
}
var bu, Ky;
function LA() {
  if (Ky) return bu;
  Ky = 1;
  const t = je(), e = Fe, {
    isReadable: r,
    isWritable: o,
    isIterable: a,
    isNodeStream: n,
    isReadableNodeStream: i,
    isWritableNodeStream: l,
    isDuplexNodeStream: s,
    isReadableStream: f,
    isWritableStream: d
  } = ir(), y = gr(), {
    AbortError: R,
    codes: { ERR_INVALID_ARG_TYPE: w, ERR_INVALID_RETURN_VALUE: I }
  } = ft(), { destroyer: A } = Wr(), u = Jt(), g = is(), p = kc(), { createDeferredPromise: b } = gt(), C = Qm(), N = globalThis.Blob || e.Blob, W = typeof N < "u" ? function(T) {
    return T instanceof N;
  } : function(T) {
    return !1;
  }, k = globalThis.AbortController || et.AbortController, { FunctionPrototypeCall: L } = Ve();
  class U extends u {
    constructor(T) {
      super(T), T?.readable === !1 && (this._readableState.readable = !1, this._readableState.ended = !0, this._readableState.endEmitted = !0), T?.writable === !1 && (this._writableState.writable = !1, this._writableState.ending = !0, this._writableState.ended = !0, this._writableState.finished = !0);
    }
  }
  bu = function F(T, J) {
    if (s(T))
      return T;
    if (i(T))
      return D({
        readable: T
      });
    if (l(T))
      return D({
        writable: T
      });
    if (n(T))
      return D({
        writable: !1,
        readable: !1
      });
    if (f(T))
      return D({
        readable: g.fromWeb(T)
      });
    if (d(T))
      return D({
        writable: p.fromWeb(T)
      });
    if (typeof T == "function") {
      const { value: se, write: ne, final: M, destroy: Q } = x(T);
      if (a(se))
        return C(U, se, {
          // TODO (ronag): highWaterMark?
          objectMode: !0,
          write: ne,
          final: M,
          destroy: Q
        });
      const v = se?.then;
      if (typeof v == "function") {
        let $;
        const ee = L(
          v,
          se,
          (B) => {
            if (B != null)
              throw new I("nully", "body", B);
          },
          (B) => {
            A($, B);
          }
        );
        return $ = new U({
          // TODO (ronag): highWaterMark?
          objectMode: !0,
          readable: !1,
          write: ne,
          final(B) {
            M(async () => {
              try {
                await ee, t.nextTick(B, null);
              } catch (le) {
                t.nextTick(B, le);
              }
            });
          },
          destroy: Q
        });
      }
      throw new I("Iterable, AsyncIterable or AsyncFunction", J, se);
    }
    if (W(T))
      return F(T.arrayBuffer());
    if (a(T))
      return C(U, T, {
        // TODO (ronag): highWaterMark?
        objectMode: !0,
        writable: !1
      });
    if (f(T?.readable) && d(T?.writable))
      return U.fromWeb(T);
    if (typeof T?.writable == "object" || typeof T?.readable == "object") {
      const se = T != null && T.readable ? i(T?.readable) ? T?.readable : F(T.readable) : void 0, ne = T != null && T.writable ? l(T?.writable) ? T?.writable : F(T.writable) : void 0;
      return D({
        readable: se,
        writable: ne
      });
    }
    const re = T?.then;
    if (typeof re == "function") {
      let se;
      return L(
        re,
        T,
        (ne) => {
          ne != null && se.push(ne), se.push(null);
        },
        (ne) => {
          A(se, ne);
        }
      ), se = new U({
        objectMode: !0,
        writable: !1,
        read() {
        }
      });
    }
    throw new w(
      J,
      [
        "Blob",
        "ReadableStream",
        "WritableStream",
        "Stream",
        "Iterable",
        "AsyncIterable",
        "Function",
        "{ readable, writable } pair",
        "Promise"
      ],
      T
    );
  };
  function x(F) {
    let { promise: T, resolve: J } = b();
    const re = new k(), se = re.signal;
    return {
      value: F(
        (async function* () {
          for (; ; ) {
            const M = T;
            T = null;
            const { chunk: Q, done: v, cb: $ } = await M;
            if (t.nextTick($), v) return;
            if (se.aborted)
              throw new R(void 0, {
                cause: se.reason
              });
            ({ promise: T, resolve: J } = b()), yield Q;
          }
        })(),
        {
          signal: se
        }
      ),
      write(M, Q, v) {
        const $ = J;
        J = null, $({
          chunk: M,
          done: !1,
          cb: v
        });
      },
      final(M) {
        const Q = J;
        J = null, Q({
          done: !0,
          cb: M
        });
      },
      destroy(M, Q) {
        re.abort(), Q(M);
      }
    };
  }
  function D(F) {
    const T = F.readable && typeof F.readable.read != "function" ? g.wrap(F.readable) : F.readable, J = F.writable;
    let re = !!r(T), se = !!o(J), ne, M, Q, v, $;
    function ee(B) {
      const le = v;
      v = null, le ? le(B) : B && $.destroy(B);
    }
    return $ = new U({
      // TODO (ronag): highWaterMark?
      readableObjectMode: !!(T != null && T.readableObjectMode),
      writableObjectMode: !!(J != null && J.writableObjectMode),
      readable: re,
      writable: se
    }), se && (y(J, (B) => {
      se = !1, B && A(T, B), ee(B);
    }), $._write = function(B, le, ae) {
      J.write(B, le) ? ae() : ne = ae;
    }, $._final = function(B) {
      J.end(), M = B;
    }, J.on("drain", function() {
      if (ne) {
        const B = ne;
        ne = null, B();
      }
    }), J.on("finish", function() {
      if (M) {
        const B = M;
        M = null, B();
      }
    })), re && (y(T, (B) => {
      re = !1, B && A(T, B), ee(B);
    }), T.on("readable", function() {
      if (Q) {
        const B = Q;
        Q = null, B();
      }
    }), T.on("end", function() {
      $.push(null);
    }), $._read = function() {
      for (; ; ) {
        const B = T.read();
        if (B === null) {
          Q = $._read;
          return;
        }
        if (!$.push(B))
          return;
      }
    }), $._destroy = function(B, le) {
      !B && v !== null && (B = new R()), Q = null, ne = null, M = null, v === null ? le(B) : (v = le, A(J, B), A(T, B));
    }, $;
  }
  return bu;
}
var _u, Jy;
function Jt() {
  if (Jy) return _u;
  Jy = 1;
  const {
    ObjectDefineProperties: t,
    ObjectGetOwnPropertyDescriptor: e,
    ObjectKeys: r,
    ObjectSetPrototypeOf: o
  } = Ve();
  _u = i;
  const a = is(), n = kc();
  o(i.prototype, a.prototype), o(i, a);
  {
    const d = r(n.prototype);
    for (let y = 0; y < d.length; y++) {
      const R = d[y];
      i.prototype[R] || (i.prototype[R] = n.prototype[R]);
    }
  }
  function i(d) {
    if (!(this instanceof i)) return new i(d);
    a.call(this, d), n.call(this, d), d ? (this.allowHalfOpen = d.allowHalfOpen !== !1, d.readable === !1 && (this._readableState.readable = !1, this._readableState.ended = !0, this._readableState.endEmitted = !0), d.writable === !1 && (this._writableState.writable = !1, this._writableState.ending = !0, this._writableState.ended = !0, this._writableState.finished = !0)) : this.allowHalfOpen = !0;
  }
  t(i.prototype, {
    writable: {
      __proto__: null,
      ...e(n.prototype, "writable")
    },
    writableHighWaterMark: {
      __proto__: null,
      ...e(n.prototype, "writableHighWaterMark")
    },
    writableObjectMode: {
      __proto__: null,
      ...e(n.prototype, "writableObjectMode")
    },
    writableBuffer: {
      __proto__: null,
      ...e(n.prototype, "writableBuffer")
    },
    writableLength: {
      __proto__: null,
      ...e(n.prototype, "writableLength")
    },
    writableFinished: {
      __proto__: null,
      ...e(n.prototype, "writableFinished")
    },
    writableCorked: {
      __proto__: null,
      ...e(n.prototype, "writableCorked")
    },
    writableEnded: {
      __proto__: null,
      ...e(n.prototype, "writableEnded")
    },
    writableNeedDrain: {
      __proto__: null,
      ...e(n.prototype, "writableNeedDrain")
    },
    destroyed: {
      __proto__: null,
      get() {
        return this._readableState === void 0 || this._writableState === void 0 ? !1 : this._readableState.destroyed && this._writableState.destroyed;
      },
      set(d) {
        this._readableState && this._writableState && (this._readableState.destroyed = d, this._writableState.destroyed = d);
      }
    }
  });
  let l;
  function s() {
    return l === void 0 && (l = {}), l;
  }
  i.fromWeb = function(d, y) {
    return s().newStreamDuplexFromReadableWritablePair(d, y);
  }, i.toWeb = function(d) {
    return s().newReadableWritablePairFromDuplex(d);
  };
  let f;
  return i.from = function(d) {
    return f || (f = LA()), f(d, "body");
  }, _u;
}
var wu, Xy;
function eS() {
  if (Xy) return wu;
  Xy = 1;
  const { ObjectSetPrototypeOf: t, Symbol: e } = Ve();
  wu = i;
  const { ERR_METHOD_NOT_IMPLEMENTED: r } = ft().codes, o = Jt(), { getHighWaterMark: a } = ns();
  t(i.prototype, o.prototype), t(i, o);
  const n = e("kCallback");
  function i(f) {
    if (!(this instanceof i)) return new i(f);
    const d = f ? a(this, f, "readableHighWaterMark", !0) : null;
    d === 0 && (f = {
      ...f,
      highWaterMark: null,
      readableHighWaterMark: d,
      // TODO (ronag): 0 is not optimal since we have
      // a "bug" where we check needDrain before calling _write and not after.
      // Refs: https://github.com/nodejs/node/pull/32887
      // Refs: https://github.com/nodejs/node/pull/35941
      writableHighWaterMark: f.writableHighWaterMark || 0
    }), o.call(this, f), this._readableState.sync = !1, this[n] = null, f && (typeof f.transform == "function" && (this._transform = f.transform), typeof f.flush == "function" && (this._flush = f.flush)), this.on("prefinish", s);
  }
  function l(f) {
    typeof this._flush == "function" && !this.destroyed ? this._flush((d, y) => {
      if (d) {
        f ? f(d) : this.destroy(d);
        return;
      }
      y != null && this.push(y), this.push(null), f && f();
    }) : (this.push(null), f && f());
  }
  function s() {
    this._final !== l && l.call(this);
  }
  return i.prototype._final = l, i.prototype._transform = function(f, d, y) {
    throw new r("_transform()");
  }, i.prototype._write = function(f, d, y) {
    const R = this._readableState, w = this._writableState, I = R.length;
    this._transform(f, d, (A, u) => {
      if (A) {
        y(A);
        return;
      }
      u != null && this.push(u), w.ended || // Backwards compat.
      I === R.length || // Backwards compat.
      R.length < R.highWaterMark ? y() : this[n] = y;
    });
  }, i.prototype._read = function() {
    if (this[n]) {
      const f = this[n];
      this[n] = null, f();
    }
  }, wu;
}
var mu, Qy;
function tS() {
  if (Qy) return mu;
  Qy = 1;
  const { ObjectSetPrototypeOf: t } = Ve();
  mu = r;
  const e = eS();
  t(r.prototype, e.prototype), t(r, e);
  function r(o) {
    if (!(this instanceof r)) return new r(o);
    e.call(this, o);
  }
  return r.prototype._transform = function(o, a, n) {
    n(null, o);
  }, mu;
}
var Su, eb;
function Fc() {
  if (eb) return Su;
  eb = 1;
  const t = je(), { ArrayIsArray: e, Promise: r, SymbolAsyncIterator: o, SymbolDispose: a } = Ve(), n = gr(), { once: i } = gt(), l = Wr(), s = Jt(), {
    aggregateTwoErrors: f,
    codes: {
      ERR_INVALID_ARG_TYPE: d,
      ERR_INVALID_RETURN_VALUE: y,
      ERR_MISSING_ARGS: R,
      ERR_STREAM_DESTROYED: w,
      ERR_STREAM_PREMATURE_CLOSE: I
    },
    AbortError: A
  } = ft(), { validateFunction: u, validateAbortSignal: g } = xn(), {
    isIterable: p,
    isReadable: b,
    isReadableNodeStream: C,
    isNodeStream: N,
    isTransformStream: W,
    isWebStream: k,
    isReadableStream: L,
    isReadableFinished: U
  } = ir(), x = globalThis.AbortController || et.AbortController;
  let D, F, T;
  function J(B, le, ae) {
    let ce = !1;
    B.on("close", () => {
      ce = !0;
    });
    const Y = n(
      B,
      {
        readable: le,
        writable: ae
      },
      (P) => {
        ce = !P;
      }
    );
    return {
      destroy: (P) => {
        ce || (ce = !0, l.destroyer(B, P || new w("pipe")));
      },
      cleanup: Y
    };
  }
  function re(B) {
    return u(B[B.length - 1], "streams[stream.length - 1]"), B.pop();
  }
  function se(B) {
    if (p(B))
      return B;
    if (C(B))
      return ne(B);
    throw new d("val", ["Readable", "Iterable", "AsyncIterable"], B);
  }
  async function* ne(B) {
    F || (F = is()), yield* F.prototype[o].call(B);
  }
  async function M(B, le, ae, { end: ce }) {
    let Y, P = null;
    const q = (X) => {
      if (X && (Y = X), P) {
        const m = P;
        P = null, m();
      }
    }, V = () => new r((X, m) => {
      Y ? m(Y) : P = () => {
        Y ? m(Y) : X();
      };
    });
    le.on("drain", q);
    const Z = n(
      le,
      {
        readable: !1
      },
      q
    );
    try {
      le.writableNeedDrain && await V();
      for await (const X of B)
        le.write(X) || await V();
      ce && (le.end(), await V()), ae();
    } catch (X) {
      ae(Y !== X ? f(Y, X) : X);
    } finally {
      Z(), le.off("drain", q);
    }
  }
  async function Q(B, le, ae, { end: ce }) {
    W(le) && (le = le.writable);
    const Y = le.getWriter();
    try {
      for await (const P of B)
        await Y.ready, Y.write(P).catch(() => {
        });
      await Y.ready, ce && await Y.close(), ae();
    } catch (P) {
      try {
        await Y.abort(P), ae(P);
      } catch (q) {
        ae(q);
      }
    }
  }
  function v(...B) {
    return $(B, i(re(B)));
  }
  function $(B, le, ae) {
    if (B.length === 1 && e(B[0]) && (B = B[0]), B.length < 2)
      throw new R("streams");
    const ce = new x(), Y = ce.signal, P = ae?.signal, q = [];
    g(P, "options.signal");
    function V() {
      G(new A());
    }
    T = T || gt().addAbortListener;
    let Z;
    P && (Z = T(P, V));
    let X, m;
    const c = [];
    let E = 0;
    function j(te) {
      G(te, --E === 0);
    }
    function G(te, z) {
      var de;
      if (te && (!X || X.code === "ERR_STREAM_PREMATURE_CLOSE") && (X = te), !(!X && !z)) {
        for (; c.length; )
          c.shift()(X);
        (de = Z) === null || de === void 0 || de[a](), ce.abort(), z && (X || q.forEach((pe) => pe()), t.nextTick(le, X, m));
      }
    }
    let K;
    for (let te = 0; te < B.length; te++) {
      const z = B[te], de = te < B.length - 1, pe = te > 0, he = de || ae?.end !== !1, we = te === B.length - 1;
      if (N(z)) {
        let be = function(_e) {
          _e && _e.name !== "AbortError" && _e.code !== "ERR_STREAM_PREMATURE_CLOSE" && j(_e);
        };
        if (he) {
          const { destroy: _e, cleanup: Ae } = J(z, de, pe);
          c.push(_e), b(z) && we && q.push(Ae);
        }
        z.on("error", be), b(z) && we && q.push(() => {
          z.removeListener("error", be);
        });
      }
      if (te === 0)
        if (typeof z == "function") {
          if (K = z({
            signal: Y
          }), !p(K))
            throw new y("Iterable, AsyncIterable or Stream", "source", K);
        } else p(z) || C(z) || W(z) ? K = z : K = s.from(z);
      else if (typeof z == "function") {
        if (W(K)) {
          var O;
          K = se((O = K) === null || O === void 0 ? void 0 : O.readable);
        } else
          K = se(K);
        if (K = z(K, {
          signal: Y
        }), de) {
          if (!p(K, !0))
            throw new y("AsyncIterable", `transform[${te - 1}]`, K);
        } else {
          var H;
          D || (D = tS());
          const be = new D({
            objectMode: !0
          }), _e = (H = K) === null || H === void 0 ? void 0 : H.then;
          if (typeof _e == "function")
            E++, _e.call(
              K,
              (me) => {
                m = me, me != null && be.write(me), he && be.end(), t.nextTick(j);
              },
              (me) => {
                be.destroy(me), t.nextTick(j, me);
              }
            );
          else if (p(K, !0))
            E++, M(K, be, j, {
              end: he
            });
          else if (L(K) || W(K)) {
            const me = K.readable || K;
            E++, M(me, be, j, {
              end: he
            });
          } else
            throw new y("AsyncIterable or Promise", "destination", K);
          K = be;
          const { destroy: Ae, cleanup: xe } = J(K, !1, !0);
          c.push(Ae), we && q.push(xe);
        }
      } else if (N(z)) {
        if (C(K)) {
          E += 2;
          const be = ee(K, z, j, {
            end: he
          });
          b(z) && we && q.push(be);
        } else if (W(K) || L(K)) {
          const be = K.readable || K;
          E++, M(be, z, j, {
            end: he
          });
        } else if (p(K))
          E++, M(K, z, j, {
            end: he
          });
        else
          throw new d(
            "val",
            ["Readable", "Iterable", "AsyncIterable", "ReadableStream", "TransformStream"],
            K
          );
        K = z;
      } else if (k(z)) {
        if (C(K))
          E++, Q(se(K), z, j, {
            end: he
          });
        else if (L(K) || p(K))
          E++, Q(K, z, j, {
            end: he
          });
        else if (W(K))
          E++, Q(K.readable, z, j, {
            end: he
          });
        else
          throw new d(
            "val",
            ["Readable", "Iterable", "AsyncIterable", "ReadableStream", "TransformStream"],
            K
          );
        K = z;
      } else
        K = s.from(z);
    }
    return (Y != null && Y.aborted || P != null && P.aborted) && t.nextTick(V), K;
  }
  function ee(B, le, ae, { end: ce }) {
    let Y = !1;
    if (le.on("close", () => {
      Y || ae(new I());
    }), B.pipe(le, {
      end: !1
    }), ce) {
      let P = function() {
        Y = !0, le.end();
      };
      U(B) ? t.nextTick(P) : B.once("end", P);
    } else
      ae();
    return n(
      B,
      {
        readable: !0,
        writable: !1
      },
      (P) => {
        const q = B._readableState;
        P && P.code === "ERR_STREAM_PREMATURE_CLOSE" && q && q.ended && !q.errored && !q.errorEmitted ? B.once("end", ae).once("error", ae) : ae(P);
      }
    ), n(
      le,
      {
        readable: !1,
        writable: !0
      },
      ae
    );
  }
  return Su = {
    pipelineImpl: $,
    pipeline: v
  }, Su;
}
var Eu, tb;
function rS() {
  if (tb) return Eu;
  tb = 1;
  const { pipeline: t } = Fc(), e = Jt(), { destroyer: r } = Wr(), {
    isNodeStream: o,
    isReadable: a,
    isWritable: n,
    isWebStream: i,
    isTransformStream: l,
    isWritableStream: s,
    isReadableStream: f
  } = ir(), {
    AbortError: d,
    codes: { ERR_INVALID_ARG_VALUE: y, ERR_MISSING_ARGS: R }
  } = ft(), w = gr();
  return Eu = function(...A) {
    if (A.length === 0)
      throw new R("streams");
    if (A.length === 1)
      return e.from(A[0]);
    const u = [...A];
    if (typeof A[0] == "function" && (A[0] = e.from(A[0])), typeof A[A.length - 1] == "function") {
      const D = A.length - 1;
      A[D] = e.from(A[D]);
    }
    for (let D = 0; D < A.length; ++D)
      if (!(!o(A[D]) && !i(A[D]))) {
        if (D < A.length - 1 && !(a(A[D]) || f(A[D]) || l(A[D])))
          throw new y(`streams[${D}]`, u[D], "must be readable");
        if (D > 0 && !(n(A[D]) || s(A[D]) || l(A[D])))
          throw new y(`streams[${D}]`, u[D], "must be writable");
      }
    let g, p, b, C, N;
    function W(D) {
      const F = C;
      C = null, F ? F(D) : D ? N.destroy(D) : !x && !U && N.destroy();
    }
    const k = A[0], L = t(A, W), U = !!(n(k) || s(k) || l(k)), x = !!(a(L) || f(L) || l(L));
    if (N = new e({
      // TODO (ronag): highWaterMark?
      writableObjectMode: !!(k != null && k.writableObjectMode),
      readableObjectMode: !!(L != null && L.readableObjectMode),
      writable: U,
      readable: x
    }), U) {
      if (o(k))
        N._write = function(F, T, J) {
          k.write(F, T) ? J() : g = J;
        }, N._final = function(F) {
          k.end(), p = F;
        }, k.on("drain", function() {
          if (g) {
            const F = g;
            g = null, F();
          }
        });
      else if (i(k)) {
        const T = (l(k) ? k.writable : k).getWriter();
        N._write = async function(J, re, se) {
          try {
            await T.ready, T.write(J).catch(() => {
            }), se();
          } catch (ne) {
            se(ne);
          }
        }, N._final = async function(J) {
          try {
            await T.ready, T.close().catch(() => {
            }), p = J;
          } catch (re) {
            J(re);
          }
        };
      }
      const D = l(L) ? L.readable : L;
      w(D, () => {
        if (p) {
          const F = p;
          p = null, F();
        }
      });
    }
    if (x) {
      if (o(L))
        L.on("readable", function() {
          if (b) {
            const D = b;
            b = null, D();
          }
        }), L.on("end", function() {
          N.push(null);
        }), N._read = function() {
          for (; ; ) {
            const D = L.read();
            if (D === null) {
              b = N._read;
              return;
            }
            if (!N.push(D))
              return;
          }
        };
      else if (i(L)) {
        const F = (l(L) ? L.readable : L).getReader();
        N._read = async function() {
          for (; ; )
            try {
              const { value: T, done: J } = await F.read();
              if (!N.push(T))
                return;
              if (J) {
                N.push(null);
                return;
              }
            } catch {
              return;
            }
        };
      }
    }
    return N._destroy = function(D, F) {
      !D && C !== null && (D = new d()), b = null, g = null, p = null, C === null ? F(D) : (C = F, o(L) && r(L, D));
    }, N;
  }, Eu;
}
var rb;
function PA() {
  if (rb) return Vn;
  rb = 1;
  const t = globalThis.AbortController || et.AbortController, {
    codes: { ERR_INVALID_ARG_VALUE: e, ERR_INVALID_ARG_TYPE: r, ERR_MISSING_ARGS: o, ERR_OUT_OF_RANGE: a },
    AbortError: n
  } = ft(), { validateAbortSignal: i, validateInteger: l, validateObject: s } = xn(), f = Ve().Symbol("kWeak"), d = Ve().Symbol("kResistStopPropagation"), { finished: y } = gr(), R = rS(), { addAbortSignalNoValidate: w } = rs(), { isWritable: I, isNodeStream: A } = ir(), { deprecate: u } = gt(), {
    ArrayPrototypePush: g,
    Boolean: p,
    MathFloor: b,
    Number: C,
    NumberIsNaN: N,
    Promise: W,
    PromiseReject: k,
    PromiseResolve: L,
    PromisePrototypeThen: U,
    Symbol: x
  } = Ve(), D = x("kEmpty"), F = x("kEof");
  function T(P, q) {
    if (q != null && s(q, "options"), q?.signal != null && i(q.signal, "options.signal"), A(P) && !I(P))
      throw new e("stream", P, "must be writable");
    const V = R(this, P);
    return q != null && q.signal && w(q.signal, V), V;
  }
  function J(P, q) {
    if (typeof P != "function")
      throw new r("fn", ["Function", "AsyncFunction"], P);
    q != null && s(q, "options"), q?.signal != null && i(q.signal, "options.signal");
    let V = 1;
    q?.concurrency != null && (V = b(q.concurrency));
    let Z = V - 1;
    return q?.highWaterMark != null && (Z = b(q.highWaterMark)), l(V, "options.concurrency", 1), l(Z, "options.highWaterMark", 0), Z += V, (async function* () {
      const m = gt().AbortSignalAny(
        [q?.signal].filter(p)
      ), c = this, E = [], j = {
        signal: m
      };
      let G, K, O = !1, H = 0;
      function te() {
        O = !0, z();
      }
      function z() {
        H -= 1, de();
      }
      function de() {
        K && !O && H < V && E.length < Z && (K(), K = null);
      }
      async function pe() {
        try {
          for await (let he of c) {
            if (O)
              return;
            if (m.aborted)
              throw new n();
            try {
              if (he = P(he, j), he === D)
                continue;
              he = L(he);
            } catch (we) {
              he = k(we);
            }
            H += 1, U(he, z, te), E.push(he), G && (G(), G = null), !O && (E.length >= Z || H >= V) && await new W((we) => {
              K = we;
            });
          }
          E.push(F);
        } catch (he) {
          const we = k(he);
          U(we, z, te), E.push(we);
        } finally {
          O = !0, G && (G(), G = null);
        }
      }
      pe();
      try {
        for (; ; ) {
          for (; E.length > 0; ) {
            const he = await E[0];
            if (he === F)
              return;
            if (m.aborted)
              throw new n();
            he !== D && (yield he), E.shift(), de();
          }
          await new W((he) => {
            G = he;
          });
        }
      } finally {
        O = !0, K && (K(), K = null);
      }
    }).call(this);
  }
  function re(P = void 0) {
    return P != null && s(P, "options"), P?.signal != null && i(P.signal, "options.signal"), (async function* () {
      let V = 0;
      for await (const X of this) {
        var Z;
        if (P != null && (Z = P.signal) !== null && Z !== void 0 && Z.aborted)
          throw new n({
            cause: P.signal.reason
          });
        yield [V++, X];
      }
    }).call(this);
  }
  async function se(P, q = void 0) {
    for await (const V of v.call(this, P, q))
      return !0;
    return !1;
  }
  async function ne(P, q = void 0) {
    if (typeof P != "function")
      throw new r("fn", ["Function", "AsyncFunction"], P);
    return !await se.call(
      this,
      async (...V) => !await P(...V),
      q
    );
  }
  async function M(P, q) {
    for await (const V of v.call(this, P, q))
      return V;
  }
  async function Q(P, q) {
    if (typeof P != "function")
      throw new r("fn", ["Function", "AsyncFunction"], P);
    async function V(Z, X) {
      return await P(Z, X), D;
    }
    for await (const Z of J.call(this, V, q)) ;
  }
  function v(P, q) {
    if (typeof P != "function")
      throw new r("fn", ["Function", "AsyncFunction"], P);
    async function V(Z, X) {
      return await P(Z, X) ? Z : D;
    }
    return J.call(this, V, q);
  }
  class $ extends o {
    constructor() {
      super("reduce"), this.message = "Reduce of an empty stream requires an initial value";
    }
  }
  async function ee(P, q, V) {
    var Z;
    if (typeof P != "function")
      throw new r("reducer", ["Function", "AsyncFunction"], P);
    V != null && s(V, "options"), V?.signal != null && i(V.signal, "options.signal");
    let X = arguments.length > 1;
    if (V != null && (Z = V.signal) !== null && Z !== void 0 && Z.aborted) {
      const G = new n(void 0, {
        cause: V.signal.reason
      });
      throw this.once("error", () => {
      }), await y(this.destroy(G)), G;
    }
    const m = new t(), c = m.signal;
    if (V != null && V.signal) {
      const G = {
        once: !0,
        [f]: this,
        [d]: !0
      };
      V.signal.addEventListener("abort", () => m.abort(), G);
    }
    let E = !1;
    try {
      for await (const G of this) {
        var j;
        if (E = !0, V != null && (j = V.signal) !== null && j !== void 0 && j.aborted)
          throw new n();
        X ? q = await P(q, G, {
          signal: c
        }) : (q = G, X = !0);
      }
      if (!E && !X)
        throw new $();
    } finally {
      m.abort();
    }
    return q;
  }
  async function B(P) {
    P != null && s(P, "options"), P?.signal != null && i(P.signal, "options.signal");
    const q = [];
    for await (const Z of this) {
      var V;
      if (P != null && (V = P.signal) !== null && V !== void 0 && V.aborted)
        throw new n(void 0, {
          cause: P.signal.reason
        });
      g(q, Z);
    }
    return q;
  }
  function le(P, q) {
    const V = J.call(this, P, q);
    return (async function* () {
      for await (const X of V)
        yield* X;
    }).call(this);
  }
  function ae(P) {
    if (P = C(P), N(P))
      return 0;
    if (P < 0)
      throw new a("number", ">= 0", P);
    return P;
  }
  function ce(P, q = void 0) {
    return q != null && s(q, "options"), q?.signal != null && i(q.signal, "options.signal"), P = ae(P), (async function* () {
      var Z;
      if (q != null && (Z = q.signal) !== null && Z !== void 0 && Z.aborted)
        throw new n();
      for await (const m of this) {
        var X;
        if (q != null && (X = q.signal) !== null && X !== void 0 && X.aborted)
          throw new n();
        P-- <= 0 && (yield m);
      }
    }).call(this);
  }
  function Y(P, q = void 0) {
    return q != null && s(q, "options"), q?.signal != null && i(q.signal, "options.signal"), P = ae(P), (async function* () {
      var Z;
      if (q != null && (Z = q.signal) !== null && Z !== void 0 && Z.aborted)
        throw new n();
      for await (const m of this) {
        var X;
        if (q != null && (X = q.signal) !== null && X !== void 0 && X.aborted)
          throw new n();
        if (P-- > 0 && (yield m), P <= 0)
          return;
      }
    }).call(this);
  }
  return Vn.streamReturningOperators = {
    asIndexedPairs: u(re, "readable.asIndexedPairs will be removed in a future version."),
    drop: ce,
    filter: v,
    flatMap: le,
    map: J,
    take: Y,
    compose: T
  }, Vn.promiseReturningOperators = {
    every: ne,
    forEach: Q,
    reduce: ee,
    toArray: B,
    some: se,
    find: M
  }, Vn;
}
var vu, nb;
function nS() {
  if (nb) return vu;
  nb = 1;
  const { ArrayPrototypePop: t, Promise: e } = Ve(), { isIterable: r, isNodeStream: o, isWebStream: a } = ir(), { pipelineImpl: n } = Fc(), { finished: i } = gr();
  iS();
  function l(...s) {
    return new e((f, d) => {
      let y, R;
      const w = s[s.length - 1];
      if (w && typeof w == "object" && !o(w) && !r(w) && !a(w)) {
        const I = t(s);
        y = I.signal, R = I.end;
      }
      n(
        s,
        (I, A) => {
          I ? d(I) : f(A);
        },
        {
          signal: y,
          end: R
        }
      );
    });
  }
  return vu = {
    finished: i,
    pipeline: l
  }, vu;
}
var ib;
function iS() {
  if (ib) return ru.exports;
  ib = 1;
  const { Buffer: t } = Fe, { ObjectDefineProperty: e, ObjectKeys: r, ReflectApply: o } = Ve(), {
    promisify: { custom: a }
  } = gt(), { streamReturningOperators: n, promiseReturningOperators: i } = PA(), {
    codes: { ERR_ILLEGAL_CONSTRUCTOR: l }
  } = ft(), s = rS(), { setDefaultHighWaterMark: f, getDefaultHighWaterMark: d } = ns(), { pipeline: y } = Fc(), { destroyer: R } = Wr(), w = gr(), I = nS(), A = ir(), u = ru.exports = Mc().Stream;
  u.isDestroyed = A.isDestroyed, u.isDisturbed = A.isDisturbed, u.isErrored = A.isErrored, u.isReadable = A.isReadable, u.isWritable = A.isWritable, u.Readable = is();
  for (const p of r(n)) {
    let C = function(...N) {
      if (new.target)
        throw l();
      return u.Readable.from(o(b, this, N));
    };
    const b = n[p];
    e(C, "name", {
      __proto__: null,
      value: b.name
    }), e(C, "length", {
      __proto__: null,
      value: b.length
    }), e(u.Readable.prototype, p, {
      __proto__: null,
      value: C,
      enumerable: !1,
      configurable: !0,
      writable: !0
    });
  }
  for (const p of r(i)) {
    let C = function(...N) {
      if (new.target)
        throw l();
      return o(b, this, N);
    };
    const b = i[p];
    e(C, "name", {
      __proto__: null,
      value: b.name
    }), e(C, "length", {
      __proto__: null,
      value: b.length
    }), e(u.Readable.prototype, p, {
      __proto__: null,
      value: C,
      enumerable: !1,
      configurable: !0,
      writable: !0
    });
  }
  u.Writable = kc(), u.Duplex = Jt(), u.Transform = eS(), u.PassThrough = tS(), u.pipeline = y;
  const { addAbortSignal: g } = rs();
  return u.addAbortSignal = g, u.finished = w, u.destroy = R, u.compose = s, u.setDefaultHighWaterMark = f, u.getDefaultHighWaterMark = d, e(u, "promises", {
    __proto__: null,
    configurable: !0,
    enumerable: !0,
    get() {
      return I;
    }
  }), e(y, a, {
    __proto__: null,
    enumerable: !0,
    get() {
      return I.pipeline;
    }
  }), e(w, a, {
    __proto__: null,
    enumerable: !0,
    get() {
      return I.finished;
    }
  }), u.Stream = u, u._isUint8Array = function(b) {
    return b instanceof Uint8Array;
  }, u._uint8ArrayToBuffer = function(b) {
    return t.from(b.buffer, b.byteOffset, b.byteLength);
  }, ru.exports;
}
oi.exports;
var sb;
function sS() {
  return sb || (sb = 1, (function(t) {
    const e = tr;
    if (e && process.env.READABLE_STREAM === "disable") {
      const r = e.promises;
      t.exports._uint8ArrayToBuffer = e._uint8ArrayToBuffer, t.exports._isUint8Array = e._isUint8Array, t.exports.isDisturbed = e.isDisturbed, t.exports.isErrored = e.isErrored, t.exports.isReadable = e.isReadable, t.exports.Readable = e.Readable, t.exports.Writable = e.Writable, t.exports.Duplex = e.Duplex, t.exports.Transform = e.Transform, t.exports.PassThrough = e.PassThrough, t.exports.addAbortSignal = e.addAbortSignal, t.exports.finished = e.finished, t.exports.destroy = e.destroy, t.exports.pipeline = e.pipeline, t.exports.compose = e.compose, Object.defineProperty(e, "promises", {
        configurable: !0,
        enumerable: !0,
        get() {
          return r;
        }
      }), t.exports.Stream = e.Stream;
    } else {
      const r = iS(), o = nS(), a = r.Readable.destroy;
      t.exports = r.Readable, t.exports._uint8ArrayToBuffer = r._uint8ArrayToBuffer, t.exports._isUint8Array = r._isUint8Array, t.exports.isDisturbed = r.isDisturbed, t.exports.isErrored = r.isErrored, t.exports.isReadable = r.isReadable, t.exports.Readable = r.Readable, t.exports.Writable = r.Writable, t.exports.Duplex = r.Duplex, t.exports.Transform = r.Transform, t.exports.PassThrough = r.PassThrough, t.exports.addAbortSignal = r.addAbortSignal, t.exports.finished = r.finished, t.exports.destroy = r.destroy, t.exports.destroy = a, t.exports.pipeline = r.pipeline, t.exports.compose = r.compose, Object.defineProperty(r, "promises", {
        configurable: !0,
        enumerable: !0,
        get() {
          return o;
        }
      }), t.exports.Stream = r.Stream;
    }
    t.exports.default = t.exports;
  })(oi)), oi.exports;
}
var Ru, ob;
function CA() {
  if (ob) return Ru;
  ob = 1;
  var t = Lt, e = xv(), r = k0, o = Ue, a = Tn(), n = It.inherits, i = OA(), l = sS().Transform, s = process.platform === "win32", f = function(d, y) {
    if (!(this instanceof f))
      return new f(d, y);
    typeof d != "string" && (y = d, d = "zip"), y = this.options = a.defaults(y, {
      highWaterMark: 1024 * 1024,
      statConcurrency: 4
    }), l.call(this, y), this._format = !1, this._module = !1, this._pending = 0, this._pointer = 0, this._entriesCount = 0, this._entriesProcessedCount = 0, this._fsEntriesTotalBytes = 0, this._fsEntriesProcessedBytes = 0, this._queue = r.queue(this._onQueueTask.bind(this), 1), this._queue.drain(this._onQueueDrain.bind(this)), this._statQueue = r.queue(this._onStatQueueTask.bind(this), y.statConcurrency), this._statQueue.drain(this._onQueueDrain.bind(this)), this._state = {
      aborted: !1,
      finalize: !1,
      finalizing: !1,
      finalized: !1,
      modulePiped: !1
    }, this._streams = [];
  };
  return n(f, l), f.prototype._abort = function() {
    this._state.aborted = !0, this._queue.kill(), this._statQueue.kill(), this._queue.idle() && this._shutdown();
  }, f.prototype._append = function(d, y) {
    y = y || {};
    var R = {
      source: null,
      filepath: d
    };
    y.name || (y.name = d), y.sourcePath = d, R.data = y, this._entriesCount++, y.stats && y.stats instanceof t.Stats ? (R = this._updateQueueTaskWithStats(R, y.stats), R && (y.stats.size && (this._fsEntriesTotalBytes += y.stats.size), this._queue.push(R))) : this._statQueue.push(R);
  }, f.prototype._finalize = function() {
    this._state.finalizing || this._state.finalized || this._state.aborted || (this._state.finalizing = !0, this._moduleFinalize(), this._state.finalizing = !1, this._state.finalized = !0);
  }, f.prototype._maybeFinalize = function() {
    return this._state.finalizing || this._state.finalized || this._state.aborted ? !1 : this._state.finalize && this._pending === 0 && this._queue.idle() && this._statQueue.idle() ? (this._finalize(), !0) : !1;
  }, f.prototype._moduleAppend = function(d, y, R) {
    if (this._state.aborted) {
      R();
      return;
    }
    this._module.append(d, y, (function(w) {
      if (this._task = null, this._state.aborted) {
        this._shutdown();
        return;
      }
      if (w) {
        this.emit("error", w), setImmediate(R);
        return;
      }
      this.emit("entry", y), this._entriesProcessedCount++, y.stats && y.stats.size && (this._fsEntriesProcessedBytes += y.stats.size), this.emit("progress", {
        entries: {
          total: this._entriesCount,
          processed: this._entriesProcessedCount
        },
        fs: {
          totalBytes: this._fsEntriesTotalBytes,
          processedBytes: this._fsEntriesProcessedBytes
        }
      }), setImmediate(R);
    }).bind(this));
  }, f.prototype._moduleFinalize = function() {
    typeof this._module.finalize == "function" ? this._module.finalize() : typeof this._module.end == "function" ? this._module.end() : this.emit("error", new i("NOENDMETHOD"));
  }, f.prototype._modulePipe = function() {
    this._module.on("error", this._onModuleError.bind(this)), this._module.pipe(this), this._state.modulePiped = !0;
  }, f.prototype._moduleSupports = function(d) {
    return !this._module.supports || !this._module.supports[d] ? !1 : this._module.supports[d];
  }, f.prototype._moduleUnpipe = function() {
    this._module.unpipe(this), this._state.modulePiped = !1;
  }, f.prototype._normalizeEntryData = function(d, y) {
    d = a.defaults(d, {
      type: "file",
      name: null,
      date: null,
      mode: null,
      prefix: null,
      sourcePath: null,
      stats: !1
    }), y && d.stats === !1 && (d.stats = y);
    var R = d.type === "directory";
    return d.name && (typeof d.prefix == "string" && d.prefix !== "" && (d.name = d.prefix + "/" + d.name, d.prefix = null), d.name = a.sanitizePath(d.name), d.type !== "symlink" && d.name.slice(-1) === "/" ? (R = !0, d.type = "directory") : R && (d.name += "/")), typeof d.mode == "number" ? s ? d.mode &= 511 : d.mode &= 4095 : d.stats && d.mode === null ? (s ? d.mode = d.stats.mode & 511 : d.mode = d.stats.mode & 4095, s && R && (d.mode = 493)) : d.mode === null && (d.mode = R ? 493 : 420), d.stats && d.date === null ? d.date = d.stats.mtime : d.date = a.dateify(d.date), d;
  }, f.prototype._onModuleError = function(d) {
    this.emit("error", d);
  }, f.prototype._onQueueDrain = function() {
    this._state.finalizing || this._state.finalized || this._state.aborted || this._state.finalize && this._pending === 0 && this._queue.idle() && this._statQueue.idle() && this._finalize();
  }, f.prototype._onQueueTask = function(d, y) {
    var R = () => {
      d.data.callback && d.data.callback(), y();
    };
    if (this._state.finalizing || this._state.finalized || this._state.aborted) {
      R();
      return;
    }
    this._task = d, this._moduleAppend(d.source, d.data, R);
  }, f.prototype._onStatQueueTask = function(d, y) {
    if (this._state.finalizing || this._state.finalized || this._state.aborted) {
      y();
      return;
    }
    t.lstat(d.filepath, (function(R, w) {
      if (this._state.aborted) {
        setImmediate(y);
        return;
      }
      if (R) {
        this._entriesCount--, this.emit("warning", R), setImmediate(y);
        return;
      }
      d = this._updateQueueTaskWithStats(d, w), d && (w.size && (this._fsEntriesTotalBytes += w.size), this._queue.push(d)), setImmediate(y);
    }).bind(this));
  }, f.prototype._shutdown = function() {
    this._moduleUnpipe(), this.end();
  }, f.prototype._transform = function(d, y, R) {
    d && (this._pointer += d.length), R(null, d);
  }, f.prototype._updateQueueTaskWithStats = function(d, y) {
    if (y.isFile())
      d.data.type = "file", d.data.sourceType = "stream", d.source = a.lazyReadStream(d.filepath);
    else if (y.isDirectory() && this._moduleSupports("directory"))
      d.data.name = a.trailingSlashIt(d.data.name), d.data.type = "directory", d.data.sourcePath = a.trailingSlashIt(d.filepath), d.data.sourceType = "buffer", d.source = Buffer.concat([]);
    else if (y.isSymbolicLink() && this._moduleSupports("symlink")) {
      var R = t.readlinkSync(d.filepath), w = o.dirname(d.filepath);
      d.data.type = "symlink", d.data.linkname = o.relative(w, o.resolve(w, R)), d.data.sourceType = "buffer", d.source = Buffer.concat([]);
    } else
      return y.isDirectory() ? this.emit("warning", new i("DIRECTORYNOTSUPPORTED", d.data)) : y.isSymbolicLink() ? this.emit("warning", new i("SYMLINKNOTSUPPORTED", d.data)) : this.emit("warning", new i("ENTRYNOTSUPPORTED", d.data)), null;
    return d.data = this._normalizeEntryData(d.data, y), d;
  }, f.prototype.abort = function() {
    return this._state.aborted || this._state.finalized ? this : (this._abort(), this);
  }, f.prototype.append = function(d, y) {
    if (this._state.finalize || this._state.aborted)
      return this.emit("error", new i("QUEUECLOSED")), this;
    if (y = this._normalizeEntryData(y), typeof y.name != "string" || y.name.length === 0)
      return this.emit("error", new i("ENTRYNAMEREQUIRED")), this;
    if (y.type === "directory" && !this._moduleSupports("directory"))
      return this.emit("error", new i("DIRECTORYNOTSUPPORTED", { name: y.name })), this;
    if (d = a.normalizeInputSource(d), Buffer.isBuffer(d))
      y.sourceType = "buffer";
    else if (a.isStream(d))
      y.sourceType = "stream";
    else
      return this.emit("error", new i("INPUTSTEAMBUFFERREQUIRED", { name: y.name })), this;
    return this._entriesCount++, this._queue.push({
      data: y,
      source: d
    }), this;
  }, f.prototype.directory = function(d, y, R) {
    if (this._state.finalize || this._state.aborted)
      return this.emit("error", new i("QUEUECLOSED")), this;
    if (typeof d != "string" || d.length === 0)
      return this.emit("error", new i("DIRECTORYDIRPATHREQUIRED")), this;
    this._pending++, y === !1 ? y = "" : typeof y != "string" && (y = d);
    var w = !1;
    typeof R == "function" ? (w = R, R = {}) : typeof R != "object" && (R = {});
    var I = {
      stat: !0,
      dot: !0
    };
    function A() {
      this._pending--, this._maybeFinalize();
    }
    function u(b) {
      this.emit("error", b);
    }
    function g(b) {
      p.pause();
      var C = !1, N = Object.assign({}, R);
      N.name = b.relative, N.prefix = y, N.stats = b.stat, N.callback = p.resume.bind(p);
      try {
        if (w) {
          if (N = w(N), N === !1)
            C = !0;
          else if (typeof N != "object")
            throw new i("DIRECTORYFUNCTIONINVALIDDATA", { dirpath: d });
        }
      } catch (W) {
        this.emit("error", W);
        return;
      }
      if (C) {
        p.resume();
        return;
      }
      this._append(b.absolute, N);
    }
    var p = e(d, I);
    return p.on("error", u.bind(this)), p.on("match", g.bind(this)), p.on("end", A.bind(this)), this;
  }, f.prototype.file = function(d, y) {
    return this._state.finalize || this._state.aborted ? (this.emit("error", new i("QUEUECLOSED")), this) : typeof d != "string" || d.length === 0 ? (this.emit("error", new i("FILEFILEPATHREQUIRED")), this) : (this._append(d, y), this);
  }, f.prototype.glob = function(d, y, R) {
    this._pending++, y = a.defaults(y, {
      stat: !0,
      pattern: d
    });
    function w() {
      this._pending--, this._maybeFinalize();
    }
    function I(g) {
      this.emit("error", g);
    }
    function A(g) {
      u.pause();
      var p = Object.assign({}, R);
      p.callback = u.resume.bind(u), p.stats = g.stat, p.name = g.relative, this._append(g.absolute, p);
    }
    var u = e(y.cwd || ".", y);
    return u.on("error", I.bind(this)), u.on("match", A.bind(this)), u.on("end", w.bind(this)), this;
  }, f.prototype.finalize = function() {
    if (this._state.aborted) {
      var d = new i("ABORTED");
      return this.emit("error", d), Promise.reject(d);
    }
    if (this._state.finalize) {
      var y = new i("FINALIZING");
      return this.emit("error", y), Promise.reject(y);
    }
    this._state.finalize = !0, this._pending === 0 && this._queue.idle() && this._statQueue.idle() && this._finalize();
    var R = this;
    return new Promise(function(w, I) {
      var A;
      R._module.on("end", function() {
        A || w();
      }), R._module.on("error", function(u) {
        A = !0, I(u);
      });
    });
  }, f.prototype.setFormat = function(d) {
    return this._format ? (this.emit("error", new i("FORMATSET")), this) : (this._format = d, this);
  }, f.prototype.setModule = function(d) {
    return this._state.aborted ? (this.emit("error", new i("ABORTED")), this) : this._state.module ? (this.emit("error", new i("MODULESET")), this) : (this._module = d, this._modulePipe(), this);
  }, f.prototype.symlink = function(d, y, R) {
    if (this._state.finalize || this._state.aborted)
      return this.emit("error", new i("QUEUECLOSED")), this;
    if (typeof d != "string" || d.length === 0)
      return this.emit("error", new i("SYMLINKFILEPATHREQUIRED")), this;
    if (typeof y != "string" || y.length === 0)
      return this.emit("error", new i("SYMLINKTARGETREQUIRED", { filepath: d })), this;
    if (!this._moduleSupports("symlink"))
      return this.emit("error", new i("SYMLINKNOTSUPPORTED", { filepath: d })), this;
    var w = {};
    return w.type = "symlink", w.name = d.replace(/\\/g, "/"), w.linkname = y.replace(/\\/g, "/"), w.sourceType = "buffer", typeof R == "number" && (w.mode = R), this._entriesCount++, this._queue.push({
      data: w,
      source: Buffer.concat([])
    }), this;
  }, f.prototype.pointer = function() {
    return this._pointer;
  }, f.prototype.use = function(d) {
    return this._streams.push(d), this;
  }, Ru = f, Ru;
}
var Au = { exports: {} }, Tu = { exports: {} }, ab;
function jc() {
  if (ab) return Tu.exports;
  ab = 1;
  var t = Tu.exports = function() {
  };
  return t.prototype.getName = function() {
  }, t.prototype.getSize = function() {
  }, t.prototype.getLastModifiedDate = function() {
  }, t.prototype.isDirectory = function() {
  }, Tu.exports;
}
var xu = { exports: {} }, Iu = { exports: {} }, Du = { exports: {} }, lb;
function $c() {
  if (lb) return Du.exports;
  lb = 1;
  var t = Du.exports = {};
  return t.dateToDos = function(e, r) {
    r = r || !1;
    var o = r ? e.getFullYear() : e.getUTCFullYear();
    if (o < 1980)
      return 2162688;
    if (o >= 2044)
      return 2141175677;
    var a = {
      year: o,
      month: r ? e.getMonth() : e.getUTCMonth(),
      date: r ? e.getDate() : e.getUTCDate(),
      hours: r ? e.getHours() : e.getUTCHours(),
      minutes: r ? e.getMinutes() : e.getUTCMinutes(),
      seconds: r ? e.getSeconds() : e.getUTCSeconds()
    };
    return a.year - 1980 << 25 | a.month + 1 << 21 | a.date << 16 | a.hours << 11 | a.minutes << 5 | a.seconds / 2;
  }, t.dosToDate = function(e) {
    return new Date((e >> 25 & 127) + 1980, (e >> 21 & 15) - 1, e >> 16 & 31, e >> 11 & 31, e >> 5 & 63, (e & 31) << 1);
  }, t.fromDosTime = function(e) {
    return t.dosToDate(e.readUInt32LE(0));
  }, t.getEightBytes = function(e) {
    var r = Buffer.alloc(8);
    return r.writeUInt32LE(e % 4294967296, 0), r.writeUInt32LE(e / 4294967296 | 0, 4), r;
  }, t.getShortBytes = function(e) {
    var r = Buffer.alloc(2);
    return r.writeUInt16LE((e & 65535) >>> 0, 0), r;
  }, t.getShortBytesValue = function(e, r) {
    return e.readUInt16LE(r);
  }, t.getLongBytes = function(e) {
    var r = Buffer.alloc(4);
    return r.writeUInt32LE((e & 4294967295) >>> 0, 0), r;
  }, t.getLongBytesValue = function(e, r) {
    return e.readUInt32LE(r);
  }, t.toDosTime = function(e) {
    return t.getLongBytes(t.dateToDos(e));
  }, Du.exports;
}
var ub;
function oS() {
  if (ub) return Iu.exports;
  ub = 1;
  var t = $c(), e = 8, r = 1, o = 4, a = 2, n = 64, i = 2048, l = Iu.exports = function() {
    return this instanceof l ? (this.descriptor = !1, this.encryption = !1, this.utf8 = !1, this.numberOfShannonFanoTrees = 0, this.strongEncryption = !1, this.slidingDictionarySize = 0, this) : new l();
  };
  return l.prototype.encode = function() {
    return t.getShortBytes(
      (this.descriptor ? e : 0) | (this.utf8 ? i : 0) | (this.encryption ? r : 0) | (this.strongEncryption ? n : 0)
    );
  }, l.prototype.parse = function(s, f) {
    var d = t.getShortBytesValue(s, f), y = new l();
    return y.useDataDescriptor((d & e) !== 0), y.useUTF8ForNames((d & i) !== 0), y.useStrongEncryption((d & n) !== 0), y.useEncryption((d & r) !== 0), y.setSlidingDictionarySize((d & a) !== 0 ? 8192 : 4096), y.setNumberOfShannonFanoTrees((d & o) !== 0 ? 3 : 2), y;
  }, l.prototype.setNumberOfShannonFanoTrees = function(s) {
    this.numberOfShannonFanoTrees = s;
  }, l.prototype.getNumberOfShannonFanoTrees = function() {
    return this.numberOfShannonFanoTrees;
  }, l.prototype.setSlidingDictionarySize = function(s) {
    this.slidingDictionarySize = s;
  }, l.prototype.getSlidingDictionarySize = function() {
    return this.slidingDictionarySize;
  }, l.prototype.useDataDescriptor = function(s) {
    this.descriptor = s;
  }, l.prototype.usesDataDescriptor = function() {
    return this.descriptor;
  }, l.prototype.useEncryption = function(s) {
    this.encryption = s;
  }, l.prototype.usesEncryption = function() {
    return this.encryption;
  }, l.prototype.useStrongEncryption = function(s) {
    this.strongEncryption = s;
  }, l.prototype.usesStrongEncryption = function() {
    return this.strongEncryption;
  }, l.prototype.useUTF8ForNames = function(s) {
    this.utf8 = s;
  }, l.prototype.usesUTF8ForNames = function() {
    return this.utf8;
  }, Iu.exports;
}
var Ou, fb;
function MA() {
  return fb || (fb = 1, Ou = {
    /**
     * Bits used for permissions (and sticky bit)
     */
    PERM_MASK: 4095,
    // 07777
    /**
     * Bits used to indicate the filesystem object type.
     */
    FILE_TYPE_FLAG: 61440,
    // 0170000
    /**
     * Indicates symbolic links.
     */
    LINK_FLAG: 40960,
    // 0120000
    /**
     * Indicates plain files.
     */
    FILE_FLAG: 32768,
    // 0100000
    /**
     * Indicates directories.
     */
    DIR_FLAG: 16384,
    // 040000
    // ----------------------------------------------------------
    // somewhat arbitrary choices that are quite common for shared
    // installations
    // -----------------------------------------------------------
    /**
     * Default permissions for symbolic links.
     */
    DEFAULT_LINK_PERM: 511,
    // 0777
    /**
     * Default permissions for directories.
     */
    DEFAULT_DIR_PERM: 493,
    // 0755
    /**
     * Default permissions for plain files.
     */
    DEFAULT_FILE_PERM: 420
    // 0644
  }), Ou;
}
var Nu, cb;
function aS() {
  return cb || (cb = 1, Nu = {
    WORD: 4,
    DWORD: 8,
    EMPTY: Buffer.alloc(0),
    SHORT: 2,
    SHORT_MASK: 65535,
    SHORT_SHIFT: 16,
    SHORT_ZERO: Buffer.from(Array(2)),
    LONG: 4,
    LONG_ZERO: Buffer.from(Array(4)),
    MIN_VERSION_INITIAL: 10,
    MIN_VERSION_DATA_DESCRIPTOR: 20,
    MIN_VERSION_ZIP64: 45,
    VERSION_MADEBY: 45,
    METHOD_STORED: 0,
    METHOD_DEFLATED: 8,
    PLATFORM_UNIX: 3,
    PLATFORM_FAT: 0,
    SIG_LFH: 67324752,
    SIG_DD: 134695760,
    SIG_CFH: 33639248,
    SIG_EOCD: 101010256,
    SIG_ZIP64_EOCD: 101075792,
    SIG_ZIP64_EOCD_LOC: 117853008,
    ZIP64_MAGIC_SHORT: 65535,
    ZIP64_MAGIC: 4294967295,
    ZIP64_EXTRA_ID: 1,
    ZLIB_NO_COMPRESSION: 0,
    ZLIB_BEST_SPEED: 1,
    ZLIB_BEST_COMPRESSION: 9,
    ZLIB_DEFAULT_COMPRESSION: -1,
    MODE_MASK: 4095,
    DEFAULT_FILE_MODE: 33188,
    // 010644 = -rw-r--r-- = S_IFREG | S_IRUSR | S_IWUSR | S_IRGRP | S_IROTH
    DEFAULT_DIR_MODE: 16877,
    // 040755 = drwxr-xr-x = S_IFDIR | S_IRWXU | S_IRGRP | S_IXGRP | S_IROTH | S_IXOTH
    EXT_FILE_ATTR_DIR: 1106051088,
    // 010173200020 = drwxr-xr-x = (((S_IFDIR | 0755) << 16) | S_DOS_D)
    EXT_FILE_ATTR_FILE: 2175008800,
    // 020151000040 = -rw-r--r-- = (((S_IFREG | 0644) << 16) | S_DOS_A) >>> 0
    // Unix file types
    S_IFMT: 61440,
    // 0170000 type of file mask
    S_IFIFO: 4096,
    // 010000 named pipe (fifo)
    S_IFCHR: 8192,
    // 020000 character special
    S_IFDIR: 16384,
    // 040000 directory
    S_IFBLK: 24576,
    // 060000 block special
    S_IFREG: 32768,
    // 0100000 regular
    S_IFLNK: 40960,
    // 0120000 symbolic link
    S_IFSOCK: 49152,
    // 0140000 socket
    // DOS file type flags
    S_DOS_A: 32,
    // 040 Archive
    S_DOS_D: 16,
    // 020 Directory
    S_DOS_V: 8,
    // 010 Volume
    S_DOS_S: 4,
    // 04 System
    S_DOS_H: 2,
    // 02 Hidden
    S_DOS_R: 1
    // 01 Read Only
  }), Nu;
}
var db;
function lS() {
  if (db) return xu.exports;
  db = 1;
  var t = It.inherits, e = Sm(), r = jc(), o = oS(), a = MA(), n = aS(), i = $c(), l = xu.exports = function(s) {
    if (!(this instanceof l))
      return new l(s);
    r.call(this), this.platform = n.PLATFORM_FAT, this.method = -1, this.name = null, this.size = 0, this.csize = 0, this.gpb = new o(), this.crc = 0, this.time = -1, this.minver = n.MIN_VERSION_INITIAL, this.mode = -1, this.extra = null, this.exattr = 0, this.inattr = 0, this.comment = null, s && this.setName(s);
  };
  return t(l, r), l.prototype.getCentralDirectoryExtra = function() {
    return this.getExtra();
  }, l.prototype.getComment = function() {
    return this.comment !== null ? this.comment : "";
  }, l.prototype.getCompressedSize = function() {
    return this.csize;
  }, l.prototype.getCrc = function() {
    return this.crc;
  }, l.prototype.getExternalAttributes = function() {
    return this.exattr;
  }, l.prototype.getExtra = function() {
    return this.extra !== null ? this.extra : n.EMPTY;
  }, l.prototype.getGeneralPurposeBit = function() {
    return this.gpb;
  }, l.prototype.getInternalAttributes = function() {
    return this.inattr;
  }, l.prototype.getLastModifiedDate = function() {
    return this.getTime();
  }, l.prototype.getLocalFileDataExtra = function() {
    return this.getExtra();
  }, l.prototype.getMethod = function() {
    return this.method;
  }, l.prototype.getName = function() {
    return this.name;
  }, l.prototype.getPlatform = function() {
    return this.platform;
  }, l.prototype.getSize = function() {
    return this.size;
  }, l.prototype.getTime = function() {
    return this.time !== -1 ? i.dosToDate(this.time) : -1;
  }, l.prototype.getTimeDos = function() {
    return this.time !== -1 ? this.time : 0;
  }, l.prototype.getUnixMode = function() {
    return this.platform !== n.PLATFORM_UNIX ? 0 : this.getExternalAttributes() >> n.SHORT_SHIFT & n.SHORT_MASK;
  }, l.prototype.getVersionNeededToExtract = function() {
    return this.minver;
  }, l.prototype.setComment = function(s) {
    Buffer.byteLength(s) !== s.length && this.getGeneralPurposeBit().useUTF8ForNames(!0), this.comment = s;
  }, l.prototype.setCompressedSize = function(s) {
    if (s < 0)
      throw new Error("invalid entry compressed size");
    this.csize = s;
  }, l.prototype.setCrc = function(s) {
    if (s < 0)
      throw new Error("invalid entry crc32");
    this.crc = s;
  }, l.prototype.setExternalAttributes = function(s) {
    this.exattr = s >>> 0;
  }, l.prototype.setExtra = function(s) {
    this.extra = s;
  }, l.prototype.setGeneralPurposeBit = function(s) {
    if (!(s instanceof o))
      throw new Error("invalid entry GeneralPurposeBit");
    this.gpb = s;
  }, l.prototype.setInternalAttributes = function(s) {
    this.inattr = s;
  }, l.prototype.setMethod = function(s) {
    if (s < 0)
      throw new Error("invalid entry compression method");
    this.method = s;
  }, l.prototype.setName = function(s, f = !1) {
    s = e(s, !1).replace(/^\w+:/, "").replace(/^(\.\.\/|\/)+/, ""), f && (s = `/${s}`), Buffer.byteLength(s) !== s.length && this.getGeneralPurposeBit().useUTF8ForNames(!0), this.name = s;
  }, l.prototype.setPlatform = function(s) {
    this.platform = s;
  }, l.prototype.setSize = function(s) {
    if (s < 0)
      throw new Error("invalid entry size");
    this.size = s;
  }, l.prototype.setTime = function(s, f) {
    if (!(s instanceof Date))
      throw new Error("invalid entry time");
    this.time = i.dateToDos(s, f);
  }, l.prototype.setUnixMode = function(s) {
    s |= this.isDirectory() ? n.S_IFDIR : n.S_IFREG;
    var f = 0;
    f |= s << n.SHORT_SHIFT | (this.isDirectory() ? n.S_DOS_D : n.S_DOS_A), this.setExternalAttributes(f), this.mode = s & n.MODE_MASK, this.platform = n.PLATFORM_UNIX;
  }, l.prototype.setVersionNeededToExtract = function(s) {
    this.minver = s;
  }, l.prototype.isDirectory = function() {
    return this.getName().slice(-1) === "/";
  }, l.prototype.isUnixSymlink = function() {
    return (this.getUnixMode() & a.FILE_TYPE_FLAG) === a.LINK_FLAG;
  }, l.prototype.isZip64 = function() {
    return this.csize > n.ZIP64_MAGIC || this.size > n.ZIP64_MAGIC;
  }, xu.exports;
}
var Lu = { exports: {} }, Pu, hb;
function uS() {
  if (hb) return Pu;
  hb = 1;
  const t = (e) => e !== null && typeof e == "object" && typeof e.pipe == "function";
  return t.writable = (e) => t(e) && e.writable !== !1 && typeof e._write == "function" && typeof e._writableState == "object", t.readable = (e) => t(e) && e.readable !== !1 && typeof e._read == "function" && typeof e._readableState == "object", t.duplex = (e) => t.writable(e) && t.readable(e), t.transform = (e) => t.duplex(e) && typeof e._transform == "function", Pu = t, Pu;
}
var ai = { exports: {} }, Cu = { exports: {} }, Mu, pb;
function Ze() {
  if (pb) return Mu;
  pb = 1;
  class t extends Error {
    constructor(r) {
      if (!Array.isArray(r))
        throw new TypeError(`Expected input to be an Array, got ${typeof r}`);
      let o = "";
      for (let a = 0; a < r.length; a++)
        o += `    ${r[a].stack}
`;
      super(o), this.name = "AggregateError", this.errors = r;
    }
  }
  return Mu = {
    AggregateError: t,
    ArrayIsArray(e) {
      return Array.isArray(e);
    },
    ArrayPrototypeIncludes(e, r) {
      return e.includes(r);
    },
    ArrayPrototypeIndexOf(e, r) {
      return e.indexOf(r);
    },
    ArrayPrototypeJoin(e, r) {
      return e.join(r);
    },
    ArrayPrototypeMap(e, r) {
      return e.map(r);
    },
    ArrayPrototypePop(e, r) {
      return e.pop(r);
    },
    ArrayPrototypePush(e, r) {
      return e.push(r);
    },
    ArrayPrototypeSlice(e, r, o) {
      return e.slice(r, o);
    },
    Error,
    FunctionPrototypeCall(e, r, ...o) {
      return e.call(r, ...o);
    },
    FunctionPrototypeSymbolHasInstance(e, r) {
      return Function.prototype[Symbol.hasInstance].call(e, r);
    },
    MathFloor: Math.floor,
    Number,
    NumberIsInteger: Number.isInteger,
    NumberIsNaN: Number.isNaN,
    NumberMAX_SAFE_INTEGER: Number.MAX_SAFE_INTEGER,
    NumberMIN_SAFE_INTEGER: Number.MIN_SAFE_INTEGER,
    NumberParseInt: Number.parseInt,
    ObjectDefineProperties(e, r) {
      return Object.defineProperties(e, r);
    },
    ObjectDefineProperty(e, r, o) {
      return Object.defineProperty(e, r, o);
    },
    ObjectGetOwnPropertyDescriptor(e, r) {
      return Object.getOwnPropertyDescriptor(e, r);
    },
    ObjectKeys(e) {
      return Object.keys(e);
    },
    ObjectSetPrototypeOf(e, r) {
      return Object.setPrototypeOf(e, r);
    },
    Promise,
    PromisePrototypeCatch(e, r) {
      return e.catch(r);
    },
    PromisePrototypeThen(e, r, o) {
      return e.then(r, o);
    },
    PromiseReject(e) {
      return Promise.reject(e);
    },
    PromiseResolve(e) {
      return Promise.resolve(e);
    },
    ReflectApply: Reflect.apply,
    RegExpPrototypeTest(e, r) {
      return e.test(r);
    },
    SafeSet: Set,
    String,
    StringPrototypeSlice(e, r, o) {
      return e.slice(r, o);
    },
    StringPrototypeToLowerCase(e) {
      return e.toLowerCase();
    },
    StringPrototypeToUpperCase(e) {
      return e.toUpperCase();
    },
    StringPrototypeTrim(e) {
      return e.trim();
    },
    Symbol,
    SymbolFor: Symbol.for,
    SymbolAsyncIterator: Symbol.asyncIterator,
    SymbolHasInstance: Symbol.hasInstance,
    SymbolIterator: Symbol.iterator,
    SymbolDispose: Symbol.dispose || /* @__PURE__ */ Symbol("Symbol.dispose"),
    SymbolAsyncDispose: Symbol.asyncDispose || /* @__PURE__ */ Symbol("Symbol.asyncDispose"),
    TypedArrayPrototypeSet(e, r, o) {
      return e.set(r, o);
    },
    Boolean,
    Uint8Array
  }, Mu;
}
var ku = { exports: {} }, Fu, gb;
function fS() {
  return gb || (gb = 1, Fu = {
    format(t, ...e) {
      return t.replace(/%([sdifj])/g, function(...[r, o]) {
        const a = e.shift();
        return o === "f" ? a.toFixed(6) : o === "j" ? JSON.stringify(a) : o === "s" && typeof a == "object" ? `${a.constructor !== Object ? a.constructor.name : ""} {}`.trim() : a.toString();
      });
    },
    inspect(t) {
      switch (typeof t) {
        case "string":
          if (t.includes("'"))
            if (t.includes('"')) {
              if (!t.includes("`") && !t.includes("${"))
                return `\`${t}\``;
            } else return `"${t}"`;
          return `'${t}'`;
        case "number":
          return isNaN(t) ? "NaN" : Object.is(t, -0) ? String(t) : t;
        case "bigint":
          return `${String(t)}n`;
        case "boolean":
        case "undefined":
          return String(t);
        case "object":
          return "{}";
      }
    }
  }), Fu;
}
var ju, yb;
function ct() {
  if (yb) return ju;
  yb = 1;
  const { format: t, inspect: e } = fS(), { AggregateError: r } = Ze(), o = globalThis.AggregateError || r, a = /* @__PURE__ */ Symbol("kIsNodeError"), n = [
    "string",
    "function",
    "number",
    "object",
    // Accept 'Function' and 'Object' as alternative to the lower cased version.
    "Function",
    "Object",
    "boolean",
    "bigint",
    "symbol"
  ], i = /^([A-Z][a-z0-9]*)+$/, l = "__node_internal_", s = {};
  function f(u, g) {
    if (!u)
      throw new s.ERR_INTERNAL_ASSERTION(g);
  }
  function d(u) {
    let g = "", p = u.length;
    const b = u[0] === "-" ? 1 : 0;
    for (; p >= b + 4; p -= 3)
      g = `_${u.slice(p - 3, p)}${g}`;
    return `${u.slice(0, p)}${g}`;
  }
  function y(u, g, p) {
    if (typeof g == "function")
      return f(
        g.length <= p.length,
        // Default options do not count.
        `Code: ${u}; The provided arguments length (${p.length}) does not match the required ones (${g.length}).`
      ), g(...p);
    const b = (g.match(/%[dfijoOs]/g) || []).length;
    return f(
      b === p.length,
      `Code: ${u}; The provided arguments length (${p.length}) does not match the required ones (${b}).`
    ), p.length === 0 ? g : t(g, ...p);
  }
  function R(u, g, p) {
    p || (p = Error);
    class b extends p {
      constructor(...N) {
        super(y(u, g, N));
      }
      toString() {
        return `${this.name} [${u}]: ${this.message}`;
      }
    }
    Object.defineProperties(b.prototype, {
      name: {
        value: p.name,
        writable: !0,
        enumerable: !1,
        configurable: !0
      },
      toString: {
        value() {
          return `${this.name} [${u}]: ${this.message}`;
        },
        writable: !0,
        enumerable: !1,
        configurable: !0
      }
    }), b.prototype.code = u, b.prototype[a] = !0, s[u] = b;
  }
  function w(u) {
    const g = l + u.name;
    return Object.defineProperty(u, "name", {
      value: g
    }), u;
  }
  function I(u, g) {
    if (u && g && u !== g) {
      if (Array.isArray(g.errors))
        return g.errors.push(u), g;
      const p = new o([g, u], g.message);
      return p.code = g.code, p;
    }
    return u || g;
  }
  class A extends Error {
    constructor(g = "The operation was aborted", p = void 0) {
      if (p !== void 0 && typeof p != "object")
        throw new s.ERR_INVALID_ARG_TYPE("options", "Object", p);
      super(g, p), this.code = "ABORT_ERR", this.name = "AbortError";
    }
  }
  return R("ERR_ASSERTION", "%s", Error), R(
    "ERR_INVALID_ARG_TYPE",
    (u, g, p) => {
      f(typeof u == "string", "'name' must be a string"), Array.isArray(g) || (g = [g]);
      let b = "The ";
      u.endsWith(" argument") ? b += `${u} ` : b += `"${u}" ${u.includes(".") ? "property" : "argument"} `, b += "must be ";
      const C = [], N = [], W = [];
      for (const L of g)
        f(typeof L == "string", "All expected entries have to be of type string"), n.includes(L) ? C.push(L.toLowerCase()) : i.test(L) ? N.push(L) : (f(L !== "object", 'The value "object" should be written as "Object"'), W.push(L));
      if (N.length > 0) {
        const L = C.indexOf("object");
        L !== -1 && (C.splice(C, L, 1), N.push("Object"));
      }
      if (C.length > 0) {
        switch (C.length) {
          case 1:
            b += `of type ${C[0]}`;
            break;
          case 2:
            b += `one of type ${C[0]} or ${C[1]}`;
            break;
          default: {
            const L = C.pop();
            b += `one of type ${C.join(", ")}, or ${L}`;
          }
        }
        (N.length > 0 || W.length > 0) && (b += " or ");
      }
      if (N.length > 0) {
        switch (N.length) {
          case 1:
            b += `an instance of ${N[0]}`;
            break;
          case 2:
            b += `an instance of ${N[0]} or ${N[1]}`;
            break;
          default: {
            const L = N.pop();
            b += `an instance of ${N.join(", ")}, or ${L}`;
          }
        }
        W.length > 0 && (b += " or ");
      }
      switch (W.length) {
        case 0:
          break;
        case 1:
          W[0].toLowerCase() !== W[0] && (b += "an "), b += `${W[0]}`;
          break;
        case 2:
          b += `one of ${W[0]} or ${W[1]}`;
          break;
        default: {
          const L = W.pop();
          b += `one of ${W.join(", ")}, or ${L}`;
        }
      }
      if (p == null)
        b += `. Received ${p}`;
      else if (typeof p == "function" && p.name)
        b += `. Received function ${p.name}`;
      else if (typeof p == "object") {
        var k;
        if ((k = p.constructor) !== null && k !== void 0 && k.name)
          b += `. Received an instance of ${p.constructor.name}`;
        else {
          const L = e(p, {
            depth: -1
          });
          b += `. Received ${L}`;
        }
      } else {
        let L = e(p, {
          colors: !1
        });
        L.length > 25 && (L = `${L.slice(0, 25)}...`), b += `. Received type ${typeof p} (${L})`;
      }
      return b;
    },
    TypeError
  ), R(
    "ERR_INVALID_ARG_VALUE",
    (u, g, p = "is invalid") => {
      let b = e(g);
      return b.length > 128 && (b = b.slice(0, 128) + "..."), `The ${u.includes(".") ? "property" : "argument"} '${u}' ${p}. Received ${b}`;
    },
    TypeError
  ), R(
    "ERR_INVALID_RETURN_VALUE",
    (u, g, p) => {
      var b;
      const C = p != null && (b = p.constructor) !== null && b !== void 0 && b.name ? `instance of ${p.constructor.name}` : `type ${typeof p}`;
      return `Expected ${u} to be returned from the "${g}" function but got ${C}.`;
    },
    TypeError
  ), R(
    "ERR_MISSING_ARGS",
    (...u) => {
      f(u.length > 0, "At least one arg needs to be specified");
      let g;
      const p = u.length;
      switch (u = (Array.isArray(u) ? u : [u]).map((b) => `"${b}"`).join(" or "), p) {
        case 1:
          g += `The ${u[0]} argument`;
          break;
        case 2:
          g += `The ${u[0]} and ${u[1]} arguments`;
          break;
        default:
          {
            const b = u.pop();
            g += `The ${u.join(", ")}, and ${b} arguments`;
          }
          break;
      }
      return `${g} must be specified`;
    },
    TypeError
  ), R(
    "ERR_OUT_OF_RANGE",
    (u, g, p) => {
      f(g, 'Missing "range" argument');
      let b;
      if (Number.isInteger(p) && Math.abs(p) > 2 ** 32)
        b = d(String(p));
      else if (typeof p == "bigint") {
        b = String(p);
        const C = BigInt(2) ** BigInt(32);
        (p > C || p < -C) && (b = d(b)), b += "n";
      } else
        b = e(p);
      return `The value of "${u}" is out of range. It must be ${g}. Received ${b}`;
    },
    RangeError
  ), R("ERR_MULTIPLE_CALLBACK", "Callback called multiple times", Error), R("ERR_METHOD_NOT_IMPLEMENTED", "The %s method is not implemented", Error), R("ERR_STREAM_ALREADY_FINISHED", "Cannot call %s after a stream was finished", Error), R("ERR_STREAM_CANNOT_PIPE", "Cannot pipe, not readable", Error), R("ERR_STREAM_DESTROYED", "Cannot call %s after a stream was destroyed", Error), R("ERR_STREAM_NULL_VALUES", "May not write null values to stream", TypeError), R("ERR_STREAM_PREMATURE_CLOSE", "Premature close", Error), R("ERR_STREAM_PUSH_AFTER_EOF", "stream.push() after EOF", Error), R("ERR_STREAM_UNSHIFT_AFTER_END_EVENT", "stream.unshift() after end event", Error), R("ERR_STREAM_WRITE_AFTER_END", "write after end", Error), R("ERR_UNKNOWN_ENCODING", "Unknown encoding: %s", TypeError), ju = {
    AbortError: A,
    aggregateTwoErrors: w(I),
    hideStackFrames: w,
    codes: s
  }, ju;
}
var bb;
function yt() {
  return bb || (bb = 1, (function(t) {
    const e = Fe, { format: r, inspect: o } = fS(), {
      codes: { ERR_INVALID_ARG_TYPE: a }
    } = ct(), { kResistStopPropagation: n, AggregateError: i, SymbolDispose: l } = Ze(), s = globalThis.AbortSignal || et.AbortSignal, f = globalThis.AbortController || et.AbortController, d = Object.getPrototypeOf(async function() {
    }).constructor, y = globalThis.Blob || e.Blob, R = typeof y < "u" ? function(u) {
      return u instanceof y;
    } : function(u) {
      return !1;
    }, w = (A, u) => {
      if (A !== void 0 && (A === null || typeof A != "object" || !("aborted" in A)))
        throw new a(u, "AbortSignal", A);
    }, I = (A, u) => {
      if (typeof A != "function")
        throw new a(u, "Function", A);
    };
    t.exports = {
      AggregateError: i,
      kEmptyObject: Object.freeze({}),
      once(A) {
        let u = !1;
        return function(...g) {
          u || (u = !0, A.apply(this, g));
        };
      },
      createDeferredPromise: function() {
        let A, u;
        return {
          promise: new Promise((p, b) => {
            A = p, u = b;
          }),
          resolve: A,
          reject: u
        };
      },
      promisify(A) {
        return new Promise((u, g) => {
          A((p, ...b) => p ? g(p) : u(...b));
        });
      },
      debuglog() {
        return function() {
        };
      },
      format: r,
      inspect: o,
      types: {
        isAsyncFunction(A) {
          return A instanceof d;
        },
        isArrayBufferView(A) {
          return ArrayBuffer.isView(A);
        }
      },
      isBlob: R,
      deprecate(A, u) {
        return A;
      },
      addAbortListener: Ke.addAbortListener || function(u, g) {
        if (u === void 0)
          throw new a("signal", "AbortSignal", u);
        w(u, "signal"), I(g, "listener");
        let p;
        return u.aborted ? queueMicrotask(() => g()) : (u.addEventListener("abort", g, {
          __proto__: null,
          once: !0,
          [n]: !0
        }), p = () => {
          u.removeEventListener("abort", g);
        }), {
          __proto__: null,
          [l]() {
            var b;
            (b = p) === null || b === void 0 || b();
          }
        };
      },
      AbortSignalAny: s.any || function(u) {
        if (u.length === 1)
          return u[0];
        const g = new f(), p = () => g.abort();
        return u.forEach((b) => {
          w(b, "signals"), b.addEventListener("abort", p, {
            once: !0
          });
        }), g.signal.addEventListener(
          "abort",
          () => {
            u.forEach((b) => b.removeEventListener("abort", p));
          },
          {
            once: !0
          }
        ), g.signal;
      }
    }, t.exports.promisify.custom = /* @__PURE__ */ Symbol.for("nodejs.util.promisify.custom");
  })(ku)), ku.exports;
}
var Yn = {}, $u, _b;
function In() {
  if (_b) return $u;
  _b = 1;
  const {
    ArrayIsArray: t,
    ArrayPrototypeIncludes: e,
    ArrayPrototypeJoin: r,
    ArrayPrototypeMap: o,
    NumberIsInteger: a,
    NumberIsNaN: n,
    NumberMAX_SAFE_INTEGER: i,
    NumberMIN_SAFE_INTEGER: l,
    NumberParseInt: s,
    ObjectPrototypeHasOwnProperty: f,
    RegExpPrototypeExec: d,
    String: y,
    StringPrototypeToUpperCase: R,
    StringPrototypeTrim: w
  } = Ze(), {
    hideStackFrames: I,
    codes: { ERR_SOCKET_BAD_PORT: A, ERR_INVALID_ARG_TYPE: u, ERR_INVALID_ARG_VALUE: g, ERR_OUT_OF_RANGE: p, ERR_UNKNOWN_SIGNAL: b }
  } = ct(), { normalizeEncoding: C } = yt(), { isAsyncFunction: N, isArrayBufferView: W } = yt().types, k = {};
  function L(O) {
    return O === (O | 0);
  }
  function U(O) {
    return O === O >>> 0;
  }
  const x = /^[0-7]+$/, D = "must be a 32-bit unsigned integer or an octal string";
  function F(O, H, te) {
    if (typeof O > "u" && (O = te), typeof O == "string") {
      if (d(x, O) === null)
        throw new g(H, O, D);
      O = s(O, 8);
    }
    return re(O, H), O;
  }
  const T = I((O, H, te = l, z = i) => {
    if (typeof O != "number") throw new u(H, "number", O);
    if (!a(O)) throw new p(H, "an integer", O);
    if (O < te || O > z) throw new p(H, `>= ${te} && <= ${z}`, O);
  }), J = I((O, H, te = -2147483648, z = 2147483647) => {
    if (typeof O != "number")
      throw new u(H, "number", O);
    if (!a(O))
      throw new p(H, "an integer", O);
    if (O < te || O > z)
      throw new p(H, `>= ${te} && <= ${z}`, O);
  }), re = I((O, H, te = !1) => {
    if (typeof O != "number")
      throw new u(H, "number", O);
    if (!a(O))
      throw new p(H, "an integer", O);
    const z = te ? 1 : 0, de = 4294967295;
    if (O < z || O > de)
      throw new p(H, `>= ${z} && <= ${de}`, O);
  });
  function se(O, H) {
    if (typeof O != "string") throw new u(H, "string", O);
  }
  function ne(O, H, te = void 0, z) {
    if (typeof O != "number") throw new u(H, "number", O);
    if (te != null && O < te || z != null && O > z || (te != null || z != null) && n(O))
      throw new p(
        H,
        `${te != null ? `>= ${te}` : ""}${te != null && z != null ? " && " : ""}${z != null ? `<= ${z}` : ""}`,
        O
      );
  }
  const M = I((O, H, te) => {
    if (!e(te, O)) {
      const de = "must be one of: " + r(
        o(te, (pe) => typeof pe == "string" ? `'${pe}'` : y(pe)),
        ", "
      );
      throw new g(H, O, de);
    }
  });
  function Q(O, H) {
    if (typeof O != "boolean") throw new u(H, "boolean", O);
  }
  function v(O, H, te) {
    return O == null || !f(O, H) ? te : O[H];
  }
  const $ = I((O, H, te = null) => {
    const z = v(te, "allowArray", !1), de = v(te, "allowFunction", !1);
    if (!v(te, "nullable", !1) && O === null || !z && t(O) || typeof O != "object" && (!de || typeof O != "function"))
      throw new u(H, "Object", O);
  }), ee = I((O, H) => {
    if (O != null && typeof O != "object" && typeof O != "function")
      throw new u(H, "a dictionary", O);
  }), B = I((O, H, te = 0) => {
    if (!t(O))
      throw new u(H, "Array", O);
    if (O.length < te) {
      const z = `must be longer than ${te}`;
      throw new g(H, O, z);
    }
  });
  function le(O, H) {
    B(O, H);
    for (let te = 0; te < O.length; te++)
      se(O[te], `${H}[${te}]`);
  }
  function ae(O, H) {
    B(O, H);
    for (let te = 0; te < O.length; te++)
      Q(O[te], `${H}[${te}]`);
  }
  function ce(O, H) {
    B(O, H);
    for (let te = 0; te < O.length; te++) {
      const z = O[te], de = `${H}[${te}]`;
      if (z == null)
        throw new u(de, "AbortSignal", z);
      Z(z, de);
    }
  }
  function Y(O, H = "signal") {
    if (se(O, H), k[O] === void 0)
      throw k[R(O)] !== void 0 ? new b(O + " (signals must use all capital letters)") : new b(O);
  }
  const P = I((O, H = "buffer") => {
    if (!W(O))
      throw new u(H, ["Buffer", "TypedArray", "DataView"], O);
  });
  function q(O, H) {
    const te = C(H), z = O.length;
    if (te === "hex" && z % 2 !== 0)
      throw new g("encoding", H, `is invalid for data of length ${z}`);
  }
  function V(O, H = "Port", te = !0) {
    if (typeof O != "number" && typeof O != "string" || typeof O == "string" && w(O).length === 0 || +O !== +O >>> 0 || O > 65535 || O === 0 && !te)
      throw new A(H, O, te);
    return O | 0;
  }
  const Z = I((O, H) => {
    if (O !== void 0 && (O === null || typeof O != "object" || !("aborted" in O)))
      throw new u(H, "AbortSignal", O);
  }), X = I((O, H) => {
    if (typeof O != "function") throw new u(H, "Function", O);
  }), m = I((O, H) => {
    if (typeof O != "function" || N(O)) throw new u(H, "Function", O);
  }), c = I((O, H) => {
    if (O !== void 0) throw new u(H, "undefined", O);
  });
  function E(O, H, te) {
    if (!e(te, O))
      throw new u(H, `('${r(te, "|")}')`, O);
  }
  const j = /^(?:<[^>]*>)(?:\s*;\s*[^;"\s]+(?:=(")?[^;"\s]*\1)?)*$/;
  function G(O, H) {
    if (typeof O > "u" || !d(j, O))
      throw new g(
        H,
        O,
        'must be an array or string of format "</styles.css>; rel=preload; as=style"'
      );
  }
  function K(O) {
    if (typeof O == "string")
      return G(O, "hints"), O;
    if (t(O)) {
      const H = O.length;
      let te = "";
      if (H === 0)
        return te;
      for (let z = 0; z < H; z++) {
        const de = O[z];
        G(de, "hints"), te += de, z !== H - 1 && (te += ", ");
      }
      return te;
    }
    throw new g(
      "hints",
      O,
      'must be an array or string of format "</styles.css>; rel=preload; as=style"'
    );
  }
  return $u = {
    isInt32: L,
    isUint32: U,
    parseFileMode: F,
    validateArray: B,
    validateStringArray: le,
    validateBooleanArray: ae,
    validateAbortSignalArray: ce,
    validateBoolean: Q,
    validateBuffer: P,
    validateDictionary: ee,
    validateEncoding: q,
    validateFunction: X,
    validateInt32: J,
    validateInteger: T,
    validateNumber: ne,
    validateObject: $,
    validateOneOf: M,
    validatePlainFunction: m,
    validatePort: V,
    validateSignalName: Y,
    validateString: se,
    validateUint32: re,
    validateUndefined: c,
    validateUnion: E,
    validateAbortSignal: Z,
    validateLinkHeaderValue: K
  }, $u;
}
var Kn = { exports: {} }, Wu, wb;
function sr() {
  if (wb) return Wu;
  wb = 1;
  const { SymbolAsyncIterator: t, SymbolIterator: e, SymbolFor: r } = Ze(), o = r("nodejs.stream.destroyed"), a = r("nodejs.stream.errored"), n = r("nodejs.stream.readable"), i = r("nodejs.stream.writable"), l = r("nodejs.stream.disturbed"), s = r("nodejs.webstream.isClosedPromise"), f = r("nodejs.webstream.controllerErrorFunction");
  function d(v, $ = !1) {
    var ee;
    return !!(v && typeof v.pipe == "function" && typeof v.on == "function" && (!$ || typeof v.pause == "function" && typeof v.resume == "function") && (!v._writableState || ((ee = v._readableState) === null || ee === void 0 ? void 0 : ee.readable) !== !1) && // Duplex
    (!v._writableState || v._readableState));
  }
  function y(v) {
    var $;
    return !!(v && typeof v.write == "function" && typeof v.on == "function" && (!v._readableState || (($ = v._writableState) === null || $ === void 0 ? void 0 : $.writable) !== !1));
  }
  function R(v) {
    return !!(v && typeof v.pipe == "function" && v._readableState && typeof v.on == "function" && typeof v.write == "function");
  }
  function w(v) {
    return v && (v._readableState || v._writableState || typeof v.write == "function" && typeof v.on == "function" || typeof v.pipe == "function" && typeof v.on == "function");
  }
  function I(v) {
    return !!(v && !w(v) && typeof v.pipeThrough == "function" && typeof v.getReader == "function" && typeof v.cancel == "function");
  }
  function A(v) {
    return !!(v && !w(v) && typeof v.getWriter == "function" && typeof v.abort == "function");
  }
  function u(v) {
    return !!(v && !w(v) && typeof v.readable == "object" && typeof v.writable == "object");
  }
  function g(v) {
    return I(v) || A(v) || u(v);
  }
  function p(v, $) {
    return v == null ? !1 : $ === !0 ? typeof v[t] == "function" : $ === !1 ? typeof v[e] == "function" : typeof v[t] == "function" || typeof v[e] == "function";
  }
  function b(v) {
    if (!w(v)) return null;
    const $ = v._writableState, ee = v._readableState, B = $ || ee;
    return !!(v.destroyed || v[o] || B != null && B.destroyed);
  }
  function C(v) {
    if (!y(v)) return null;
    if (v.writableEnded === !0) return !0;
    const $ = v._writableState;
    return $ != null && $.errored ? !1 : typeof $?.ended != "boolean" ? null : $.ended;
  }
  function N(v, $) {
    if (!y(v)) return null;
    if (v.writableFinished === !0) return !0;
    const ee = v._writableState;
    return ee != null && ee.errored ? !1 : typeof ee?.finished != "boolean" ? null : !!(ee.finished || $ === !1 && ee.ended === !0 && ee.length === 0);
  }
  function W(v) {
    if (!d(v)) return null;
    if (v.readableEnded === !0) return !0;
    const $ = v._readableState;
    return !$ || $.errored ? !1 : typeof $?.ended != "boolean" ? null : $.ended;
  }
  function k(v, $) {
    if (!d(v)) return null;
    const ee = v._readableState;
    return ee != null && ee.errored ? !1 : typeof ee?.endEmitted != "boolean" ? null : !!(ee.endEmitted || $ === !1 && ee.ended === !0 && ee.length === 0);
  }
  function L(v) {
    return v && v[n] != null ? v[n] : typeof v?.readable != "boolean" ? null : b(v) ? !1 : d(v) && v.readable && !k(v);
  }
  function U(v) {
    return v && v[i] != null ? v[i] : typeof v?.writable != "boolean" ? null : b(v) ? !1 : y(v) && v.writable && !C(v);
  }
  function x(v, $) {
    return w(v) ? b(v) ? !0 : !($?.readable !== !1 && L(v) || $?.writable !== !1 && U(v)) : null;
  }
  function D(v) {
    var $, ee;
    return w(v) ? v.writableErrored ? v.writableErrored : ($ = (ee = v._writableState) === null || ee === void 0 ? void 0 : ee.errored) !== null && $ !== void 0 ? $ : null : null;
  }
  function F(v) {
    var $, ee;
    return w(v) ? v.readableErrored ? v.readableErrored : ($ = (ee = v._readableState) === null || ee === void 0 ? void 0 : ee.errored) !== null && $ !== void 0 ? $ : null : null;
  }
  function T(v) {
    if (!w(v))
      return null;
    if (typeof v.closed == "boolean")
      return v.closed;
    const $ = v._writableState, ee = v._readableState;
    return typeof $?.closed == "boolean" || typeof ee?.closed == "boolean" ? $?.closed || ee?.closed : typeof v._closed == "boolean" && J(v) ? v._closed : null;
  }
  function J(v) {
    return typeof v._closed == "boolean" && typeof v._defaultKeepAlive == "boolean" && typeof v._removedConnection == "boolean" && typeof v._removedContLen == "boolean";
  }
  function re(v) {
    return typeof v._sent100 == "boolean" && J(v);
  }
  function se(v) {
    var $;
    return typeof v._consuming == "boolean" && typeof v._dumped == "boolean" && (($ = v.req) === null || $ === void 0 ? void 0 : $.upgradeOrConnect) === void 0;
  }
  function ne(v) {
    if (!w(v)) return null;
    const $ = v._writableState, ee = v._readableState, B = $ || ee;
    return !B && re(v) || !!(B && B.autoDestroy && B.emitClose && B.closed === !1);
  }
  function M(v) {
    var $;
    return !!(v && (($ = v[l]) !== null && $ !== void 0 ? $ : v.readableDidRead || v.readableAborted));
  }
  function Q(v) {
    var $, ee, B, le, ae, ce, Y, P, q, V;
    return !!(v && (($ = (ee = (B = (le = (ae = (ce = v[a]) !== null && ce !== void 0 ? ce : v.readableErrored) !== null && ae !== void 0 ? ae : v.writableErrored) !== null && le !== void 0 ? le : (Y = v._readableState) === null || Y === void 0 ? void 0 : Y.errorEmitted) !== null && B !== void 0 ? B : (P = v._writableState) === null || P === void 0 ? void 0 : P.errorEmitted) !== null && ee !== void 0 ? ee : (q = v._readableState) === null || q === void 0 ? void 0 : q.errored) !== null && $ !== void 0 ? $ : !((V = v._writableState) === null || V === void 0) && V.errored));
  }
  return Wu = {
    isDestroyed: b,
    kIsDestroyed: o,
    isDisturbed: M,
    kIsDisturbed: l,
    isErrored: Q,
    kIsErrored: a,
    isReadable: L,
    kIsReadable: n,
    kIsClosedPromise: s,
    kControllerErrorFunction: f,
    kIsWritable: i,
    isClosed: T,
    isDuplexNodeStream: R,
    isFinished: x,
    isIterable: p,
    isReadableNodeStream: d,
    isReadableStream: I,
    isReadableEnded: W,
    isReadableFinished: k,
    isReadableErrored: F,
    isNodeStream: w,
    isWebStream: g,
    isWritable: U,
    isWritableNodeStream: y,
    isWritableStream: A,
    isWritableEnded: C,
    isWritableFinished: N,
    isWritableErrored: D,
    isServerRequest: se,
    isServerResponse: re,
    willEmitClose: ne,
    isTransformStream: u
  }, Wu;
}
var mb;
function yr() {
  if (mb) return Kn.exports;
  mb = 1;
  const t = je(), { AbortError: e, codes: r } = ct(), { ERR_INVALID_ARG_TYPE: o, ERR_STREAM_PREMATURE_CLOSE: a } = r, { kEmptyObject: n, once: i } = yt(), { validateAbortSignal: l, validateFunction: s, validateObject: f, validateBoolean: d } = In(), { Promise: y, PromisePrototypeThen: R, SymbolDispose: w } = Ze(), {
    isClosed: I,
    isReadable: A,
    isReadableNodeStream: u,
    isReadableStream: g,
    isReadableFinished: p,
    isReadableErrored: b,
    isWritable: C,
    isWritableNodeStream: N,
    isWritableStream: W,
    isWritableFinished: k,
    isWritableErrored: L,
    isNodeStream: U,
    willEmitClose: x,
    kIsClosedPromise: D
  } = sr();
  let F;
  function T(M) {
    return M.setHeader && typeof M.abort == "function";
  }
  const J = () => {
  };
  function re(M, Q, v) {
    var $, ee;
    if (arguments.length === 2 ? (v = Q, Q = n) : Q == null ? Q = n : f(Q, "options"), s(v, "callback"), l(Q.signal, "options.signal"), v = i(v), g(M) || W(M))
      return se(M, Q, v);
    if (!U(M))
      throw new o("stream", ["ReadableStream", "WritableStream", "Stream"], M);
    const B = ($ = Q.readable) !== null && $ !== void 0 ? $ : u(M), le = (ee = Q.writable) !== null && ee !== void 0 ? ee : N(M), ae = M._writableState, ce = M._readableState, Y = () => {
      M.writable || V();
    };
    let P = x(M) && u(M) === B && N(M) === le, q = k(M, !1);
    const V = () => {
      q = !0, M.destroyed && (P = !1), !(P && (!M.readable || B)) && (!B || Z) && v.call(M);
    };
    let Z = p(M, !1);
    const X = () => {
      Z = !0, M.destroyed && (P = !1), !(P && (!M.writable || le)) && (!le || q) && v.call(M);
    }, m = (O) => {
      v.call(M, O);
    };
    let c = I(M);
    const E = () => {
      c = !0;
      const O = L(M) || b(M);
      if (O && typeof O != "boolean")
        return v.call(M, O);
      if (B && !Z && u(M, !0) && !p(M, !1))
        return v.call(M, new a());
      if (le && !q && !k(M, !1))
        return v.call(M, new a());
      v.call(M);
    }, j = () => {
      c = !0;
      const O = L(M) || b(M);
      if (O && typeof O != "boolean")
        return v.call(M, O);
      v.call(M);
    }, G = () => {
      M.req.on("finish", V);
    };
    T(M) ? (M.on("complete", V), P || M.on("abort", E), M.req ? G() : M.on("request", G)) : le && !ae && (M.on("end", Y), M.on("close", Y)), !P && typeof M.aborted == "boolean" && M.on("aborted", E), M.on("end", X), M.on("finish", V), Q.error !== !1 && M.on("error", m), M.on("close", E), c ? t.nextTick(E) : ae != null && ae.errorEmitted || ce != null && ce.errorEmitted ? P || t.nextTick(j) : (!B && (!P || A(M)) && (q || C(M) === !1) || !le && (!P || C(M)) && (Z || A(M) === !1) || ce && M.req && M.aborted) && t.nextTick(j);
    const K = () => {
      v = J, M.removeListener("aborted", E), M.removeListener("complete", V), M.removeListener("abort", E), M.removeListener("request", G), M.req && M.req.removeListener("finish", V), M.removeListener("end", Y), M.removeListener("close", Y), M.removeListener("finish", V), M.removeListener("end", X), M.removeListener("error", m), M.removeListener("close", E);
    };
    if (Q.signal && !c) {
      const O = () => {
        const H = v;
        K(), H.call(
          M,
          new e(void 0, {
            cause: Q.signal.reason
          })
        );
      };
      if (Q.signal.aborted)
        t.nextTick(O);
      else {
        F = F || yt().addAbortListener;
        const H = F(Q.signal, O), te = v;
        v = i((...z) => {
          H[w](), te.apply(M, z);
        });
      }
    }
    return K;
  }
  function se(M, Q, v) {
    let $ = !1, ee = J;
    if (Q.signal)
      if (ee = () => {
        $ = !0, v.call(
          M,
          new e(void 0, {
            cause: Q.signal.reason
          })
        );
      }, Q.signal.aborted)
        t.nextTick(ee);
      else {
        F = F || yt().addAbortListener;
        const le = F(Q.signal, ee), ae = v;
        v = i((...ce) => {
          le[w](), ae.apply(M, ce);
        });
      }
    const B = (...le) => {
      $ || t.nextTick(() => v.apply(M, le));
    };
    return R(M[D].promise, B, B), J;
  }
  function ne(M, Q) {
    var v;
    let $ = !1;
    return Q === null && (Q = n), (v = Q) !== null && v !== void 0 && v.cleanup && (d(Q.cleanup, "cleanup"), $ = Q.cleanup), new y((ee, B) => {
      const le = re(M, Q, (ae) => {
        $ && le(), ae ? B(ae) : ee();
      });
    });
  }
  return Kn.exports = re, Kn.exports.finished = ne, Kn.exports;
}
var qu, Sb;
function qr() {
  if (Sb) return qu;
  Sb = 1;
  const t = je(), {
    aggregateTwoErrors: e,
    codes: { ERR_MULTIPLE_CALLBACK: r },
    AbortError: o
  } = ct(), { Symbol: a } = Ze(), { kIsDestroyed: n, isDestroyed: i, isFinished: l, isServerRequest: s } = sr(), f = a("kDestroy"), d = a("kConstruct");
  function y(x, D, F) {
    x && (x.stack, D && !D.errored && (D.errored = x), F && !F.errored && (F.errored = x));
  }
  function R(x, D) {
    const F = this._readableState, T = this._writableState, J = T || F;
    return T != null && T.destroyed || F != null && F.destroyed ? (typeof D == "function" && D(), this) : (y(x, T, F), T && (T.destroyed = !0), F && (F.destroyed = !0), J.constructed ? w(this, x, D) : this.once(f, function(re) {
      w(this, e(re, x), D);
    }), this);
  }
  function w(x, D, F) {
    let T = !1;
    function J(re) {
      if (T)
        return;
      T = !0;
      const se = x._readableState, ne = x._writableState;
      y(re, ne, se), ne && (ne.closed = !0), se && (se.closed = !0), typeof F == "function" && F(re), re ? t.nextTick(I, x, re) : t.nextTick(A, x);
    }
    try {
      x._destroy(D || null, J);
    } catch (re) {
      J(re);
    }
  }
  function I(x, D) {
    u(x, D), A(x);
  }
  function A(x) {
    const D = x._readableState, F = x._writableState;
    F && (F.closeEmitted = !0), D && (D.closeEmitted = !0), (F != null && F.emitClose || D != null && D.emitClose) && x.emit("close");
  }
  function u(x, D) {
    const F = x._readableState, T = x._writableState;
    T != null && T.errorEmitted || F != null && F.errorEmitted || (T && (T.errorEmitted = !0), F && (F.errorEmitted = !0), x.emit("error", D));
  }
  function g() {
    const x = this._readableState, D = this._writableState;
    x && (x.constructed = !0, x.closed = !1, x.closeEmitted = !1, x.destroyed = !1, x.errored = null, x.errorEmitted = !1, x.reading = !1, x.ended = x.readable === !1, x.endEmitted = x.readable === !1), D && (D.constructed = !0, D.destroyed = !1, D.closed = !1, D.closeEmitted = !1, D.errored = null, D.errorEmitted = !1, D.finalCalled = !1, D.prefinished = !1, D.ended = D.writable === !1, D.ending = D.writable === !1, D.finished = D.writable === !1);
  }
  function p(x, D, F) {
    const T = x._readableState, J = x._writableState;
    if (J != null && J.destroyed || T != null && T.destroyed)
      return this;
    T != null && T.autoDestroy || J != null && J.autoDestroy ? x.destroy(D) : D && (D.stack, J && !J.errored && (J.errored = D), T && !T.errored && (T.errored = D), F ? t.nextTick(u, x, D) : u(x, D));
  }
  function b(x, D) {
    if (typeof x._construct != "function")
      return;
    const F = x._readableState, T = x._writableState;
    F && (F.constructed = !1), T && (T.constructed = !1), x.once(d, D), !(x.listenerCount(d) > 1) && t.nextTick(C, x);
  }
  function C(x) {
    let D = !1;
    function F(T) {
      if (D) {
        p(x, T ?? new r());
        return;
      }
      D = !0;
      const J = x._readableState, re = x._writableState, se = re || J;
      J && (J.constructed = !0), re && (re.constructed = !0), se.destroyed ? x.emit(f, T) : T ? p(x, T, !0) : t.nextTick(N, x);
    }
    try {
      x._construct((T) => {
        t.nextTick(F, T);
      });
    } catch (T) {
      t.nextTick(F, T);
    }
  }
  function N(x) {
    x.emit(d);
  }
  function W(x) {
    return x?.setHeader && typeof x.abort == "function";
  }
  function k(x) {
    x.emit("close");
  }
  function L(x, D) {
    x.emit("error", D), t.nextTick(k, x);
  }
  function U(x, D) {
    !x || i(x) || (!D && !l(x) && (D = new o()), s(x) ? (x.socket = null, x.destroy(D)) : W(x) ? x.abort() : W(x.req) ? x.req.abort() : typeof x.destroy == "function" ? x.destroy(D) : typeof x.close == "function" ? x.close() : D ? t.nextTick(L, x, D) : t.nextTick(k, x), x.destroyed || (x[n] = !0));
  }
  return qu = {
    construct: b,
    destroyer: U,
    destroy: R,
    undestroy: g,
    errorOrDestroy: p
  }, qu;
}
var Bu, Eb;
function Wc() {
  if (Eb) return Bu;
  Eb = 1;
  const { ArrayIsArray: t, ObjectSetPrototypeOf: e } = Ze(), { EventEmitter: r } = Ke;
  function o(n) {
    r.call(this, n);
  }
  e(o.prototype, r.prototype), e(o, r), o.prototype.pipe = function(n, i) {
    const l = this;
    function s(A) {
      n.writable && n.write(A) === !1 && l.pause && l.pause();
    }
    l.on("data", s);
    function f() {
      l.readable && l.resume && l.resume();
    }
    n.on("drain", f), !n._isStdio && (!i || i.end !== !1) && (l.on("end", y), l.on("close", R));
    let d = !1;
    function y() {
      d || (d = !0, n.end());
    }
    function R() {
      d || (d = !0, typeof n.destroy == "function" && n.destroy());
    }
    function w(A) {
      I(), r.listenerCount(this, "error") === 0 && this.emit("error", A);
    }
    a(l, "error", w), a(n, "error", w);
    function I() {
      l.removeListener("data", s), n.removeListener("drain", f), l.removeListener("end", y), l.removeListener("close", R), l.removeListener("error", w), n.removeListener("error", w), l.removeListener("end", I), l.removeListener("close", I), n.removeListener("close", I);
    }
    return l.on("end", I), l.on("close", I), n.on("close", I), n.emit("pipe", l), n;
  };
  function a(n, i, l) {
    if (typeof n.prependListener == "function") return n.prependListener(i, l);
    !n._events || !n._events[i] ? n.on(i, l) : t(n._events[i]) ? n._events[i].unshift(l) : n._events[i] = [l, n._events[i]];
  }
  return Bu = {
    Stream: o,
    prependListener: a
  }, Bu;
}
var Uu = { exports: {} }, vb;
function ss() {
  return vb || (vb = 1, (function(t) {
    const { SymbolDispose: e } = Ze(), { AbortError: r, codes: o } = ct(), { isNodeStream: a, isWebStream: n, kControllerErrorFunction: i } = sr(), l = yr(), { ERR_INVALID_ARG_TYPE: s } = o;
    let f;
    const d = (y, R) => {
      if (typeof y != "object" || !("aborted" in y))
        throw new s(R, "AbortSignal", y);
    };
    t.exports.addAbortSignal = function(R, w) {
      if (d(R, "signal"), !a(w) && !n(w))
        throw new s("stream", ["ReadableStream", "WritableStream", "Stream"], w);
      return t.exports.addAbortSignalNoValidate(R, w);
    }, t.exports.addAbortSignalNoValidate = function(y, R) {
      if (typeof y != "object" || !("aborted" in y))
        return R;
      const w = a(R) ? () => {
        R.destroy(
          new r(void 0, {
            cause: y.reason
          })
        );
      } : () => {
        R[i](
          new r(void 0, {
            cause: y.reason
          })
        );
      };
      if (y.aborted)
        w();
      else {
        f = f || yt().addAbortListener;
        const I = f(y, w);
        l(R, I[e]);
      }
      return R;
    };
  })(Uu)), Uu.exports;
}
var Gu, Rb;
function kA() {
  if (Rb) return Gu;
  Rb = 1;
  const { StringPrototypeSlice: t, SymbolIterator: e, TypedArrayPrototypeSet: r, Uint8Array: o } = Ze(), { Buffer: a } = Fe, { inspect: n } = yt();
  return Gu = class {
    constructor() {
      this.head = null, this.tail = null, this.length = 0;
    }
    push(l) {
      const s = {
        data: l,
        next: null
      };
      this.length > 0 ? this.tail.next = s : this.head = s, this.tail = s, ++this.length;
    }
    unshift(l) {
      const s = {
        data: l,
        next: this.head
      };
      this.length === 0 && (this.tail = s), this.head = s, ++this.length;
    }
    shift() {
      if (this.length === 0) return;
      const l = this.head.data;
      return this.length === 1 ? this.head = this.tail = null : this.head = this.head.next, --this.length, l;
    }
    clear() {
      this.head = this.tail = null, this.length = 0;
    }
    join(l) {
      if (this.length === 0) return "";
      let s = this.head, f = "" + s.data;
      for (; (s = s.next) !== null; ) f += l + s.data;
      return f;
    }
    concat(l) {
      if (this.length === 0) return a.alloc(0);
      const s = a.allocUnsafe(l >>> 0);
      let f = this.head, d = 0;
      for (; f; )
        r(s, f.data, d), d += f.data.length, f = f.next;
      return s;
    }
    // Consumes a specified amount of bytes or characters from the buffered data.
    consume(l, s) {
      const f = this.head.data;
      if (l < f.length) {
        const d = f.slice(0, l);
        return this.head.data = f.slice(l), d;
      }
      return l === f.length ? this.shift() : s ? this._getString(l) : this._getBuffer(l);
    }
    first() {
      return this.head.data;
    }
    *[e]() {
      for (let l = this.head; l; l = l.next)
        yield l.data;
    }
    // Consumes a specified amount of characters from the buffered data.
    _getString(l) {
      let s = "", f = this.head, d = 0;
      do {
        const y = f.data;
        if (l > y.length)
          s += y, l -= y.length;
        else {
          l === y.length ? (s += y, ++d, f.next ? this.head = f.next : this.head = this.tail = null) : (s += t(y, 0, l), this.head = f, f.data = t(y, l));
          break;
        }
        ++d;
      } while ((f = f.next) !== null);
      return this.length -= d, s;
    }
    // Consumes a specified amount of bytes from the buffered data.
    _getBuffer(l) {
      const s = a.allocUnsafe(l), f = l;
      let d = this.head, y = 0;
      do {
        const R = d.data;
        if (l > R.length)
          r(s, R, f - l), l -= R.length;
        else {
          l === R.length ? (r(s, R, f - l), ++y, d.next ? this.head = d.next : this.head = this.tail = null) : (r(s, new o(R.buffer, R.byteOffset, l), f - l), this.head = d, d.data = R.slice(l));
          break;
        }
        ++y;
      } while ((d = d.next) !== null);
      return this.length -= y, s;
    }
    // Make sure the linked list only shows the minimal necessary information.
    [/* @__PURE__ */ Symbol.for("nodejs.util.inspect.custom")](l, s) {
      return n(this, {
        ...s,
        // Only inspect one level.
        depth: 0,
        // It should not recurse.
        customInspect: !1
      });
    }
  }, Gu;
}
var Hu, Ab;
function os() {
  if (Ab) return Hu;
  Ab = 1;
  const { MathFloor: t, NumberIsInteger: e } = Ze(), { validateInteger: r } = In(), { ERR_INVALID_ARG_VALUE: o } = ct().codes;
  let a = 16 * 1024, n = 16;
  function i(d, y, R) {
    return d.highWaterMark != null ? d.highWaterMark : y ? d[R] : null;
  }
  function l(d) {
    return d ? n : a;
  }
  function s(d, y) {
    r(y, "value", 0), d ? n = y : a = y;
  }
  function f(d, y, R, w) {
    const I = i(y, w, R);
    if (I != null) {
      if (!e(I) || I < 0) {
        const A = w ? `options.${R}` : "options.highWaterMark";
        throw new o(A, I);
      }
      return t(I);
    }
    return l(d.objectMode);
  }
  return Hu = {
    getHighWaterMark: f,
    getDefaultHighWaterMark: l,
    setDefaultHighWaterMark: s
  }, Hu;
}
var zu, Tb;
function cS() {
  if (Tb) return zu;
  Tb = 1;
  const t = je(), { PromisePrototypeThen: e, SymbolAsyncIterator: r, SymbolIterator: o } = Ze(), { Buffer: a } = Fe, { ERR_INVALID_ARG_TYPE: n, ERR_STREAM_NULL_VALUES: i } = ct().codes;
  function l(s, f, d) {
    let y;
    if (typeof f == "string" || f instanceof a)
      return new s({
        objectMode: !0,
        ...d,
        read() {
          this.push(f), this.push(null);
        }
      });
    let R;
    if (f && f[r])
      R = !0, y = f[r]();
    else if (f && f[o])
      R = !1, y = f[o]();
    else
      throw new n("iterable", ["Iterable"], f);
    const w = new s({
      objectMode: !0,
      highWaterMark: 1,
      // TODO(ronag): What options should be allowed?
      ...d
    });
    let I = !1;
    w._read = function() {
      I || (I = !0, u());
    }, w._destroy = function(g, p) {
      e(
        A(g),
        () => t.nextTick(p, g),
        // nextTick is here in case cb throws
        (b) => t.nextTick(p, b || g)
      );
    };
    async function A(g) {
      const p = g != null, b = typeof y.throw == "function";
      if (p && b) {
        const { value: C, done: N } = await y.throw(g);
        if (await C, N)
          return;
      }
      if (typeof y.return == "function") {
        const { value: C } = await y.return();
        await C;
      }
    }
    async function u() {
      for (; ; ) {
        try {
          const { value: g, done: p } = R ? await y.next() : y.next();
          if (p)
            w.push(null);
          else {
            const b = g && typeof g.then == "function" ? await g : g;
            if (b === null)
              throw I = !1, new i();
            if (w.push(b))
              continue;
            I = !1;
          }
        } catch (g) {
          w.destroy(g);
        }
        break;
      }
    }
    return w;
  }
  return zu = l, zu;
}
var Vu, xb;
function as() {
  if (xb) return Vu;
  xb = 1;
  const t = je(), {
    ArrayPrototypeIndexOf: e,
    NumberIsInteger: r,
    NumberIsNaN: o,
    NumberParseInt: a,
    ObjectDefineProperties: n,
    ObjectKeys: i,
    ObjectSetPrototypeOf: l,
    Promise: s,
    SafeSet: f,
    SymbolAsyncDispose: d,
    SymbolAsyncIterator: y,
    Symbol: R
  } = Ze();
  Vu = z, z.ReadableState = te;
  const { EventEmitter: w } = Ke, { Stream: I, prependListener: A } = Wc(), { Buffer: u } = Fe, { addAbortSignal: g } = ss(), p = yr();
  let b = yt().debuglog("stream", (h) => {
    b = h;
  });
  const C = kA(), N = qr(), { getHighWaterMark: W, getDefaultHighWaterMark: k } = os(), {
    aggregateTwoErrors: L,
    codes: {
      ERR_INVALID_ARG_TYPE: U,
      ERR_METHOD_NOT_IMPLEMENTED: x,
      ERR_OUT_OF_RANGE: D,
      ERR_STREAM_PUSH_AFTER_EOF: F,
      ERR_STREAM_UNSHIFT_AFTER_END_EVENT: T
    },
    AbortError: J
  } = ct(), { validateObject: re } = In(), se = R("kPaused"), { StringDecoder: ne } = Ji(), M = cS();
  l(z.prototype, I.prototype), l(z, I);
  const Q = () => {
  }, { errorOrDestroy: v } = N, $ = 1, ee = 2, B = 4, le = 8, ae = 16, ce = 32, Y = 64, P = 128, q = 256, V = 512, Z = 1024, X = 2048, m = 4096, c = 8192, E = 16384, j = 32768, G = 65536, K = 1 << 17, O = 1 << 18;
  function H(h) {
    return {
      enumerable: !1,
      get() {
        return (this.state & h) !== 0;
      },
      set(S) {
        S ? this.state |= h : this.state &= ~h;
      }
    };
  }
  n(te.prototype, {
    objectMode: H($),
    ended: H(ee),
    endEmitted: H(B),
    reading: H(le),
    // Stream is still being constructed and cannot be
    // destroyed until construction finished or failed.
    // Async construction is opt in, therefore we start as
    // constructed.
    constructed: H(ae),
    // A flag to be able to tell if the event 'readable'/'data' is emitted
    // immediately, or on a later tick.  We set this to true at first, because
    // any actions that shouldn't happen until "later" should generally also
    // not happen before the first read call.
    sync: H(ce),
    // Whenever we return null, then we set a flag to say
    // that we're awaiting a 'readable' event emission.
    needReadable: H(Y),
    emittedReadable: H(P),
    readableListening: H(q),
    resumeScheduled: H(V),
    // True if the error was already emitted and should not be thrown again.
    errorEmitted: H(Z),
    emitClose: H(X),
    autoDestroy: H(m),
    // Has it been destroyed.
    destroyed: H(c),
    // Indicates whether the stream has finished destroying.
    closed: H(E),
    // True if close has been emitted or would have been emitted
    // depending on emitClose.
    closeEmitted: H(j),
    multiAwaitDrain: H(G),
    // If true, a maybeReadMore has been scheduled.
    readingMore: H(K),
    dataEmitted: H(O)
  });
  function te(h, S, ie) {
    typeof ie != "boolean" && (ie = S instanceof Xt()), this.state = X | m | ae | ce, h && h.objectMode && (this.state |= $), ie && h && h.readableObjectMode && (this.state |= $), this.highWaterMark = h ? W(this, h, "readableHighWaterMark", ie) : k(!1), this.buffer = new C(), this.length = 0, this.pipes = [], this.flowing = null, this[se] = null, h && h.emitClose === !1 && (this.state &= ~X), h && h.autoDestroy === !1 && (this.state &= ~m), this.errored = null, this.defaultEncoding = h && h.defaultEncoding || "utf8", this.awaitDrainWriters = null, this.decoder = null, this.encoding = null, h && h.encoding && (this.decoder = new ne(h.encoding), this.encoding = h.encoding);
  }
  function z(h) {
    if (!(this instanceof z)) return new z(h);
    const S = this instanceof Xt();
    this._readableState = new te(h, this, S), h && (typeof h.read == "function" && (this._read = h.read), typeof h.destroy == "function" && (this._destroy = h.destroy), typeof h.construct == "function" && (this._construct = h.construct), h.signal && !S && g(h.signal, this)), I.call(this, h), N.construct(this, () => {
      this._readableState.needReadable && me(this, this._readableState);
    });
  }
  z.prototype.destroy = N.destroy, z.prototype._undestroy = N.undestroy, z.prototype._destroy = function(h, S) {
    S(h);
  }, z.prototype[w.captureRejectionSymbol] = function(h) {
    this.destroy(h);
  }, z.prototype[d] = function() {
    let h;
    return this.destroyed || (h = this.readableEnded ? null : new J(), this.destroy(h)), new s((S, ie) => p(this, (oe) => oe && oe !== h ? ie(oe) : S(null)));
  }, z.prototype.push = function(h, S) {
    return de(this, h, S, !1);
  }, z.prototype.unshift = function(h, S) {
    return de(this, h, S, !0);
  };
  function de(h, S, ie, oe) {
    b("readableAddChunk", S);
    const fe = h._readableState;
    let ve;
    if ((fe.state & $) === 0 && (typeof S == "string" ? (ie = ie || fe.defaultEncoding, fe.encoding !== ie && (oe && fe.encoding ? S = u.from(S, ie).toString(fe.encoding) : (S = u.from(S, ie), ie = ""))) : S instanceof u ? ie = "" : I._isUint8Array(S) ? (S = I._uint8ArrayToBuffer(S), ie = "") : S != null && (ve = new U("chunk", ["string", "Buffer", "Uint8Array"], S))), ve)
      v(h, ve);
    else if (S === null)
      fe.state &= ~le, _e(h, fe);
    else if ((fe.state & $) !== 0 || S && S.length > 0)
      if (oe)
        if ((fe.state & B) !== 0) v(h, new T());
        else {
          if (fe.destroyed || fe.errored) return !1;
          pe(h, fe, S, !0);
        }
      else if (fe.ended)
        v(h, new F());
      else {
        if (fe.destroyed || fe.errored)
          return !1;
        fe.state &= ~le, fe.decoder && !ie ? (S = fe.decoder.write(S), fe.objectMode || S.length !== 0 ? pe(h, fe, S, !1) : me(h, fe)) : pe(h, fe, S, !1);
      }
    else oe || (fe.state &= ~le, me(h, fe));
    return !fe.ended && (fe.length < fe.highWaterMark || fe.length === 0);
  }
  function pe(h, S, ie, oe) {
    S.flowing && S.length === 0 && !S.sync && h.listenerCount("data") > 0 ? ((S.state & G) !== 0 ? S.awaitDrainWriters.clear() : S.awaitDrainWriters = null, S.dataEmitted = !0, h.emit("data", ie)) : (S.length += S.objectMode ? 1 : ie.length, oe ? S.buffer.unshift(ie) : S.buffer.push(ie), (S.state & Y) !== 0 && Ae(h)), me(h, S);
  }
  z.prototype.isPaused = function() {
    const h = this._readableState;
    return h[se] === !0 || h.flowing === !1;
  }, z.prototype.setEncoding = function(h) {
    const S = new ne(h);
    this._readableState.decoder = S, this._readableState.encoding = this._readableState.decoder.encoding;
    const ie = this._readableState.buffer;
    let oe = "";
    for (const fe of ie)
      oe += S.write(fe);
    return ie.clear(), oe !== "" && ie.push(oe), this._readableState.length = oe.length, this;
  };
  const he = 1073741824;
  function we(h) {
    if (h > he)
      throw new D("size", "<= 1GiB", h);
    return h--, h |= h >>> 1, h |= h >>> 2, h |= h >>> 4, h |= h >>> 8, h |= h >>> 16, h++, h;
  }
  function be(h, S) {
    return h <= 0 || S.length === 0 && S.ended ? 0 : (S.state & $) !== 0 ? 1 : o(h) ? S.flowing && S.length ? S.buffer.first().length : S.length : h <= S.length ? h : S.ended ? S.length : 0;
  }
  z.prototype.read = function(h) {
    b("read", h), h === void 0 ? h = NaN : r(h) || (h = a(h, 10));
    const S = this._readableState, ie = h;
    if (h > S.highWaterMark && (S.highWaterMark = we(h)), h !== 0 && (S.state &= ~P), h === 0 && S.needReadable && ((S.highWaterMark !== 0 ? S.length >= S.highWaterMark : S.length > 0) || S.ended))
      return b("read: emitReadable", S.length, S.ended), S.length === 0 && S.ended ? tt(this) : Ae(this), null;
    if (h = be(h, S), h === 0 && S.ended)
      return S.length === 0 && tt(this), null;
    let oe = (S.state & Y) !== 0;
    if (b("need readable", oe), (S.length === 0 || S.length - h < S.highWaterMark) && (oe = !0, b("length less than watermark", oe)), S.ended || S.reading || S.destroyed || S.errored || !S.constructed)
      oe = !1, b("reading, ended or constructing", oe);
    else if (oe) {
      b("do read"), S.state |= le | ce, S.length === 0 && (S.state |= Y);
      try {
        this._read(S.highWaterMark);
      } catch (ve) {
        v(this, ve);
      }
      S.state &= ~ce, S.reading || (h = be(ie, S));
    }
    let fe;
    return h > 0 ? fe = mt(h, S) : fe = null, fe === null ? (S.needReadable = S.length <= S.highWaterMark, h = 0) : (S.length -= h, S.multiAwaitDrain ? S.awaitDrainWriters.clear() : S.awaitDrainWriters = null), S.length === 0 && (S.ended || (S.needReadable = !0), ie !== h && S.ended && tt(this)), fe !== null && !S.errorEmitted && !S.closeEmitted && (S.dataEmitted = !0, this.emit("data", fe)), fe;
  };
  function _e(h, S) {
    if (b("onEofChunk"), !S.ended) {
      if (S.decoder) {
        const ie = S.decoder.end();
        ie && ie.length && (S.buffer.push(ie), S.length += S.objectMode ? 1 : ie.length);
      }
      S.ended = !0, S.sync ? Ae(h) : (S.needReadable = !1, S.emittedReadable = !0, xe(h));
    }
  }
  function Ae(h) {
    const S = h._readableState;
    b("emitReadable", S.needReadable, S.emittedReadable), S.needReadable = !1, S.emittedReadable || (b("emitReadable", S.flowing), S.emittedReadable = !0, t.nextTick(xe, h));
  }
  function xe(h) {
    const S = h._readableState;
    b("emitReadable_", S.destroyed, S.length, S.ended), !S.destroyed && !S.errored && (S.length || S.ended) && (h.emit("readable"), S.emittedReadable = !1), S.needReadable = !S.flowing && !S.ended && S.length <= S.highWaterMark, ht(h);
  }
  function me(h, S) {
    !S.readingMore && S.constructed && (S.readingMore = !0, t.nextTick(Ge, h, S));
  }
  function Ge(h, S) {
    for (; !S.reading && !S.ended && (S.length < S.highWaterMark || S.flowing && S.length === 0); ) {
      const ie = S.length;
      if (b("maybeReadMore read 0"), h.read(0), ie === S.length)
        break;
    }
    S.readingMore = !1;
  }
  z.prototype._read = function(h) {
    throw new x("_read()");
  }, z.prototype.pipe = function(h, S) {
    const ie = this, oe = this._readableState;
    oe.pipes.length === 1 && (oe.multiAwaitDrain || (oe.multiAwaitDrain = !0, oe.awaitDrainWriters = new f(oe.awaitDrainWriters ? [oe.awaitDrainWriters] : []))), oe.pipes.push(h), b("pipe count=%d opts=%j", oe.pipes.length, S);
    const ve = (!S || S.end !== !1) && h !== t.stdout && h !== t.stderr ? Et : $e;
    oe.endEmitted ? t.nextTick(ve) : ie.once("end", ve), h.on("unpipe", Te);
    function Te(Ce, De) {
      b("onunpipe"), Ce === ie && De && De.hasUnpiped === !1 && (De.hasUnpiped = !0, Mt());
    }
    function Et() {
      b("onend"), h.end();
    }
    let Pe, lt = !1;
    function Mt() {
      b("cleanup"), h.removeListener("close", it), h.removeListener("finish", st), Pe && h.removeListener("drain", Pe), h.removeListener("error", nt), h.removeListener("unpipe", Te), ie.removeListener("end", Et), ie.removeListener("end", $e), ie.removeListener("data", Rt), lt = !0, Pe && oe.awaitDrainWriters && (!h._writableState || h._writableState.needDrain) && Pe();
    }
    function vt() {
      lt || (oe.pipes.length === 1 && oe.pipes[0] === h ? (b("false write response, pause", 0), oe.awaitDrainWriters = h, oe.multiAwaitDrain = !1) : oe.pipes.length > 1 && oe.pipes.includes(h) && (b("false write response, pause", oe.awaitDrainWriters.size), oe.awaitDrainWriters.add(h)), ie.pause()), Pe || (Pe = Je(ie, h), h.on("drain", Pe));
    }
    ie.on("data", Rt);
    function Rt(Ce) {
      b("ondata");
      const De = h.write(Ce);
      b("dest.write", De), De === !1 && vt();
    }
    function nt(Ce) {
      if (b("onerror", Ce), $e(), h.removeListener("error", nt), h.listenerCount("error") === 0) {
        const De = h._writableState || h._readableState;
        De && !De.errorEmitted ? v(h, Ce) : h.emit("error", Ce);
      }
    }
    A(h, "error", nt);
    function it() {
      h.removeListener("finish", st), $e();
    }
    h.once("close", it);
    function st() {
      b("onfinish"), h.removeListener("close", it), $e();
    }
    h.once("finish", st);
    function $e() {
      b("unpipe"), ie.unpipe(h);
    }
    return h.emit("pipe", ie), h.writableNeedDrain === !0 ? vt() : oe.flowing || (b("pipe resume"), ie.resume()), h;
  };
  function Je(h, S) {
    return function() {
      const oe = h._readableState;
      oe.awaitDrainWriters === S ? (b("pipeOnDrain", 1), oe.awaitDrainWriters = null) : oe.multiAwaitDrain && (b("pipeOnDrain", oe.awaitDrainWriters.size), oe.awaitDrainWriters.delete(S)), (!oe.awaitDrainWriters || oe.awaitDrainWriters.size === 0) && h.listenerCount("data") && h.resume();
    };
  }
  z.prototype.unpipe = function(h) {
    const S = this._readableState, ie = {
      hasUnpiped: !1
    };
    if (S.pipes.length === 0) return this;
    if (!h) {
      const fe = S.pipes;
      S.pipes = [], this.pause();
      for (let ve = 0; ve < fe.length; ve++)
        fe[ve].emit("unpipe", this, {
          hasUnpiped: !1
        });
      return this;
    }
    const oe = e(S.pipes, h);
    return oe === -1 ? this : (S.pipes.splice(oe, 1), S.pipes.length === 0 && this.pause(), h.emit("unpipe", this, ie), this);
  }, z.prototype.on = function(h, S) {
    const ie = I.prototype.on.call(this, h, S), oe = this._readableState;
    return h === "data" ? (oe.readableListening = this.listenerCount("readable") > 0, oe.flowing !== !1 && this.resume()) : h === "readable" && !oe.endEmitted && !oe.readableListening && (oe.readableListening = oe.needReadable = !0, oe.flowing = !1, oe.emittedReadable = !1, b("on readable", oe.length, oe.reading), oe.length ? Ae(this) : oe.reading || t.nextTick(jt, this)), ie;
  }, z.prototype.addListener = z.prototype.on, z.prototype.removeListener = function(h, S) {
    const ie = I.prototype.removeListener.call(this, h, S);
    return h === "readable" && t.nextTick(Xe, this), ie;
  }, z.prototype.off = z.prototype.removeListener, z.prototype.removeAllListeners = function(h) {
    const S = I.prototype.removeAllListeners.apply(this, arguments);
    return (h === "readable" || h === void 0) && t.nextTick(Xe, this), S;
  };
  function Xe(h) {
    const S = h._readableState;
    S.readableListening = h.listenerCount("readable") > 0, S.resumeScheduled && S[se] === !1 ? S.flowing = !0 : h.listenerCount("data") > 0 ? h.resume() : S.readableListening || (S.flowing = null);
  }
  function jt(h) {
    b("readable nexttick read 0"), h.read(0);
  }
  z.prototype.resume = function() {
    const h = this._readableState;
    return h.flowing || (b("resume"), h.flowing = !h.readableListening, Ct(this, h)), h[se] = !1, this;
  };
  function Ct(h, S) {
    S.resumeScheduled || (S.resumeScheduled = !0, t.nextTick($t, h, S));
  }
  function $t(h, S) {
    b("resume", S.reading), S.reading || h.read(0), S.resumeScheduled = !1, h.emit("resume"), ht(h), S.flowing && !S.reading && h.read(0);
  }
  z.prototype.pause = function() {
    return b("call pause flowing=%j", this._readableState.flowing), this._readableState.flowing !== !1 && (b("pause"), this._readableState.flowing = !1, this.emit("pause")), this._readableState[se] = !0, this;
  };
  function ht(h) {
    const S = h._readableState;
    for (b("flow", S.flowing); S.flowing && h.read() !== null; ) ;
  }
  z.prototype.wrap = function(h) {
    let S = !1;
    h.on("data", (oe) => {
      !this.push(oe) && h.pause && (S = !0, h.pause());
    }), h.on("end", () => {
      this.push(null);
    }), h.on("error", (oe) => {
      v(this, oe);
    }), h.on("close", () => {
      this.destroy();
    }), h.on("destroy", () => {
      this.destroy();
    }), this._read = () => {
      S && h.resume && (S = !1, h.resume());
    };
    const ie = i(h);
    for (let oe = 1; oe < ie.length; oe++) {
      const fe = ie[oe];
      this[fe] === void 0 && typeof h[fe] == "function" && (this[fe] = h[fe].bind(h));
    }
    return this;
  }, z.prototype[y] = function() {
    return wt(this);
  }, z.prototype.iterator = function(h) {
    return h !== void 0 && re(h, "options"), wt(this, h);
  };
  function wt(h, S) {
    typeof h.read != "function" && (h = z.wrap(h, {
      objectMode: !0
    }));
    const ie = Wt(h, S);
    return ie.stream = h, ie;
  }
  async function* Wt(h, S) {
    let ie = Q;
    function oe(Te) {
      this === h ? (ie(), ie = Q) : ie = Te;
    }
    h.on("readable", oe);
    let fe;
    const ve = p(
      h,
      {
        writable: !1
      },
      (Te) => {
        fe = Te ? L(fe, Te) : null, ie(), ie = Q;
      }
    );
    try {
      for (; ; ) {
        const Te = h.destroyed ? null : h.read();
        if (Te !== null)
          yield Te;
        else {
          if (fe)
            throw fe;
          if (fe === null)
            return;
          await new s(oe);
        }
      }
    } catch (Te) {
      throw fe = L(fe, Te), fe;
    } finally {
      (fe || S?.destroyOnReturn !== !1) && (fe === void 0 || h._readableState.autoDestroy) ? N.destroyer(h, null) : (h.off("readable", oe), ve());
    }
  }
  n(z.prototype, {
    readable: {
      __proto__: null,
      get() {
        const h = this._readableState;
        return !!h && h.readable !== !1 && !h.destroyed && !h.errorEmitted && !h.endEmitted;
      },
      set(h) {
        this._readableState && (this._readableState.readable = !!h);
      }
    },
    readableDidRead: {
      __proto__: null,
      enumerable: !1,
      get: function() {
        return this._readableState.dataEmitted;
      }
    },
    readableAborted: {
      __proto__: null,
      enumerable: !1,
      get: function() {
        return !!(this._readableState.readable !== !1 && (this._readableState.destroyed || this._readableState.errored) && !this._readableState.endEmitted);
      }
    },
    readableHighWaterMark: {
      __proto__: null,
      enumerable: !1,
      get: function() {
        return this._readableState.highWaterMark;
      }
    },
    readableBuffer: {
      __proto__: null,
      enumerable: !1,
      get: function() {
        return this._readableState && this._readableState.buffer;
      }
    },
    readableFlowing: {
      __proto__: null,
      enumerable: !1,
      get: function() {
        return this._readableState.flowing;
      },
      set: function(h) {
        this._readableState && (this._readableState.flowing = h);
      }
    },
    readableLength: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._readableState.length;
      }
    },
    readableObjectMode: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._readableState ? this._readableState.objectMode : !1;
      }
    },
    readableEncoding: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._readableState ? this._readableState.encoding : null;
      }
    },
    errored: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._readableState ? this._readableState.errored : null;
      }
    },
    closed: {
      __proto__: null,
      get() {
        return this._readableState ? this._readableState.closed : !1;
      }
    },
    destroyed: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._readableState ? this._readableState.destroyed : !1;
      },
      set(h) {
        this._readableState && (this._readableState.destroyed = h);
      }
    },
    readableEnded: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._readableState ? this._readableState.endEmitted : !1;
      }
    }
  }), n(te.prototype, {
    // Legacy getter for `pipesCount`.
    pipesCount: {
      __proto__: null,
      get() {
        return this.pipes.length;
      }
    },
    // Legacy property for `paused`.
    paused: {
      __proto__: null,
      get() {
        return this[se] !== !1;
      },
      set(h) {
        this[se] = !!h;
      }
    }
  }), z._fromList = mt;
  function mt(h, S) {
    if (S.length === 0) return null;
    let ie;
    return S.objectMode ? ie = S.buffer.shift() : !h || h >= S.length ? (S.decoder ? ie = S.buffer.join("") : S.buffer.length === 1 ? ie = S.buffer.first() : ie = S.buffer.concat(S.length), S.buffer.clear()) : ie = S.buffer.consume(h, S.decoder), ie;
  }
  function tt(h) {
    const S = h._readableState;
    b("endReadable", S.endEmitted), S.endEmitted || (S.ended = !0, t.nextTick(qt, S, h));
  }
  function qt(h, S) {
    if (b("endReadableNT", h.endEmitted, h.length), !h.errored && !h.closeEmitted && !h.endEmitted && h.length === 0) {
      if (h.endEmitted = !0, S.emit("end"), S.writable && S.allowHalfOpen === !1)
        t.nextTick(Bt, S);
      else if (h.autoDestroy) {
        const ie = S._writableState;
        (!ie || ie.autoDestroy && // We don't expect the writable to ever 'finish'
        // if writable is explicitly set to false.
        (ie.finished || ie.writable === !1)) && S.destroy();
      }
    }
  }
  function Bt(h) {
    h.writable && !h.writableEnded && !h.destroyed && h.end();
  }
  z.from = function(h, S) {
    return M(z, h, S);
  };
  let rt;
  function St() {
    return rt === void 0 && (rt = {}), rt;
  }
  return z.fromWeb = function(h, S) {
    return St().newStreamReadableFromReadableStream(h, S);
  }, z.toWeb = function(h, S) {
    return St().newReadableStreamFromStreamReadable(h, S);
  }, z.wrap = function(h, S) {
    var ie, oe;
    return new z({
      objectMode: (ie = (oe = h.readableObjectMode) !== null && oe !== void 0 ? oe : h.objectMode) !== null && ie !== void 0 ? ie : !0,
      ...S,
      destroy(fe, ve) {
        N.destroyer(h, fe), ve(fe);
      }
    }).wrap(h);
  }, Vu;
}
var Zu, Ib;
function qc() {
  if (Ib) return Zu;
  Ib = 1;
  const t = je(), {
    ArrayPrototypeSlice: e,
    Error: r,
    FunctionPrototypeSymbolHasInstance: o,
    ObjectDefineProperty: a,
    ObjectDefineProperties: n,
    ObjectSetPrototypeOf: i,
    StringPrototypeToLowerCase: l,
    Symbol: s,
    SymbolHasInstance: f
  } = Ze();
  Zu = re, re.WritableState = T;
  const { EventEmitter: d } = Ke, y = Wc().Stream, { Buffer: R } = Fe, w = qr(), { addAbortSignal: I } = ss(), { getHighWaterMark: A, getDefaultHighWaterMark: u } = os(), {
    ERR_INVALID_ARG_TYPE: g,
    ERR_METHOD_NOT_IMPLEMENTED: p,
    ERR_MULTIPLE_CALLBACK: b,
    ERR_STREAM_CANNOT_PIPE: C,
    ERR_STREAM_DESTROYED: N,
    ERR_STREAM_ALREADY_FINISHED: W,
    ERR_STREAM_NULL_VALUES: k,
    ERR_STREAM_WRITE_AFTER_END: L,
    ERR_UNKNOWN_ENCODING: U
  } = ct().codes, { errorOrDestroy: x } = w;
  i(re.prototype, y.prototype), i(re, y);
  function D() {
  }
  const F = s("kOnFinished");
  function T(m, c, E) {
    typeof E != "boolean" && (E = c instanceof Xt()), this.objectMode = !!(m && m.objectMode), E && (this.objectMode = this.objectMode || !!(m && m.writableObjectMode)), this.highWaterMark = m ? A(this, m, "writableHighWaterMark", E) : u(!1), this.finalCalled = !1, this.needDrain = !1, this.ending = !1, this.ended = !1, this.finished = !1, this.destroyed = !1;
    const j = !!(m && m.decodeStrings === !1);
    this.decodeStrings = !j, this.defaultEncoding = m && m.defaultEncoding || "utf8", this.length = 0, this.writing = !1, this.corked = 0, this.sync = !0, this.bufferProcessing = !1, this.onwrite = v.bind(void 0, c), this.writecb = null, this.writelen = 0, this.afterWriteTickInfo = null, J(this), this.pendingcb = 0, this.constructed = !0, this.prefinished = !1, this.errorEmitted = !1, this.emitClose = !m || m.emitClose !== !1, this.autoDestroy = !m || m.autoDestroy !== !1, this.errored = null, this.closed = !1, this.closeEmitted = !1, this[F] = [];
  }
  function J(m) {
    m.buffered = [], m.bufferedIndex = 0, m.allBuffers = !0, m.allNoop = !0;
  }
  T.prototype.getBuffer = function() {
    return e(this.buffered, this.bufferedIndex);
  }, a(T.prototype, "bufferedRequestCount", {
    __proto__: null,
    get() {
      return this.buffered.length - this.bufferedIndex;
    }
  });
  function re(m) {
    const c = this instanceof Xt();
    if (!c && !o(re, this)) return new re(m);
    this._writableState = new T(m, this, c), m && (typeof m.write == "function" && (this._write = m.write), typeof m.writev == "function" && (this._writev = m.writev), typeof m.destroy == "function" && (this._destroy = m.destroy), typeof m.final == "function" && (this._final = m.final), typeof m.construct == "function" && (this._construct = m.construct), m.signal && I(m.signal, this)), y.call(this, m), w.construct(this, () => {
      const E = this._writableState;
      E.writing || le(this, E), P(this, E);
    });
  }
  a(re, f, {
    __proto__: null,
    value: function(m) {
      return o(this, m) ? !0 : this !== re ? !1 : m && m._writableState instanceof T;
    }
  }), re.prototype.pipe = function() {
    x(this, new C());
  };
  function se(m, c, E, j) {
    const G = m._writableState;
    if (typeof E == "function")
      j = E, E = G.defaultEncoding;
    else {
      if (!E) E = G.defaultEncoding;
      else if (E !== "buffer" && !R.isEncoding(E)) throw new U(E);
      typeof j != "function" && (j = D);
    }
    if (c === null)
      throw new k();
    if (!G.objectMode)
      if (typeof c == "string")
        G.decodeStrings !== !1 && (c = R.from(c, E), E = "buffer");
      else if (c instanceof R)
        E = "buffer";
      else if (y._isUint8Array(c))
        c = y._uint8ArrayToBuffer(c), E = "buffer";
      else
        throw new g("chunk", ["string", "Buffer", "Uint8Array"], c);
    let K;
    return G.ending ? K = new L() : G.destroyed && (K = new N("write")), K ? (t.nextTick(j, K), x(m, K, !0), K) : (G.pendingcb++, ne(m, G, c, E, j));
  }
  re.prototype.write = function(m, c, E) {
    return se(this, m, c, E) === !0;
  }, re.prototype.cork = function() {
    this._writableState.corked++;
  }, re.prototype.uncork = function() {
    const m = this._writableState;
    m.corked && (m.corked--, m.writing || le(this, m));
  }, re.prototype.setDefaultEncoding = function(c) {
    if (typeof c == "string" && (c = l(c)), !R.isEncoding(c)) throw new U(c);
    return this._writableState.defaultEncoding = c, this;
  };
  function ne(m, c, E, j, G) {
    const K = c.objectMode ? 1 : E.length;
    c.length += K;
    const O = c.length < c.highWaterMark;
    return O || (c.needDrain = !0), c.writing || c.corked || c.errored || !c.constructed ? (c.buffered.push({
      chunk: E,
      encoding: j,
      callback: G
    }), c.allBuffers && j !== "buffer" && (c.allBuffers = !1), c.allNoop && G !== D && (c.allNoop = !1)) : (c.writelen = K, c.writecb = G, c.writing = !0, c.sync = !0, m._write(E, j, c.onwrite), c.sync = !1), O && !c.errored && !c.destroyed;
  }
  function M(m, c, E, j, G, K, O) {
    c.writelen = j, c.writecb = O, c.writing = !0, c.sync = !0, c.destroyed ? c.onwrite(new N("write")) : E ? m._writev(G, c.onwrite) : m._write(G, K, c.onwrite), c.sync = !1;
  }
  function Q(m, c, E, j) {
    --c.pendingcb, j(E), B(c), x(m, E);
  }
  function v(m, c) {
    const E = m._writableState, j = E.sync, G = E.writecb;
    if (typeof G != "function") {
      x(m, new b());
      return;
    }
    E.writing = !1, E.writecb = null, E.length -= E.writelen, E.writelen = 0, c ? (c.stack, E.errored || (E.errored = c), m._readableState && !m._readableState.errored && (m._readableState.errored = c), j ? t.nextTick(Q, m, E, c, G) : Q(m, E, c, G)) : (E.buffered.length > E.bufferedIndex && le(m, E), j ? E.afterWriteTickInfo !== null && E.afterWriteTickInfo.cb === G ? E.afterWriteTickInfo.count++ : (E.afterWriteTickInfo = {
      count: 1,
      cb: G,
      stream: m,
      state: E
    }, t.nextTick($, E.afterWriteTickInfo)) : ee(m, E, 1, G));
  }
  function $({ stream: m, state: c, count: E, cb: j }) {
    return c.afterWriteTickInfo = null, ee(m, c, E, j);
  }
  function ee(m, c, E, j) {
    for (!c.ending && !m.destroyed && c.length === 0 && c.needDrain && (c.needDrain = !1, m.emit("drain")); E-- > 0; )
      c.pendingcb--, j();
    c.destroyed && B(c), P(m, c);
  }
  function B(m) {
    if (m.writing)
      return;
    for (let G = m.bufferedIndex; G < m.buffered.length; ++G) {
      var c;
      const { chunk: K, callback: O } = m.buffered[G], H = m.objectMode ? 1 : K.length;
      m.length -= H, O(
        (c = m.errored) !== null && c !== void 0 ? c : new N("write")
      );
    }
    const E = m[F].splice(0);
    for (let G = 0; G < E.length; G++) {
      var j;
      E[G](
        (j = m.errored) !== null && j !== void 0 ? j : new N("end")
      );
    }
    J(m);
  }
  function le(m, c) {
    if (c.corked || c.bufferProcessing || c.destroyed || !c.constructed)
      return;
    const { buffered: E, bufferedIndex: j, objectMode: G } = c, K = E.length - j;
    if (!K)
      return;
    let O = j;
    if (c.bufferProcessing = !0, K > 1 && m._writev) {
      c.pendingcb -= K - 1;
      const H = c.allNoop ? D : (z) => {
        for (let de = O; de < E.length; ++de)
          E[de].callback(z);
      }, te = c.allNoop && O === 0 ? E : e(E, O);
      te.allBuffers = c.allBuffers, M(m, c, !0, c.length, te, "", H), J(c);
    } else {
      do {
        const { chunk: H, encoding: te, callback: z } = E[O];
        E[O++] = null;
        const de = G ? 1 : H.length;
        M(m, c, !1, de, H, te, z);
      } while (O < E.length && !c.writing);
      O === E.length ? J(c) : O > 256 ? (E.splice(0, O), c.bufferedIndex = 0) : c.bufferedIndex = O;
    }
    c.bufferProcessing = !1;
  }
  re.prototype._write = function(m, c, E) {
    if (this._writev)
      this._writev(
        [
          {
            chunk: m,
            encoding: c
          }
        ],
        E
      );
    else
      throw new p("_write()");
  }, re.prototype._writev = null, re.prototype.end = function(m, c, E) {
    const j = this._writableState;
    typeof m == "function" ? (E = m, m = null, c = null) : typeof c == "function" && (E = c, c = null);
    let G;
    if (m != null) {
      const K = se(this, m, c);
      K instanceof r && (G = K);
    }
    return j.corked && (j.corked = 1, this.uncork()), G || (!j.errored && !j.ending ? (j.ending = !0, P(this, j, !0), j.ended = !0) : j.finished ? G = new W("end") : j.destroyed && (G = new N("end"))), typeof E == "function" && (G || j.finished ? t.nextTick(E, G) : j[F].push(E)), this;
  };
  function ae(m) {
    return m.ending && !m.destroyed && m.constructed && m.length === 0 && !m.errored && m.buffered.length === 0 && !m.finished && !m.writing && !m.errorEmitted && !m.closeEmitted;
  }
  function ce(m, c) {
    let E = !1;
    function j(G) {
      if (E) {
        x(m, G ?? b());
        return;
      }
      if (E = !0, c.pendingcb--, G) {
        const K = c[F].splice(0);
        for (let O = 0; O < K.length; O++)
          K[O](G);
        x(m, G, c.sync);
      } else ae(c) && (c.prefinished = !0, m.emit("prefinish"), c.pendingcb++, t.nextTick(q, m, c));
    }
    c.sync = !0, c.pendingcb++;
    try {
      m._final(j);
    } catch (G) {
      j(G);
    }
    c.sync = !1;
  }
  function Y(m, c) {
    !c.prefinished && !c.finalCalled && (typeof m._final == "function" && !c.destroyed ? (c.finalCalled = !0, ce(m, c)) : (c.prefinished = !0, m.emit("prefinish")));
  }
  function P(m, c, E) {
    ae(c) && (Y(m, c), c.pendingcb === 0 && (E ? (c.pendingcb++, t.nextTick(
      (j, G) => {
        ae(G) ? q(j, G) : G.pendingcb--;
      },
      m,
      c
    )) : ae(c) && (c.pendingcb++, q(m, c))));
  }
  function q(m, c) {
    c.pendingcb--, c.finished = !0;
    const E = c[F].splice(0);
    for (let j = 0; j < E.length; j++)
      E[j]();
    if (m.emit("finish"), c.autoDestroy) {
      const j = m._readableState;
      (!j || j.autoDestroy && // We don't expect the readable to ever 'end'
      // if readable is explicitly set to false.
      (j.endEmitted || j.readable === !1)) && m.destroy();
    }
  }
  n(re.prototype, {
    closed: {
      __proto__: null,
      get() {
        return this._writableState ? this._writableState.closed : !1;
      }
    },
    destroyed: {
      __proto__: null,
      get() {
        return this._writableState ? this._writableState.destroyed : !1;
      },
      set(m) {
        this._writableState && (this._writableState.destroyed = m);
      }
    },
    writable: {
      __proto__: null,
      get() {
        const m = this._writableState;
        return !!m && m.writable !== !1 && !m.destroyed && !m.errored && !m.ending && !m.ended;
      },
      set(m) {
        this._writableState && (this._writableState.writable = !!m);
      }
    },
    writableFinished: {
      __proto__: null,
      get() {
        return this._writableState ? this._writableState.finished : !1;
      }
    },
    writableObjectMode: {
      __proto__: null,
      get() {
        return this._writableState ? this._writableState.objectMode : !1;
      }
    },
    writableBuffer: {
      __proto__: null,
      get() {
        return this._writableState && this._writableState.getBuffer();
      }
    },
    writableEnded: {
      __proto__: null,
      get() {
        return this._writableState ? this._writableState.ending : !1;
      }
    },
    writableNeedDrain: {
      __proto__: null,
      get() {
        const m = this._writableState;
        return m ? !m.destroyed && !m.ending && m.needDrain : !1;
      }
    },
    writableHighWaterMark: {
      __proto__: null,
      get() {
        return this._writableState && this._writableState.highWaterMark;
      }
    },
    writableCorked: {
      __proto__: null,
      get() {
        return this._writableState ? this._writableState.corked : 0;
      }
    },
    writableLength: {
      __proto__: null,
      get() {
        return this._writableState && this._writableState.length;
      }
    },
    errored: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._writableState ? this._writableState.errored : null;
      }
    },
    writableAborted: {
      __proto__: null,
      enumerable: !1,
      get: function() {
        return !!(this._writableState.writable !== !1 && (this._writableState.destroyed || this._writableState.errored) && !this._writableState.finished);
      }
    }
  });
  const V = w.destroy;
  re.prototype.destroy = function(m, c) {
    const E = this._writableState;
    return !E.destroyed && (E.bufferedIndex < E.buffered.length || E[F].length) && t.nextTick(B, E), V.call(this, m, c), this;
  }, re.prototype._undestroy = w.undestroy, re.prototype._destroy = function(m, c) {
    c(m);
  }, re.prototype[d.captureRejectionSymbol] = function(m) {
    this.destroy(m);
  };
  let Z;
  function X() {
    return Z === void 0 && (Z = {}), Z;
  }
  return re.fromWeb = function(m, c) {
    return X().newStreamWritableFromWritableStream(m, c);
  }, re.toWeb = function(m) {
    return X().newWritableStreamFromStreamWritable(m);
  }, Zu;
}
var Yu, Db;
function FA() {
  if (Db) return Yu;
  Db = 1;
  const t = je(), e = Fe, {
    isReadable: r,
    isWritable: o,
    isIterable: a,
    isNodeStream: n,
    isReadableNodeStream: i,
    isWritableNodeStream: l,
    isDuplexNodeStream: s,
    isReadableStream: f,
    isWritableStream: d
  } = sr(), y = yr(), {
    AbortError: R,
    codes: { ERR_INVALID_ARG_TYPE: w, ERR_INVALID_RETURN_VALUE: I }
  } = ct(), { destroyer: A } = qr(), u = Xt(), g = as(), p = qc(), { createDeferredPromise: b } = yt(), C = cS(), N = globalThis.Blob || e.Blob, W = typeof N < "u" ? function(T) {
    return T instanceof N;
  } : function(T) {
    return !1;
  }, k = globalThis.AbortController || et.AbortController, { FunctionPrototypeCall: L } = Ze();
  class U extends u {
    constructor(T) {
      super(T), T?.readable === !1 && (this._readableState.readable = !1, this._readableState.ended = !0, this._readableState.endEmitted = !0), T?.writable === !1 && (this._writableState.writable = !1, this._writableState.ending = !0, this._writableState.ended = !0, this._writableState.finished = !0);
    }
  }
  Yu = function F(T, J) {
    if (s(T))
      return T;
    if (i(T))
      return D({
        readable: T
      });
    if (l(T))
      return D({
        writable: T
      });
    if (n(T))
      return D({
        writable: !1,
        readable: !1
      });
    if (f(T))
      return D({
        readable: g.fromWeb(T)
      });
    if (d(T))
      return D({
        writable: p.fromWeb(T)
      });
    if (typeof T == "function") {
      const { value: se, write: ne, final: M, destroy: Q } = x(T);
      if (a(se))
        return C(U, se, {
          // TODO (ronag): highWaterMark?
          objectMode: !0,
          write: ne,
          final: M,
          destroy: Q
        });
      const v = se?.then;
      if (typeof v == "function") {
        let $;
        const ee = L(
          v,
          se,
          (B) => {
            if (B != null)
              throw new I("nully", "body", B);
          },
          (B) => {
            A($, B);
          }
        );
        return $ = new U({
          // TODO (ronag): highWaterMark?
          objectMode: !0,
          readable: !1,
          write: ne,
          final(B) {
            M(async () => {
              try {
                await ee, t.nextTick(B, null);
              } catch (le) {
                t.nextTick(B, le);
              }
            });
          },
          destroy: Q
        });
      }
      throw new I("Iterable, AsyncIterable or AsyncFunction", J, se);
    }
    if (W(T))
      return F(T.arrayBuffer());
    if (a(T))
      return C(U, T, {
        // TODO (ronag): highWaterMark?
        objectMode: !0,
        writable: !1
      });
    if (f(T?.readable) && d(T?.writable))
      return U.fromWeb(T);
    if (typeof T?.writable == "object" || typeof T?.readable == "object") {
      const se = T != null && T.readable ? i(T?.readable) ? T?.readable : F(T.readable) : void 0, ne = T != null && T.writable ? l(T?.writable) ? T?.writable : F(T.writable) : void 0;
      return D({
        readable: se,
        writable: ne
      });
    }
    const re = T?.then;
    if (typeof re == "function") {
      let se;
      return L(
        re,
        T,
        (ne) => {
          ne != null && se.push(ne), se.push(null);
        },
        (ne) => {
          A(se, ne);
        }
      ), se = new U({
        objectMode: !0,
        writable: !1,
        read() {
        }
      });
    }
    throw new w(
      J,
      [
        "Blob",
        "ReadableStream",
        "WritableStream",
        "Stream",
        "Iterable",
        "AsyncIterable",
        "Function",
        "{ readable, writable } pair",
        "Promise"
      ],
      T
    );
  };
  function x(F) {
    let { promise: T, resolve: J } = b();
    const re = new k(), se = re.signal;
    return {
      value: F(
        (async function* () {
          for (; ; ) {
            const M = T;
            T = null;
            const { chunk: Q, done: v, cb: $ } = await M;
            if (t.nextTick($), v) return;
            if (se.aborted)
              throw new R(void 0, {
                cause: se.reason
              });
            ({ promise: T, resolve: J } = b()), yield Q;
          }
        })(),
        {
          signal: se
        }
      ),
      write(M, Q, v) {
        const $ = J;
        J = null, $({
          chunk: M,
          done: !1,
          cb: v
        });
      },
      final(M) {
        const Q = J;
        J = null, Q({
          done: !0,
          cb: M
        });
      },
      destroy(M, Q) {
        re.abort(), Q(M);
      }
    };
  }
  function D(F) {
    const T = F.readable && typeof F.readable.read != "function" ? g.wrap(F.readable) : F.readable, J = F.writable;
    let re = !!r(T), se = !!o(J), ne, M, Q, v, $;
    function ee(B) {
      const le = v;
      v = null, le ? le(B) : B && $.destroy(B);
    }
    return $ = new U({
      // TODO (ronag): highWaterMark?
      readableObjectMode: !!(T != null && T.readableObjectMode),
      writableObjectMode: !!(J != null && J.writableObjectMode),
      readable: re,
      writable: se
    }), se && (y(J, (B) => {
      se = !1, B && A(T, B), ee(B);
    }), $._write = function(B, le, ae) {
      J.write(B, le) ? ae() : ne = ae;
    }, $._final = function(B) {
      J.end(), M = B;
    }, J.on("drain", function() {
      if (ne) {
        const B = ne;
        ne = null, B();
      }
    }), J.on("finish", function() {
      if (M) {
        const B = M;
        M = null, B();
      }
    })), re && (y(T, (B) => {
      re = !1, B && A(T, B), ee(B);
    }), T.on("readable", function() {
      if (Q) {
        const B = Q;
        Q = null, B();
      }
    }), T.on("end", function() {
      $.push(null);
    }), $._read = function() {
      for (; ; ) {
        const B = T.read();
        if (B === null) {
          Q = $._read;
          return;
        }
        if (!$.push(B))
          return;
      }
    }), $._destroy = function(B, le) {
      !B && v !== null && (B = new R()), Q = null, ne = null, M = null, v === null ? le(B) : (v = le, A(J, B), A(T, B));
    }, $;
  }
  return Yu;
}
var Ku, Ob;
function Xt() {
  if (Ob) return Ku;
  Ob = 1;
  const {
    ObjectDefineProperties: t,
    ObjectGetOwnPropertyDescriptor: e,
    ObjectKeys: r,
    ObjectSetPrototypeOf: o
  } = Ze();
  Ku = i;
  const a = as(), n = qc();
  o(i.prototype, a.prototype), o(i, a);
  {
    const d = r(n.prototype);
    for (let y = 0; y < d.length; y++) {
      const R = d[y];
      i.prototype[R] || (i.prototype[R] = n.prototype[R]);
    }
  }
  function i(d) {
    if (!(this instanceof i)) return new i(d);
    a.call(this, d), n.call(this, d), d ? (this.allowHalfOpen = d.allowHalfOpen !== !1, d.readable === !1 && (this._readableState.readable = !1, this._readableState.ended = !0, this._readableState.endEmitted = !0), d.writable === !1 && (this._writableState.writable = !1, this._writableState.ending = !0, this._writableState.ended = !0, this._writableState.finished = !0)) : this.allowHalfOpen = !0;
  }
  t(i.prototype, {
    writable: {
      __proto__: null,
      ...e(n.prototype, "writable")
    },
    writableHighWaterMark: {
      __proto__: null,
      ...e(n.prototype, "writableHighWaterMark")
    },
    writableObjectMode: {
      __proto__: null,
      ...e(n.prototype, "writableObjectMode")
    },
    writableBuffer: {
      __proto__: null,
      ...e(n.prototype, "writableBuffer")
    },
    writableLength: {
      __proto__: null,
      ...e(n.prototype, "writableLength")
    },
    writableFinished: {
      __proto__: null,
      ...e(n.prototype, "writableFinished")
    },
    writableCorked: {
      __proto__: null,
      ...e(n.prototype, "writableCorked")
    },
    writableEnded: {
      __proto__: null,
      ...e(n.prototype, "writableEnded")
    },
    writableNeedDrain: {
      __proto__: null,
      ...e(n.prototype, "writableNeedDrain")
    },
    destroyed: {
      __proto__: null,
      get() {
        return this._readableState === void 0 || this._writableState === void 0 ? !1 : this._readableState.destroyed && this._writableState.destroyed;
      },
      set(d) {
        this._readableState && this._writableState && (this._readableState.destroyed = d, this._writableState.destroyed = d);
      }
    }
  });
  let l;
  function s() {
    return l === void 0 && (l = {}), l;
  }
  i.fromWeb = function(d, y) {
    return s().newStreamDuplexFromReadableWritablePair(d, y);
  }, i.toWeb = function(d) {
    return s().newReadableWritablePairFromDuplex(d);
  };
  let f;
  return i.from = function(d) {
    return f || (f = FA()), f(d, "body");
  }, Ku;
}
var Ju, Nb;
function dS() {
  if (Nb) return Ju;
  Nb = 1;
  const { ObjectSetPrototypeOf: t, Symbol: e } = Ze();
  Ju = i;
  const { ERR_METHOD_NOT_IMPLEMENTED: r } = ct().codes, o = Xt(), { getHighWaterMark: a } = os();
  t(i.prototype, o.prototype), t(i, o);
  const n = e("kCallback");
  function i(f) {
    if (!(this instanceof i)) return new i(f);
    const d = f ? a(this, f, "readableHighWaterMark", !0) : null;
    d === 0 && (f = {
      ...f,
      highWaterMark: null,
      readableHighWaterMark: d,
      // TODO (ronag): 0 is not optimal since we have
      // a "bug" where we check needDrain before calling _write and not after.
      // Refs: https://github.com/nodejs/node/pull/32887
      // Refs: https://github.com/nodejs/node/pull/35941
      writableHighWaterMark: f.writableHighWaterMark || 0
    }), o.call(this, f), this._readableState.sync = !1, this[n] = null, f && (typeof f.transform == "function" && (this._transform = f.transform), typeof f.flush == "function" && (this._flush = f.flush)), this.on("prefinish", s);
  }
  function l(f) {
    typeof this._flush == "function" && !this.destroyed ? this._flush((d, y) => {
      if (d) {
        f ? f(d) : this.destroy(d);
        return;
      }
      y != null && this.push(y), this.push(null), f && f();
    }) : (this.push(null), f && f());
  }
  function s() {
    this._final !== l && l.call(this);
  }
  return i.prototype._final = l, i.prototype._transform = function(f, d, y) {
    throw new r("_transform()");
  }, i.prototype._write = function(f, d, y) {
    const R = this._readableState, w = this._writableState, I = R.length;
    this._transform(f, d, (A, u) => {
      if (A) {
        y(A);
        return;
      }
      u != null && this.push(u), w.ended || // Backwards compat.
      I === R.length || // Backwards compat.
      R.length < R.highWaterMark ? y() : this[n] = y;
    });
  }, i.prototype._read = function() {
    if (this[n]) {
      const f = this[n];
      this[n] = null, f();
    }
  }, Ju;
}
var Xu, Lb;
function hS() {
  if (Lb) return Xu;
  Lb = 1;
  const { ObjectSetPrototypeOf: t } = Ze();
  Xu = r;
  const e = dS();
  t(r.prototype, e.prototype), t(r, e);
  function r(o) {
    if (!(this instanceof r)) return new r(o);
    e.call(this, o);
  }
  return r.prototype._transform = function(o, a, n) {
    n(null, o);
  }, Xu;
}
var Qu, Pb;
function Bc() {
  if (Pb) return Qu;
  Pb = 1;
  const t = je(), { ArrayIsArray: e, Promise: r, SymbolAsyncIterator: o, SymbolDispose: a } = Ze(), n = yr(), { once: i } = yt(), l = qr(), s = Xt(), {
    aggregateTwoErrors: f,
    codes: {
      ERR_INVALID_ARG_TYPE: d,
      ERR_INVALID_RETURN_VALUE: y,
      ERR_MISSING_ARGS: R,
      ERR_STREAM_DESTROYED: w,
      ERR_STREAM_PREMATURE_CLOSE: I
    },
    AbortError: A
  } = ct(), { validateFunction: u, validateAbortSignal: g } = In(), {
    isIterable: p,
    isReadable: b,
    isReadableNodeStream: C,
    isNodeStream: N,
    isTransformStream: W,
    isWebStream: k,
    isReadableStream: L,
    isReadableFinished: U
  } = sr(), x = globalThis.AbortController || et.AbortController;
  let D, F, T;
  function J(B, le, ae) {
    let ce = !1;
    B.on("close", () => {
      ce = !0;
    });
    const Y = n(
      B,
      {
        readable: le,
        writable: ae
      },
      (P) => {
        ce = !P;
      }
    );
    return {
      destroy: (P) => {
        ce || (ce = !0, l.destroyer(B, P || new w("pipe")));
      },
      cleanup: Y
    };
  }
  function re(B) {
    return u(B[B.length - 1], "streams[stream.length - 1]"), B.pop();
  }
  function se(B) {
    if (p(B))
      return B;
    if (C(B))
      return ne(B);
    throw new d("val", ["Readable", "Iterable", "AsyncIterable"], B);
  }
  async function* ne(B) {
    F || (F = as()), yield* F.prototype[o].call(B);
  }
  async function M(B, le, ae, { end: ce }) {
    let Y, P = null;
    const q = (X) => {
      if (X && (Y = X), P) {
        const m = P;
        P = null, m();
      }
    }, V = () => new r((X, m) => {
      Y ? m(Y) : P = () => {
        Y ? m(Y) : X();
      };
    });
    le.on("drain", q);
    const Z = n(
      le,
      {
        readable: !1
      },
      q
    );
    try {
      le.writableNeedDrain && await V();
      for await (const X of B)
        le.write(X) || await V();
      ce && (le.end(), await V()), ae();
    } catch (X) {
      ae(Y !== X ? f(Y, X) : X);
    } finally {
      Z(), le.off("drain", q);
    }
  }
  async function Q(B, le, ae, { end: ce }) {
    W(le) && (le = le.writable);
    const Y = le.getWriter();
    try {
      for await (const P of B)
        await Y.ready, Y.write(P).catch(() => {
        });
      await Y.ready, ce && await Y.close(), ae();
    } catch (P) {
      try {
        await Y.abort(P), ae(P);
      } catch (q) {
        ae(q);
      }
    }
  }
  function v(...B) {
    return $(B, i(re(B)));
  }
  function $(B, le, ae) {
    if (B.length === 1 && e(B[0]) && (B = B[0]), B.length < 2)
      throw new R("streams");
    const ce = new x(), Y = ce.signal, P = ae?.signal, q = [];
    g(P, "options.signal");
    function V() {
      G(new A());
    }
    T = T || yt().addAbortListener;
    let Z;
    P && (Z = T(P, V));
    let X, m;
    const c = [];
    let E = 0;
    function j(te) {
      G(te, --E === 0);
    }
    function G(te, z) {
      var de;
      if (te && (!X || X.code === "ERR_STREAM_PREMATURE_CLOSE") && (X = te), !(!X && !z)) {
        for (; c.length; )
          c.shift()(X);
        (de = Z) === null || de === void 0 || de[a](), ce.abort(), z && (X || q.forEach((pe) => pe()), t.nextTick(le, X, m));
      }
    }
    let K;
    for (let te = 0; te < B.length; te++) {
      const z = B[te], de = te < B.length - 1, pe = te > 0, he = de || ae?.end !== !1, we = te === B.length - 1;
      if (N(z)) {
        let be = function(_e) {
          _e && _e.name !== "AbortError" && _e.code !== "ERR_STREAM_PREMATURE_CLOSE" && j(_e);
        };
        if (he) {
          const { destroy: _e, cleanup: Ae } = J(z, de, pe);
          c.push(_e), b(z) && we && q.push(Ae);
        }
        z.on("error", be), b(z) && we && q.push(() => {
          z.removeListener("error", be);
        });
      }
      if (te === 0)
        if (typeof z == "function") {
          if (K = z({
            signal: Y
          }), !p(K))
            throw new y("Iterable, AsyncIterable or Stream", "source", K);
        } else p(z) || C(z) || W(z) ? K = z : K = s.from(z);
      else if (typeof z == "function") {
        if (W(K)) {
          var O;
          K = se((O = K) === null || O === void 0 ? void 0 : O.readable);
        } else
          K = se(K);
        if (K = z(K, {
          signal: Y
        }), de) {
          if (!p(K, !0))
            throw new y("AsyncIterable", `transform[${te - 1}]`, K);
        } else {
          var H;
          D || (D = hS());
          const be = new D({
            objectMode: !0
          }), _e = (H = K) === null || H === void 0 ? void 0 : H.then;
          if (typeof _e == "function")
            E++, _e.call(
              K,
              (me) => {
                m = me, me != null && be.write(me), he && be.end(), t.nextTick(j);
              },
              (me) => {
                be.destroy(me), t.nextTick(j, me);
              }
            );
          else if (p(K, !0))
            E++, M(K, be, j, {
              end: he
            });
          else if (L(K) || W(K)) {
            const me = K.readable || K;
            E++, M(me, be, j, {
              end: he
            });
          } else
            throw new y("AsyncIterable or Promise", "destination", K);
          K = be;
          const { destroy: Ae, cleanup: xe } = J(K, !1, !0);
          c.push(Ae), we && q.push(xe);
        }
      } else if (N(z)) {
        if (C(K)) {
          E += 2;
          const be = ee(K, z, j, {
            end: he
          });
          b(z) && we && q.push(be);
        } else if (W(K) || L(K)) {
          const be = K.readable || K;
          E++, M(be, z, j, {
            end: he
          });
        } else if (p(K))
          E++, M(K, z, j, {
            end: he
          });
        else
          throw new d(
            "val",
            ["Readable", "Iterable", "AsyncIterable", "ReadableStream", "TransformStream"],
            K
          );
        K = z;
      } else if (k(z)) {
        if (C(K))
          E++, Q(se(K), z, j, {
            end: he
          });
        else if (L(K) || p(K))
          E++, Q(K, z, j, {
            end: he
          });
        else if (W(K))
          E++, Q(K.readable, z, j, {
            end: he
          });
        else
          throw new d(
            "val",
            ["Readable", "Iterable", "AsyncIterable", "ReadableStream", "TransformStream"],
            K
          );
        K = z;
      } else
        K = s.from(z);
    }
    return (Y != null && Y.aborted || P != null && P.aborted) && t.nextTick(V), K;
  }
  function ee(B, le, ae, { end: ce }) {
    let Y = !1;
    if (le.on("close", () => {
      Y || ae(new I());
    }), B.pipe(le, {
      end: !1
    }), ce) {
      let P = function() {
        Y = !0, le.end();
      };
      U(B) ? t.nextTick(P) : B.once("end", P);
    } else
      ae();
    return n(
      B,
      {
        readable: !0,
        writable: !1
      },
      (P) => {
        const q = B._readableState;
        P && P.code === "ERR_STREAM_PREMATURE_CLOSE" && q && q.ended && !q.errored && !q.errorEmitted ? B.once("end", ae).once("error", ae) : ae(P);
      }
    ), n(
      le,
      {
        readable: !1,
        writable: !0
      },
      ae
    );
  }
  return Qu = {
    pipelineImpl: $,
    pipeline: v
  }, Qu;
}
var ef, Cb;
function pS() {
  if (Cb) return ef;
  Cb = 1;
  const { pipeline: t } = Bc(), e = Xt(), { destroyer: r } = qr(), {
    isNodeStream: o,
    isReadable: a,
    isWritable: n,
    isWebStream: i,
    isTransformStream: l,
    isWritableStream: s,
    isReadableStream: f
  } = sr(), {
    AbortError: d,
    codes: { ERR_INVALID_ARG_VALUE: y, ERR_MISSING_ARGS: R }
  } = ct(), w = yr();
  return ef = function(...A) {
    if (A.length === 0)
      throw new R("streams");
    if (A.length === 1)
      return e.from(A[0]);
    const u = [...A];
    if (typeof A[0] == "function" && (A[0] = e.from(A[0])), typeof A[A.length - 1] == "function") {
      const D = A.length - 1;
      A[D] = e.from(A[D]);
    }
    for (let D = 0; D < A.length; ++D)
      if (!(!o(A[D]) && !i(A[D]))) {
        if (D < A.length - 1 && !(a(A[D]) || f(A[D]) || l(A[D])))
          throw new y(`streams[${D}]`, u[D], "must be readable");
        if (D > 0 && !(n(A[D]) || s(A[D]) || l(A[D])))
          throw new y(`streams[${D}]`, u[D], "must be writable");
      }
    let g, p, b, C, N;
    function W(D) {
      const F = C;
      C = null, F ? F(D) : D ? N.destroy(D) : !x && !U && N.destroy();
    }
    const k = A[0], L = t(A, W), U = !!(n(k) || s(k) || l(k)), x = !!(a(L) || f(L) || l(L));
    if (N = new e({
      // TODO (ronag): highWaterMark?
      writableObjectMode: !!(k != null && k.writableObjectMode),
      readableObjectMode: !!(L != null && L.readableObjectMode),
      writable: U,
      readable: x
    }), U) {
      if (o(k))
        N._write = function(F, T, J) {
          k.write(F, T) ? J() : g = J;
        }, N._final = function(F) {
          k.end(), p = F;
        }, k.on("drain", function() {
          if (g) {
            const F = g;
            g = null, F();
          }
        });
      else if (i(k)) {
        const T = (l(k) ? k.writable : k).getWriter();
        N._write = async function(J, re, se) {
          try {
            await T.ready, T.write(J).catch(() => {
            }), se();
          } catch (ne) {
            se(ne);
          }
        }, N._final = async function(J) {
          try {
            await T.ready, T.close().catch(() => {
            }), p = J;
          } catch (re) {
            J(re);
          }
        };
      }
      const D = l(L) ? L.readable : L;
      w(D, () => {
        if (p) {
          const F = p;
          p = null, F();
        }
      });
    }
    if (x) {
      if (o(L))
        L.on("readable", function() {
          if (b) {
            const D = b;
            b = null, D();
          }
        }), L.on("end", function() {
          N.push(null);
        }), N._read = function() {
          for (; ; ) {
            const D = L.read();
            if (D === null) {
              b = N._read;
              return;
            }
            if (!N.push(D))
              return;
          }
        };
      else if (i(L)) {
        const F = (l(L) ? L.readable : L).getReader();
        N._read = async function() {
          for (; ; )
            try {
              const { value: T, done: J } = await F.read();
              if (!N.push(T))
                return;
              if (J) {
                N.push(null);
                return;
              }
            } catch {
              return;
            }
        };
      }
    }
    return N._destroy = function(D, F) {
      !D && C !== null && (D = new d()), b = null, g = null, p = null, C === null ? F(D) : (C = F, o(L) && r(L, D));
    }, N;
  }, ef;
}
var Mb;
function jA() {
  if (Mb) return Yn;
  Mb = 1;
  const t = globalThis.AbortController || et.AbortController, {
    codes: { ERR_INVALID_ARG_VALUE: e, ERR_INVALID_ARG_TYPE: r, ERR_MISSING_ARGS: o, ERR_OUT_OF_RANGE: a },
    AbortError: n
  } = ct(), { validateAbortSignal: i, validateInteger: l, validateObject: s } = In(), f = Ze().Symbol("kWeak"), d = Ze().Symbol("kResistStopPropagation"), { finished: y } = yr(), R = pS(), { addAbortSignalNoValidate: w } = ss(), { isWritable: I, isNodeStream: A } = sr(), { deprecate: u } = yt(), {
    ArrayPrototypePush: g,
    Boolean: p,
    MathFloor: b,
    Number: C,
    NumberIsNaN: N,
    Promise: W,
    PromiseReject: k,
    PromiseResolve: L,
    PromisePrototypeThen: U,
    Symbol: x
  } = Ze(), D = x("kEmpty"), F = x("kEof");
  function T(P, q) {
    if (q != null && s(q, "options"), q?.signal != null && i(q.signal, "options.signal"), A(P) && !I(P))
      throw new e("stream", P, "must be writable");
    const V = R(this, P);
    return q != null && q.signal && w(q.signal, V), V;
  }
  function J(P, q) {
    if (typeof P != "function")
      throw new r("fn", ["Function", "AsyncFunction"], P);
    q != null && s(q, "options"), q?.signal != null && i(q.signal, "options.signal");
    let V = 1;
    q?.concurrency != null && (V = b(q.concurrency));
    let Z = V - 1;
    return q?.highWaterMark != null && (Z = b(q.highWaterMark)), l(V, "options.concurrency", 1), l(Z, "options.highWaterMark", 0), Z += V, (async function* () {
      const m = yt().AbortSignalAny(
        [q?.signal].filter(p)
      ), c = this, E = [], j = {
        signal: m
      };
      let G, K, O = !1, H = 0;
      function te() {
        O = !0, z();
      }
      function z() {
        H -= 1, de();
      }
      function de() {
        K && !O && H < V && E.length < Z && (K(), K = null);
      }
      async function pe() {
        try {
          for await (let he of c) {
            if (O)
              return;
            if (m.aborted)
              throw new n();
            try {
              if (he = P(he, j), he === D)
                continue;
              he = L(he);
            } catch (we) {
              he = k(we);
            }
            H += 1, U(he, z, te), E.push(he), G && (G(), G = null), !O && (E.length >= Z || H >= V) && await new W((we) => {
              K = we;
            });
          }
          E.push(F);
        } catch (he) {
          const we = k(he);
          U(we, z, te), E.push(we);
        } finally {
          O = !0, G && (G(), G = null);
        }
      }
      pe();
      try {
        for (; ; ) {
          for (; E.length > 0; ) {
            const he = await E[0];
            if (he === F)
              return;
            if (m.aborted)
              throw new n();
            he !== D && (yield he), E.shift(), de();
          }
          await new W((he) => {
            G = he;
          });
        }
      } finally {
        O = !0, K && (K(), K = null);
      }
    }).call(this);
  }
  function re(P = void 0) {
    return P != null && s(P, "options"), P?.signal != null && i(P.signal, "options.signal"), (async function* () {
      let V = 0;
      for await (const X of this) {
        var Z;
        if (P != null && (Z = P.signal) !== null && Z !== void 0 && Z.aborted)
          throw new n({
            cause: P.signal.reason
          });
        yield [V++, X];
      }
    }).call(this);
  }
  async function se(P, q = void 0) {
    for await (const V of v.call(this, P, q))
      return !0;
    return !1;
  }
  async function ne(P, q = void 0) {
    if (typeof P != "function")
      throw new r("fn", ["Function", "AsyncFunction"], P);
    return !await se.call(
      this,
      async (...V) => !await P(...V),
      q
    );
  }
  async function M(P, q) {
    for await (const V of v.call(this, P, q))
      return V;
  }
  async function Q(P, q) {
    if (typeof P != "function")
      throw new r("fn", ["Function", "AsyncFunction"], P);
    async function V(Z, X) {
      return await P(Z, X), D;
    }
    for await (const Z of J.call(this, V, q)) ;
  }
  function v(P, q) {
    if (typeof P != "function")
      throw new r("fn", ["Function", "AsyncFunction"], P);
    async function V(Z, X) {
      return await P(Z, X) ? Z : D;
    }
    return J.call(this, V, q);
  }
  class $ extends o {
    constructor() {
      super("reduce"), this.message = "Reduce of an empty stream requires an initial value";
    }
  }
  async function ee(P, q, V) {
    var Z;
    if (typeof P != "function")
      throw new r("reducer", ["Function", "AsyncFunction"], P);
    V != null && s(V, "options"), V?.signal != null && i(V.signal, "options.signal");
    let X = arguments.length > 1;
    if (V != null && (Z = V.signal) !== null && Z !== void 0 && Z.aborted) {
      const G = new n(void 0, {
        cause: V.signal.reason
      });
      throw this.once("error", () => {
      }), await y(this.destroy(G)), G;
    }
    const m = new t(), c = m.signal;
    if (V != null && V.signal) {
      const G = {
        once: !0,
        [f]: this,
        [d]: !0
      };
      V.signal.addEventListener("abort", () => m.abort(), G);
    }
    let E = !1;
    try {
      for await (const G of this) {
        var j;
        if (E = !0, V != null && (j = V.signal) !== null && j !== void 0 && j.aborted)
          throw new n();
        X ? q = await P(q, G, {
          signal: c
        }) : (q = G, X = !0);
      }
      if (!E && !X)
        throw new $();
    } finally {
      m.abort();
    }
    return q;
  }
  async function B(P) {
    P != null && s(P, "options"), P?.signal != null && i(P.signal, "options.signal");
    const q = [];
    for await (const Z of this) {
      var V;
      if (P != null && (V = P.signal) !== null && V !== void 0 && V.aborted)
        throw new n(void 0, {
          cause: P.signal.reason
        });
      g(q, Z);
    }
    return q;
  }
  function le(P, q) {
    const V = J.call(this, P, q);
    return (async function* () {
      for await (const X of V)
        yield* X;
    }).call(this);
  }
  function ae(P) {
    if (P = C(P), N(P))
      return 0;
    if (P < 0)
      throw new a("number", ">= 0", P);
    return P;
  }
  function ce(P, q = void 0) {
    return q != null && s(q, "options"), q?.signal != null && i(q.signal, "options.signal"), P = ae(P), (async function* () {
      var Z;
      if (q != null && (Z = q.signal) !== null && Z !== void 0 && Z.aborted)
        throw new n();
      for await (const m of this) {
        var X;
        if (q != null && (X = q.signal) !== null && X !== void 0 && X.aborted)
          throw new n();
        P-- <= 0 && (yield m);
      }
    }).call(this);
  }
  function Y(P, q = void 0) {
    return q != null && s(q, "options"), q?.signal != null && i(q.signal, "options.signal"), P = ae(P), (async function* () {
      var Z;
      if (q != null && (Z = q.signal) !== null && Z !== void 0 && Z.aborted)
        throw new n();
      for await (const m of this) {
        var X;
        if (q != null && (X = q.signal) !== null && X !== void 0 && X.aborted)
          throw new n();
        if (P-- > 0 && (yield m), P <= 0)
          return;
      }
    }).call(this);
  }
  return Yn.streamReturningOperators = {
    asIndexedPairs: u(re, "readable.asIndexedPairs will be removed in a future version."),
    drop: ce,
    filter: v,
    flatMap: le,
    map: J,
    take: Y,
    compose: T
  }, Yn.promiseReturningOperators = {
    every: ne,
    forEach: Q,
    reduce: ee,
    toArray: B,
    some: se,
    find: M
  }, Yn;
}
var tf, kb;
function gS() {
  if (kb) return tf;
  kb = 1;
  const { ArrayPrototypePop: t, Promise: e } = Ze(), { isIterable: r, isNodeStream: o, isWebStream: a } = sr(), { pipelineImpl: n } = Bc(), { finished: i } = yr();
  yS();
  function l(...s) {
    return new e((f, d) => {
      let y, R;
      const w = s[s.length - 1];
      if (w && typeof w == "object" && !o(w) && !r(w) && !a(w)) {
        const I = t(s);
        y = I.signal, R = I.end;
      }
      n(
        s,
        (I, A) => {
          I ? d(I) : f(A);
        },
        {
          signal: y,
          end: R
        }
      );
    });
  }
  return tf = {
    finished: i,
    pipeline: l
  }, tf;
}
var Fb;
function yS() {
  if (Fb) return Cu.exports;
  Fb = 1;
  const { Buffer: t } = Fe, { ObjectDefineProperty: e, ObjectKeys: r, ReflectApply: o } = Ze(), {
    promisify: { custom: a }
  } = yt(), { streamReturningOperators: n, promiseReturningOperators: i } = jA(), {
    codes: { ERR_ILLEGAL_CONSTRUCTOR: l }
  } = ct(), s = pS(), { setDefaultHighWaterMark: f, getDefaultHighWaterMark: d } = os(), { pipeline: y } = Bc(), { destroyer: R } = qr(), w = yr(), I = gS(), A = sr(), u = Cu.exports = Wc().Stream;
  u.isDestroyed = A.isDestroyed, u.isDisturbed = A.isDisturbed, u.isErrored = A.isErrored, u.isReadable = A.isReadable, u.isWritable = A.isWritable, u.Readable = as();
  for (const p of r(n)) {
    let C = function(...N) {
      if (new.target)
        throw l();
      return u.Readable.from(o(b, this, N));
    };
    const b = n[p];
    e(C, "name", {
      __proto__: null,
      value: b.name
    }), e(C, "length", {
      __proto__: null,
      value: b.length
    }), e(u.Readable.prototype, p, {
      __proto__: null,
      value: C,
      enumerable: !1,
      configurable: !0,
      writable: !0
    });
  }
  for (const p of r(i)) {
    let C = function(...N) {
      if (new.target)
        throw l();
      return o(b, this, N);
    };
    const b = i[p];
    e(C, "name", {
      __proto__: null,
      value: b.name
    }), e(C, "length", {
      __proto__: null,
      value: b.length
    }), e(u.Readable.prototype, p, {
      __proto__: null,
      value: C,
      enumerable: !1,
      configurable: !0,
      writable: !0
    });
  }
  u.Writable = qc(), u.Duplex = Xt(), u.Transform = dS(), u.PassThrough = hS(), u.pipeline = y;
  const { addAbortSignal: g } = ss();
  return u.addAbortSignal = g, u.finished = w, u.destroy = R, u.compose = s, u.setDefaultHighWaterMark = f, u.getDefaultHighWaterMark = d, e(u, "promises", {
    __proto__: null,
    configurable: !0,
    enumerable: !0,
    get() {
      return I;
    }
  }), e(y, a, {
    __proto__: null,
    enumerable: !0,
    get() {
      return I.pipeline;
    }
  }), e(w, a, {
    __proto__: null,
    enumerable: !0,
    get() {
      return I.finished;
    }
  }), u.Stream = u, u._isUint8Array = function(b) {
    return b instanceof Uint8Array;
  }, u._uint8ArrayToBuffer = function(b) {
    return t.from(b.buffer, b.byteOffset, b.byteLength);
  }, Cu.exports;
}
ai.exports;
var jb;
function bS() {
  return jb || (jb = 1, (function(t) {
    const e = tr;
    if (e && process.env.READABLE_STREAM === "disable") {
      const r = e.promises;
      t.exports._uint8ArrayToBuffer = e._uint8ArrayToBuffer, t.exports._isUint8Array = e._isUint8Array, t.exports.isDisturbed = e.isDisturbed, t.exports.isErrored = e.isErrored, t.exports.isReadable = e.isReadable, t.exports.Readable = e.Readable, t.exports.Writable = e.Writable, t.exports.Duplex = e.Duplex, t.exports.Transform = e.Transform, t.exports.PassThrough = e.PassThrough, t.exports.addAbortSignal = e.addAbortSignal, t.exports.finished = e.finished, t.exports.destroy = e.destroy, t.exports.pipeline = e.pipeline, t.exports.compose = e.compose, Object.defineProperty(e, "promises", {
        configurable: !0,
        enumerable: !0,
        get() {
          return r;
        }
      }), t.exports.Stream = e.Stream;
    } else {
      const r = yS(), o = gS(), a = r.Readable.destroy;
      t.exports = r.Readable, t.exports._uint8ArrayToBuffer = r._uint8ArrayToBuffer, t.exports._isUint8Array = r._isUint8Array, t.exports.isDisturbed = r.isDisturbed, t.exports.isErrored = r.isErrored, t.exports.isReadable = r.isReadable, t.exports.Readable = r.Readable, t.exports.Writable = r.Writable, t.exports.Duplex = r.Duplex, t.exports.Transform = r.Transform, t.exports.PassThrough = r.PassThrough, t.exports.addAbortSignal = r.addAbortSignal, t.exports.finished = r.finished, t.exports.destroy = r.destroy, t.exports.destroy = a, t.exports.pipeline = r.pipeline, t.exports.compose = r.compose, Object.defineProperty(r, "promises", {
        configurable: !0,
        enumerable: !0,
        get() {
          return o;
        }
      }), t.exports.Stream = r.Stream;
    }
    t.exports.default = t.exports;
  })(ai)), ai.exports;
}
var rf = { exports: {} }, $b;
function _S() {
  if ($b) return rf.exports;
  $b = 1, tr.Stream;
  var t = bS().PassThrough, e = uS(), r = rf.exports = {};
  return r.normalizeInputSource = function(o) {
    if (o === null)
      return Buffer.alloc(0);
    if (typeof o == "string")
      return Buffer.from(o);
    if (e(o) && !o._readableState) {
      var a = new t();
      return o.pipe(a), a;
    }
    return o;
  }, rf.exports;
}
var Wb;
function wS() {
  if (Wb) return Lu.exports;
  Wb = 1;
  var t = It.inherits, e = uS(), r = bS().Transform, o = jc(), a = _S(), n = Lu.exports = function(i) {
    if (!(this instanceof n))
      return new n(i);
    r.call(this, i), this.offset = 0, this._archive = {
      finish: !1,
      finished: !1,
      processing: !1
    };
  };
  return t(n, r), n.prototype._appendBuffer = function(i, l, s) {
  }, n.prototype._appendStream = function(i, l, s) {
  }, n.prototype._emitErrorCallback = function(i) {
    i && this.emit("error", i);
  }, n.prototype._finish = function(i) {
  }, n.prototype._normalizeEntry = function(i) {
  }, n.prototype._transform = function(i, l, s) {
    s(null, i);
  }, n.prototype.entry = function(i, l, s) {
    if (l = l || null, typeof s != "function" && (s = this._emitErrorCallback.bind(this)), !(i instanceof o)) {
      s(new Error("not a valid instance of ArchiveEntry"));
      return;
    }
    if (this._archive.finish || this._archive.finished) {
      s(new Error("unacceptable entry after finish"));
      return;
    }
    if (this._archive.processing) {
      s(new Error("already processing an entry"));
      return;
    }
    if (this._archive.processing = !0, this._normalizeEntry(i), this._entry = i, l = a.normalizeInputSource(l), Buffer.isBuffer(l))
      this._appendBuffer(i, l, s);
    else if (e(l))
      this._appendStream(i, l, s);
    else {
      this._archive.processing = !1, s(new Error("input source must be valid Stream or Buffer instance"));
      return;
    }
    return this;
  }, n.prototype.finish = function() {
    if (this._archive.processing) {
      this._archive.finish = !0;
      return;
    }
    this._finish();
  }, n.prototype.getBytesWritten = function() {
    return this.offset;
  }, n.prototype.write = function(i, l) {
    return i && (this.offset += i.length), r.prototype.write.call(this, i, l);
  }, Lu.exports;
}
var nf = { exports: {} }, sf = {};
var qb;
function Uc() {
  return qb || (qb = 1, (function(t) {
    (function(e) {
      e(typeof DO_NOT_EXPORT_CRC > "u" ? t : {});
    })(function(e) {
      e.version = "1.2.2";
      function r() {
        for (var L = 0, U = new Array(256), x = 0; x != 256; ++x)
          L = x, L = L & 1 ? -306674912 ^ L >>> 1 : L >>> 1, L = L & 1 ? -306674912 ^ L >>> 1 : L >>> 1, L = L & 1 ? -306674912 ^ L >>> 1 : L >>> 1, L = L & 1 ? -306674912 ^ L >>> 1 : L >>> 1, L = L & 1 ? -306674912 ^ L >>> 1 : L >>> 1, L = L & 1 ? -306674912 ^ L >>> 1 : L >>> 1, L = L & 1 ? -306674912 ^ L >>> 1 : L >>> 1, L = L & 1 ? -306674912 ^ L >>> 1 : L >>> 1, U[x] = L;
        return typeof Int32Array < "u" ? new Int32Array(U) : U;
      }
      var o = r();
      function a(L) {
        var U = 0, x = 0, D = 0, F = typeof Int32Array < "u" ? new Int32Array(4096) : new Array(4096);
        for (D = 0; D != 256; ++D) F[D] = L[D];
        for (D = 0; D != 256; ++D)
          for (x = L[D], U = 256 + D; U < 4096; U += 256) x = F[U] = x >>> 8 ^ L[x & 255];
        var T = [];
        for (D = 1; D != 16; ++D) T[D - 1] = typeof Int32Array < "u" ? F.subarray(D * 256, D * 256 + 256) : F.slice(D * 256, D * 256 + 256);
        return T;
      }
      var n = a(o), i = n[0], l = n[1], s = n[2], f = n[3], d = n[4], y = n[5], R = n[6], w = n[7], I = n[8], A = n[9], u = n[10], g = n[11], p = n[12], b = n[13], C = n[14];
      function N(L, U) {
        for (var x = U ^ -1, D = 0, F = L.length; D < F; ) x = x >>> 8 ^ o[(x ^ L.charCodeAt(D++)) & 255];
        return ~x;
      }
      function W(L, U) {
        for (var x = U ^ -1, D = L.length - 15, F = 0; F < D; ) x = C[L[F++] ^ x & 255] ^ b[L[F++] ^ x >> 8 & 255] ^ p[L[F++] ^ x >> 16 & 255] ^ g[L[F++] ^ x >>> 24] ^ u[L[F++]] ^ A[L[F++]] ^ I[L[F++]] ^ w[L[F++]] ^ R[L[F++]] ^ y[L[F++]] ^ d[L[F++]] ^ f[L[F++]] ^ s[L[F++]] ^ l[L[F++]] ^ i[L[F++]] ^ o[L[F++]];
        for (D += 15; F < D; ) x = x >>> 8 ^ o[(x ^ L[F++]) & 255];
        return ~x;
      }
      function k(L, U) {
        for (var x = U ^ -1, D = 0, F = L.length, T = 0, J = 0; D < F; )
          T = L.charCodeAt(D++), T < 128 ? x = x >>> 8 ^ o[(x ^ T) & 255] : T < 2048 ? (x = x >>> 8 ^ o[(x ^ (192 | T >> 6 & 31)) & 255], x = x >>> 8 ^ o[(x ^ (128 | T & 63)) & 255]) : T >= 55296 && T < 57344 ? (T = (T & 1023) + 64, J = L.charCodeAt(D++) & 1023, x = x >>> 8 ^ o[(x ^ (240 | T >> 8 & 7)) & 255], x = x >>> 8 ^ o[(x ^ (128 | T >> 2 & 63)) & 255], x = x >>> 8 ^ o[(x ^ (128 | J >> 6 & 15 | (T & 3) << 4)) & 255], x = x >>> 8 ^ o[(x ^ (128 | J & 63)) & 255]) : (x = x >>> 8 ^ o[(x ^ (224 | T >> 12 & 15)) & 255], x = x >>> 8 ^ o[(x ^ (128 | T >> 6 & 63)) & 255], x = x >>> 8 ^ o[(x ^ (128 | T & 63)) & 255]);
        return ~x;
      }
      e.table = o, e.bstr = N, e.buf = W, e.str = k;
    });
  })(sf)), sf;
}
var li = { exports: {} }, of = { exports: {} }, af, Bb;
function Ye() {
  if (Bb) return af;
  Bb = 1;
  class t extends Error {
    constructor(r) {
      if (!Array.isArray(r))
        throw new TypeError(`Expected input to be an Array, got ${typeof r}`);
      let o = "";
      for (let a = 0; a < r.length; a++)
        o += `    ${r[a].stack}
`;
      super(o), this.name = "AggregateError", this.errors = r;
    }
  }
  return af = {
    AggregateError: t,
    ArrayIsArray(e) {
      return Array.isArray(e);
    },
    ArrayPrototypeIncludes(e, r) {
      return e.includes(r);
    },
    ArrayPrototypeIndexOf(e, r) {
      return e.indexOf(r);
    },
    ArrayPrototypeJoin(e, r) {
      return e.join(r);
    },
    ArrayPrototypeMap(e, r) {
      return e.map(r);
    },
    ArrayPrototypePop(e, r) {
      return e.pop(r);
    },
    ArrayPrototypePush(e, r) {
      return e.push(r);
    },
    ArrayPrototypeSlice(e, r, o) {
      return e.slice(r, o);
    },
    Error,
    FunctionPrototypeCall(e, r, ...o) {
      return e.call(r, ...o);
    },
    FunctionPrototypeSymbolHasInstance(e, r) {
      return Function.prototype[Symbol.hasInstance].call(e, r);
    },
    MathFloor: Math.floor,
    Number,
    NumberIsInteger: Number.isInteger,
    NumberIsNaN: Number.isNaN,
    NumberMAX_SAFE_INTEGER: Number.MAX_SAFE_INTEGER,
    NumberMIN_SAFE_INTEGER: Number.MIN_SAFE_INTEGER,
    NumberParseInt: Number.parseInt,
    ObjectDefineProperties(e, r) {
      return Object.defineProperties(e, r);
    },
    ObjectDefineProperty(e, r, o) {
      return Object.defineProperty(e, r, o);
    },
    ObjectGetOwnPropertyDescriptor(e, r) {
      return Object.getOwnPropertyDescriptor(e, r);
    },
    ObjectKeys(e) {
      return Object.keys(e);
    },
    ObjectSetPrototypeOf(e, r) {
      return Object.setPrototypeOf(e, r);
    },
    Promise,
    PromisePrototypeCatch(e, r) {
      return e.catch(r);
    },
    PromisePrototypeThen(e, r, o) {
      return e.then(r, o);
    },
    PromiseReject(e) {
      return Promise.reject(e);
    },
    PromiseResolve(e) {
      return Promise.resolve(e);
    },
    ReflectApply: Reflect.apply,
    RegExpPrototypeTest(e, r) {
      return e.test(r);
    },
    SafeSet: Set,
    String,
    StringPrototypeSlice(e, r, o) {
      return e.slice(r, o);
    },
    StringPrototypeToLowerCase(e) {
      return e.toLowerCase();
    },
    StringPrototypeToUpperCase(e) {
      return e.toUpperCase();
    },
    StringPrototypeTrim(e) {
      return e.trim();
    },
    Symbol,
    SymbolFor: Symbol.for,
    SymbolAsyncIterator: Symbol.asyncIterator,
    SymbolHasInstance: Symbol.hasInstance,
    SymbolIterator: Symbol.iterator,
    SymbolDispose: Symbol.dispose || /* @__PURE__ */ Symbol("Symbol.dispose"),
    SymbolAsyncDispose: Symbol.asyncDispose || /* @__PURE__ */ Symbol("Symbol.asyncDispose"),
    TypedArrayPrototypeSet(e, r, o) {
      return e.set(r, o);
    },
    Boolean,
    Uint8Array
  }, af;
}
var lf = { exports: {} }, uf, Ub;
function mS() {
  return Ub || (Ub = 1, uf = {
    format(t, ...e) {
      return t.replace(/%([sdifj])/g, function(...[r, o]) {
        const a = e.shift();
        return o === "f" ? a.toFixed(6) : o === "j" ? JSON.stringify(a) : o === "s" && typeof a == "object" ? `${a.constructor !== Object ? a.constructor.name : ""} {}`.trim() : a.toString();
      });
    },
    inspect(t) {
      switch (typeof t) {
        case "string":
          if (t.includes("'"))
            if (t.includes('"')) {
              if (!t.includes("`") && !t.includes("${"))
                return `\`${t}\``;
            } else return `"${t}"`;
          return `'${t}'`;
        case "number":
          return isNaN(t) ? "NaN" : Object.is(t, -0) ? String(t) : t;
        case "bigint":
          return `${String(t)}n`;
        case "boolean":
        case "undefined":
          return String(t);
        case "object":
          return "{}";
      }
    }
  }), uf;
}
var ff, Gb;
function dt() {
  if (Gb) return ff;
  Gb = 1;
  const { format: t, inspect: e } = mS(), { AggregateError: r } = Ye(), o = globalThis.AggregateError || r, a = /* @__PURE__ */ Symbol("kIsNodeError"), n = [
    "string",
    "function",
    "number",
    "object",
    // Accept 'Function' and 'Object' as alternative to the lower cased version.
    "Function",
    "Object",
    "boolean",
    "bigint",
    "symbol"
  ], i = /^([A-Z][a-z0-9]*)+$/, l = "__node_internal_", s = {};
  function f(u, g) {
    if (!u)
      throw new s.ERR_INTERNAL_ASSERTION(g);
  }
  function d(u) {
    let g = "", p = u.length;
    const b = u[0] === "-" ? 1 : 0;
    for (; p >= b + 4; p -= 3)
      g = `_${u.slice(p - 3, p)}${g}`;
    return `${u.slice(0, p)}${g}`;
  }
  function y(u, g, p) {
    if (typeof g == "function")
      return f(
        g.length <= p.length,
        // Default options do not count.
        `Code: ${u}; The provided arguments length (${p.length}) does not match the required ones (${g.length}).`
      ), g(...p);
    const b = (g.match(/%[dfijoOs]/g) || []).length;
    return f(
      b === p.length,
      `Code: ${u}; The provided arguments length (${p.length}) does not match the required ones (${b}).`
    ), p.length === 0 ? g : t(g, ...p);
  }
  function R(u, g, p) {
    p || (p = Error);
    class b extends p {
      constructor(...N) {
        super(y(u, g, N));
      }
      toString() {
        return `${this.name} [${u}]: ${this.message}`;
      }
    }
    Object.defineProperties(b.prototype, {
      name: {
        value: p.name,
        writable: !0,
        enumerable: !1,
        configurable: !0
      },
      toString: {
        value() {
          return `${this.name} [${u}]: ${this.message}`;
        },
        writable: !0,
        enumerable: !1,
        configurable: !0
      }
    }), b.prototype.code = u, b.prototype[a] = !0, s[u] = b;
  }
  function w(u) {
    const g = l + u.name;
    return Object.defineProperty(u, "name", {
      value: g
    }), u;
  }
  function I(u, g) {
    if (u && g && u !== g) {
      if (Array.isArray(g.errors))
        return g.errors.push(u), g;
      const p = new o([g, u], g.message);
      return p.code = g.code, p;
    }
    return u || g;
  }
  class A extends Error {
    constructor(g = "The operation was aborted", p = void 0) {
      if (p !== void 0 && typeof p != "object")
        throw new s.ERR_INVALID_ARG_TYPE("options", "Object", p);
      super(g, p), this.code = "ABORT_ERR", this.name = "AbortError";
    }
  }
  return R("ERR_ASSERTION", "%s", Error), R(
    "ERR_INVALID_ARG_TYPE",
    (u, g, p) => {
      f(typeof u == "string", "'name' must be a string"), Array.isArray(g) || (g = [g]);
      let b = "The ";
      u.endsWith(" argument") ? b += `${u} ` : b += `"${u}" ${u.includes(".") ? "property" : "argument"} `, b += "must be ";
      const C = [], N = [], W = [];
      for (const L of g)
        f(typeof L == "string", "All expected entries have to be of type string"), n.includes(L) ? C.push(L.toLowerCase()) : i.test(L) ? N.push(L) : (f(L !== "object", 'The value "object" should be written as "Object"'), W.push(L));
      if (N.length > 0) {
        const L = C.indexOf("object");
        L !== -1 && (C.splice(C, L, 1), N.push("Object"));
      }
      if (C.length > 0) {
        switch (C.length) {
          case 1:
            b += `of type ${C[0]}`;
            break;
          case 2:
            b += `one of type ${C[0]} or ${C[1]}`;
            break;
          default: {
            const L = C.pop();
            b += `one of type ${C.join(", ")}, or ${L}`;
          }
        }
        (N.length > 0 || W.length > 0) && (b += " or ");
      }
      if (N.length > 0) {
        switch (N.length) {
          case 1:
            b += `an instance of ${N[0]}`;
            break;
          case 2:
            b += `an instance of ${N[0]} or ${N[1]}`;
            break;
          default: {
            const L = N.pop();
            b += `an instance of ${N.join(", ")}, or ${L}`;
          }
        }
        W.length > 0 && (b += " or ");
      }
      switch (W.length) {
        case 0:
          break;
        case 1:
          W[0].toLowerCase() !== W[0] && (b += "an "), b += `${W[0]}`;
          break;
        case 2:
          b += `one of ${W[0]} or ${W[1]}`;
          break;
        default: {
          const L = W.pop();
          b += `one of ${W.join(", ")}, or ${L}`;
        }
      }
      if (p == null)
        b += `. Received ${p}`;
      else if (typeof p == "function" && p.name)
        b += `. Received function ${p.name}`;
      else if (typeof p == "object") {
        var k;
        if ((k = p.constructor) !== null && k !== void 0 && k.name)
          b += `. Received an instance of ${p.constructor.name}`;
        else {
          const L = e(p, {
            depth: -1
          });
          b += `. Received ${L}`;
        }
      } else {
        let L = e(p, {
          colors: !1
        });
        L.length > 25 && (L = `${L.slice(0, 25)}...`), b += `. Received type ${typeof p} (${L})`;
      }
      return b;
    },
    TypeError
  ), R(
    "ERR_INVALID_ARG_VALUE",
    (u, g, p = "is invalid") => {
      let b = e(g);
      return b.length > 128 && (b = b.slice(0, 128) + "..."), `The ${u.includes(".") ? "property" : "argument"} '${u}' ${p}. Received ${b}`;
    },
    TypeError
  ), R(
    "ERR_INVALID_RETURN_VALUE",
    (u, g, p) => {
      var b;
      const C = p != null && (b = p.constructor) !== null && b !== void 0 && b.name ? `instance of ${p.constructor.name}` : `type ${typeof p}`;
      return `Expected ${u} to be returned from the "${g}" function but got ${C}.`;
    },
    TypeError
  ), R(
    "ERR_MISSING_ARGS",
    (...u) => {
      f(u.length > 0, "At least one arg needs to be specified");
      let g;
      const p = u.length;
      switch (u = (Array.isArray(u) ? u : [u]).map((b) => `"${b}"`).join(" or "), p) {
        case 1:
          g += `The ${u[0]} argument`;
          break;
        case 2:
          g += `The ${u[0]} and ${u[1]} arguments`;
          break;
        default:
          {
            const b = u.pop();
            g += `The ${u.join(", ")}, and ${b} arguments`;
          }
          break;
      }
      return `${g} must be specified`;
    },
    TypeError
  ), R(
    "ERR_OUT_OF_RANGE",
    (u, g, p) => {
      f(g, 'Missing "range" argument');
      let b;
      if (Number.isInteger(p) && Math.abs(p) > 2 ** 32)
        b = d(String(p));
      else if (typeof p == "bigint") {
        b = String(p);
        const C = BigInt(2) ** BigInt(32);
        (p > C || p < -C) && (b = d(b)), b += "n";
      } else
        b = e(p);
      return `The value of "${u}" is out of range. It must be ${g}. Received ${b}`;
    },
    RangeError
  ), R("ERR_MULTIPLE_CALLBACK", "Callback called multiple times", Error), R("ERR_METHOD_NOT_IMPLEMENTED", "The %s method is not implemented", Error), R("ERR_STREAM_ALREADY_FINISHED", "Cannot call %s after a stream was finished", Error), R("ERR_STREAM_CANNOT_PIPE", "Cannot pipe, not readable", Error), R("ERR_STREAM_DESTROYED", "Cannot call %s after a stream was destroyed", Error), R("ERR_STREAM_NULL_VALUES", "May not write null values to stream", TypeError), R("ERR_STREAM_PREMATURE_CLOSE", "Premature close", Error), R("ERR_STREAM_PUSH_AFTER_EOF", "stream.push() after EOF", Error), R("ERR_STREAM_UNSHIFT_AFTER_END_EVENT", "stream.unshift() after end event", Error), R("ERR_STREAM_WRITE_AFTER_END", "write after end", Error), R("ERR_UNKNOWN_ENCODING", "Unknown encoding: %s", TypeError), ff = {
    AbortError: A,
    aggregateTwoErrors: w(I),
    hideStackFrames: w,
    codes: s
  }, ff;
}
var Hb;
function bt() {
  return Hb || (Hb = 1, (function(t) {
    const e = Fe, { format: r, inspect: o } = mS(), {
      codes: { ERR_INVALID_ARG_TYPE: a }
    } = dt(), { kResistStopPropagation: n, AggregateError: i, SymbolDispose: l } = Ye(), s = globalThis.AbortSignal || et.AbortSignal, f = globalThis.AbortController || et.AbortController, d = Object.getPrototypeOf(async function() {
    }).constructor, y = globalThis.Blob || e.Blob, R = typeof y < "u" ? function(u) {
      return u instanceof y;
    } : function(u) {
      return !1;
    }, w = (A, u) => {
      if (A !== void 0 && (A === null || typeof A != "object" || !("aborted" in A)))
        throw new a(u, "AbortSignal", A);
    }, I = (A, u) => {
      if (typeof A != "function")
        throw new a(u, "Function", A);
    };
    t.exports = {
      AggregateError: i,
      kEmptyObject: Object.freeze({}),
      once(A) {
        let u = !1;
        return function(...g) {
          u || (u = !0, A.apply(this, g));
        };
      },
      createDeferredPromise: function() {
        let A, u;
        return {
          promise: new Promise((p, b) => {
            A = p, u = b;
          }),
          resolve: A,
          reject: u
        };
      },
      promisify(A) {
        return new Promise((u, g) => {
          A((p, ...b) => p ? g(p) : u(...b));
        });
      },
      debuglog() {
        return function() {
        };
      },
      format: r,
      inspect: o,
      types: {
        isAsyncFunction(A) {
          return A instanceof d;
        },
        isArrayBufferView(A) {
          return ArrayBuffer.isView(A);
        }
      },
      isBlob: R,
      deprecate(A, u) {
        return A;
      },
      addAbortListener: Ke.addAbortListener || function(u, g) {
        if (u === void 0)
          throw new a("signal", "AbortSignal", u);
        w(u, "signal"), I(g, "listener");
        let p;
        return u.aborted ? queueMicrotask(() => g()) : (u.addEventListener("abort", g, {
          __proto__: null,
          once: !0,
          [n]: !0
        }), p = () => {
          u.removeEventListener("abort", g);
        }), {
          __proto__: null,
          [l]() {
            var b;
            (b = p) === null || b === void 0 || b();
          }
        };
      },
      AbortSignalAny: s.any || function(u) {
        if (u.length === 1)
          return u[0];
        const g = new f(), p = () => g.abort();
        return u.forEach((b) => {
          w(b, "signals"), b.addEventListener("abort", p, {
            once: !0
          });
        }), g.signal.addEventListener(
          "abort",
          () => {
            u.forEach((b) => b.removeEventListener("abort", p));
          },
          {
            once: !0
          }
        ), g.signal;
      }
    }, t.exports.promisify.custom = /* @__PURE__ */ Symbol.for("nodejs.util.promisify.custom");
  })(lf)), lf.exports;
}
var Jn = {}, cf, zb;
function Dn() {
  if (zb) return cf;
  zb = 1;
  const {
    ArrayIsArray: t,
    ArrayPrototypeIncludes: e,
    ArrayPrototypeJoin: r,
    ArrayPrototypeMap: o,
    NumberIsInteger: a,
    NumberIsNaN: n,
    NumberMAX_SAFE_INTEGER: i,
    NumberMIN_SAFE_INTEGER: l,
    NumberParseInt: s,
    ObjectPrototypeHasOwnProperty: f,
    RegExpPrototypeExec: d,
    String: y,
    StringPrototypeToUpperCase: R,
    StringPrototypeTrim: w
  } = Ye(), {
    hideStackFrames: I,
    codes: { ERR_SOCKET_BAD_PORT: A, ERR_INVALID_ARG_TYPE: u, ERR_INVALID_ARG_VALUE: g, ERR_OUT_OF_RANGE: p, ERR_UNKNOWN_SIGNAL: b }
  } = dt(), { normalizeEncoding: C } = bt(), { isAsyncFunction: N, isArrayBufferView: W } = bt().types, k = {};
  function L(O) {
    return O === (O | 0);
  }
  function U(O) {
    return O === O >>> 0;
  }
  const x = /^[0-7]+$/, D = "must be a 32-bit unsigned integer or an octal string";
  function F(O, H, te) {
    if (typeof O > "u" && (O = te), typeof O == "string") {
      if (d(x, O) === null)
        throw new g(H, O, D);
      O = s(O, 8);
    }
    return re(O, H), O;
  }
  const T = I((O, H, te = l, z = i) => {
    if (typeof O != "number") throw new u(H, "number", O);
    if (!a(O)) throw new p(H, "an integer", O);
    if (O < te || O > z) throw new p(H, `>= ${te} && <= ${z}`, O);
  }), J = I((O, H, te = -2147483648, z = 2147483647) => {
    if (typeof O != "number")
      throw new u(H, "number", O);
    if (!a(O))
      throw new p(H, "an integer", O);
    if (O < te || O > z)
      throw new p(H, `>= ${te} && <= ${z}`, O);
  }), re = I((O, H, te = !1) => {
    if (typeof O != "number")
      throw new u(H, "number", O);
    if (!a(O))
      throw new p(H, "an integer", O);
    const z = te ? 1 : 0, de = 4294967295;
    if (O < z || O > de)
      throw new p(H, `>= ${z} && <= ${de}`, O);
  });
  function se(O, H) {
    if (typeof O != "string") throw new u(H, "string", O);
  }
  function ne(O, H, te = void 0, z) {
    if (typeof O != "number") throw new u(H, "number", O);
    if (te != null && O < te || z != null && O > z || (te != null || z != null) && n(O))
      throw new p(
        H,
        `${te != null ? `>= ${te}` : ""}${te != null && z != null ? " && " : ""}${z != null ? `<= ${z}` : ""}`,
        O
      );
  }
  const M = I((O, H, te) => {
    if (!e(te, O)) {
      const de = "must be one of: " + r(
        o(te, (pe) => typeof pe == "string" ? `'${pe}'` : y(pe)),
        ", "
      );
      throw new g(H, O, de);
    }
  });
  function Q(O, H) {
    if (typeof O != "boolean") throw new u(H, "boolean", O);
  }
  function v(O, H, te) {
    return O == null || !f(O, H) ? te : O[H];
  }
  const $ = I((O, H, te = null) => {
    const z = v(te, "allowArray", !1), de = v(te, "allowFunction", !1);
    if (!v(te, "nullable", !1) && O === null || !z && t(O) || typeof O != "object" && (!de || typeof O != "function"))
      throw new u(H, "Object", O);
  }), ee = I((O, H) => {
    if (O != null && typeof O != "object" && typeof O != "function")
      throw new u(H, "a dictionary", O);
  }), B = I((O, H, te = 0) => {
    if (!t(O))
      throw new u(H, "Array", O);
    if (O.length < te) {
      const z = `must be longer than ${te}`;
      throw new g(H, O, z);
    }
  });
  function le(O, H) {
    B(O, H);
    for (let te = 0; te < O.length; te++)
      se(O[te], `${H}[${te}]`);
  }
  function ae(O, H) {
    B(O, H);
    for (let te = 0; te < O.length; te++)
      Q(O[te], `${H}[${te}]`);
  }
  function ce(O, H) {
    B(O, H);
    for (let te = 0; te < O.length; te++) {
      const z = O[te], de = `${H}[${te}]`;
      if (z == null)
        throw new u(de, "AbortSignal", z);
      Z(z, de);
    }
  }
  function Y(O, H = "signal") {
    if (se(O, H), k[O] === void 0)
      throw k[R(O)] !== void 0 ? new b(O + " (signals must use all capital letters)") : new b(O);
  }
  const P = I((O, H = "buffer") => {
    if (!W(O))
      throw new u(H, ["Buffer", "TypedArray", "DataView"], O);
  });
  function q(O, H) {
    const te = C(H), z = O.length;
    if (te === "hex" && z % 2 !== 0)
      throw new g("encoding", H, `is invalid for data of length ${z}`);
  }
  function V(O, H = "Port", te = !0) {
    if (typeof O != "number" && typeof O != "string" || typeof O == "string" && w(O).length === 0 || +O !== +O >>> 0 || O > 65535 || O === 0 && !te)
      throw new A(H, O, te);
    return O | 0;
  }
  const Z = I((O, H) => {
    if (O !== void 0 && (O === null || typeof O != "object" || !("aborted" in O)))
      throw new u(H, "AbortSignal", O);
  }), X = I((O, H) => {
    if (typeof O != "function") throw new u(H, "Function", O);
  }), m = I((O, H) => {
    if (typeof O != "function" || N(O)) throw new u(H, "Function", O);
  }), c = I((O, H) => {
    if (O !== void 0) throw new u(H, "undefined", O);
  });
  function E(O, H, te) {
    if (!e(te, O))
      throw new u(H, `('${r(te, "|")}')`, O);
  }
  const j = /^(?:<[^>]*>)(?:\s*;\s*[^;"\s]+(?:=(")?[^;"\s]*\1)?)*$/;
  function G(O, H) {
    if (typeof O > "u" || !d(j, O))
      throw new g(
        H,
        O,
        'must be an array or string of format "</styles.css>; rel=preload; as=style"'
      );
  }
  function K(O) {
    if (typeof O == "string")
      return G(O, "hints"), O;
    if (t(O)) {
      const H = O.length;
      let te = "";
      if (H === 0)
        return te;
      for (let z = 0; z < H; z++) {
        const de = O[z];
        G(de, "hints"), te += de, z !== H - 1 && (te += ", ");
      }
      return te;
    }
    throw new g(
      "hints",
      O,
      'must be an array or string of format "</styles.css>; rel=preload; as=style"'
    );
  }
  return cf = {
    isInt32: L,
    isUint32: U,
    parseFileMode: F,
    validateArray: B,
    validateStringArray: le,
    validateBooleanArray: ae,
    validateAbortSignalArray: ce,
    validateBoolean: Q,
    validateBuffer: P,
    validateDictionary: ee,
    validateEncoding: q,
    validateFunction: X,
    validateInt32: J,
    validateInteger: T,
    validateNumber: ne,
    validateObject: $,
    validateOneOf: M,
    validatePlainFunction: m,
    validatePort: V,
    validateSignalName: Y,
    validateString: se,
    validateUint32: re,
    validateUndefined: c,
    validateUnion: E,
    validateAbortSignal: Z,
    validateLinkHeaderValue: K
  }, cf;
}
var Xn = { exports: {} }, df, Vb;
function or() {
  if (Vb) return df;
  Vb = 1;
  const { SymbolAsyncIterator: t, SymbolIterator: e, SymbolFor: r } = Ye(), o = r("nodejs.stream.destroyed"), a = r("nodejs.stream.errored"), n = r("nodejs.stream.readable"), i = r("nodejs.stream.writable"), l = r("nodejs.stream.disturbed"), s = r("nodejs.webstream.isClosedPromise"), f = r("nodejs.webstream.controllerErrorFunction");
  function d(v, $ = !1) {
    var ee;
    return !!(v && typeof v.pipe == "function" && typeof v.on == "function" && (!$ || typeof v.pause == "function" && typeof v.resume == "function") && (!v._writableState || ((ee = v._readableState) === null || ee === void 0 ? void 0 : ee.readable) !== !1) && // Duplex
    (!v._writableState || v._readableState));
  }
  function y(v) {
    var $;
    return !!(v && typeof v.write == "function" && typeof v.on == "function" && (!v._readableState || (($ = v._writableState) === null || $ === void 0 ? void 0 : $.writable) !== !1));
  }
  function R(v) {
    return !!(v && typeof v.pipe == "function" && v._readableState && typeof v.on == "function" && typeof v.write == "function");
  }
  function w(v) {
    return v && (v._readableState || v._writableState || typeof v.write == "function" && typeof v.on == "function" || typeof v.pipe == "function" && typeof v.on == "function");
  }
  function I(v) {
    return !!(v && !w(v) && typeof v.pipeThrough == "function" && typeof v.getReader == "function" && typeof v.cancel == "function");
  }
  function A(v) {
    return !!(v && !w(v) && typeof v.getWriter == "function" && typeof v.abort == "function");
  }
  function u(v) {
    return !!(v && !w(v) && typeof v.readable == "object" && typeof v.writable == "object");
  }
  function g(v) {
    return I(v) || A(v) || u(v);
  }
  function p(v, $) {
    return v == null ? !1 : $ === !0 ? typeof v[t] == "function" : $ === !1 ? typeof v[e] == "function" : typeof v[t] == "function" || typeof v[e] == "function";
  }
  function b(v) {
    if (!w(v)) return null;
    const $ = v._writableState, ee = v._readableState, B = $ || ee;
    return !!(v.destroyed || v[o] || B != null && B.destroyed);
  }
  function C(v) {
    if (!y(v)) return null;
    if (v.writableEnded === !0) return !0;
    const $ = v._writableState;
    return $ != null && $.errored ? !1 : typeof $?.ended != "boolean" ? null : $.ended;
  }
  function N(v, $) {
    if (!y(v)) return null;
    if (v.writableFinished === !0) return !0;
    const ee = v._writableState;
    return ee != null && ee.errored ? !1 : typeof ee?.finished != "boolean" ? null : !!(ee.finished || $ === !1 && ee.ended === !0 && ee.length === 0);
  }
  function W(v) {
    if (!d(v)) return null;
    if (v.readableEnded === !0) return !0;
    const $ = v._readableState;
    return !$ || $.errored ? !1 : typeof $?.ended != "boolean" ? null : $.ended;
  }
  function k(v, $) {
    if (!d(v)) return null;
    const ee = v._readableState;
    return ee != null && ee.errored ? !1 : typeof ee?.endEmitted != "boolean" ? null : !!(ee.endEmitted || $ === !1 && ee.ended === !0 && ee.length === 0);
  }
  function L(v) {
    return v && v[n] != null ? v[n] : typeof v?.readable != "boolean" ? null : b(v) ? !1 : d(v) && v.readable && !k(v);
  }
  function U(v) {
    return v && v[i] != null ? v[i] : typeof v?.writable != "boolean" ? null : b(v) ? !1 : y(v) && v.writable && !C(v);
  }
  function x(v, $) {
    return w(v) ? b(v) ? !0 : !($?.readable !== !1 && L(v) || $?.writable !== !1 && U(v)) : null;
  }
  function D(v) {
    var $, ee;
    return w(v) ? v.writableErrored ? v.writableErrored : ($ = (ee = v._writableState) === null || ee === void 0 ? void 0 : ee.errored) !== null && $ !== void 0 ? $ : null : null;
  }
  function F(v) {
    var $, ee;
    return w(v) ? v.readableErrored ? v.readableErrored : ($ = (ee = v._readableState) === null || ee === void 0 ? void 0 : ee.errored) !== null && $ !== void 0 ? $ : null : null;
  }
  function T(v) {
    if (!w(v))
      return null;
    if (typeof v.closed == "boolean")
      return v.closed;
    const $ = v._writableState, ee = v._readableState;
    return typeof $?.closed == "boolean" || typeof ee?.closed == "boolean" ? $?.closed || ee?.closed : typeof v._closed == "boolean" && J(v) ? v._closed : null;
  }
  function J(v) {
    return typeof v._closed == "boolean" && typeof v._defaultKeepAlive == "boolean" && typeof v._removedConnection == "boolean" && typeof v._removedContLen == "boolean";
  }
  function re(v) {
    return typeof v._sent100 == "boolean" && J(v);
  }
  function se(v) {
    var $;
    return typeof v._consuming == "boolean" && typeof v._dumped == "boolean" && (($ = v.req) === null || $ === void 0 ? void 0 : $.upgradeOrConnect) === void 0;
  }
  function ne(v) {
    if (!w(v)) return null;
    const $ = v._writableState, ee = v._readableState, B = $ || ee;
    return !B && re(v) || !!(B && B.autoDestroy && B.emitClose && B.closed === !1);
  }
  function M(v) {
    var $;
    return !!(v && (($ = v[l]) !== null && $ !== void 0 ? $ : v.readableDidRead || v.readableAborted));
  }
  function Q(v) {
    var $, ee, B, le, ae, ce, Y, P, q, V;
    return !!(v && (($ = (ee = (B = (le = (ae = (ce = v[a]) !== null && ce !== void 0 ? ce : v.readableErrored) !== null && ae !== void 0 ? ae : v.writableErrored) !== null && le !== void 0 ? le : (Y = v._readableState) === null || Y === void 0 ? void 0 : Y.errorEmitted) !== null && B !== void 0 ? B : (P = v._writableState) === null || P === void 0 ? void 0 : P.errorEmitted) !== null && ee !== void 0 ? ee : (q = v._readableState) === null || q === void 0 ? void 0 : q.errored) !== null && $ !== void 0 ? $ : !((V = v._writableState) === null || V === void 0) && V.errored));
  }
  return df = {
    isDestroyed: b,
    kIsDestroyed: o,
    isDisturbed: M,
    kIsDisturbed: l,
    isErrored: Q,
    kIsErrored: a,
    isReadable: L,
    kIsReadable: n,
    kIsClosedPromise: s,
    kControllerErrorFunction: f,
    kIsWritable: i,
    isClosed: T,
    isDuplexNodeStream: R,
    isFinished: x,
    isIterable: p,
    isReadableNodeStream: d,
    isReadableStream: I,
    isReadableEnded: W,
    isReadableFinished: k,
    isReadableErrored: F,
    isNodeStream: w,
    isWebStream: g,
    isWritable: U,
    isWritableNodeStream: y,
    isWritableStream: A,
    isWritableEnded: C,
    isWritableFinished: N,
    isWritableErrored: D,
    isServerRequest: se,
    isServerResponse: re,
    willEmitClose: ne,
    isTransformStream: u
  }, df;
}
var Zb;
function br() {
  if (Zb) return Xn.exports;
  Zb = 1;
  const t = je(), { AbortError: e, codes: r } = dt(), { ERR_INVALID_ARG_TYPE: o, ERR_STREAM_PREMATURE_CLOSE: a } = r, { kEmptyObject: n, once: i } = bt(), { validateAbortSignal: l, validateFunction: s, validateObject: f, validateBoolean: d } = Dn(), { Promise: y, PromisePrototypeThen: R, SymbolDispose: w } = Ye(), {
    isClosed: I,
    isReadable: A,
    isReadableNodeStream: u,
    isReadableStream: g,
    isReadableFinished: p,
    isReadableErrored: b,
    isWritable: C,
    isWritableNodeStream: N,
    isWritableStream: W,
    isWritableFinished: k,
    isWritableErrored: L,
    isNodeStream: U,
    willEmitClose: x,
    kIsClosedPromise: D
  } = or();
  let F;
  function T(M) {
    return M.setHeader && typeof M.abort == "function";
  }
  const J = () => {
  };
  function re(M, Q, v) {
    var $, ee;
    if (arguments.length === 2 ? (v = Q, Q = n) : Q == null ? Q = n : f(Q, "options"), s(v, "callback"), l(Q.signal, "options.signal"), v = i(v), g(M) || W(M))
      return se(M, Q, v);
    if (!U(M))
      throw new o("stream", ["ReadableStream", "WritableStream", "Stream"], M);
    const B = ($ = Q.readable) !== null && $ !== void 0 ? $ : u(M), le = (ee = Q.writable) !== null && ee !== void 0 ? ee : N(M), ae = M._writableState, ce = M._readableState, Y = () => {
      M.writable || V();
    };
    let P = x(M) && u(M) === B && N(M) === le, q = k(M, !1);
    const V = () => {
      q = !0, M.destroyed && (P = !1), !(P && (!M.readable || B)) && (!B || Z) && v.call(M);
    };
    let Z = p(M, !1);
    const X = () => {
      Z = !0, M.destroyed && (P = !1), !(P && (!M.writable || le)) && (!le || q) && v.call(M);
    }, m = (O) => {
      v.call(M, O);
    };
    let c = I(M);
    const E = () => {
      c = !0;
      const O = L(M) || b(M);
      if (O && typeof O != "boolean")
        return v.call(M, O);
      if (B && !Z && u(M, !0) && !p(M, !1))
        return v.call(M, new a());
      if (le && !q && !k(M, !1))
        return v.call(M, new a());
      v.call(M);
    }, j = () => {
      c = !0;
      const O = L(M) || b(M);
      if (O && typeof O != "boolean")
        return v.call(M, O);
      v.call(M);
    }, G = () => {
      M.req.on("finish", V);
    };
    T(M) ? (M.on("complete", V), P || M.on("abort", E), M.req ? G() : M.on("request", G)) : le && !ae && (M.on("end", Y), M.on("close", Y)), !P && typeof M.aborted == "boolean" && M.on("aborted", E), M.on("end", X), M.on("finish", V), Q.error !== !1 && M.on("error", m), M.on("close", E), c ? t.nextTick(E) : ae != null && ae.errorEmitted || ce != null && ce.errorEmitted ? P || t.nextTick(j) : (!B && (!P || A(M)) && (q || C(M) === !1) || !le && (!P || C(M)) && (Z || A(M) === !1) || ce && M.req && M.aborted) && t.nextTick(j);
    const K = () => {
      v = J, M.removeListener("aborted", E), M.removeListener("complete", V), M.removeListener("abort", E), M.removeListener("request", G), M.req && M.req.removeListener("finish", V), M.removeListener("end", Y), M.removeListener("close", Y), M.removeListener("finish", V), M.removeListener("end", X), M.removeListener("error", m), M.removeListener("close", E);
    };
    if (Q.signal && !c) {
      const O = () => {
        const H = v;
        K(), H.call(
          M,
          new e(void 0, {
            cause: Q.signal.reason
          })
        );
      };
      if (Q.signal.aborted)
        t.nextTick(O);
      else {
        F = F || bt().addAbortListener;
        const H = F(Q.signal, O), te = v;
        v = i((...z) => {
          H[w](), te.apply(M, z);
        });
      }
    }
    return K;
  }
  function se(M, Q, v) {
    let $ = !1, ee = J;
    if (Q.signal)
      if (ee = () => {
        $ = !0, v.call(
          M,
          new e(void 0, {
            cause: Q.signal.reason
          })
        );
      }, Q.signal.aborted)
        t.nextTick(ee);
      else {
        F = F || bt().addAbortListener;
        const le = F(Q.signal, ee), ae = v;
        v = i((...ce) => {
          le[w](), ae.apply(M, ce);
        });
      }
    const B = (...le) => {
      $ || t.nextTick(() => v.apply(M, le));
    };
    return R(M[D].promise, B, B), J;
  }
  function ne(M, Q) {
    var v;
    let $ = !1;
    return Q === null && (Q = n), (v = Q) !== null && v !== void 0 && v.cleanup && (d(Q.cleanup, "cleanup"), $ = Q.cleanup), new y((ee, B) => {
      const le = re(M, Q, (ae) => {
        $ && le(), ae ? B(ae) : ee();
      });
    });
  }
  return Xn.exports = re, Xn.exports.finished = ne, Xn.exports;
}
var hf, Yb;
function Br() {
  if (Yb) return hf;
  Yb = 1;
  const t = je(), {
    aggregateTwoErrors: e,
    codes: { ERR_MULTIPLE_CALLBACK: r },
    AbortError: o
  } = dt(), { Symbol: a } = Ye(), { kIsDestroyed: n, isDestroyed: i, isFinished: l, isServerRequest: s } = or(), f = a("kDestroy"), d = a("kConstruct");
  function y(x, D, F) {
    x && (x.stack, D && !D.errored && (D.errored = x), F && !F.errored && (F.errored = x));
  }
  function R(x, D) {
    const F = this._readableState, T = this._writableState, J = T || F;
    return T != null && T.destroyed || F != null && F.destroyed ? (typeof D == "function" && D(), this) : (y(x, T, F), T && (T.destroyed = !0), F && (F.destroyed = !0), J.constructed ? w(this, x, D) : this.once(f, function(re) {
      w(this, e(re, x), D);
    }), this);
  }
  function w(x, D, F) {
    let T = !1;
    function J(re) {
      if (T)
        return;
      T = !0;
      const se = x._readableState, ne = x._writableState;
      y(re, ne, se), ne && (ne.closed = !0), se && (se.closed = !0), typeof F == "function" && F(re), re ? t.nextTick(I, x, re) : t.nextTick(A, x);
    }
    try {
      x._destroy(D || null, J);
    } catch (re) {
      J(re);
    }
  }
  function I(x, D) {
    u(x, D), A(x);
  }
  function A(x) {
    const D = x._readableState, F = x._writableState;
    F && (F.closeEmitted = !0), D && (D.closeEmitted = !0), (F != null && F.emitClose || D != null && D.emitClose) && x.emit("close");
  }
  function u(x, D) {
    const F = x._readableState, T = x._writableState;
    T != null && T.errorEmitted || F != null && F.errorEmitted || (T && (T.errorEmitted = !0), F && (F.errorEmitted = !0), x.emit("error", D));
  }
  function g() {
    const x = this._readableState, D = this._writableState;
    x && (x.constructed = !0, x.closed = !1, x.closeEmitted = !1, x.destroyed = !1, x.errored = null, x.errorEmitted = !1, x.reading = !1, x.ended = x.readable === !1, x.endEmitted = x.readable === !1), D && (D.constructed = !0, D.destroyed = !1, D.closed = !1, D.closeEmitted = !1, D.errored = null, D.errorEmitted = !1, D.finalCalled = !1, D.prefinished = !1, D.ended = D.writable === !1, D.ending = D.writable === !1, D.finished = D.writable === !1);
  }
  function p(x, D, F) {
    const T = x._readableState, J = x._writableState;
    if (J != null && J.destroyed || T != null && T.destroyed)
      return this;
    T != null && T.autoDestroy || J != null && J.autoDestroy ? x.destroy(D) : D && (D.stack, J && !J.errored && (J.errored = D), T && !T.errored && (T.errored = D), F ? t.nextTick(u, x, D) : u(x, D));
  }
  function b(x, D) {
    if (typeof x._construct != "function")
      return;
    const F = x._readableState, T = x._writableState;
    F && (F.constructed = !1), T && (T.constructed = !1), x.once(d, D), !(x.listenerCount(d) > 1) && t.nextTick(C, x);
  }
  function C(x) {
    let D = !1;
    function F(T) {
      if (D) {
        p(x, T ?? new r());
        return;
      }
      D = !0;
      const J = x._readableState, re = x._writableState, se = re || J;
      J && (J.constructed = !0), re && (re.constructed = !0), se.destroyed ? x.emit(f, T) : T ? p(x, T, !0) : t.nextTick(N, x);
    }
    try {
      x._construct((T) => {
        t.nextTick(F, T);
      });
    } catch (T) {
      t.nextTick(F, T);
    }
  }
  function N(x) {
    x.emit(d);
  }
  function W(x) {
    return x?.setHeader && typeof x.abort == "function";
  }
  function k(x) {
    x.emit("close");
  }
  function L(x, D) {
    x.emit("error", D), t.nextTick(k, x);
  }
  function U(x, D) {
    !x || i(x) || (!D && !l(x) && (D = new o()), s(x) ? (x.socket = null, x.destroy(D)) : W(x) ? x.abort() : W(x.req) ? x.req.abort() : typeof x.destroy == "function" ? x.destroy(D) : typeof x.close == "function" ? x.close() : D ? t.nextTick(L, x, D) : t.nextTick(k, x), x.destroyed || (x[n] = !0));
  }
  return hf = {
    construct: b,
    destroyer: U,
    destroy: R,
    undestroy: g,
    errorOrDestroy: p
  }, hf;
}
var pf, Kb;
function Gc() {
  if (Kb) return pf;
  Kb = 1;
  const { ArrayIsArray: t, ObjectSetPrototypeOf: e } = Ye(), { EventEmitter: r } = Ke;
  function o(n) {
    r.call(this, n);
  }
  e(o.prototype, r.prototype), e(o, r), o.prototype.pipe = function(n, i) {
    const l = this;
    function s(A) {
      n.writable && n.write(A) === !1 && l.pause && l.pause();
    }
    l.on("data", s);
    function f() {
      l.readable && l.resume && l.resume();
    }
    n.on("drain", f), !n._isStdio && (!i || i.end !== !1) && (l.on("end", y), l.on("close", R));
    let d = !1;
    function y() {
      d || (d = !0, n.end());
    }
    function R() {
      d || (d = !0, typeof n.destroy == "function" && n.destroy());
    }
    function w(A) {
      I(), r.listenerCount(this, "error") === 0 && this.emit("error", A);
    }
    a(l, "error", w), a(n, "error", w);
    function I() {
      l.removeListener("data", s), n.removeListener("drain", f), l.removeListener("end", y), l.removeListener("close", R), l.removeListener("error", w), n.removeListener("error", w), l.removeListener("end", I), l.removeListener("close", I), n.removeListener("close", I);
    }
    return l.on("end", I), l.on("close", I), n.on("close", I), n.emit("pipe", l), n;
  };
  function a(n, i, l) {
    if (typeof n.prependListener == "function") return n.prependListener(i, l);
    !n._events || !n._events[i] ? n.on(i, l) : t(n._events[i]) ? n._events[i].unshift(l) : n._events[i] = [l, n._events[i]];
  }
  return pf = {
    Stream: o,
    prependListener: a
  }, pf;
}
var gf = { exports: {} }, Jb;
function ls() {
  return Jb || (Jb = 1, (function(t) {
    const { SymbolDispose: e } = Ye(), { AbortError: r, codes: o } = dt(), { isNodeStream: a, isWebStream: n, kControllerErrorFunction: i } = or(), l = br(), { ERR_INVALID_ARG_TYPE: s } = o;
    let f;
    const d = (y, R) => {
      if (typeof y != "object" || !("aborted" in y))
        throw new s(R, "AbortSignal", y);
    };
    t.exports.addAbortSignal = function(R, w) {
      if (d(R, "signal"), !a(w) && !n(w))
        throw new s("stream", ["ReadableStream", "WritableStream", "Stream"], w);
      return t.exports.addAbortSignalNoValidate(R, w);
    }, t.exports.addAbortSignalNoValidate = function(y, R) {
      if (typeof y != "object" || !("aborted" in y))
        return R;
      const w = a(R) ? () => {
        R.destroy(
          new r(void 0, {
            cause: y.reason
          })
        );
      } : () => {
        R[i](
          new r(void 0, {
            cause: y.reason
          })
        );
      };
      if (y.aborted)
        w();
      else {
        f = f || bt().addAbortListener;
        const I = f(y, w);
        l(R, I[e]);
      }
      return R;
    };
  })(gf)), gf.exports;
}
var yf, Xb;
function $A() {
  if (Xb) return yf;
  Xb = 1;
  const { StringPrototypeSlice: t, SymbolIterator: e, TypedArrayPrototypeSet: r, Uint8Array: o } = Ye(), { Buffer: a } = Fe, { inspect: n } = bt();
  return yf = class {
    constructor() {
      this.head = null, this.tail = null, this.length = 0;
    }
    push(l) {
      const s = {
        data: l,
        next: null
      };
      this.length > 0 ? this.tail.next = s : this.head = s, this.tail = s, ++this.length;
    }
    unshift(l) {
      const s = {
        data: l,
        next: this.head
      };
      this.length === 0 && (this.tail = s), this.head = s, ++this.length;
    }
    shift() {
      if (this.length === 0) return;
      const l = this.head.data;
      return this.length === 1 ? this.head = this.tail = null : this.head = this.head.next, --this.length, l;
    }
    clear() {
      this.head = this.tail = null, this.length = 0;
    }
    join(l) {
      if (this.length === 0) return "";
      let s = this.head, f = "" + s.data;
      for (; (s = s.next) !== null; ) f += l + s.data;
      return f;
    }
    concat(l) {
      if (this.length === 0) return a.alloc(0);
      const s = a.allocUnsafe(l >>> 0);
      let f = this.head, d = 0;
      for (; f; )
        r(s, f.data, d), d += f.data.length, f = f.next;
      return s;
    }
    // Consumes a specified amount of bytes or characters from the buffered data.
    consume(l, s) {
      const f = this.head.data;
      if (l < f.length) {
        const d = f.slice(0, l);
        return this.head.data = f.slice(l), d;
      }
      return l === f.length ? this.shift() : s ? this._getString(l) : this._getBuffer(l);
    }
    first() {
      return this.head.data;
    }
    *[e]() {
      for (let l = this.head; l; l = l.next)
        yield l.data;
    }
    // Consumes a specified amount of characters from the buffered data.
    _getString(l) {
      let s = "", f = this.head, d = 0;
      do {
        const y = f.data;
        if (l > y.length)
          s += y, l -= y.length;
        else {
          l === y.length ? (s += y, ++d, f.next ? this.head = f.next : this.head = this.tail = null) : (s += t(y, 0, l), this.head = f, f.data = t(y, l));
          break;
        }
        ++d;
      } while ((f = f.next) !== null);
      return this.length -= d, s;
    }
    // Consumes a specified amount of bytes from the buffered data.
    _getBuffer(l) {
      const s = a.allocUnsafe(l), f = l;
      let d = this.head, y = 0;
      do {
        const R = d.data;
        if (l > R.length)
          r(s, R, f - l), l -= R.length;
        else {
          l === R.length ? (r(s, R, f - l), ++y, d.next ? this.head = d.next : this.head = this.tail = null) : (r(s, new o(R.buffer, R.byteOffset, l), f - l), this.head = d, d.data = R.slice(l));
          break;
        }
        ++y;
      } while ((d = d.next) !== null);
      return this.length -= y, s;
    }
    // Make sure the linked list only shows the minimal necessary information.
    [/* @__PURE__ */ Symbol.for("nodejs.util.inspect.custom")](l, s) {
      return n(this, {
        ...s,
        // Only inspect one level.
        depth: 0,
        // It should not recurse.
        customInspect: !1
      });
    }
  }, yf;
}
var bf, Qb;
function us() {
  if (Qb) return bf;
  Qb = 1;
  const { MathFloor: t, NumberIsInteger: e } = Ye(), { validateInteger: r } = Dn(), { ERR_INVALID_ARG_VALUE: o } = dt().codes;
  let a = 16 * 1024, n = 16;
  function i(d, y, R) {
    return d.highWaterMark != null ? d.highWaterMark : y ? d[R] : null;
  }
  function l(d) {
    return d ? n : a;
  }
  function s(d, y) {
    r(y, "value", 0), d ? n = y : a = y;
  }
  function f(d, y, R, w) {
    const I = i(y, w, R);
    if (I != null) {
      if (!e(I) || I < 0) {
        const A = w ? `options.${R}` : "options.highWaterMark";
        throw new o(A, I);
      }
      return t(I);
    }
    return l(d.objectMode);
  }
  return bf = {
    getHighWaterMark: f,
    getDefaultHighWaterMark: l,
    setDefaultHighWaterMark: s
  }, bf;
}
var _f, e_;
function SS() {
  if (e_) return _f;
  e_ = 1;
  const t = je(), { PromisePrototypeThen: e, SymbolAsyncIterator: r, SymbolIterator: o } = Ye(), { Buffer: a } = Fe, { ERR_INVALID_ARG_TYPE: n, ERR_STREAM_NULL_VALUES: i } = dt().codes;
  function l(s, f, d) {
    let y;
    if (typeof f == "string" || f instanceof a)
      return new s({
        objectMode: !0,
        ...d,
        read() {
          this.push(f), this.push(null);
        }
      });
    let R;
    if (f && f[r])
      R = !0, y = f[r]();
    else if (f && f[o])
      R = !1, y = f[o]();
    else
      throw new n("iterable", ["Iterable"], f);
    const w = new s({
      objectMode: !0,
      highWaterMark: 1,
      // TODO(ronag): What options should be allowed?
      ...d
    });
    let I = !1;
    w._read = function() {
      I || (I = !0, u());
    }, w._destroy = function(g, p) {
      e(
        A(g),
        () => t.nextTick(p, g),
        // nextTick is here in case cb throws
        (b) => t.nextTick(p, b || g)
      );
    };
    async function A(g) {
      const p = g != null, b = typeof y.throw == "function";
      if (p && b) {
        const { value: C, done: N } = await y.throw(g);
        if (await C, N)
          return;
      }
      if (typeof y.return == "function") {
        const { value: C } = await y.return();
        await C;
      }
    }
    async function u() {
      for (; ; ) {
        try {
          const { value: g, done: p } = R ? await y.next() : y.next();
          if (p)
            w.push(null);
          else {
            const b = g && typeof g.then == "function" ? await g : g;
            if (b === null)
              throw I = !1, new i();
            if (w.push(b))
              continue;
            I = !1;
          }
        } catch (g) {
          w.destroy(g);
        }
        break;
      }
    }
    return w;
  }
  return _f = l, _f;
}
var wf, t_;
function fs() {
  if (t_) return wf;
  t_ = 1;
  const t = je(), {
    ArrayPrototypeIndexOf: e,
    NumberIsInteger: r,
    NumberIsNaN: o,
    NumberParseInt: a,
    ObjectDefineProperties: n,
    ObjectKeys: i,
    ObjectSetPrototypeOf: l,
    Promise: s,
    SafeSet: f,
    SymbolAsyncDispose: d,
    SymbolAsyncIterator: y,
    Symbol: R
  } = Ye();
  wf = z, z.ReadableState = te;
  const { EventEmitter: w } = Ke, { Stream: I, prependListener: A } = Gc(), { Buffer: u } = Fe, { addAbortSignal: g } = ls(), p = br();
  let b = bt().debuglog("stream", (h) => {
    b = h;
  });
  const C = $A(), N = Br(), { getHighWaterMark: W, getDefaultHighWaterMark: k } = us(), {
    aggregateTwoErrors: L,
    codes: {
      ERR_INVALID_ARG_TYPE: U,
      ERR_METHOD_NOT_IMPLEMENTED: x,
      ERR_OUT_OF_RANGE: D,
      ERR_STREAM_PUSH_AFTER_EOF: F,
      ERR_STREAM_UNSHIFT_AFTER_END_EVENT: T
    },
    AbortError: J
  } = dt(), { validateObject: re } = Dn(), se = R("kPaused"), { StringDecoder: ne } = Ji(), M = SS();
  l(z.prototype, I.prototype), l(z, I);
  const Q = () => {
  }, { errorOrDestroy: v } = N, $ = 1, ee = 2, B = 4, le = 8, ae = 16, ce = 32, Y = 64, P = 128, q = 256, V = 512, Z = 1024, X = 2048, m = 4096, c = 8192, E = 16384, j = 32768, G = 65536, K = 1 << 17, O = 1 << 18;
  function H(h) {
    return {
      enumerable: !1,
      get() {
        return (this.state & h) !== 0;
      },
      set(S) {
        S ? this.state |= h : this.state &= ~h;
      }
    };
  }
  n(te.prototype, {
    objectMode: H($),
    ended: H(ee),
    endEmitted: H(B),
    reading: H(le),
    // Stream is still being constructed and cannot be
    // destroyed until construction finished or failed.
    // Async construction is opt in, therefore we start as
    // constructed.
    constructed: H(ae),
    // A flag to be able to tell if the event 'readable'/'data' is emitted
    // immediately, or on a later tick.  We set this to true at first, because
    // any actions that shouldn't happen until "later" should generally also
    // not happen before the first read call.
    sync: H(ce),
    // Whenever we return null, then we set a flag to say
    // that we're awaiting a 'readable' event emission.
    needReadable: H(Y),
    emittedReadable: H(P),
    readableListening: H(q),
    resumeScheduled: H(V),
    // True if the error was already emitted and should not be thrown again.
    errorEmitted: H(Z),
    emitClose: H(X),
    autoDestroy: H(m),
    // Has it been destroyed.
    destroyed: H(c),
    // Indicates whether the stream has finished destroying.
    closed: H(E),
    // True if close has been emitted or would have been emitted
    // depending on emitClose.
    closeEmitted: H(j),
    multiAwaitDrain: H(G),
    // If true, a maybeReadMore has been scheduled.
    readingMore: H(K),
    dataEmitted: H(O)
  });
  function te(h, S, ie) {
    typeof ie != "boolean" && (ie = S instanceof Qt()), this.state = X | m | ae | ce, h && h.objectMode && (this.state |= $), ie && h && h.readableObjectMode && (this.state |= $), this.highWaterMark = h ? W(this, h, "readableHighWaterMark", ie) : k(!1), this.buffer = new C(), this.length = 0, this.pipes = [], this.flowing = null, this[se] = null, h && h.emitClose === !1 && (this.state &= ~X), h && h.autoDestroy === !1 && (this.state &= ~m), this.errored = null, this.defaultEncoding = h && h.defaultEncoding || "utf8", this.awaitDrainWriters = null, this.decoder = null, this.encoding = null, h && h.encoding && (this.decoder = new ne(h.encoding), this.encoding = h.encoding);
  }
  function z(h) {
    if (!(this instanceof z)) return new z(h);
    const S = this instanceof Qt();
    this._readableState = new te(h, this, S), h && (typeof h.read == "function" && (this._read = h.read), typeof h.destroy == "function" && (this._destroy = h.destroy), typeof h.construct == "function" && (this._construct = h.construct), h.signal && !S && g(h.signal, this)), I.call(this, h), N.construct(this, () => {
      this._readableState.needReadable && me(this, this._readableState);
    });
  }
  z.prototype.destroy = N.destroy, z.prototype._undestroy = N.undestroy, z.prototype._destroy = function(h, S) {
    S(h);
  }, z.prototype[w.captureRejectionSymbol] = function(h) {
    this.destroy(h);
  }, z.prototype[d] = function() {
    let h;
    return this.destroyed || (h = this.readableEnded ? null : new J(), this.destroy(h)), new s((S, ie) => p(this, (oe) => oe && oe !== h ? ie(oe) : S(null)));
  }, z.prototype.push = function(h, S) {
    return de(this, h, S, !1);
  }, z.prototype.unshift = function(h, S) {
    return de(this, h, S, !0);
  };
  function de(h, S, ie, oe) {
    b("readableAddChunk", S);
    const fe = h._readableState;
    let ve;
    if ((fe.state & $) === 0 && (typeof S == "string" ? (ie = ie || fe.defaultEncoding, fe.encoding !== ie && (oe && fe.encoding ? S = u.from(S, ie).toString(fe.encoding) : (S = u.from(S, ie), ie = ""))) : S instanceof u ? ie = "" : I._isUint8Array(S) ? (S = I._uint8ArrayToBuffer(S), ie = "") : S != null && (ve = new U("chunk", ["string", "Buffer", "Uint8Array"], S))), ve)
      v(h, ve);
    else if (S === null)
      fe.state &= ~le, _e(h, fe);
    else if ((fe.state & $) !== 0 || S && S.length > 0)
      if (oe)
        if ((fe.state & B) !== 0) v(h, new T());
        else {
          if (fe.destroyed || fe.errored) return !1;
          pe(h, fe, S, !0);
        }
      else if (fe.ended)
        v(h, new F());
      else {
        if (fe.destroyed || fe.errored)
          return !1;
        fe.state &= ~le, fe.decoder && !ie ? (S = fe.decoder.write(S), fe.objectMode || S.length !== 0 ? pe(h, fe, S, !1) : me(h, fe)) : pe(h, fe, S, !1);
      }
    else oe || (fe.state &= ~le, me(h, fe));
    return !fe.ended && (fe.length < fe.highWaterMark || fe.length === 0);
  }
  function pe(h, S, ie, oe) {
    S.flowing && S.length === 0 && !S.sync && h.listenerCount("data") > 0 ? ((S.state & G) !== 0 ? S.awaitDrainWriters.clear() : S.awaitDrainWriters = null, S.dataEmitted = !0, h.emit("data", ie)) : (S.length += S.objectMode ? 1 : ie.length, oe ? S.buffer.unshift(ie) : S.buffer.push(ie), (S.state & Y) !== 0 && Ae(h)), me(h, S);
  }
  z.prototype.isPaused = function() {
    const h = this._readableState;
    return h[se] === !0 || h.flowing === !1;
  }, z.prototype.setEncoding = function(h) {
    const S = new ne(h);
    this._readableState.decoder = S, this._readableState.encoding = this._readableState.decoder.encoding;
    const ie = this._readableState.buffer;
    let oe = "";
    for (const fe of ie)
      oe += S.write(fe);
    return ie.clear(), oe !== "" && ie.push(oe), this._readableState.length = oe.length, this;
  };
  const he = 1073741824;
  function we(h) {
    if (h > he)
      throw new D("size", "<= 1GiB", h);
    return h--, h |= h >>> 1, h |= h >>> 2, h |= h >>> 4, h |= h >>> 8, h |= h >>> 16, h++, h;
  }
  function be(h, S) {
    return h <= 0 || S.length === 0 && S.ended ? 0 : (S.state & $) !== 0 ? 1 : o(h) ? S.flowing && S.length ? S.buffer.first().length : S.length : h <= S.length ? h : S.ended ? S.length : 0;
  }
  z.prototype.read = function(h) {
    b("read", h), h === void 0 ? h = NaN : r(h) || (h = a(h, 10));
    const S = this._readableState, ie = h;
    if (h > S.highWaterMark && (S.highWaterMark = we(h)), h !== 0 && (S.state &= ~P), h === 0 && S.needReadable && ((S.highWaterMark !== 0 ? S.length >= S.highWaterMark : S.length > 0) || S.ended))
      return b("read: emitReadable", S.length, S.ended), S.length === 0 && S.ended ? tt(this) : Ae(this), null;
    if (h = be(h, S), h === 0 && S.ended)
      return S.length === 0 && tt(this), null;
    let oe = (S.state & Y) !== 0;
    if (b("need readable", oe), (S.length === 0 || S.length - h < S.highWaterMark) && (oe = !0, b("length less than watermark", oe)), S.ended || S.reading || S.destroyed || S.errored || !S.constructed)
      oe = !1, b("reading, ended or constructing", oe);
    else if (oe) {
      b("do read"), S.state |= le | ce, S.length === 0 && (S.state |= Y);
      try {
        this._read(S.highWaterMark);
      } catch (ve) {
        v(this, ve);
      }
      S.state &= ~ce, S.reading || (h = be(ie, S));
    }
    let fe;
    return h > 0 ? fe = mt(h, S) : fe = null, fe === null ? (S.needReadable = S.length <= S.highWaterMark, h = 0) : (S.length -= h, S.multiAwaitDrain ? S.awaitDrainWriters.clear() : S.awaitDrainWriters = null), S.length === 0 && (S.ended || (S.needReadable = !0), ie !== h && S.ended && tt(this)), fe !== null && !S.errorEmitted && !S.closeEmitted && (S.dataEmitted = !0, this.emit("data", fe)), fe;
  };
  function _e(h, S) {
    if (b("onEofChunk"), !S.ended) {
      if (S.decoder) {
        const ie = S.decoder.end();
        ie && ie.length && (S.buffer.push(ie), S.length += S.objectMode ? 1 : ie.length);
      }
      S.ended = !0, S.sync ? Ae(h) : (S.needReadable = !1, S.emittedReadable = !0, xe(h));
    }
  }
  function Ae(h) {
    const S = h._readableState;
    b("emitReadable", S.needReadable, S.emittedReadable), S.needReadable = !1, S.emittedReadable || (b("emitReadable", S.flowing), S.emittedReadable = !0, t.nextTick(xe, h));
  }
  function xe(h) {
    const S = h._readableState;
    b("emitReadable_", S.destroyed, S.length, S.ended), !S.destroyed && !S.errored && (S.length || S.ended) && (h.emit("readable"), S.emittedReadable = !1), S.needReadable = !S.flowing && !S.ended && S.length <= S.highWaterMark, ht(h);
  }
  function me(h, S) {
    !S.readingMore && S.constructed && (S.readingMore = !0, t.nextTick(Ge, h, S));
  }
  function Ge(h, S) {
    for (; !S.reading && !S.ended && (S.length < S.highWaterMark || S.flowing && S.length === 0); ) {
      const ie = S.length;
      if (b("maybeReadMore read 0"), h.read(0), ie === S.length)
        break;
    }
    S.readingMore = !1;
  }
  z.prototype._read = function(h) {
    throw new x("_read()");
  }, z.prototype.pipe = function(h, S) {
    const ie = this, oe = this._readableState;
    oe.pipes.length === 1 && (oe.multiAwaitDrain || (oe.multiAwaitDrain = !0, oe.awaitDrainWriters = new f(oe.awaitDrainWriters ? [oe.awaitDrainWriters] : []))), oe.pipes.push(h), b("pipe count=%d opts=%j", oe.pipes.length, S);
    const ve = (!S || S.end !== !1) && h !== t.stdout && h !== t.stderr ? Et : $e;
    oe.endEmitted ? t.nextTick(ve) : ie.once("end", ve), h.on("unpipe", Te);
    function Te(Ce, De) {
      b("onunpipe"), Ce === ie && De && De.hasUnpiped === !1 && (De.hasUnpiped = !0, Mt());
    }
    function Et() {
      b("onend"), h.end();
    }
    let Pe, lt = !1;
    function Mt() {
      b("cleanup"), h.removeListener("close", it), h.removeListener("finish", st), Pe && h.removeListener("drain", Pe), h.removeListener("error", nt), h.removeListener("unpipe", Te), ie.removeListener("end", Et), ie.removeListener("end", $e), ie.removeListener("data", Rt), lt = !0, Pe && oe.awaitDrainWriters && (!h._writableState || h._writableState.needDrain) && Pe();
    }
    function vt() {
      lt || (oe.pipes.length === 1 && oe.pipes[0] === h ? (b("false write response, pause", 0), oe.awaitDrainWriters = h, oe.multiAwaitDrain = !1) : oe.pipes.length > 1 && oe.pipes.includes(h) && (b("false write response, pause", oe.awaitDrainWriters.size), oe.awaitDrainWriters.add(h)), ie.pause()), Pe || (Pe = Je(ie, h), h.on("drain", Pe));
    }
    ie.on("data", Rt);
    function Rt(Ce) {
      b("ondata");
      const De = h.write(Ce);
      b("dest.write", De), De === !1 && vt();
    }
    function nt(Ce) {
      if (b("onerror", Ce), $e(), h.removeListener("error", nt), h.listenerCount("error") === 0) {
        const De = h._writableState || h._readableState;
        De && !De.errorEmitted ? v(h, Ce) : h.emit("error", Ce);
      }
    }
    A(h, "error", nt);
    function it() {
      h.removeListener("finish", st), $e();
    }
    h.once("close", it);
    function st() {
      b("onfinish"), h.removeListener("close", it), $e();
    }
    h.once("finish", st);
    function $e() {
      b("unpipe"), ie.unpipe(h);
    }
    return h.emit("pipe", ie), h.writableNeedDrain === !0 ? vt() : oe.flowing || (b("pipe resume"), ie.resume()), h;
  };
  function Je(h, S) {
    return function() {
      const oe = h._readableState;
      oe.awaitDrainWriters === S ? (b("pipeOnDrain", 1), oe.awaitDrainWriters = null) : oe.multiAwaitDrain && (b("pipeOnDrain", oe.awaitDrainWriters.size), oe.awaitDrainWriters.delete(S)), (!oe.awaitDrainWriters || oe.awaitDrainWriters.size === 0) && h.listenerCount("data") && h.resume();
    };
  }
  z.prototype.unpipe = function(h) {
    const S = this._readableState, ie = {
      hasUnpiped: !1
    };
    if (S.pipes.length === 0) return this;
    if (!h) {
      const fe = S.pipes;
      S.pipes = [], this.pause();
      for (let ve = 0; ve < fe.length; ve++)
        fe[ve].emit("unpipe", this, {
          hasUnpiped: !1
        });
      return this;
    }
    const oe = e(S.pipes, h);
    return oe === -1 ? this : (S.pipes.splice(oe, 1), S.pipes.length === 0 && this.pause(), h.emit("unpipe", this, ie), this);
  }, z.prototype.on = function(h, S) {
    const ie = I.prototype.on.call(this, h, S), oe = this._readableState;
    return h === "data" ? (oe.readableListening = this.listenerCount("readable") > 0, oe.flowing !== !1 && this.resume()) : h === "readable" && !oe.endEmitted && !oe.readableListening && (oe.readableListening = oe.needReadable = !0, oe.flowing = !1, oe.emittedReadable = !1, b("on readable", oe.length, oe.reading), oe.length ? Ae(this) : oe.reading || t.nextTick(jt, this)), ie;
  }, z.prototype.addListener = z.prototype.on, z.prototype.removeListener = function(h, S) {
    const ie = I.prototype.removeListener.call(this, h, S);
    return h === "readable" && t.nextTick(Xe, this), ie;
  }, z.prototype.off = z.prototype.removeListener, z.prototype.removeAllListeners = function(h) {
    const S = I.prototype.removeAllListeners.apply(this, arguments);
    return (h === "readable" || h === void 0) && t.nextTick(Xe, this), S;
  };
  function Xe(h) {
    const S = h._readableState;
    S.readableListening = h.listenerCount("readable") > 0, S.resumeScheduled && S[se] === !1 ? S.flowing = !0 : h.listenerCount("data") > 0 ? h.resume() : S.readableListening || (S.flowing = null);
  }
  function jt(h) {
    b("readable nexttick read 0"), h.read(0);
  }
  z.prototype.resume = function() {
    const h = this._readableState;
    return h.flowing || (b("resume"), h.flowing = !h.readableListening, Ct(this, h)), h[se] = !1, this;
  };
  function Ct(h, S) {
    S.resumeScheduled || (S.resumeScheduled = !0, t.nextTick($t, h, S));
  }
  function $t(h, S) {
    b("resume", S.reading), S.reading || h.read(0), S.resumeScheduled = !1, h.emit("resume"), ht(h), S.flowing && !S.reading && h.read(0);
  }
  z.prototype.pause = function() {
    return b("call pause flowing=%j", this._readableState.flowing), this._readableState.flowing !== !1 && (b("pause"), this._readableState.flowing = !1, this.emit("pause")), this._readableState[se] = !0, this;
  };
  function ht(h) {
    const S = h._readableState;
    for (b("flow", S.flowing); S.flowing && h.read() !== null; ) ;
  }
  z.prototype.wrap = function(h) {
    let S = !1;
    h.on("data", (oe) => {
      !this.push(oe) && h.pause && (S = !0, h.pause());
    }), h.on("end", () => {
      this.push(null);
    }), h.on("error", (oe) => {
      v(this, oe);
    }), h.on("close", () => {
      this.destroy();
    }), h.on("destroy", () => {
      this.destroy();
    }), this._read = () => {
      S && h.resume && (S = !1, h.resume());
    };
    const ie = i(h);
    for (let oe = 1; oe < ie.length; oe++) {
      const fe = ie[oe];
      this[fe] === void 0 && typeof h[fe] == "function" && (this[fe] = h[fe].bind(h));
    }
    return this;
  }, z.prototype[y] = function() {
    return wt(this);
  }, z.prototype.iterator = function(h) {
    return h !== void 0 && re(h, "options"), wt(this, h);
  };
  function wt(h, S) {
    typeof h.read != "function" && (h = z.wrap(h, {
      objectMode: !0
    }));
    const ie = Wt(h, S);
    return ie.stream = h, ie;
  }
  async function* Wt(h, S) {
    let ie = Q;
    function oe(Te) {
      this === h ? (ie(), ie = Q) : ie = Te;
    }
    h.on("readable", oe);
    let fe;
    const ve = p(
      h,
      {
        writable: !1
      },
      (Te) => {
        fe = Te ? L(fe, Te) : null, ie(), ie = Q;
      }
    );
    try {
      for (; ; ) {
        const Te = h.destroyed ? null : h.read();
        if (Te !== null)
          yield Te;
        else {
          if (fe)
            throw fe;
          if (fe === null)
            return;
          await new s(oe);
        }
      }
    } catch (Te) {
      throw fe = L(fe, Te), fe;
    } finally {
      (fe || S?.destroyOnReturn !== !1) && (fe === void 0 || h._readableState.autoDestroy) ? N.destroyer(h, null) : (h.off("readable", oe), ve());
    }
  }
  n(z.prototype, {
    readable: {
      __proto__: null,
      get() {
        const h = this._readableState;
        return !!h && h.readable !== !1 && !h.destroyed && !h.errorEmitted && !h.endEmitted;
      },
      set(h) {
        this._readableState && (this._readableState.readable = !!h);
      }
    },
    readableDidRead: {
      __proto__: null,
      enumerable: !1,
      get: function() {
        return this._readableState.dataEmitted;
      }
    },
    readableAborted: {
      __proto__: null,
      enumerable: !1,
      get: function() {
        return !!(this._readableState.readable !== !1 && (this._readableState.destroyed || this._readableState.errored) && !this._readableState.endEmitted);
      }
    },
    readableHighWaterMark: {
      __proto__: null,
      enumerable: !1,
      get: function() {
        return this._readableState.highWaterMark;
      }
    },
    readableBuffer: {
      __proto__: null,
      enumerable: !1,
      get: function() {
        return this._readableState && this._readableState.buffer;
      }
    },
    readableFlowing: {
      __proto__: null,
      enumerable: !1,
      get: function() {
        return this._readableState.flowing;
      },
      set: function(h) {
        this._readableState && (this._readableState.flowing = h);
      }
    },
    readableLength: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._readableState.length;
      }
    },
    readableObjectMode: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._readableState ? this._readableState.objectMode : !1;
      }
    },
    readableEncoding: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._readableState ? this._readableState.encoding : null;
      }
    },
    errored: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._readableState ? this._readableState.errored : null;
      }
    },
    closed: {
      __proto__: null,
      get() {
        return this._readableState ? this._readableState.closed : !1;
      }
    },
    destroyed: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._readableState ? this._readableState.destroyed : !1;
      },
      set(h) {
        this._readableState && (this._readableState.destroyed = h);
      }
    },
    readableEnded: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._readableState ? this._readableState.endEmitted : !1;
      }
    }
  }), n(te.prototype, {
    // Legacy getter for `pipesCount`.
    pipesCount: {
      __proto__: null,
      get() {
        return this.pipes.length;
      }
    },
    // Legacy property for `paused`.
    paused: {
      __proto__: null,
      get() {
        return this[se] !== !1;
      },
      set(h) {
        this[se] = !!h;
      }
    }
  }), z._fromList = mt;
  function mt(h, S) {
    if (S.length === 0) return null;
    let ie;
    return S.objectMode ? ie = S.buffer.shift() : !h || h >= S.length ? (S.decoder ? ie = S.buffer.join("") : S.buffer.length === 1 ? ie = S.buffer.first() : ie = S.buffer.concat(S.length), S.buffer.clear()) : ie = S.buffer.consume(h, S.decoder), ie;
  }
  function tt(h) {
    const S = h._readableState;
    b("endReadable", S.endEmitted), S.endEmitted || (S.ended = !0, t.nextTick(qt, S, h));
  }
  function qt(h, S) {
    if (b("endReadableNT", h.endEmitted, h.length), !h.errored && !h.closeEmitted && !h.endEmitted && h.length === 0) {
      if (h.endEmitted = !0, S.emit("end"), S.writable && S.allowHalfOpen === !1)
        t.nextTick(Bt, S);
      else if (h.autoDestroy) {
        const ie = S._writableState;
        (!ie || ie.autoDestroy && // We don't expect the writable to ever 'finish'
        // if writable is explicitly set to false.
        (ie.finished || ie.writable === !1)) && S.destroy();
      }
    }
  }
  function Bt(h) {
    h.writable && !h.writableEnded && !h.destroyed && h.end();
  }
  z.from = function(h, S) {
    return M(z, h, S);
  };
  let rt;
  function St() {
    return rt === void 0 && (rt = {}), rt;
  }
  return z.fromWeb = function(h, S) {
    return St().newStreamReadableFromReadableStream(h, S);
  }, z.toWeb = function(h, S) {
    return St().newReadableStreamFromStreamReadable(h, S);
  }, z.wrap = function(h, S) {
    var ie, oe;
    return new z({
      objectMode: (ie = (oe = h.readableObjectMode) !== null && oe !== void 0 ? oe : h.objectMode) !== null && ie !== void 0 ? ie : !0,
      ...S,
      destroy(fe, ve) {
        N.destroyer(h, fe), ve(fe);
      }
    }).wrap(h);
  }, wf;
}
var mf, r_;
function Hc() {
  if (r_) return mf;
  r_ = 1;
  const t = je(), {
    ArrayPrototypeSlice: e,
    Error: r,
    FunctionPrototypeSymbolHasInstance: o,
    ObjectDefineProperty: a,
    ObjectDefineProperties: n,
    ObjectSetPrototypeOf: i,
    StringPrototypeToLowerCase: l,
    Symbol: s,
    SymbolHasInstance: f
  } = Ye();
  mf = re, re.WritableState = T;
  const { EventEmitter: d } = Ke, y = Gc().Stream, { Buffer: R } = Fe, w = Br(), { addAbortSignal: I } = ls(), { getHighWaterMark: A, getDefaultHighWaterMark: u } = us(), {
    ERR_INVALID_ARG_TYPE: g,
    ERR_METHOD_NOT_IMPLEMENTED: p,
    ERR_MULTIPLE_CALLBACK: b,
    ERR_STREAM_CANNOT_PIPE: C,
    ERR_STREAM_DESTROYED: N,
    ERR_STREAM_ALREADY_FINISHED: W,
    ERR_STREAM_NULL_VALUES: k,
    ERR_STREAM_WRITE_AFTER_END: L,
    ERR_UNKNOWN_ENCODING: U
  } = dt().codes, { errorOrDestroy: x } = w;
  i(re.prototype, y.prototype), i(re, y);
  function D() {
  }
  const F = s("kOnFinished");
  function T(m, c, E) {
    typeof E != "boolean" && (E = c instanceof Qt()), this.objectMode = !!(m && m.objectMode), E && (this.objectMode = this.objectMode || !!(m && m.writableObjectMode)), this.highWaterMark = m ? A(this, m, "writableHighWaterMark", E) : u(!1), this.finalCalled = !1, this.needDrain = !1, this.ending = !1, this.ended = !1, this.finished = !1, this.destroyed = !1;
    const j = !!(m && m.decodeStrings === !1);
    this.decodeStrings = !j, this.defaultEncoding = m && m.defaultEncoding || "utf8", this.length = 0, this.writing = !1, this.corked = 0, this.sync = !0, this.bufferProcessing = !1, this.onwrite = v.bind(void 0, c), this.writecb = null, this.writelen = 0, this.afterWriteTickInfo = null, J(this), this.pendingcb = 0, this.constructed = !0, this.prefinished = !1, this.errorEmitted = !1, this.emitClose = !m || m.emitClose !== !1, this.autoDestroy = !m || m.autoDestroy !== !1, this.errored = null, this.closed = !1, this.closeEmitted = !1, this[F] = [];
  }
  function J(m) {
    m.buffered = [], m.bufferedIndex = 0, m.allBuffers = !0, m.allNoop = !0;
  }
  T.prototype.getBuffer = function() {
    return e(this.buffered, this.bufferedIndex);
  }, a(T.prototype, "bufferedRequestCount", {
    __proto__: null,
    get() {
      return this.buffered.length - this.bufferedIndex;
    }
  });
  function re(m) {
    const c = this instanceof Qt();
    if (!c && !o(re, this)) return new re(m);
    this._writableState = new T(m, this, c), m && (typeof m.write == "function" && (this._write = m.write), typeof m.writev == "function" && (this._writev = m.writev), typeof m.destroy == "function" && (this._destroy = m.destroy), typeof m.final == "function" && (this._final = m.final), typeof m.construct == "function" && (this._construct = m.construct), m.signal && I(m.signal, this)), y.call(this, m), w.construct(this, () => {
      const E = this._writableState;
      E.writing || le(this, E), P(this, E);
    });
  }
  a(re, f, {
    __proto__: null,
    value: function(m) {
      return o(this, m) ? !0 : this !== re ? !1 : m && m._writableState instanceof T;
    }
  }), re.prototype.pipe = function() {
    x(this, new C());
  };
  function se(m, c, E, j) {
    const G = m._writableState;
    if (typeof E == "function")
      j = E, E = G.defaultEncoding;
    else {
      if (!E) E = G.defaultEncoding;
      else if (E !== "buffer" && !R.isEncoding(E)) throw new U(E);
      typeof j != "function" && (j = D);
    }
    if (c === null)
      throw new k();
    if (!G.objectMode)
      if (typeof c == "string")
        G.decodeStrings !== !1 && (c = R.from(c, E), E = "buffer");
      else if (c instanceof R)
        E = "buffer";
      else if (y._isUint8Array(c))
        c = y._uint8ArrayToBuffer(c), E = "buffer";
      else
        throw new g("chunk", ["string", "Buffer", "Uint8Array"], c);
    let K;
    return G.ending ? K = new L() : G.destroyed && (K = new N("write")), K ? (t.nextTick(j, K), x(m, K, !0), K) : (G.pendingcb++, ne(m, G, c, E, j));
  }
  re.prototype.write = function(m, c, E) {
    return se(this, m, c, E) === !0;
  }, re.prototype.cork = function() {
    this._writableState.corked++;
  }, re.prototype.uncork = function() {
    const m = this._writableState;
    m.corked && (m.corked--, m.writing || le(this, m));
  }, re.prototype.setDefaultEncoding = function(c) {
    if (typeof c == "string" && (c = l(c)), !R.isEncoding(c)) throw new U(c);
    return this._writableState.defaultEncoding = c, this;
  };
  function ne(m, c, E, j, G) {
    const K = c.objectMode ? 1 : E.length;
    c.length += K;
    const O = c.length < c.highWaterMark;
    return O || (c.needDrain = !0), c.writing || c.corked || c.errored || !c.constructed ? (c.buffered.push({
      chunk: E,
      encoding: j,
      callback: G
    }), c.allBuffers && j !== "buffer" && (c.allBuffers = !1), c.allNoop && G !== D && (c.allNoop = !1)) : (c.writelen = K, c.writecb = G, c.writing = !0, c.sync = !0, m._write(E, j, c.onwrite), c.sync = !1), O && !c.errored && !c.destroyed;
  }
  function M(m, c, E, j, G, K, O) {
    c.writelen = j, c.writecb = O, c.writing = !0, c.sync = !0, c.destroyed ? c.onwrite(new N("write")) : E ? m._writev(G, c.onwrite) : m._write(G, K, c.onwrite), c.sync = !1;
  }
  function Q(m, c, E, j) {
    --c.pendingcb, j(E), B(c), x(m, E);
  }
  function v(m, c) {
    const E = m._writableState, j = E.sync, G = E.writecb;
    if (typeof G != "function") {
      x(m, new b());
      return;
    }
    E.writing = !1, E.writecb = null, E.length -= E.writelen, E.writelen = 0, c ? (c.stack, E.errored || (E.errored = c), m._readableState && !m._readableState.errored && (m._readableState.errored = c), j ? t.nextTick(Q, m, E, c, G) : Q(m, E, c, G)) : (E.buffered.length > E.bufferedIndex && le(m, E), j ? E.afterWriteTickInfo !== null && E.afterWriteTickInfo.cb === G ? E.afterWriteTickInfo.count++ : (E.afterWriteTickInfo = {
      count: 1,
      cb: G,
      stream: m,
      state: E
    }, t.nextTick($, E.afterWriteTickInfo)) : ee(m, E, 1, G));
  }
  function $({ stream: m, state: c, count: E, cb: j }) {
    return c.afterWriteTickInfo = null, ee(m, c, E, j);
  }
  function ee(m, c, E, j) {
    for (!c.ending && !m.destroyed && c.length === 0 && c.needDrain && (c.needDrain = !1, m.emit("drain")); E-- > 0; )
      c.pendingcb--, j();
    c.destroyed && B(c), P(m, c);
  }
  function B(m) {
    if (m.writing)
      return;
    for (let G = m.bufferedIndex; G < m.buffered.length; ++G) {
      var c;
      const { chunk: K, callback: O } = m.buffered[G], H = m.objectMode ? 1 : K.length;
      m.length -= H, O(
        (c = m.errored) !== null && c !== void 0 ? c : new N("write")
      );
    }
    const E = m[F].splice(0);
    for (let G = 0; G < E.length; G++) {
      var j;
      E[G](
        (j = m.errored) !== null && j !== void 0 ? j : new N("end")
      );
    }
    J(m);
  }
  function le(m, c) {
    if (c.corked || c.bufferProcessing || c.destroyed || !c.constructed)
      return;
    const { buffered: E, bufferedIndex: j, objectMode: G } = c, K = E.length - j;
    if (!K)
      return;
    let O = j;
    if (c.bufferProcessing = !0, K > 1 && m._writev) {
      c.pendingcb -= K - 1;
      const H = c.allNoop ? D : (z) => {
        for (let de = O; de < E.length; ++de)
          E[de].callback(z);
      }, te = c.allNoop && O === 0 ? E : e(E, O);
      te.allBuffers = c.allBuffers, M(m, c, !0, c.length, te, "", H), J(c);
    } else {
      do {
        const { chunk: H, encoding: te, callback: z } = E[O];
        E[O++] = null;
        const de = G ? 1 : H.length;
        M(m, c, !1, de, H, te, z);
      } while (O < E.length && !c.writing);
      O === E.length ? J(c) : O > 256 ? (E.splice(0, O), c.bufferedIndex = 0) : c.bufferedIndex = O;
    }
    c.bufferProcessing = !1;
  }
  re.prototype._write = function(m, c, E) {
    if (this._writev)
      this._writev(
        [
          {
            chunk: m,
            encoding: c
          }
        ],
        E
      );
    else
      throw new p("_write()");
  }, re.prototype._writev = null, re.prototype.end = function(m, c, E) {
    const j = this._writableState;
    typeof m == "function" ? (E = m, m = null, c = null) : typeof c == "function" && (E = c, c = null);
    let G;
    if (m != null) {
      const K = se(this, m, c);
      K instanceof r && (G = K);
    }
    return j.corked && (j.corked = 1, this.uncork()), G || (!j.errored && !j.ending ? (j.ending = !0, P(this, j, !0), j.ended = !0) : j.finished ? G = new W("end") : j.destroyed && (G = new N("end"))), typeof E == "function" && (G || j.finished ? t.nextTick(E, G) : j[F].push(E)), this;
  };
  function ae(m) {
    return m.ending && !m.destroyed && m.constructed && m.length === 0 && !m.errored && m.buffered.length === 0 && !m.finished && !m.writing && !m.errorEmitted && !m.closeEmitted;
  }
  function ce(m, c) {
    let E = !1;
    function j(G) {
      if (E) {
        x(m, G ?? b());
        return;
      }
      if (E = !0, c.pendingcb--, G) {
        const K = c[F].splice(0);
        for (let O = 0; O < K.length; O++)
          K[O](G);
        x(m, G, c.sync);
      } else ae(c) && (c.prefinished = !0, m.emit("prefinish"), c.pendingcb++, t.nextTick(q, m, c));
    }
    c.sync = !0, c.pendingcb++;
    try {
      m._final(j);
    } catch (G) {
      j(G);
    }
    c.sync = !1;
  }
  function Y(m, c) {
    !c.prefinished && !c.finalCalled && (typeof m._final == "function" && !c.destroyed ? (c.finalCalled = !0, ce(m, c)) : (c.prefinished = !0, m.emit("prefinish")));
  }
  function P(m, c, E) {
    ae(c) && (Y(m, c), c.pendingcb === 0 && (E ? (c.pendingcb++, t.nextTick(
      (j, G) => {
        ae(G) ? q(j, G) : G.pendingcb--;
      },
      m,
      c
    )) : ae(c) && (c.pendingcb++, q(m, c))));
  }
  function q(m, c) {
    c.pendingcb--, c.finished = !0;
    const E = c[F].splice(0);
    for (let j = 0; j < E.length; j++)
      E[j]();
    if (m.emit("finish"), c.autoDestroy) {
      const j = m._readableState;
      (!j || j.autoDestroy && // We don't expect the readable to ever 'end'
      // if readable is explicitly set to false.
      (j.endEmitted || j.readable === !1)) && m.destroy();
    }
  }
  n(re.prototype, {
    closed: {
      __proto__: null,
      get() {
        return this._writableState ? this._writableState.closed : !1;
      }
    },
    destroyed: {
      __proto__: null,
      get() {
        return this._writableState ? this._writableState.destroyed : !1;
      },
      set(m) {
        this._writableState && (this._writableState.destroyed = m);
      }
    },
    writable: {
      __proto__: null,
      get() {
        const m = this._writableState;
        return !!m && m.writable !== !1 && !m.destroyed && !m.errored && !m.ending && !m.ended;
      },
      set(m) {
        this._writableState && (this._writableState.writable = !!m);
      }
    },
    writableFinished: {
      __proto__: null,
      get() {
        return this._writableState ? this._writableState.finished : !1;
      }
    },
    writableObjectMode: {
      __proto__: null,
      get() {
        return this._writableState ? this._writableState.objectMode : !1;
      }
    },
    writableBuffer: {
      __proto__: null,
      get() {
        return this._writableState && this._writableState.getBuffer();
      }
    },
    writableEnded: {
      __proto__: null,
      get() {
        return this._writableState ? this._writableState.ending : !1;
      }
    },
    writableNeedDrain: {
      __proto__: null,
      get() {
        const m = this._writableState;
        return m ? !m.destroyed && !m.ending && m.needDrain : !1;
      }
    },
    writableHighWaterMark: {
      __proto__: null,
      get() {
        return this._writableState && this._writableState.highWaterMark;
      }
    },
    writableCorked: {
      __proto__: null,
      get() {
        return this._writableState ? this._writableState.corked : 0;
      }
    },
    writableLength: {
      __proto__: null,
      get() {
        return this._writableState && this._writableState.length;
      }
    },
    errored: {
      __proto__: null,
      enumerable: !1,
      get() {
        return this._writableState ? this._writableState.errored : null;
      }
    },
    writableAborted: {
      __proto__: null,
      enumerable: !1,
      get: function() {
        return !!(this._writableState.writable !== !1 && (this._writableState.destroyed || this._writableState.errored) && !this._writableState.finished);
      }
    }
  });
  const V = w.destroy;
  re.prototype.destroy = function(m, c) {
    const E = this._writableState;
    return !E.destroyed && (E.bufferedIndex < E.buffered.length || E[F].length) && t.nextTick(B, E), V.call(this, m, c), this;
  }, re.prototype._undestroy = w.undestroy, re.prototype._destroy = function(m, c) {
    c(m);
  }, re.prototype[d.captureRejectionSymbol] = function(m) {
    this.destroy(m);
  };
  let Z;
  function X() {
    return Z === void 0 && (Z = {}), Z;
  }
  return re.fromWeb = function(m, c) {
    return X().newStreamWritableFromWritableStream(m, c);
  }, re.toWeb = function(m) {
    return X().newWritableStreamFromStreamWritable(m);
  }, mf;
}
var Sf, n_;
function WA() {
  if (n_) return Sf;
  n_ = 1;
  const t = je(), e = Fe, {
    isReadable: r,
    isWritable: o,
    isIterable: a,
    isNodeStream: n,
    isReadableNodeStream: i,
    isWritableNodeStream: l,
    isDuplexNodeStream: s,
    isReadableStream: f,
    isWritableStream: d
  } = or(), y = br(), {
    AbortError: R,
    codes: { ERR_INVALID_ARG_TYPE: w, ERR_INVALID_RETURN_VALUE: I }
  } = dt(), { destroyer: A } = Br(), u = Qt(), g = fs(), p = Hc(), { createDeferredPromise: b } = bt(), C = SS(), N = globalThis.Blob || e.Blob, W = typeof N < "u" ? function(T) {
    return T instanceof N;
  } : function(T) {
    return !1;
  }, k = globalThis.AbortController || et.AbortController, { FunctionPrototypeCall: L } = Ye();
  class U extends u {
    constructor(T) {
      super(T), T?.readable === !1 && (this._readableState.readable = !1, this._readableState.ended = !0, this._readableState.endEmitted = !0), T?.writable === !1 && (this._writableState.writable = !1, this._writableState.ending = !0, this._writableState.ended = !0, this._writableState.finished = !0);
    }
  }
  Sf = function F(T, J) {
    if (s(T))
      return T;
    if (i(T))
      return D({
        readable: T
      });
    if (l(T))
      return D({
        writable: T
      });
    if (n(T))
      return D({
        writable: !1,
        readable: !1
      });
    if (f(T))
      return D({
        readable: g.fromWeb(T)
      });
    if (d(T))
      return D({
        writable: p.fromWeb(T)
      });
    if (typeof T == "function") {
      const { value: se, write: ne, final: M, destroy: Q } = x(T);
      if (a(se))
        return C(U, se, {
          // TODO (ronag): highWaterMark?
          objectMode: !0,
          write: ne,
          final: M,
          destroy: Q
        });
      const v = se?.then;
      if (typeof v == "function") {
        let $;
        const ee = L(
          v,
          se,
          (B) => {
            if (B != null)
              throw new I("nully", "body", B);
          },
          (B) => {
            A($, B);
          }
        );
        return $ = new U({
          // TODO (ronag): highWaterMark?
          objectMode: !0,
          readable: !1,
          write: ne,
          final(B) {
            M(async () => {
              try {
                await ee, t.nextTick(B, null);
              } catch (le) {
                t.nextTick(B, le);
              }
            });
          },
          destroy: Q
        });
      }
      throw new I("Iterable, AsyncIterable or AsyncFunction", J, se);
    }
    if (W(T))
      return F(T.arrayBuffer());
    if (a(T))
      return C(U, T, {
        // TODO (ronag): highWaterMark?
        objectMode: !0,
        writable: !1
      });
    if (f(T?.readable) && d(T?.writable))
      return U.fromWeb(T);
    if (typeof T?.writable == "object" || typeof T?.readable == "object") {
      const se = T != null && T.readable ? i(T?.readable) ? T?.readable : F(T.readable) : void 0, ne = T != null && T.writable ? l(T?.writable) ? T?.writable : F(T.writable) : void 0;
      return D({
        readable: se,
        writable: ne
      });
    }
    const re = T?.then;
    if (typeof re == "function") {
      let se;
      return L(
        re,
        T,
        (ne) => {
          ne != null && se.push(ne), se.push(null);
        },
        (ne) => {
          A(se, ne);
        }
      ), se = new U({
        objectMode: !0,
        writable: !1,
        read() {
        }
      });
    }
    throw new w(
      J,
      [
        "Blob",
        "ReadableStream",
        "WritableStream",
        "Stream",
        "Iterable",
        "AsyncIterable",
        "Function",
        "{ readable, writable } pair",
        "Promise"
      ],
      T
    );
  };
  function x(F) {
    let { promise: T, resolve: J } = b();
    const re = new k(), se = re.signal;
    return {
      value: F(
        (async function* () {
          for (; ; ) {
            const M = T;
            T = null;
            const { chunk: Q, done: v, cb: $ } = await M;
            if (t.nextTick($), v) return;
            if (se.aborted)
              throw new R(void 0, {
                cause: se.reason
              });
            ({ promise: T, resolve: J } = b()), yield Q;
          }
        })(),
        {
          signal: se
        }
      ),
      write(M, Q, v) {
        const $ = J;
        J = null, $({
          chunk: M,
          done: !1,
          cb: v
        });
      },
      final(M) {
        const Q = J;
        J = null, Q({
          done: !0,
          cb: M
        });
      },
      destroy(M, Q) {
        re.abort(), Q(M);
      }
    };
  }
  function D(F) {
    const T = F.readable && typeof F.readable.read != "function" ? g.wrap(F.readable) : F.readable, J = F.writable;
    let re = !!r(T), se = !!o(J), ne, M, Q, v, $;
    function ee(B) {
      const le = v;
      v = null, le ? le(B) : B && $.destroy(B);
    }
    return $ = new U({
      // TODO (ronag): highWaterMark?
      readableObjectMode: !!(T != null && T.readableObjectMode),
      writableObjectMode: !!(J != null && J.writableObjectMode),
      readable: re,
      writable: se
    }), se && (y(J, (B) => {
      se = !1, B && A(T, B), ee(B);
    }), $._write = function(B, le, ae) {
      J.write(B, le) ? ae() : ne = ae;
    }, $._final = function(B) {
      J.end(), M = B;
    }, J.on("drain", function() {
      if (ne) {
        const B = ne;
        ne = null, B();
      }
    }), J.on("finish", function() {
      if (M) {
        const B = M;
        M = null, B();
      }
    })), re && (y(T, (B) => {
      re = !1, B && A(T, B), ee(B);
    }), T.on("readable", function() {
      if (Q) {
        const B = Q;
        Q = null, B();
      }
    }), T.on("end", function() {
      $.push(null);
    }), $._read = function() {
      for (; ; ) {
        const B = T.read();
        if (B === null) {
          Q = $._read;
          return;
        }
        if (!$.push(B))
          return;
      }
    }), $._destroy = function(B, le) {
      !B && v !== null && (B = new R()), Q = null, ne = null, M = null, v === null ? le(B) : (v = le, A(J, B), A(T, B));
    }, $;
  }
  return Sf;
}
var Ef, i_;
function Qt() {
  if (i_) return Ef;
  i_ = 1;
  const {
    ObjectDefineProperties: t,
    ObjectGetOwnPropertyDescriptor: e,
    ObjectKeys: r,
    ObjectSetPrototypeOf: o
  } = Ye();
  Ef = i;
  const a = fs(), n = Hc();
  o(i.prototype, a.prototype), o(i, a);
  {
    const d = r(n.prototype);
    for (let y = 0; y < d.length; y++) {
      const R = d[y];
      i.prototype[R] || (i.prototype[R] = n.prototype[R]);
    }
  }
  function i(d) {
    if (!(this instanceof i)) return new i(d);
    a.call(this, d), n.call(this, d), d ? (this.allowHalfOpen = d.allowHalfOpen !== !1, d.readable === !1 && (this._readableState.readable = !1, this._readableState.ended = !0, this._readableState.endEmitted = !0), d.writable === !1 && (this._writableState.writable = !1, this._writableState.ending = !0, this._writableState.ended = !0, this._writableState.finished = !0)) : this.allowHalfOpen = !0;
  }
  t(i.prototype, {
    writable: {
      __proto__: null,
      ...e(n.prototype, "writable")
    },
    writableHighWaterMark: {
      __proto__: null,
      ...e(n.prototype, "writableHighWaterMark")
    },
    writableObjectMode: {
      __proto__: null,
      ...e(n.prototype, "writableObjectMode")
    },
    writableBuffer: {
      __proto__: null,
      ...e(n.prototype, "writableBuffer")
    },
    writableLength: {
      __proto__: null,
      ...e(n.prototype, "writableLength")
    },
    writableFinished: {
      __proto__: null,
      ...e(n.prototype, "writableFinished")
    },
    writableCorked: {
      __proto__: null,
      ...e(n.prototype, "writableCorked")
    },
    writableEnded: {
      __proto__: null,
      ...e(n.prototype, "writableEnded")
    },
    writableNeedDrain: {
      __proto__: null,
      ...e(n.prototype, "writableNeedDrain")
    },
    destroyed: {
      __proto__: null,
      get() {
        return this._readableState === void 0 || this._writableState === void 0 ? !1 : this._readableState.destroyed && this._writableState.destroyed;
      },
      set(d) {
        this._readableState && this._writableState && (this._readableState.destroyed = d, this._writableState.destroyed = d);
      }
    }
  });
  let l;
  function s() {
    return l === void 0 && (l = {}), l;
  }
  i.fromWeb = function(d, y) {
    return s().newStreamDuplexFromReadableWritablePair(d, y);
  }, i.toWeb = function(d) {
    return s().newReadableWritablePairFromDuplex(d);
  };
  let f;
  return i.from = function(d) {
    return f || (f = WA()), f(d, "body");
  }, Ef;
}
var vf, s_;
function ES() {
  if (s_) return vf;
  s_ = 1;
  const { ObjectSetPrototypeOf: t, Symbol: e } = Ye();
  vf = i;
  const { ERR_METHOD_NOT_IMPLEMENTED: r } = dt().codes, o = Qt(), { getHighWaterMark: a } = us();
  t(i.prototype, o.prototype), t(i, o);
  const n = e("kCallback");
  function i(f) {
    if (!(this instanceof i)) return new i(f);
    const d = f ? a(this, f, "readableHighWaterMark", !0) : null;
    d === 0 && (f = {
      ...f,
      highWaterMark: null,
      readableHighWaterMark: d,
      // TODO (ronag): 0 is not optimal since we have
      // a "bug" where we check needDrain before calling _write and not after.
      // Refs: https://github.com/nodejs/node/pull/32887
      // Refs: https://github.com/nodejs/node/pull/35941
      writableHighWaterMark: f.writableHighWaterMark || 0
    }), o.call(this, f), this._readableState.sync = !1, this[n] = null, f && (typeof f.transform == "function" && (this._transform = f.transform), typeof f.flush == "function" && (this._flush = f.flush)), this.on("prefinish", s);
  }
  function l(f) {
    typeof this._flush == "function" && !this.destroyed ? this._flush((d, y) => {
      if (d) {
        f ? f(d) : this.destroy(d);
        return;
      }
      y != null && this.push(y), this.push(null), f && f();
    }) : (this.push(null), f && f());
  }
  function s() {
    this._final !== l && l.call(this);
  }
  return i.prototype._final = l, i.prototype._transform = function(f, d, y) {
    throw new r("_transform()");
  }, i.prototype._write = function(f, d, y) {
    const R = this._readableState, w = this._writableState, I = R.length;
    this._transform(f, d, (A, u) => {
      if (A) {
        y(A);
        return;
      }
      u != null && this.push(u), w.ended || // Backwards compat.
      I === R.length || // Backwards compat.
      R.length < R.highWaterMark ? y() : this[n] = y;
    });
  }, i.prototype._read = function() {
    if (this[n]) {
      const f = this[n];
      this[n] = null, f();
    }
  }, vf;
}
var Rf, o_;
function vS() {
  if (o_) return Rf;
  o_ = 1;
  const { ObjectSetPrototypeOf: t } = Ye();
  Rf = r;
  const e = ES();
  t(r.prototype, e.prototype), t(r, e);
  function r(o) {
    if (!(this instanceof r)) return new r(o);
    e.call(this, o);
  }
  return r.prototype._transform = function(o, a, n) {
    n(null, o);
  }, Rf;
}
var Af, a_;
function zc() {
  if (a_) return Af;
  a_ = 1;
  const t = je(), { ArrayIsArray: e, Promise: r, SymbolAsyncIterator: o, SymbolDispose: a } = Ye(), n = br(), { once: i } = bt(), l = Br(), s = Qt(), {
    aggregateTwoErrors: f,
    codes: {
      ERR_INVALID_ARG_TYPE: d,
      ERR_INVALID_RETURN_VALUE: y,
      ERR_MISSING_ARGS: R,
      ERR_STREAM_DESTROYED: w,
      ERR_STREAM_PREMATURE_CLOSE: I
    },
    AbortError: A
  } = dt(), { validateFunction: u, validateAbortSignal: g } = Dn(), {
    isIterable: p,
    isReadable: b,
    isReadableNodeStream: C,
    isNodeStream: N,
    isTransformStream: W,
    isWebStream: k,
    isReadableStream: L,
    isReadableFinished: U
  } = or(), x = globalThis.AbortController || et.AbortController;
  let D, F, T;
  function J(B, le, ae) {
    let ce = !1;
    B.on("close", () => {
      ce = !0;
    });
    const Y = n(
      B,
      {
        readable: le,
        writable: ae
      },
      (P) => {
        ce = !P;
      }
    );
    return {
      destroy: (P) => {
        ce || (ce = !0, l.destroyer(B, P || new w("pipe")));
      },
      cleanup: Y
    };
  }
  function re(B) {
    return u(B[B.length - 1], "streams[stream.length - 1]"), B.pop();
  }
  function se(B) {
    if (p(B))
      return B;
    if (C(B))
      return ne(B);
    throw new d("val", ["Readable", "Iterable", "AsyncIterable"], B);
  }
  async function* ne(B) {
    F || (F = fs()), yield* F.prototype[o].call(B);
  }
  async function M(B, le, ae, { end: ce }) {
    let Y, P = null;
    const q = (X) => {
      if (X && (Y = X), P) {
        const m = P;
        P = null, m();
      }
    }, V = () => new r((X, m) => {
      Y ? m(Y) : P = () => {
        Y ? m(Y) : X();
      };
    });
    le.on("drain", q);
    const Z = n(
      le,
      {
        readable: !1
      },
      q
    );
    try {
      le.writableNeedDrain && await V();
      for await (const X of B)
        le.write(X) || await V();
      ce && (le.end(), await V()), ae();
    } catch (X) {
      ae(Y !== X ? f(Y, X) : X);
    } finally {
      Z(), le.off("drain", q);
    }
  }
  async function Q(B, le, ae, { end: ce }) {
    W(le) && (le = le.writable);
    const Y = le.getWriter();
    try {
      for await (const P of B)
        await Y.ready, Y.write(P).catch(() => {
        });
      await Y.ready, ce && await Y.close(), ae();
    } catch (P) {
      try {
        await Y.abort(P), ae(P);
      } catch (q) {
        ae(q);
      }
    }
  }
  function v(...B) {
    return $(B, i(re(B)));
  }
  function $(B, le, ae) {
    if (B.length === 1 && e(B[0]) && (B = B[0]), B.length < 2)
      throw new R("streams");
    const ce = new x(), Y = ce.signal, P = ae?.signal, q = [];
    g(P, "options.signal");
    function V() {
      G(new A());
    }
    T = T || bt().addAbortListener;
    let Z;
    P && (Z = T(P, V));
    let X, m;
    const c = [];
    let E = 0;
    function j(te) {
      G(te, --E === 0);
    }
    function G(te, z) {
      var de;
      if (te && (!X || X.code === "ERR_STREAM_PREMATURE_CLOSE") && (X = te), !(!X && !z)) {
        for (; c.length; )
          c.shift()(X);
        (de = Z) === null || de === void 0 || de[a](), ce.abort(), z && (X || q.forEach((pe) => pe()), t.nextTick(le, X, m));
      }
    }
    let K;
    for (let te = 0; te < B.length; te++) {
      const z = B[te], de = te < B.length - 1, pe = te > 0, he = de || ae?.end !== !1, we = te === B.length - 1;
      if (N(z)) {
        let be = function(_e) {
          _e && _e.name !== "AbortError" && _e.code !== "ERR_STREAM_PREMATURE_CLOSE" && j(_e);
        };
        if (he) {
          const { destroy: _e, cleanup: Ae } = J(z, de, pe);
          c.push(_e), b(z) && we && q.push(Ae);
        }
        z.on("error", be), b(z) && we && q.push(() => {
          z.removeListener("error", be);
        });
      }
      if (te === 0)
        if (typeof z == "function") {
          if (K = z({
            signal: Y
          }), !p(K))
            throw new y("Iterable, AsyncIterable or Stream", "source", K);
        } else p(z) || C(z) || W(z) ? K = z : K = s.from(z);
      else if (typeof z == "function") {
        if (W(K)) {
          var O;
          K = se((O = K) === null || O === void 0 ? void 0 : O.readable);
        } else
          K = se(K);
        if (K = z(K, {
          signal: Y
        }), de) {
          if (!p(K, !0))
            throw new y("AsyncIterable", `transform[${te - 1}]`, K);
        } else {
          var H;
          D || (D = vS());
          const be = new D({
            objectMode: !0
          }), _e = (H = K) === null || H === void 0 ? void 0 : H.then;
          if (typeof _e == "function")
            E++, _e.call(
              K,
              (me) => {
                m = me, me != null && be.write(me), he && be.end(), t.nextTick(j);
              },
              (me) => {
                be.destroy(me), t.nextTick(j, me);
              }
            );
          else if (p(K, !0))
            E++, M(K, be, j, {
              end: he
            });
          else if (L(K) || W(K)) {
            const me = K.readable || K;
            E++, M(me, be, j, {
              end: he
            });
          } else
            throw new y("AsyncIterable or Promise", "destination", K);
          K = be;
          const { destroy: Ae, cleanup: xe } = J(K, !1, !0);
          c.push(Ae), we && q.push(xe);
        }
      } else if (N(z)) {
        if (C(K)) {
          E += 2;
          const be = ee(K, z, j, {
            end: he
          });
          b(z) && we && q.push(be);
        } else if (W(K) || L(K)) {
          const be = K.readable || K;
          E++, M(be, z, j, {
            end: he
          });
        } else if (p(K))
          E++, M(K, z, j, {
            end: he
          });
        else
          throw new d(
            "val",
            ["Readable", "Iterable", "AsyncIterable", "ReadableStream", "TransformStream"],
            K
          );
        K = z;
      } else if (k(z)) {
        if (C(K))
          E++, Q(se(K), z, j, {
            end: he
          });
        else if (L(K) || p(K))
          E++, Q(K, z, j, {
            end: he
          });
        else if (W(K))
          E++, Q(K.readable, z, j, {
            end: he
          });
        else
          throw new d(
            "val",
            ["Readable", "Iterable", "AsyncIterable", "ReadableStream", "TransformStream"],
            K
          );
        K = z;
      } else
        K = s.from(z);
    }
    return (Y != null && Y.aborted || P != null && P.aborted) && t.nextTick(V), K;
  }
  function ee(B, le, ae, { end: ce }) {
    let Y = !1;
    if (le.on("close", () => {
      Y || ae(new I());
    }), B.pipe(le, {
      end: !1
    }), ce) {
      let P = function() {
        Y = !0, le.end();
      };
      U(B) ? t.nextTick(P) : B.once("end", P);
    } else
      ae();
    return n(
      B,
      {
        readable: !0,
        writable: !1
      },
      (P) => {
        const q = B._readableState;
        P && P.code === "ERR_STREAM_PREMATURE_CLOSE" && q && q.ended && !q.errored && !q.errorEmitted ? B.once("end", ae).once("error", ae) : ae(P);
      }
    ), n(
      le,
      {
        readable: !1,
        writable: !0
      },
      ae
    );
  }
  return Af = {
    pipelineImpl: $,
    pipeline: v
  }, Af;
}
var Tf, l_;
function RS() {
  if (l_) return Tf;
  l_ = 1;
  const { pipeline: t } = zc(), e = Qt(), { destroyer: r } = Br(), {
    isNodeStream: o,
    isReadable: a,
    isWritable: n,
    isWebStream: i,
    isTransformStream: l,
    isWritableStream: s,
    isReadableStream: f
  } = or(), {
    AbortError: d,
    codes: { ERR_INVALID_ARG_VALUE: y, ERR_MISSING_ARGS: R }
  } = dt(), w = br();
  return Tf = function(...A) {
    if (A.length === 0)
      throw new R("streams");
    if (A.length === 1)
      return e.from(A[0]);
    const u = [...A];
    if (typeof A[0] == "function" && (A[0] = e.from(A[0])), typeof A[A.length - 1] == "function") {
      const D = A.length - 1;
      A[D] = e.from(A[D]);
    }
    for (let D = 0; D < A.length; ++D)
      if (!(!o(A[D]) && !i(A[D]))) {
        if (D < A.length - 1 && !(a(A[D]) || f(A[D]) || l(A[D])))
          throw new y(`streams[${D}]`, u[D], "must be readable");
        if (D > 0 && !(n(A[D]) || s(A[D]) || l(A[D])))
          throw new y(`streams[${D}]`, u[D], "must be writable");
      }
    let g, p, b, C, N;
    function W(D) {
      const F = C;
      C = null, F ? F(D) : D ? N.destroy(D) : !x && !U && N.destroy();
    }
    const k = A[0], L = t(A, W), U = !!(n(k) || s(k) || l(k)), x = !!(a(L) || f(L) || l(L));
    if (N = new e({
      // TODO (ronag): highWaterMark?
      writableObjectMode: !!(k != null && k.writableObjectMode),
      readableObjectMode: !!(L != null && L.readableObjectMode),
      writable: U,
      readable: x
    }), U) {
      if (o(k))
        N._write = function(F, T, J) {
          k.write(F, T) ? J() : g = J;
        }, N._final = function(F) {
          k.end(), p = F;
        }, k.on("drain", function() {
          if (g) {
            const F = g;
            g = null, F();
          }
        });
      else if (i(k)) {
        const T = (l(k) ? k.writable : k).getWriter();
        N._write = async function(J, re, se) {
          try {
            await T.ready, T.write(J).catch(() => {
            }), se();
          } catch (ne) {
            se(ne);
          }
        }, N._final = async function(J) {
          try {
            await T.ready, T.close().catch(() => {
            }), p = J;
          } catch (re) {
            J(re);
          }
        };
      }
      const D = l(L) ? L.readable : L;
      w(D, () => {
        if (p) {
          const F = p;
          p = null, F();
        }
      });
    }
    if (x) {
      if (o(L))
        L.on("readable", function() {
          if (b) {
            const D = b;
            b = null, D();
          }
        }), L.on("end", function() {
          N.push(null);
        }), N._read = function() {
          for (; ; ) {
            const D = L.read();
            if (D === null) {
              b = N._read;
              return;
            }
            if (!N.push(D))
              return;
          }
        };
      else if (i(L)) {
        const F = (l(L) ? L.readable : L).getReader();
        N._read = async function() {
          for (; ; )
            try {
              const { value: T, done: J } = await F.read();
              if (!N.push(T))
                return;
              if (J) {
                N.push(null);
                return;
              }
            } catch {
              return;
            }
        };
      }
    }
    return N._destroy = function(D, F) {
      !D && C !== null && (D = new d()), b = null, g = null, p = null, C === null ? F(D) : (C = F, o(L) && r(L, D));
    }, N;
  }, Tf;
}
var u_;
function qA() {
  if (u_) return Jn;
  u_ = 1;
  const t = globalThis.AbortController || et.AbortController, {
    codes: { ERR_INVALID_ARG_VALUE: e, ERR_INVALID_ARG_TYPE: r, ERR_MISSING_ARGS: o, ERR_OUT_OF_RANGE: a },
    AbortError: n
  } = dt(), { validateAbortSignal: i, validateInteger: l, validateObject: s } = Dn(), f = Ye().Symbol("kWeak"), d = Ye().Symbol("kResistStopPropagation"), { finished: y } = br(), R = RS(), { addAbortSignalNoValidate: w } = ls(), { isWritable: I, isNodeStream: A } = or(), { deprecate: u } = bt(), {
    ArrayPrototypePush: g,
    Boolean: p,
    MathFloor: b,
    Number: C,
    NumberIsNaN: N,
    Promise: W,
    PromiseReject: k,
    PromiseResolve: L,
    PromisePrototypeThen: U,
    Symbol: x
  } = Ye(), D = x("kEmpty"), F = x("kEof");
  function T(P, q) {
    if (q != null && s(q, "options"), q?.signal != null && i(q.signal, "options.signal"), A(P) && !I(P))
      throw new e("stream", P, "must be writable");
    const V = R(this, P);
    return q != null && q.signal && w(q.signal, V), V;
  }
  function J(P, q) {
    if (typeof P != "function")
      throw new r("fn", ["Function", "AsyncFunction"], P);
    q != null && s(q, "options"), q?.signal != null && i(q.signal, "options.signal");
    let V = 1;
    q?.concurrency != null && (V = b(q.concurrency));
    let Z = V - 1;
    return q?.highWaterMark != null && (Z = b(q.highWaterMark)), l(V, "options.concurrency", 1), l(Z, "options.highWaterMark", 0), Z += V, (async function* () {
      const m = bt().AbortSignalAny(
        [q?.signal].filter(p)
      ), c = this, E = [], j = {
        signal: m
      };
      let G, K, O = !1, H = 0;
      function te() {
        O = !0, z();
      }
      function z() {
        H -= 1, de();
      }
      function de() {
        K && !O && H < V && E.length < Z && (K(), K = null);
      }
      async function pe() {
        try {
          for await (let he of c) {
            if (O)
              return;
            if (m.aborted)
              throw new n();
            try {
              if (he = P(he, j), he === D)
                continue;
              he = L(he);
            } catch (we) {
              he = k(we);
            }
            H += 1, U(he, z, te), E.push(he), G && (G(), G = null), !O && (E.length >= Z || H >= V) && await new W((we) => {
              K = we;
            });
          }
          E.push(F);
        } catch (he) {
          const we = k(he);
          U(we, z, te), E.push(we);
        } finally {
          O = !0, G && (G(), G = null);
        }
      }
      pe();
      try {
        for (; ; ) {
          for (; E.length > 0; ) {
            const he = await E[0];
            if (he === F)
              return;
            if (m.aborted)
              throw new n();
            he !== D && (yield he), E.shift(), de();
          }
          await new W((he) => {
            G = he;
          });
        }
      } finally {
        O = !0, K && (K(), K = null);
      }
    }).call(this);
  }
  function re(P = void 0) {
    return P != null && s(P, "options"), P?.signal != null && i(P.signal, "options.signal"), (async function* () {
      let V = 0;
      for await (const X of this) {
        var Z;
        if (P != null && (Z = P.signal) !== null && Z !== void 0 && Z.aborted)
          throw new n({
            cause: P.signal.reason
          });
        yield [V++, X];
      }
    }).call(this);
  }
  async function se(P, q = void 0) {
    for await (const V of v.call(this, P, q))
      return !0;
    return !1;
  }
  async function ne(P, q = void 0) {
    if (typeof P != "function")
      throw new r("fn", ["Function", "AsyncFunction"], P);
    return !await se.call(
      this,
      async (...V) => !await P(...V),
      q
    );
  }
  async function M(P, q) {
    for await (const V of v.call(this, P, q))
      return V;
  }
  async function Q(P, q) {
    if (typeof P != "function")
      throw new r("fn", ["Function", "AsyncFunction"], P);
    async function V(Z, X) {
      return await P(Z, X), D;
    }
    for await (const Z of J.call(this, V, q)) ;
  }
  function v(P, q) {
    if (typeof P != "function")
      throw new r("fn", ["Function", "AsyncFunction"], P);
    async function V(Z, X) {
      return await P(Z, X) ? Z : D;
    }
    return J.call(this, V, q);
  }
  class $ extends o {
    constructor() {
      super("reduce"), this.message = "Reduce of an empty stream requires an initial value";
    }
  }
  async function ee(P, q, V) {
    var Z;
    if (typeof P != "function")
      throw new r("reducer", ["Function", "AsyncFunction"], P);
    V != null && s(V, "options"), V?.signal != null && i(V.signal, "options.signal");
    let X = arguments.length > 1;
    if (V != null && (Z = V.signal) !== null && Z !== void 0 && Z.aborted) {
      const G = new n(void 0, {
        cause: V.signal.reason
      });
      throw this.once("error", () => {
      }), await y(this.destroy(G)), G;
    }
    const m = new t(), c = m.signal;
    if (V != null && V.signal) {
      const G = {
        once: !0,
        [f]: this,
        [d]: !0
      };
      V.signal.addEventListener("abort", () => m.abort(), G);
    }
    let E = !1;
    try {
      for await (const G of this) {
        var j;
        if (E = !0, V != null && (j = V.signal) !== null && j !== void 0 && j.aborted)
          throw new n();
        X ? q = await P(q, G, {
          signal: c
        }) : (q = G, X = !0);
      }
      if (!E && !X)
        throw new $();
    } finally {
      m.abort();
    }
    return q;
  }
  async function B(P) {
    P != null && s(P, "options"), P?.signal != null && i(P.signal, "options.signal");
    const q = [];
    for await (const Z of this) {
      var V;
      if (P != null && (V = P.signal) !== null && V !== void 0 && V.aborted)
        throw new n(void 0, {
          cause: P.signal.reason
        });
      g(q, Z);
    }
    return q;
  }
  function le(P, q) {
    const V = J.call(this, P, q);
    return (async function* () {
      for await (const X of V)
        yield* X;
    }).call(this);
  }
  function ae(P) {
    if (P = C(P), N(P))
      return 0;
    if (P < 0)
      throw new a("number", ">= 0", P);
    return P;
  }
  function ce(P, q = void 0) {
    return q != null && s(q, "options"), q?.signal != null && i(q.signal, "options.signal"), P = ae(P), (async function* () {
      var Z;
      if (q != null && (Z = q.signal) !== null && Z !== void 0 && Z.aborted)
        throw new n();
      for await (const m of this) {
        var X;
        if (q != null && (X = q.signal) !== null && X !== void 0 && X.aborted)
          throw new n();
        P-- <= 0 && (yield m);
      }
    }).call(this);
  }
  function Y(P, q = void 0) {
    return q != null && s(q, "options"), q?.signal != null && i(q.signal, "options.signal"), P = ae(P), (async function* () {
      var Z;
      if (q != null && (Z = q.signal) !== null && Z !== void 0 && Z.aborted)
        throw new n();
      for await (const m of this) {
        var X;
        if (q != null && (X = q.signal) !== null && X !== void 0 && X.aborted)
          throw new n();
        if (P-- > 0 && (yield m), P <= 0)
          return;
      }
    }).call(this);
  }
  return Jn.streamReturningOperators = {
    asIndexedPairs: u(re, "readable.asIndexedPairs will be removed in a future version."),
    drop: ce,
    filter: v,
    flatMap: le,
    map: J,
    take: Y,
    compose: T
  }, Jn.promiseReturningOperators = {
    every: ne,
    forEach: Q,
    reduce: ee,
    toArray: B,
    some: se,
    find: M
  }, Jn;
}
var xf, f_;
function AS() {
  if (f_) return xf;
  f_ = 1;
  const { ArrayPrototypePop: t, Promise: e } = Ye(), { isIterable: r, isNodeStream: o, isWebStream: a } = or(), { pipelineImpl: n } = zc(), { finished: i } = br();
  TS();
  function l(...s) {
    return new e((f, d) => {
      let y, R;
      const w = s[s.length - 1];
      if (w && typeof w == "object" && !o(w) && !r(w) && !a(w)) {
        const I = t(s);
        y = I.signal, R = I.end;
      }
      n(
        s,
        (I, A) => {
          I ? d(I) : f(A);
        },
        {
          signal: y,
          end: R
        }
      );
    });
  }
  return xf = {
    finished: i,
    pipeline: l
  }, xf;
}
var c_;
function TS() {
  if (c_) return of.exports;
  c_ = 1;
  const { Buffer: t } = Fe, { ObjectDefineProperty: e, ObjectKeys: r, ReflectApply: o } = Ye(), {
    promisify: { custom: a }
  } = bt(), { streamReturningOperators: n, promiseReturningOperators: i } = qA(), {
    codes: { ERR_ILLEGAL_CONSTRUCTOR: l }
  } = dt(), s = RS(), { setDefaultHighWaterMark: f, getDefaultHighWaterMark: d } = us(), { pipeline: y } = zc(), { destroyer: R } = Br(), w = br(), I = AS(), A = or(), u = of.exports = Gc().Stream;
  u.isDestroyed = A.isDestroyed, u.isDisturbed = A.isDisturbed, u.isErrored = A.isErrored, u.isReadable = A.isReadable, u.isWritable = A.isWritable, u.Readable = fs();
  for (const p of r(n)) {
    let C = function(...N) {
      if (new.target)
        throw l();
      return u.Readable.from(o(b, this, N));
    };
    const b = n[p];
    e(C, "name", {
      __proto__: null,
      value: b.name
    }), e(C, "length", {
      __proto__: null,
      value: b.length
    }), e(u.Readable.prototype, p, {
      __proto__: null,
      value: C,
      enumerable: !1,
      configurable: !0,
      writable: !0
    });
  }
  for (const p of r(i)) {
    let C = function(...N) {
      if (new.target)
        throw l();
      return o(b, this, N);
    };
    const b = i[p];
    e(C, "name", {
      __proto__: null,
      value: b.name
    }), e(C, "length", {
      __proto__: null,
      value: b.length
    }), e(u.Readable.prototype, p, {
      __proto__: null,
      value: C,
      enumerable: !1,
      configurable: !0,
      writable: !0
    });
  }
  u.Writable = Hc(), u.Duplex = Qt(), u.Transform = ES(), u.PassThrough = vS(), u.pipeline = y;
  const { addAbortSignal: g } = ls();
  return u.addAbortSignal = g, u.finished = w, u.destroy = R, u.compose = s, u.setDefaultHighWaterMark = f, u.getDefaultHighWaterMark = d, e(u, "promises", {
    __proto__: null,
    configurable: !0,
    enumerable: !0,
    get() {
      return I;
    }
  }), e(y, a, {
    __proto__: null,
    enumerable: !0,
    get() {
      return I.pipeline;
    }
  }), e(w, a, {
    __proto__: null,
    enumerable: !0,
    get() {
      return I.finished;
    }
  }), u.Stream = u, u._isUint8Array = function(b) {
    return b instanceof Uint8Array;
  }, u._uint8ArrayToBuffer = function(b) {
    return t.from(b.buffer, b.byteOffset, b.byteLength);
  }, of.exports;
}
li.exports;
var d_;
function BA() {
  return d_ || (d_ = 1, (function(t) {
    const e = tr;
    if (e && process.env.READABLE_STREAM === "disable") {
      const r = e.promises;
      t.exports._uint8ArrayToBuffer = e._uint8ArrayToBuffer, t.exports._isUint8Array = e._isUint8Array, t.exports.isDisturbed = e.isDisturbed, t.exports.isErrored = e.isErrored, t.exports.isReadable = e.isReadable, t.exports.Readable = e.Readable, t.exports.Writable = e.Writable, t.exports.Duplex = e.Duplex, t.exports.Transform = e.Transform, t.exports.PassThrough = e.PassThrough, t.exports.addAbortSignal = e.addAbortSignal, t.exports.finished = e.finished, t.exports.destroy = e.destroy, t.exports.pipeline = e.pipeline, t.exports.compose = e.compose, Object.defineProperty(e, "promises", {
        configurable: !0,
        enumerable: !0,
        get() {
          return r;
        }
      }), t.exports.Stream = e.Stream;
    } else {
      const r = TS(), o = AS(), a = r.Readable.destroy;
      t.exports = r.Readable, t.exports._uint8ArrayToBuffer = r._uint8ArrayToBuffer, t.exports._isUint8Array = r._isUint8Array, t.exports.isDisturbed = r.isDisturbed, t.exports.isErrored = r.isErrored, t.exports.isReadable = r.isReadable, t.exports.Readable = r.Readable, t.exports.Writable = r.Writable, t.exports.Duplex = r.Duplex, t.exports.Transform = r.Transform, t.exports.PassThrough = r.PassThrough, t.exports.addAbortSignal = r.addAbortSignal, t.exports.finished = r.finished, t.exports.destroy = r.destroy, t.exports.destroy = a, t.exports.pipeline = r.pipeline, t.exports.compose = r.compose, Object.defineProperty(r, "promises", {
        configurable: !0,
        enumerable: !0,
        get() {
          return o;
        }
      }), t.exports.Stream = r.Stream;
    }
    t.exports.default = t.exports;
  })(li)), li.exports;
}
var If, h_;
function UA() {
  if (h_) return If;
  h_ = 1;
  const { Transform: t } = BA(), e = Uc();
  class r extends t {
    constructor(a) {
      super(a), this.checksum = Buffer.allocUnsafe(4), this.checksum.writeInt32BE(0, 0), this.rawSize = 0;
    }
    _transform(a, n, i) {
      a && (this.checksum = e.buf(a, this.checksum) >>> 0, this.rawSize += a.length), i(null, a);
    }
    digest(a) {
      const n = Buffer.allocUnsafe(4);
      return n.writeUInt32BE(this.checksum >>> 0, 0), a ? n.toString(a) : n;
    }
    hex() {
      return this.digest("hex").toUpperCase();
    }
    size() {
      return this.rawSize;
    }
  }
  return If = r, If;
}
var Df, p_;
function GA() {
  if (p_) return Df;
  p_ = 1;
  const { DeflateRaw: t } = Pi, e = Uc();
  class r extends t {
    constructor(a) {
      super(a), this.checksum = Buffer.allocUnsafe(4), this.checksum.writeInt32BE(0, 0), this.rawSize = 0, this.compressedSize = 0;
    }
    push(a, n) {
      return a && (this.compressedSize += a.length), super.push(a, n);
    }
    _transform(a, n, i) {
      a && (this.checksum = e.buf(a, this.checksum) >>> 0, this.rawSize += a.length), super._transform(a, n, i);
    }
    digest(a) {
      const n = Buffer.allocUnsafe(4);
      return n.writeUInt32BE(this.checksum >>> 0, 0), a ? n.toString(a) : n;
    }
    hex() {
      return this.digest("hex").toUpperCase();
    }
    size(a = !1) {
      return a ? this.compressedSize : this.rawSize;
    }
  }
  return Df = r, Df;
}
var Of, g_;
function y_() {
  return g_ || (g_ = 1, Of = {
    CRC32Stream: UA(),
    DeflateCRC32Stream: GA()
  }), Of;
}
var b_;
function HA() {
  if (b_) return nf.exports;
  b_ = 1;
  var t = It.inherits, e = Uc(), { CRC32Stream: r } = y_(), { DeflateCRC32Stream: o } = y_(), a = wS();
  lS(), oS();
  var n = aS();
  _S();
  var i = $c(), l = nf.exports = function(s) {
    if (!(this instanceof l))
      return new l(s);
    s = this.options = this._defaults(s), a.call(this, s), this._entry = null, this._entries = [], this._archive = {
      centralLength: 0,
      centralOffset: 0,
      comment: "",
      finish: !1,
      finished: !1,
      processing: !1,
      forceZip64: s.forceZip64,
      forceLocalTime: s.forceLocalTime
    };
  };
  return t(l, a), l.prototype._afterAppend = function(s) {
    this._entries.push(s), s.getGeneralPurposeBit().usesDataDescriptor() && this._writeDataDescriptor(s), this._archive.processing = !1, this._entry = null, this._archive.finish && !this._archive.finished && this._finish();
  }, l.prototype._appendBuffer = function(s, f, d) {
    f.length === 0 && s.setMethod(n.METHOD_STORED);
    var y = s.getMethod();
    if (y === n.METHOD_STORED && (s.setSize(f.length), s.setCompressedSize(f.length), s.setCrc(e.buf(f) >>> 0)), this._writeLocalFileHeader(s), y === n.METHOD_STORED) {
      this.write(f), this._afterAppend(s), d(null, s);
      return;
    } else if (y === n.METHOD_DEFLATED) {
      this._smartStream(s, d).end(f);
      return;
    } else {
      d(new Error("compression method " + y + " not implemented"));
      return;
    }
  }, l.prototype._appendStream = function(s, f, d) {
    s.getGeneralPurposeBit().useDataDescriptor(!0), s.setVersionNeededToExtract(n.MIN_VERSION_DATA_DESCRIPTOR), this._writeLocalFileHeader(s);
    var y = this._smartStream(s, d);
    f.once("error", function(R) {
      y.emit("error", R), y.end();
    }), f.pipe(y);
  }, l.prototype._defaults = function(s) {
    return typeof s != "object" && (s = {}), typeof s.zlib != "object" && (s.zlib = {}), typeof s.zlib.level != "number" && (s.zlib.level = n.ZLIB_BEST_SPEED), s.forceZip64 = !!s.forceZip64, s.forceLocalTime = !!s.forceLocalTime, s;
  }, l.prototype._finish = function() {
    this._archive.centralOffset = this.offset, this._entries.forEach((function(s) {
      this._writeCentralFileHeader(s);
    }).bind(this)), this._archive.centralLength = this.offset - this._archive.centralOffset, this.isZip64() && this._writeCentralDirectoryZip64(), this._writeCentralDirectoryEnd(), this._archive.processing = !1, this._archive.finish = !0, this._archive.finished = !0, this.end();
  }, l.prototype._normalizeEntry = function(s) {
    s.getMethod() === -1 && s.setMethod(n.METHOD_DEFLATED), s.getMethod() === n.METHOD_DEFLATED && (s.getGeneralPurposeBit().useDataDescriptor(!0), s.setVersionNeededToExtract(n.MIN_VERSION_DATA_DESCRIPTOR)), s.getTime() === -1 && s.setTime(/* @__PURE__ */ new Date(), this._archive.forceLocalTime), s._offsets = {
      file: 0,
      data: 0,
      contents: 0
    };
  }, l.prototype._smartStream = function(s, f) {
    var d = s.getMethod() === n.METHOD_DEFLATED, y = d ? new o(this.options.zlib) : new r(), R = null;
    function w() {
      var I = y.digest().readUInt32BE(0);
      s.setCrc(I), s.setSize(y.size()), s.setCompressedSize(y.size(!0)), this._afterAppend(s), f(R, s);
    }
    return y.once("end", w.bind(this)), y.once("error", function(I) {
      R = I;
    }), y.pipe(this, { end: !1 }), y;
  }, l.prototype._writeCentralDirectoryEnd = function() {
    var s = this._entries.length, f = this._archive.centralLength, d = this._archive.centralOffset;
    this.isZip64() && (s = n.ZIP64_MAGIC_SHORT, f = n.ZIP64_MAGIC, d = n.ZIP64_MAGIC), this.write(i.getLongBytes(n.SIG_EOCD)), this.write(n.SHORT_ZERO), this.write(n.SHORT_ZERO), this.write(i.getShortBytes(s)), this.write(i.getShortBytes(s)), this.write(i.getLongBytes(f)), this.write(i.getLongBytes(d));
    var y = this.getComment(), R = Buffer.byteLength(y);
    this.write(i.getShortBytes(R)), this.write(y);
  }, l.prototype._writeCentralDirectoryZip64 = function() {
    this.write(i.getLongBytes(n.SIG_ZIP64_EOCD)), this.write(i.getEightBytes(44)), this.write(i.getShortBytes(n.MIN_VERSION_ZIP64)), this.write(i.getShortBytes(n.MIN_VERSION_ZIP64)), this.write(n.LONG_ZERO), this.write(n.LONG_ZERO), this.write(i.getEightBytes(this._entries.length)), this.write(i.getEightBytes(this._entries.length)), this.write(i.getEightBytes(this._archive.centralLength)), this.write(i.getEightBytes(this._archive.centralOffset)), this.write(i.getLongBytes(n.SIG_ZIP64_EOCD_LOC)), this.write(n.LONG_ZERO), this.write(i.getEightBytes(this._archive.centralOffset + this._archive.centralLength)), this.write(i.getLongBytes(1));
  }, l.prototype._writeCentralFileHeader = function(s) {
    var f = s.getGeneralPurposeBit(), d = s.getMethod(), y = s._offsets.file, R = s.getSize(), w = s.getCompressedSize();
    if (s.isZip64() || y > n.ZIP64_MAGIC) {
      R = n.ZIP64_MAGIC, w = n.ZIP64_MAGIC, y = n.ZIP64_MAGIC, s.setVersionNeededToExtract(n.MIN_VERSION_ZIP64);
      var I = Buffer.concat([
        i.getShortBytes(n.ZIP64_EXTRA_ID),
        i.getShortBytes(24),
        i.getEightBytes(s.getSize()),
        i.getEightBytes(s.getCompressedSize()),
        i.getEightBytes(s._offsets.file)
      ], 28);
      s.setExtra(I);
    }
    this.write(i.getLongBytes(n.SIG_CFH)), this.write(i.getShortBytes(s.getPlatform() << 8 | n.VERSION_MADEBY)), this.write(i.getShortBytes(s.getVersionNeededToExtract())), this.write(f.encode()), this.write(i.getShortBytes(d)), this.write(i.getLongBytes(s.getTimeDos())), this.write(i.getLongBytes(s.getCrc())), this.write(i.getLongBytes(w)), this.write(i.getLongBytes(R));
    var A = s.getName(), u = s.getComment(), g = s.getCentralDirectoryExtra();
    f.usesUTF8ForNames() && (A = Buffer.from(A), u = Buffer.from(u)), this.write(i.getShortBytes(A.length)), this.write(i.getShortBytes(g.length)), this.write(i.getShortBytes(u.length)), this.write(n.SHORT_ZERO), this.write(i.getShortBytes(s.getInternalAttributes())), this.write(i.getLongBytes(s.getExternalAttributes())), this.write(i.getLongBytes(y)), this.write(A), this.write(g), this.write(u);
  }, l.prototype._writeDataDescriptor = function(s) {
    this.write(i.getLongBytes(n.SIG_DD)), this.write(i.getLongBytes(s.getCrc())), s.isZip64() ? (this.write(i.getEightBytes(s.getCompressedSize())), this.write(i.getEightBytes(s.getSize()))) : (this.write(i.getLongBytes(s.getCompressedSize())), this.write(i.getLongBytes(s.getSize())));
  }, l.prototype._writeLocalFileHeader = function(s) {
    var f = s.getGeneralPurposeBit(), d = s.getMethod(), y = s.getName(), R = s.getLocalFileDataExtra();
    s.isZip64() && (f.useDataDescriptor(!0), s.setVersionNeededToExtract(n.MIN_VERSION_ZIP64)), f.usesUTF8ForNames() && (y = Buffer.from(y)), s._offsets.file = this.offset, this.write(i.getLongBytes(n.SIG_LFH)), this.write(i.getShortBytes(s.getVersionNeededToExtract())), this.write(f.encode()), this.write(i.getShortBytes(d)), this.write(i.getLongBytes(s.getTimeDos())), s._offsets.data = this.offset, f.usesDataDescriptor() ? (this.write(n.LONG_ZERO), this.write(n.LONG_ZERO), this.write(n.LONG_ZERO)) : (this.write(i.getLongBytes(s.getCrc())), this.write(i.getLongBytes(s.getCompressedSize())), this.write(i.getLongBytes(s.getSize()))), this.write(i.getShortBytes(y.length)), this.write(i.getShortBytes(R.length)), this.write(y), this.write(R), s._offsets.contents = this.offset;
  }, l.prototype.getComment = function(s) {
    return this._archive.comment !== null ? this._archive.comment : "";
  }, l.prototype.isZip64 = function() {
    return this._archive.forceZip64 || this._entries.length > n.ZIP64_MAGIC_SHORT || this._archive.centralLength > n.ZIP64_MAGIC || this._archive.centralOffset > n.ZIP64_MAGIC;
  }, l.prototype.setComment = function(s) {
    this._archive.comment = s;
  }, nf.exports;
}
var Nf, __;
function w_() {
  return __ || (__ = 1, Nf = {
    ArchiveEntry: jc(),
    ZipArchiveEntry: lS(),
    ArchiveOutputStream: wS(),
    ZipArchiveOutputStream: HA()
  }), Nf;
}
var m_;
function zA() {
  if (m_) return Au.exports;
  m_ = 1;
  var t = It.inherits, e = w_().ZipArchiveOutputStream, r = w_().ZipArchiveEntry, o = Tn(), a = Au.exports = function(n) {
    if (!(this instanceof a))
      return new a(n);
    n = this.options = n || {}, n.zlib = n.zlib || {}, e.call(this, n), typeof n.level == "number" && n.level >= 0 && (n.zlib.level = n.level, delete n.level), !n.forceZip64 && typeof n.zlib.level == "number" && n.zlib.level === 0 && (n.store = !0), n.namePrependSlash = n.namePrependSlash || !1, n.comment && n.comment.length > 0 && this.setComment(n.comment);
  };
  return t(a, e), a.prototype._normalizeFileData = function(n) {
    n = o.defaults(n, {
      type: "file",
      name: null,
      namePrependSlash: this.options.namePrependSlash,
      linkname: null,
      date: null,
      mode: null,
      store: this.options.store,
      comment: ""
    });
    var i = n.type === "directory", l = n.type === "symlink";
    return n.name && (n.name = o.sanitizePath(n.name), !l && n.name.slice(-1) === "/" ? (i = !0, n.type = "directory") : i && (n.name += "/")), (i || l) && (n.store = !0), n.date = o.dateify(n.date), n;
  }, a.prototype.entry = function(n, i, l) {
    if (typeof l != "function" && (l = this._emitErrorCallback.bind(this)), i = this._normalizeFileData(i), i.type !== "file" && i.type !== "directory" && i.type !== "symlink") {
      l(new Error(i.type + " entries not currently supported"));
      return;
    }
    if (typeof i.name != "string" || i.name.length === 0) {
      l(new Error("entry name must be a non-empty string value"));
      return;
    }
    if (i.type === "symlink" && typeof i.linkname != "string") {
      l(new Error("entry linkname must be a non-empty string value when type equals symlink"));
      return;
    }
    var s = new r(i.name);
    return s.setTime(i.date, this.options.forceLocalTime), i.namePrependSlash && s.setName(i.name, !0), i.store && s.setMethod(0), i.comment.length > 0 && s.setComment(i.comment), i.type === "symlink" && typeof i.mode != "number" && (i.mode = 40960), typeof i.mode == "number" && (i.type === "symlink" && (i.mode |= 40960), s.setUnixMode(i.mode)), i.type === "symlink" && typeof i.linkname == "string" && (n = Buffer.from(i.linkname)), e.prototype.entry.call(this, s, n, l);
  }, a.prototype.finalize = function() {
    this.finish();
  }, Au.exports;
}
var Lf, S_;
function VA() {
  if (S_) return Lf;
  S_ = 1;
  var t = zA(), e = Tn(), r = function(o) {
    if (!(this instanceof r))
      return new r(o);
    o = this.options = e.defaults(o, {
      comment: "",
      forceUTC: !1,
      namePrependSlash: !1,
      store: !1
    }), this.supports = {
      directory: !0,
      symlink: !0
    }, this.engine = new t(o);
  };
  return r.prototype.append = function(o, a, n) {
    this.engine.entry(o, a, n);
  }, r.prototype.finalize = function() {
    this.engine.finalize();
  }, r.prototype.on = function() {
    return this.engine.on.apply(this.engine, arguments);
  }, r.prototype.pipe = function() {
    return this.engine.pipe.apply(this.engine, arguments);
  }, r.prototype.unpipe = function() {
    return this.engine.unpipe.apply(this.engine, arguments);
  }, Lf = r, Lf;
}
var Qn = {}, Pf, E_;
function ZA() {
  return E_ || (E_ = 1, Pf = Ke), Pf;
}
var Cf, v_;
function YA() {
  return v_ || (v_ = 1, Cf = class {
    constructor(e) {
      if (!(e > 0) || (e - 1 & e) !== 0) throw new Error("Max size for a FixedFIFO should be a power of two");
      this.buffer = new Array(e), this.mask = e - 1, this.top = 0, this.btm = 0, this.next = null;
    }
    clear() {
      this.top = this.btm = 0, this.next = null, this.buffer.fill(void 0);
    }
    push(e) {
      return this.buffer[this.top] !== void 0 ? !1 : (this.buffer[this.top] = e, this.top = this.top + 1 & this.mask, !0);
    }
    shift() {
      const e = this.buffer[this.btm];
      if (e !== void 0)
        return this.buffer[this.btm] = void 0, this.btm = this.btm + 1 & this.mask, e;
    }
    peek() {
      return this.buffer[this.btm];
    }
    isEmpty() {
      return this.buffer[this.btm] === void 0;
    }
  }), Cf;
}
var Mf, R_;
function xS() {
  if (R_) return Mf;
  R_ = 1;
  const t = YA();
  return Mf = class {
    constructor(r) {
      this.hwm = r || 16, this.head = new t(this.hwm), this.tail = this.head, this.length = 0;
    }
    clear() {
      this.head = this.tail, this.head.clear(), this.length = 0;
    }
    push(r) {
      if (this.length++, !this.head.push(r)) {
        const o = this.head;
        this.head = o.next = new t(2 * this.head.buffer.length), this.head.push(r);
      }
    }
    shift() {
      this.length !== 0 && this.length--;
      const r = this.tail.shift();
      if (r === void 0 && this.tail.next) {
        const o = this.tail.next;
        return this.tail.next = null, this.tail = o, this.tail.shift();
      }
      return r;
    }
    peek() {
      const r = this.tail.peek();
      return r === void 0 && this.tail.next ? this.tail.next.peek() : r;
    }
    isEmpty() {
      return this.length === 0;
    }
  }, Mf;
}
var kf, A_;
function On() {
  if (A_) return kf;
  A_ = 1;
  function t($) {
    return Buffer.isBuffer($) || $ instanceof Uint8Array;
  }
  function e($) {
    return Buffer.isEncoding($);
  }
  function r($, ee, B) {
    return Buffer.alloc($, ee, B);
  }
  function o($) {
    return Buffer.allocUnsafe($);
  }
  function a($) {
    return Buffer.allocUnsafeSlow($);
  }
  function n($, ee) {
    return Buffer.byteLength($, ee);
  }
  function i($, ee) {
    return Buffer.compare($, ee);
  }
  function l($, ee) {
    return Buffer.concat($, ee);
  }
  function s($, ee, B, le, ae) {
    return p($).copy(ee, B, le, ae);
  }
  function f($, ee) {
    return p($).equals(ee);
  }
  function d($, ee, B, le, ae) {
    return p($).fill(ee, B, le, ae);
  }
  function y($, ee, B) {
    return Buffer.from($, ee, B);
  }
  function R($, ee, B, le) {
    return p($).includes(ee, B, le);
  }
  function w($, ee, B, le) {
    return p($).indexOf(ee, B, le);
  }
  function I($, ee, B, le) {
    return p($).lastIndexOf(ee, B, le);
  }
  function A($) {
    return p($).swap16();
  }
  function u($) {
    return p($).swap32();
  }
  function g($) {
    return p($).swap64();
  }
  function p($) {
    return Buffer.isBuffer($) ? $ : Buffer.from($.buffer, $.byteOffset, $.byteLength);
  }
  function b($, ee, B, le) {
    return p($).toString(ee, B, le);
  }
  function C($, ee, B, le, ae) {
    return p($).write(ee, B, le, ae);
  }
  function N($, ee) {
    return p($).readDoubleBE(ee);
  }
  function W($, ee) {
    return p($).readDoubleLE(ee);
  }
  function k($, ee) {
    return p($).readFloatBE(ee);
  }
  function L($, ee) {
    return p($).readFloatLE(ee);
  }
  function U($, ee) {
    return p($).readInt32BE(ee);
  }
  function x($, ee) {
    return p($).readInt32LE(ee);
  }
  function D($, ee) {
    return p($).readUInt32BE(ee);
  }
  function F($, ee) {
    return p($).readUInt32LE(ee);
  }
  function T($, ee, B) {
    return p($).writeDoubleBE(ee, B);
  }
  function J($, ee, B) {
    return p($).writeDoubleLE(ee, B);
  }
  function re($, ee, B) {
    return p($).writeFloatBE(ee, B);
  }
  function se($, ee, B) {
    return p($).writeFloatLE(ee, B);
  }
  function ne($, ee, B) {
    return p($).writeInt32BE(ee, B);
  }
  function M($, ee, B) {
    return p($).writeInt32LE(ee, B);
  }
  function Q($, ee, B) {
    return p($).writeUInt32BE(ee, B);
  }
  function v($, ee, B) {
    return p($).writeUInt32LE(ee, B);
  }
  return kf = {
    isBuffer: t,
    isEncoding: e,
    alloc: r,
    allocUnsafe: o,
    allocUnsafeSlow: a,
    byteLength: n,
    compare: i,
    concat: l,
    copy: s,
    equals: f,
    fill: d,
    from: y,
    includes: R,
    indexOf: w,
    lastIndexOf: I,
    swap16: A,
    swap32: u,
    swap64: g,
    toBuffer: p,
    toString: b,
    write: C,
    readDoubleBE: N,
    readDoubleLE: W,
    readFloatBE: k,
    readFloatLE: L,
    readInt32BE: U,
    readInt32LE: x,
    readUInt32BE: D,
    readUInt32LE: F,
    writeDoubleBE: T,
    writeDoubleLE: J,
    writeFloatBE: re,
    writeFloatLE: se,
    writeInt32BE: ne,
    writeInt32LE: M,
    writeUInt32BE: Q,
    writeUInt32LE: v
  }, kf;
}
var Ff, T_;
function KA() {
  if (T_) return Ff;
  T_ = 1;
  const t = On();
  return Ff = class {
    constructor(r) {
      this.encoding = r;
    }
    get remaining() {
      return 0;
    }
    decode(r) {
      return t.toString(r, this.encoding);
    }
    flush() {
      return "";
    }
  }, Ff;
}
var jf, x_;
function JA() {
  if (x_) return jf;
  x_ = 1;
  const t = On();
  jf = class {
    constructor() {
      this._reset();
    }
    get remaining() {
      return this.bytesSeen;
    }
    decode(a) {
      if (a.byteLength === 0) return "";
      if (this.bytesNeeded === 0 && e(a, 0) === 0)
        return this.bytesSeen = r(a), t.toString(a, "utf8");
      let n = "", i = 0;
      if (this.bytesNeeded > 0) {
        for (; i < a.byteLength; ) {
          const f = a[i];
          if (f < this.lowerBoundary || f > this.upperBoundary) {
            n += "�", this._reset();
            break;
          }
          if (this.lowerBoundary = 128, this.upperBoundary = 191, this.codePoint = this.codePoint << 6 | f & 63, this.bytesSeen++, i++, this.bytesSeen === this.bytesNeeded) {
            n += String.fromCodePoint(this.codePoint), this._reset();
            break;
          }
        }
        if (this.bytesNeeded > 0) return n;
      }
      const l = e(a, i), s = a.byteLength - l;
      s > i && (n += t.toString(a, "utf8", i, s));
      for (let f = s; f < a.byteLength; f++) {
        const d = a[f];
        if (this.bytesNeeded === 0) {
          d <= 127 ? (this.bytesSeen = 0, n += String.fromCharCode(d)) : d >= 194 && d <= 223 ? (this.bytesNeeded = 2, this.bytesSeen = 1, this.codePoint = d & 31) : d >= 224 && d <= 239 ? (d === 224 ? this.lowerBoundary = 160 : d === 237 && (this.upperBoundary = 159), this.bytesNeeded = 3, this.bytesSeen = 1, this.codePoint = d & 15) : d >= 240 && d <= 244 ? (d === 240 ? this.lowerBoundary = 144 : d === 244 && (this.upperBoundary = 143), this.bytesNeeded = 4, this.bytesSeen = 1, this.codePoint = d & 7) : (this.bytesSeen = 1, n += "�");
          continue;
        }
        if (d < this.lowerBoundary || d > this.upperBoundary) {
          n += "�", f--, this._reset();
          continue;
        }
        this.lowerBoundary = 128, this.upperBoundary = 191, this.codePoint = this.codePoint << 6 | d & 63, this.bytesSeen++, this.bytesSeen === this.bytesNeeded && (n += String.fromCodePoint(this.codePoint), this._reset());
      }
      return n;
    }
    flush() {
      const a = this.bytesNeeded > 0 ? "�" : "";
      return this._reset(), a;
    }
    _reset() {
      this.codePoint = 0, this.bytesNeeded = 0, this.bytesSeen = 0, this.lowerBoundary = 128, this.upperBoundary = 191;
    }
  };
  function e(o, a) {
    const n = o.byteLength;
    if (n <= a) return 0;
    const i = Math.max(a, n - 4);
    let l = n - 1;
    for (; l > i && (o[l] & 192) === 128; ) l--;
    if (l < a) return 0;
    const s = o[l];
    let f;
    if (s <= 127) return 0;
    if (s >= 194 && s <= 223) f = 2;
    else if (s >= 224 && s <= 239) f = 3;
    else if (s >= 240 && s <= 244) f = 4;
    else return 0;
    const d = n - l;
    return d < f ? d : 0;
  }
  function r(o) {
    const a = o.byteLength;
    if (a === 0) return 0;
    const n = o[a - 1];
    if (n <= 127) return 0;
    if ((n & 192) !== 128) return 1;
    const i = Math.max(0, a - 4);
    let l = a - 2;
    for (; l >= i && (o[l] & 192) === 128; ) l--;
    if (l < 0) return 1;
    const s = o[l];
    let f;
    if (s >= 194 && s <= 223) f = 2;
    else if (s >= 224 && s <= 239) f = 3;
    else if (s >= 240 && s <= 244) f = 4;
    else return 1;
    if (a - l !== f) return 1;
    if (f >= 3) {
      const d = o[l + 1];
      if (s === 224 && d < 160 || s === 237 && d > 159 || s === 240 && d < 144 || s === 244 && d > 143) return 1;
    }
    return 0;
  }
  return jf;
}
var $f, I_;
function XA() {
  if (I_) return $f;
  I_ = 1;
  const t = KA(), e = JA();
  $f = class {
    constructor(a = "utf8") {
      switch (this.encoding = r(a), this.encoding) {
        case "utf8":
          this.decoder = new e();
          break;
        case "utf16le":
        case "base64":
          throw new Error("Unsupported encoding: " + this.encoding);
        default:
          this.decoder = new t(this.encoding);
      }
    }
    get remaining() {
      return this.decoder.remaining;
    }
    push(a) {
      return typeof a == "string" ? a : this.decoder.decode(a);
    }
    // For Node.js compatibility
    write(a) {
      return this.push(a);
    }
    end(a) {
      let n = "";
      return a && (n = this.push(a)), n += this.decoder.flush(), n;
    }
  };
  function r(o) {
    switch (o = o.toLowerCase(), o) {
      case "utf8":
      case "utf-8":
        return "utf8";
      case "ucs2":
      case "ucs-2":
      case "utf16le":
      case "utf-16le":
        return "utf16le";
      case "latin1":
      case "binary":
        return "latin1";
      case "base64":
      case "ascii":
      case "hex":
        return o;
      default:
        throw new Error("Unknown encoding: " + o);
    }
  }
  return $f;
}
var Wf, D_;
function IS() {
  if (D_) return Wf;
  D_ = 1;
  const { EventEmitter: t } = ZA(), e = new Error("Stream was destroyed"), r = new Error("Premature close"), o = xS(), a = XA(), n = typeof queueMicrotask > "u" ? (ye) => Tt.process.nextTick(ye) : queueMicrotask, i = (1 << 29) - 1, l = 1, s = 2, f = 4, d = 8, y = i ^ l, R = i ^ s, w = 16, I = 32, A = 64, u = 128, g = 256, p = 512, b = 1024, C = 2048, N = 4096, W = 8192, k = 16384, L = 32768, U = 65536, x = 131072, D = g | p, F = w | U, T = A | w, J = N | u, re = g | x, se = i ^ w, ne = i ^ A, M = i ^ (A | U), Q = i ^ U, v = i ^ g, $ = i ^ (u | W), ee = i ^ b, B = i ^ D, le = i ^ L, ae = i ^ I, ce = i ^ x, Y = i ^ re, P = 1 << 18, q = 2 << 18, V = 4 << 18, Z = 8 << 18, X = 16 << 18, m = 32 << 18, c = 64 << 18, E = 128 << 18, j = 256 << 18, G = 512 << 18, K = 1024 << 18, O = i ^ (P | j), H = i ^ V, te = i ^ (P | G), z = i ^ X, de = i ^ Z, pe = i ^ E, he = i ^ q, we = i ^ K, be = w | P, _e = i ^ be, Ae = k | m, xe = f | d | s, me = xe | l, Ge = xe | Ae, Je = H & ne, Xe = E | L, jt = Xe & _e, Ct = me | jt, $t = me | b | k, ht = me | k | u, wt = me | b | u, Wt = me | N | u | W, mt = me | w | b | k | U | x, tt = xe | b | k, qt = I | me | L | A, Bt = L | l, rt = me | G | m, St = Z | X, h = Z | P, S = Z | X | me | P, ie = me | P | Z | K, oe = V | P, fe = P | j, ve = me | G | h | m, Te = X | xe | G | m, Et = q | me | E | V, Pe = G | m | xe, lt = Symbol.asyncIterator || /* @__PURE__ */ Symbol("asyncIterator");
  class Mt {
    constructor(ue, { highWaterMark: ge = 16384, map: Ie = null, mapWritable: Le, byteLength: He, byteLengthWritable: qe } = {}) {
      this.stream = ue, this.queue = new o(), this.highWaterMark = ge, this.buffered = 0, this.error = null, this.pipeline = null, this.drains = null, this.byteLength = qe || He || ed, this.map = Le || Ie, this.afterWrite = Ce.bind(this), this.afterUpdateNextTick = BS.bind(this);
    }
    get ending() {
      return (this.stream._duplexState & G) !== 0;
    }
    get ended() {
      return (this.stream._duplexState & m) !== 0;
    }
    push(ue) {
      return (this.stream._duplexState & Pe) !== 0 ? !1 : (this.map !== null && (ue = this.map(ue)), this.buffered += this.byteLength(ue), this.queue.push(ue), this.buffered < this.highWaterMark ? (this.stream._duplexState |= Z, !0) : (this.stream._duplexState |= St, !1));
    }
    shift() {
      const ue = this.queue.shift();
      return this.buffered -= this.byteLength(ue), this.buffered === 0 && (this.stream._duplexState &= de), ue;
    }
    end(ue) {
      typeof ue == "function" ? this.stream.once("finish", ue) : ue != null && this.push(ue), this.stream._duplexState = (this.stream._duplexState | G) & H;
    }
    autoBatch(ue, ge) {
      const Ie = [], Le = this.stream;
      for (Ie.push(ue); (Le._duplexState & ie) === h; )
        Ie.push(Le._writableState.shift());
      if ((Le._duplexState & me) !== 0) return ge(null);
      Le._writev(Ie, ge);
    }
    update() {
      const ue = this.stream;
      ue._duplexState |= q;
      do {
        for (; (ue._duplexState & ie) === Z; ) {
          const ge = this.shift();
          ue._duplexState |= fe, ue._write(ge, this.afterWrite);
        }
        (ue._duplexState & oe) === 0 && this.updateNonPrimary();
      } while (this.continueUpdate() === !0);
      ue._duplexState &= he;
    }
    updateNonPrimary() {
      const ue = this.stream;
      if ((ue._duplexState & ve) === G) {
        ue._duplexState = ue._duplexState | P, ue._final(st.bind(this));
        return;
      }
      if ((ue._duplexState & xe) === f) {
        (ue._duplexState & Xe) === 0 && (ue._duplexState |= be, ue._destroy($e.bind(this)));
        return;
      }
      (ue._duplexState & Ct) === l && (ue._duplexState = (ue._duplexState | be) & y, ue._open(Yc.bind(this)));
    }
    continueUpdate() {
      return (this.stream._duplexState & E) === 0 ? !1 : (this.stream._duplexState &= pe, !0);
    }
    updateCallback() {
      (this.stream._duplexState & Et) === V ? this.update() : this.updateNextTick();
    }
    updateNextTick() {
      (this.stream._duplexState & E) === 0 && (this.stream._duplexState |= E, (this.stream._duplexState & q) === 0 && n(this.afterUpdateNextTick));
    }
  }
  class vt {
    constructor(ue, { highWaterMark: ge = 16384, map: Ie = null, mapReadable: Le, byteLength: He, byteLengthReadable: qe } = {}) {
      this.stream = ue, this.queue = new o(), this.highWaterMark = ge === 0 ? 1 : ge, this.buffered = 0, this.readAhead = ge > 0, this.error = null, this.pipeline = null, this.byteLength = qe || He || ed, this.map = Le || Ie, this.pipeTo = null, this.afterRead = De.bind(this), this.afterUpdateNextTick = qS.bind(this);
    }
    get ending() {
      return (this.stream._duplexState & b) !== 0;
    }
    get ended() {
      return (this.stream._duplexState & k) !== 0;
    }
    pipe(ue, ge) {
      if (this.pipeTo !== null) throw new Error("Can only pipe to one destination");
      if (typeof ge != "function" && (ge = null), this.stream._duplexState |= p, this.pipeTo = ue, this.pipeline = new nt(this.stream, ue, ge), ge && this.stream.on("error", td), Gr(ue))
        ue._writableState.pipeline = this.pipeline, ge && ue.on("error", td), ue.on("finish", this.pipeline.finished.bind(this.pipeline));
      else {
        const Ie = this.pipeline.done.bind(this.pipeline, ue), Le = this.pipeline.done.bind(this.pipeline, ue, null);
        ue.on("error", Ie), ue.on("close", Le), ue.on("finish", this.pipeline.finished.bind(this.pipeline));
      }
      ue.on("drain", it.bind(this)), this.stream.emit("piping", ue), ue.emit("pipe", this.stream);
    }
    push(ue) {
      const ge = this.stream;
      return ue === null ? (this.highWaterMark = 0, ge._duplexState = (ge._duplexState | b) & M, !1) : this.map !== null && (ue = this.map(ue), ue === null) ? (ge._duplexState &= Q, this.buffered < this.highWaterMark) : (this.buffered += this.byteLength(ue), this.queue.push(ue), ge._duplexState = (ge._duplexState | u) & Q, this.buffered < this.highWaterMark);
    }
    shift() {
      const ue = this.queue.shift();
      return this.buffered -= this.byteLength(ue), this.buffered === 0 && (this.stream._duplexState &= $), ue;
    }
    unshift(ue) {
      const ge = [this.map !== null ? this.map(ue) : ue];
      for (; this.buffered > 0; ) ge.push(this.shift());
      for (let Ie = 0; Ie < ge.length - 1; Ie++) {
        const Le = ge[Ie];
        this.buffered += this.byteLength(Le), this.queue.push(Le);
      }
      this.push(ge[ge.length - 1]);
    }
    read() {
      const ue = this.stream;
      if ((ue._duplexState & ht) === u) {
        const ge = this.shift();
        return this.pipeTo !== null && this.pipeTo.write(ge) === !1 && (ue._duplexState &= B), (ue._duplexState & C) !== 0 && ue.emit("data", ge), ge;
      }
      return this.readAhead === !1 && (ue._duplexState |= x, this.updateNextTick()), null;
    }
    drain() {
      const ue = this.stream;
      for (; (ue._duplexState & ht) === u && (ue._duplexState & D) !== 0; ) {
        const ge = this.shift();
        this.pipeTo !== null && this.pipeTo.write(ge) === !1 && (ue._duplexState &= B), (ue._duplexState & C) !== 0 && ue.emit("data", ge);
      }
    }
    update() {
      const ue = this.stream;
      ue._duplexState |= I;
      do {
        for (this.drain(); this.buffered < this.highWaterMark && (ue._duplexState & mt) === x; )
          ue._duplexState |= F, ue._read(this.afterRead), this.drain();
        (ue._duplexState & Wt) === J && (ue._duplexState |= W, ue.emit("readable")), (ue._duplexState & T) === 0 && this.updateNonPrimary();
      } while (this.continueUpdate() === !0);
      ue._duplexState &= ae;
    }
    updateNonPrimary() {
      const ue = this.stream;
      if ((ue._duplexState & wt) === b && (ue._duplexState = (ue._duplexState | k) & ee, ue.emit("end"), (ue._duplexState & Ge) === Ae && (ue._duplexState |= f), this.pipeTo !== null && this.pipeTo.end()), (ue._duplexState & xe) === f) {
        (ue._duplexState & Xe) === 0 && (ue._duplexState |= be, ue._destroy($e.bind(this)));
        return;
      }
      (ue._duplexState & Ct) === l && (ue._duplexState = (ue._duplexState | be) & y, ue._open(Yc.bind(this)));
    }
    continueUpdate() {
      return (this.stream._duplexState & L) === 0 ? !1 : (this.stream._duplexState &= le, !0);
    }
    updateCallback() {
      (this.stream._duplexState & qt) === A ? this.update() : this.updateNextTick();
    }
    updateNextTickIfOpen() {
      (this.stream._duplexState & Bt) === 0 && (this.stream._duplexState |= L, (this.stream._duplexState & I) === 0 && n(this.afterUpdateNextTick));
    }
    updateNextTick() {
      (this.stream._duplexState & L) === 0 && (this.stream._duplexState |= L, (this.stream._duplexState & I) === 0 && n(this.afterUpdateNextTick));
    }
  }
  class Rt {
    constructor(ue) {
      this.data = null, this.afterTransform = GS.bind(ue), this.afterFinal = null;
    }
  }
  class nt {
    constructor(ue, ge, Ie) {
      this.from = ue, this.to = ge, this.afterPipe = Ie, this.error = null, this.pipeToFinished = !1;
    }
    finished() {
      this.pipeToFinished = !0;
    }
    done(ue, ge) {
      if (ge && (this.error = ge), ue === this.to && (this.to = null, this.from !== null)) {
        ((this.from._duplexState & k) === 0 || !this.pipeToFinished) && this.from.destroy(this.error || new Error("Writable stream closed prematurely"));
        return;
      }
      if (ue === this.from && (this.from = null, this.to !== null)) {
        (ue._duplexState & k) === 0 && this.to.destroy(this.error || new Error("Readable stream closed before ending"));
        return;
      }
      this.afterPipe !== null && this.afterPipe(this.error), this.to = this.from = this.afterPipe = null;
    }
  }
  function it() {
    this.stream._duplexState |= p, this.updateCallback();
  }
  function st(ye) {
    const ue = this.stream;
    ye && ue.destroy(ye), (ue._duplexState & xe) === 0 && (ue._duplexState |= m, ue.emit("finish")), (ue._duplexState & Ge) === Ae && (ue._duplexState |= f), ue._duplexState &= te, (ue._duplexState & q) === 0 ? this.update() : this.updateNextTick();
  }
  function $e(ye) {
    const ue = this.stream;
    !ye && this.error !== e && (ye = this.error), ye && ue.emit("error", ye), ue._duplexState |= d, ue.emit("close");
    const ge = ue._readableState, Ie = ue._writableState;
    if (ge !== null && ge.pipeline !== null && ge.pipeline.done(ue, ye), Ie !== null) {
      for (; Ie.drains !== null && Ie.drains.length > 0; ) Ie.drains.shift().resolve(!1);
      Ie.pipeline !== null && Ie.pipeline.done(ue, ye);
    }
  }
  function Ce(ye) {
    const ue = this.stream;
    ye && ue.destroy(ye), ue._duplexState &= O, this.drains !== null && US(this.drains), (ue._duplexState & S) === X && (ue._duplexState &= z, (ue._duplexState & c) === c && ue.emit("drain")), this.updateCallback();
  }
  function De(ye) {
    ye && this.stream.destroy(ye), this.stream._duplexState &= se, this.readAhead === !1 && (this.stream._duplexState & g) === 0 && (this.stream._duplexState &= ce), this.updateCallback();
  }
  function qS() {
    (this.stream._duplexState & I) === 0 && (this.stream._duplexState &= le, this.update());
  }
  function BS() {
    (this.stream._duplexState & q) === 0 && (this.stream._duplexState &= pe, this.update());
  }
  function US(ye) {
    for (let ue = 0; ue < ye.length; ue++)
      --ye[ue].writes === 0 && (ye.shift().resolve(!0), ue--);
  }
  function Yc(ye) {
    const ue = this.stream;
    ye && ue.destroy(ye), (ue._duplexState & f) === 0 && ((ue._duplexState & $t) === 0 && (ue._duplexState |= A), (ue._duplexState & rt) === 0 && (ue._duplexState |= V), ue.emit("open")), ue._duplexState &= _e, ue._writableState !== null && ue._writableState.updateCallback(), ue._readableState !== null && ue._readableState.updateCallback();
  }
  function GS(ye, ue) {
    ue != null && this.push(ue), this._writableState.afterWrite(ye);
  }
  function HS(ye) {
    this._readableState !== null && (ye === "data" && (this._duplexState |= C | re, this._readableState.updateNextTick()), ye === "readable" && (this._duplexState |= N, this._readableState.updateNextTick())), this._writableState !== null && ye === "drain" && (this._duplexState |= c, this._writableState.updateNextTick());
  }
  class ds extends t {
    constructor(ue) {
      super(), this._duplexState = 0, this._readableState = null, this._writableState = null, ue && (ue.open && (this._open = ue.open), ue.destroy && (this._destroy = ue.destroy), ue.predestroy && (this._predestroy = ue.predestroy), ue.signal && ue.signal.addEventListener("abort", iE.bind(this))), this.on("newListener", HS);
    }
    _open(ue) {
      ue(null);
    }
    _destroy(ue) {
      ue(null);
    }
    _predestroy() {
    }
    get readable() {
      return this._readableState !== null ? !0 : void 0;
    }
    get writable() {
      return this._writableState !== null ? !0 : void 0;
    }
    get destroyed() {
      return (this._duplexState & d) !== 0;
    }
    get destroying() {
      return (this._duplexState & xe) !== 0;
    }
    destroy(ue) {
      (this._duplexState & xe) === 0 && (ue || (ue = e), this._duplexState = (this._duplexState | f) & Je, this._readableState !== null && (this._readableState.highWaterMark = 0, this._readableState.error = ue), this._writableState !== null && (this._writableState.highWaterMark = 0, this._writableState.error = ue), this._duplexState |= s, this._predestroy(), this._duplexState &= R, this._readableState !== null && this._readableState.updateNextTick(), this._writableState !== null && this._writableState.updateNextTick());
    }
  }
  class Ur extends ds {
    constructor(ue) {
      super(ue), this._duplexState |= l | m | x, this._readableState = new vt(this, ue), ue && (this._readableState.readAhead === !1 && (this._duplexState &= ce), ue.read && (this._read = ue.read), ue.eagerOpen && this._readableState.updateNextTick(), ue.encoding && this.setEncoding(ue.encoding));
    }
    setEncoding(ue) {
      const ge = new a(ue), Ie = this._readableState.map || YS;
      return this._readableState.map = Le, this;
      function Le(He) {
        const qe = ge.push(He);
        return qe === "" && (He.byteLength !== 0 || ge.remaining > 0) ? null : Ie(qe);
      }
    }
    _read(ue) {
      ue(null);
    }
    pipe(ue, ge) {
      return this._readableState.updateNextTick(), this._readableState.pipe(ue, ge), ue;
    }
    read() {
      return this._readableState.updateNextTick(), this._readableState.read();
    }
    push(ue) {
      return this._readableState.updateNextTickIfOpen(), this._readableState.push(ue);
    }
    unshift(ue) {
      return this._readableState.updateNextTickIfOpen(), this._readableState.unshift(ue);
    }
    resume() {
      return this._duplexState |= re, this._readableState.updateNextTick(), this;
    }
    pause() {
      return this._duplexState &= this._readableState.readAhead === !1 ? Y : v, this;
    }
    static _fromAsyncIterator(ue, ge) {
      let Ie;
      const Le = new Ur({
        ...ge,
        read(qe) {
          ue.next().then(He).then(qe.bind(null, null)).catch(qe);
        },
        predestroy() {
          Ie = ue.return();
        },
        destroy(qe) {
          if (!Ie) return qe(null);
          Ie.then(qe.bind(null, null)).catch(qe);
        }
      });
      return Le;
      function He(qe) {
        qe.done ? Le.push(null) : Le.push(qe.value);
      }
    }
    static from(ue, ge) {
      if (tE(ue)) return ue;
      if (ue[lt]) return this._fromAsyncIterator(ue[lt](), ge);
      Array.isArray(ue) || (ue = ue === void 0 ? [] : [ue]);
      let Ie = 0;
      return new Ur({
        ...ge,
        read(Le) {
          this.push(Ie === ue.length ? null : ue[Ie++]), Le(null);
        }
      });
    }
    static isBackpressured(ue) {
      return (ue._duplexState & tt) !== 0 || ue._readableState.buffered >= ue._readableState.highWaterMark;
    }
    static isPaused(ue) {
      return (ue._duplexState & g) === 0;
    }
    [lt]() {
      const ue = this;
      let ge = null, Ie = null, Le = null;
      return this.on("error", (Me) => {
        ge = Me;
      }), this.on("readable", He), this.on("close", qe), {
        [lt]() {
          return this;
        },
        next() {
          return new Promise(function(Me, kt) {
            Ie = Me, Le = kt;
            const ar = ue.read();
            ar !== null ? xr(ar) : (ue._duplexState & d) !== 0 && xr(null);
          });
        },
        return() {
          return Hr(null);
        },
        throw(Me) {
          return Hr(Me);
        }
      };
      function He() {
        Ie !== null && xr(ue.read());
      }
      function qe() {
        Ie !== null && xr(null);
      }
      function xr(Me) {
        Le !== null && (ge ? Le(ge) : Me === null && (ue._duplexState & k) === 0 ? Le(e) : Ie({ value: Me, done: Me === null }), Le = Ie = null);
      }
      function Hr(Me) {
        return ue.destroy(Me), new Promise((kt, ar) => {
          if (ue._duplexState & d) return kt({ value: void 0, done: !0 });
          ue.once("close", function() {
            Me ? ar(Me) : kt({ value: void 0, done: !0 });
          });
        });
      }
    }
  }
  class Kc extends ds {
    constructor(ue) {
      super(ue), this._duplexState |= l | k, this._writableState = new Mt(this, ue), ue && (ue.writev && (this._writev = ue.writev), ue.write && (this._write = ue.write), ue.final && (this._final = ue.final), ue.eagerOpen && this._writableState.updateNextTick());
    }
    cork() {
      this._duplexState |= K;
    }
    uncork() {
      this._duplexState &= we, this._writableState.updateNextTick();
    }
    _writev(ue, ge) {
      ge(null);
    }
    _write(ue, ge) {
      this._writableState.autoBatch(ue, ge);
    }
    _final(ue) {
      ue(null);
    }
    static isBackpressured(ue) {
      return (ue._duplexState & Te) !== 0;
    }
    static drained(ue) {
      if (ue.destroyed) return Promise.resolve(!1);
      const ge = ue._writableState, Le = (sE(ue) ? Math.min(1, ge.queue.length) : ge.queue.length) + (ue._duplexState & j ? 1 : 0);
      return Le === 0 ? Promise.resolve(!0) : (ge.drains === null && (ge.drains = []), new Promise((He) => {
        ge.drains.push({ writes: Le, resolve: He });
      }));
    }
    write(ue) {
      return this._writableState.updateNextTick(), this._writableState.push(ue);
    }
    end(ue) {
      return this._writableState.updateNextTick(), this._writableState.end(ue), this;
    }
  }
  class hs extends Ur {
    // and Writable
    constructor(ue) {
      super(ue), this._duplexState = l | this._duplexState & x, this._writableState = new Mt(this, ue), ue && (ue.writev && (this._writev = ue.writev), ue.write && (this._write = ue.write), ue.final && (this._final = ue.final));
    }
    cork() {
      this._duplexState |= K;
    }
    uncork() {
      this._duplexState &= we, this._writableState.updateNextTick();
    }
    _writev(ue, ge) {
      ge(null);
    }
    _write(ue, ge) {
      this._writableState.autoBatch(ue, ge);
    }
    _final(ue) {
      ue(null);
    }
    write(ue) {
      return this._writableState.updateNextTick(), this._writableState.push(ue);
    }
    end(ue) {
      return this._writableState.updateNextTick(), this._writableState.end(ue), this;
    }
  }
  class Jc extends hs {
    constructor(ue) {
      super(ue), this._transformState = new Rt(this), ue && (ue.transform && (this._transform = ue.transform), ue.flush && (this._flush = ue.flush));
    }
    _write(ue, ge) {
      this._readableState.buffered >= this._readableState.highWaterMark ? this._transformState.data = ue : this._transform(ue, this._transformState.afterTransform);
    }
    _read(ue) {
      if (this._transformState.data !== null) {
        const ge = this._transformState.data;
        this._transformState.data = null, ue(null), this._transform(ge, this._transformState.afterTransform);
      } else
        ue(null);
    }
    destroy(ue) {
      super.destroy(ue), this._transformState.data !== null && (this._transformState.data = null, this._transformState.afterTransform());
    }
    _transform(ue, ge) {
      ge(null, ue);
    }
    _flush(ue) {
      ue(null);
    }
    _final(ue) {
      this._transformState.afterFinal = ue, this._flush(VS.bind(this));
    }
  }
  class zS extends Jc {
  }
  function VS(ye, ue) {
    const ge = this._transformState.afterFinal;
    if (ye) return ge(ye);
    ue != null && this.push(ue), this.push(null), ge(null);
  }
  function ZS(...ye) {
    return new Promise((ue, ge) => Xc(...ye, (Ie) => {
      if (Ie) return ge(Ie);
      ue();
    }));
  }
  function Xc(ye, ...ue) {
    const ge = Array.isArray(ye) ? [...ye, ...ue] : [ye, ...ue], Ie = ge.length && typeof ge[ge.length - 1] == "function" ? ge.pop() : null;
    if (ge.length < 2) throw new Error("Pipeline requires at least 2 streams");
    let Le = ge[0], He = null, qe = null;
    for (let Me = 1; Me < ge.length; Me++)
      He = ge[Me], Gr(Le) ? Le.pipe(He, Hr) : (xr(Le, !0, Me > 1, Hr), Le.pipe(He)), Le = He;
    if (Ie) {
      let Me = !1;
      const kt = Gr(He) || !!(He._writableState && He._writableState.autoDestroy);
      He.on("error", (ar) => {
        qe === null && (qe = ar);
      }), He.on("finish", () => {
        Me = !0, kt || Ie(qe);
      }), kt && He.on("close", () => Ie(qe || (Me ? null : r)));
    }
    return He;
    function xr(Me, kt, ar, ps) {
      Me.on("error", ps), Me.on("close", oE);
      function oE() {
        if (Me._readableState && !Me._readableState.ended || ar && Me._writableState && !Me._writableState.ended) return ps(r);
      }
    }
    function Hr(Me) {
      if (!(!Me || qe)) {
        qe = Me;
        for (const kt of ge)
          kt.destroy(Me);
      }
    }
  }
  function YS(ye) {
    return ye;
  }
  function Qc(ye) {
    return !!ye._readableState || !!ye._writableState;
  }
  function Gr(ye) {
    return typeof ye._duplexState == "number" && Qc(ye);
  }
  function KS(ye) {
    return !!ye._readableState && ye._readableState.ending;
  }
  function JS(ye) {
    return !!ye._readableState && ye._readableState.ended;
  }
  function XS(ye) {
    return !!ye._writableState && ye._writableState.ending;
  }
  function QS(ye) {
    return !!ye._writableState && ye._writableState.ended;
  }
  function eE(ye, ue = {}) {
    const ge = ye._readableState && ye._readableState.error || ye._writableState && ye._writableState.error;
    return !ue.all && ge === e ? null : ge;
  }
  function tE(ye) {
    return Gr(ye) && ye.readable;
  }
  function rE(ye) {
    return (ye._duplexState & l) !== l || (ye._duplexState & f) === f || (ye._duplexState & Xe) !== 0;
  }
  function nE(ye) {
    return typeof ye == "object" && ye !== null && typeof ye.byteLength == "number";
  }
  function ed(ye) {
    return nE(ye) ? ye.byteLength : 1024;
  }
  function td() {
  }
  function iE() {
    this.destroy(new Error("Stream aborted."));
  }
  function sE(ye) {
    return ye._writev !== Kc.prototype._writev && ye._writev !== hs.prototype._writev;
  }
  return Wf = {
    pipeline: Xc,
    pipelinePromise: ZS,
    isStream: Qc,
    isStreamx: Gr,
    isEnding: KS,
    isEnded: JS,
    isFinishing: XS,
    isFinished: QS,
    isDisturbed: rE,
    getStreamError: eE,
    Stream: ds,
    Writable: Kc,
    Readable: Ur,
    Duplex: hs,
    Transform: Jc,
    // Export PassThrough for compatibility with Node.js core's stream module
    PassThrough: zS
  }, Wf;
}
var _r = {}, O_;
function DS() {
  if (O_) return _r;
  O_ = 1;
  const t = On(), e = "0000000000000000000", r = "7777777777777777777", o = 48, a = t.from([117, 115, 116, 97, 114, 0]), n = t.from([o, o]), i = t.from([117, 115, 116, 97, 114, 32]), l = t.from([32, 0]), s = 4095, f = 257, d = 263;
  _r.decodeLongPath = function(x, D) {
    return k(x, 0, x.length, D);
  }, _r.encodePax = function(x) {
    let D = "";
    x.name && (D += L(" path=" + x.name + `
`)), x.linkname && (D += L(" linkpath=" + x.linkname + `
`));
    const F = x.pax;
    if (F)
      for (const T in F)
        D += L(" " + T + "=" + F[T] + `
`);
    return t.from(D);
  }, _r.decodePax = function(x) {
    const D = {};
    for (; x.length; ) {
      let F = 0;
      for (; F < x.length && x[F] !== 32; ) F++;
      const T = parseInt(t.toString(x.subarray(0, F)), 10);
      if (!T) return D;
      const J = t.toString(x.subarray(F + 1, T - 1)), re = J.indexOf("=");
      if (re === -1) return D;
      D[J.slice(0, re)] = J.slice(re + 1), x = x.subarray(T);
    }
    return D;
  }, _r.encode = function(x) {
    const D = t.alloc(512);
    let F = x.name, T = "";
    if (x.typeflag === 5 && F[F.length - 1] !== "/" && (F += "/"), t.byteLength(F) !== F.length) return null;
    for (; t.byteLength(F) > 100; ) {
      const J = F.indexOf("/");
      if (J === -1) return null;
      T += T ? "/" + F.slice(0, J) : F.slice(0, J), F = F.slice(J + 1);
    }
    return t.byteLength(F) > 100 || t.byteLength(T) > 155 || x.linkname && t.byteLength(x.linkname) > 100 ? null : (t.write(D, F), t.write(D, p(x.mode & s, 6), 100), t.write(D, p(x.uid, 6), 108), t.write(D, p(x.gid, 6), 116), C(x.size, D, 124), t.write(D, p(x.mtime.getTime() / 1e3 | 0, 11), 136), D[156] = o + A(x.type), x.linkname && t.write(D, x.linkname, 157), t.copy(a, D, f), t.copy(n, D, d), x.uname && t.write(D, x.uname, 265), x.gname && t.write(D, x.gname, 297), t.write(D, p(x.devmajor || 0, 6), 329), t.write(D, p(x.devminor || 0, 6), 337), T && t.write(D, T, 345), t.write(D, p(g(D), 6), 148), D);
  }, _r.decode = function(x, D, F) {
    let T = x[156] === 0 ? 0 : x[156] - o, J = k(x, 0, 100, D);
    const re = W(x, 100, 8), se = W(x, 108, 8), ne = W(x, 116, 8), M = W(x, 124, 12), Q = W(x, 136, 12), v = I(T), $ = x[157] === 0 ? null : k(x, 157, 100, D), ee = k(x, 265, 32), B = k(x, 297, 32), le = W(x, 329, 8), ae = W(x, 337, 8), ce = g(x);
    if (ce === 256) return null;
    if (ce !== W(x, 148, 8)) throw new Error("Invalid tar header. Maybe the tar is corrupted or it needs to be gunzipped?");
    if (y(x))
      x[345] && (J = k(x, 345, 155, D) + "/" + J);
    else if (!R(x)) {
      if (!F)
        throw new Error("Invalid tar header: unknown format.");
    }
    return T === 0 && J && J[J.length - 1] === "/" && (T = 5), {
      name: J,
      mode: re,
      uid: se,
      gid: ne,
      size: M,
      mtime: new Date(1e3 * Q),
      type: v,
      linkname: $,
      uname: ee,
      gname: B,
      devmajor: le,
      devminor: ae,
      pax: null
    };
  };
  function y(U) {
    return t.equals(a, U.subarray(f, f + 6));
  }
  function R(U) {
    return t.equals(i, U.subarray(f, f + 6)) && t.equals(l, U.subarray(d, d + 2));
  }
  function w(U, x, D) {
    return typeof U != "number" ? D : (U = ~~U, U >= x ? x : U >= 0 || (U += x, U >= 0) ? U : 0);
  }
  function I(U) {
    switch (U) {
      case 0:
        return "file";
      case 1:
        return "link";
      case 2:
        return "symlink";
      case 3:
        return "character-device";
      case 4:
        return "block-device";
      case 5:
        return "directory";
      case 6:
        return "fifo";
      case 7:
        return "contiguous-file";
      case 72:
        return "pax-header";
      case 55:
        return "pax-global-header";
      case 27:
        return "gnu-long-link-path";
      case 28:
      case 30:
        return "gnu-long-path";
    }
    return null;
  }
  function A(U) {
    switch (U) {
      case "file":
        return 0;
      case "link":
        return 1;
      case "symlink":
        return 2;
      case "character-device":
        return 3;
      case "block-device":
        return 4;
      case "directory":
        return 5;
      case "fifo":
        return 6;
      case "contiguous-file":
        return 7;
      case "pax-header":
        return 72;
    }
    return 0;
  }
  function u(U, x, D, F) {
    for (; D < F; D++)
      if (U[D] === x) return D;
    return F;
  }
  function g(U) {
    let x = 256;
    for (let D = 0; D < 148; D++) x += U[D];
    for (let D = 156; D < 512; D++) x += U[D];
    return x;
  }
  function p(U, x) {
    return U = U.toString(8), U.length > x ? r.slice(0, x) + " " : e.slice(0, x - U.length) + U + " ";
  }
  function b(U, x, D) {
    x[D] = 128;
    for (let F = 11; F > 0; F--)
      x[D + F] = U & 255, U = Math.floor(U / 256);
  }
  function C(U, x, D) {
    U.toString(8).length > 11 ? b(U, x, D) : t.write(x, p(U, 11), D);
  }
  function N(U) {
    let x;
    if (U[0] === 128) x = !0;
    else if (U[0] === 255) x = !1;
    else return null;
    const D = [];
    let F;
    for (F = U.length - 1; F > 0; F--) {
      const re = U[F];
      x ? D.push(re) : D.push(255 - re);
    }
    let T = 0;
    const J = D.length;
    for (F = 0; F < J; F++)
      T += D[F] * Math.pow(256, F);
    return x ? T : -1 * T;
  }
  function W(U, x, D) {
    if (U = U.subarray(x, x + D), x = 0, U[x] & 128)
      return N(U);
    {
      for (; x < U.length && U[x] === 32; ) x++;
      const F = w(u(U, 32, x, U.length), U.length, U.length);
      for (; x < F && U[x] === 0; ) x++;
      return F === x ? 0 : parseInt(t.toString(U.subarray(x, F)), 8);
    }
  }
  function k(U, x, D, F) {
    return t.toString(U.subarray(x, u(U, 0, x, x + D)), F);
  }
  function L(U) {
    const x = t.byteLength(U);
    let D = Math.floor(Math.log(x) / Math.log(10)) + 1;
    return x + D >= Math.pow(10, D) && D++, x + D + U;
  }
  return _r;
}
var qf, N_;
function QA() {
  if (N_) return qf;
  N_ = 1;
  const { Writable: t, Readable: e, getStreamError: r } = IS(), o = xS(), a = On(), n = DS(), i = a.alloc(0);
  class l {
    constructor() {
      this.buffered = 0, this.shifted = 0, this.queue = new o(), this._offset = 0;
    }
    push(w) {
      this.buffered += w.byteLength, this.queue.push(w);
    }
    shiftFirst(w) {
      return this._buffered === 0 ? null : this._next(w);
    }
    shift(w) {
      if (w > this.buffered) return null;
      if (w === 0) return i;
      let I = this._next(w);
      if (w === I.byteLength) return I;
      const A = [I];
      for (; (w -= I.byteLength) > 0; )
        I = this._next(w), A.push(I);
      return a.concat(A);
    }
    _next(w) {
      const I = this.queue.peek(), A = I.byteLength - this._offset;
      if (w >= A) {
        const u = this._offset ? I.subarray(this._offset, I.byteLength) : I;
        return this.queue.shift(), this._offset = 0, this.buffered -= A, this.shifted += A, u;
      }
      return this.buffered -= w, this.shifted += w, I.subarray(this._offset, this._offset += w);
    }
  }
  class s extends e {
    constructor(w, I, A) {
      super(), this.header = I, this.offset = A, this._parent = w;
    }
    _read(w) {
      this.header.size === 0 && this.push(null), this._parent._stream === this && this._parent._update(), w(null);
    }
    _predestroy() {
      this._parent.destroy(r(this));
    }
    _detach() {
      this._parent._stream === this && (this._parent._stream = null, this._parent._missing = y(this.header.size), this._parent._update());
    }
    _destroy(w) {
      this._detach(), w(null);
    }
  }
  class f extends t {
    constructor(w) {
      super(w), w || (w = {}), this._buffer = new l(), this._offset = 0, this._header = null, this._stream = null, this._missing = 0, this._longHeader = !1, this._callback = d, this._locked = !1, this._finished = !1, this._pax = null, this._paxGlobal = null, this._gnuLongPath = null, this._gnuLongLinkPath = null, this._filenameEncoding = w.filenameEncoding || "utf-8", this._allowUnknownFormat = !!w.allowUnknownFormat, this._unlockBound = this._unlock.bind(this);
    }
    _unlock(w) {
      if (this._locked = !1, w) {
        this.destroy(w), this._continueWrite(w);
        return;
      }
      this._update();
    }
    _consumeHeader() {
      if (this._locked) return !1;
      this._offset = this._buffer.shifted;
      try {
        this._header = n.decode(this._buffer.shift(512), this._filenameEncoding, this._allowUnknownFormat);
      } catch (w) {
        return this._continueWrite(w), !1;
      }
      if (!this._header) return !0;
      switch (this._header.type) {
        case "gnu-long-path":
        case "gnu-long-link-path":
        case "pax-global-header":
        case "pax-header":
          return this._longHeader = !0, this._missing = this._header.size, !0;
      }
      return this._locked = !0, this._applyLongHeaders(), this._header.size === 0 || this._header.type === "directory" ? (this.emit("entry", this._header, this._createStream(), this._unlockBound), !0) : (this._stream = this._createStream(), this._missing = this._header.size, this.emit("entry", this._header, this._stream, this._unlockBound), !0);
    }
    _applyLongHeaders() {
      this._gnuLongPath && (this._header.name = this._gnuLongPath, this._gnuLongPath = null), this._gnuLongLinkPath && (this._header.linkname = this._gnuLongLinkPath, this._gnuLongLinkPath = null), this._pax && (this._pax.path && (this._header.name = this._pax.path), this._pax.linkpath && (this._header.linkname = this._pax.linkpath), this._pax.size && (this._header.size = parseInt(this._pax.size, 10)), this._header.pax = this._pax, this._pax = null);
    }
    _decodeLongHeader(w) {
      switch (this._header.type) {
        case "gnu-long-path":
          this._gnuLongPath = n.decodeLongPath(w, this._filenameEncoding);
          break;
        case "gnu-long-link-path":
          this._gnuLongLinkPath = n.decodeLongPath(w, this._filenameEncoding);
          break;
        case "pax-global-header":
          this._paxGlobal = n.decodePax(w);
          break;
        case "pax-header":
          this._pax = this._paxGlobal === null ? n.decodePax(w) : Object.assign({}, this._paxGlobal, n.decodePax(w));
          break;
      }
    }
    _consumeLongHeader() {
      this._longHeader = !1, this._missing = y(this._header.size);
      const w = this._buffer.shift(this._header.size);
      try {
        this._decodeLongHeader(w);
      } catch (I) {
        return this._continueWrite(I), !1;
      }
      return !0;
    }
    _consumeStream() {
      const w = this._buffer.shiftFirst(this._missing);
      if (w === null) return !1;
      this._missing -= w.byteLength;
      const I = this._stream.push(w);
      return this._missing === 0 ? (this._stream.push(null), I && this._stream._detach(), I && this._locked === !1) : I;
    }
    _createStream() {
      return new s(this, this._header, this._offset);
    }
    _update() {
      for (; this._buffer.buffered > 0 && !this.destroying; ) {
        if (this._missing > 0) {
          if (this._stream !== null) {
            if (this._consumeStream() === !1) return;
            continue;
          }
          if (this._longHeader === !0) {
            if (this._missing > this._buffer.buffered) break;
            if (this._consumeLongHeader() === !1) return !1;
            continue;
          }
          const w = this._buffer.shiftFirst(this._missing);
          w !== null && (this._missing -= w.byteLength);
          continue;
        }
        if (this._buffer.buffered < 512) break;
        if (this._stream !== null || this._consumeHeader() === !1) return;
      }
      this._continueWrite(null);
    }
    _continueWrite(w) {
      const I = this._callback;
      this._callback = d, I(w);
    }
    _write(w, I) {
      this._callback = I, this._buffer.push(w), this._update();
    }
    _final(w) {
      this._finished = this._missing === 0 && this._buffer.buffered === 0, w(this._finished ? null : new Error("Unexpected end of data"));
    }
    _predestroy() {
      this._continueWrite(null);
    }
    _destroy(w) {
      this._stream && this._stream.destroy(r(this)), w(null);
    }
    [Symbol.asyncIterator]() {
      let w = null, I = null, A = null, u = null, g = null;
      const p = this;
      return this.on("entry", N), this.on("error", (L) => {
        w = L;
      }), this.on("close", W), {
        [Symbol.asyncIterator]() {
          return this;
        },
        next() {
          return new Promise(C);
        },
        return() {
          return k(null);
        },
        throw(L) {
          return k(L);
        }
      };
      function b(L) {
        if (!g) return;
        const U = g;
        g = null, U(L);
      }
      function C(L, U) {
        if (w)
          return U(w);
        if (u) {
          L({ value: u, done: !1 }), u = null;
          return;
        }
        I = L, A = U, b(null), p._finished && I && (I({ value: void 0, done: !0 }), I = A = null);
      }
      function N(L, U, x) {
        g = x, U.on("error", d), I ? (I({ value: U, done: !1 }), I = A = null) : u = U;
      }
      function W() {
        b(w), I && (w ? A(w) : I({ value: void 0, done: !0 }), I = A = null);
      }
      function k(L) {
        return p.destroy(L), b(L), new Promise((U, x) => {
          if (p.destroyed) return U({ value: void 0, done: !0 });
          p.once("close", function() {
            L ? x(L) : U({ value: void 0, done: !0 });
          });
        });
      }
    }
  }
  qf = function(w) {
    return new f(w);
  };
  function d() {
  }
  function y(R) {
    return R &= 511, R && 512 - R;
  }
  return qf;
}
var ei = { exports: {} }, L_;
function eT() {
  if (L_) return ei.exports;
  L_ = 1;
  const t = {
    // just for envs without fs
    S_IFMT: 61440,
    S_IFDIR: 16384,
    S_IFCHR: 8192,
    S_IFBLK: 24576,
    S_IFIFO: 4096,
    S_IFLNK: 40960
  };
  try {
    ei.exports = require("fs").constants || t;
  } catch {
    ei.exports = t;
  }
  return ei.exports;
}
var Bf, P_;
function tT() {
  if (P_) return Bf;
  P_ = 1;
  const { Readable: t, Writable: e, getStreamError: r } = IS(), o = On(), a = eT(), n = DS(), i = 493, l = 420, s = o.alloc(1024);
  class f extends e {
    constructor(u, g, p) {
      super({ mapWritable: I, eagerOpen: !0 }), this.written = 0, this.header = g, this._callback = p, this._linkname = null, this._isLinkname = g.type === "symlink" && !g.linkname, this._isVoid = g.type !== "file" && g.type !== "contiguous-file", this._finished = !1, this._pack = u, this._openCallback = null, this._pack._stream === null ? this._pack._stream = this : this._pack._pending.push(this);
    }
    _open(u) {
      this._openCallback = u, this._pack._stream === this && this._continueOpen();
    }
    _continuePack(u) {
      if (this._callback === null) return;
      const g = this._callback;
      this._callback = null, g(u);
    }
    _continueOpen() {
      this._pack._stream === null && (this._pack._stream = this);
      const u = this._openCallback;
      if (this._openCallback = null, u !== null) {
        if (this._pack.destroying) return u(new Error("pack stream destroyed"));
        if (this._pack._finalized) return u(new Error("pack stream is already finalized"));
        this._pack._stream = this, this._isLinkname || this._pack._encode(this.header), this._isVoid && (this._finish(), this._continuePack(null)), u(null);
      }
    }
    _write(u, g) {
      if (this._isLinkname)
        return this._linkname = this._linkname ? o.concat([this._linkname, u]) : u, g(null);
      if (this._isVoid)
        return u.byteLength > 0 ? g(new Error("No body allowed for this entry")) : g();
      if (this.written += u.byteLength, this._pack.push(u)) return g();
      this._pack._drain = g;
    }
    _finish() {
      this._finished || (this._finished = !0, this._isLinkname && (this.header.linkname = this._linkname ? o.toString(this._linkname, "utf-8") : "", this._pack._encode(this.header)), w(this._pack, this.header.size), this._pack._done(this));
    }
    _final(u) {
      if (this.written !== this.header.size)
        return u(new Error("Size mismatch"));
      this._finish(), u(null);
    }
    _getError() {
      return r(this) || new Error("tar entry destroyed");
    }
    _predestroy() {
      this._pack.destroy(this._getError());
    }
    _destroy(u) {
      this._pack._done(this), this._continuePack(this._finished ? null : this._getError()), u();
    }
  }
  class d extends t {
    constructor(u) {
      super(u), this._drain = R, this._finalized = !1, this._finalizing = !1, this._pending = [], this._stream = null;
    }
    entry(u, g, p) {
      if (this._finalized || this.destroying) throw new Error("already finalized or destroyed");
      typeof g == "function" && (p = g, g = null), p || (p = R), (!u.size || u.type === "symlink") && (u.size = 0), u.type || (u.type = y(u.mode)), u.mode || (u.mode = u.type === "directory" ? i : l), u.uid || (u.uid = 0), u.gid || (u.gid = 0), u.mtime || (u.mtime = /* @__PURE__ */ new Date()), typeof g == "string" && (g = o.from(g));
      const b = new f(this, u, p);
      return o.isBuffer(g) ? (u.size = g.byteLength, b.write(g), b.end(), b) : (b._isVoid, b);
    }
    finalize() {
      if (this._stream || this._pending.length > 0) {
        this._finalizing = !0;
        return;
      }
      this._finalized || (this._finalized = !0, this.push(s), this.push(null));
    }
    _done(u) {
      u === this._stream && (this._stream = null, this._finalizing && this.finalize(), this._pending.length && this._pending.shift()._continueOpen());
    }
    _encode(u) {
      if (!u.pax) {
        const g = n.encode(u);
        if (g) {
          this.push(g);
          return;
        }
      }
      this._encodePax(u);
    }
    _encodePax(u) {
      const g = n.encodePax({
        name: u.name,
        linkname: u.linkname,
        pax: u.pax
      }), p = {
        name: "PaxHeader",
        mode: u.mode,
        uid: u.uid,
        gid: u.gid,
        size: g.byteLength,
        mtime: u.mtime,
        type: "pax-header",
        linkname: u.linkname && "PaxHeader",
        uname: u.uname,
        gname: u.gname,
        devmajor: u.devmajor,
        devminor: u.devminor
      };
      this.push(n.encode(p)), this.push(g), w(this, g.byteLength), p.size = u.size, p.type = u.type, this.push(n.encode(p));
    }
    _doDrain() {
      const u = this._drain;
      this._drain = R, u();
    }
    _predestroy() {
      const u = r(this);
      for (this._stream && this._stream.destroy(u); this._pending.length; ) {
        const g = this._pending.shift();
        g.destroy(u), g._continueOpen();
      }
      this._doDrain();
    }
    _read(u) {
      this._doDrain(), u();
    }
  }
  Bf = function(u) {
    return new d(u);
  };
  function y(A) {
    switch (A & a.S_IFMT) {
      case a.S_IFBLK:
        return "block-device";
      case a.S_IFCHR:
        return "character-device";
      case a.S_IFDIR:
        return "directory";
      case a.S_IFIFO:
        return "fifo";
      case a.S_IFLNK:
        return "symlink";
    }
    return "file";
  }
  function R() {
  }
  function w(A, u) {
    u &= 511, u && A.push(s.subarray(0, 512 - u));
  }
  function I(A) {
    return o.isBuffer(A) ? A : o.from(A);
  }
  return Bf;
}
var C_;
function rT() {
  return C_ || (C_ = 1, Qn.extract = QA(), Qn.pack = tT()), Qn;
}
var Uf, M_;
function nT() {
  if (M_) return Uf;
  M_ = 1;
  var t = Pi, e = rT(), r = Tn(), o = function(a) {
    if (!(this instanceof o))
      return new o(a);
    a = this.options = r.defaults(a, {
      gzip: !1
    }), typeof a.gzipOptions != "object" && (a.gzipOptions = {}), this.supports = {
      directory: !0,
      symlink: !0
    }, this.engine = e.pack(a), this.compressor = !1, a.gzip && (this.compressor = t.createGzip(a.gzipOptions), this.compressor.on("error", this._onCompressorError.bind(this)));
  };
  return o.prototype._onCompressorError = function(a) {
    this.engine.emit("error", a);
  }, o.prototype.append = function(a, n, i) {
    var l = this;
    n.mtime = n.date;
    function s(d, y) {
      if (d) {
        i(d);
        return;
      }
      l.engine.entry(n, y, function(R) {
        i(R, n);
      });
    }
    if (n.sourceType === "buffer")
      s(null, a);
    else if (n.sourceType === "stream" && n.stats) {
      n.size = n.stats.size;
      var f = l.engine.entry(n, function(d) {
        i(d, n);
      });
      a.pipe(f);
    } else n.sourceType === "stream" && r.collectStream(a, s);
  }, o.prototype.finalize = function() {
    this.engine.finalize();
  }, o.prototype.on = function() {
    return this.engine.on.apply(this.engine, arguments);
  }, o.prototype.pipe = function(a, n) {
    return this.compressor ? this.engine.pipe.apply(this.engine, [this.compressor]).pipe(a, n) : this.engine.pipe.apply(this.engine, arguments);
  }, o.prototype.unpipe = function() {
    return this.compressor ? this.compressor.unpipe.apply(this.compressor, arguments) : this.engine.unpipe.apply(this.engine, arguments);
  }, Uf = o, Uf;
}
var Gf, k_;
function iT() {
  if (k_) return Gf;
  k_ = 1;
  function t(s) {
    return s && s.__esModule && Object.prototype.hasOwnProperty.call(s, "default") ? s.default : s;
  }
  const e = new Int32Array([
    0,
    1996959894,
    3993919788,
    2567524794,
    124634137,
    1886057615,
    3915621685,
    2657392035,
    249268274,
    2044508324,
    3772115230,
    2547177864,
    162941995,
    2125561021,
    3887607047,
    2428444049,
    498536548,
    1789927666,
    4089016648,
    2227061214,
    450548861,
    1843258603,
    4107580753,
    2211677639,
    325883990,
    1684777152,
    4251122042,
    2321926636,
    335633487,
    1661365465,
    4195302755,
    2366115317,
    997073096,
    1281953886,
    3579855332,
    2724688242,
    1006888145,
    1258607687,
    3524101629,
    2768942443,
    901097722,
    1119000684,
    3686517206,
    2898065728,
    853044451,
    1172266101,
    3705015759,
    2882616665,
    651767980,
    1373503546,
    3369554304,
    3218104598,
    565507253,
    1454621731,
    3485111705,
    3099436303,
    671266974,
    1594198024,
    3322730930,
    2970347812,
    795835527,
    1483230225,
    3244367275,
    3060149565,
    1994146192,
    31158534,
    2563907772,
    4023717930,
    1907459465,
    112637215,
    2680153253,
    3904427059,
    2013776290,
    251722036,
    2517215374,
    3775830040,
    2137656763,
    141376813,
    2439277719,
    3865271297,
    1802195444,
    476864866,
    2238001368,
    4066508878,
    1812370925,
    453092731,
    2181625025,
    4111451223,
    1706088902,
    314042704,
    2344532202,
    4240017532,
    1658658271,
    366619977,
    2362670323,
    4224994405,
    1303535960,
    984961486,
    2747007092,
    3569037538,
    1256170817,
    1037604311,
    2765210733,
    3554079995,
    1131014506,
    879679996,
    2909243462,
    3663771856,
    1141124467,
    855842277,
    2852801631,
    3708648649,
    1342533948,
    654459306,
    3188396048,
    3373015174,
    1466479909,
    544179635,
    3110523913,
    3462522015,
    1591671054,
    702138776,
    2966460450,
    3352799412,
    1504918807,
    783551873,
    3082640443,
    3233442989,
    3988292384,
    2596254646,
    62317068,
    1957810842,
    3939845945,
    2647816111,
    81470997,
    1943803523,
    3814918930,
    2489596804,
    225274430,
    2053790376,
    3826175755,
    2466906013,
    167816743,
    2097651377,
    4027552580,
    2265490386,
    503444072,
    1762050814,
    4150417245,
    2154129355,
    426522225,
    1852507879,
    4275313526,
    2312317920,
    282753626,
    1742555852,
    4189708143,
    2394877945,
    397917763,
    1622183637,
    3604390888,
    2714866558,
    953729732,
    1340076626,
    3518719985,
    2797360999,
    1068828381,
    1219638859,
    3624741850,
    2936675148,
    906185462,
    1090812512,
    3747672003,
    2825379669,
    829329135,
    1181335161,
    3412177804,
    3160834842,
    628085408,
    1382605366,
    3423369109,
    3138078467,
    570562233,
    1426400815,
    3317316542,
    2998733608,
    733239954,
    1555261956,
    3268935591,
    3050360625,
    752459403,
    1541320221,
    2607071920,
    3965973030,
    1969922972,
    40735498,
    2617837225,
    3943577151,
    1913087877,
    83908371,
    2512341634,
    3803740692,
    2075208622,
    213261112,
    2463272603,
    3855990285,
    2094854071,
    198958881,
    2262029012,
    4057260610,
    1759359992,
    534414190,
    2176718541,
    4139329115,
    1873836001,
    414664567,
    2282248934,
    4279200368,
    1711684554,
    285281116,
    2405801727,
    4167216745,
    1634467795,
    376229701,
    2685067896,
    3608007406,
    1308918612,
    956543938,
    2808555105,
    3495958263,
    1231636301,
    1047427035,
    2932959818,
    3654703836,
    1088359270,
    936918e3,
    2847714899,
    3736837829,
    1202900863,
    817233897,
    3183342108,
    3401237130,
    1404277552,
    615818150,
    3134207493,
    3453421203,
    1423857449,
    601450431,
    3009837614,
    3294710456,
    1567103746,
    711928724,
    3020668471,
    3272380065,
    1510334235,
    755167117
  ]);
  function r(s) {
    if (Buffer.isBuffer(s))
      return s;
    if (typeof s == "number")
      return Buffer.alloc(s);
    if (typeof s == "string")
      return Buffer.from(s);
    throw new Error("input must be buffer, number, or string, received " + typeof s);
  }
  function o(s) {
    const f = r(4);
    return f.writeInt32BE(s, 0), f;
  }
  function a(s, f) {
    s = r(s), Buffer.isBuffer(f) && (f = f.readUInt32BE(0));
    let d = ~~f ^ -1;
    for (var y = 0; y < s.length; y++)
      d = e[(d ^ s[y]) & 255] ^ d >>> 8;
    return d ^ -1;
  }
  function n() {
    return o(a.apply(null, arguments));
  }
  n.signed = function() {
    return a.apply(null, arguments);
  }, n.unsigned = function() {
    return a.apply(null, arguments) >>> 0;
  };
  var i = n;
  return Gf = /* @__PURE__ */ t(i), Gf;
}
var Hf, F_;
function sT() {
  if (F_) return Hf;
  F_ = 1;
  var t = It.inherits, e = sS().Transform, r = iT(), o = Tn(), a = function(n) {
    if (!(this instanceof a))
      return new a(n);
    n = this.options = o.defaults(n, {}), e.call(this, n), this.supports = {
      directory: !0,
      symlink: !0
    }, this.files = [];
  };
  return t(a, e), a.prototype._transform = function(n, i, l) {
    l(null, n);
  }, a.prototype._writeStringified = function() {
    var n = JSON.stringify(this.files);
    this.write(n);
  }, a.prototype.append = function(n, i, l) {
    var s = this;
    i.crc32 = 0;
    function f(d, y) {
      if (d) {
        l(d);
        return;
      }
      i.size = y.length || 0, i.crc32 = r.unsigned(y), s.files.push(i), l(null, i);
    }
    i.sourceType === "buffer" ? f(null, n) : i.sourceType === "stream" && o.collectStream(n, f);
  }, a.prototype.finalize = function() {
    this._writeStringified(), this.end();
  }, Hf = a, Hf;
}
var zf, j_;
function oT() {
  if (j_) return zf;
  j_ = 1;
  var t = CA(), e = {}, r = function(o, a) {
    return r.create(o, a);
  };
  return r.create = function(o, a) {
    if (e[o]) {
      var n = new t(o, a);
      return n.setFormat(o), n.setModule(new e[o](a)), n;
    } else
      throw new Error("create(" + o + "): format not registered");
  }, r.registerFormat = function(o, a) {
    if (e[o])
      throw new Error("register(" + o + "): format already registered");
    if (typeof a != "function")
      throw new Error("register(" + o + "): format module invalid");
    if (typeof a.prototype.append != "function" || typeof a.prototype.finalize != "function")
      throw new Error("register(" + o + "): format module missing methods");
    e[o] = a;
  }, r.isRegisteredFormat = function(o) {
    return !!e[o];
  }, r.registerFormat("zip", VA()), r.registerFormat("tar", nT()), r.registerFormat("json", sT()), zf = r, zf;
}
var aT = oT();
const lT = /* @__PURE__ */ Ci(aT);
var wr = { exports: {} }, Vf, $_;
function OS() {
  return $_ || ($_ = 1, Vf = {
    /* The local file header */
    LOCHDR: 30,
    // LOC header size
    LOCSIG: 67324752,
    // "PK\003\004"
    LOCVER: 4,
    // version needed to extract
    LOCFLG: 6,
    // general purpose bit flag
    LOCHOW: 8,
    // compression method
    LOCTIM: 10,
    // modification time (2 bytes time, 2 bytes date)
    LOCCRC: 14,
    // uncompressed file crc-32 value
    LOCSIZ: 18,
    // compressed size
    LOCLEN: 22,
    // uncompressed size
    LOCNAM: 26,
    // filename length
    LOCEXT: 28,
    // extra field length
    /* The Data descriptor */
    EXTSIG: 134695760,
    // "PK\007\008"
    EXTHDR: 16,
    // EXT header size
    EXTCRC: 4,
    // uncompressed file crc-32 value
    EXTSIZ: 8,
    // compressed size
    EXTLEN: 12,
    // uncompressed size
    /* The central directory file header */
    CENHDR: 46,
    // CEN header size
    CENSIG: 33639248,
    // "PK\001\002"
    CENVEM: 4,
    // version made by
    CENVER: 6,
    // version needed to extract
    CENFLG: 8,
    // encrypt, decrypt flags
    CENHOW: 10,
    // compression method
    CENTIM: 12,
    // modification time (2 bytes time, 2 bytes date)
    CENCRC: 16,
    // uncompressed file crc-32 value
    CENSIZ: 20,
    // compressed size
    CENLEN: 24,
    // uncompressed size
    CENNAM: 28,
    // filename length
    CENEXT: 30,
    // extra field length
    CENCOM: 32,
    // file comment length
    CENDSK: 34,
    // volume number start
    CENATT: 36,
    // internal file attributes
    CENATX: 38,
    // external file attributes (host system dependent)
    CENOFF: 42,
    // LOC header offset
    /* The entries in the end of central directory */
    ENDHDR: 22,
    // END header size
    ENDSIG: 101010256,
    // "PK\005\006"
    ENDSUB: 8,
    // number of entries on this disk
    ENDTOT: 10,
    // total number of entries
    ENDSIZ: 12,
    // central directory size in bytes
    ENDOFF: 16,
    // offset of first CEN header
    ENDCOM: 20,
    // zip file comment length
    END64HDR: 20,
    // zip64 END header size
    END64SIG: 117853008,
    // zip64 Locator signature, "PK\006\007"
    END64START: 4,
    // number of the disk with the start of the zip64
    END64OFF: 8,
    // relative offset of the zip64 end of central directory
    END64NUMDISKS: 16,
    // total number of disks
    ZIP64SIG: 101075792,
    // zip64 signature, "PK\006\006"
    ZIP64HDR: 56,
    // zip64 record minimum size
    ZIP64LEAD: 12,
    // leading bytes at the start of the record, not counted by the value stored in ZIP64SIZE
    ZIP64SIZE: 4,
    // zip64 size of the central directory record
    ZIP64VEM: 12,
    // zip64 version made by
    ZIP64VER: 14,
    // zip64 version needed to extract
    ZIP64DSK: 16,
    // zip64 number of this disk
    ZIP64DSKDIR: 20,
    // number of the disk with the start of the record directory
    ZIP64SUB: 24,
    // number of entries on this disk
    ZIP64TOT: 32,
    // total number of entries
    ZIP64SIZB: 40,
    // zip64 central directory size in bytes
    ZIP64OFF: 48,
    // offset of start of central directory with respect to the starting disk number
    ZIP64EXTRA: 56,
    // extensible data sector
    /* Compression methods */
    STORED: 0,
    // no compression
    SHRUNK: 1,
    // shrunk
    REDUCED1: 2,
    // reduced with compression factor 1
    REDUCED2: 3,
    // reduced with compression factor 2
    REDUCED3: 4,
    // reduced with compression factor 3
    REDUCED4: 5,
    // reduced with compression factor 4
    IMPLODED: 6,
    // imploded
    // 7 reserved for Tokenizing compression algorithm
    DEFLATED: 8,
    // deflated
    ENHANCED_DEFLATED: 9,
    // enhanced deflated
    PKWARE: 10,
    // PKWare DCL imploded
    // 11 reserved by PKWARE
    BZIP2: 12,
    //  compressed using BZIP2
    // 13 reserved by PKWARE
    LZMA: 14,
    // LZMA
    // 15-17 reserved by PKWARE
    IBM_TERSE: 18,
    // compressed using IBM TERSE
    IBM_LZ77: 19,
    // IBM LZ77 z
    AES_ENCRYPT: 99,
    // WinZIP AES encryption method
    /* General purpose bit flag */
    // values can obtained with expression 2**bitnr
    FLG_ENC: 1,
    // Bit 0: encrypted file
    FLG_COMP1: 2,
    // Bit 1, compression option
    FLG_COMP2: 4,
    // Bit 2, compression option
    FLG_DESC: 8,
    // Bit 3, data descriptor
    FLG_ENH: 16,
    // Bit 4, enhanced deflating
    FLG_PATCH: 32,
    // Bit 5, indicates that the file is compressed patched data.
    FLG_STR: 64,
    // Bit 6, strong encryption (patented)
    // Bits 7-10: Currently unused.
    FLG_EFS: 2048,
    // Bit 11: Language encoding flag (EFS)
    // Bit 12: Reserved by PKWARE for enhanced compression.
    // Bit 13: encrypted the Central Directory (patented).
    // Bits 14-15: Reserved by PKWARE.
    FLG_MSK: 4096,
    // mask header values
    /* Load type */
    FILE: 2,
    BUFFER: 1,
    NONE: 0,
    /* 4.5 Extensible data fields */
    EF_ID: 0,
    EF_SIZE: 2,
    /* Header IDs */
    ID_ZIP64: 1,
    ID_AVINFO: 7,
    ID_PFS: 8,
    ID_OS2: 9,
    ID_NTFS: 10,
    ID_OPENVMS: 12,
    ID_UNIX: 13,
    ID_FORK: 14,
    ID_PATCH: 15,
    ID_X509_PKCS7: 20,
    ID_X509_CERTID_F: 21,
    ID_X509_CERTID_C: 22,
    ID_STRONGENC: 23,
    ID_RECORD_MGT: 24,
    ID_X509_PKCS7_RL: 25,
    ID_IBM1: 101,
    ID_IBM2: 102,
    ID_POSZIP: 18064,
    EF_ZIP64_OR_32: 4294967295,
    EF_ZIP64_OR_16: 65535,
    EF_ZIP64_SUNCOMP: 0,
    EF_ZIP64_SCOMP: 8,
    EF_ZIP64_RHO: 16,
    EF_ZIP64_DSN: 24
  }), Vf;
}
var Zf = {}, W_;
function Vc() {
  return W_ || (W_ = 1, (function(t) {
    const e = {
      /* Header error messages */
      INVALID_LOC: "Invalid LOC header (bad signature)",
      INVALID_CEN: "Invalid CEN header (bad signature)",
      INVALID_END: "Invalid END header (bad signature)",
      /* Descriptor */
      DESCRIPTOR_NOT_EXIST: "No descriptor present",
      DESCRIPTOR_UNKNOWN: "Unknown descriptor format",
      DESCRIPTOR_FAULTY: "Descriptor data is malformed",
      /* ZipEntry error messages*/
      NO_DATA: "Nothing to decompress",
      BAD_CRC: "CRC32 checksum failed {0}",
      FILE_IN_THE_WAY: "There is a file in the way: {0}",
      UNKNOWN_METHOD: "Invalid/unsupported compression method",
      /* Inflater error messages */
      AVAIL_DATA: "inflate::Available inflate data did not terminate",
      INVALID_DISTANCE: "inflate::Invalid literal/length or distance code in fixed or dynamic block",
      TO_MANY_CODES: "inflate::Dynamic block code description: too many length or distance codes",
      INVALID_REPEAT_LEN: "inflate::Dynamic block code description: repeat more than specified lengths",
      INVALID_REPEAT_FIRST: "inflate::Dynamic block code description: repeat lengths with no first length",
      INCOMPLETE_CODES: "inflate::Dynamic block code description: code lengths codes incomplete",
      INVALID_DYN_DISTANCE: "inflate::Dynamic block code description: invalid distance code lengths",
      INVALID_CODES_LEN: "inflate::Dynamic block code description: invalid literal/length code lengths",
      INVALID_STORE_BLOCK: "inflate::Stored block length did not match one's complement",
      INVALID_BLOCK_TYPE: "inflate::Invalid block type (type == 3)",
      /* ADM-ZIP error messages */
      CANT_EXTRACT_FILE: "Could not extract the file",
      CANT_OVERRIDE: "Target file already exists",
      DISK_ENTRY_TOO_LARGE: "Number of disk entries is too large",
      NO_ZIP: "No zip file was loaded",
      NO_ENTRY: "Entry doesn't exist",
      DIRECTORY_CONTENT_ERROR: "A directory cannot have content",
      FILE_NOT_FOUND: 'File not found: "{0}"',
      NOT_IMPLEMENTED: "Not implemented",
      INVALID_FILENAME: "Invalid filename",
      INVALID_FORMAT: "Invalid or unsupported zip format. No END header found",
      INVALID_PASS_PARAM: "Incompatible password parameter",
      WRONG_PASSWORD: "Wrong Password",
      /* ADM-ZIP */
      COMMENT_TOO_LONG: "Comment is too long",
      // Comment can be max 65535 bytes long (NOTE: some non-US characters may take more space)
      EXTRA_FIELD_PARSE_ERROR: "Extra field parsing error"
    };
    function r(o) {
      return function(...a) {
        return a.length && (o = o.replace(/\{(\d)\}/g, (n, i) => a[i] || "")), new Error("ADM-ZIP: " + o);
      };
    }
    for (const o of Object.keys(e))
      t[o] = r(e[o]);
  })(Zf)), Zf;
}
var Yf, q_;
function uT() {
  if (q_) return Yf;
  q_ = 1;
  const t = Lt, e = Ue, r = OS(), o = Vc(), a = typeof process == "object" && process.platform === "win32", n = (s) => typeof s == "object" && s !== null, i = new Uint32Array(256).map((s, f) => {
    for (let d = 0; d < 8; d++)
      (f & 1) !== 0 ? f = 3988292384 ^ f >>> 1 : f >>>= 1;
    return f >>> 0;
  });
  function l(s) {
    this.sep = e.sep, this.fs = t, n(s) && n(s.fs) && typeof s.fs.statSync == "function" && (this.fs = s.fs);
  }
  return Yf = l, l.prototype.makeDir = function(s) {
    const f = this;
    function d(y) {
      let R = y.split(f.sep)[0];
      y.split(f.sep).forEach(function(w) {
        if (!(!w || w.substr(-1, 1) === ":")) {
          R += f.sep + w;
          var I;
          try {
            I = f.fs.statSync(R);
          } catch (A) {
            if (A.message && A.message.startsWith("ENOENT"))
              f.fs.mkdirSync(R);
            else
              throw A;
          }
          if (I && I.isFile()) throw o.FILE_IN_THE_WAY(`"${R}"`);
        }
      });
    }
    d(s);
  }, l.prototype.writeFileTo = function(s, f, d, y) {
    const R = this;
    if (R.fs.existsSync(s)) {
      if (!d) return !1;
      var w = R.fs.statSync(s);
      if (w.isDirectory())
        return !1;
    }
    var I = e.dirname(s);
    R.fs.existsSync(I) || R.makeDir(I);
    var A;
    try {
      A = R.fs.openSync(s, "w", 438);
    } catch {
      R.fs.chmodSync(s, 438), A = R.fs.openSync(s, "w", 438);
    }
    if (A)
      try {
        R.fs.writeSync(A, f, 0, f.length, 0);
      } finally {
        R.fs.closeSync(A);
      }
    return R.fs.chmodSync(s, y || 438), !0;
  }, l.prototype.writeFileToAsync = function(s, f, d, y, R) {
    typeof y == "function" && (R = y, y = void 0);
    const w = this;
    w.fs.exists(s, function(I) {
      if (I && !d) return R(!1);
      w.fs.stat(s, function(A, u) {
        if (I && u.isDirectory())
          return R(!1);
        var g = e.dirname(s);
        w.fs.exists(g, function(p) {
          p || w.makeDir(g), w.fs.open(s, "w", 438, function(b, C) {
            b ? w.fs.chmod(s, 438, function() {
              w.fs.open(s, "w", 438, function(N, W) {
                w.fs.write(W, f, 0, f.length, 0, function() {
                  w.fs.close(W, function() {
                    w.fs.chmod(s, y || 438, function() {
                      R(!0);
                    });
                  });
                });
              });
            }) : C ? w.fs.write(C, f, 0, f.length, 0, function() {
              w.fs.close(C, function() {
                w.fs.chmod(s, y || 438, function() {
                  R(!0);
                });
              });
            }) : w.fs.chmod(s, y || 438, function() {
              R(!0);
            });
          });
        });
      });
    });
  }, l.prototype.findFiles = function(s) {
    const f = this;
    function d(y, R, w) {
      let I = [];
      return f.fs.readdirSync(y).forEach(function(A) {
        const u = e.join(y, A), g = f.fs.statSync(u);
        I.push(e.normalize(u) + (g.isDirectory() ? f.sep : "")), g.isDirectory() && w && (I = I.concat(d(u, R, w)));
      }), I;
    }
    return d(s, void 0, !0);
  }, l.prototype.findFilesAsync = function(s, f) {
    const d = this;
    let y = [];
    d.fs.readdir(s, function(R, w) {
      if (R) return f(R);
      let I = w.length;
      if (!I) return f(null, y);
      w.forEach(function(A) {
        A = e.join(s, A), d.fs.stat(A, function(u, g) {
          if (u) return f(u);
          g && (y.push(e.normalize(A) + (g.isDirectory() ? d.sep : "")), g.isDirectory() ? d.findFilesAsync(A, function(p, b) {
            if (p) return f(p);
            y = y.concat(b), --I || f(null, y);
          }) : --I || f(null, y));
        });
      });
    });
  }, l.prototype.getAttributes = function() {
  }, l.prototype.setAttributes = function() {
  }, l.crc32update = function(s, f) {
    return i[(s ^ f) & 255] ^ s >>> 8;
  }, l.crc32 = function(s) {
    typeof s == "string" && (s = Buffer.from(s, "utf8"));
    let f = s.length, d = -1;
    for (let y = 0; y < f; ) d = l.crc32update(d, s[y++]);
    return ~d >>> 0;
  }, l.methodToString = function(s) {
    switch (s) {
      case r.STORED:
        return "STORED (" + s + ")";
      case r.DEFLATED:
        return "DEFLATED (" + s + ")";
      default:
        return "UNSUPPORTED (" + s + ")";
    }
  }, l.canonical = function(s) {
    if (!s) return "";
    const f = e.posix.normalize("/" + s.split("\\").join("/"));
    return e.join(".", f);
  }, l.zipnamefix = function(s) {
    if (!s) return "";
    const f = e.posix.normalize("/" + s.split("\\").join("/"));
    return e.posix.join(".", f);
  }, l.findLast = function(s, f) {
    if (!Array.isArray(s)) throw new TypeError("arr is not array");
    const d = s.length >>> 0;
    for (let y = d - 1; y >= 0; y--)
      if (f(s[y], y, s))
        return s[y];
  }, l.sanitize = function(s, f) {
    s = e.resolve(e.normalize(s));
    for (var d = f.split("/"), y = 0, R = d.length; y < R; y++) {
      var w = e.normalize(e.join(s, d.slice(y, R).join(e.sep)));
      if (w.indexOf(s) === 0)
        return w;
    }
    return e.normalize(e.join(s, e.basename(f)));
  }, l.toBuffer = function(f, d) {
    return Buffer.isBuffer(f) ? f : f instanceof Uint8Array ? Buffer.from(f) : typeof f == "string" ? d(f) : Buffer.alloc(0);
  }, l.readBigUInt64LE = function(s, f) {
    const d = s.readUInt32LE(f);
    return s.readUInt32LE(f + 4) * 4294967296 + d;
  }, l.fromDOS2Date = function(s) {
    return new Date((s >> 25 & 127) + 1980, Math.max((s >> 21 & 15) - 1, 0), Math.max(s >> 16 & 31, 1), s >> 11 & 31, s >> 5 & 63, (s & 31) << 1);
  }, l.fromDate2DOS = function(s) {
    let f = 0, d = 0;
    return s.getFullYear() > 1979 && (f = (s.getFullYear() - 1980 & 127) << 9 | s.getMonth() + 1 << 5 | s.getDate(), d = s.getHours() << 11 | s.getMinutes() << 5 | s.getSeconds() >> 1), f << 16 | d;
  }, l.isWin = a, l.crcTable = i, Yf;
}
var Kf, B_;
function fT() {
  if (B_) return Kf;
  B_ = 1;
  const t = Ue;
  return Kf = function(e, { fs: r }) {
    var o = e || "", a = i(), n = null;
    function i() {
      return {
        directory: !1,
        readonly: !1,
        hidden: !1,
        executable: !1,
        mtime: 0,
        atime: 0
      };
    }
    return o && r.existsSync(o) ? (n = r.statSync(o), a.directory = n.isDirectory(), a.mtime = n.mtime, a.atime = n.atime, a.executable = (73 & n.mode) !== 0, a.readonly = (128 & n.mode) === 0, a.hidden = t.basename(o)[0] === ".") : console.warn("Invalid path: " + o), {
      get directory() {
        return a.directory;
      },
      get readOnly() {
        return a.readonly;
      },
      get hidden() {
        return a.hidden;
      },
      get mtime() {
        return a.mtime;
      },
      get atime() {
        return a.atime;
      },
      get executable() {
        return a.executable;
      },
      decodeAttributes: function() {
      },
      encodeAttributes: function() {
      },
      toJSON: function() {
        return {
          path: o,
          isDirectory: a.directory,
          isReadOnly: a.readonly,
          isHidden: a.hidden,
          isExecutable: a.executable,
          mTime: a.mtime,
          aTime: a.atime
        };
      },
      toString: function() {
        return JSON.stringify(this.toJSON(), null, "	");
      }
    };
  }, Kf;
}
var Jf, U_;
function cT() {
  return U_ || (U_ = 1, Jf = {
    efs: !0,
    encode: (t) => Buffer.from(t, "utf8"),
    decode: (t) => t.toString("utf8")
  }), Jf;
}
var G_;
function Nn() {
  return G_ || (G_ = 1, wr.exports = uT(), wr.exports.Constants = OS(), wr.exports.Errors = Vc(), wr.exports.FileAttr = fT(), wr.exports.decoder = cT()), wr.exports;
}
var ti = {}, Xf, H_;
function dT() {
  if (H_) return Xf;
  H_ = 1;
  var t = Nn(), e = t.Constants;
  return Xf = function() {
    var r = 20, o = 10, a = 0, n = 0, i = 0, l = 0, s = 0, f = 0, d = 0, y = 0, R = 0, w = 0, I = 0, A = 0, u = 0;
    r |= t.isWin ? 2560 : 768, a |= e.FLG_EFS;
    const g = {
      extraLen: 0
    }, p = (C) => Math.max(0, C) >>> 0, b = (C) => Math.max(0, C) & 255;
    return i = t.fromDate2DOS(/* @__PURE__ */ new Date()), {
      get made() {
        return r;
      },
      set made(C) {
        r = C;
      },
      get version() {
        return o;
      },
      set version(C) {
        o = C;
      },
      get flags() {
        return a;
      },
      set flags(C) {
        a = C;
      },
      get flags_efs() {
        return (a & e.FLG_EFS) > 0;
      },
      set flags_efs(C) {
        C ? a |= e.FLG_EFS : a &= ~e.FLG_EFS;
      },
      get flags_desc() {
        return (a & e.FLG_DESC) > 0;
      },
      set flags_desc(C) {
        C ? a |= e.FLG_DESC : a &= ~e.FLG_DESC;
      },
      get method() {
        return n;
      },
      set method(C) {
        switch (C) {
          case e.STORED:
            this.version = 10;
          case e.DEFLATED:
          default:
            this.version = 20;
        }
        n = C;
      },
      get time() {
        return t.fromDOS2Date(this.timeval);
      },
      set time(C) {
        C = new Date(C), this.timeval = t.fromDate2DOS(C);
      },
      get timeval() {
        return i;
      },
      set timeval(C) {
        i = p(C);
      },
      get timeHighByte() {
        return b(i >>> 8);
      },
      get crc() {
        return l;
      },
      set crc(C) {
        l = p(C);
      },
      get compressedSize() {
        return s;
      },
      set compressedSize(C) {
        s = p(C);
      },
      get size() {
        return f;
      },
      set size(C) {
        f = p(C);
      },
      get fileNameLength() {
        return d;
      },
      set fileNameLength(C) {
        d = C;
      },
      get extraLength() {
        return y;
      },
      set extraLength(C) {
        y = C;
      },
      get extraLocalLength() {
        return g.extraLen;
      },
      set extraLocalLength(C) {
        g.extraLen = C;
      },
      get commentLength() {
        return R;
      },
      set commentLength(C) {
        R = C;
      },
      get diskNumStart() {
        return w;
      },
      set diskNumStart(C) {
        w = p(C);
      },
      get inAttr() {
        return I;
      },
      set inAttr(C) {
        I = p(C);
      },
      get attr() {
        return A;
      },
      set attr(C) {
        A = p(C);
      },
      // get Unix file permissions
      get fileAttr() {
        return (A || 0) >> 16 & 4095;
      },
      get offset() {
        return u;
      },
      set offset(C) {
        u = p(C);
      },
      get encrypted() {
        return (a & e.FLG_ENC) === e.FLG_ENC;
      },
      get centralHeaderSize() {
        return e.CENHDR + d + y + R;
      },
      get realDataOffset() {
        return u + e.LOCHDR + g.fnameLen + g.extraLen;
      },
      get localHeader() {
        return g;
      },
      loadLocalHeaderFromBinary: function(C) {
        var N = C.slice(u, u + e.LOCHDR);
        if (N.readUInt32LE(0) !== e.LOCSIG)
          throw t.Errors.INVALID_LOC();
        g.version = N.readUInt16LE(e.LOCVER), g.flags = N.readUInt16LE(e.LOCFLG), g.flags_desc = (g.flags & e.FLG_DESC) > 0, g.method = N.readUInt16LE(e.LOCHOW), g.time = N.readUInt32LE(e.LOCTIM), g.crc = N.readUInt32LE(e.LOCCRC), g.compressedSize = N.readUInt32LE(e.LOCSIZ), g.size = N.readUInt32LE(e.LOCLEN), g.fnameLen = N.readUInt16LE(e.LOCNAM), g.extraLen = N.readUInt16LE(e.LOCEXT);
        const W = u + e.LOCHDR + g.fnameLen, k = W + g.extraLen;
        return C.slice(W, k);
      },
      loadFromBinary: function(C) {
        if (C.length !== e.CENHDR || C.readUInt32LE(0) !== e.CENSIG)
          throw t.Errors.INVALID_CEN();
        r = C.readUInt16LE(e.CENVEM), o = C.readUInt16LE(e.CENVER), a = C.readUInt16LE(e.CENFLG), n = C.readUInt16LE(e.CENHOW), i = C.readUInt32LE(e.CENTIM), l = C.readUInt32LE(e.CENCRC), s = C.readUInt32LE(e.CENSIZ), f = C.readUInt32LE(e.CENLEN), d = C.readUInt16LE(e.CENNAM), y = C.readUInt16LE(e.CENEXT), R = C.readUInt16LE(e.CENCOM), w = C.readUInt16LE(e.CENDSK), I = C.readUInt16LE(e.CENATT), A = C.readUInt32LE(e.CENATX), u = C.readUInt32LE(e.CENOFF);
      },
      localHeaderToBinary: function() {
        var C = Buffer.alloc(e.LOCHDR);
        return C.writeUInt32LE(e.LOCSIG, 0), C.writeUInt16LE(o, e.LOCVER), C.writeUInt16LE(a, e.LOCFLG), C.writeUInt16LE(n, e.LOCHOW), C.writeUInt32LE(i, e.LOCTIM), C.writeUInt32LE(l, e.LOCCRC), C.writeUInt32LE(s, e.LOCSIZ), C.writeUInt32LE(f, e.LOCLEN), C.writeUInt16LE(d, e.LOCNAM), C.writeUInt16LE(g.extraLen, e.LOCEXT), C;
      },
      centralHeaderToBinary: function() {
        var C = Buffer.alloc(e.CENHDR + d + y + R);
        return C.writeUInt32LE(e.CENSIG, 0), C.writeUInt16LE(r, e.CENVEM), C.writeUInt16LE(o, e.CENVER), C.writeUInt16LE(a, e.CENFLG), C.writeUInt16LE(n, e.CENHOW), C.writeUInt32LE(i, e.CENTIM), C.writeUInt32LE(l, e.CENCRC), C.writeUInt32LE(s, e.CENSIZ), C.writeUInt32LE(f, e.CENLEN), C.writeUInt16LE(d, e.CENNAM), C.writeUInt16LE(y, e.CENEXT), C.writeUInt16LE(R, e.CENCOM), C.writeUInt16LE(w, e.CENDSK), C.writeUInt16LE(I, e.CENATT), C.writeUInt32LE(A, e.CENATX), C.writeUInt32LE(u, e.CENOFF), C;
      },
      toJSON: function() {
        const C = function(N) {
          return N + " bytes";
        };
        return {
          made: r,
          version: o,
          flags: a,
          method: t.methodToString(n),
          time: this.time,
          crc: "0x" + l.toString(16).toUpperCase(),
          compressedSize: C(s),
          size: C(f),
          fileNameLength: C(d),
          extraLength: C(y),
          commentLength: C(R),
          diskNumStart: w,
          inAttr: I,
          attr: A,
          offset: u,
          centralHeaderSize: C(e.CENHDR + d + y + R)
        };
      },
      toString: function() {
        return JSON.stringify(this.toJSON(), null, "	");
      }
    };
  }, Xf;
}
var Qf, z_;
function hT() {
  if (z_) return Qf;
  z_ = 1;
  var t = Nn(), e = t.Constants;
  return Qf = function() {
    var r = 0, o = 0, a = 0, n = 0, i = 0;
    return {
      get diskEntries() {
        return r;
      },
      set diskEntries(l) {
        r = o = l;
      },
      get totalEntries() {
        return o;
      },
      set totalEntries(l) {
        o = r = l;
      },
      get size() {
        return a;
      },
      set size(l) {
        a = l;
      },
      get offset() {
        return n;
      },
      set offset(l) {
        n = l;
      },
      get commentLength() {
        return i;
      },
      set commentLength(l) {
        i = l;
      },
      get mainHeaderSize() {
        return e.ENDHDR + i;
      },
      loadFromBinary: function(l) {
        if ((l.length !== e.ENDHDR || l.readUInt32LE(0) !== e.ENDSIG) && (l.length < e.ZIP64HDR || l.readUInt32LE(0) !== e.ZIP64SIG))
          throw t.Errors.INVALID_END();
        l.readUInt32LE(0) === e.ENDSIG ? (r = l.readUInt16LE(e.ENDSUB), o = l.readUInt16LE(e.ENDTOT), a = l.readUInt32LE(e.ENDSIZ), n = l.readUInt32LE(e.ENDOFF), i = l.readUInt16LE(e.ENDCOM)) : (r = t.readBigUInt64LE(l, e.ZIP64SUB), o = t.readBigUInt64LE(l, e.ZIP64TOT), a = t.readBigUInt64LE(l, e.ZIP64SIZE), n = t.readBigUInt64LE(l, e.ZIP64OFF), i = 0);
      },
      toBinary: function() {
        var l = Buffer.alloc(e.ENDHDR + i);
        return l.writeUInt32LE(e.ENDSIG, 0), l.writeUInt32LE(0, 4), l.writeUInt16LE(r, e.ENDSUB), l.writeUInt16LE(o, e.ENDTOT), l.writeUInt32LE(a, e.ENDSIZ), l.writeUInt32LE(n, e.ENDOFF), l.writeUInt16LE(i, e.ENDCOM), l.fill(" ", e.ENDHDR), l;
      },
      toJSON: function() {
        const l = function(s, f) {
          let d = s.toString(16).toUpperCase();
          for (; d.length < f; ) d = "0" + d;
          return "0x" + d;
        };
        return {
          diskEntries: r,
          totalEntries: o,
          size: a + " bytes",
          offset: l(n, 4),
          commentLength: i
        };
      },
      toString: function() {
        return JSON.stringify(this.toJSON(), null, "	");
      }
    };
  }, Qf;
}
var V_;
function NS() {
  return V_ || (V_ = 1, ti.EntryHeader = dT(), ti.MainHeader = hT()), ti;
}
var on = {}, ec, Z_;
function pT() {
  return Z_ || (Z_ = 1, ec = function(t) {
    var e = Pi, r = { chunkSize: (parseInt(t.length / 1024) + 1) * 1024 };
    return {
      deflate: function() {
        return e.deflateRawSync(t, r);
      },
      deflateAsync: function(o) {
        var a = e.createDeflateRaw(r), n = [], i = 0;
        a.on("data", function(l) {
          n.push(l), i += l.length;
        }), a.on("end", function() {
          var l = Buffer.alloc(i), s = 0;
          l.fill(0);
          for (var f = 0; f < n.length; f++) {
            var d = n[f];
            d.copy(l, s), s += d.length;
          }
          o && o(l);
        }), a.end(t);
      }
    };
  }), ec;
}
var tc, Y_;
function gT() {
  if (Y_) return tc;
  Y_ = 1;
  const t = +(process.versions ? process.versions.node : "").split(".")[0] || 0;
  return tc = function(e, r) {
    var o = Pi;
    const a = t >= 15 && r > 0 ? { maxOutputLength: r } : {};
    return {
      inflate: function() {
        return o.inflateRawSync(e, a);
      },
      inflateAsync: function(n) {
        var i = o.createInflateRaw(a), l = [], s = 0;
        i.on("data", function(f) {
          l.push(f), s += f.length;
        }), i.on("end", function() {
          var f = Buffer.alloc(s), d = 0;
          f.fill(0);
          for (var y = 0; y < l.length; y++) {
            var R = l[y];
            R.copy(f, d), d += R.length;
          }
          n && n(f);
        }), i.end(e);
      }
    };
  }, tc;
}
var rc, K_;
function yT() {
  if (K_) return rc;
  K_ = 1;
  const { randomFillSync: t } = EE, e = Vc(), r = new Uint32Array(256).map((w, I) => {
    for (let A = 0; A < 8; A++)
      (I & 1) !== 0 ? I = I >>> 1 ^ 3988292384 : I >>>= 1;
    return I >>> 0;
  }), o = (w, I) => Math.imul(w, I) >>> 0, a = (w, I) => r[(w ^ I) & 255] ^ w >>> 8, n = () => typeof t == "function" ? t(Buffer.alloc(12)) : n.node();
  n.node = () => {
    const w = Buffer.alloc(12), I = w.length;
    for (let A = 0; A < I; A++) w[A] = Math.random() * 256 & 255;
    return w;
  };
  const i = {
    genSalt: n
  };
  function l(w) {
    const I = Buffer.isBuffer(w) ? w : Buffer.from(w);
    this.keys = new Uint32Array([305419896, 591751049, 878082192]);
    for (let A = 0; A < I.length; A++)
      this.updateKeys(I[A]);
  }
  l.prototype.updateKeys = function(w) {
    const I = this.keys;
    return I[0] = a(I[0], w), I[1] += I[0] & 255, I[1] = o(I[1], 134775813) + 1, I[2] = a(I[2], I[1] >>> 24), w;
  }, l.prototype.next = function() {
    const w = (this.keys[2] | 2) >>> 0;
    return o(w, w ^ 1) >> 8 & 255;
  };
  function s(w) {
    const I = new l(w);
    return function(A) {
      const u = Buffer.alloc(A.length);
      let g = 0;
      for (let p of A)
        u[g++] = I.updateKeys(p ^ I.next());
      return u;
    };
  }
  function f(w) {
    const I = new l(w);
    return function(A, u, g = 0) {
      u || (u = Buffer.alloc(A.length));
      for (let p of A) {
        const b = I.next();
        u[g++] = p ^ b, I.updateKeys(p);
      }
      return u;
    };
  }
  function d(w, I, A) {
    if (!w || !Buffer.isBuffer(w) || w.length < 12)
      return Buffer.alloc(0);
    const u = s(A), g = u(w.slice(0, 12)), p = (I.flags & 8) === 8 ? I.timeHighByte : I.crc >>> 24;
    if (g[11] !== p)
      throw e.WRONG_PASSWORD();
    return u(w.slice(12));
  }
  function y(w) {
    Buffer.isBuffer(w) && w.length >= 12 ? i.genSalt = function() {
      return w.slice(0, 12);
    } : w === "node" ? i.genSalt = n.node : i.genSalt = n;
  }
  function R(w, I, A, u = !1) {
    w == null && (w = Buffer.alloc(0)), Buffer.isBuffer(w) || (w = Buffer.from(w.toString()));
    const g = f(A), p = i.genSalt();
    p[11] = I.crc >>> 24 & 255, u && (p[10] = I.crc >>> 16 & 255);
    const b = Buffer.alloc(w.length + 12);
    return g(p, b), g(w, b, 12);
  }
  return rc = { decrypt: d, encrypt: R, _salter: y }, rc;
}
var J_;
function bT() {
  return J_ || (J_ = 1, on.Deflater = pT(), on.Inflater = gT(), on.ZipCrypto = yT()), on;
}
var nc, X_;
function LS() {
  if (X_) return nc;
  X_ = 1;
  var t = Nn(), e = NS(), r = t.Constants, o = bT();
  return nc = function(a, n) {
    var i = new e.EntryHeader(), l = Buffer.alloc(0), s = Buffer.alloc(0), f = !1, d = null, y = Buffer.alloc(0), R = Buffer.alloc(0), w = !0;
    const I = a, A = typeof I.decoder == "object" ? I.decoder : t.decoder;
    w = A.hasOwnProperty("efs") ? A.efs : !1;
    function u() {
      return !n || !(n instanceof Uint8Array) ? Buffer.alloc(0) : (R = i.loadLocalHeaderFromBinary(n), n.slice(i.realDataOffset, i.realDataOffset + i.compressedSize));
    }
    function g(k) {
      if (!i.flags_desc && !i.localHeader.flags_desc) {
        if (t.crc32(k) !== i.localHeader.crc)
          return !1;
      } else {
        const L = {}, U = i.realDataOffset + i.compressedSize;
        if (n.readUInt32LE(U) == r.LOCSIG || n.readUInt32LE(U) == r.CENSIG)
          throw t.Errors.DESCRIPTOR_NOT_EXIST();
        if (n.readUInt32LE(U) == r.EXTSIG)
          L.crc = n.readUInt32LE(U + r.EXTCRC), L.compressedSize = n.readUInt32LE(U + r.EXTSIZ), L.size = n.readUInt32LE(U + r.EXTLEN);
        else if (n.readUInt16LE(U + 12) === 19280)
          L.crc = n.readUInt32LE(U + r.EXTCRC - 4), L.compressedSize = n.readUInt32LE(U + r.EXTSIZ - 4), L.size = n.readUInt32LE(U + r.EXTLEN - 4);
        else
          throw t.Errors.DESCRIPTOR_UNKNOWN();
        if (L.compressedSize !== i.compressedSize || L.size !== i.size || L.crc !== i.crc)
          throw t.Errors.DESCRIPTOR_FAULTY();
        if (t.crc32(k) !== L.crc)
          return !1;
      }
      return !0;
    }
    function p(k, L, U) {
      if (typeof L > "u" && typeof k == "string" && (U = k, k = void 0), f)
        return k && L && L(Buffer.alloc(0), t.Errors.DIRECTORY_CONTENT_ERROR()), Buffer.alloc(0);
      var x = u();
      if (x.length === 0)
        return k && L && L(x), x;
      if (i.encrypted) {
        if (typeof U != "string" && !Buffer.isBuffer(U))
          throw t.Errors.INVALID_PASS_PARAM();
        x = o.ZipCrypto.decrypt(x, i, U);
      }
      var D = Buffer.alloc(i.size);
      switch (i.method) {
        case t.Constants.STORED:
          if (x.copy(D), g(D))
            return k && L && L(D), D;
          throw k && L && L(D, t.Errors.BAD_CRC()), t.Errors.BAD_CRC();
        case t.Constants.DEFLATED:
          var F = new o.Inflater(x, i.size);
          if (k)
            F.inflateAsync(function(T) {
              T.copy(T, 0), L && (g(T) ? L(T) : L(T, t.Errors.BAD_CRC()));
            });
          else {
            if (F.inflate(D).copy(D, 0), !g(D))
              throw t.Errors.BAD_CRC(`"${A.decode(l)}"`);
            return D;
          }
          break;
        default:
          throw k && L && L(Buffer.alloc(0), t.Errors.UNKNOWN_METHOD()), t.Errors.UNKNOWN_METHOD();
      }
    }
    function b(k, L) {
      if ((!d || !d.length) && Buffer.isBuffer(n))
        return k && L && L(u()), u();
      if (d.length && !f) {
        var U;
        switch (i.method) {
          case t.Constants.STORED:
            return i.compressedSize = i.size, U = Buffer.alloc(d.length), d.copy(U), k && L && L(U), U;
          default:
          case t.Constants.DEFLATED:
            var x = new o.Deflater(d);
            if (k)
              x.deflateAsync(function(F) {
                U = Buffer.alloc(F.length), i.compressedSize = F.length, F.copy(U), L && L(U);
              });
            else {
              var D = x.deflate();
              return i.compressedSize = D.length, D;
            }
            x = null;
            break;
        }
      } else if (k && L)
        L(Buffer.alloc(0));
      else
        return Buffer.alloc(0);
    }
    function C(k, L) {
      return t.readBigUInt64LE(k, L);
    }
    function N(k) {
      try {
        for (var L = 0, U, x, D; L + 4 < k.length; )
          U = k.readUInt16LE(L), L += 2, x = k.readUInt16LE(L), L += 2, D = k.slice(L, L + x), L += x, r.ID_ZIP64 === U && W(D);
      } catch {
        throw t.Errors.EXTRA_FIELD_PARSE_ERROR();
      }
    }
    function W(k) {
      var L, U, x, D;
      k.length >= r.EF_ZIP64_SCOMP && (L = C(k, r.EF_ZIP64_SUNCOMP), i.size === r.EF_ZIP64_OR_32 && (i.size = L)), k.length >= r.EF_ZIP64_RHO && (U = C(k, r.EF_ZIP64_SCOMP), i.compressedSize === r.EF_ZIP64_OR_32 && (i.compressedSize = U)), k.length >= r.EF_ZIP64_DSN && (x = C(k, r.EF_ZIP64_RHO), i.offset === r.EF_ZIP64_OR_32 && (i.offset = x)), k.length >= r.EF_ZIP64_DSN + 4 && (D = k.readUInt32LE(r.EF_ZIP64_DSN), i.diskNumStart === r.EF_ZIP64_OR_16 && (i.diskNumStart = D));
    }
    return {
      get entryName() {
        return A.decode(l);
      },
      get rawEntryName() {
        return l;
      },
      set entryName(k) {
        l = t.toBuffer(k, A.encode);
        var L = l[l.length - 1];
        f = L === 47 || L === 92, i.fileNameLength = l.length;
      },
      get efs() {
        return typeof w == "function" ? w(this.entryName) : w;
      },
      get extra() {
        return y;
      },
      set extra(k) {
        y = k, i.extraLength = k.length, N(k);
      },
      get comment() {
        return A.decode(s);
      },
      set comment(k) {
        if (s = t.toBuffer(k, A.encode), i.commentLength = s.length, s.length > 65535) throw t.Errors.COMMENT_TOO_LONG();
      },
      get name() {
        var k = A.decode(l);
        return f ? k.substr(k.length - 1).split("/").pop() : k.split("/").pop();
      },
      get isDirectory() {
        return f;
      },
      getCompressedData: function() {
        return b(!1, null);
      },
      getCompressedDataAsync: function(k) {
        b(!0, k);
      },
      setData: function(k) {
        d = t.toBuffer(k, t.decoder.encode), !f && d.length ? (i.size = d.length, i.method = t.Constants.DEFLATED, i.crc = t.crc32(k), i.changed = !0) : i.method = t.Constants.STORED;
      },
      getData: function(k) {
        return i.changed ? d : p(!1, null, k);
      },
      getDataAsync: function(k, L) {
        i.changed ? k(d) : p(!0, k, L);
      },
      set attr(k) {
        i.attr = k;
      },
      get attr() {
        return i.attr;
      },
      set header(k) {
        i.loadFromBinary(k);
      },
      get header() {
        return i;
      },
      packCentralHeader: function() {
        i.flags_efs = this.efs, i.extraLength = y.length;
        var k = i.centralHeaderToBinary(), L = t.Constants.CENHDR;
        return l.copy(k, L), L += l.length, y.copy(k, L), L += i.extraLength, s.copy(k, L), k;
      },
      packLocalHeader: function() {
        let k = 0;
        i.flags_efs = this.efs, i.extraLocalLength = R.length;
        const L = i.localHeaderToBinary(), U = Buffer.alloc(L.length + l.length + i.extraLocalLength);
        return L.copy(U, k), k += L.length, l.copy(U, k), k += l.length, R.copy(U, k), k += R.length, U;
      },
      toJSON: function() {
        const k = function(L) {
          return "<" + (L && L.length + " bytes buffer" || "null") + ">";
        };
        return {
          entryName: this.entryName,
          name: this.name,
          comment: this.comment,
          isDirectory: this.isDirectory,
          header: i.toJSON(),
          compressedData: k(n),
          data: k(d)
        };
      },
      toString: function() {
        return JSON.stringify(this.toJSON(), null, "	");
      }
    };
  }, nc;
}
var ic, Q_;
function _T() {
  if (Q_) return ic;
  Q_ = 1;
  const t = LS(), e = NS(), r = Nn();
  return ic = function(o, a) {
    var n = [], i = {}, l = Buffer.alloc(0), s = new e.MainHeader(), f = !1;
    const d = /* @__PURE__ */ new Set(), y = a, { noSort: R, decoder: w } = y;
    o ? u(y.readEntries) : f = !0;
    function I() {
      const p = /* @__PURE__ */ new Set();
      for (const b of Object.keys(i)) {
        const C = b.split("/");
        if (C.pop(), !!C.length)
          for (let N = 0; N < C.length; N++) {
            const W = C.slice(0, N + 1).join("/") + "/";
            p.add(W);
          }
      }
      for (const b of p)
        if (!(b in i)) {
          const C = new t(y);
          C.entryName = b, C.attr = 16, C.temporary = !0, n.push(C), i[C.entryName] = C, d.add(C);
        }
    }
    function A() {
      if (f = !0, i = {}, s.diskEntries > (o.length - s.offset) / r.Constants.CENHDR)
        throw r.Errors.DISK_ENTRY_TOO_LARGE();
      n = new Array(s.diskEntries);
      for (var p = s.offset, b = 0; b < n.length; b++) {
        var C = p, N = new t(y, o);
        N.header = o.slice(C, C += r.Constants.CENHDR), N.entryName = o.slice(C, C += N.header.fileNameLength), N.header.extraLength && (N.extra = o.slice(C, C += N.header.extraLength)), N.header.commentLength && (N.comment = o.slice(C, C + N.header.commentLength)), p += N.header.centralHeaderSize, n[b] = N, i[N.entryName] = N;
      }
      d.clear(), I();
    }
    function u(p) {
      var b = o.length - r.Constants.ENDHDR, C = Math.max(0, b - 65535), N = C, W = o.length, k = -1, L = 0;
      for (typeof y.trailingSpace == "boolean" && y.trailingSpace && (C = 0), b; b >= N; b--)
        if (o[b] === 80) {
          if (o.readUInt32LE(b) === r.Constants.ENDSIG) {
            k = b, L = b, W = b + r.Constants.ENDHDR, N = b - r.Constants.END64HDR;
            continue;
          }
          if (o.readUInt32LE(b) === r.Constants.END64SIG) {
            N = C;
            continue;
          }
          if (o.readUInt32LE(b) === r.Constants.ZIP64SIG) {
            k = b, W = b + r.readBigUInt64LE(o, b + r.Constants.ZIP64SIZE) + r.Constants.ZIP64LEAD;
            break;
          }
        }
      if (k == -1) throw r.Errors.INVALID_FORMAT();
      s.loadFromBinary(o.slice(k, W)), s.commentLength && (l = o.slice(L + r.Constants.ENDHDR)), p && A();
    }
    function g() {
      n.length > 1 && !R && n.sort((p, b) => p.entryName.toLowerCase().localeCompare(b.entryName.toLowerCase()));
    }
    return {
      /**
       * Returns an array of ZipEntry objects existent in the current opened archive
       * @return Array
       */
      get entries() {
        return f || A(), n.filter((p) => !d.has(p));
      },
      /**
       * Archive comment
       * @return {String}
       */
      get comment() {
        return w.decode(l);
      },
      set comment(p) {
        l = r.toBuffer(p, w.encode), s.commentLength = l.length;
      },
      getEntryCount: function() {
        return f ? n.length : s.diskEntries;
      },
      forEach: function(p) {
        this.entries.forEach(p);
      },
      /**
       * Returns a reference to the entry with the given name or null if entry is inexistent
       *
       * @param entryName
       * @return ZipEntry
       */
      getEntry: function(p) {
        return f || A(), i[p] || null;
      },
      /**
       * Adds the given entry to the entry list
       *
       * @param entry
       */
      setEntry: function(p) {
        f || A(), n.push(p), i[p.entryName] = p, s.totalEntries = n.length;
      },
      /**
       * Removes the file with the given name from the entry list.
       *
       * If the entry is a directory, then all nested files and directories will be removed
       * @param entryName
       * @returns {void}
       */
      deleteFile: function(p, b = !0) {
        f || A();
        const C = i[p];
        this.getEntryChildren(C, b).map((W) => W.entryName).forEach(this.deleteEntry);
      },
      /**
       * Removes the entry with the given name from the entry list.
       *
       * @param {string} entryName
       * @returns {void}
       */
      deleteEntry: function(p) {
        f || A();
        const b = i[p], C = n.indexOf(b);
        C >= 0 && (n.splice(C, 1), delete i[p], s.totalEntries = n.length);
      },
      /**
       *  Iterates and returns all nested files and directories of the given entry
       *
       * @param entry
       * @return Array
       */
      getEntryChildren: function(p, b = !0) {
        if (f || A(), typeof p == "object")
          if (p.isDirectory && b) {
            const C = [], N = p.entryName;
            for (const W of n)
              W.entryName.startsWith(N) && C.push(W);
            return C;
          } else
            return [p];
        return [];
      },
      /**
       *  How many child elements entry has
       *
       * @param {ZipEntry} entry
       * @return {integer}
       */
      getChildCount: function(p) {
        if (p && p.isDirectory) {
          const b = this.getEntryChildren(p);
          return b.includes(p) ? b.length - 1 : b.length;
        }
        return 0;
      },
      /**
       * Returns the zip file
       *
       * @return Buffer
       */
      compressToBuffer: function() {
        f || A(), g();
        const p = [], b = [];
        let C = 0, N = 0;
        s.size = 0, s.offset = 0;
        let W = 0;
        for (const U of this.entries) {
          const x = U.getCompressedData();
          U.header.offset = N;
          const D = U.packLocalHeader(), F = D.length + x.length;
          N += F, p.push(D), p.push(x);
          const T = U.packCentralHeader();
          b.push(T), s.size += T.length, C += F + T.length, W++;
        }
        C += s.mainHeaderSize, s.offset = N, s.totalEntries = W, N = 0;
        const k = Buffer.alloc(C);
        for (const U of p)
          U.copy(k, N), N += U.length;
        for (const U of b)
          U.copy(k, N), N += U.length;
        const L = s.toBinary();
        return l && l.copy(L, r.Constants.ENDHDR), L.copy(k, N), o = k, f = !1, k;
      },
      toAsyncBuffer: function(p, b, C, N) {
        try {
          f || A(), g();
          const W = [], k = [];
          let L = 0, U = 0, x = 0;
          s.size = 0, s.offset = 0;
          const D = function(F) {
            if (F.length > 0) {
              const T = F.shift(), J = T.entryName + T.extra.toString();
              C && C(J), T.getCompressedDataAsync(function(re) {
                N && N(J), T.header.offset = U;
                const se = T.packLocalHeader(), ne = se.length + re.length;
                U += ne, W.push(se), W.push(re);
                const M = T.packCentralHeader();
                k.push(M), s.size += M.length, L += ne + M.length, x++, D(F);
              });
            } else {
              L += s.mainHeaderSize, s.offset = U, s.totalEntries = x, U = 0;
              const T = Buffer.alloc(L);
              W.forEach(function(re) {
                re.copy(T, U), U += re.length;
              }), k.forEach(function(re) {
                re.copy(T, U), U += re.length;
              });
              const J = s.toBinary();
              l && l.copy(J, r.Constants.ENDHDR), J.copy(T, U), o = T, f = !1, p(T);
            }
          };
          D(Array.from(this.entries));
        } catch (W) {
          b(W);
        }
      }
    };
  }, ic;
}
var sc, ew;
function wT() {
  if (ew) return sc;
  ew = 1;
  const t = Nn(), e = Ue, r = LS(), o = _T(), a = (...s) => t.findLast(s, (f) => typeof f == "boolean"), n = (...s) => t.findLast(s, (f) => typeof f == "string"), i = (...s) => t.findLast(s, (f) => typeof f == "function"), l = {
    // option "noSort" : if true it disables files sorting
    noSort: !1,
    // read entries during load (initial loading may be slower)
    readEntries: !1,
    // default method is none
    method: t.Constants.NONE,
    // file system
    fs: null
  };
  return sc = function(s, f) {
    let d = null;
    const y = Object.assign(/* @__PURE__ */ Object.create(null), l);
    s && typeof s == "object" && (s instanceof Uint8Array || (Object.assign(y, s), s = y.input ? y.input : void 0, y.input && delete y.input), Buffer.isBuffer(s) && (d = s, y.method = t.Constants.BUFFER, s = void 0)), Object.assign(y, f);
    const R = new t(y);
    if ((typeof y.decoder != "object" || typeof y.decoder.encode != "function" || typeof y.decoder.decode != "function") && (y.decoder = t.decoder), s && typeof s == "string")
      if (R.fs.existsSync(s))
        y.method = t.Constants.FILE, y.filename = s, d = R.fs.readFileSync(s);
      else
        throw t.Errors.INVALID_FILENAME();
    const w = new o(d, y), { canonical: I, sanitize: A, zipnamefix: u } = t;
    function g(N) {
      if (N && w) {
        var W;
        if (typeof N == "string" && (W = w.getEntry(e.posix.normalize(N))), typeof N == "object" && typeof N.entryName < "u" && typeof N.header < "u" && (W = w.getEntry(N.entryName)), W)
          return W;
      }
      return null;
    }
    function p(N) {
      const { join: W, normalize: k, sep: L } = e.posix;
      return W(e.isAbsolute(N) ? "/" : ".", k(L + N.split("\\").join(L) + L));
    }
    function b(N) {
      return N instanceof RegExp ? /* @__PURE__ */ (function(W) {
        return function(k) {
          return W.test(k);
        };
      })(N) : typeof N != "function" ? () => !0 : N;
    }
    const C = (N, W) => {
      let k = W.slice(-1);
      return k = k === R.sep ? R.sep : "", e.relative(N, W) + k;
    };
    return {
      /**
       * Extracts the given entry from the archive and returns the content as a Buffer object
       * @param {ZipEntry|string} entry ZipEntry object or String with the full path of the entry
       * @param {Buffer|string} [pass] - password
       * @return Buffer or Null in case of error
       */
      readFile: function(N, W) {
        var k = g(N);
        return k && k.getData(W) || null;
      },
      /**
       * Returns how many child elements has on entry (directories) on files it is always 0
       * @param {ZipEntry|string} entry ZipEntry object or String with the full path of the entry
       * @returns {integer}
       */
      childCount: function(N) {
        const W = g(N);
        if (W)
          return w.getChildCount(W);
      },
      /**
       * Asynchronous readFile
       * @param {ZipEntry|string} entry ZipEntry object or String with the full path of the entry
       * @param {callback} callback
       *
       * @return Buffer or Null in case of error
       */
      readFileAsync: function(N, W) {
        var k = g(N);
        k ? k.getDataAsync(W) : W(null, "getEntry failed for:" + N);
      },
      /**
       * Extracts the given entry from the archive and returns the content as plain text in the given encoding
       * @param {ZipEntry|string} entry - ZipEntry object or String with the full path of the entry
       * @param {string} encoding - Optional. If no encoding is specified utf8 is used
       *
       * @return String
       */
      readAsText: function(N, W) {
        var k = g(N);
        if (k) {
          var L = k.getData();
          if (L && L.length)
            return L.toString(W || "utf8");
        }
        return "";
      },
      /**
       * Asynchronous readAsText
       * @param {ZipEntry|string} entry ZipEntry object or String with the full path of the entry
       * @param {callback} callback
       * @param {string} [encoding] - Optional. If no encoding is specified utf8 is used
       *
       * @return String
       */
      readAsTextAsync: function(N, W, k) {
        var L = g(N);
        L ? L.getDataAsync(function(U, x) {
          if (x) {
            W(U, x);
            return;
          }
          U && U.length ? W(U.toString(k || "utf8")) : W("");
        }) : W("");
      },
      /**
       * Remove the entry from the file or the entry and all it's nested directories and files if the given entry is a directory
       *
       * @param {ZipEntry|string} entry
       * @returns {void}
       */
      deleteFile: function(N, W = !0) {
        var k = g(N);
        k && w.deleteFile(k.entryName, W);
      },
      /**
       * Remove the entry from the file or directory without affecting any nested entries
       *
       * @param {ZipEntry|string} entry
       * @returns {void}
       */
      deleteEntry: function(N) {
        var W = g(N);
        W && w.deleteEntry(W.entryName);
      },
      /**
       * Adds a comment to the zip. The zip must be rewritten after adding the comment.
       *
       * @param {string} comment
       */
      addZipComment: function(N) {
        w.comment = N;
      },
      /**
       * Returns the zip comment
       *
       * @return String
       */
      getZipComment: function() {
        return w.comment || "";
      },
      /**
       * Adds a comment to a specified zipEntry. The zip must be rewritten after adding the comment
       * The comment cannot exceed 65535 characters in length
       *
       * @param {ZipEntry} entry
       * @param {string} comment
       */
      addZipEntryComment: function(N, W) {
        var k = g(N);
        k && (k.comment = W);
      },
      /**
       * Returns the comment of the specified entry
       *
       * @param {ZipEntry} entry
       * @return String
       */
      getZipEntryComment: function(N) {
        var W = g(N);
        return W && W.comment || "";
      },
      /**
       * Updates the content of an existing entry inside the archive. The zip must be rewritten after updating the content
       *
       * @param {ZipEntry} entry
       * @param {Buffer} content
       */
      updateFile: function(N, W) {
        var k = g(N);
        k && k.setData(W);
      },
      /**
       * Adds a file from the disk to the archive
       *
       * @param {string} localPath File to add to zip
       * @param {string} [zipPath] Optional path inside the zip
       * @param {string} [zipName] Optional name for the file
       * @param {string} [comment] Optional file comment
       */
      addLocalFile: function(N, W, k, L) {
        if (R.fs.existsSync(N)) {
          W = W ? p(W) : "";
          const U = e.win32.basename(e.win32.normalize(N));
          W += k || U;
          const x = R.fs.statSync(N), D = x.isFile() ? R.fs.readFileSync(N) : Buffer.alloc(0);
          x.isDirectory() && (W += R.sep), this.addFile(W, D, L, x);
        } else
          throw t.Errors.FILE_NOT_FOUND(N);
      },
      /**
       * Callback for showing if everything was done.
       *
       * @callback doneCallback
       * @param {Error} err - Error object
       * @param {boolean} done - was request fully completed
       */
      /**
       * Adds a file from the disk to the archive
       *
       * @param {(object|string)} options - options object, if it is string it us used as localPath.
       * @param {string} options.localPath - Local path to the file.
       * @param {string} [options.comment] - Optional file comment.
       * @param {string} [options.zipPath] - Optional path inside the zip
       * @param {string} [options.zipName] - Optional name for the file
       * @param {doneCallback} callback - The callback that handles the response.
       */
      addLocalFileAsync: function(N, W) {
        N = typeof N == "object" ? N : { localPath: N };
        const k = e.resolve(N.localPath), { comment: L } = N;
        let { zipPath: U, zipName: x } = N;
        const D = this;
        R.fs.stat(k, function(F, T) {
          if (F) return W(F, !1);
          U = U ? p(U) : "";
          const J = e.win32.basename(e.win32.normalize(k));
          if (U += x || J, T.isFile())
            R.fs.readFile(k, function(re, se) {
              return re ? W(re, !1) : (D.addFile(U, se, L, T), setImmediate(W, void 0, !0));
            });
          else if (T.isDirectory())
            return U += R.sep, D.addFile(U, Buffer.alloc(0), L, T), setImmediate(W, void 0, !0);
        });
      },
      /**
       * Adds a local directory and all its nested files and directories to the archive
       *
       * @param {string} localPath - local path to the folder
       * @param {string} [zipPath] - optional path inside zip
       * @param {(RegExp|function)} [filter] - optional RegExp or Function if files match will be included.
       */
      addLocalFolder: function(N, W, k) {
        if (k = b(k), W = W ? p(W) : "", N = e.normalize(N), R.fs.existsSync(N)) {
          const L = R.findFiles(N), U = this;
          if (L.length)
            for (const x of L) {
              const D = e.join(W, C(N, x));
              k(D) && U.addLocalFile(x, e.dirname(D));
            }
        } else
          throw t.Errors.FILE_NOT_FOUND(N);
      },
      /**
       * Asynchronous addLocalFolder
       * @param {string} localPath
       * @param {callback} callback
       * @param {string} [zipPath] optional path inside zip
       * @param {RegExp|function} [filter] optional RegExp or Function if files match will
       *               be included.
       */
      addLocalFolderAsync: function(N, W, k, L) {
        L = b(L), k = k ? p(k) : "", N = e.normalize(N);
        var U = this;
        R.fs.open(N, "r", function(x) {
          if (x && x.code === "ENOENT")
            W(void 0, t.Errors.FILE_NOT_FOUND(N));
          else if (x)
            W(void 0, x);
          else {
            var D = R.findFiles(N), F = -1, T = function() {
              if (F += 1, F < D.length) {
                var J = D[F], re = C(N, J).split("\\").join("/");
                re = re.normalize("NFD").replace(/[\u0300-\u036f]/g, "").replace(/[^\x20-\x7E]/g, ""), L(re) ? R.fs.stat(J, function(se, ne) {
                  se && W(void 0, se), ne.isFile() ? R.fs.readFile(J, function(M, Q) {
                    M ? W(void 0, M) : (U.addFile(k + re, Q, "", ne), T());
                  }) : (U.addFile(k + re + "/", Buffer.alloc(0), "", ne), T());
                }) : process.nextTick(() => {
                  T();
                });
              } else
                W(!0, void 0);
            };
            T();
          }
        });
      },
      /**
       * Adds a local directory and all its nested files and directories to the archive
       *
       * @param {object | string} options - options object, if it is string it us used as localPath.
       * @param {string} options.localPath - Local path to the folder.
       * @param {string} [options.zipPath] - optional path inside zip.
       * @param {RegExp|function} [options.filter] - optional RegExp or Function if files match will be included.
       * @param {function|string} [options.namefix] - optional function to help fix filename
       * @param {doneCallback} callback - The callback that handles the response.
       *
       */
      addLocalFolderAsync2: function(N, W) {
        const k = this;
        N = typeof N == "object" ? N : { localPath: N }, localPath = e.resolve(p(N.localPath));
        let { zipPath: L, filter: U, namefix: x } = N;
        U instanceof RegExp ? U = /* @__PURE__ */ (function(T) {
          return function(J) {
            return T.test(J);
          };
        })(U) : typeof U != "function" && (U = function() {
          return !0;
        }), L = L ? p(L) : "", x == "latin1" && (x = (T) => T.normalize("NFD").replace(/[\u0300-\u036f]/g, "").replace(/[^\x20-\x7E]/g, "")), typeof x != "function" && (x = (T) => T);
        const D = (T) => e.join(L, x(C(localPath, T))), F = (T) => e.win32.basename(e.win32.normalize(x(T)));
        R.fs.open(localPath, "r", function(T) {
          T && T.code === "ENOENT" ? W(void 0, t.Errors.FILE_NOT_FOUND(localPath)) : T ? W(void 0, T) : R.findFilesAsync(localPath, function(J, re) {
            if (J) return W(J);
            re = re.filter((se) => U(D(se))), re.length || W(void 0, !1), setImmediate(
              re.reverse().reduce(function(se, ne) {
                return function(M, Q) {
                  if (M || Q === !1) return setImmediate(se, M, !1);
                  k.addLocalFileAsync(
                    {
                      localPath: ne,
                      zipPath: e.dirname(D(ne)),
                      zipName: F(ne)
                    },
                    se
                  );
                };
              }, W)
            );
          });
        });
      },
      /**
       * Adds a local directory and all its nested files and directories to the archive
       *
       * @param {string} localPath - path where files will be extracted
       * @param {object} props - optional properties
       * @param {string} [props.zipPath] - optional path inside zip
       * @param {RegExp|function} [props.filter] - optional RegExp or Function if files match will be included.
       * @param {function|string} [props.namefix] - optional function to help fix filename
       */
      addLocalFolderPromise: function(N, W) {
        return new Promise((k, L) => {
          this.addLocalFolderAsync2(Object.assign({ localPath: N }, W), (U, x) => {
            U && L(U), x && k(this);
          });
        });
      },
      /**
       * Allows you to create a entry (file or directory) in the zip file.
       * If you want to create a directory the entryName must end in / and a null buffer should be provided.
       * Comment and attributes are optional
       *
       * @param {string} entryName
       * @param {Buffer | string} content - file content as buffer or utf8 coded string
       * @param {string} [comment] - file comment
       * @param {number | object} [attr] - number as unix file permissions, object as filesystem Stats object
       */
      addFile: function(N, W, k, L) {
        N = u(N);
        let U = g(N);
        const x = U != null;
        x || (U = new r(y), U.entryName = N), U.comment = k || "";
        const D = typeof L == "object" && L instanceof R.fs.Stats;
        D && (U.header.time = L.mtime);
        var F = U.isDirectory ? 16 : 0;
        let T = U.isDirectory ? 16384 : 32768;
        return D ? T |= 4095 & L.mode : typeof L == "number" ? T |= 4095 & L : T |= U.isDirectory ? 493 : 420, F = (F | T << 16) >>> 0, U.attr = F, U.setData(W), x || w.setEntry(U), U;
      },
      /**
       * Returns an array of ZipEntry objects representing the files and folders inside the archive
       *
       * @param {string} [password]
       * @returns Array
       */
      getEntries: function(N) {
        return w.password = N, w ? w.entries : [];
      },
      /**
       * Returns a ZipEntry object representing the file or folder specified by ``name``.
       *
       * @param {string} name
       * @return ZipEntry
       */
      getEntry: function(N) {
        return g(N);
      },
      getEntryCount: function() {
        return w.getEntryCount();
      },
      forEach: function(N) {
        return w.forEach(N);
      },
      /**
       * Extracts the given entry to the given targetPath
       * If the entry is a directory inside the archive, the entire directory and it's subdirectories will be extracted
       *
       * @param {string|ZipEntry} entry - ZipEntry object or String with the full path of the entry
       * @param {string} targetPath - Target folder where to write the file
       * @param {boolean} [maintainEntryPath=true] - If maintainEntryPath is true and the entry is inside a folder, the entry folder will be created in targetPath as well. Default is TRUE
       * @param {boolean} [overwrite=false] - If the file already exists at the target path, the file will be overwriten if this is true.
       * @param {boolean} [keepOriginalPermission=false] - The file will be set as the permission from the entry if this is true.
       * @param {string} [outFileName] - String If set will override the filename of the extracted file (Only works if the entry is a file)
       *
       * @return Boolean
       */
      extractEntryTo: function(N, W, k, L, U, x) {
        L = a(!1, L), U = a(!1, U), k = a(!0, k), x = n(U, x);
        var D = g(N);
        if (!D)
          throw t.Errors.NO_ENTRY();
        var F = I(D.entryName), T = A(W, x && !D.isDirectory ? x : k ? F : e.basename(F));
        if (D.isDirectory) {
          var J = w.getEntryChildren(D);
          return J.forEach(function(ne) {
            if (ne.isDirectory) return;
            var M = ne.getData();
            if (!M)
              throw t.Errors.CANT_EXTRACT_FILE();
            var Q = I(ne.entryName), v = A(W, k ? Q : e.basename(Q));
            const $ = U ? ne.header.fileAttr : void 0;
            R.writeFileTo(v, M, L, $);
          }), !0;
        }
        var re = D.getData(w.password);
        if (!re) throw t.Errors.CANT_EXTRACT_FILE();
        if (R.fs.existsSync(T) && !L)
          throw t.Errors.CANT_OVERRIDE();
        const se = U ? N.header.fileAttr : void 0;
        return R.writeFileTo(T, re, L, se), !0;
      },
      /**
       * Test the archive
       * @param {string} [pass]
       */
      test: function(N) {
        if (!w)
          return !1;
        for (var W in w.entries)
          try {
            if (W.isDirectory)
              continue;
            var k = w.entries[W].getData(N);
            if (!k)
              return !1;
          } catch {
            return !1;
          }
        return !0;
      },
      /**
       * Extracts the entire archive to the given location
       *
       * @param {string} targetPath Target location
       * @param {boolean} [overwrite=false] If the file already exists at the target path, the file will be overwriten if this is true.
       *                  Default is FALSE
       * @param {boolean} [keepOriginalPermission=false] The file will be set as the permission from the entry if this is true.
       *                  Default is FALSE
       * @param {string|Buffer} [pass] password
       */
      extractAllTo: function(N, W, k, L) {
        if (k = a(!1, k), L = n(k, L), W = a(!1, W), !w) throw t.Errors.NO_ZIP();
        w.entries.forEach(function(U) {
          var x = A(N, I(U.entryName));
          if (U.isDirectory) {
            R.makeDir(x);
            return;
          }
          var D = U.getData(L);
          if (!D)
            throw t.Errors.CANT_EXTRACT_FILE();
          const F = k ? U.header.fileAttr : void 0;
          R.writeFileTo(x, D, W, F);
          try {
            R.fs.utimesSync(x, U.header.time, U.header.time);
          } catch {
            throw t.Errors.CANT_EXTRACT_FILE();
          }
        });
      },
      /**
       * Asynchronous extractAllTo
       *
       * @param {string} targetPath Target location
       * @param {boolean} [overwrite=false] If the file already exists at the target path, the file will be overwriten if this is true.
       *                  Default is FALSE
       * @param {boolean} [keepOriginalPermission=false] The file will be set as the permission from the entry if this is true.
       *                  Default is FALSE
       * @param {function} callback The callback will be executed when all entries are extracted successfully or any error is thrown.
       */
      extractAllToAsync: function(N, W, k, L) {
        if (L = i(W, k, L), k = a(!1, k), W = a(!1, W), !L)
          return new Promise((T, J) => {
            this.extractAllToAsync(N, W, k, function(re) {
              re ? J(re) : T(this);
            });
          });
        if (!w) {
          L(t.Errors.NO_ZIP());
          return;
        }
        N = e.resolve(N);
        const U = (T) => A(N, e.normalize(I(T.entryName))), x = (T, J) => new Error(T + ': "' + J + '"'), D = [], F = [];
        w.entries.forEach((T) => {
          T.isDirectory ? D.push(T) : F.push(T);
        });
        for (const T of D) {
          const J = U(T), re = k ? T.header.fileAttr : void 0;
          try {
            R.makeDir(J), re && R.fs.chmodSync(J, re), R.fs.utimesSync(J, T.header.time, T.header.time);
          } catch {
            L(x("Unable to create folder", J));
          }
        }
        F.reverse().reduce(function(T, J) {
          return function(re) {
            if (re)
              T(re);
            else {
              const se = e.normalize(I(J.entryName)), ne = A(N, se);
              J.getDataAsync(function(M, Q) {
                if (Q)
                  T(Q);
                else if (!M)
                  T(t.Errors.CANT_EXTRACT_FILE());
                else {
                  const v = k ? J.header.fileAttr : void 0;
                  R.writeFileToAsync(ne, M, W, v, function($) {
                    $ || T(x("Unable to write file", ne)), R.fs.utimes(ne, J.header.time, J.header.time, function(ee) {
                      ee ? T(x("Unable to set times", ne)) : T();
                    });
                  });
                }
              });
            }
          };
        }, L)();
      },
      /**
       * Writes the newly created zip file to disk at the specified location or if a zip was opened and no ``targetFileName`` is provided, it will overwrite the opened zip
       *
       * @param {string} targetFileName
       * @param {function} callback
       */
      writeZip: function(N, W) {
        if (arguments.length === 1 && typeof N == "function" && (W = N, N = ""), !N && y.filename && (N = y.filename), !!N) {
          var k = w.compressToBuffer();
          if (k) {
            var L = R.writeFileTo(N, k, !0);
            typeof W == "function" && W(L ? null : new Error("failed"), "");
          }
        }
      },
      /**
      	         *
      	         * @param {string} targetFileName
      	         * @param {object} [props]
      	         * @param {boolean} [props.overwrite=true] If the file already exists at the target path, the file will be overwriten if this is true.
      	         * @param {boolean} [props.perm] The file will be set as the permission from the entry if this is true.
      
      	         * @returns {Promise<void>}
      	         */
      writeZipPromise: function(N, W) {
        const { overwrite: k, perm: L } = Object.assign({ overwrite: !0 }, W);
        return new Promise((U, x) => {
          !N && y.filename && (N = y.filename), N || x("ADM-ZIP: ZIP File Name Missing"), this.toBufferPromise().then((D) => {
            const F = (T) => T ? U(T) : x("ADM-ZIP: Wasn't able to write zip file");
            R.writeFileToAsync(N, D, k, L, F);
          }, x);
        });
      },
      /**
       * @returns {Promise<Buffer>} A promise to the Buffer.
       */
      toBufferPromise: function() {
        return new Promise((N, W) => {
          w.toAsyncBuffer(N, W);
        });
      },
      /**
       * Returns the content of the entire zip file as a Buffer object
       *
       * @prop {function} [onSuccess]
       * @prop {function} [onFail]
       * @prop {function} [onItemStart]
       * @prop {function} [onItemEnd]
       * @returns {Buffer}
       */
      toBuffer: function(N, W, k, L) {
        return typeof N == "function" ? (w.toAsyncBuffer(N, W, k, L), null) : w.compressToBuffer();
      }
    };
  }, sc;
}
var mT = wT();
const ST = /* @__PURE__ */ Ci(mT), PS = ".gmos", CS = ".gmos-driver", cc = 1, MS = /^m-\d+/, Er = /* @__PURE__ */ new Map();
async function ET(t) {
  return new Promise((e, r) => {
    const o = uw.createHash("sha256"), a = Se.createReadStream(t);
    a.on("data", (n) => o.update(n)), a.on("end", () => e(o.digest("hex"))), a.on("error", r);
  });
}
function vT(t) {
  const e = Ee.extname(t).toLowerCase().slice(1);
  return {
    png: "image/png",
    jpg: "image/jpeg",
    jpeg: "image/jpeg",
    webp: "image/webp",
    gif: "image/gif",
    svg: "image/svg+xml",
    mp4: "video/mp4",
    webm: "video/webm",
    mp3: "audio/mpeg",
    ogg: "audio/ogg",
    wav: "audio/wav",
    json: "application/json"
  }[e] ?? "application/octet-stream";
}
function RT(t) {
  const e = Ee.extname(t).toLowerCase().slice(1), r = ["png", "jpg", "jpeg", "webp", "gif", "svg"], o = ["mp4", "webm", "mov"], a = ["mp3", "wav", "ogg", "flac", "m4a", "aac"];
  return t.includes("avatar") || t.includes("portrait") || t.includes("token") || MS.test(t) ? "assets/profiles" : a.includes(e) ? "assets/audio" : o.includes(e) || r.includes(e) ? "assets/maps" : "assets/misc";
}
function AT(t) {
  return !t || typeof t != "string" ? !0 : /\.\.[\\/]/.test(t) || /^[A-Za-z]:[\\/]/.test(t) || t.startsWith("/");
}
function TT(t) {
  return Ee.isAbsolute(t) && !MS.test(t);
}
async function xT(t, e, r, o, a) {
  console.log(`[Nexus Bridge] Export démarré : ${t} → ${e}`);
  const n = Er.size;
  console.log(`[Nexus Bridge] ${n} asset(s) en cache mémoire prêts à archiver.`);
  const i = [], l = [];
  let s = 0;
  try {
    const f = JSON.parse(o), d = Ee.join(Ee.dirname(e), `.nexus-tmp-${Date.now()}`);
    await Se.ensureDir(d);
    try {
      for (const u of a) {
        if (!TT(u)) {
          console.warn(`[Nexus Bridge] Ref non-résolvable ignorée (Media Hub ID ou chemin non-absolu) : ${u}`), i.push(u);
          continue;
        }
        if (!await Se.pathExists(u)) {
          console.warn(`[Nexus Bridge] Fichier introuvable : ${u}`), i.push(u);
          continue;
        }
        const g = Ee.basename(u), p = RT(u), b = `${p}/${g}`, C = l.map((U) => U.relativePath);
        let N = b;
        if (C.includes(b)) {
          const U = Ee.extname(g), x = Ee.basename(g, U);
          N = `${p}/${x}-${Date.now()}${U}`;
        }
        const W = Ee.join(d, N);
        await Se.ensureDir(Ee.dirname(W)), await Se.copyFile(u, W);
        const k = await ET(u), L = await Se.stat(u);
        s += L.size, l.push({
          originalRef: u,
          relativePath: N,
          checksum: k,
          sizeBytes: L.size,
          mimeType: vT(u)
        });
      }
      const y = Er.size;
      y > 0 && console.log(`[Nexus Bridge] Traitement de ${y} inline assets (depuis cache)...`);
      for (const [u, g] of Er.entries())
        try {
          const p = g.match(/^data:([^;]+);base64,(.+)$/);
          if (!p) {
            console.warn(`[Nexus Bridge] Data URL invalide pour ${u}, ignoré.`), i.push(u);
            continue;
          }
          const [, b, C] = p, N = Buffer.from(C, "base64"), k = {
            "image/png": "png",
            "image/jpeg": "jpg",
            "image/webp": "webp",
            "image/gif": "gif",
            "image/svg+xml": "svg",
            "video/mp4": "mp4",
            "video/webm": "webm",
            "audio/mpeg": "mp3",
            "audio/ogg": "ogg",
            "audio/wav": "wav"
          }[b] ?? "bin", U = `assets/profiles/${`${u}.${k}`}`, x = Ee.join(d, U);
          await Se.ensureDir(Ee.dirname(x)), await Se.writeFile(x, N);
          const D = uw.createHash("sha256").update(N).digest("hex");
          s += N.length, l.push({
            originalRef: u,
            relativePath: U,
            checksum: D,
            sizeBytes: N.length,
            mimeType: b
          }), console.log(`[Nexus Bridge] Inline asset écrit : ${U} (${N.length} octets)`);
        } catch (p) {
          console.error(`[Nexus Bridge] Erreur traitement inline asset ${u} :`, p), i.push(u);
        }
      Er.clear();
      const R = JSON.parse(r), w = {
        schemaVersion: cc,
        bundleId: f.bundleId ?? `nexus-${Date.now()}`,
        bundleType: f.bundleType ?? "campaign",
        campaignId: f.campaignId,
        campaignName: f.campaignName,
        driverId: f.driverId,
        driverName: f.driverName,
        exportedAt: f.exportedAt ?? (/* @__PURE__ */ new Date()).toISOString(),
        gmosVersion: f.gmosVersion ?? "5.3.0",
        requiredDriverIds: f.requiredDriverIds ?? [],
        requiredTemplateIds: f.requiredTemplateIds ?? [],
        assetMap: l,
        stats: {
          entityCount: R?.entities?.length ?? 0,
          sessionCount: R?.sessions?.length ?? 0,
          atlasMapCount: R?.atlasMaps?.length ?? 0,
          wikiEntryCount: R?.wikiEntries?.length ?? 0,
          clueCount: R?.clues?.length ?? 0,
          assetCount: l.length,
          totalSizeBytes: s
        }
      };
      await Se.writeFile(Ee.join(d, "manifest.json"), JSON.stringify(w, null, 2), "utf-8"), await Se.writeFile(Ee.join(d, "state.json"), r, "utf-8");
      const I = f.bundleType === "driver" ? CS : PS, A = e.endsWith(I) ? e : `${e}${I}`;
      return await IT(d, A), console.log(`[Nexus Bridge] Export terminé : ${A} (${l.length} assets, ${i.length} manquants)`), {
        success: !0,
        outputPath: A,
        manifest: w,
        missingAssets: i
      };
    } finally {
      await Se.remove(d).catch((y) => console.warn("[Nexus Bridge] Cleanup failed:", y));
    }
  } catch (f) {
    const d = f instanceof Error ? f.message : String(f);
    return console.error("[Nexus Bridge] Export échoué:", f), { success: !1, missingAssets: i, error: d };
  }
}
async function IT(t, e) {
  return new Promise((r, o) => {
    const a = bE(e), n = lT("zip", {
      zlib: { level: 6 }
      // Compression équilibrée (vitesse/taille)
    });
    a.on("close", () => {
      console.log(`[Nexus Bridge] Archive créée : ${n.pointer()} octets`), r();
    }), n.on("error", (i) => {
      console.error("[Nexus Bridge] Erreur archiver:", i), o(i);
    }), n.pipe(a), n.directory(t, !1), n.finalize();
  });
}
async function DT(t) {
  console.log(`[Nexus Bridge] Import démarré : ${t}`);
  try {
    if (!await Se.pathExists(t))
      return { success: !1, error: `Fichier introuvable : ${t}` };
    const e = new ST(t), r = e.getEntries();
    let o, a;
    const n = {};
    for (const f of r) {
      const d = f.entryName;
      if (AT(d)) {
        console.warn(`[Nexus Bridge] Entrée ignorée (path dangereux) : ${d}`);
        continue;
      }
      if (d === "manifest.json") {
        o = e.readAsText(f);
        continue;
      }
      if (d === "state.json") {
        a = e.readAsText(f);
        continue;
      }
      if (d.startsWith("assets/")) {
        const y = e.readFile(f);
        if (!y) continue;
        const R = Ee.extname(d).toLowerCase().slice(1), I = {
          png: "image/png",
          jpg: "image/jpeg",
          jpeg: "image/jpeg",
          webp: "image/webp",
          gif: "image/gif",
          svg: "image/svg+xml",
          mp4: "video/mp4",
          webm: "video/webm"
        }[R] ?? "application/octet-stream", A = y.toString("base64");
        n[d] = `data:${I};base64,${A}`;
      }
    }
    if (!o || !a)
      return {
        success: !1,
        error: "Archive invalide : manifest.json ou state.json manquant."
      };
    const i = JSON.parse(o);
    if (i.schemaVersion !== cc)
      return {
        success: !1,
        error: `Version de schéma incompatible : attendu ${cc}, reçu ${i.schemaVersion}.`
      };
    let l, s;
    try {
      const f = JSON.parse(a);
      f.atmospheres && (l = JSON.stringify(f.atmospheres)), f.playlists && (s = JSON.stringify(f.playlists));
    } catch {
      console.warn("[Nexus Bridge] Impossible d'extraire les données audio du state.json.");
    }
    return console.log(`[Nexus Bridge] Import parsé : Bundle = ${i.bundleId}, ${Object.keys(n).length} assets`), {
      success: !0,
      manifestJson: o,
      stateJson: a,
      assetData: n,
      atmospheresJson: l,
      playlistsJson: s
    };
  } catch (e) {
    const r = e instanceof Error ? e.message : String(e);
    return console.error("[Nexus Bridge] Import échoué:", e), { success: !1, error: r };
  }
}
function OT() {
  console.log("[Nexus Bridge] Registering IPC Handlers"), Re.handle("nexus:register-asset", (t, e, r) => !e || !r ? { ok: !1, error: "Paramètres manquants." } : (Er.set(e, r), console.log(`[Nexus Bridge] Asset en cache : ${e} (${Math.round(r.length / 1024)} Ko)`), { ok: !0 })), Re.handle("nexus:clear-assets", () => {
    const t = Er.size;
    return Er.clear(), console.log(`[Nexus Bridge] Cache vidé : ${t} asset(s).`), { ok: !0 };
  }), Re.handle(
    "nexus:export-bundle",
    async (t, e, r, o, a, n) => xT(e, r, o, a, n)
  ), Re.handle("nexus:import-bundle", async (t, e) => DT(e)), Re.handle("nexus:select-export-path", async (t, e) => {
    const r = e === "driver" ? CS : PS, o = e === "driver" ? `driver_gmos_${Date.now()}${r}` : `campagne_gmos_${Date.now()}${r}`, a = e === "driver" ? "GM-OS Driver" : "GM-OS Bundle", n = r.replace(".", ""), { filePath: i } = await Vt.showSaveDialog({
      title: e === "driver" ? "Exporter le GameDriver GM-OS" : "Exporter la Campagne GM-OS",
      defaultPath: o,
      filters: [
        { name: a, extensions: [n] }
      ]
    });
    return i ?? null;
  }), Re.handle("nexus:select-import-file", async () => {
    const { filePaths: t } = await Vt.showOpenDialog({
      title: "Importer une Archive GM-OS (.gmos / .gmos-driver)",
      filters: [
        { name: "GM-OS Archive", extensions: ["gmos", "gmos-driver"] }
      ],
      properties: ["openFile"]
    });
    return t?.[0] ?? null;
  });
}
class NT {
  constructor() {
    this.secrets = {}, this.secretsPath = Ee.join(Zt.getPath("userData"), "vault", "secrets.enc"), this.ensureStoreExists(), this.loadSecrets();
  }
  /**
   * Garantit que le dossier du coffre-fort existe.
   */
  ensureStoreExists() {
    const e = Ee.dirname(this.secretsPath);
    Se.existsSync(e) || Se.mkdirpSync(e);
  }
  /**
   * Charge et déchiffre les secrets depuis le disque.
   */
  loadSecrets() {
    if (!Se.existsSync(this.secretsPath)) {
      this.secrets = {};
      return;
    }
    try {
      const e = Se.readFileSync(this.secretsPath);
      if (e.length === 0) {
        this.secrets = {};
        return;
      }
      if (Pn.isEncryptionAvailable())
        try {
          const r = Pn.decryptString(e);
          this.secrets = JSON.parse(r);
        } catch {
          const o = Buffer.from(e.toString("utf-8"), "base64").toString("utf-8");
          this.secrets = JSON.parse(o);
        }
      else {
        console.warn("[SecurityManager] Encryption not available on this system. Using Base64 fallback.");
        const r = Buffer.from(e.toString("utf-8"), "base64").toString("utf-8");
        this.secrets = JSON.parse(r);
      }
    } catch (e) {
      console.error("[SecurityManager] Failed to load or decrypt secrets:", e), this.secrets = {};
    }
  }
  /**
   * Chiffre et sauvegarde les secrets sur le disque.
   */
  saveSecrets() {
    try {
      const e = JSON.stringify(this.secrets);
      if (Pn.isEncryptionAvailable()) {
        const r = Pn.encryptString(e);
        Se.writeFileSync(this.secretsPath, r);
      } else {
        console.warn("[SecurityManager] Encryption not available. Saving via Base64 fallback.");
        const r = Buffer.from(e, "utf-8").toString("base64");
        Se.writeFileSync(this.secretsPath, r);
      }
    } catch (e) {
      console.error("[SecurityManager] Failed to encrypt or save secrets:", e);
    }
  }
  /**
   * Enregistre un secret de manière sécurisée.
   */
  setSecret(e, r) {
    this.secrets[e] = r, this.saveSecrets();
  }
  /**
   * Récupère un secret déchiffré.
   */
  getSecret(e) {
    return this.secrets[e] || null;
  }
  /**
   * Supprime un secret.
   */
  deleteSecret(e) {
    this.secrets[e] && (delete this.secrets[e], this.saveSecrets());
  }
}
function LT() {
  const t = new NT();
  Re.handle("security:get-secret", (e, r) => t.getSecret(r)), Re.handle("security:set-secret", (e, r, o) => (t.setSecret(r, o), !0)), Re.handle("security:delete-secret", (e, r) => (t.deleteSecret(r), !0));
}
class PT {
  constructor() {
    this.sessions = /* @__PURE__ */ new Map(), this.ghostTimeouts = /* @__PURE__ */ new Map(), this.GHOST_DURATION = 120 * 1e3;
  }
  // 2 minutes
  registerClient(e, r, o, a, n, i) {
    if (n) {
      const f = Array.from(this.sessions.values()).find(
        (d) => d.characterId === n && d.deviceId !== e && (d.status === "active" || d.status === "ghost")
      );
      if (f)
        if (f.status === "ghost" && f.ip === i)
          console.log(`[SessionManager] IP-based Recovery: Taking over ghost session for ${n} from ${f.deviceId} to ${e}`), this.disconnectClient(f.deviceId);
        else
          throw console.warn(`[SessionManager] Character Collision: ${n} already taken by ${f.deviceId} (IP: ${f.ip} vs ${i})`), new Error("character_taken");
    }
    this.ghostTimeouts.has(e) && (clearTimeout(this.ghostTimeouts.get(e)), this.ghostTimeouts.delete(e));
    const l = this.sessions.get(e), s = {
      deviceId: e,
      pseudo: r || l?.pseudo || "Anonyme",
      playerName: a || l?.playerName,
      characterId: n || l?.characterId,
      role: o,
      status: "active",
      lastSeen: Date.now(),
      ip: i || l?.ip
    };
    return this.sessions.set(e, s), s;
  }
  ghostClient(e) {
    const r = this.sessions.get(e);
    if (r && r.status === "active") {
      r.status = "ghost", r.lastSeen = Date.now();
      const o = setTimeout(() => {
        this.disconnectClient(e);
      }, this.GHOST_DURATION);
      this.ghostTimeouts.set(e, o);
    }
  }
  disconnectClient(e) {
    this.sessions.delete(e), this.ghostTimeouts.has(e) && (clearTimeout(this.ghostTimeouts.get(e)), this.ghostTimeouts.delete(e));
  }
  getAllClients() {
    return Array.from(this.sessions.values());
  }
  getClient(e) {
    return this.sessions.get(e);
  }
  updateClientStatus(e, r) {
    const o = this.sessions.get(e);
    o && (o.status = r, o.lastSeen = Date.now());
  }
  clearDisconnected() {
    for (const [e, r] of this.sessions.entries())
      r.status !== "active" && this.disconnectClient(e);
  }
  clearAll() {
    for (const [e] of this.ghostTimeouts.entries())
      clearTimeout(this.ghostTimeouts.get(e));
    this.ghostTimeouts.clear(), this.sessions.clear();
  }
}
const ur = new PT();
class Zc {
  constructor() {
    this.baseUrl = "http://127.0.0.1:11434";
  }
  /**
   * Vérifie si le serveur Ollama est accessible
   */
  async checkStatus(e) {
    const r = (e || this.baseUrl).replace(/\/$/, "");
    try {
      return (await mr.fetch(`${r}/api/tags`)).ok;
    } catch (o) {
      return console.error(`[Ollama] Erreur de vérification du statut sur ${r}:`, o), !1;
    }
  }
  /**
   * Envoie une requête de chat au modèle local (Bloquant)
   */
  async chat(e, r, o) {
    const a = (o || this.baseUrl).replace(/\/$/, "");
    try {
      const n = await mr.fetch(`${a}/api/chat`, {
        method: "POST",
        body: JSON.stringify({
          model: e,
          messages: r,
          stream: !1
        }),
        headers: { "Content-Type": "application/json" }
      });
      if (!n.ok) {
        const l = await n.text().catch(() => n.statusText);
        throw new Error(`Ollama error (${n.status}): ${l}`);
      }
      return (await n.json()).message.content;
    } catch (n) {
      const i = n;
      throw i.code === "ECONNREFUSED" || i.message?.includes("fetch failed") ? new Error(`Ollama est inaccessible sur ${a}. Assurez-vous qu'Ollama est lancé et que le port est correct.`) : n;
    }
  }
  /**
   * Envoie une requête de chat au modèle local avec streaming (Réactifs)
   */
  async chatStream(e, r, o, a) {
    const n = (a || this.baseUrl).replace(/\/$/, "");
    try {
      const i = await mr.fetch(`${n}/api/chat`, {
        method: "POST",
        body: JSON.stringify({
          model: e,
          messages: r,
          stream: !0
        }),
        headers: { "Content-Type": "application/json" }
      });
      if (!i.ok)
        throw new Error(`Ollama stream error: ${i.statusText}`);
      const l = i.body?.getReader();
      if (!l) throw new Error("Réponse vide d'Ollama (Stream body introuvable).");
      const s = new TextDecoder();
      let f = "";
      for (; ; ) {
        const { done: d, value: y } = await l.read();
        if (d) break;
        f += s.decode(y, { stream: !0 });
        const R = f.split(`
`);
        f = R.pop() || "";
        for (const w of R)
          if (w.trim())
            try {
              const I = JSON.parse(w);
              I.message?.content && o(I.message.content);
            } catch {
            }
      }
    } catch (i) {
      throw console.error("[Ollama] Stream error:", i), i;
    }
  }
  /**
   * Liste les modèles installés localement
   */
  async listModels(e) {
    const r = (e || this.baseUrl).replace(/\/$/, "");
    try {
      const o = await mr.fetch(`${r}/api/tags`);
      return o.ok ? (await o.json()).models?.map((n) => n.name) || [] : [];
    } catch (o) {
      return console.error(`[Ollama] Erreur de listing des modèles sur ${r}:`, o), [];
    }
  }
  /**
   * Télécharge un modèle depuis la bibliothèque Ollama
   */
  async pullModel(e, r) {
    const o = (r || this.baseUrl).replace(/\/$/, "");
    try {
      return console.log(`[Ollama] Pulling model: ${e} from ${o}`), (await mr.fetch(`${o}/api/pull`, {
        method: "POST",
        body: JSON.stringify({ name: e, stream: !1 }),
        headers: { "Content-Type": "application/json" }
      })).ok;
    } catch (a) {
      return console.error(`[Ollama] Erreur lors du pull de ${e} sur ${o}:`, a), !1;
    }
  }
  /**
   * Génère une image via l'API Ollama (modèles expérimentaux type Flux)
   */
  async generateImage(e, r, o) {
    const a = (o || this.baseUrl).replace(/\/$/, "");
    try {
      console.log(`[Ollama] Generating image with: ${e} at ${a}`);
      const n = await mr.fetch(`${a}/api/generate`, {
        method: "POST",
        body: JSON.stringify({
          model: e,
          prompt: r,
          stream: !1
        }),
        headers: { "Content-Type": "application/json" }
      });
      if (!n.ok)
        throw new Error(`Ollama image generator error: ${n.statusText}`);
      return (await n.json()).response;
    } catch (n) {
      throw console.error(`[Ollama] Erreur de génération d'image sur ${a}:`, n), n;
    }
  }
  /**
   * Enregistre les gestionnaires IPC pour Ollama
   */
  static registerHandlers() {
    const e = new Zc();
    Re.handle("ai:ollama-status", async (r, o) => await e.checkStatus(o)), Re.handle("ai:ollama-chat", async (r, o, a, n) => await e.chat(o, a, n)), Re.handle("ai:ollama-generate-image", async (r, o, a, n) => await e.generateImage(o, a, n)), Re.handle("ai:ollama-chat-stream", async (r, o, a, n) => {
      try {
        return await e.chatStream(o, a, (i) => {
          r.sender.isDestroyed() || r.sender.send("ai:ollama-stream-token", i);
        }, n), { success: !0 };
      } catch (i) {
        throw console.error("[Ollama Bridge] Streaming error:", i), i;
      }
    }), Re.handle("ai:ollama-list-models", async (r, o) => await e.listModels(o)), Re.handle("ai:ollama-pull", async (r, o, a) => await e.pullModel(o, a));
  }
}
class CT {
  constructor(e, r, o) {
    this.wss = null, this.server = null, this.deviceSocketMap = /* @__PURE__ */ new Map(), this.mainWindow = e, this.port = r, this.tempMediaDir = o;
  }
  start() {
    try {
      this.server = hc.createServer((e, r) => this.handleHttpRequest(e, r)), this.wss = new vE({ server: this.server }), this.wss.on("connection", (e) => {
        this.handleConnection(e);
      }), this.server.listen(this.port, "0.0.0.0", () => {
        console.log(`[Nexus Sync] Server + Media proxy listening on 0.0.0.0:${this.port}`);
      }), this.registerIpcHandlers();
    } catch (e) {
      console.error("[Nexus Sync] Failed to start server:", e);
    }
  }
  handleHttpRequest(e, r) {
    if (!e.url) return;
    if (e.url.startsWith("/media/")) {
      const l = e.url.substring(7), s = decodeURIComponent(l);
      if (console.log(`[SyncServer] Requesting media: ${s}`), Se.existsSync(s) && Se.lstatSync(s).isFile()) {
        console.log(`[SyncServer] Serving media file: ${s}`);
        const f = Ee.extname(s).toLowerCase(), d = {
          ".png": "image/png",
          ".jpg": "image/jpeg",
          ".jpeg": "image/jpeg",
          ".gif": "image/gif",
          ".webp": "image/webp",
          ".svg": "image/svg+xml",
          ".mp3": "audio/mpeg",
          ".wav": "audio/wav"
        };
        r.writeHead(200, {
          "Content-Type": d[f] || "application/octet-stream",
          "Access-Control-Allow-Origin": "*"
        }), Se.createReadStream(s).pipe(r);
      } else
        console.warn(`[SyncServer] Media file not found: ${s}`), r.writeHead(404), r.end("Media not found");
      return;
    }
    if (e.url.startsWith("/temp/")) {
      const l = e.url.substring(6), s = Ee.join(this.tempMediaDir, l);
      if (console.log(`[SyncServer] Requesting temp asset: ${l} -> ${s}`), Se.existsSync(s)) {
        console.log(`[SyncServer] Serving temp asset: ${l}`);
        const f = Ee.extname(s).toLowerCase(), d = {
          ".png": "image/png",
          ".jpg": "image/jpeg",
          ".jpeg": "image/jpeg",
          ".gif": "image/gif",
          ".webp": "image/webp",
          ".svg": "image/svg+xml",
          ".mp3": "audio/mpeg",
          ".wav": "audio/wav"
        };
        r.writeHead(200, {
          "Content-Type": d[f] || "image/webp",
          // Default to webp if no ext (historical)
          "Access-Control-Allow-Origin": "*"
        }), Se.createReadStream(s).pipe(r);
      } else
        console.warn(`[SyncServer] Temp asset not found: ${l}`), r.writeHead(404), r.end("Temp Media not found");
      return;
    }
    const o = process.env.APP_ROOT || process.cwd(), a = Ee.join(o, "dist");
    let n = e.url.split("?")[0];
    n === "/" && (n = "/index.html");
    let i = Ee.join(a, n);
    if (Se.existsSync(i) && Se.lstatSync(i).isFile()) {
      const l = Ee.extname(i).toLowerCase(), s = {
        ".html": "text/html",
        ".js": "text/javascript",
        ".css": "text/css",
        ".png": "image/png",
        ".jpg": "image/jpeg",
        ".svg": "image/svg+xml",
        ".json": "application/json",
        ".ico": "image/x-icon",
        ".mjs": "text/javascript",
        ".webmanifest": "application/manifest+json"
      };
      r.writeHead(200, { "Content-Type": s[l] || "application/octet-stream" }), Se.createReadStream(i).pipe(r);
      return;
    } else if (Se.existsSync(Ee.join(a, "index.html"))) {
      r.writeHead(200, { "Content-Type": "text/html" }), Se.createReadStream(Ee.join(a, "index.html")).pipe(r);
      return;
    }
    r.writeHead(404), r.end();
  }
  handleConnection(e) {
    const r = e._socket?.remoteAddress;
    e.remoteAddress = r, console.log(`[Nexus Sync] New device connected from ${r}`), this.mainWindow && !this.mainWindow.isDestroyed() && this.mainWindow.webContents.send("remote:request-sync"), e.on("message", (o) => {
      try {
        const a = JSON.parse(o);
        a.type === "remote:register" ? this.handleRegister(e, a.payload) : a.type === "remote:ping" ? e.send(JSON.stringify({ type: "remote:pong", payload: a.payload })) : this.forwardToGM(e, a);
      } catch (a) {
        console.error("[Nexus Sync] Failed to parse message:", a);
      }
    }), e.on("close", () => {
      if (e.deviceId) {
        console.log(`[Nexus Sync] Socket closed for device: ${e.deviceId}`);
        const o = this.deviceSocketMap.get(e.deviceId);
        o && (o.delete(e), o.size === 0 && (console.log(`[Nexus Sync] Last socket closed for device ${e.deviceId}, entering ghost mode`), ur.ghostClient(e.deviceId), this.deviceSocketMap.delete(e.deviceId))), this.updateGMClients();
      }
    });
  }
  handleRegister(e, r) {
    const { deviceId: o, pseudo: a, role: n, playerName: i, characterId: l } = r || {}, s = o || `remote-${Math.random().toString(36).substring(2, 9)}`;
    e.deviceId = s, e.role = n || "player", this.deviceSocketMap.has(s) || this.deviceSocketMap.set(s, /* @__PURE__ */ new Set()), this.deviceSocketMap.get(s).add(e);
    try {
      ur.registerClient(s, a || "Unknown", n || "remote", i, l, e.remoteAddress), this.updateGMClients(), e.send(JSON.stringify({ type: "remote:registered", payload: { deviceId: s, role: e.role } }));
    } catch (f) {
      f.message === "character_taken" && e.send(JSON.stringify({
        type: "remote:error",
        payload: {
          code: "character_taken",
          message: "Signature biométrique déjà active sur un autre terminal."
        }
      }));
    }
  }
  forwardToGM(e, r) {
    r.type === "session:send-message" && r.payload?.toId !== "GM" && this.broadcastAction({ ...r, type: "session:receive-message" }, e);
    const o = r.type === "session:send-message" && r.payload?.toId !== "GM" && r.payload?.toId !== "all";
    this.mainWindow && !this.mainWindow.isDestroyed() && !o && this.mainWindow.webContents.send("remote:action", r);
  }
  broadcastAction(e, r, o) {
    if (!this.wss) return;
    const a = JSON.stringify(e);
    this.wss.clients.forEach((n) => {
      n !== r && n.readyState === RE.OPEN && (!o || n.role === o) && n.send(a);
    });
  }
  updateGMClients() {
    this.mainWindow && !this.mainWindow.isDestroyed() && this.mainWindow.webContents.send("remote:sync-clients", ur.getAllClients());
  }
  registerIpcHandlers() {
    Re.on("remote:broadcast-sync", (e, r, o) => {
      this.broadcastAction({ type: "sync", payload: r }, void 0, o);
    }), Re.on("remote:broadcast-ui-action", (e, r, o) => {
      this.broadcastAction(r, void 0, o);
    }), Re.on("remote:request-client-sync", () => {
      this.updateGMClients();
    }), Re.on("remote:clear-disconnected", () => {
      ur.clearDisconnected(), this.updateGMClients();
    }), Re.on("remote:eject-all", () => {
      console.log("[Nexus Sync] ⚠️ EJECT ALL — Force disconnecting all clients"), this.wss && this.wss.clients.forEach((e) => {
        try {
          e.send(JSON.stringify({ type: "remote:ejected", payload: { reason: "GM initiated full reset" } })), e.close(1e3, "Ejected by GM");
        } catch {
        }
      }), ur.clearAll(), this.updateGMClients();
    }), Re.handle("remote:cache-media", async (e, r, o) => {
      try {
        if (!this.tempMediaDir) return !1;
        await Se.ensureDir(this.tempMediaDir);
        const a = Ee.join(this.tempMediaDir, o);
        return await Se.writeFile(a, Buffer.from(r)), console.log(`[Nexus Sync] Media cached: ${o} at ${a}`), !0;
      } catch (a) {
        return console.error(`[Nexus Sync] Failed to cache media ${o}:`, a), !1;
      }
    });
  }
  stop() {
    this.wss?.close(), this.server?.close();
  }
}
const MT = ow(import.meta.url), { WebSocketServer: x1 } = MT("ws");
aw.platform() === "win32" && dE.setDefaultResultOrder("ipv4first");
Zt.name = "gm-os-v5";
Ht.transports.file.level = "info";
Ht.transports.console.level = "debug";
Ht.initialize();
console.log("[Main] Logger initialized at:", Ht.transports.file.getFile().path);
const Ln = Ee.dirname(uE(import.meta.url)), kS = Ee.join(Ln, "..");
process.env.APP_ROOT = kS;
const kT = Ee.join(kS, "crash.log"), FS = (t, e) => {
  const r = (/* @__PURE__ */ new Date()).toISOString(), o = t instanceof Error ? t.stack : JSON.stringify(t, null, 2), a = t instanceof Error ? t.message : String(t), n = `[${r}] [${e}] ${a}
Stack: ${o}
${"=".repeat(50)}
`;
  try {
    Se.appendFileSync(kT, n), console.error(`[Main] ${e} logged to crash.log`);
  } catch (i) {
    console.error("[Main] Failed to write to crash.log:", i);
  }
};
process.on("uncaughtException", (t) => FS(t, "Uncaught Exception"));
process.on("unhandledRejection", (t) => FS(t, "Unhandled Rejection"));
_v();
Ev();
vv();
OT();
LT();
Zc.registerHandlers();
rw.registerSchemesAsPrivileged([
  { scheme: "gmos", privileges: { standard: !0, secure: !0, supportFetchAPI: !0, bypassCSP: !0, stream: !0 } }
]);
Zt.commandLine.appendSwitch("ignore-certificate-errors");
const er = process.env.VITE_DEV_SERVER_URL, I1 = Ln, cs = Ee.join(process.env.APP_ROOT, "dist");
process.env.VITE_PUBLIC = er ? Ee.join(process.env.APP_ROOT, "public") : cs;
let At, tw = null;
const jS = 3001, dc = Ee.join(Zt.getPath("userData"), "temp-media");
function $S() {
  At = new Oi({
    width: 1200,
    height: 800,
    backgroundColor: "#000000",
    icon: Ee.join(process.env.VITE_PUBLIC || "", "electron-vite.svg"),
    webPreferences: {
      preload: Ee.join(Ln, "preload.mjs"),
      sandbox: !1,
      // SECURITY NOTE: webSecurity is currently false to allow legacy file:// access.
      // Switch to TRUE once you've verified that all media loads via gmos:// protocol.
      webSecurity: !0
    }
  }), At.webContents.on("did-finish-load", () => {
    At?.webContents.send("main-process-message", (/* @__PURE__ */ new Date()).toLocaleString());
  }), At.webContents.openDevTools({ mode: "detach" }), er ? At.loadURL(er) : At.loadFile(Ee.join(cs, "index.html")), tw = new CT(At, jS, dc), tw.start();
}
const Di = Ee.join(process.env.APP_ROOT || "", "sessions");
Re.handle("save-session", async (t, e) => {
  await Se.ensureDir(Di);
  const { filePath: r } = await Vt.showSaveDialog({
    title: "Sauvegarder la Session GM-OS",
    defaultPath: Ee.join(Di, "gmos-session.json"),
    filters: [{ name: "GM-OS Session", extensions: ["json"] }]
  });
  return r ? (await Se.writeJson(r, e, { spaces: 2 }), !0) : !1;
});
Re.handle("load-session", async () => {
  await Se.ensureDir(Di);
  const { filePaths: t } = await Vt.showOpenDialog({
    title: "Charger une Session GM-OS",
    defaultPath: Di,
    filters: [{ name: "GM-OS Session", extensions: ["json"] }],
    properties: ["openFile"]
  });
  return t && t.length > 0 ? await Se.readJson(t[0]) : null;
});
Re.handle("npc:list-databases", async (t, e) => {
  const r = process.env.APP_ROOT || "", o = Ee.join(r, "databases", e);
  return await Se.pathExists(o) ? (await Se.readdir(o)).filter((n) => n.endsWith(".json")).map((n) => n.replace(".json", "")) : [];
});
Re.handle("npc:load-database", async (t, e, r) => {
  const o = process.env.APP_ROOT || "", a = Ee.join(o, "databases", e, `${r}.json`);
  return await Se.pathExists(a) ? await Se.readJson(a) : null;
});
Re.handle("tables:list-universes", async () => {
  const t = process.env.APP_ROOT || "", e = Ee.join(t, "databases", "tables");
  return await Se.pathExists(e) ? (await Se.readdir(e, { withFileTypes: !0 })).filter((o) => o.isDirectory()).map((o) => o.name) : [];
});
Re.handle("tables:list-tables", async (t, e) => {
  const r = process.env.APP_ROOT || "", o = Ee.join(r, "databases", "tables", e);
  return await Se.pathExists(o) ? (await Se.readdir(o)).filter((n) => n.endsWith(".json")).map((n) => n.replace(".json", "")) : [];
});
Re.handle("tables:load-table", async (t, e, r) => {
  const o = process.env.APP_ROOT || "", a = Ee.join(o, "databases", "tables", e, `${r}.json`);
  return await Se.pathExists(a) ? await Se.readJson(a) : null;
});
Re.handle("clock:list-calendars", async () => {
  const t = process.env.APP_ROOT || "", e = Ee.join(t, "databases", "calendars");
  return await Se.pathExists(e) ? (await Se.readdir(e)).filter((o) => o.endsWith(".json")).map((o) => o.replace(".json", "")) : [];
});
Re.handle("clock:load-calendar", async (t, e) => {
  const r = process.env.APP_ROOT || "", o = Ee.join(r, "databases", "calendars", `${e}.json`);
  return await Se.pathExists(o) ? await Se.readJson(o) : null;
});
Re.on("web:open-external", (t, e) => {
  lE.openExternal(e);
});
Re.handle("web:save-list", async (t, e) => {
  const { filePath: r } = await Vt.showSaveDialog({
    title: "Exporter les marque-pages",
    defaultPath: Ee.join(Zt.getPath("documents") || "", "web-os-bookmarks.json"),
    filters: [{ name: "JSON", extensions: ["json"] }]
  });
  return r ? (await Se.writeJson(r, e, { spaces: 2 }), !0) : !1;
});
Re.handle("web:load-list", async () => {
  const { filePaths: t } = await Vt.showOpenDialog({
    title: "Importer des marque-pages",
    filters: [{ name: "JSON", extensions: ["json"] }],
    properties: ["openFile"]
  });
  return t && t.length > 0 ? await Se.readJson(t[0]) : null;
});
Re.handle("sound:load-audios", async () => {
  const { filePaths: t } = await Vt.showOpenDialog({
    title: "Sélectionner des effets sonores",
    filters: [{ name: "Audio", extensions: ["mp3", "wav", "ogg"] }],
    properties: ["openFile", "multiSelections"]
  });
  return t;
});
Re.handle("tactical:list-sounds", async () => {
  try {
    const t = Ee.join(process.env.VITE_PUBLIC || "", "assets/sounds/tactical");
    if (await Se.pathExists(t))
      return (await Se.readdir(t)).filter((r) => r.match(/\.(mp3|wav|ogg|m4a)$/i));
  } catch (t) {
    console.error("[Main] Error listing tactical sounds:", t);
  }
  return [];
});
Re.on("log:message", (t, e, r, ...o) => {
  switch (e) {
    case "info":
      Ht.info("[Renderer]", r, ...o);
      break;
    case "warn":
      Ht.warn("[Renderer]", r, ...o);
      break;
    case "error":
      Ht.error("[Renderer]", r, ...o);
      break;
    case "debug":
      Ht.debug("[Renderer]", r, ...o);
      break;
    default:
      Ht.info("[Renderer]", r, ...o);
  }
});
Re.handle("light:request", async (t, e, r, o) => new Promise((a, n) => {
  try {
    const i = new URL(e), l = i.protocol === "https:" ? sw : hc, s = {
      method: r,
      rejectUnauthorized: !1,
      timeout: 5e3
      // 5 seconds timeout
    }, f = l.request(i, s, (d) => {
      let y = "";
      d.on("data", (R) => y += R), d.on("end", () => {
        try {
          const R = y ? JSON.parse(y) : null;
          a(R);
        } catch {
          a(y);
        }
      });
    });
    f.on("error", (d) => {
      console.error(`[Light OS] Node request error for ${e}:`, d.message), n(d);
    }), f.on("timeout", () => {
      f.destroy(), n(new Error("Request timed out"));
    }), o && f.write(JSON.stringify(o)), f.end();
  } catch (i) {
    console.error(`[Light OS] Request setup failed for ${e}:`, i), n(i);
  }
}));
const zt = /* @__PURE__ */ new Map(), WS = /* @__PURE__ */ new Map();
let Qe = null;
Re.handle("image:get-displays", () => Gt.getAllDisplays().map((e, r) => ({
  id: e.id.toString(),
  bounds: e.bounds,
  label: `Moniteur ${r + 1}`
})));
Re.on("image:sync-hub-data", (t, e, r) => {
  Qe && !Qe.isDestroyed() && Qe.webContents.send("image:sync-hub-data", e, r);
  for (const [, o] of zt)
    o.isDestroyed() || o.webContents.send("image:sync-hub-data", e, r);
  if (At && !At.isDestroyed()) {
    const o = {
      type: "hub-projection",
      payload: { type: e, data: r }
    };
    Re.emit("remote:broadcast-ui-action", null, o);
  }
});
Re.on("remote:broadcast-sync", (t, e) => {
  Qe && !Qe.isDestroyed() && Qe.webContents.send("remote:broadcast-sync", e);
  for (const [, r] of zt)
    r.isDestroyed() || r.webContents.send("remote:broadcast-sync", e);
});
Re.on("session:launch-hub-window", (t, e = "hub") => {
  if (console.log(`[Main] session:launch-hub-window received (mode: ${e})`), Qe && !Qe.isDestroyed()) {
    console.log("[Main] Hub window already exists, restoring and focusing..."), Qe.isMinimized() && Qe.restore(), Qe.show(), Qe.focus();
    return;
  }
  console.log(`[Main] Creating new ${e} window...`);
  const r = Gt.getAllDisplays(), o = r.length > 1 ? r[1] : r[0];
  Qe = new Oi({
    x: o.bounds.x + 50,
    y: o.bounds.y + 50,
    width: e === "tablet" ? 1024 : 1280,
    height: e === "tablet" ? 768 : 720,
    frame: !0,
    // Allow GM to move it around or fullscreen it manually
    webPreferences: {
      preload: Ee.join(Ln, "preload.mjs"),
      sandbox: !1,
      webSecurity: !0
    },
    backgroundColor: "#000000"
  }), er ? Qe.loadURL(`${er}?window=${e}`) : Qe.loadFile(Ee.join(cs, "index.html"), { query: { window: e } }), Qe.on("closed", () => {
    console.log(`[Main] ${e} window closed`), Qe = null;
  });
});
Re.on("image:launch-display", (t, e, r) => {
  if (console.log(`[Image OS] Launch Display -> Target: ${r}, Paths:`, e), WS.set(r, e), r === "hub") {
    Qe && !Qe.isDestroyed() && Qe.webContents.send("image:update-display", e);
    return;
  }
  const a = Gt.getAllDisplays().find((i) => i.id.toString() === r);
  if (!a) {
    console.error(`[Image OS] Target display ${r} not found.`);
    return;
  }
  if (e && e.length === 0) {
    const i = zt.get(r);
    i && !i.isDestroyed() && i.close(), zt.delete(r);
    return;
  }
  let n = zt.get(r);
  !n || n.isDestroyed() ? (n = new Oi({
    x: a.bounds.x,
    y: a.bounds.y,
    fullscreen: !0,
    // We want the projector to be fullscreen on that display
    frame: !1,
    webPreferences: {
      preload: Ee.join(Ln, "preload.mjs"),
      sandbox: !1,
      webSecurity: !0
    },
    backgroundColor: "#000000"
  }), zt.set(r, n), n.on("closed", () => {
    zt.delete(r);
  }), er ? n.loadURL(`${er}?window=projector&displayId=${r}`) : n.loadFile(Ee.join(cs, "index.html"), { query: { window: "projector", displayId: r } }), n.webContents.on("did-finish-load", () => {
    n?.webContents.send("image:update-display", e);
  })) : n.webContents.send("image:update-display", e);
});
Re.on("image:request-current-display", (t, e) => {
  const r = WS.get(e);
  r && r.length > 0 && t.sender.send("image:update-display", r);
});
Re.on("image:close-all-displays", () => {
  console.log("[Image OS] Close All Displays");
  for (const [, t] of zt)
    t.isDestroyed() || t.close();
  zt.clear();
});
function FT() {
  const t = aw.networkInterfaces();
  for (const e of Object.keys(t)) {
    const r = t[e];
    if (r) {
      for (const o of r)
        if ((o.family === "IPv4" || o.family === 4) && !o.internal)
          return o.address;
    }
  }
  return "localhost";
}
Re.handle("remote:get-connection-info", () => {
  const t = er ? new URL(er).port : null;
  return {
    ip: FT(),
    port: t ? parseInt(t, 10) : jS
  };
});
Re.on("remote:request-client-sync", (t) => {
  t.reply("remote:sync-clients", ur.getAllClients());
});
Re.on("remote:clear-disconnected", (t) => {
  console.log("[Remote] MJ requested clearing of non-active clients"), ur.clearDisconnected(), t.reply("remote:sync-clients", ur.getAllClients());
});
Re.handle("ai:proxy-request", async (t, e, r, o, a) => new Promise((n, i) => {
  try {
    const l = new URL(e), s = l.protocol === "https:" ? sw : hc, f = {
      method: r,
      headers: o,
      rejectUnauthorized: !1,
      timeout: 3e5
      // 300 seconds (5 minutes) for heavy AI analysis (PDFs, etc.)
    }, d = s.request(l, f, (y) => {
      let R = "";
      y.on("data", (w) => R += w), y.on("end", () => {
        try {
          const w = R ? JSON.parse(R) : null;
          n({
            ok: y.statusCode ? y.statusCode >= 200 && y.statusCode < 300 : !1,
            status: y.statusCode,
            statusText: y.statusMessage,
            data: w
          });
        } catch {
          n({
            ok: y.statusCode ? y.statusCode >= 200 && y.statusCode < 300 : !1,
            status: y.statusCode,
            statusText: y.statusMessage,
            data: R
          });
        }
      });
    });
    d.on("error", (y) => {
      console.error(`[AI Main] Proxy request failed for ${e}:`, y.message), i(y);
    }), d.on("timeout", () => {
      d.destroy(), i(new Error("AI Request timed out"));
    }), a && d.write(typeof a == "string" ? a : JSON.stringify(a)), d.end();
  } catch (l) {
    console.error(`[AI Main] Proxy setup failed for ${e}:`, l), i(l);
  }
}));
Re.handle("npc:select-avatar", async () => {
  const { filePaths: t } = await Vt.showOpenDialog({
    title: "Sélectionner un Avatar",
    filters: [{ name: "Images", extensions: ["jpg", "png", "gif", "webp", "jpeg"] }],
    properties: ["openFile"]
  });
  return t && t.length > 0 && typeof t[0] == "string" ? t[0].replace(/\\/g, "/") : null;
});
Re.handle("npc:save-avatar", async (t, e, r) => {
  try {
    const o = Ee.join(process.env.APP_ROOT || "", "public", "assets", "avatars", "npc");
    await Se.ensureDir(o);
    const a = Ee.join(o, r);
    if (!a || typeof a != "string") throw new Error("Chemin d'avatar invalide.");
    return await Se.writeFile(a, e), `gmos://media/${a.replace(/\\/g, "/")}`;
  } catch (o) {
    return console.error("[Main] Error saving avatar:", o), null;
  }
});
Zt.on("window-all-closed", () => {
  process.platform !== "darwin" && (Zt.quit(), At = null);
});
Zt.on("activate", () => {
  Oi.getAllWindows().length === 0 && $S();
});
Zt.whenReady().then(async () => {
  try {
    await Se.pathExists(dc) && (await Se.emptyDir(dc), console.log("[Main] Temp media directory cleared"));
  } catch (t) {
    console.warn("[Main] Could not clear temp media directory:", t);
  }
  rw.handle("gmos", (t) => {
    if (console.log(`[Protocol:GMOS] Request received: ${t.url}`), !t || !t.url)
      return console.error("[Protocol:GMOS] Missing request URL"), new Response("URL manquante", { status: 400 });
    try {
      const e = String(t.url);
      console.log(`[Protocol:GMOS] Raw URL string: "${e}"`);
      const r = e.replace(/^gmos:\/\/media\//, "");
      console.log(`[Protocol:GMOS] Decoded path part: "${r}"`);
      const o = decodeURIComponent(r), a = process.env.APP_ROOT || "", n = Ee.isAbsolute(o) ? o : Ee.join(a, o);
      return console.log(`[Protocol:GMOS] Final absolute path: "${n}"`), mr.fetch(`file:///${n}`);
    } catch (e) {
      console.error("[Main] protocol:gmos error:", e);
      const r = e instanceof Error ? e.message : String(e);
      return new Response(`Erreur interne du protocole gmos: ${r}`, { status: 500 });
    }
  }), $S(), startRemoteServer(), Gt.on("display-added", () => {
    console.log("[Main] Display added"), At?.webContents.send("app:display-changed", Gt.getAllDisplays().length);
  }), Gt.on("display-removed", () => {
    console.log("[Main] Display removed"), At?.webContents.send("app:display-changed", Gt.getAllDisplays().length);
  }), Gt.on("display-metrics-changed", () => {
    console.log("[Main] Display metrics changed"), At?.webContents.send("app:display-changed", Gt.getAllDisplays().length);
  });
});
export {
  I1 as MAIN_DIST,
  cs as RENDERER_DIST,
  er as VITE_DEV_SERVER_URL
};
